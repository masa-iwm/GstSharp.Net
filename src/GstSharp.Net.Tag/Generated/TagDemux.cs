// <auto-generated/>
// Generated by GstSharp.Generator from GstTag-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Tag;

/// <summary>
/// Provides a base class for demuxing tags at the beginning or end of a
/// stream and handles things like typefinding, querying, seeking, and
/// different modes of operation (chain-based, pull_range-based, and providing
/// downstream elements with random access if upstream supports that). The tag
/// is stripped from the output, and all offsets are adjusted for the tag
/// sizes, so that to the downstream element the stream will appear as if
/// there was no tag at all. Also, once the tag has been parsed, GstTagDemux
/// will try to determine the media type of the resulting stream and add a
/// source pad with the appropriate caps in order to facilitate auto-plugging.
/// </summary>
/// <remarks>
/// <para>## Deriving from GstTagDemux</para>
/// <para>Subclasses have to do four things:</para>
/// <para>
///  * In their base init function, they must add a pad template for the sink
///    pad to the element class, describing the media type they can parse in
///    the caps of the pad template.
///  * In their class init function, they must override
///    GST_TAG_DEMUX_CLASS(demux_klass)-&gt;identify_tag with their own identify
///    function.
///  * In their class init function, they must override
///  GST_TAG_DEMUX_CLASS(demux_klass)-&gt;parse_tag with their own parse
///  function.
///  * In their class init function, they must also set
///    GST_TAG_DEMUX_CLASS(demux_klass)-&gt;min_start_size and/or
///  GST_TAG_DEMUX_CLASS(demux_klass)-&gt;min_end_size to the minimum size required
///  for the identify function to decide whether the stream has a supported tag
///  or not. A class parsing ID3v1 tags, for example, would set min_end_size to
///  128 bytes.
/// </para>
/// </remarks>
public abstract unsafe partial class TagDemux : Gst.Element
{
    /// <summary>Wraps a native <c>GstTagDemux</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstTagDemux</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected TagDemux(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstTagDemux</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstTag", EntryPoint = "gst_tag_demux_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstTagDemux</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : TagDemux
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
