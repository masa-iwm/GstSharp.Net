// <auto-generated/>
// Generated by GstSharp.Generator from GstNet-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Net;

/// <summary>
/// GstPtpClock implements a PTP (IEEE1588:2008) ordinary clock in slave-only
/// mode, that allows a GStreamer pipeline to synchronize to a PTP network
/// clock in some specific domain.
/// </summary>
/// <remarks>
/// <para>
/// The PTP subsystem can be initialized with gst_ptp_init(), which then starts
/// a helper process to do the actual communication via the PTP ports. This is
/// required as PTP listens on ports &lt; 1024 and thus requires special
/// privileges. Once this helper process is started, the main process will
/// synchronize to all PTP domains that are detected on the selected
/// interfaces.
/// </para>
/// <para>
/// gst_ptp_clock_new() then allows to create a GstClock that provides the PTP
/// time from a master clock inside a specific PTP domain. This clock will only
/// return valid timestamps once the timestamps in the PTP domain are known. To
/// check this, you can use gst_clock_wait_for_sync(), the GstClock::synced
/// signal and gst_clock_is_synced().
/// </para>
/// <para>
/// To gather statistics about the PTP clock synchronization,
/// gst_ptp_statistics_callback_add() can be used. This gives the application
/// the possibility to collect all kinds of statistics from the clock
/// synchronization.
/// </para>
/// </remarks>
public unsafe partial class PtpClock : Gst.SystemClock
{
    /// <summary>Wraps a native <c>GstPtpClock</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstPtpClock</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected PtpClock(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Creates a new PTP clock instance that exports the PTP time of the master
    /// clock in @domain. This clock can be slaved to other clocks as needed.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If gst_ptp_init() was not called before, this will call gst_ptp_init() with
    /// default parameters.
    /// </para>
    /// <para>
    /// This clock only returns valid timestamps after it received the first
    /// times from the PTP master clock on the network. Once this happens the
    /// GstPtpClock::internal-clock property will become non-NULL. You can
    /// check this with gst_clock_wait_for_sync(), the GstClock::synced signal and
    /// gst_clock_is_synced().
    /// </para>
    /// </remarks>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <param name="domain">The <c>domain</c> argument.</param>
    /// <returns>A new #GstClock</returns>
    public static Gst.Net.PtpClock? New(string? name, uint domain)
    {
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstPtpClockNew(nameScope.Pointer, domain);
        return Gst.GObject.Object.FromNative<Gst.Net.PtpClock>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>domain</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>The property is construct-only and therefore read-only here.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public uint Domain
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("domain");
            return holder.GetUInt();
        }
    }

    /// <summary>The <c>grandmaster-clock-id</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public ulong GrandmasterClockId
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("grandmaster-clock-id");
            return holder.GetUInt64();
        }
    }

    /// <summary>The <c>internal-clock</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>
    /// Reading hands back the interned wrapper of the object, which the binding
    /// keeps; it is not the reader's to dispose.
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public Gst.Clock? InternalClock
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("internal-clock");
            return (Gst.Clock?)holder.GetObject();
        }
    }

    /// <summary>The <c>master-clock-id</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public ulong MasterClockId
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("master-clock-id");
            return holder.GetUInt64();
        }
    }

    /// <summary>The <c>gst_ptp_clock_new</c> entry point.</summary>
    [LibraryImport("GstNet", EntryPoint = "gst_ptp_clock_new")]
    private static partial nint GstPtpClockNew(byte* name, uint domain);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstPtpClock</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstNet", EntryPoint = "gst_ptp_clock_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new PtpClock(handle, transfer);
}
