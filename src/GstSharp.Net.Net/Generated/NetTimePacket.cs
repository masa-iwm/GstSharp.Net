// <auto-generated/>
// Generated by GstSharp.Generator from GstNet-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Net;

/// <summary>
/// Various functions for receiving, sending an serializing #GstNetTimePacket
/// structures.
/// </summary>
public sealed unsafe partial class NetTimePacket : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstNetTimePacket</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal NetTimePacket(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a <c>GstNetTimePacket</c> that GStreamer keeps owning, for the length of one call.</summary>
    /// <param name="handle">The value that is lent to managed code.</param>
    /// <returns>The wrapper, which holds nothing of its own.</returns>
    /// <remarks>
    /// This is how the override of a virtual method receives a <c>transfer none</c>
    /// boxed value. No <c>g_boxed_copy</c> is taken, so every write the override
    /// makes lands in the instance the caller of the slot reads back, and disposing
    /// the wrapper only detaches it: a wrapper kept past the call throws instead of
    /// freeing a value it never owned. See <see cref="Gst.Interop.Borrowed"/>.
    /// </remarks>
    internal static NetTimePacket Borrow(nint handle) => new(new Gst.Interop.Borrowed(handle));

    /// <summary>Wraps a <c>GstNetTimePacket</c> the caller keeps owning.</summary>
    /// <param name="borrowed">The value that is lent to managed code.</param>
    private NetTimePacket(Gst.Interop.Borrowed borrowed)
        : base(borrowed, new Gst.GObject.GType(GetGType()))
    {
    }

    /// <summary>the local time when this packet was sent</summary>
    public Gst.ClockTime LocalTime
    {
        get
        {
            Gst.ClockTime value = new(((NetTimePacketRaw*)Handle)->LocalTime);
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>the remote time observation</summary>
    public Gst.ClockTime RemoteTime
    {
        get
        {
            Gst.ClockTime value = new(((NetTimePacketRaw*)Handle)->RemoteTime);
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>Wraps a native <c>GstNetTimePacket</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static NetTimePacket? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>
    /// Creates a new #GstNetTimePacket from a buffer received over the network. The
    /// caller is responsible for ensuring that @buffer is at least
    /// #GST_NET_TIME_PACKET_SIZE bytes long.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @buffer is %NULL, the local and remote times will be set to
    /// #GST_CLOCK_TIME_NONE.
    /// </para>
    /// <para>MT safe. Caller owns return value (gst_net_time_packet_free to free).</para>
    /// </remarks>
    /// <param name="buffer">
    /// a buffer from which to construct the packet, or NULL
    /// The C declaration sizes this buffer at 16 elements; pass exactly 16, or an empty span for <c>NULL</c>.
    /// </param>
    /// <returns>The new #GstNetTimePacket.</returns>
    /// <exception cref="ArgumentException">
    /// <paramref name="buffer"/> does not have exactly 16 elements and is not empty.
    /// </exception>
    public static Gst.Net.NetTimePacket New(System.ReadOnlySpan<byte> buffer)
    {
        if (buffer.Length != 16 && buffer.Length != 0)
        {
            throw new ArgumentException(
                "buffer must have exactly 16 elements, or none at all.",
                nameof(buffer));
        }
        fixed (byte* bufferPointer = buffer)
        {
            nint nativeResult = GstNetTimePacketNew(bufferPointer);
            return Gst.Net.NetTimePacket.FromNative(nativeResult, Gst.Interop.Transfer.Full)
                ?? throw new InvalidOperationException("gst_net_time_packet_new returned no value.");
        }
    }

    /// <summary>Make a copy of @packet.</summary>
    /// <returns>a copy of @packet, free with gst_net_time_packet_free().</returns>
    public Gst.Net.NetTimePacket Copy()
    {
        nint nativeResult = GstNetTimePacketCopy(Handle);
        System.GC.KeepAlive(this);
        return Gst.Net.NetTimePacket.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_net_time_packet_copy returned no value.");
    }

    /// <summary>Sends a #GstNetTimePacket over a socket.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="socket">The <c>socket</c> argument.</param>
    /// <param name="destAddress">The <c>destAddress</c> argument.</param>
    /// <returns>TRUE if successful, FALSE in case an error occurred.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool Send(Gst.Gio.Socket socket, Gst.Gio.SocketAddress destAddress)
    {
        ArgumentNullException.ThrowIfNull(socket);
        ArgumentNullException.ThrowIfNull(destAddress);
        nint errorNative = 0;
        int nativeResult = GstNetTimePacketSend(Handle, socket.Handle, destAddress.Handle, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(socket);
        System.GC.KeepAlive(destAddress);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Serialized a #GstNetTimePacket into a newly-allocated sequence of
    /// #GST_NET_TIME_PACKET_SIZE bytes, in network byte order. The value returned is
    /// suitable for passing to write(2) or sendto(2) for communication over the
    /// network.
    /// </summary>
    /// <remarks>
    /// <para>MT safe. Caller owns return value (g_free to free).</para>
    /// </remarks>
    /// <returns>A newly allocated sequence of #GST_NET_TIME_PACKET_SIZE bytes.</returns>
    public byte[]? Serialize()
    {
        nint nativeResult = GstNetTimePacketSerialize(Handle);
        System.GC.KeepAlive(this);
        byte[]? result = null;
        if (nativeResult != 0)
        {
            result = new byte[16];
            new System.ReadOnlySpan<byte>((void*)nativeResult, 16).CopyTo(result);
            Gst.Interop.GMarshal.Free(nativeResult);
        }
        return result;
    }

    /// <summary>
    /// Receives a #GstNetTimePacket over a socket. Handles interrupted system
    /// calls, but otherwise returns NULL on error.
    /// </summary>
    /// <param name="socket">The <c>socket</c> argument.</param>
    /// <param name="srcAddress">The <c>srcAddress</c> argument.</param>
    /// <returns>
    /// a new #GstNetTimePacket, or NULL on error. Free
    ///    with gst_net_time_packet_free() when done.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public static Gst.Net.NetTimePacket Receive(Gst.Gio.Socket socket, out Gst.Gio.SocketAddress? srcAddress)
    {
        ArgumentNullException.ThrowIfNull(socket);
        nint srcAddressNative = default;
        nint errorNative = 0;
        nint nativeResult = GstNetTimePacketReceive(socket.Handle, &srcAddressNative, &errorNative);
        System.GC.KeepAlive(socket);
        if (errorNative != 0 && nativeResult != 0)
        {
            // The call failed and transferred a value all the same. The throw
            // below puts it out of reach, so it is released rather than leaked.
            Gst.Net.NetTimePacket.FromNative(nativeResult, Gst.Interop.Transfer.Full)?.Dispose();
        }
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        srcAddress = Gst.GObject.Object.FromNative<Gst.Gio.SocketAddress>(srcAddressNative, Gst.Interop.Transfer.Full);
        return Gst.Net.NetTimePacket.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_net_time_packet_receive returned no value.");
    }

    /// <summary>The <c>gst_net_time_packet_new</c> entry point.</summary>
    [LibraryImport("GstNet", EntryPoint = "gst_net_time_packet_new")]
    private static partial nint GstNetTimePacketNew(byte* buffer);

    /// <summary>The <c>gst_net_time_packet_copy</c> entry point.</summary>
    [LibraryImport("GstNet", EntryPoint = "gst_net_time_packet_copy")]
    private static partial nint GstNetTimePacketCopy(nint packet);

    /// <summary>The <c>gst_net_time_packet_send</c> entry point.</summary>
    [LibraryImport("GstNet", EntryPoint = "gst_net_time_packet_send")]
    private static partial int GstNetTimePacketSend(nint packet, nint socket, nint destAddress, nint* error);

    /// <summary>The <c>gst_net_time_packet_serialize</c> entry point.</summary>
    [LibraryImport("GstNet", EntryPoint = "gst_net_time_packet_serialize")]
    private static partial nint GstNetTimePacketSerialize(nint packet);

    /// <summary>The <c>gst_net_time_packet_receive</c> entry point.</summary>
    [LibraryImport("GstNet", EntryPoint = "gst_net_time_packet_receive")]
    private static partial nint GstNetTimePacketReceive(nint socket, nint* srcAddress, nint* error);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstNetTimePacket</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstNet", EntryPoint = "gst_net_time_packet_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new NetTimePacket(handle, transfer);
}

/// <summary>The native layout of <c>GstNetTimePacket</c>.</summary>
/// <remarks>
/// <para>
/// The mirror is only ever read through a pointer into memory that GStreamer
/// owns; it is never allocated, assigned or copied.
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
internal unsafe struct NetTimePacketRaw
{
    /// <summary>The <c>local_time</c> field.</summary>
    internal ulong LocalTime;

    /// <summary>The <c>remote_time</c> field.</summary>
    internal ulong RemoteTime;
}
