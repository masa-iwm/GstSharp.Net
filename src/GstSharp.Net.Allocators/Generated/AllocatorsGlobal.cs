// <auto-generated/>
// Generated by GstSharp.Generator from GstAllocators-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Allocators;

/// <summary>The functions of the <c>GstAllocators</c> namespace that belong to no type.</summary>
public static unsafe partial class AllocatorsGlobal
{
    /// <summary>Return the file descriptor associated with @mem.</summary>
    /// <param name="mem">the memory to get the file descriptor</param>
    /// <returns>
    /// the file descriptor associated with the memory, or -1.  The file
    ///     descriptor is still owned by the GstMemory.  Use dup to take a copy
    ///     if you intend to use it beyond the lifetime of this GstMemory.
    /// </returns>
    public static int DmabufMemoryGetFd(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        int nativeResult = GstDmabufMemoryGetFd(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult;
    }

    /// <summary>
    /// Exports a DMABuf from the DRM Bumb buffer object. One can check if this
    /// feature is supported using gst_drm_dumb_allocator_has_prime_export();
    /// </summary>
    /// <param name="mem">the memory to export from</param>
    /// <returns>
    /// a #GstMemory from #GstDmaBufAllocator wrapping the exported dma-buf
    ///    file descriptor.
    /// </returns>
    public static Gst.Memory? DrmDumbMemoryExportDmabuf(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        nint nativeResult = GstDrmDumbMemoryExportDmabuf(mem.Handle);
        System.GC.KeepAlive(mem);
        return Gst.Memory.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Return the DRM buffer object handle associated with @mem.</summary>
    /// <param name="mem">the memory to get the handle from</param>
    /// <returns>
    /// the DRM buffer object handle associated with the memory, or 0.
    ///     The handle is still owned by the GstMemory and cannot be used
    ///     beyond the lifetime of this GstMemory unless it is being passed
    ///     to DRM driver, which does handle a refcount internally.
    /// </returns>
    public static uint DrmDumbMemoryGetHandle(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        uint nativeResult = GstDrmDumbMemoryGetHandle(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult;
    }

    /// <summary>
    /// Get the fd from @mem. Call gst_is_fd_memory() to check if @mem has
    /// an fd.
    /// </summary>
    /// <param name="mem">#GstMemory</param>
    /// <returns>the fd of @mem or -1 when there is no fd on @mem</returns>
    public static int FdMemoryGetFd(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        int nativeResult = GstFdMemoryGetFd(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult;
    }

    /// <summary>Check if @mem is dmabuf memory.</summary>
    /// <param name="mem">the memory to be check</param>
    /// <returns>%TRUE if @mem is dmabuf memory, otherwise %FALSE</returns>
    public static bool IsDmabufMemory(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        int nativeResult = GstIsDmabufMemory(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_is_drm_dumb_memory</c> function.</summary>
    /// <param name="mem">the memory to be checked</param>
    /// <returns>%TRUE if @mem is DRM Dumb memory, otherwise %FALSE</returns>
    public static bool IsDrmDumbMemory(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        int nativeResult = GstIsDrmDumbMemory(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult != 0;
    }

    /// <summary>Check if @mem is memory backed by an fd</summary>
    /// <param name="mem">#GstMemory</param>
    /// <returns>
    /// %TRUE when @mem has an fd that can be retrieved with
    /// gst_fd_memory_get_fd().
    /// </returns>
    public static bool IsFdMemory(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        int nativeResult = GstIsFdMemory(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_is_phys_memory</c> function.</summary>
    /// <param name="mem">a #GstMemory</param>
    /// <returns>whether the memory at @mem is backed by physical memory</returns>
    public static bool IsPhysMemory(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        int nativeResult = GstIsPhysMemory(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_phys_memory_get_phys_addr</c> function.</summary>
    /// <param name="mem">a #GstMemory</param>
    /// <returns>Physical memory address that is backing @mem, or 0 if none</returns>
    public static nuint PhysMemoryGetPhysAddr(Gst.Memory mem)
    {
        ArgumentNullException.ThrowIfNull(mem);
        nuint nativeResult = GstPhysMemoryGetPhysAddr(mem.Handle);
        System.GC.KeepAlive(mem);
        return nativeResult;
    }

    /// <summary>The <c>gst_dmabuf_memory_get_fd</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_dmabuf_memory_get_fd")]
    private static partial int GstDmabufMemoryGetFd(nint mem);

    /// <summary>The <c>gst_drm_dumb_memory_export_dmabuf</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_drm_dumb_memory_export_dmabuf")]
    private static partial nint GstDrmDumbMemoryExportDmabuf(nint mem);

    /// <summary>The <c>gst_drm_dumb_memory_get_handle</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_drm_dumb_memory_get_handle")]
    private static partial uint GstDrmDumbMemoryGetHandle(nint mem);

    /// <summary>The <c>gst_fd_memory_get_fd</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_fd_memory_get_fd")]
    private static partial int GstFdMemoryGetFd(nint mem);

    /// <summary>The <c>gst_is_dmabuf_memory</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_is_dmabuf_memory")]
    private static partial int GstIsDmabufMemory(nint mem);

    /// <summary>The <c>gst_is_drm_dumb_memory</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_is_drm_dumb_memory")]
    private static partial int GstIsDrmDumbMemory(nint mem);

    /// <summary>The <c>gst_is_fd_memory</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_is_fd_memory")]
    private static partial int GstIsFdMemory(nint mem);

    /// <summary>The <c>gst_is_phys_memory</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_is_phys_memory")]
    private static partial int GstIsPhysMemory(nint mem);

    /// <summary>The <c>gst_phys_memory_get_phys_addr</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_phys_memory_get_phys_addr")]
    private static partial nuint GstPhysMemoryGetPhysAddr(nint mem);
}
