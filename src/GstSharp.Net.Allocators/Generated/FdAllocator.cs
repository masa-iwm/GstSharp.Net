// <auto-generated/>
// Generated by GstSharp.Generator from GstAllocators-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Allocators;

/// <summary>Base class for allocators with fd-backed memory</summary>
public unsafe partial class FdAllocator : Gst.Allocator
{
    /// <summary>Wraps a native <c>GstFdAllocator</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstFdAllocator</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected FdAllocator(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Return a new fd allocator.</summary>
    /// <returns>
    /// a new fd allocator. Use gst_object_unref() to
    /// release the allocator after usage
    /// </returns>
    public static Gst.Allocator New()
    {
        nint nativeResult = GstFdAllocatorNew();
        return Gst.GObject.Object.FromNative<Gst.Allocator>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_fd_allocator_new returned no value.");
    }

    /// <summary>Return a %GstMemory that wraps a generic file descriptor.</summary>
    /// <param name="allocator">The <c>allocator</c> argument.</param>
    /// <param name="fd">The <c>fd</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="flags">The <c>flags</c> argument.</param>
    /// <returns>
    /// a GstMemory based on @allocator.
    /// When the buffer will be released the allocator will close the @fd unless
    /// the %GST_FD_MEMORY_FLAG_DONT_CLOSE flag is specified.
    /// The memory is only mmapped on gst_buffer_map() request.
    /// </returns>
    public static Gst.Memory? Alloc(Gst.Allocator allocator, int fd, nuint size, Gst.Allocators.FdMemoryFlags flags)
    {
        ArgumentNullException.ThrowIfNull(allocator);
        nint nativeResult = GstFdAllocatorAlloc(allocator.Handle, fd, size, (int)flags);
        System.GC.KeepAlive(allocator);
        return Gst.Memory.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Return a %GstMemory that wraps a generic file descriptor.</summary>
    /// <remarks>
    /// <para>Available since GStreamer 1.28.</para>
    /// </remarks>
    /// <param name="allocator">The <c>allocator</c> argument.</param>
    /// <param name="fd">The <c>fd</c> argument.</param>
    /// <param name="maxsize">The <c>maxsize</c> argument.</param>
    /// <param name="offset">The <c>offset</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="flags">The <c>flags</c> argument.</param>
    /// <returns>
    /// a GstMemory based on @allocator.
    /// When the buffer will be released the allocator will close the @fd unless
    /// the %GST_FD_MEMORY_FLAG_DONT_CLOSE flag is specified.
    /// The memory is only mmapped on gst_buffer_map() request.
    /// </returns>
    public static Gst.Memory? AllocFull(Gst.Allocator allocator, int fd, nuint maxsize, nuint offset, nuint size, Gst.Allocators.FdMemoryFlags flags)
    {
        ArgumentNullException.ThrowIfNull(allocator);
        nint nativeResult = GstFdAllocatorAllocFull(allocator.Handle, fd, maxsize, offset, size, (int)flags);
        System.GC.KeepAlive(allocator);
        return Gst.Memory.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_fd_allocator_new</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_fd_allocator_new")]
    private static partial nint GstFdAllocatorNew();

    /// <summary>The <c>gst_fd_allocator_alloc</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_fd_allocator_alloc")]
    private static partial nint GstFdAllocatorAlloc(nint allocator, int fd, nuint size, int flags);

    /// <summary>The <c>gst_fd_allocator_alloc_full</c> entry point.</summary>
    [LibraryImport("GstAllocators", EntryPoint = "gst_fd_allocator_alloc_full")]
    private static partial nint GstFdAllocatorAllocFull(nint allocator, int fd, nuint maxsize, nuint offset, nuint size, int flags);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstFdAllocator</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAllocators", EntryPoint = "gst_fd_allocator_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new FdAllocator(handle, transfer);
}
