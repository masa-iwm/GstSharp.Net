// <auto-generated/>
// Generated by GstSharp.Generator from GstPbutils-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Pbutils;

/// <summary>
/// The #GstDiscoverer is a utility object which allows to get as much
/// information as possible from one or many URIs.
/// </summary>
/// <remarks>
/// <para>It provides two APIs, allowing usage in blocking or non-blocking mode.</para>
/// <para>
/// The blocking mode just requires calling gst_discoverer_discover_uri()
/// with the URI one wishes to discover.
/// </para>
/// <para>
/// The non-blocking mode requires a running #GMainLoop iterating a
/// #GMainContext, where one connects to the various signals, appends the
/// URIs to be processed (through gst_discoverer_discover_uri_async()) and then
/// asks for the discovery to begin (through gst_discoverer_start()).
/// By default this will use the GLib default main context unless you have
/// set a custom context using g_main_context_push_thread_default().
/// </para>
/// <para>All the information is returned in a #GstDiscovererInfo structure.</para>
/// </remarks>
public unsafe partial class Discoverer : Gst.GObject.Object
{
    /// <summary>Wraps a native <c>GstDiscoverer</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstDiscoverer</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Discoverer(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new #GstDiscoverer with the provided timeout.</summary>
    /// <param name="timeout">The <c>timeout</c> argument.</param>
    /// <returns>
    /// The new #GstDiscoverer.
    /// If an error occurred when creating the discoverer, @err will be set
    /// accordingly and %NULL will be returned. If @err is set, the caller must
    /// free it when no longer needed using g_error_free().
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public static Gst.Pbutils.Discoverer New(Gst.ClockTime timeout)
    {
        nint errorNative = 0;
        nint nativeResult = GstDiscovererNew(timeout.Nanoseconds, &errorNative);
        if (errorNative != 0 && nativeResult != 0)
        {
            // The call failed and transferred a value all the same. The throw
            // below puts it out of reach, so it is released rather than leaked.
            Gst.Interop.GObjectNative.ObjectUnref(nativeResult);
        }
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return Gst.GObject.Object.FromNative<Gst.Pbutils.Discoverer>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_discoverer_new returned no value.");
    }

    /// <summary>Synchronously discovers the given @uri.</summary>
    /// <remarks>
    /// <para>
    /// A copy of @uri will be made internally, so the caller can safely g_free()
    /// afterwards.
    /// </para>
    /// </remarks>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>
    /// the result of the scanning. Can be %NULL if an
    /// error occurred.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public Gst.Pbutils.DiscovererInfo DiscoverUri(string uri)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        nint nativeResult = GstDiscovererDiscoverUri(Handle, uriScope.Pointer, &errorNative);
        System.GC.KeepAlive(this);
        if (errorNative != 0 && nativeResult != 0)
        {
            // The call failed and transferred a value all the same. The throw
            // below puts it out of reach, so it is released rather than leaked.
            Gst.Interop.GObjectNative.ObjectUnref(nativeResult);
        }
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return Gst.GObject.Object.FromNative<Gst.Pbutils.DiscovererInfo>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_discoverer_discover_uri returned no value.");
    }

    /// <summary>
    /// Appends the given @uri to the list of URIs to discoverer. The actual
    /// discovery of the @uri will only take place if gst_discoverer_start() has
    /// been called.
    /// </summary>
    /// <remarks>
    /// <para>
    /// A copy of @uri will be made internally, so the caller can safely g_free()
    /// afterwards.
    /// </para>
    /// </remarks>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>
    /// %TRUE if the @uri was successfully appended to the list of pending
    /// uris, else %FALSE
    /// </returns>
    public bool DiscoverUriAsync(string uri)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        int nativeResult = GstDiscovererDiscoverUriAsync(Handle, uriScope.Pointer);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Allow asynchronous discovering of URIs to take place.
    /// A #GMainLoop must be available for #GstDiscoverer to properly work in
    /// asynchronous mode.
    /// </summary>
    public void Start()
    {
        GstDiscovererStart(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Stop the discovery of any pending URIs and clears the list of
    /// pending URIS (if any).
    /// </summary>
    public void Stop()
    {
        GstDiscovererStop(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Will be emitted in async mode when all pending URIs have been processed.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler Finished
    {
        add => Gst.Pbutils.SignalConnections.Add(this, "finished", (nint)(delegate* unmanaged[Cdecl]<nint, nint, void>)&FinishedTrampoline, value);
        remove => Gst.Pbutils.SignalConnections.Remove(this, "finished", value);
    }

    /// <summary>The native handler of the <c>finished</c> signal of <c>GstDiscoverer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void FinishedTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler>(userData) is not { } handler)
            {
                return;
            }

            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>source-setup</c> signal of <c>GstDiscoverer</c>.</summary>
    public sealed class SourceSetupSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="SourceSetupSignalArgs"/> class.</summary>
        /// <param name="source">source element</param>
        internal SourceSetupSignalArgs(Gst.Element source)
        {
            Source = source;
        }

        /// <summary>source element</summary>
        public Gst.Element Source { get; }
    }

    /// <summary>
    /// This signal is emitted after the source element has been created for, so
    /// the URI being discovered, so it can be configured by setting additional
    /// properties (e.g. set a proxy server for an http source, or set the device
    /// and read speed for an audio cd source).
    /// </summary>
    /// <remarks>
    /// <para>
    /// This signal is usually emitted from the context of a GStreamer streaming
    /// thread.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Pbutils.Discoverer.SourceSetupSignalArgs> SourceSetup
    {
        add => Gst.Pbutils.SignalConnections.Add(this, "source-setup", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&SourceSetupTrampoline, value);
        remove => Gst.Pbutils.SignalConnections.Remove(this, "source-setup", value);
    }

    /// <summary>The native handler of the <c>source-setup</c> signal of <c>GstDiscoverer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void SourceSetupTrampoline(nint instance, nint source, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Pbutils.Discoverer.SourceSetupSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Element sourceValue = Gst.GObject.Object.FromNative<Gst.Element>(source, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The source-setup signal of GstDiscoverer passed no source.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Pbutils.Discoverer.SourceSetupSignalArgs(sourceValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>Will be emitted when the discover starts analyzing the pending URIs</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler Starting
    {
        add => Gst.Pbutils.SignalConnections.Add(this, "starting", (nint)(delegate* unmanaged[Cdecl]<nint, nint, void>)&StartingTrampoline, value);
        remove => Gst.Pbutils.SignalConnections.Remove(this, "starting", value);
    }

    /// <summary>The native handler of the <c>starting</c> signal of <c>GstDiscoverer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void StartingTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler>(userData) is not { } handler)
            {
                return;
            }

            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_discoverer_new</c> entry point.</summary>
    [LibraryImport("GstPbutils", EntryPoint = "gst_discoverer_new")]
    private static partial nint GstDiscovererNew(ulong timeout, nint* error);

    /// <summary>The <c>gst_discoverer_discover_uri</c> entry point.</summary>
    [LibraryImport("GstPbutils", EntryPoint = "gst_discoverer_discover_uri")]
    private static partial nint GstDiscovererDiscoverUri(nint discoverer, byte* uri, nint* error);

    /// <summary>The <c>gst_discoverer_discover_uri_async</c> entry point.</summary>
    [LibraryImport("GstPbutils", EntryPoint = "gst_discoverer_discover_uri_async")]
    private static partial int GstDiscovererDiscoverUriAsync(nint discoverer, byte* uri);

    /// <summary>The <c>gst_discoverer_start</c> entry point.</summary>
    [LibraryImport("GstPbutils", EntryPoint = "gst_discoverer_start")]
    private static partial void GstDiscovererStart(nint discoverer);

    /// <summary>The <c>gst_discoverer_stop</c> entry point.</summary>
    [LibraryImport("GstPbutils", EntryPoint = "gst_discoverer_stop")]
    private static partial void GstDiscovererStop(nint discoverer);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstDiscoverer</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstPbutils", EntryPoint = "gst_discoverer_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Discoverer(handle, transfer);
}
