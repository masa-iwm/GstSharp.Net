// <auto-generated/>
// Generated by GstSharp.Generator from GstSdp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Sdp;

/// <summary>
/// The contents of the SDP "z=" field which allows the sender to
/// specify a list of time zone adjustments and offsets from the base
/// time.
/// </summary>
public sealed unsafe partial class SDPZone
{
    /// <summary>The native instance.</summary>
    internal nint Handle;

    /// <summary>Wraps a native <c>GstSDPZone</c>.</summary>
    /// <param name="handle">The native instance.</param>
    internal SDPZone(nint handle) => Handle = handle;

    /// <summary>the NTP time that a time zone adjustment happens</summary>
    /// <remarks>
    /// <para>
    /// The string is copied out of the structure on every read. The storage
    /// belongs to the C structure and is released or replaced with it, so what
    /// comes back here is the caller's and outlives it.
    /// </para>
    /// </remarks>
    public string? Time
    {
        get
        {
            string? value = Gst.Interop.GMarshal.PtrToStringUtf8(((SDPZoneRaw*)Handle)->Time);
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>the offset from the time when the session was first scheduled</summary>
    /// <remarks>
    /// <para>
    /// The string is copied out of the structure on every read. The storage
    /// belongs to the C structure and is released or replaced with it, so what
    /// comes back here is the caller's and outlives it.
    /// </para>
    /// </remarks>
    public string? TypedTime
    {
        get
        {
            string? value = Gst.Interop.GMarshal.PtrToStringUtf8(((SDPZoneRaw*)Handle)->TypedTime);
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>Wraps a native <c>GstSDPZone</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    /// <remarks>
    /// The wrapper of an opaque record is a bare pointer holder: the gir
    /// describes no way of releasing one, so it does not take part in the
    /// ownership of what it points at.
    /// </remarks>
    internal static SDPZone? FromNative(nint handle) =>
        handle == 0 ? null : new(handle);

    /// <summary>Reset the zone information in @zone.</summary>
    /// <returns>a #GstSDPResult.</returns>
    public Gst.Sdp.SDPResult Clear()
    {
        int nativeResult = GstSdpZoneClear(Handle);
        System.GC.KeepAlive(this);
        return (Gst.Sdp.SDPResult)nativeResult;
    }

    /// <summary>Set zone information in @zone.</summary>
    /// <param name="adjTime">the NTP time that a time zone adjustment happens</param>
    /// <param name="typedTime">the offset from the time when the session was first scheduled</param>
    /// <returns>a #GstSDPResult.</returns>
    public Gst.Sdp.SDPResult Set(string adjTime, string typedTime)
    {
        ArgumentNullException.ThrowIfNull(adjTime);
        System.Span<byte> adjTimeBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope adjTimeScope = Gst.Interop.GMarshal.StackUtf8(adjTime, adjTimeBuffer);
        ArgumentNullException.ThrowIfNull(typedTime);
        System.Span<byte> typedTimeBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope typedTimeScope = Gst.Interop.GMarshal.StackUtf8(typedTime, typedTimeBuffer);
        int nativeResult = GstSdpZoneSet(Handle, adjTimeScope.Pointer, typedTimeScope.Pointer);
        System.GC.KeepAlive(this);
        return (Gst.Sdp.SDPResult)nativeResult;
    }

    /// <summary>The <c>gst_sdp_zone_clear</c> entry point.</summary>
    [LibraryImport("GstSdp", EntryPoint = "gst_sdp_zone_clear")]
    private static partial int GstSdpZoneClear(nint zone);

    /// <summary>The <c>gst_sdp_zone_set</c> entry point.</summary>
    [LibraryImport("GstSdp", EntryPoint = "gst_sdp_zone_set")]
    private static partial int GstSdpZoneSet(nint zone, byte* adjTime, byte* typedTime);
}

/// <summary>The native layout of <c>GstSDPZone</c>.</summary>
/// <remarks>
/// <para>
/// The mirror is only ever read through a pointer into memory that GStreamer
/// owns; it is never allocated, assigned or copied.
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
internal unsafe struct SDPZoneRaw
{
    /// <summary>The <c>time</c> field.</summary>
    internal nint Time;

    /// <summary>The <c>typed_time</c> field.</summary>
    internal nint TypedTime;
}
