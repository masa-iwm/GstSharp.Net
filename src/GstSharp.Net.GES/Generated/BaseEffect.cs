// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>
/// A #GESBaseEffect is some operation that applies an effect to the data
/// it receives.
/// </summary>
/// <remarks>
/// <para>## Time Effects</para>
/// <para>
/// Some operations will change the timing of the stream data they receive
/// in some way. In particular, the #GstElement that they wrap could alter
/// the times of the segment they receive in a #GST_EVENT_SEGMENT event,
/// or the times of a seek they receive in a #GST_EVENT_SEEK event. Such
/// operations would be considered time effects since they translate the
/// times they receive on their source to different times at their sink,
/// and vis versa. This introduces two sets of time coordinates for the
/// event: (internal) sink coordinates and (internal) source coordinates,
/// where segment times are translated from the sink coordinates to the
/// source coordinates, and seek times are translated from the source
/// coordinates to the sink coordinates.
/// </para>
/// <para>
/// If you use such an effect in GES, you will need to inform GES of the
/// properties that control the timing with
/// ges_base_effect_register_time_property(), and the effect's timing
/// behaviour using ges_base_effect_set_time_translation_funcs().
/// </para>
/// <para>
/// Note that a time effect should not have its
/// #GESTrackElement:has-internal-source set to %TRUE.
/// </para>
/// <para>
/// In addition, note that GES only *fully* supports time effects whose
/// mapping from the source to sink coordinates (those applied to seeks)
/// obeys:
/// </para>
/// <para>
/// + Maps the time `0` to `0`. So initial time-shifting effects are
///   excluded.
/// + Is monotonically increasing. So reversing effects, and effects that
///   jump backwards in the stream are excluded.
/// + Can handle a reasonable #GstClockTime, relative to the project. So
///   this would exclude a time effect with an extremely large speed-up
///   that would cause the converted #GstClockTime seeks to overflow.
/// + Is 'continuously reversible'. This essentially means that for every
///   time in the sink coordinates, we can, to 'good enough' accuracy,
///   calculate the corresponding time in the source coordinates. Moreover,
///   this should correspond to how segment times are translated from
///   sink to source.
/// + Only depends on the registered time properties, rather than the
///   state of the #GstElement or the data it receives. This would exclude,
///   say, an effect that would speedup if there is more red in the image
///   it receives.
/// </para>
/// <para>
/// Note that a constant-rate-change effect that is not extremely fast or
/// slow would satisfy these conditions. For such effects, you may wish to
/// use ges_effect_class_register_rate_property().
/// </para>
/// </remarks>
public abstract unsafe partial class BaseEffect : GES.Operation, GES.IExtractable, GES.IMetaContainer
{
    /// <summary>Wraps a native <c>GESBaseEffect</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESBaseEffect</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected BaseEffect(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Get whether the effect is considered a time effect or not. An effect
    /// with registered time properties or set translation functions is
    /// considered a time effect.
    /// </summary>
    /// <returns>%TRUE if @effect is considered a time effect.</returns>
    public bool IsTimeEffect()
    {
        int nativeResult = GesBaseEffectIsTimeEffect(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Register a child property of the effect as a property that, when set,
    /// can change the timing of its input data. The child property should be
    /// specified as in ges_timeline_element_lookup_child().
    /// </summary>
    /// <remarks>
    /// <para>
    /// You should also set the corresponding time translation using
    /// ges_base_effect_set_time_translation_funcs().
    /// </para>
    /// <para>
    /// Note that @effect must not be part of a clip, nor can it have
    /// #GESTrackElement:has-internal-source set to %TRUE.
    /// </para>
    /// </remarks>
    /// <param name="childPropertyName">
    /// The name of the child property to register as
    /// a time property
    /// </param>
    /// <returns>%TRUE if the child property was found and newly registered.</returns>
    public bool RegisterTimeProperty(string childPropertyName)
    {
        ArgumentNullException.ThrowIfNull(childPropertyName);
        System.Span<byte> childPropertyNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope childPropertyNameScope = Gst.Interop.GMarshal.StackUtf8(childPropertyName, childPropertyNameBuffer);
        int nativeResult = GesBaseEffectRegisterTimeProperty(Handle, childPropertyNameScope.Pointer);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>The <c>ges_base_effect_is_time_effect</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_base_effect_is_time_effect")]
    private static partial int GesBaseEffectIsTimeEffect(nint effect);

    /// <summary>The <c>ges_base_effect_register_time_property</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_base_effect_register_time_property")]
    private static partial int GesBaseEffectRegisterTimeProperty(nint effect, byte* childPropertyName);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESBaseEffect</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_base_effect_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GESBaseEffect</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : BaseEffect
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
