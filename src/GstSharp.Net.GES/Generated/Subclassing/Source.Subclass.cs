// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace GES;

/// <content>The subclassing surface of <c>GESSource</c>.</content>
public unsafe partial class Source
{
    /// <summary>
    /// Constructs the managed subclass that
    /// <see cref="Gst.GObject.SubclassType.NewInstance()"/> created an instance of.
    /// </summary>
    /// <param name="args">The instance, as the registration handed it out.</param>
    /// <exception cref="System.ArgumentException">The instance is not a <c>GESSource</c>.</exception>
    protected Source(Gst.GObject.SubclassCtorArgs args)
        : this(args.HandleFor(GetGType()), args.Transfer)
    {
    }

    /// <summary>
    /// Gets the declaration of <c>GESSource.select_pad</c>, for a subclass that
    /// overrides <see cref="OnSelectPad"/>.
    /// </summary>
    public static Gst.GObject.VfuncOverride SelectPadOverride { get; } = new(
        &GetGType,
        GES.SourceClassRaw.SelectPadOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, nint, int>)&SelectPadTrampoline);

    /// <summary>
    /// Gets the declaration of <c>GESSource.create_source</c>, for a subclass that
    /// overrides <see cref="OnCreateSource"/>.
    /// </summary>
    public static Gst.GObject.VfuncOverride CreateSourceOverride { get; } = new(
        &GetGType,
        GES.SourceClassRaw.CreateSourceOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, nint>)&CreateSourceTrampoline);

    /// <summary>Registers a managed subclass of <c>GESSource</c> with GObject.</summary>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GESSource</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        params Gst.GObject.VfuncOverride[] overrides) =>
        DefineSubclassCore(typeName, configureClass, overrides, null, null);

    /// <summary>Registers a managed subclass of <c>GESSource</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GESSource</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : Source, Gst.GObject.IManagedSubclass<TSelf> =>
        DefineSubclassCore(typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), null);

    /// <summary>Registers a managed subclass of <c>GESSource</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="options">
    /// The optional parts of the registration, the interfaces the subclass
    /// implements above all. Interfaces can be declared here and nowhere else:
    /// GObject refuses to attach one once the class of the type is being
    /// initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, a declared slot belongs to a
    /// class that <c>GESSource</c> does not derive from, an interface is declared
    /// twice, or the parent type implements it already.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        Gst.GObject.SubclassOptions options,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : Source, Gst.GObject.IManagedSubclass<TSelf>
    {
        ArgumentNullException.ThrowIfNull(options);

        return DefineSubclassCore(
            typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), options);
    }

    private static Gst.GObject.SubclassType DefineSubclassCore(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        Gst.GObject.VfuncOverride[] overrides,
        Func<Gst.GObject.SubclassCtorArgs, Gst.GObject.Object>? wrapFactory,
        Gst.GObject.SubclassOptions? options)
    {
        Gst.GObject.SubclassType type = Gst.GObject.SubclassType.Define(
            new Gst.GObject.GType(GetGType()), typeName, configureClass, overrides, wrapFactory, options);
        return type;
    }

    /// <summary>Check whether @pad should be exposed/used.</summary>
    /// <remarks>
    /// <para>This slot may run on the thread that emits the pad-added of the sub element
    /// (ges-source.c:126, 190, 240), which is a streaming thread when the source is a dynamic
    /// one.</para>
    /// </remarks>
    /// <param name="pad">
    /// The <c>pad</c> argument.
    /// The element lends this for the duration of the call. Keeping the wrapper is
    /// safe: a GObject wrapper is interned and its reference outlives the call.
    /// </param>
    /// <returns>%TRUE if @pad should be used %FALSE otherwise.</returns>
    protected virtual bool OnSelectPad(Gst.Pad pad) =>
        ChainUpSelectPad(pad);

    /// <summary>
    /// Creates the GstElement to put in the source topbin. Other elements will be
    /// queued, like a volume. In the case of a AudioUriSource, for example, the
    /// subclass will return a decodebin, and we will append a volume.
    /// </summary>
    /// <remarks>
    /// <para>The bin of the source adds the answer and takes a reference of its own (gst_bin_add
    /// sinks it, ges-source.c:203, ges-audio-source.c:147 for an audio source), so the wrapper
    /// keeps working and no reference is added on the way out. The trampoline hands the handle
    /// on and moves no reference, so a wrapper you answer keeps the reference it already owns
    /// and may be disposed after the call or read back from the track element instead of being
    /// kept. Answer an element that has no parent yet and keep no extra reference to it:
    /// add_failure unrefs both the answer and the nlesource when the bin refuses it
    /// (ges-track-element.c:1073-1078). Answering null, or throwing, which the trampoline turns
    /// into a null answer, would leave the track element with an nleobject it no longer owns:
    /// child_failure unrefs it while it is still floating (ges-track-element.c:1022,
    /// 1066-1070), the composition sinks the last reference and frees the object when the
    /// element is removed, and dispose unrefs it again. GES 1.28.6 does that; the binding
    /// therefore answers an identity element in place of a null one and reports the
    /// substitution through the exception trap, so nothing feeds the substitute, the source
    /// produces no data and the pipeline cannot preroll. Answer a real element, or do not
    /// declare the slot. For a VideoSource or an AudioSource subclass no ancestor implements
    /// the slot: there is nothing below to chain up to, and ChainUpCreateSource throws.</para>
    /// </remarks>
    /// <returns>
    /// The source element to use.
    /// No reference is added on the way out: the base class takes one of its own
    /// from the answer, which the remarks describe. Keep no extra reference to it.
    /// Answering <see langword="null"/> is not allowed: the caller of the slot does
    /// not check for it. A null answer is reported through the exception trap and
    /// the slot answers a value the caller fails cleanly on.
    /// </returns>
    protected virtual Gst.Element OnCreateSource() =>
        ChainUpCreateSource();

    /// <summary>Runs the implementation of <c>select_pad</c> below the managed override.</summary>
    /// <remarks>
    /// <para>This slot may run on the thread that emits the pad-added of the sub element
    /// (ges-source.c:126, 190, 240), which is a streaming thread when the source is a dynamic
    /// one.</para>
    /// </remarks>
    /// <param name="pad">
    /// The <c>pad</c> argument.
    /// The element lends this for the duration of the call. Keeping the wrapper is
    /// safe: a GObject wrapper is interned and its reference outlives the call.
    /// </param>
    /// <returns>%TRUE if @pad should be used %FALSE otherwise.</returns>
    protected bool ChainUpSelectPad(Gst.Pad pad)
    {
        ArgumentNullException.ThrowIfNull(pad);
        bool result = ChainUpSelectPad(Handle, pad.Handle);
        GC.KeepAlive(this);
        GC.KeepAlive(pad);
        return result;
    }

    /// <summary>Runs the implementation of <c>create_source</c> below the managed override.</summary>
    /// <remarks>
    /// <para>The bin of the source adds the answer and takes a reference of its own (gst_bin_add
    /// sinks it, ges-source.c:203, ges-audio-source.c:147 for an audio source), so the wrapper
    /// keeps working and no reference is added on the way out. The trampoline hands the handle
    /// on and moves no reference, so a wrapper you answer keeps the reference it already owns
    /// and may be disposed after the call or read back from the track element instead of being
    /// kept. Answer an element that has no parent yet and keep no extra reference to it:
    /// add_failure unrefs both the answer and the nlesource when the bin refuses it
    /// (ges-track-element.c:1073-1078). Answering null, or throwing, which the trampoline turns
    /// into a null answer, would leave the track element with an nleobject it no longer owns:
    /// child_failure unrefs it while it is still floating (ges-track-element.c:1022,
    /// 1066-1070), the composition sinks the last reference and frees the object when the
    /// element is removed, and dispose unrefs it again. GES 1.28.6 does that; the binding
    /// therefore answers an identity element in place of a null one and reports the
    /// substitution through the exception trap, so nothing feeds the substitute, the source
    /// produces no data and the pipeline cannot preroll. Answer a real element, or do not
    /// declare the slot. For a VideoSource or an AudioSource subclass no ancestor implements
    /// the slot: there is nothing below to chain up to, and ChainUpCreateSource throws.</para>
    /// </remarks>
    /// <returns>
    /// The source element to use.
    /// No reference is added on the way out: the base class takes one of its own
    /// from the answer, which the remarks describe. Keep no extra reference to it.
    /// Answering <see langword="null"/> is not allowed: the caller of the slot does
    /// not check for it. A null answer is reported through the exception trap and
    /// the slot answers a value the caller fails cleanly on.
    /// </returns>
    protected Gst.Element ChainUpCreateSource()
    {
        nint resultNative = ChainUpCreateSource(Handle);
        Gst.Element result = Gst.GObject.Object.FromNative<Gst.Element>(resultNative, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException(
                "create_source answered null below the managed override.");
        GC.KeepAlive(this);
        return result;
    }

    private static bool ChainUpSelectPad(nint source, nint pad)
    {
        delegate* unmanaged[Cdecl]<nint, nint, int> slot =
            (delegate* unmanaged[Cdecl]<nint, nint, int>)ParentClassOf(source)->SelectPad;

        if (slot is null)
        {
            throw new InvalidOperationException(
                "Source.select_pad has no parent implementation; override OnSelectPad.");
        }

        return slot(source, pad) != 0;
    }

    private static nint ChainUpCreateSource(nint source)
    {
        delegate* unmanaged[Cdecl]<nint, nint> slot =
            (delegate* unmanaged[Cdecl]<nint, nint>)ParentClassOf(source)->CreateSource;

        if (slot is null)
        {
            throw new InvalidOperationException(
                "Source.create_source has no parent implementation; override OnCreateSource.");
        }

        return slot(source);
    }

    /// <summary>
    /// Returns the class the registration captured, which is the one an override
    /// chains up through.
    /// </summary>
    /// <param name="instance">The native <c>GESSource</c>.</param>
    /// <returns>The parent class of the managed subclass.</returns>
    private static GES.SourceClassRaw* ParentClassOf(nint instance) =>
        (GES.SourceClassRaw*)Gst.GObject.SubclassRegistry.DescriptorFor(instance).ParentClass;

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static int SelectPadTrampoline(nint source, nint pad)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(source) is not Source managed)
            {
                return (ChainUpSelectPad(source, pad)) ? 1 : 0;
            }

            Gst.Pad? padValue = Gst.GObject.Object.FromNative<Gst.Pad>(pad, Gst.Interop.Transfer.None);
            return (managed.OnSelectPad(padValue!)) ? 1 : 0;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static nint CreateSourceTrampoline(nint source)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(source) is not Source managed)
            {
                return ChainUpCreateSource(source);
            }

            Gst.Element? result = managed.OnCreateSource();
            if (result is null)
            {
                throw new InvalidOperationException(
                    "OnCreateSource answered null, which create_source does not allow.");
            }

            return result is null ? nint.Zero : result.Handle;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return Gst.GstNative.ElementFactoryMakeRaw("identity", null);
        }
    }
}
