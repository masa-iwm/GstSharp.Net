// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace GES;

/// <content>The subclassing surface of <c>GESClip</c>.</content>
public unsafe partial class Clip
{
    /// <summary>
    /// Constructs the managed subclass that
    /// <see cref="Gst.GObject.SubclassType.NewInstance()"/> created an instance of.
    /// </summary>
    /// <param name="args">The instance, as the registration handed it out.</param>
    /// <exception cref="System.ArgumentException">The instance is not a <c>GESClip</c>.</exception>
    protected Clip(Gst.GObject.SubclassCtorArgs args)
        : this(args.HandleFor(GetGType()), args.Transfer)
    {
    }

    /// <summary>
    /// Gets the declaration of <c>GESClip.create_track_element</c>, for a subclass that
    /// overrides <see cref="OnCreateTrackElement"/>.
    /// </summary>
    public static Gst.GObject.VfuncOverride CreateTrackElementOverride { get; } = new(
        &GetGType,
        GES.ClipClassRaw.CreateTrackElementOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, int, nint>)&CreateTrackElementTrampoline);

    /// <summary>Registers a managed subclass of <c>GESClip</c> with GObject.</summary>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GESClip</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        params Gst.GObject.VfuncOverride[] overrides) =>
        DefineSubclassCore(typeName, configureClass, overrides, null, null);

    /// <summary>Registers a managed subclass of <c>GESClip</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GESClip</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : Clip, Gst.GObject.IManagedSubclass<TSelf> =>
        DefineSubclassCore(typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), null);

    /// <summary>Registers a managed subclass of <c>GESClip</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="options">
    /// The optional parts of the registration, the interfaces the subclass
    /// implements above all. Interfaces can be declared here and nowhere else:
    /// GObject refuses to attach one once the class of the type is being
    /// initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GESClip</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        Gst.GObject.SubclassOptions options,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : Clip, Gst.GObject.IManagedSubclass<TSelf>
    {
        ArgumentNullException.ThrowIfNull(options);

        return DefineSubclassCore(
            typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), options);
    }

    private static Gst.GObject.SubclassType DefineSubclassCore(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        Gst.GObject.VfuncOverride[] overrides,
        Func<Gst.GObject.SubclassCtorArgs, Gst.GObject.Object>? wrapFactory,
        Gst.GObject.SubclassOptions? options)
    {
        Gst.GObject.SubclassType type = Gst.GObject.SubclassType.Define(
            new Gst.GObject.GType(GetGType()), typeName, configureClass, overrides, wrapFactory, options);
        return type;
    }

    /// <summary>
    /// Method to create the core #GESTrackElement of a clip
    /// of this class. If a clip of this class may create several track elements per
    /// track type, this should be left as %NULL, and
    /// GESClipClass::create_track_elements should be used instead. Otherwise, you
    /// should implement this class method and leave
    /// GESClipClass::create_track_elements as the default implementation
    /// </summary>
    /// <remarks>
    /// <para>Answer an element extracted from an asset - Asset.Request(subclassType.GType, null)
    /// followed by Extract&lt;T&gt;() - which owns the only reference to the element until
    /// ges_container_add takes one of its own (ges-container.c:731), so do not dispose it
    /// before returning. A child built with new has no asset: it never gets an nleobject,
    /// ges_layer_add_clip_full removes it from the clip again (ges-layer.c:781-800), and
    /// splitting or pasting the clip aborts the process on g_assert (asset)
    /// (ges-timeline-element.c:1675). A null answer is a clip with no child of that track type,
    /// which is not an error.</para>
    /// </remarks>
    /// <param name="type">The <c>type</c> argument.</param>
    /// <returns>
    /// The #GESTrackElement created
    /// by @clip, or %NULL if @clip can not provide a track element for the
    /// given @type or an error occurred.
    /// The answer is borrowed: no reference is added for the caller, so the
    /// override has to keep the object alive by other means.
    /// </returns>
    protected virtual GES.TrackElement? OnCreateTrackElement(GES.TrackType type) =>
        ChainUpCreateTrackElement(type);

    /// <summary>Runs the implementation of <c>create_track_element</c> below the managed override.</summary>
    /// <remarks>
    /// <para>Answer an element extracted from an asset - Asset.Request(subclassType.GType, null)
    /// followed by Extract&lt;T&gt;() - which owns the only reference to the element until
    /// ges_container_add takes one of its own (ges-container.c:731), so do not dispose it
    /// before returning. A child built with new has no asset: it never gets an nleobject,
    /// ges_layer_add_clip_full removes it from the clip again (ges-layer.c:781-800), and
    /// splitting or pasting the clip aborts the process on g_assert (asset)
    /// (ges-timeline-element.c:1675). A null answer is a clip with no child of that track type,
    /// which is not an error.</para>
    /// </remarks>
    /// <param name="type">The <c>type</c> argument.</param>
    /// <returns>
    /// The #GESTrackElement created
    /// by @clip, or %NULL if @clip can not provide a track element for the
    /// given @type or an error occurred.
    /// The answer is borrowed: no reference is added for the caller, so the
    /// override has to keep the object alive by other means.
    /// </returns>
    protected GES.TrackElement? ChainUpCreateTrackElement(GES.TrackType type)
    {
        nint resultNative = ChainUpCreateTrackElement(Handle, (int)type);
        GES.TrackElement? result = Gst.GObject.Object.FromNative<GES.TrackElement>(resultNative, Gst.Interop.Transfer.None);
        GC.KeepAlive(this);
        return result;
    }

    private static nint ChainUpCreateTrackElement(nint clip, int type)
    {
        delegate* unmanaged[Cdecl]<nint, int, nint> slot =
            (delegate* unmanaged[Cdecl]<nint, int, nint>)ParentClassOf(clip)->CreateTrackElement;

        if (slot is null)
        {
            return nint.Zero;
        }

        return slot(clip, type);
    }

    /// <summary>
    /// Returns the class the registration captured, which is the one an override
    /// chains up through.
    /// </summary>
    /// <param name="instance">The native <c>GESClip</c>.</param>
    /// <returns>The parent class of the managed subclass.</returns>
    private static GES.ClipClassRaw* ParentClassOf(nint instance) =>
        (GES.ClipClassRaw*)Gst.GObject.SubclassRegistry.DescriptorFor(instance).ParentClass;

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static nint CreateTrackElementTrampoline(nint clip, int type)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(clip) is not Clip managed)
            {
                return ChainUpCreateTrackElement(clip, type);
            }

            GES.TrackElement? result = managed.OnCreateTrackElement((GES.TrackType)type);
            return result is null ? nint.Zero : result.Handle;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return nint.Zero;
        }
    }
}
