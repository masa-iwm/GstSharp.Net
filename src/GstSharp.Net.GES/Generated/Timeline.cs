// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>#GESTimeline is the central object for any multimedia timeline.</summary>
/// <remarks>
/// <para>
/// A timeline is composed of a set of #GESTrack-s and a set of
/// #GESLayer-s, which are added to the timeline using
/// ges_timeline_add_track() and ges_timeline_append_layer(), respectively.
/// </para>
/// <para>
/// The contained tracks define the supported types of the timeline
/// and provide the media output. Essentially, each track provides an
/// additional source #GstPad.
/// </para>
/// <para>
/// Most usage of a timeline will likely only need a single #GESAudioTrack
/// and/or a single #GESVideoTrack. You can create such a timeline with
/// ges_timeline_new_audio_video(). After this, you are unlikely to need to
/// work with the tracks directly.
/// </para>
/// <para>
/// A timeline's layers contain #GESClip-s, which in turn control the
/// creation of #GESTrackElement-s, which are added to the timeline's
/// tracks. See #GESTimeline::select-tracks-for-object if you wish to have
/// more control over which track a clip's elements are added to.
/// </para>
/// <para>
/// The layers are ordered, with higher priority layers having their
/// content prioritised in the tracks. This ordering can be changed using
/// ges_timeline_move_layer().
/// </para>
/// <para>## Editing</para>
/// <para>
/// See #GESTimelineElement for the various ways the elements of a timeline
/// can be edited.
/// </para>
/// <para>
/// If you change the timing or ordering of a timeline's
/// #GESTimelineElement-s, then these changes will not actually be taken
/// into account in the output of the timeline's tracks until the
/// ges_timeline_commit() method is called. This allows you to move its
/// elements around, say, in response to an end user's mouse dragging, with
/// little expense before finalising their effect on the produced data.
/// </para>
/// <para>## Overlaps and Auto-Transitions</para>
/// <para>
/// There are certain restrictions placed on how #GESSource-s may overlap
/// in a #GESTrack that belongs to a timeline. These will be enforced by
/// GES, so the user will not need to keep track of them, but they should
/// be aware that certain edits will be refused as a result if the overlap
/// rules would be broken.
/// </para>
/// <para>
/// Consider two #GESSource-s, `A` and `B`, with start times `startA` and
/// `startB`, and end times `endA` and `endB`, respectively. The start
/// time refers to their #GESTimelineElement:start, and the end time is
/// their #GESTimelineElement:start + #GESTimelineElement:duration. These
/// two sources *overlap* if:
/// </para>
/// <para>
/// + they share the same #GESTrackElement:track (non %NULL), which belongs
///   to the timeline;
/// + they share the same #GES_TIMELINE_ELEMENT_LAYER_PRIORITY; and
/// + `startA &lt; endB` and `startB &lt; endA `.
/// </para>
/// <para>
/// Note that when `startA = endB` or `startB = endA` then the two sources
/// will *touch* at their edges, but are not considered overlapping.
/// </para>
/// <para>
/// If, in addition, `startA &lt; startB &lt; endA`, then we can say that the
/// end of `A` overlaps the start of `B`.
/// </para>
/// <para>
/// If, instead, `startA &lt;= startB` and `endA &gt;= endB`, then we can say
/// that `A` fully overlaps `B`.
/// </para>
/// <para>The overlap rules for a timeline are that:</para>
/// <para>
/// 1. One source cannot fully overlap another source.
/// 2. A source can only overlap the end of up to one other source at its
///    start.
/// 3. A source can only overlap the start of up to one other source at its
///    end.
/// </para>
/// <para>
/// The last two rules combined essentially mean that at any given timeline
/// position, only up to two #GESSource-s may overlap at that position. So
/// triple or more overlaps are not allowed.
/// </para>
/// <para>
/// If you switch on #GESTimeline:auto-transition, then at any moment when
/// the end of one source (the first source) overlaps the start of another
/// (the second source), a #GESTransitionClip will be automatically created
/// for the pair in the same layer and it will cover their overlap. If the
/// two elements are edited in a way such that the end of the first source
/// no longer overlaps the start of the second, the transition will be
/// automatically removed from the timeline. However, if the two sources
/// still overlap at the same edges after the edit, then the same
/// transition object will be kept, but with its timing and layer adjusted
/// accordingly.
/// </para>
/// <para>
/// NOTE: if you know what you are doing and want to be in full control of the
/// timeline layout, you can disable the edit APIs with
/// #ges_timeline_disable_edit_apis.
/// </para>
/// <para>## Saving</para>
/// <para>
/// To save/load a timeline, you can use the ges_timeline_load_from_uri()
/// and ges_timeline_save_to_uri() methods that use the default format.
/// </para>
/// <para>## Playing</para>
/// <para>
/// A timeline is a #GstBin with a source #GstPad for each of its
/// tracks, which you can fetch with ges_timeline_get_pad_for_track(). You
/// will likely want to link these to some compatible sink #GstElement-s to
/// be able to play or capture the content of the timeline.
/// </para>
/// <para>
/// You can use a #GESPipeline to easily preview/play the timeline's
/// content, or render it to a file.
/// </para>
/// </remarks>
public unsafe partial class Timeline : Gst.Bin, GES.IExtractable, GES.IMetaContainer, Gst.IChildProxy
{
    /// <summary>Wraps a native <c>GESTimeline</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESTimeline</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Timeline(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new empty timeline.</summary>
    /// <returns>The new timeline.</returns>
    public static GES.Timeline New()
    {
        nint nativeResult = GesTimelineNew();
        return Gst.GObject.Object.FromNative<GES.Timeline>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_timeline_new returned no value.");
    }

    /// <summary>
    /// Creates a new timeline containing a single #GESAudioTrack and a
    /// single #GESVideoTrack.
    /// </summary>
    /// <returns>The new timeline.</returns>
    public static GES.Timeline NewAudioVideo()
    {
        nint nativeResult = GesTimelineNewAudioVideo();
        return Gst.GObject.Object.FromNative<GES.Timeline>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_timeline_new_audio_video returned no value.");
    }

    /// <summary>Creates a timeline from the given URI.</summary>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>
    /// A new timeline if the uri was loaded
    /// successfully, or %NULL if the uri could not be loaded.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public static GES.Timeline NewFromUri(string uri)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        nint nativeResult = GesTimelineNewFromUri(uriScope.Pointer, &errorNative);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return Gst.GObject.Object.FromNative<GES.Timeline>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_timeline_new_from_uri returned no value.");
    }

    /// <summary>Add a layer to the timeline.</summary>
    /// <remarks>
    /// <para>
    /// If the layer contains #GESClip-s, then this may trigger the creation of
    /// their core track element children for the timeline's tracks, and the
    /// placement of the clip's children in the tracks of the timeline using
    /// #GESTimeline::select-tracks-for-object. Some errors may occur if this
    /// would break one of the configuration rules of the timeline in one of
    /// its tracks. In such cases, some track elements would fail to be added
    /// to their tracks, but this method would still return %TRUE. As such, it
    /// is advised that you only add clips to layers that already part of a
    /// timeline. In such situations, ges_layer_add_clip() is able to fail if
    /// adding the clip would cause such an error.
    /// </para>
    /// </remarks>
    /// <param name="layer">The <c>layer</c> argument.</param>
    /// <returns>%TRUE if @layer was properly added.</returns>
    [Obsolete("This method requires you to ensure the layer's #GESLayer:priority will be unique to the timeline. Use ges_timeline_append_layer() and ges_timeline_move_layer() instead. (deprecated since 1.18)")]
    public bool AddLayer(GES.Layer layer)
    {
        ArgumentNullException.ThrowIfNull(layer);
        int nativeResult = GesTimelineAddLayer(Handle, layer.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(layer);
        return nativeResult != 0;
    }

    /// <summary>Add a track to the timeline.</summary>
    /// <remarks>
    /// <para>
    /// If the timeline already contains clips, then this may trigger the
    /// creation of their core track element children for the track, and the
    /// placement of the clip's children in the track of the timeline using
    /// #GESTimeline::select-tracks-for-object. Some errors may occur if this
    /// would break one of the configuration rules for the timeline in the
    /// track. In such cases, some track elements would fail to be added to the
    /// track, but this method would still return %TRUE. As such, it is advised
    /// that you avoid adding tracks to timelines that already contain clips.
    /// </para>
    /// </remarks>
    /// <param name="track">The <c>track</c> argument.</param>
    /// <returns>%TRUE if @track was properly added.</returns>
    public bool AddTrack(GES.Track track)
    {
        ArgumentNullException.ThrowIfNull(track);
        int nativeResult = GesTimelineAddTrack(Handle, track.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(track);
        return nativeResult != 0;
    }

    /// <summary>
    /// Append a newly created layer to the timeline. The layer will
    /// be added at the lowest #GESLayer:priority (numerically, the highest).
    /// </summary>
    /// <returns>The newly created layer.</returns>
    public GES.Layer AppendLayer()
    {
        nint nativeResult = GesTimelineAppendLayer(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.Layer>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_timeline_append_layer returned no value.");
    }

    /// <summary>
    /// Commit all the pending changes of the clips contained in the
    /// timeline.
    /// </summary>
    /// <remarks>
    /// <para>
    /// When changes happen in a timeline, they are not immediately executed
    /// internally, in a way that effects the output data of the timeline. You
    /// should call this method when you are done with a set of changes and you
    /// want them to be executed.
    /// </para>
    /// <para>
    /// Any pending changes will be executed in the backend. The
    /// #GESTimeline::commited signal will be emitted once this has completed.
    /// You should not try to change the state of the timeline, seek it or add
    /// tracks to it before receiving this signal. You can use
    /// ges_timeline_commit_sync() if you do not want to perform other tasks in
    /// the mean time.
    /// </para>
    /// <para>
    /// Note that all the pending changes will automatically be executed when
    /// the timeline goes from #GST_STATE_READY to #GST_STATE_PAUSED, which is
    /// usually triggered by a corresponding state changes in a containing
    /// #GESPipeline.
    /// </para>
    /// </remarks>
    /// <returns>
    /// %TRUE if pending changes were committed, or %FALSE if nothing
    /// needed to be committed.
    /// </returns>
    public bool Commit()
    {
        int nativeResult = GesTimelineCommit(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Commit all the pending changes of the clips contained in the
    /// timeline and wait for the changes to complete.
    /// </summary>
    /// <remarks>
    /// <para>See ges_timeline_commit().</para>
    /// </remarks>
    /// <returns>
    /// %TRUE if pending changes were committed, or %FALSE if nothing
    /// needed to be committed.
    /// </returns>
    public bool CommitSync()
    {
        int nativeResult = GesTimelineCommitSync(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// WARNING: When using that mode, GES won't guarantee the coherence of the
    /// timeline. You need to ensure that the rules described in the [Overlaps and
    /// auto transitions](#overlaps-and-autotransitions) section are respected any time
    /// the timeline is [commited](ges_timeline_commit) (otherwise playback will most
    /// probably fail in different ways).
    /// </summary>
    /// <remarks>
    /// <para>
    /// When disabling editing APIs, GES won't be able to enforce the rules that
    /// makes the timeline overall state to be valid but some feature won't be
    /// usable:
    ///   * #GESTimeline:snapping-distance
    ///   * #GESTimeline:auto-transition
    /// </para>
    /// </remarks>
    /// <param name="disableEditApis">The <c>disableEditApis</c> argument.</param>
    public void DisableEditApis(bool disableEditApis)
    {
        GesTimelineDisableEditApis(Handle, disableEditApis ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Freezes the timeline from being committed. This is usually needed while the
    /// timeline is being rendered to ensure that not change to the timeline are
    /// taken into account during that moment. Once the rendering is done, you
    /// should call #ges_timeline_thaw_commit so that committing becomes possible
    /// again and any call to `commit()` that happened during the rendering is
    /// actually taken into account.
    /// </summary>
    public void FreezeCommit()
    {
        GesTimelineFreezeCommit(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Gets #GESTimeline:auto-transition for the timeline.</summary>
    /// <returns>The auto-transition of @self.</returns>
    public bool GetAutoTransition()
    {
        int nativeResult = GesTimelineGetAutoTransition(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get the current #GESTimeline:duration of the timeline</summary>
    /// <returns>The current duration of @timeline.</returns>
    public Gst.ClockTime GetDuration()
    {
        ulong nativeResult = GesTimelineGetDuration(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>The <c>ges_timeline_get_edit_apis_disabled</c> function.</summary>
    /// <returns>%TRUE if edit APIs are disabled, %FALSE otherwise.</returns>
    public bool GetEditApisDisabled()
    {
        int nativeResult = GesTimelineGetEditApisDisabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Gets the element contained in the timeline with the given name.</summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>
    /// The timeline element in @timeline
    /// with the given @name, or %NULL if it was not found.
    /// </returns>
    public GES.TimelineElement? GetElement(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GesTimelineGetElement(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.TimelineElement>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// This method allows you to convert a timeline #GstClockTime into its
    /// corresponding #GESFrameNumber in the timeline's output.
    /// </summary>
    /// <param name="timestamp">The <c>timestamp</c> argument.</param>
    /// <returns>The frame number @timestamp corresponds to.</returns>
    public long GetFrameAt(Gst.ClockTime timestamp)
    {
        long nativeResult = GesTimelineGetFrameAt(Handle, timestamp.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// This method allows you to convert a timeline output frame number into a
    /// timeline #GstClockTime. For example, this time could be used to seek to a
    /// particular frame in the timeline's output, or as the edit position for
    /// an element within the timeline.
    /// </summary>
    /// <param name="frameNumber">The <c>frameNumber</c> argument.</param>
    /// <returns>The timestamp corresponding to @frame_number in the output of @self.</returns>
    public Gst.ClockTime GetFrameTime(long frameNumber)
    {
        ulong nativeResult = GesTimelineGetFrameTime(Handle, frameNumber);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Get the list of #GESGroup-s present in the timeline.</summary>
    /// <returns>
    /// The list of
    /// groups that contain clips present in @timeline's layers.
    /// Must not be changed.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Group> GetGroups()
    {
        nint nativeResult = GesTimelineGetGroups(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.Collect(nativeResult);
        System.Collections.Generic.List<GES.Group> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Group>(nativeItem, Gst.Interop.Transfer.None) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Retrieve the layer whose index in the timeline matches the given
    /// priority.
    /// </summary>
    /// <param name="priority">The <c>priority</c> argument.</param>
    /// <returns>
    /// The layer with the given
    /// @priority, or %NULL if none was found.
    /// </returns>
    public GES.Layer? GetLayer(uint priority)
    {
        nint nativeResult = GesTimelineGetLayer(Handle, priority);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.Layer>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the list of #GESLayer-s present in the timeline.</summary>
    /// <returns>
    /// The list of
    /// layers present in @timeline sorted by priority.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Layer> GetLayers()
    {
        nint nativeResult = GesTimelineGetLayers(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.Layer> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Layer>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Search for the #GstPad corresponding to the given timeline's track.
    /// You can link to this pad to receive the output data of the given track.
    /// </summary>
    /// <param name="track">The <c>track</c> argument.</param>
    /// <returns>
    /// The pad corresponding to @track,
    /// or %NULL if there is an error.
    /// </returns>
    public Gst.Pad? GetPadForTrack(GES.Track track)
    {
        ArgumentNullException.ThrowIfNull(track);
        nint nativeResult = GesTimelineGetPadForTrack(Handle, track.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(track);
        return Gst.GObject.Object.FromNative<Gst.Pad>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Gets the #GESTimeline:snapping-distance for the timeline.</summary>
    /// <returns>The snapping distance (in nanoseconds) of @timeline.</returns>
    public Gst.ClockTime GetSnappingDistance()
    {
        ulong nativeResult = GesTimelineGetSnappingDistance(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Search for the #GESTrack corresponding to the given timeline's pad.</summary>
    /// <param name="pad">The <c>pad</c> argument.</param>
    /// <returns>
    /// The track corresponding to @pad,
    /// or %NULL if there is an error.
    /// </returns>
    public GES.Track? GetTrackForPad(Gst.Pad pad)
    {
        ArgumentNullException.ThrowIfNull(pad);
        nint nativeResult = GesTimelineGetTrackForPad(Handle, pad.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pad);
        return Gst.GObject.Object.FromNative<GES.Track>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Get the list of #GESTrack-s used by the timeline.</summary>
    /// <returns>
    /// The list of tracks
    /// used by @timeline.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Track> GetTracks()
    {
        nint nativeResult = GesTimelineGetTracks(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.Track> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Track>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Check whether the timeline is empty or not.</summary>
    /// <returns>%TRUE if @timeline is empty.</returns>
    public bool IsEmpty()
    {
        int nativeResult = GesTimelineIsEmpty(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Loads the contents of URI into the timeline.</summary>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>%TRUE if the timeline was loaded successfully from @uri.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool LoadFromUri(string uri)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        int nativeResult = GesTimelineLoadFromUri(Handle, uriScope.Pointer, &errorNative);
        System.GC.KeepAlive(this);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Moves a layer within the timeline to the index given by
    /// @new_layer_priority.
    /// An index of 0 corresponds to the layer with the highest priority in a
    /// timeline. If @new_layer_priority is greater than the number of layers
    /// present in the timeline, it will become the lowest priority layer.
    /// </summary>
    /// <param name="layer">The <c>layer</c> argument.</param>
    /// <param name="newLayerPriority">The <c>newLayerPriority</c> argument.</param>
    /// <returns>The result of <c>ges_timeline_move_layer</c>.</returns>
    public bool MoveLayer(GES.Layer layer, uint newLayerPriority)
    {
        ArgumentNullException.ThrowIfNull(layer);
        int nativeResult = GesTimelineMoveLayer(Handle, layer.Handle, newLayerPriority);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(layer);
        return nativeResult != 0;
    }

    /// <summary>
    /// Paste an element inside the timeline. @element **must** be the return of
    /// ges_timeline_element_copy() with `deep=TRUE`,
    /// and it should not be changed before pasting. @element itself is not
    /// placed in the timeline, instead a new element is created, alike to the
    /// originally copied element. Note that the originally copied element must
    /// also lie within @timeline, at both the point of copying and pasting.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Pasting may fail if it would place the timeline in an unsupported
    /// configuration.
    /// </para>
    /// <para>
    /// After calling this function @element should not be used. In particular,
    /// @element can **not** be pasted again. Instead, you can copy the
    /// returned element and paste that copy (although, this is only possible
    /// if the paste was successful).
    /// </para>
    /// <para>See also ges_timeline_element_paste().</para>
    /// </remarks>
    /// <param name="element">The <c>element</c> argument.</param>
    /// <param name="position">The <c>position</c> argument.</param>
    /// <param name="layerPriority">The <c>layerPriority</c> argument.</param>
    /// <returns>
    /// The newly created element, or
    /// %NULL if pasting fails.
    /// </returns>
    public GES.TimelineElement? PasteElement(GES.TimelineElement element, Gst.ClockTime position, int layerPriority)
    {
        ArgumentNullException.ThrowIfNull(element);
        nint nativeResult = GesTimelinePasteElement(Handle, element.Handle, position.Nanoseconds, layerPriority);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(element);
        return Gst.GObject.Object.FromNative<GES.TimelineElement>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Removes a layer from the timeline.</summary>
    /// <param name="layer">The <c>layer</c> argument.</param>
    /// <returns>%TRUE if @layer was properly removed.</returns>
    public bool RemoveLayer(GES.Layer layer)
    {
        ArgumentNullException.ThrowIfNull(layer);
        int nativeResult = GesTimelineRemoveLayer(Handle, layer.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(layer);
        return nativeResult != 0;
    }

    /// <summary>Remove a track from the timeline.</summary>
    /// <param name="track">The <c>track</c> argument.</param>
    /// <returns>%TRUE if @track was properly removed.</returns>
    public bool RemoveTrack(GES.Track track)
    {
        ArgumentNullException.ThrowIfNull(track);
        int nativeResult = GesTimelineRemoveTrack(Handle, track.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(track);
        return nativeResult != 0;
    }

    /// <summary>
    /// Saves the timeline to the given location. If @formatter_asset is %NULL,
    /// the method will attempt to save in the same format the timeline was
    /// loaded from, before defaulting to the formatter with highest rank.
    /// </summary>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <param name="formatterAsset">The <c>formatterAsset</c> argument.</param>
    /// <param name="overwrite">The <c>overwrite</c> argument.</param>
    /// <returns>%TRUE if @timeline was successfully saved to @uri.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool SaveToUri(string uri, GES.Asset? formatterAsset, bool overwrite)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        int nativeResult = GesTimelineSaveToUri(Handle, uriScope.Pointer, formatterAsset is null ? 0 : formatterAsset.Handle, overwrite ? 1 : 0, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(formatterAsset);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets #GESTimeline:auto-transition for the timeline. This will also set
    /// the corresponding #GESLayer:auto-transition for all of the timeline's
    /// layers to the same value. See ges_layer_set_auto_transition() if you
    /// wish to set the layer's #GESLayer:auto-transition individually.
    /// </summary>
    /// <param name="autoTransition">The <c>autoTransition</c> argument.</param>
    public void SetAutoTransition(bool autoTransition)
    {
        GesTimelineSetAutoTransition(Handle, autoTransition ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets #GESTimeline:snapping-distance for the timeline. This new value
    /// will only effect future snappings and will not be used to snap the
    /// current element positions within the timeline.
    /// </summary>
    /// <param name="snappingDistance">The <c>snappingDistance</c> argument.</param>
    public void SetSnappingDistance(Gst.ClockTime snappingDistance)
    {
        GesTimelineSetSnappingDistance(Handle, snappingDistance.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Thaw the timeline so that comiting becomes possible
    /// again and any call to `commit()` that happened during the rendering is
    /// actually taken into account.
    /// </summary>
    public void ThawCommit()
    {
        GesTimelineThawCommit(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Whether to automatically create a transition whenever two
    /// #GESSource-s overlap in a track of the timeline. See
    /// #GESLayer:auto-transition if you want this to only happen in some
    /// layers.
    /// </summary>
    public bool AutoTransition
    {
        get => GetAutoTransition();
        set => SetAutoTransition(value);
    }

    /// <summary>
    /// The current duration (in nanoseconds) of the timeline. A timeline
    /// 'starts' at time 0, so this is the maximum end time of all of its
    /// #GESTimelineElement-s.
    /// </summary>
    public Gst.ClockTime Duration => GetDuration();

    /// <summary>
    /// The distance (in nanoseconds) at which a #GESTimelineElement being
    /// moved within the timeline should snap one of its #GESSource-s with
    /// another #GESSource-s edge. See #GESEditMode for which edges can
    /// snap during an edit. 0 means no snapping.
    /// </summary>
    public Gst.ClockTime SnappingDistance
    {
        get => GetSnappingDistance();
        set => SetSnappingDistance(value);
    }

    /// <summary>
    /// This signal will be emitted once the changes initiated by
    /// ges_timeline_commit() have been executed in the backend. Use
    /// ges_timeline_commit_sync() if you do not want to have to connect
    /// to this signal.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler Committed
    {
        add => GES.SignalConnections.Add(this, "commited", (nint)(delegate* unmanaged[Cdecl]<nint, nint, void>)&CommittedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "commited", value);
    }

    /// <summary>The native handler of the <c>commited</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void CommittedTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler>(userData) is not { } handler)
            {
                return;
            }

            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>group-added</c> signal of <c>GESTimeline</c>.</summary>
    public sealed class GroupAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="GroupAddedSignalArgs"/> class.</summary>
        /// <param name="group">The group that was added to @timeline</param>
        internal GroupAddedSignalArgs(GES.Group group)
        {
            Group = group;
        }

        /// <summary>The group that was added to @timeline</summary>
        public GES.Group Group { get; }
    }

    /// <summary>
    /// Will be emitted after the group is added to to the timeline. This can
    /// happen when grouping with `ges_container_group`, or by adding
    /// containers to a newly created group.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that this should not be emitted whilst a timeline is being
    /// loaded from its #GESProject asset. You should connect to the
    /// project's #GESProject::loaded signal if you want to know which groups
    /// were created for the timeline.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Timeline.GroupAddedSignalArgs> GroupAdded
    {
        add => GES.SignalConnections.Add(this, "group-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&GroupAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "group-added", value);
    }

    /// <summary>The native handler of the <c>group-added</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void GroupAddedTrampoline(nint instance, nint group, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Timeline.GroupAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Group groupValue = Gst.GObject.Object.FromNative<GES.Group>(group, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The group-added signal of GESTimeline passed no group.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Timeline.GroupAddedSignalArgs(groupValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>layer-added</c> signal of <c>GESTimeline</c>.</summary>
    public sealed class LayerAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="LayerAddedSignalArgs"/> class.</summary>
        /// <param name="layer">The layer that was added to @timeline</param>
        internal LayerAddedSignalArgs(GES.Layer layer)
        {
            Layer = layer;
        }

        /// <summary>The layer that was added to @timeline</summary>
        public GES.Layer Layer { get; }
    }

    /// <summary>Will be emitted after the layer is added to the timeline.</summary>
    /// <remarks>
    /// <para>
    /// Note that this should not be emitted whilst a timeline is being
    /// loaded from its #GESProject asset. You should connect to the
    /// project's #GESProject::loaded signal if you want to know which
    /// layers were created for the timeline.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Timeline.LayerAddedSignalArgs> LayerAdded
    {
        add => GES.SignalConnections.Add(this, "layer-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&LayerAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "layer-added", value);
    }

    /// <summary>The native handler of the <c>layer-added</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void LayerAddedTrampoline(nint instance, nint layer, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Timeline.LayerAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Layer layerValue = Gst.GObject.Object.FromNative<GES.Layer>(layer, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The layer-added signal of GESTimeline passed no layer.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Timeline.LayerAddedSignalArgs(layerValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>layer-removed</c> signal of <c>GESTimeline</c>.</summary>
    public sealed class LayerRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="LayerRemovedSignalArgs"/> class.</summary>
        /// <param name="layer">The layer that was removed from @timeline</param>
        internal LayerRemovedSignalArgs(GES.Layer layer)
        {
            Layer = layer;
        }

        /// <summary>The layer that was removed from @timeline</summary>
        public GES.Layer Layer { get; }
    }

    /// <summary>Will be emitted after the layer is removed from the timeline.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Timeline.LayerRemovedSignalArgs> LayerRemoved
    {
        add => GES.SignalConnections.Add(this, "layer-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&LayerRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "layer-removed", value);
    }

    /// <summary>The native handler of the <c>layer-removed</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void LayerRemovedTrampoline(nint instance, nint layer, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Timeline.LayerRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Layer layerValue = Gst.GObject.Object.FromNative<GES.Layer>(layer, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The layer-removed signal of GESTimeline passed no layer.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Timeline.LayerRemovedSignalArgs(layerValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>snapping-ended</c> signal of <c>GESTimeline</c>.</summary>
    public sealed class SnappingEndedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="SnappingEndedSignalArgs"/> class.</summary>
        /// <param name="obj1">The first element that was snapping</param>
        /// <param name="obj2">The second element that was snapping</param>
        /// <param name="position">The position where the two objects were to be snapped to</param>
        internal SnappingEndedSignalArgs(GES.TrackElement obj1, GES.TrackElement obj2, ulong position)
        {
            Obj1 = obj1;
            Obj2 = obj2;
            Position = position;
        }

        /// <summary>The first element that was snapping</summary>
        public GES.TrackElement Obj1 { get; }

        /// <summary>The second element that was snapping</summary>
        public GES.TrackElement Obj2 { get; }

        /// <summary>The position where the two objects were to be snapped to</summary>
        public ulong Position { get; }
    }

    /// <summary>
    /// Will be emitted whenever a snapping event ends. After a snap event
    /// has started (see #GESTimeline::snapping-started), it can later end
    /// because either another timeline edit has occurred (which may or may
    /// not have created a new snapping event), or because the timeline has
    /// been committed.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Timeline.SnappingEndedSignalArgs> SnappingEnded
    {
        add => GES.SignalConnections.Add(this, "snapping-ended", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, ulong, nint, void>)&SnappingEndedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "snapping-ended", value);
    }

    /// <summary>The native handler of the <c>snapping-ended</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void SnappingEndedTrampoline(nint instance, nint obj1, nint obj2, ulong position, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Timeline.SnappingEndedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.TrackElement obj1Value = Gst.GObject.Object.FromNative<GES.TrackElement>(obj1, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The snapping-ended signal of GESTimeline passed no obj1.");
            GES.TrackElement obj2Value = Gst.GObject.Object.FromNative<GES.TrackElement>(obj2, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The snapping-ended signal of GESTimeline passed no obj2.");
            ulong positionValue = position;
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Timeline.SnappingEndedSignalArgs(obj1Value, obj2Value, positionValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>snapping-started</c> signal of <c>GESTimeline</c>.</summary>
    public sealed class SnappingStartedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="SnappingStartedSignalArgs"/> class.</summary>
        /// <param name="obj1">The first element that is snapping</param>
        /// <param name="obj2">The second element that is snapping</param>
        /// <param name="position">The position where the two objects will snap to</param>
        internal SnappingStartedSignalArgs(GES.TrackElement obj1, GES.TrackElement obj2, ulong position)
        {
            Obj1 = obj1;
            Obj2 = obj2;
            Position = position;
        }

        /// <summary>The first element that is snapping</summary>
        public GES.TrackElement Obj1 { get; }

        /// <summary>The second element that is snapping</summary>
        public GES.TrackElement Obj2 { get; }

        /// <summary>The position where the two objects will snap to</summary>
        public ulong Position { get; }
    }

    /// <summary>
    /// Will be emitted whenever an element's movement invokes a snapping
    /// event during an edit (usually of one of its ancestors) because its
    /// start or end point lies within the #GESTimeline:snapping-distance of
    /// another element's start or end point.
    /// </summary>
    /// <remarks>
    /// <para>See #GESEditMode to see what can snap during an edit.</para>
    /// <para>
    /// Note that only up to one snapping-started signal will be emitted per
    /// element edit within a timeline.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Timeline.SnappingStartedSignalArgs> SnappingStarted
    {
        add => GES.SignalConnections.Add(this, "snapping-started", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, ulong, nint, void>)&SnappingStartedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "snapping-started", value);
    }

    /// <summary>The native handler of the <c>snapping-started</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void SnappingStartedTrampoline(nint instance, nint obj1, nint obj2, ulong position, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Timeline.SnappingStartedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.TrackElement obj1Value = Gst.GObject.Object.FromNative<GES.TrackElement>(obj1, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The snapping-started signal of GESTimeline passed no obj1.");
            GES.TrackElement obj2Value = Gst.GObject.Object.FromNative<GES.TrackElement>(obj2, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The snapping-started signal of GESTimeline passed no obj2.");
            ulong positionValue = position;
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Timeline.SnappingStartedSignalArgs(obj1Value, obj2Value, positionValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>track-added</c> signal of <c>GESTimeline</c>.</summary>
    public sealed class TrackAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="TrackAddedSignalArgs"/> class.</summary>
        /// <param name="track">The track that was added to @timeline</param>
        internal TrackAddedSignalArgs(GES.Track track)
        {
            Track = track;
        }

        /// <summary>The track that was added to @timeline</summary>
        public GES.Track Track { get; }
    }

    /// <summary>Will be emitted after the track is added to the timeline.</summary>
    /// <remarks>
    /// <para>
    /// Note that this should not be emitted whilst a timeline is being
    /// loaded from its #GESProject asset. You should connect to the
    /// project's #GESProject::loaded signal if you want to know which
    /// tracks were created for the timeline.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Timeline.TrackAddedSignalArgs> TrackAdded
    {
        add => GES.SignalConnections.Add(this, "track-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&TrackAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "track-added", value);
    }

    /// <summary>The native handler of the <c>track-added</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void TrackAddedTrampoline(nint instance, nint track, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Timeline.TrackAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Track trackValue = Gst.GObject.Object.FromNative<GES.Track>(track, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The track-added signal of GESTimeline passed no track.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Timeline.TrackAddedSignalArgs(trackValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>track-removed</c> signal of <c>GESTimeline</c>.</summary>
    public sealed class TrackRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="TrackRemovedSignalArgs"/> class.</summary>
        /// <param name="track">The track that was removed from @timeline</param>
        internal TrackRemovedSignalArgs(GES.Track track)
        {
            Track = track;
        }

        /// <summary>The track that was removed from @timeline</summary>
        public GES.Track Track { get; }
    }

    /// <summary>Will be emitted after the track is removed from the timeline.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Timeline.TrackRemovedSignalArgs> TrackRemoved
    {
        add => GES.SignalConnections.Add(this, "track-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&TrackRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "track-removed", value);
    }

    /// <summary>The native handler of the <c>track-removed</c> signal of <c>GESTimeline</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void TrackRemovedTrampoline(nint instance, nint track, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Timeline.TrackRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Track trackValue = Gst.GObject.Object.FromNative<GES.Track>(track, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The track-removed signal of GESTimeline passed no track.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Timeline.TrackRemovedSignalArgs(trackValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>ges_timeline_new</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_new")]
    private static partial nint GesTimelineNew();

    /// <summary>The <c>ges_timeline_new_audio_video</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_new_audio_video")]
    private static partial nint GesTimelineNewAudioVideo();

    /// <summary>The <c>ges_timeline_new_from_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_new_from_uri")]
    private static partial nint GesTimelineNewFromUri(byte* uri, nint* error);

    /// <summary>The <c>ges_timeline_add_layer</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_add_layer")]
    private static partial int GesTimelineAddLayer(nint timeline, nint layer);

    /// <summary>The <c>ges_timeline_add_track</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_add_track")]
    private static partial int GesTimelineAddTrack(nint timeline, nint track);

    /// <summary>The <c>ges_timeline_append_layer</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_append_layer")]
    private static partial nint GesTimelineAppendLayer(nint timeline);

    /// <summary>The <c>ges_timeline_commit</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_commit")]
    private static partial int GesTimelineCommit(nint timeline);

    /// <summary>The <c>ges_timeline_commit_sync</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_commit_sync")]
    private static partial int GesTimelineCommitSync(nint timeline);

    /// <summary>The <c>ges_timeline_disable_edit_apis</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_disable_edit_apis")]
    private static partial void GesTimelineDisableEditApis(nint self, int disableEditApis);

    /// <summary>The <c>ges_timeline_freeze_commit</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_freeze_commit")]
    private static partial void GesTimelineFreezeCommit(nint timeline);

    /// <summary>The <c>ges_timeline_get_auto_transition</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_auto_transition")]
    private static partial int GesTimelineGetAutoTransition(nint timeline);

    /// <summary>The <c>ges_timeline_get_duration</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_duration")]
    private static partial ulong GesTimelineGetDuration(nint timeline);

    /// <summary>The <c>ges_timeline_get_edit_apis_disabled</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_edit_apis_disabled")]
    private static partial int GesTimelineGetEditApisDisabled(nint self);

    /// <summary>The <c>ges_timeline_get_element</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_element")]
    private static partial nint GesTimelineGetElement(nint timeline, byte* name);

    /// <summary>The <c>ges_timeline_get_frame_at</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_frame_at")]
    private static partial long GesTimelineGetFrameAt(nint self, ulong timestamp);

    /// <summary>The <c>ges_timeline_get_frame_time</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_frame_time")]
    private static partial ulong GesTimelineGetFrameTime(nint self, long frameNumber);

    /// <summary>The <c>ges_timeline_get_groups</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_groups")]
    private static partial nint GesTimelineGetGroups(nint timeline);

    /// <summary>The <c>ges_timeline_get_layer</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_layer")]
    private static partial nint GesTimelineGetLayer(nint timeline, uint priority);

    /// <summary>The <c>ges_timeline_get_layers</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_layers")]
    private static partial nint GesTimelineGetLayers(nint timeline);

    /// <summary>The <c>ges_timeline_get_pad_for_track</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_pad_for_track")]
    private static partial nint GesTimelineGetPadForTrack(nint timeline, nint track);

    /// <summary>The <c>ges_timeline_get_snapping_distance</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_snapping_distance")]
    private static partial ulong GesTimelineGetSnappingDistance(nint timeline);

    /// <summary>The <c>ges_timeline_get_track_for_pad</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_track_for_pad")]
    private static partial nint GesTimelineGetTrackForPad(nint timeline, nint pad);

    /// <summary>The <c>ges_timeline_get_tracks</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_tracks")]
    private static partial nint GesTimelineGetTracks(nint timeline);

    /// <summary>The <c>ges_timeline_is_empty</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_is_empty")]
    private static partial int GesTimelineIsEmpty(nint timeline);

    /// <summary>The <c>ges_timeline_load_from_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_load_from_uri")]
    private static partial int GesTimelineLoadFromUri(nint timeline, byte* uri, nint* error);

    /// <summary>The <c>ges_timeline_move_layer</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_move_layer")]
    private static partial int GesTimelineMoveLayer(nint timeline, nint layer, uint newLayerPriority);

    /// <summary>The <c>ges_timeline_paste_element</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_paste_element")]
    private static partial nint GesTimelinePasteElement(nint timeline, nint element, ulong position, int layerPriority);

    /// <summary>The <c>ges_timeline_remove_layer</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_remove_layer")]
    private static partial int GesTimelineRemoveLayer(nint timeline, nint layer);

    /// <summary>The <c>ges_timeline_remove_track</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_remove_track")]
    private static partial int GesTimelineRemoveTrack(nint timeline, nint track);

    /// <summary>The <c>ges_timeline_save_to_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_save_to_uri")]
    private static partial int GesTimelineSaveToUri(nint timeline, byte* uri, nint formatterAsset, int overwrite, nint* error);

    /// <summary>The <c>ges_timeline_set_auto_transition</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_set_auto_transition")]
    private static partial void GesTimelineSetAutoTransition(nint timeline, int autoTransition);

    /// <summary>The <c>ges_timeline_set_snapping_distance</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_set_snapping_distance")]
    private static partial void GesTimelineSetSnappingDistance(nint timeline, ulong snappingDistance);

    /// <summary>The <c>ges_timeline_thaw_commit</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_thaw_commit")]
    private static partial void GesTimelineThawCommit(nint timeline);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESTimeline</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_timeline_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Timeline(handle, transfer);
}
