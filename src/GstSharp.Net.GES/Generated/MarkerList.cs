// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>A #GESMarker can be colored by setting the #GES_META_MARKER_COLOR meta.</summary>
public unsafe partial class MarkerList : Gst.GObject.Object
{
    /// <summary>Wraps a native <c>GESMarkerList</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESMarkerList</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected MarkerList(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new #GESMarkerList.</summary>
    /// <returns>A new #GESMarkerList</returns>
    public static GES.MarkerList New()
    {
        nint nativeResult = GesMarkerListNew();
        return Gst.GObject.Object.FromNative<GES.MarkerList>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("ges_marker_list_new returned no value.");
    }

    /// <summary>The <c>ges_marker_list_add</c> function.</summary>
    /// <param name="position">The <c>position</c> argument.</param>
    /// <returns>
    /// The newly-added marker, the list keeps ownership
    /// of the marker
    /// </returns>
    public GES.Marker Add(Gst.ClockTime position)
    {
        nint nativeResult = GesMarkerListAdd(Handle, position.Nanoseconds);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.Marker>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_marker_list_add returned no value.");
    }

    /// <summary>The <c>ges_marker_list_get_markers</c> function.</summary>
    /// <returns>
    /// a #GList
    /// of the #GESMarker within the GESMarkerList. The user will have
    /// to unref each #GESMarker and free the #GList.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Marker> GetMarkers()
    {
        nint nativeResult = GesMarkerListGetMarkers(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.Marker> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Marker>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Moves a @marker in a @list to a new @position</summary>
    /// <param name="marker">The <c>marker</c> argument.</param>
    /// <param name="position">The <c>position</c> argument.</param>
    /// <returns>
    /// %TRUE if the marker could be moved, %FALSE otherwise
    ///   (if the marker was not present in the list for example)
    /// </returns>
    public bool Move(GES.Marker marker, Gst.ClockTime position)
    {
        ArgumentNullException.ThrowIfNull(marker);
        int nativeResult = GesMarkerListMove(Handle, marker.Handle, position.Nanoseconds);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(marker);
        return nativeResult != 0;
    }

    /// <summary>
    /// Removes @marker from @list, this decreases the refcount of the
    /// marker by 1.
    /// </summary>
    /// <param name="marker">The <c>marker</c> argument.</param>
    /// <returns>
    /// %TRUE if the marker could be removed, %FALSE otherwise
    ///   (if the marker was not present in the list for example)
    /// </returns>
    public bool Remove(GES.Marker marker)
    {
        ArgumentNullException.ThrowIfNull(marker);
        int nativeResult = GesMarkerListRemove(Handle, marker.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(marker);
        return nativeResult != 0;
    }

    /// <summary>The <c>ges_marker_list_size</c> function.</summary>
    /// <returns>The number of markers in @list</returns>
    public uint Size()
    {
        uint nativeResult = GesMarkerListSize(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>The arguments of the <c>marker-added</c> signal of <c>GESMarkerList</c>.</summary>
    public sealed class MarkerAddedSignalArgs
    {
        /// <summary>Initializes a new instance of the <see cref="MarkerAddedSignalArgs"/> class.</summary>
        /// <param name="position">the position of the added marker</param>
        /// <param name="marker">the #GESMarker that was added.</param>
        internal MarkerAddedSignalArgs(ulong position, GES.Marker marker)
        {
            Position = position;
            Marker = marker;
        }

        /// <summary>the position of the added marker</summary>
        public ulong Position { get; }

        /// <summary>the #GESMarker that was added.</summary>
        public GES.Marker Marker { get; }
    }

    /// <summary>Will be emitted after the marker was added to the marker-list.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.MarkerList.MarkerAddedSignalArgs> MarkerAdded
    {
        add => GES.SignalConnections.Add(this, "marker-added", (nint)(delegate* unmanaged[Cdecl]<nint, ulong, nint, nint, void>)&MarkerAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "marker-added", value);
    }

    /// <summary>The native handler of the <c>marker-added</c> signal of <c>GESMarkerList</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void MarkerAddedTrampoline(nint instance, ulong position, nint marker, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.MarkerList.MarkerAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            ulong positionValue = position;
            GES.Marker markerValue = Gst.GObject.Object.FromNative<GES.Marker>(marker, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The marker-added signal of GESMarkerList passed no marker.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.MarkerList.MarkerAddedSignalArgs(positionValue, markerValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>marker-moved</c> signal of <c>GESMarkerList</c>.</summary>
    public sealed class MarkerMovedSignalArgs
    {
        /// <summary>Initializes a new instance of the <see cref="MarkerMovedSignalArgs"/> class.</summary>
        /// <param name="previousPosition">the previous position of the marker</param>
        /// <param name="newPosition">the new position of the marker</param>
        /// <param name="marker">the #GESMarker that was moved.</param>
        internal MarkerMovedSignalArgs(ulong previousPosition, ulong newPosition, GES.Marker marker)
        {
            PreviousPosition = previousPosition;
            NewPosition = newPosition;
            Marker = marker;
        }

        /// <summary>the previous position of the marker</summary>
        public ulong PreviousPosition { get; }

        /// <summary>the new position of the marker</summary>
        public ulong NewPosition { get; }

        /// <summary>the #GESMarker that was moved.</summary>
        public GES.Marker Marker { get; }
    }

    /// <summary>Will be emitted after the marker was moved to.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.MarkerList.MarkerMovedSignalArgs> MarkerMoved
    {
        add => GES.SignalConnections.Add(this, "marker-moved", (nint)(delegate* unmanaged[Cdecl]<nint, ulong, ulong, nint, nint, void>)&MarkerMovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "marker-moved", value);
    }

    /// <summary>The native handler of the <c>marker-moved</c> signal of <c>GESMarkerList</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void MarkerMovedTrampoline(nint instance, ulong previousPosition, ulong newPosition, nint marker, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.MarkerList.MarkerMovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            ulong previousPositionValue = previousPosition;
            ulong newPositionValue = newPosition;
            GES.Marker markerValue = Gst.GObject.Object.FromNative<GES.Marker>(marker, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The marker-moved signal of GESMarkerList passed no marker.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.MarkerList.MarkerMovedSignalArgs(previousPositionValue, newPositionValue, markerValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>marker-removed</c> signal of <c>GESMarkerList</c>.</summary>
    public sealed class MarkerRemovedSignalArgs
    {
        /// <summary>Initializes a new instance of the <see cref="MarkerRemovedSignalArgs"/> class.</summary>
        /// <param name="marker">the #GESMarker that was removed.</param>
        internal MarkerRemovedSignalArgs(GES.Marker marker)
        {
            Marker = marker;
        }

        /// <summary>the #GESMarker that was removed.</summary>
        public GES.Marker Marker { get; }
    }

    /// <summary>Will be emitted after the marker was removed the marker-list.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.MarkerList.MarkerRemovedSignalArgs> MarkerRemoved
    {
        add => GES.SignalConnections.Add(this, "marker-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&MarkerRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "marker-removed", value);
    }

    /// <summary>The native handler of the <c>marker-removed</c> signal of <c>GESMarkerList</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void MarkerRemovedTrampoline(nint instance, nint marker, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.MarkerList.MarkerRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Marker markerValue = Gst.GObject.Object.FromNative<GES.Marker>(marker, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The marker-removed signal of GESMarkerList passed no marker.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.MarkerList.MarkerRemovedSignalArgs(markerValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>ges_marker_list_new</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_marker_list_new")]
    private static partial nint GesMarkerListNew();

    /// <summary>The <c>ges_marker_list_add</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_marker_list_add")]
    private static partial nint GesMarkerListAdd(nint list, ulong position);

    /// <summary>The <c>ges_marker_list_get_markers</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_marker_list_get_markers")]
    private static partial nint GesMarkerListGetMarkers(nint list);

    /// <summary>The <c>ges_marker_list_move</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_marker_list_move")]
    private static partial int GesMarkerListMove(nint list, nint marker, ulong position);

    /// <summary>The <c>ges_marker_list_remove</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_marker_list_remove")]
    private static partial int GesMarkerListRemove(nint list, nint marker);

    /// <summary>The <c>ges_marker_list_size</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_marker_list_size")]
    private static partial uint GesMarkerListSize(nint list);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESMarkerList</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_marker_list_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new MarkerList(handle, transfer);
}
