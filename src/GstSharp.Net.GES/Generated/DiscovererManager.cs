// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>The <c>GESDiscovererManager</c> class.</summary>
public unsafe partial class DiscovererManager : Gst.GObject.Object
{
    /// <summary>Wraps a native <c>GESDiscovererManager</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESDiscovererManager</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected DiscovererManager(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>The <c>ges_discoverer_manager_get_timeout</c> function.</summary>
    /// <returns>The timeout to use for the discoverer</returns>
    public Gst.ClockTime GetTimeout()
    {
        ulong nativeResult = GesDiscovererManagerGetTimeout(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>The <c>ges_discoverer_manager_get_use_cache</c> function.</summary>
    /// <returns>Whether to use the cache or not</returns>
    public bool GetUseCache()
    {
        int nativeResult = GesDiscovererManagerGetUseCache(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Sets the timeout to use for the discoverer</summary>
    /// <param name="timeout">The timeout to set</param>
    public void SetTimeout(Gst.ClockTime timeout)
    {
        GesDiscovererManagerSetTimeout(Handle, timeout.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets whether to use the cache or not</summary>
    /// <param name="useCache">Whether to use the cache</param>
    public void SetUseCache(bool useCache)
    {
        GesDiscovererManagerSetUseCache(Handle, useCache ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>ges_discoverer_manager_get_default</c> function.</summary>
    /// <returns>The default #GESDiscovererManager</returns>
    public static GES.DiscovererManager GetDefault()
    {
        nint nativeResult = GesDiscovererManagerGetDefault();
        return Gst.GObject.Object.FromNative<GES.DiscovererManager>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("ges_discoverer_manager_get_default returned no value.");
    }

    /// <summary>The timeout (in milliseconds) for the #GstDiscoverer operations</summary>
    public Gst.ClockTime Timeout
    {
        get => GetTimeout();
        set => SetTimeout(value);
    }

    /// <summary>The <c>use-cache</c> property.</summary>
    public bool UseCache
    {
        get => GetUseCache();
        set => SetUseCache(value);
    }

    /// <summary>The arguments of the <c>discovered</c> signal of <c>GESDiscovererManager</c>.</summary>
    public sealed class DiscoveredSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="DiscoveredSignalArgs"/> class.</summary>
        /// <param name="info">The #GstDiscovererInfo representing the discovered URI</param>
        /// <param name="error">The #GError that occurred, or %NULL</param>
        internal DiscoveredSignalArgs(Gst.Pbutils.DiscovererInfo info, Gst.GLib.GException? error)
        {
            Info = info;
            Error = error;
        }

        /// <summary>The #GstDiscovererInfo representing the discovered URI</summary>
        public Gst.Pbutils.DiscovererInfo Info { get; }

        /// <summary>The #GError that occurred, or %NULL</summary>
        public Gst.GLib.GException? Error { get; }
    }

    /// <summary>Raised for the <c>discovered</c> signal of <c>GESDiscovererManager</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.DiscovererManager.DiscoveredSignalArgs> Discovered
    {
        add => GES.SignalConnections.Add(this, "discovered", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&DiscoveredTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "discovered", value);
    }

    /// <summary>The native handler of the <c>discovered</c> signal of <c>GESDiscovererManager</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void DiscoveredTrampoline(nint instance, nint info, nint error, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.DiscovererManager.DiscoveredSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Pbutils.DiscovererInfo infoValue = Gst.GObject.Object.FromNative<Gst.Pbutils.DiscovererInfo>(info, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The discovered signal of GESDiscovererManager passed no info.");
            Gst.GLib.GException? errorValue = Gst.GLib.GException.FromBorrowed(error);
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.DiscovererManager.DiscoveredSignalArgs(infoValue, errorValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>load-serialized-info</c> signal of <c>GESDiscovererManager</c>.</summary>
    public sealed class LoadSerializedInfoSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="LoadSerializedInfoSignalArgs"/> class.</summary>
        /// <param name="uri">The URI to load the serialized info for</param>
        internal LoadSerializedInfoSignalArgs(string uri)
        {
            Uri = uri;
        }

        /// <summary>The URI to load the serialized info for</summary>
        public string Uri { get; }
    }

    /// <summary>The handler of the <c>load-serialized-info</c> signal of <c>GESDiscovererManager</c>.</summary>
    /// <remarks>
    /// The value the handler returns is handed to native code with a reference
    /// minted for it, so the wrapper the handler holds stays usable and is still
    /// the caller's to manage. Returning <see langword="null"/> answers no value,
    /// and what the emission makes of that is the contract of the signal, stated
    /// in its own returns documentation: it may go on to another handler, to the
    /// class handler, or read the empty answer as the result. That choice belongs
    /// to the emitting library and can differ between GStreamer versions.
    /// </remarks>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>
    /// The #GstDiscovererInfo representing
    /// @uri, or %NULL if no information
    /// </returns>
    public delegate Gst.Pbutils.DiscovererInfo? LoadSerializedInfoHandler(object? sender, GES.DiscovererManager.LoadSerializedInfoSignalArgs args);

    /// <summary>
    /// Retrieves information about a URI from and external source of information,
    /// like a cache file. This is used by the discoverer to speed up the
    /// discovery.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event GES.DiscovererManager.LoadSerializedInfoHandler LoadSerializedInfo
    {
        add => GES.SignalConnections.Add(this, "load-serialized-info", (nint)(delegate* unmanaged[Cdecl]<nint, byte*, nint, nint>)&LoadSerializedInfoTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "load-serialized-info", value);
    }

    /// <summary>The native handler of the <c>load-serialized-info</c> signal of <c>GESDiscovererManager</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static nint LoadSerializedInfoTrampoline(nint instance, byte* uri, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<GES.DiscovererManager.LoadSerializedInfoHandler>(userData) is not { } handler)
            {
                return default;
            }

            string uriValue = Gst.Interop.GMarshal.PtrToStringUtf8((nint)uri)
                ?? throw new InvalidOperationException("The load-serialized-info signal of GESDiscovererManager passed no uri.");
            Gst.Pbutils.DiscovererInfo? result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.DiscovererManager.LoadSerializedInfoSignalArgs(uriValue));
            nint owned = result is null ? 0 : Gst.Interop.GObjectNative.ObjectRef(result.Handle);
            System.GC.KeepAlive(result);
            return owned;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The arguments of the <c>source-setup</c> signal of <c>GESDiscovererManager</c>.</summary>
    public sealed class SourceSetupSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="SourceSetupSignalArgs"/> class.</summary>
        /// <param name="source">The source #GstElement to setup</param>
        internal SourceSetupSignalArgs(Gst.Element source)
        {
            Source = source;
        }

        /// <summary>The source #GstElement to setup</summary>
        public Gst.Element Source { get; }
    }

    /// <summary>Allows to setup the source element before the discoverer runs.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.DiscovererManager.SourceSetupSignalArgs> SourceSetup
    {
        add => GES.SignalConnections.Add(this, "source-setup", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&SourceSetupTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "source-setup", value);
    }

    /// <summary>The native handler of the <c>source-setup</c> signal of <c>GESDiscovererManager</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void SourceSetupTrampoline(nint instance, nint source, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.DiscovererManager.SourceSetupSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Element sourceValue = Gst.GObject.Object.FromNative<Gst.Element>(source, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The source-setup signal of GESDiscovererManager passed no source.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.DiscovererManager.SourceSetupSignalArgs(sourceValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>ges_discoverer_manager_get_timeout</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_discoverer_manager_get_timeout")]
    private static partial ulong GesDiscovererManagerGetTimeout(nint self);

    /// <summary>The <c>ges_discoverer_manager_get_use_cache</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_discoverer_manager_get_use_cache")]
    private static partial int GesDiscovererManagerGetUseCache(nint self);

    /// <summary>The <c>ges_discoverer_manager_set_timeout</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_discoverer_manager_set_timeout")]
    private static partial void GesDiscovererManagerSetTimeout(nint self, ulong timeout);

    /// <summary>The <c>ges_discoverer_manager_set_use_cache</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_discoverer_manager_set_use_cache")]
    private static partial void GesDiscovererManagerSetUseCache(nint self, int useCache);

    /// <summary>The <c>ges_discoverer_manager_get_default</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_discoverer_manager_get_default")]
    private static partial nint GesDiscovererManagerGetDefault();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESDiscovererManager</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_discoverer_manager_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new DiscovererManager(handle, transfer);
}
