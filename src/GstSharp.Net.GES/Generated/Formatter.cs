// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>Base class for timeline data serialization and deserialization.</summary>
public abstract unsafe partial class Formatter : Gst.GObject.InitiallyUnowned, GES.IExtractable
{
    /// <summary>Wraps a native <c>GESFormatter</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESFormatter</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Formatter(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Load data from the given URI into timeline.</summary>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>
    /// TRUE if the timeline data was successfully loaded from the URI,
    /// else FALSE.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    [Obsolete("Use @ges_timeline_load_from_uri (deprecated since 1.18)")]
    public bool LoadFromUri(GES.Timeline timeline, string uri)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        int nativeResult = GesFormatterLoadFromUri(Handle, timeline.Handle, uriScope.Pointer, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>Save data from timeline to the given URI.</summary>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <param name="overwrite">The <c>overwrite</c> argument.</param>
    /// <returns>
    /// TRUE if the timeline data was successfully saved to the URI
    /// else FALSE.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    [Obsolete("Use @ges_timeline_save_to_uri (deprecated since 1.18)")]
    public bool SaveToUri(GES.Timeline timeline, string uri, bool overwrite)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        int nativeResult = GesFormatterSaveToUri(Handle, timeline.Handle, uriScope.Pointer, overwrite ? 1 : 0, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks if there is a #GESFormatter available which can load a #GESTimeline
    /// from the given URI.
    /// </summary>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>
    /// TRUE if there is a #GESFormatter that can support the given uri
    /// or FALSE if not.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public static bool CanLoadUri(string uri)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        int nativeResult = GesFormatterCanLoadUri(uriScope.Pointer, &errorNative);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns TRUE if there is a #GESFormatter available which can save a
    /// #GESTimeline to the given URI.
    /// </summary>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>TRUE if the given @uri is supported, else FALSE.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public static bool CanSaveUri(string uri)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint errorNative = 0;
        int nativeResult = GesFormatterCanSaveUri(uriScope.Pointer, &errorNative);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Get the default #GESAsset to use as formatter. It will return
    /// the asset for the #GESFormatter that has the highest @rank
    /// </summary>
    /// <returns>The #GESAsset for the formatter with highest @rank</returns>
    public static GES.Asset GetDefault()
    {
        nint nativeResult = GesFormatterGetDefault();
        return Gst.GObject.Object.FromNative<GES.Asset>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_formatter_get_default returned no value.");
    }

    /// <summary>The <c>ges_formatter_load_from_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_formatter_load_from_uri")]
    private static partial int GesFormatterLoadFromUri(nint formatter, nint timeline, byte* uri, nint* error);

    /// <summary>The <c>ges_formatter_save_to_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_formatter_save_to_uri")]
    private static partial int GesFormatterSaveToUri(nint formatter, nint timeline, byte* uri, int overwrite, nint* error);

    /// <summary>The <c>ges_formatter_can_load_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_formatter_can_load_uri")]
    private static partial int GesFormatterCanLoadUri(byte* uri, nint* error);

    /// <summary>The <c>ges_formatter_can_save_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_formatter_can_save_uri")]
    private static partial int GesFormatterCanSaveUri(byte* uri, nint* error);

    /// <summary>The <c>ges_formatter_get_default</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_formatter_get_default")]
    private static partial nint GesFormatterGetDefault();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESFormatter</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_formatter_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GESFormatter</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : Formatter
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
