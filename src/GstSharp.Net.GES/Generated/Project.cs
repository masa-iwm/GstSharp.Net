// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>
/// The #GESProject is used to control a set of #GESAsset and is a
/// #GESAsset with `GES_TYPE_TIMELINE` as @extractable_type itself. That
/// means that you can extract #GESTimeline from a project as followed:
/// </summary>
/// <remarks>
/// <para>
/// ```c
/// GESProject *project;
/// GESTimeline *timeline;
/// </para>
/// <para>project = ges_project_new ("file:///path/to/a/valid/project/uri");</para>
/// <para>
/// // Here you can connect to the various signal to get more infos about
/// // what is happening and recover from errors if possible
/// ...
/// </para>
/// <para>
/// timeline = ges_asset_extract (GES_ASSET (project));
/// ```
/// </para>
/// <para>
/// The #GESProject class offers a higher level API to handle #GESAsset-s.
/// It lets you request new asset, and it informs you about new assets through
/// a set of signals. Also it handles problem such as missing files/missing
/// #GstElement and lets you try to recover from those.
/// </para>
/// <para>## Subprojects</para>
/// <para>
/// In order to add a subproject, the only thing to do is to add the subproject
/// to the main project:
/// </para>
/// <para>
/// ``` c
/// ges_project_add_asset (project, GES_ASSET (subproject));
/// ```
/// then the subproject will be serialized in the project files. To use
/// the subproject in a timeline, you should use a #GESUriClip with the
/// same subproject URI.
/// </para>
/// <para>
/// When loading a project with subproject, subprojects URIs will be temporary
/// writable local files. If you want to edit the subproject timeline,
/// you should retrieve the subproject from the parent project asset list and
/// extract the timeline with ges_asset_extract() and save it at
/// the same temporary location.
/// </para>
/// </remarks>
public unsafe partial class Project : GES.Asset, GES.IMetaContainer
{
    /// <summary>Wraps a native <c>GESProject</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESProject</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Project(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Creates a new #GESProject and sets its uri to @uri if provided. Note that
    /// if @uri is not valid or %NULL, the uri of the project will then be set
    /// the first time you save the project. If you then save the project to
    /// other locations, it will never be updated again and the first valid URI is
    /// the URI it will keep refering to.
    /// </summary>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>A newly created #GESProject</returns>
    public static GES.Project New(string? uri)
    {
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint nativeResult = GesProjectNew(uriScope.Pointer);
        return Gst.GObject.Object.FromNative<GES.Project>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("ges_project_new returned no value.");
    }

    /// <summary>
    /// Adds a #GESAsset to @project, the project will keep a reference on
    /// @asset.
    /// </summary>
    /// <param name="asset">The <c>asset</c> argument.</param>
    /// <returns>
    /// %TRUE if the asset could be added %FALSE it was already
    /// in the project.
    /// </returns>
    public bool AddAsset(GES.Asset asset)
    {
        ArgumentNullException.ThrowIfNull(asset);
        int nativeResult = GesProjectAddAsset(Handle, asset.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(asset);
        return nativeResult != 0;
    }

    /// <summary>
    /// Adds @profile to the project. It lets you save in what format
    /// the project will be rendered and keep a reference to those formats.
    /// Also, those formats will be saved to the project file when possible.
    /// </summary>
    /// <param name="profile">The <c>profile</c> argument.</param>
    /// <returns>%TRUE if @profile could be added, %FALSE otherwise</returns>
    public bool AddEncodingProfile(Gst.Pbutils.EncodingProfile profile)
    {
        ArgumentNullException.ThrowIfNull(profile);
        int nativeResult = GesProjectAddEncodingProfile(Handle, profile.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(profile);
        return nativeResult != 0;
    }

    /// <summary>Adds a formatter to be used to load @project</summary>
    /// <param name="formatter">The <c>formatter</c> argument.</param>
    public void AddFormatter(GES.Formatter formatter)
    {
        ArgumentNullException.ThrowIfNull(formatter);
        GesProjectAddFormatter(Handle, formatter.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(formatter);
    }

    /// <summary>
    /// Create and add a #GESAsset to @project. You should connect to the
    /// "asset-added" signal to get the asset when it finally gets added to
    /// @project
    /// </summary>
    /// <param name="id">The <c>id</c> argument.</param>
    /// <param name="extractableType">The <c>extractableType</c> argument.</param>
    /// <returns>
    /// %TRUE if the asset was added and started loading, %FALSE it was
    /// already in the project.
    /// </returns>
    public bool CreateAsset(string? id, Gst.GObject.GType extractableType)
    {
        System.Span<byte> idBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope idScope = Gst.Interop.GMarshal.StackUtf8(id, idBuffer);
        int nativeResult = GesProjectCreateAsset(Handle, idScope.Pointer, extractableType.Value);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Create and add a #GESAsset to @project. You should connect to the
    /// "asset-added" signal to get the asset when it finally gets added to
    /// @project
    /// </summary>
    /// <param name="id">The <c>id</c> argument.</param>
    /// <param name="extractableType">The <c>extractableType</c> argument.</param>
    /// <returns>The newly created #GESAsset or %NULL.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public GES.Asset? CreateAssetSync(string? id, Gst.GObject.GType extractableType)
    {
        System.Span<byte> idBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope idScope = Gst.Interop.GMarshal.StackUtf8(id, idBuffer);
        nint errorNative = 0;
        nint nativeResult = GesProjectCreateAssetSync(Handle, idScope.Pointer, extractableType.Value, &errorNative);
        System.GC.KeepAlive(this);
        if (errorNative != 0 && nativeResult != 0)
        {
            // The call failed and transferred a value all the same. The throw
            // below puts it out of reach, so it is released rather than leaked.
            Gst.Interop.GObjectNative.ObjectUnref(nativeResult);
        }
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return Gst.GObject.Object.FromNative<GES.Asset>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>ges_project_get_asset</c> function.</summary>
    /// <param name="id">The <c>id</c> argument.</param>
    /// <param name="extractableType">The <c>extractableType</c> argument.</param>
    /// <returns>
    /// The #GESAsset with
    /// @id or %NULL if no asset with @id as an ID
    /// </returns>
    public GES.Asset? GetAsset(string id, Gst.GObject.GType extractableType)
    {
        ArgumentNullException.ThrowIfNull(id);
        System.Span<byte> idBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope idScope = Gst.Interop.GMarshal.StackUtf8(id, idBuffer);
        nint nativeResult = GesProjectGetAsset(Handle, idScope.Pointer, extractableType.Value);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.Asset>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the assets that are being loaded</summary>
    /// <returns>
    /// A set of loading asset
    /// that will be added to @project. Note that those Asset are *not* loaded yet,
    /// and thus can not be used.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Asset> GetLoadingAssets()
    {
        nint nativeResult = GesProjectGetLoadingAssets(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.Asset> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Asset>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Retrieve the uri that is currently set on @project</summary>
    /// <returns>a newly allocated string representing uri.</returns>
    public string? GetUri()
    {
        nint nativeResult = GesProjectGetUri(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult);
    }

    /// <summary>
    /// List all @asset contained in @project filtering per extractable_type
    /// as defined by @filter. It copies the asset and thus will not be updated
    /// in time.
    /// </summary>
    /// <param name="filter">The <c>filter</c> argument.</param>
    /// <returns>
    /// The list of
    /// #GESAsset the object contains
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Asset> ListAssets(Gst.GObject.GType filter)
    {
        nint nativeResult = GesProjectListAssets(Handle, filter.Value);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.Asset> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Asset>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Lists the encoding profile that have been set to @project. The first one
    /// is the latest added.
    /// </summary>
    /// <returns>
    /// The
    /// list of #GstEncodingProfile used in @project
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.Pbutils.EncodingProfile> ListEncodingProfiles()
    {
        nint nativeResult = GesProjectListEncodingProfiles(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.Collect(nativeResult);
        System.Collections.Generic.List<Gst.Pbutils.EncodingProfile> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.Pbutils.EncodingProfile>(nativeItem, Gst.Interop.Transfer.None) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Loads @project into @timeline</summary>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    /// <returns>%TRUE if the project could be loaded %FALSE otherwise.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool Load(GES.Timeline timeline)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        nint errorNative = 0;
        int nativeResult = GesProjectLoad(Handle, timeline.Handle, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>Remove @asset from @project.</summary>
    /// <param name="asset">The <c>asset</c> argument.</param>
    /// <returns>%TRUE if the asset could be removed %FALSE otherwise</returns>
    public bool RemoveAsset(GES.Asset asset)
    {
        ArgumentNullException.ThrowIfNull(asset);
        int nativeResult = GesProjectRemoveAsset(Handle, asset.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(asset);
        return nativeResult != 0;
    }

    /// <summary>
    /// Save the timeline of @project to @uri. You should make sure that @timeline
    /// is one of the timelines that have been extracted from @project
    /// (using ges_asset_extract (@project);)
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>formatterAsset</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it. A
    /// GObject wrapper is interned, so disposing it gives the object up for the
    /// whole process rather than for one holder: after this call there is no
    /// wrapper for that object anywhere.
    /// <see cref="Gst.GObject.Object.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <param name="formatterAsset">
    /// The <c>formatterAsset</c> argument.
    /// The call consumes it: <paramref name="formatterAsset"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// It may be <see langword="null"/>, which is the absence of a payload and leaves
    /// nothing to consume.
    /// </param>
    /// <param name="overwrite">The <c>overwrite</c> argument.</param>
    /// <returns>%TRUE if the project could be save, %FALSE otherwise</returns>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="formatterAsset"/> was disposed.
    /// </exception>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool Save(GES.Timeline timeline, string uri, GES.Asset? formatterAsset, bool overwrite)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        ArgumentNullException.ThrowIfNull(uri);
        nint instanceHandle = Handle;
        nint timelineNative = timeline.Handle;
        nint formatterAssetNative = formatterAsset is null ? 0 : formatterAsset.Handle;
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint formatterAssetOwned = formatterAsset is null ? 0 : Gst.Interop.GObjectNative.ObjectRef(formatterAssetNative);
        nint errorNative = 0;
        int nativeResult = GesProjectSave(instanceHandle, timelineNative, uriScope.Pointer, formatterAssetOwned, overwrite ? 1 : 0, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
        formatterAsset?.Dispose();
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>The <c>uri</c> property.</summary>
    public string? Uri => GetUri();

    /// <summary>The arguments of the <c>asset-added</c> signal of <c>GESProject</c>.</summary>
    public sealed class AssetAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="AssetAddedSignalArgs"/> class.</summary>
        /// <param name="asset">The #GESAsset that has been added to @project</param>
        internal AssetAddedSignalArgs(GES.Asset asset)
        {
            Asset = asset;
        }

        /// <summary>The #GESAsset that has been added to @project</summary>
        public GES.Asset Asset { get; }
    }

    /// <summary>Raised for the <c>asset-added</c> signal of <c>GESProject</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Project.AssetAddedSignalArgs> AssetAdded
    {
        add => GES.SignalConnections.Add(this, "asset-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&AssetAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "asset-added", value);
    }

    /// <summary>The native handler of the <c>asset-added</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void AssetAddedTrampoline(nint instance, nint asset, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Project.AssetAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Asset assetValue = Gst.GObject.Object.FromNative<GES.Asset>(asset, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The asset-added signal of GESProject passed no asset.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.AssetAddedSignalArgs(assetValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>asset-loading</c> signal of <c>GESProject</c>.</summary>
    public sealed class AssetLoadingSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="AssetLoadingSignalArgs"/> class.</summary>
        /// <param name="asset">The #GESAsset that started loading</param>
        internal AssetLoadingSignalArgs(GES.Asset asset)
        {
            Asset = asset;
        }

        /// <summary>The #GESAsset that started loading</summary>
        public GES.Asset Asset { get; }
    }

    /// <summary>Raised for the <c>asset-loading</c> signal of <c>GESProject</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Project.AssetLoadingSignalArgs> AssetLoading
    {
        add => GES.SignalConnections.Add(this, "asset-loading", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&AssetLoadingTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "asset-loading", value);
    }

    /// <summary>The native handler of the <c>asset-loading</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void AssetLoadingTrampoline(nint instance, nint asset, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Project.AssetLoadingSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Asset assetValue = Gst.GObject.Object.FromNative<GES.Asset>(asset, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The asset-loading signal of GESProject passed no asset.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.AssetLoadingSignalArgs(assetValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>asset-removed</c> signal of <c>GESProject</c>.</summary>
    public sealed class AssetRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="AssetRemovedSignalArgs"/> class.</summary>
        /// <param name="asset">The #GESAsset that has been removed from @project</param>
        internal AssetRemovedSignalArgs(GES.Asset asset)
        {
            Asset = asset;
        }

        /// <summary>The #GESAsset that has been removed from @project</summary>
        public GES.Asset Asset { get; }
    }

    /// <summary>Raised for the <c>asset-removed</c> signal of <c>GESProject</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Project.AssetRemovedSignalArgs> AssetRemoved
    {
        add => GES.SignalConnections.Add(this, "asset-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&AssetRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "asset-removed", value);
    }

    /// <summary>The native handler of the <c>asset-removed</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void AssetRemovedTrampoline(nint instance, nint asset, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Project.AssetRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Asset assetValue = Gst.GObject.Object.FromNative<GES.Asset>(asset, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The asset-removed signal of GESProject passed no asset.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.AssetRemovedSignalArgs(assetValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>error-loading</c> signal of <c>GESProject</c>.</summary>
    public sealed class ErrorLoadingSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ErrorLoadingSignalArgs"/> class.</summary>
        /// <param name="timeline">The timeline that failed loading</param>
        /// <param name="error">The #GError defining the error that occured</param>
        internal ErrorLoadingSignalArgs(GES.Timeline timeline, Gst.GLib.GException error)
        {
            Timeline = timeline;
            Error = error;
        }

        /// <summary>The timeline that failed loading</summary>
        public GES.Timeline Timeline { get; }

        /// <summary>The #GError defining the error that occured</summary>
        public Gst.GLib.GException Error { get; }
    }

    /// <summary>Raised for the <c>error-loading</c> signal of <c>GESProject</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Project.ErrorLoadingSignalArgs> ErrorLoading
    {
        add => GES.SignalConnections.Add(this, "error-loading", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&ErrorLoadingTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "error-loading", value);
    }

    /// <summary>The native handler of the <c>error-loading</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ErrorLoadingTrampoline(nint instance, nint timeline, nint error, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Project.ErrorLoadingSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Timeline timelineValue = Gst.GObject.Object.FromNative<GES.Timeline>(timeline, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The error-loading signal of GESProject passed no timeline.");
            Gst.GLib.GException errorValue = Gst.GLib.GException.FromBorrowed(error)
                ?? throw new InvalidOperationException("The error-loading signal of GESProject passed no error.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.ErrorLoadingSignalArgs(timelineValue, errorValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>error-loading-asset</c> signal of <c>GESProject</c>.</summary>
    public sealed class ErrorLoadingAssetSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ErrorLoadingAssetSignalArgs"/> class.</summary>
        /// <param name="error">The #GError defining the error that occured, might be %NULL</param>
        /// <param name="id">The @id of the asset that failed loading</param>
        /// <param name="extractableType">
        /// The @extractable_type of the asset that
        /// failed loading
        /// </param>
        internal ErrorLoadingAssetSignalArgs(Gst.GLib.GException? error, string id, Gst.GObject.GType extractableType)
        {
            Error = error;
            Id = id;
            ExtractableType = extractableType;
        }

        /// <summary>The #GError defining the error that occured, might be %NULL</summary>
        public Gst.GLib.GException? Error { get; }

        /// <summary>The @id of the asset that failed loading</summary>
        public string Id { get; }

        /// <summary>
        /// The @extractable_type of the asset that
        /// failed loading
        /// </summary>
        public Gst.GObject.GType ExtractableType { get; }
    }

    /// <summary>
    /// Informs you that a #GESAsset could not be created. In case of
    /// missing GStreamer plugins, the error will be set to #GST_CORE_ERROR
    /// #GST_CORE_ERROR_MISSING_PLUGIN
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Project.ErrorLoadingAssetSignalArgs> ErrorLoadingAsset
    {
        add => GES.SignalConnections.Add(this, "error-loading-asset", (nint)(delegate* unmanaged[Cdecl]<nint, nint, byte*, nuint, nint, void>)&ErrorLoadingAssetTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "error-loading-asset", value);
    }

    /// <summary>The native handler of the <c>error-loading-asset</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ErrorLoadingAssetTrampoline(nint instance, nint error, byte* id, nuint extractableType, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Project.ErrorLoadingAssetSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.GLib.GException? errorValue = Gst.GLib.GException.FromBorrowed(error);
            string idValue = Gst.Interop.GMarshal.PtrToStringUtf8((nint)id)
                ?? throw new InvalidOperationException("The error-loading-asset signal of GESProject passed no id.");
            Gst.GObject.GType extractableTypeValue = new Gst.GObject.GType(extractableType);
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.ErrorLoadingAssetSignalArgs(errorValue, idValue, extractableTypeValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>loaded</c> signal of <c>GESProject</c>.</summary>
    public sealed class LoadedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="LoadedSignalArgs"/> class.</summary>
        /// <param name="timeline">The #GESTimeline that completed loading</param>
        internal LoadedSignalArgs(GES.Timeline timeline)
        {
            Timeline = timeline;
        }

        /// <summary>The #GESTimeline that completed loading</summary>
        public GES.Timeline Timeline { get; }
    }

    /// <summary>Raised for the <c>loaded</c> signal of <c>GESProject</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Project.LoadedSignalArgs> Loaded
    {
        add => GES.SignalConnections.Add(this, "loaded", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&LoadedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "loaded", value);
    }

    /// <summary>The native handler of the <c>loaded</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void LoadedTrampoline(nint instance, nint timeline, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Project.LoadedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Timeline timelineValue = Gst.GObject.Object.FromNative<GES.Timeline>(timeline, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The loaded signal of GESProject passed no timeline.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.LoadedSignalArgs(timelineValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>loading</c> signal of <c>GESProject</c>.</summary>
    public sealed class LoadingSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="LoadingSignalArgs"/> class.</summary>
        /// <param name="timeline">The #GESTimeline that started loading</param>
        internal LoadingSignalArgs(GES.Timeline timeline)
        {
            Timeline = timeline;
        }

        /// <summary>The #GESTimeline that started loading</summary>
        public GES.Timeline Timeline { get; }
    }

    /// <summary>Raised for the <c>loading</c> signal of <c>GESProject</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Project.LoadingSignalArgs> Loading
    {
        add => GES.SignalConnections.Add(this, "loading", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&LoadingTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "loading", value);
    }

    /// <summary>The native handler of the <c>loading</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void LoadingTrampoline(nint instance, nint timeline, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Project.LoadingSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Timeline timelineValue = Gst.GObject.Object.FromNative<GES.Timeline>(timeline, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The loading signal of GESProject passed no timeline.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.LoadingSignalArgs(timelineValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>missing-uri</c> signal of <c>GESProject</c>.</summary>
    public sealed class MissingUriSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="MissingUriSignalArgs"/> class.</summary>
        /// <param name="error">The error that happened</param>
        /// <param name="wrongAsset">
        /// The asset with the wrong ID, you should us it and its content
        /// only to find out what the new location is.
        /// </param>
        internal MissingUriSignalArgs(Gst.GLib.GException error, GES.Asset wrongAsset)
        {
            Error = error;
            WrongAsset = wrongAsset;
        }

        /// <summary>The error that happened</summary>
        public Gst.GLib.GException Error { get; }

        /// <summary>
        /// The asset with the wrong ID, you should us it and its content
        /// only to find out what the new location is.
        /// </summary>
        public GES.Asset WrongAsset { get; }
    }

    /// <summary>The handler of the <c>missing-uri</c> signal of <c>GESProject</c>.</summary>
    /// <remarks>
    /// The string the handler returns is copied into memory the emitting library
    /// owns and frees. Returning <see langword="null"/> answers no value, and what
    /// the emission makes of that is the contract of the signal, stated in its own
    /// returns documentation.
    /// </remarks>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>The new URI of @wrong_asset</returns>
    public delegate string? MissingUriHandler(object? sender, GES.Project.MissingUriSignalArgs args);

    /// <summary>
    /// ```c
    /// static gchar
    /// source_moved_cb (GESProject *project, GError *error, GESAsset *asset_with_error)
    /// {
    ///   return g_strdup ("file:///the/new/uri.ogg");
    /// }
    /// </summary>
    /// <remarks>
    /// <para>
    /// static int
    /// main (int argc, gchar ** argv)
    /// {
    ///   GESTimeline *timeline;
    ///   GESProject *project = ges_project_new ("file:///some/uri.xges");
    /// </para>
    /// <para>
    ///   g_signal_connect (project, "missing-uri", source_moved_cb, NULL);
    ///   timeline = ges_asset_extract (GES_ASSET (project));
    /// }
    /// ```
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event GES.Project.MissingUriHandler MissingUri
    {
        add => GES.SignalConnections.Add(this, "missing-uri", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, nint>)&MissingUriTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "missing-uri", value);
    }

    /// <summary>The native handler of the <c>missing-uri</c> signal of <c>GESProject</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static nint MissingUriTrampoline(nint instance, nint error, nint wrongAsset, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<GES.Project.MissingUriHandler>(userData) is not { } handler)
            {
                return default;
            }

            Gst.GLib.GException errorValue = Gst.GLib.GException.FromBorrowed(error)
                ?? throw new InvalidOperationException("The missing-uri signal of GESProject passed no error.");
            GES.Asset wrongAssetValue = Gst.GObject.Object.FromNative<GES.Asset>(wrongAsset, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The missing-uri signal of GESProject passed no wrong_asset.");
            string? result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Project.MissingUriSignalArgs(errorValue, wrongAssetValue));
            return Gst.Interop.GMarshal.StringToUtf8Ptr(result);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The <c>ges_project_new</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_new")]
    private static partial nint GesProjectNew(byte* uri);

    /// <summary>The <c>ges_project_add_asset</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_add_asset")]
    private static partial int GesProjectAddAsset(nint project, nint asset);

    /// <summary>The <c>ges_project_add_encoding_profile</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_add_encoding_profile")]
    private static partial int GesProjectAddEncodingProfile(nint project, nint profile);

    /// <summary>The <c>ges_project_add_formatter</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_add_formatter")]
    private static partial void GesProjectAddFormatter(nint project, nint formatter);

    /// <summary>The <c>ges_project_create_asset</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_create_asset")]
    private static partial int GesProjectCreateAsset(nint project, byte* id, nuint extractableType);

    /// <summary>The <c>ges_project_create_asset_sync</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_create_asset_sync")]
    private static partial nint GesProjectCreateAssetSync(nint project, byte* id, nuint extractableType, nint* error);

    /// <summary>The <c>ges_project_get_asset</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_get_asset")]
    private static partial nint GesProjectGetAsset(nint project, byte* id, nuint extractableType);

    /// <summary>The <c>ges_project_get_loading_assets</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_get_loading_assets")]
    private static partial nint GesProjectGetLoadingAssets(nint project);

    /// <summary>The <c>ges_project_get_uri</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_get_uri")]
    private static partial nint GesProjectGetUri(nint project);

    /// <summary>The <c>ges_project_list_assets</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_list_assets")]
    private static partial nint GesProjectListAssets(nint project, nuint filter);

    /// <summary>The <c>ges_project_list_encoding_profiles</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_list_encoding_profiles")]
    private static partial nint GesProjectListEncodingProfiles(nint project);

    /// <summary>The <c>ges_project_load</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_load")]
    private static partial int GesProjectLoad(nint project, nint timeline, nint* error);

    /// <summary>The <c>ges_project_remove_asset</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_remove_asset")]
    private static partial int GesProjectRemoveAsset(nint project, nint asset);

    /// <summary>The <c>ges_project_save</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_project_save")]
    private static partial int GesProjectSave(nint project, nint timeline, byte* uri, nint formatterAsset, int overwrite, nint* error);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESProject</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_project_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Project(handle, transfer);
}
