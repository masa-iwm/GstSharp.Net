// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>
/// A #GESTimelineElement will have some temporal extent in its
/// corresponding #GESTimelineElement:timeline, controlled by its
/// #GESTimelineElement:start and #GESTimelineElement:duration. This
/// determines when its content will be displayed, or its effect applied,
/// in the timeline. Several objects may overlap within a given
/// #GESTimeline, in which case their #GESTimelineElement:priority is used
/// to determine their ordering in the timeline. Priority is mostly handled
/// internally by #GESLayer-s and #GESClip-s.
/// </summary>
/// <remarks>
/// <para>
/// A timeline element can have a #GESTimelineElement:parent,
/// such as a #GESClip, which is responsible for controlling its timing.
/// </para>
/// <para>## Editing</para>
/// <para>
/// Elements can be moved around in their #GESTimelineElement:timeline by
/// setting their #GESTimelineElement:start and
/// #GESTimelineElement:duration using ges_timeline_element_set_start()
/// and ges_timeline_element_set_duration(). Additionally, which parts of
/// the underlying content are played in the timeline can be adjusted by
/// setting the #GESTimelineElement:in-point using
/// ges_timeline_element_set_inpoint(). The library also provides
/// ges_timeline_element_edit(), with various #GESEditMode-s, which can
/// adjust these properties in a convenient way, as well as introduce
/// similar changes in neighbouring or later elements in the timeline.
/// </para>
/// <para>
/// However, a timeline may refuse a change in these properties if they
/// would place the timeline in an unsupported configuration. See
/// #GESTimeline for its overlap rules.
/// </para>
/// <para>
/// Additionally, an edit may be refused if it would place one of the
/// timing properties out of bounds (such as a negative time value for
/// #GESTimelineElement:start, or having insufficient internal
/// content to last for the desired #GESTimelineElement:duration).
/// </para>
/// <para>## Time Coordinates</para>
/// <para>
/// There are three main sets of time coordinates to consider when using
/// timeline elements:
/// </para>
/// <para>
/// + Timeline coordinates: these are the time coordinates used in the
///   output of the timeline in its #GESTrack-s. Each track share the same
///   coordinates, so there is only one set of coordinates for the
///   timeline. These extend indefinitely from 0. The times used for
///   editing (including setting #GESTimelineElement:start and
///   #GESTimelineElement:duration) use these coordinates, since these
///   define when an element is present and for how long the element lasts
///   for in the timeline.
/// + Internal source coordinates: these are the time coordinates used
///   internally at the element's output. This is only really defined for
///   #GESTrackElement-s, where it refers to time coordinates used at the
///   final source pad of the wrapped #GstElement-s. However, these
///   coordinates may also be used in a #GESClip in reference to its
///   children. In particular, these are the coordinates used for
///   #GESTimelineElement:in-point and #GESTimelineElement:max-duration.
/// + Internal sink coordinates: these are the time coordinates used
///   internally at the element's input. A #GESSource has no input, so
///   these would be undefined. Otherwise, for most #GESTrackElement-s
///   these will be the same set of coordinates as the internal source
///   coordinates because the element does not change the timing
///   internally. Only #GESBaseEffect can support elements where these
///   are different. See #GESBaseEffect for more information.
/// </para>
/// <para>
/// You can determine the timeline time for a given internal source time
/// in a #GESTrack in a #GESClip using
/// ges_clip_get_timeline_time_from_internal_time(), and vice versa using
/// ges_clip_get_internal_time_from_timeline_time(), for the purposes of
/// editing and setting timings properties.
/// </para>
/// <para>## Children Properties</para>
/// <para>
/// If a timeline element owns another #GstObject and wishes to expose
/// some of its properties, it can do so by registering the property as one
/// of the timeline element's children properties using
/// ges_timeline_element_add_child_property(). The registered property of
/// the child can then be read and set using the
/// ges_timeline_element_get_child_property() and
/// ges_timeline_element_set_child_property() methods, respectively. Some
/// sub-classed objects will be created with pre-registered children
/// properties; for example, to expose part of an underlying #GstElement
/// that is used internally. The registered properties can be listed with
/// ges_timeline_element_list_children_properties().
/// </para>
/// </remarks>
public abstract unsafe partial class TimelineElement : Gst.GObject.InitiallyUnowned, GES.IExtractable, GES.IMetaContainer
{
    /// <summary>Wraps a native <c>GESTimelineElement</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESTimelineElement</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected TimelineElement(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Register a property of a child of the element to allow it to be
    /// written with ges_timeline_element_set_child_property() and read with
    /// ges_timeline_element_get_child_property(). A change in the property
    /// will also appear in the #GESTimelineElement::deep-notify signal.
    /// </summary>
    /// <remarks>
    /// <para>
    /// @pspec should be unique from other children properties that have been
    /// registered on @self.
    /// </para>
    /// </remarks>
    /// <param name="pspec">The specification for the property to add</param>
    /// <param name="child">The #GstObject who the property belongs to</param>
    /// <returns>%TRUE if the property was successfully registered.</returns>
    public bool AddChildProperty(Gst.GObject.ParamSpec pspec, Gst.GObject.Object child)
    {
        ArgumentNullException.ThrowIfNull(pspec);
        ArgumentNullException.ThrowIfNull(child);
        int nativeResult = GesTimelineElementAddChildProperty(Handle, pspec.Handle, child.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pspec);
        System.GC.KeepAlive(child);
        return nativeResult != 0;
    }

    /// <summary>
    /// Create a copy of @self. All the properties of @self are copied into
    /// a new element, with the exception of #GESTimelineElement:parent,
    /// #GESTimelineElement:timeline and #GESTimelineElement:name. Other data,
    /// such the list of a #GESContainer's children, is **not** copied.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @deep is %TRUE, then the new element is prepared so that it can be
    /// used in ges_timeline_element_paste() or ges_timeline_paste_element().
    /// In the case of copying a #GESContainer, this ensures that the children
    /// of @self will also be pasted. The new element should not be used for
    /// anything else and can only be used **once** in a pasting operation. In
    /// particular, the new element itself is not an actual 'deep' copy of
    /// @self, but should be thought of as an intermediate object used for a
    /// single paste operation.
    /// </para>
    /// </remarks>
    /// <param name="deep">Whether the copy is needed for pasting</param>
    /// <returns>The newly create element, copied from @self.</returns>
    public GES.TimelineElement Copy(bool deep)
    {
        nint nativeResult = GesTimelineElementCopy(Handle, deep ? 1 : 0);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.TimelineElement>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_timeline_element_copy returned no value.");
    }

    /// <summary>See ges_timeline_element_edit_full(), which also gives an error.</summary>
    /// <remarks>
    /// <para>
    /// Note that the @layers argument is currently ignored, so you should
    /// just pass %NULL.
    /// </para>
    /// <para>
    /// GStreamer ignores this list. ges_timeline_element_edit forwards to
    /// ges_timeline_element_edit_full with NULL and never reads it
    /// (ges-timeline-element.c:2533-2543, where the upstream FIXME says so), and the
    /// two deprecated wrappers forward to it (ges-container.c:1063-1070,
    /// ges-track-element.c:1823-1831). Pass null.
    /// </para>
    /// </remarks>
    /// <param name="layers">
    /// A whitelist of layers
    /// where the edit can be performed, %NULL allows all layers in the
    /// timeline.
    /// The call reads the list while it runs and copies whatever it keeps. A
    /// temporary native list is built for the call and released when it returns,
    /// and an empty sequence is passed as the null pointer, which is how C spells
    /// the empty list.
    /// </param>
    /// <param name="newLayerPriority">
    /// The priority/index of the layer @self should be
    /// moved to. -1 means no move
    /// </param>
    /// <param name="mode">The edit mode</param>
    /// <param name="edge">The edge of @self where the edit should occur</param>
    /// <param name="position">
    /// The edit position: a new location for the edge of @self
    /// (in nanoseconds) in the timeline coordinates
    /// </param>
    /// <returns>%TRUE if the edit of @self completed, %FALSE on failure.</returns>
    public bool Edit(System.Collections.Generic.IEnumerable<GES.Layer>? layers, long newLayerPriority, GES.EditMode mode, GES.Edge edge, ulong position)
    {
        using Gst.Interop.GListScope layersScope = Gst.Interop.GMarshal.AllocList(layers, singly: false);
        int nativeResult = GesTimelineElementEdit(Handle, layersScope.Head, newLayerPriority, (int)mode, (int)edge, position);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Edits the element within its timeline by adjusting its
    /// #GESTimelineElement:start, #GESTimelineElement:duration or
    /// #GESTimelineElement:in-point, and potentially doing the same for
    /// other elements in the timeline. See #GESEditMode for details about each
    /// edit mode. An edit may fail if it would place one of these properties
    /// out of bounds, or if it would place the timeline in an unsupported
    /// configuration.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that if you act on a #GESTrackElement, this will edit its parent
    /// #GESClip instead. Moreover, for any #GESTimelineElement, if you select
    /// #GES_EDGE_NONE for #GES_EDIT_MODE_NORMAL or #GES_EDIT_MODE_RIPPLE, this
    /// will edit the toplevel instead, but still in such a way as to make the
    /// #GESTimelineElement:start of @self reach the edit @position.
    /// </para>
    /// <para>
    /// Note that if the element's timeline has a
    /// #GESTimeline:snapping-distance set, then the edit position may be
    /// snapped to the edge of some element under the edited element.
    /// </para>
    /// <para>
    /// @new_layer_priority can be used to switch @self, and other elements
    /// moved by the edit, to a new layer. New layers may be be created if the
    /// the corresponding layer priority/index does not yet exist for the
    /// timeline.
    /// </para>
    /// </remarks>
    /// <param name="newLayerPriority">
    /// The priority/index of the layer @self should be
    /// moved to. -1 means no move
    /// </param>
    /// <param name="mode">The edit mode</param>
    /// <param name="edge">The edge of @self where the edit should occur</param>
    /// <param name="position">
    /// The edit position: a new location for the edge of @self
    /// (in nanoseconds) in the timeline coordinates
    /// </param>
    /// <returns>%TRUE if the edit of @self completed, %FALSE on failure.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool EditFull(long newLayerPriority, GES.EditMode mode, GES.Edge edge, ulong position)
    {
        nint errorNative = 0;
        int nativeResult = GesTimelineElementEditFull(Handle, newLayerPriority, (int)mode, (int)edge, position, &errorNative);
        System.GC.KeepAlive(this);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets the property of a child of the element. Specifically, the property
    /// corresponding to the @pspec used in
    /// ges_timeline_element_add_child_property() is copied into @value.
    /// </summary>
    /// <param name="pspec">The specification of a registered child property to get</param>
    /// <param name="value">
    /// The return location for the value
    /// On success the caller owns the contents and disposes the value; on
    /// failure it is left empty, and disposing an empty value does nothing.
    /// </param>
    public void GetChildPropertyByPspec(Gst.GObject.ParamSpec pspec, out Gst.GObject.Value value)
    {
        ArgumentNullException.ThrowIfNull(pspec);
        value = default;
        fixed (Gst.GObject.GValueNative* valuePointer = &value.NativeValue)
        {
            GesTimelineElementGetChildPropertyByPspec(Handle, pspec.Handle, valuePointer);
            System.GC.KeepAlive(this);
            System.GC.KeepAlive(pspec);
        }
    }

    /// <summary>Gets the #GESTimelineElement:duration for the element.</summary>
    /// <returns>The duration of @self (in nanoseconds).</returns>
    public Gst.ClockTime GetDuration()
    {
        ulong nativeResult = GesTimelineElementGetDuration(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Gets the #GESTimelineElement:in-point for the element.</summary>
    /// <returns>The in-point of @self (in nanoseconds).</returns>
    public Gst.ClockTime GetInpoint()
    {
        ulong nativeResult = GesTimelineElementGetInpoint(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Gets the priority of the layer the element is in. A #GESGroup may span
    /// several layers, so this would return the highest priority (numerically,
    /// the smallest) amongst them.
    /// </summary>
    /// <returns>
    /// The priority of the layer @self is in, or
    /// #GES_TIMELINE_ELEMENT_NO_LAYER_PRIORITY if @self does not exist in a
    /// layer.
    /// </returns>
    public uint GetLayerPriority()
    {
        uint nativeResult = GesTimelineElementGetLayerPriority(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Gets the #GESTimelineElement:max-duration for the element.</summary>
    /// <returns>The max-duration of @self (in nanoseconds).</returns>
    public Gst.ClockTime GetMaxDuration()
    {
        ulong nativeResult = GesTimelineElementGetMaxDuration(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Gets the #GESTimelineElement:name for the element.</summary>
    /// <returns>The name of @self.</returns>
    public string? GetName()
    {
        nint nativeResult = GesTimelineElementGetName(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult);
    }

    /// <summary>
    /// Get the "natural" framerate of @self. This is to say, for example
    /// for a #GESVideoUriSource the framerate of the source.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that a #GESAudioSource may also have a natural framerate if it derives
    /// from the same #GESSourceClip asset as a #GESVideoSource, and its value will
    /// be that of the video source. For example, if the uri of a #GESUriClip points
    /// to a file that contains both a video and audio stream, then the corresponding
    /// #GESAudioUriSource will share the natural framerate of the corresponding
    /// #GESVideoUriSource.
    /// </para>
    /// </remarks>
    /// <param name="framerateN">The framerate numerator</param>
    /// <param name="framerateD">The framerate denominator</param>
    /// <returns>
    /// Whether @self has a natural framerate or not, @framerate_n
    /// and @framerate_d will be set to, respectively, 0 and -1 if it is
    /// not the case.
    /// </returns>
    public bool GetNaturalFramerate(out int framerateN, out int framerateD)
    {
        int framerateNNative = default;
        int framerateDNative = default;
        int nativeResult = GesTimelineElementGetNaturalFramerate(Handle, &framerateNNative, &framerateDNative);
        System.GC.KeepAlive(this);
        framerateN = framerateNNative;
        framerateD = framerateDNative;
        return nativeResult != 0;
    }

    /// <summary>Gets the #GESTimelineElement:parent for the element.</summary>
    /// <returns>
    /// The parent of @self, or %NULL if
    /// @self has no parent.
    /// </returns>
    public GES.TimelineElement? GetParent()
    {
        nint nativeResult = GesTimelineElementGetParent(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.TimelineElement>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Gets the #GESTimelineElement:priority for the element.</summary>
    /// <returns>The priority of @self.</returns>
    public uint GetPriority()
    {
        uint nativeResult = GesTimelineElementGetPriority(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Gets the #GESTimelineElement:start for the element.</summary>
    /// <returns>The start of @self (in nanoseconds).</returns>
    public Gst.ClockTime GetStart()
    {
        ulong nativeResult = GesTimelineElementGetStart(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Gets the #GESTimelineElement:timeline for the element.</summary>
    /// <returns>
    /// The timeline of @self, or %NULL
    /// if @self has no timeline.
    /// </returns>
    public GES.Timeline? GetTimeline()
    {
        nint nativeResult = GesTimelineElementGetTimeline(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.Timeline>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Gets the toplevel #GESTimelineElement:parent of the element.</summary>
    /// <returns>The toplevel parent of @self.</returns>
    public GES.TimelineElement GetToplevelParent()
    {
        nint nativeResult = GesTimelineElementGetToplevelParent(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.TimelineElement>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("ges_timeline_element_get_toplevel_parent returned no value.");
    }

    /// <summary>
    /// Gets the track types that the element can interact with, i.e. the type
    /// of #GESTrack it can exist in, or will create #GESTrackElement-s for.
    /// </summary>
    /// <returns>The track types that @self supports.</returns>
    public GES.TrackType GetTrackTypes()
    {
        int nativeResult = GesTimelineElementGetTrackTypes(Handle);
        System.GC.KeepAlive(this);
        return (GES.TrackType)nativeResult;
    }

    /// <summary>Looks up a child property of the element.</summary>
    /// <remarks>
    /// <para>
    /// @prop_name can either be in the format "prop-name" or
    /// "TypeName::prop-name", where "prop-name" is the name of the property
    /// to look up (as used in g_object_get()), and "TypeName" is the type name
    /// of the child (as returned by G_OBJECT_TYPE_NAME()). The latter format is
    /// useful when two children of different types share the same property
    /// name.
    /// </para>
    /// <para>
    /// The first child found with the given "prop-name" property that was
    /// registered with ges_timeline_element_add_child_property() (and of the
    /// type "TypeName", if it was given) will be passed to @child, and the
    /// registered specification of this property will be passed to @pspec.
    /// </para>
    /// </remarks>
    /// <param name="propName">The name of a child property</param>
    /// <param name="child">
    /// The return location for the
    /// found child
    /// </param>
    /// <param name="pspec">
    /// The return location for the
    /// specification of the child property
    /// </param>
    /// <returns>
    /// %TRUE if a child corresponding to the property was found, in
    /// which case @child and @pspec are set.
    /// </returns>
    public bool LookupChild(string propName, out Gst.GObject.Object? child, out Gst.GObject.ParamSpec? pspec)
    {
        ArgumentNullException.ThrowIfNull(propName);
        System.Span<byte> propNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope propNameScope = Gst.Interop.GMarshal.StackUtf8(propName, propNameBuffer);
        nint childNative = default;
        nint pspecNative = default;
        int nativeResult = GesTimelineElementLookupChild(Handle, propNameScope.Pointer, &childNative, &pspecNative);
        System.GC.KeepAlive(this);
        child = Gst.GObject.Object.FromNative<Gst.GObject.Object>(childNative, Gst.Interop.Transfer.Full);
        pspec = (pspecNative == 0 ? null : Gst.GObject.ParamSpec.FromNative(pspecNative, Gst.Interop.Transfer.Full));
        return nativeResult != 0;
    }

    /// <summary>
    /// Paste an element inside the same timeline and layer as @self. @self
    /// **must** be the return of ges_timeline_element_copy() with `deep=TRUE`,
    /// and it should not be changed before pasting.
    /// @self is not placed in the timeline, instead a new element is created,
    /// alike to the originally copied element. Note that the originally
    /// copied element must stay within the same timeline and layer, at both
    /// the point of copying and pasting.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Pasting may fail if it would place the timeline in an unsupported
    /// configuration.
    /// </para>
    /// <para>
    /// After calling this function @element should not be used. In particular,
    /// @element can **not** be pasted again. Instead, you can copy the
    /// returned element and paste that copy (although, this is only possible
    /// if the paste was successful).
    /// </para>
    /// <para>See also ges_timeline_paste_element().</para>
    /// </remarks>
    /// <param name="pastePosition">
    /// The position in the timeline @element should be pasted
    /// to, i.e. the #GESTimelineElement:start value for the pasted element.
    /// </param>
    /// <returns>
    /// The newly created element, or
    /// %NULL if pasting fails.
    /// </returns>
    public GES.TimelineElement? Paste(Gst.ClockTime pastePosition)
    {
        nint nativeResult = GesTimelineElementPaste(Handle, pastePosition.Nanoseconds);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.TimelineElement>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Remove a child property from the element. @pspec should be a
    /// specification that was passed to
    /// ges_timeline_element_add_child_property(). The corresponding property
    /// will no longer be registered as a child property for the element.
    /// </summary>
    /// <param name="pspec">The specification for the property to remove</param>
    /// <returns>%TRUE if the property was successfully un-registered for @self.</returns>
    public bool RemoveChildProperty(Gst.GObject.ParamSpec pspec)
    {
        ArgumentNullException.ThrowIfNull(pspec);
        int nativeResult = GesTimelineElementRemoveChildProperty(Handle, pspec.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pspec);
        return nativeResult != 0;
    }

    /// <summary>
    /// Edits the start time of an element within its timeline in ripple mode.
    /// See ges_timeline_element_edit() with #GES_EDIT_MODE_RIPPLE and
    /// #GES_EDGE_NONE.
    /// </summary>
    /// <param name="start">The new start time of @self in ripple mode</param>
    /// <returns>
    /// %TRUE if the ripple edit of @self completed, %FALSE on
    /// failure.
    /// </returns>
    public bool Ripple(Gst.ClockTime start)
    {
        int nativeResult = GesTimelineElementRipple(Handle, start.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Edits the end time of an element within its timeline in ripple mode.
    /// See ges_timeline_element_edit() with #GES_EDIT_MODE_RIPPLE and
    /// #GES_EDGE_END.
    /// </summary>
    /// <param name="end">The new end time of @self in ripple mode</param>
    /// <returns>
    /// %TRUE if the ripple edit of @self completed, %FALSE on
    /// failure.
    /// </returns>
    public bool RippleEnd(Gst.ClockTime end)
    {
        int nativeResult = GesTimelineElementRippleEnd(Handle, end.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Edits the end time of an element within its timeline in roll mode.
    /// See ges_timeline_element_edit() with #GES_EDIT_MODE_ROLL and
    /// #GES_EDGE_END.
    /// </summary>
    /// <param name="end">The new end time of @self in roll mode</param>
    /// <returns>%TRUE if the roll edit of @self completed, %FALSE on failure.</returns>
    public bool RollEnd(Gst.ClockTime end)
    {
        int nativeResult = GesTimelineElementRollEnd(Handle, end.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Edits the start time of an element within its timeline in roll mode.
    /// See ges_timeline_element_edit() with #GES_EDIT_MODE_ROLL and
    /// #GES_EDGE_START.
    /// </summary>
    /// <param name="start">The new start time of @self in roll mode</param>
    /// <returns>%TRUE if the roll edit of @self completed, %FALSE on failure.</returns>
    public bool RollStart(Gst.ClockTime start)
    {
        int nativeResult = GesTimelineElementRollStart(Handle, start.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets the property of a child of the element. Specifically, the property
    /// corresponding to the @pspec used in
    /// ges_timeline_element_add_child_property() is set to @value.
    /// </summary>
    /// <param name="pspec">The specification of a registered child property to set</param>
    /// <param name="value">
    /// The value to set the property to
    /// The callee copies what it keeps, so the caller keeps ownership of
    /// <paramref name="value"/> and still disposes it.
    /// </param>
    /// <exception cref="ArgumentException">
    /// <paramref name="value"/> is empty.
    /// </exception>
    public void SetChildPropertyByPspec(Gst.GObject.ParamSpec pspec, in Gst.GObject.Value value)
    {
        ArgumentNullException.ThrowIfNull(pspec);
        if (value.IsEmpty)
        {
            throw new ArgumentException(
                "An empty value cannot be passed: it has no type for the call to read.",
                nameof(value));
        }
        fixed (Gst.GObject.GValueNative* valuePointer = &System.Runtime.CompilerServices.Unsafe.AsRef(in value).NativeValue)
        {
            GesTimelineElementSetChildPropertyByPspec(Handle, pspec.Handle, valuePointer);
            System.GC.KeepAlive(this);
            System.GC.KeepAlive(pspec);
        }
    }

    /// <summary>Sets the property of a child of the element.</summary>
    /// <remarks>
    /// <para>
    /// @property_name can either be in the format "prop-name" or
    /// "TypeName::prop-name", where "prop-name" is the name of the property
    /// to set (as used in g_object_set()), and "TypeName" is the type name of
    /// the child (as returned by G_OBJECT_TYPE_NAME()). The latter format is
    /// useful when two children of different types share the same property
    /// name.
    /// </para>
    /// <para>
    /// The first child found with the given "prop-name" property that was
    /// registered with ges_timeline_element_add_child_property() (and of the
    /// type "TypeName", if it was given) will have the corresponding
    /// property set to @value. Other children that may have also matched the
    /// property name (and type name) are left unchanged!
    /// </para>
    /// </remarks>
    /// <param name="propertyName">The name of the child property to set</param>
    /// <param name="value">
    /// The value to set the property to
    /// The callee copies what it keeps, so the caller keeps ownership of
    /// <paramref name="value"/> and still disposes it.
    /// </param>
    /// <returns>%TRUE if the property was found and set.</returns>
    /// <exception cref="ArgumentException">
    /// <paramref name="value"/> is empty.
    /// </exception>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool SetChildPropertyFull(string propertyName, in Gst.GObject.Value value)
    {
        ArgumentNullException.ThrowIfNull(propertyName);
        System.Span<byte> propertyNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope propertyNameScope = Gst.Interop.GMarshal.StackUtf8(propertyName, propertyNameBuffer);
        if (value.IsEmpty)
        {
            throw new ArgumentException(
                "An empty value cannot be passed: it has no type for the call to read.",
                nameof(value));
        }
        nint errorNative = 0;
        fixed (Gst.GObject.GValueNative* valuePointer = &System.Runtime.CompilerServices.Unsafe.AsRef(in value).NativeValue)
        {
            int nativeResult = GesTimelineElementSetChildPropertyFull(Handle, propertyNameScope.Pointer, valuePointer, &errorNative);
            System.GC.KeepAlive(this);
            Gst.GLib.GException.ThrowIfSet(ref errorNative);
            return nativeResult != 0;
        }
    }

    /// <summary>Sets #GESTimelineElement:duration for the element.</summary>
    /// <remarks>
    /// <para>
    /// Whilst the element is part of a #GESTimeline, this is the same as
    /// editing the element with ges_timeline_element_edit() under
    /// #GES_EDIT_MODE_TRIM with #GES_EDGE_END. In particular, the
    /// #GESTimelineElement:duration of the element may be snapped to a
    /// different timeline time difference from the one given. In addition,
    /// setting may fail if it would place the timeline in an unsupported
    /// configuration, or the element does not have enough internal content to
    /// last the desired duration.
    /// </para>
    /// </remarks>
    /// <param name="duration">The desired duration in its timeline</param>
    /// <returns>%TRUE if @duration could be set for @self.</returns>
    public bool SetDuration(Gst.ClockTime duration)
    {
        int nativeResult = GesTimelineElementSetDuration(Handle, duration.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets #GESTimelineElement:in-point for the element. If the new in-point
    /// is above the current #GESTimelineElement:max-duration of the element,
    /// this method will fail.
    /// </summary>
    /// <param name="inpoint">The in-point, in internal time coordinates</param>
    /// <returns>%TRUE if @inpoint could be set for @self.</returns>
    public bool SetInpoint(Gst.ClockTime inpoint)
    {
        int nativeResult = GesTimelineElementSetInpoint(Handle, inpoint.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets #GESTimelineElement:max-duration for the element. If the new
    /// maximum duration is below the current #GESTimelineElement:in-point of
    /// the element, this method will fail.
    /// </summary>
    /// <param name="maxduration">The maximum duration, in internal time coordinates</param>
    /// <returns>%TRUE if @maxduration could be set for @self.</returns>
    public bool SetMaxDuration(Gst.ClockTime maxduration)
    {
        int nativeResult = GesTimelineElementSetMaxDuration(Handle, maxduration.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets the #GESTimelineElement:name for the element. If %NULL is given
    /// for @name, then the library will instead generate a new name based on
    /// the type name of the element, such as the name "uriclip3" for a
    /// #GESUriClip, and will set that name instead.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @self already has a #GESTimelineElement:timeline, you should not
    /// call this function with @name set to %NULL.
    /// </para>
    /// <para>
    /// You should ensure that, within each #GESTimeline, every element has a
    /// unique name. If you call this function with @name as %NULL, then
    /// the library should ensure that the set generated name is unique from
    /// previously **generated** names. However, if you choose a @name that
    /// interferes with the naming conventions of the library, the library will
    /// attempt to ensure that the generated names will not conflict with the
    /// chosen name, which may lead to a different name being set instead, but
    /// the uniqueness between generated and user-chosen names is not
    /// guaranteed.
    /// </para>
    /// </remarks>
    /// <param name="name">The name @self should take</param>
    /// <returns>%TRUE if @name or a generated name for @self could be set.</returns>
    public bool SetName(string? name)
    {
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        int nativeResult = GesTimelineElementSetName(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Sets the #GESTimelineElement:parent for the element.</summary>
    /// <remarks>
    /// <para>
    /// This is used internally and you should normally not call this. A
    /// #GESContainer will set the #GESTimelineElement:parent of its children
    /// in ges_container_add() and ges_container_remove().
    /// </para>
    /// <para>
    /// Note, if @parent is not %NULL, @self must not already have a parent
    /// set. Therefore, if you wish to switch parents, you will need to call
    /// this function twice: first to set the parent to %NULL, and then to the
    /// new parent.
    /// </para>
    /// <para>
    /// If @parent is not %NULL, you must ensure it already has a
    /// (non-floating) reference to @self before calling this.
    /// </para>
    /// </remarks>
    /// <param name="parent">The <c>parent</c> argument.</param>
    /// <returns>%TRUE if @parent could be set for @self.</returns>
    public bool SetParent(GES.TimelineElement parent)
    {
        ArgumentNullException.ThrowIfNull(parent);
        int nativeResult = GesTimelineElementSetParent(Handle, parent.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(parent);
        return nativeResult != 0;
    }

    /// <summary>Sets the priority of the element within the containing layer.</summary>
    /// <param name="priority">The priority</param>
    /// <returns>%TRUE if @priority could be set for @self.</returns>
    [Obsolete("All priority management is done by GES itself now. To set #GESEffect priorities #ges_clip_set_top_effect_index should be used. (deprecated since 1.10)")]
    public bool SetPriority(uint priority)
    {
        int nativeResult = GesTimelineElementSetPriority(Handle, priority);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets #GESTimelineElement:start for the element. If the element has a
    /// parent, this will also move its siblings with the same shift.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Whilst the element is part of a #GESTimeline, this is the same as
    /// editing the element with ges_timeline_element_edit() under
    /// #GES_EDIT_MODE_NORMAL with #GES_EDGE_NONE. In particular, the
    /// #GESTimelineElement:start of the element may be snapped to a different
    /// timeline time from the one given. In addition, setting may fail if it
    /// would place the timeline in an unsupported configuration.
    /// </para>
    /// </remarks>
    /// <param name="start">The desired start position of the element in its timeline</param>
    /// <returns>%TRUE if @start could be set for @self.</returns>
    public bool SetStart(Gst.ClockTime start)
    {
        int nativeResult = GesTimelineElementSetStart(Handle, start.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Sets the #GESTimelineElement:timeline of the element.</summary>
    /// <remarks>
    /// <para>
    /// This is used internally and you should normally not call this. A
    /// #GESClip will have its #GESTimelineElement:timeline set through its
    /// #GESLayer. A #GESTrack will similarly take care of setting the
    /// #GESTimelineElement:timeline of its #GESTrackElement-s. A #GESGroup
    /// will adopt the same #GESTimelineElement:timeline as its children.
    /// </para>
    /// <para>
    /// If @timeline is %NULL, this will stop its current
    /// #GESTimelineElement:timeline from tracking it, otherwise @timeline will
    /// start tracking @self. Note, in the latter case, @self must not already
    /// have a timeline set. Therefore, if you wish to switch timelines, you
    /// will need to call this function twice: first to set the timeline to
    /// %NULL, and then to the new timeline.
    /// </para>
    /// </remarks>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    /// <returns>%TRUE if @timeline could be set for @self.</returns>
    public bool SetTimeline(GES.Timeline timeline)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        int nativeResult = GesTimelineElementSetTimeline(Handle, timeline.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
        return nativeResult != 0;
    }

    /// <summary>
    /// Edits the start time of an element within its timeline in trim mode.
    /// See ges_timeline_element_edit() with #GES_EDIT_MODE_TRIM and
    /// #GES_EDGE_START.
    /// </summary>
    /// <param name="start">The new start time of @self in trim mode</param>
    /// <returns>%TRUE if the trim edit of @self completed, %FALSE on failure.</returns>
    public bool Trim(Gst.ClockTime start)
    {
        int nativeResult = GesTimelineElementTrim(Handle, start.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// The duration that the element is in effect for in the timeline (a
    /// time difference in nanoseconds using the time coordinates of the
    /// timeline). For example, for a source element, this would determine
    /// for how long it should output its internal content for. For an
    /// operation element, this would determine for how long its effect
    /// should be applied to any source content.
    /// </summary>
    public Gst.ClockTime Duration => GetDuration();

    /// <summary>
    /// The initial offset to use internally when outputting content (in
    /// nanoseconds, but in the time coordinates of the internal content).
    /// </summary>
    /// <remarks>
    /// <para>
    /// For example, for a #GESVideoUriSource that references some media
    /// file, the "internal content" is the media file data, and the
    /// in-point would correspond to some timestamp in the media file.
    /// When playing the timeline, and when the element is first reached at
    /// timeline-time #GESTimelineElement:start, it will begin outputting the
    /// data from the timestamp in-point **onwards**, until it reaches the
    /// end of its #GESTimelineElement:duration in the timeline.
    /// </para>
    /// <para>
    /// For elements that have no internal content, this should be kept
    /// as 0.
    /// </para>
    /// </remarks>
    public Gst.ClockTime InPoint => GetInpoint();

    /// <summary>
    /// The full duration of internal content that is available (a time
    /// difference in nanoseconds using the time coordinates of the internal
    /// content).
    /// </summary>
    /// <remarks>
    /// <para>
    /// This will act as a cap on the #GESTimelineElement:in-point of the
    /// element (which is in the same time coordinates), and will sometimes
    /// be used to limit the #GESTimelineElement:duration of the element in
    /// the timeline.
    /// </para>
    /// <para>
    /// For example, for a #GESVideoUriSource that references some media
    /// file, this would be the length of the media file.
    /// </para>
    /// <para>
    /// For elements that have no internal content, or whose content is
    /// indefinite, this should be kept as #GST_CLOCK_TIME_NONE.
    /// </para>
    /// </remarks>
    public Gst.ClockTime MaxDuration => GetMaxDuration();

    /// <summary>The name of the element. This should be unique within its timeline.</summary>
    public string? Name => GetName();

    /// <summary>The parent container of the element.</summary>
    public GES.TimelineElement? Parent => GetParent();

    /// <summary>The priority of the element.</summary>
    [Obsolete("Priority management is now done by GES itself. (deprecated since 1.10)")]
    public uint Priority => GetPriority();

    /// <summary>
    /// The starting position of the element in the timeline (in nanoseconds
    /// and in the time coordinates of the timeline). For example, for a
    /// source element, this would determine the time at which it should
    /// start outputting its internal content. For an operation element, this
    /// would determine the time at which it should start applying its effect
    /// to any source content.
    /// </summary>
    public Gst.ClockTime Start => GetStart();

    /// <summary>The timeline that the element lies within.</summary>
    public GES.Timeline? Timeline => GetTimeline();

    /// <summary>Whether the element should be serialized.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool Serialize
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("serialize");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("serialize");
            holder.SetBoolean(value);
            SetPropertyValue("serialize", in holder);
        }
    }

    /// <summary>The arguments of the <c>child-property-added</c> signal of <c>GESTimelineElement</c>.</summary>
    public sealed class ChildPropertyAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ChildPropertyAddedSignalArgs"/> class.</summary>
        /// <param name="propObject">The child whose property has been registered</param>
        /// <param name="prop">The specification for the property that has been registered</param>
        internal ChildPropertyAddedSignalArgs(Gst.GObject.Object propObject, Gst.GObject.ParamSpec prop)
        {
            PropObject = propObject;
            Prop = prop;
        }

        /// <summary>The child whose property has been registered</summary>
        public Gst.GObject.Object PropObject { get; }

        /// <summary>The specification for the property that has been registered</summary>
        /// <remarks>
        /// The value is only valid while the handler runs: the wrapper is disposed
        /// once it returns. Read out of it what is needed, or copy it where the
        /// type offers a copy.
        /// </remarks>
        public Gst.GObject.ParamSpec Prop { get; }
    }

    /// <summary>
    /// Emitted when the element has a new child property registered. See
    /// ges_timeline_element_add_child_property().
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that some GES elements will be automatically created with
    /// pre-registered children properties. You can use
    /// ges_timeline_element_list_children_properties() to list these.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.TimelineElement.ChildPropertyAddedSignalArgs> ChildPropertyAdded
    {
        add => GES.SignalConnections.Add(this, "child-property-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&ChildPropertyAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "child-property-added", value);
    }

    /// <summary>The native handler of the <c>child-property-added</c> signal of <c>GESTimelineElement</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ChildPropertyAddedTrampoline(nint instance, nint propObject, nint prop, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.TimelineElement.ChildPropertyAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.GObject.Object propObjectValue = Gst.GObject.Object.FromNative<Gst.GObject.Object>(propObject, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The child-property-added signal of GESTimelineElement passed no prop_object.");
            using Gst.GObject.ParamSpec propValue = Gst.GObject.ParamSpec.FromNative(prop, Gst.Interop.Transfer.None);
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.TimelineElement.ChildPropertyAddedSignalArgs(propObjectValue, propValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>child-property-removed</c> signal of <c>GESTimelineElement</c>.</summary>
    public sealed class ChildPropertyRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ChildPropertyRemovedSignalArgs"/> class.</summary>
        /// <param name="propObject">The child whose property has been unregistered</param>
        /// <param name="prop">The specification for the property that has been unregistered</param>
        internal ChildPropertyRemovedSignalArgs(Gst.GObject.Object propObject, Gst.GObject.ParamSpec prop)
        {
            PropObject = propObject;
            Prop = prop;
        }

        /// <summary>The child whose property has been unregistered</summary>
        public Gst.GObject.Object PropObject { get; }

        /// <summary>The specification for the property that has been unregistered</summary>
        /// <remarks>
        /// The value is only valid while the handler runs: the wrapper is disposed
        /// once it returns. Read out of it what is needed, or copy it where the
        /// type offers a copy.
        /// </remarks>
        public Gst.GObject.ParamSpec Prop { get; }
    }

    /// <summary>
    /// Emitted when the element has a child property unregistered. See
    /// ges_timeline_element_remove_child_property().
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.TimelineElement.ChildPropertyRemovedSignalArgs> ChildPropertyRemoved
    {
        add => GES.SignalConnections.Add(this, "child-property-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&ChildPropertyRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "child-property-removed", value);
    }

    /// <summary>The native handler of the <c>child-property-removed</c> signal of <c>GESTimelineElement</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ChildPropertyRemovedTrampoline(nint instance, nint propObject, nint prop, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.TimelineElement.ChildPropertyRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.GObject.Object propObjectValue = Gst.GObject.Object.FromNative<Gst.GObject.Object>(propObject, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The child-property-removed signal of GESTimelineElement passed no prop_object.");
            using Gst.GObject.ParamSpec propValue = Gst.GObject.ParamSpec.FromNative(prop, Gst.Interop.Transfer.None);
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.TimelineElement.ChildPropertyRemovedSignalArgs(propObjectValue, propValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>deep-notify</c> signal of <c>GESTimelineElement</c>.</summary>
    public sealed class DeepNotifySignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="DeepNotifySignalArgs"/> class.</summary>
        /// <param name="propObject">The child whose property has been set</param>
        /// <param name="prop">The specification for the property that been set</param>
        internal DeepNotifySignalArgs(Gst.GObject.Object propObject, Gst.GObject.ParamSpec prop)
        {
            PropObject = propObject;
            Prop = prop;
        }

        /// <summary>The child whose property has been set</summary>
        public Gst.GObject.Object PropObject { get; }

        /// <summary>The specification for the property that been set</summary>
        /// <remarks>
        /// The value is only valid while the handler runs: the wrapper is disposed
        /// once it returns. Read out of it what is needed, or copy it where the
        /// type offers a copy.
        /// </remarks>
        public Gst.GObject.ParamSpec Prop { get; }
    }

    /// <summary>
    /// Emitted when a child of the element has one of its registered
    /// properties set. See ges_timeline_element_add_child_property().
    /// Note that unlike #GObject::notify, a child property name can not be
    /// used as a signal detail.
    /// </summary>
    /// <remarks>
    /// The signal is detailed. The handler is connected to <c>deep-notify</c>
    /// without a detail, so it runs for every detail of the signal.
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.TimelineElement.DeepNotifySignalArgs> DeepNotify
    {
        add => GES.SignalConnections.Add(this, "deep-notify", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&DeepNotifyTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "deep-notify", value);
    }

    /// <summary>The native handler of the <c>deep-notify</c> signal of <c>GESTimelineElement</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void DeepNotifyTrampoline(nint instance, nint propObject, nint prop, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.TimelineElement.DeepNotifySignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.GObject.Object propObjectValue = Gst.GObject.Object.FromNative<Gst.GObject.Object>(propObject, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The deep-notify signal of GESTimelineElement passed no prop_object.");
            using Gst.GObject.ParamSpec propValue = Gst.GObject.ParamSpec.FromNative(prop, Gst.Interop.Transfer.None);
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.TimelineElement.DeepNotifySignalArgs(propObjectValue, propValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>ges_timeline_element_add_child_property</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_add_child_property")]
    private static partial int GesTimelineElementAddChildProperty(nint self, nint pspec, nint child);

    /// <summary>The <c>ges_timeline_element_copy</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_copy")]
    private static partial nint GesTimelineElementCopy(nint self, int deep);

    /// <summary>The <c>ges_timeline_element_edit</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_edit")]
    private static partial int GesTimelineElementEdit(nint self, nint layers, long newLayerPriority, int mode, int edge, ulong position);

    /// <summary>The <c>ges_timeline_element_edit_full</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_edit_full")]
    private static partial int GesTimelineElementEditFull(nint self, long newLayerPriority, int mode, int edge, ulong position, nint* error);

    /// <summary>The <c>ges_timeline_element_get_child_property_by_pspec</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_child_property_by_pspec")]
    private static partial void GesTimelineElementGetChildPropertyByPspec(nint self, nint pspec, Gst.GObject.GValueNative* value);

    /// <summary>The <c>ges_timeline_element_get_duration</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_duration")]
    private static partial ulong GesTimelineElementGetDuration(nint self);

    /// <summary>The <c>ges_timeline_element_get_inpoint</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_inpoint")]
    private static partial ulong GesTimelineElementGetInpoint(nint self);

    /// <summary>The <c>ges_timeline_element_get_layer_priority</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_layer_priority")]
    private static partial uint GesTimelineElementGetLayerPriority(nint self);

    /// <summary>The <c>ges_timeline_element_get_max_duration</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_max_duration")]
    private static partial ulong GesTimelineElementGetMaxDuration(nint self);

    /// <summary>The <c>ges_timeline_element_get_name</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_name")]
    private static partial nint GesTimelineElementGetName(nint self);

    /// <summary>The <c>ges_timeline_element_get_natural_framerate</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_natural_framerate")]
    private static partial int GesTimelineElementGetNaturalFramerate(nint self, int* framerateN, int* framerateD);

    /// <summary>The <c>ges_timeline_element_get_parent</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_parent")]
    private static partial nint GesTimelineElementGetParent(nint self);

    /// <summary>The <c>ges_timeline_element_get_priority</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_priority")]
    private static partial uint GesTimelineElementGetPriority(nint self);

    /// <summary>The <c>ges_timeline_element_get_start</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_start")]
    private static partial ulong GesTimelineElementGetStart(nint self);

    /// <summary>The <c>ges_timeline_element_get_timeline</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_timeline")]
    private static partial nint GesTimelineElementGetTimeline(nint self);

    /// <summary>The <c>ges_timeline_element_get_toplevel_parent</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_toplevel_parent")]
    private static partial nint GesTimelineElementGetToplevelParent(nint self);

    /// <summary>The <c>ges_timeline_element_get_track_types</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_track_types")]
    private static partial int GesTimelineElementGetTrackTypes(nint self);

    /// <summary>The <c>ges_timeline_element_lookup_child</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_lookup_child")]
    private static partial int GesTimelineElementLookupChild(nint self, byte* propName, nint* child, nint* pspec);

    /// <summary>The <c>ges_timeline_element_paste</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_paste")]
    private static partial nint GesTimelineElementPaste(nint self, ulong pastePosition);

    /// <summary>The <c>ges_timeline_element_remove_child_property</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_remove_child_property")]
    private static partial int GesTimelineElementRemoveChildProperty(nint self, nint pspec);

    /// <summary>The <c>ges_timeline_element_ripple</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_ripple")]
    private static partial int GesTimelineElementRipple(nint self, ulong start);

    /// <summary>The <c>ges_timeline_element_ripple_end</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_ripple_end")]
    private static partial int GesTimelineElementRippleEnd(nint self, ulong end);

    /// <summary>The <c>ges_timeline_element_roll_end</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_roll_end")]
    private static partial int GesTimelineElementRollEnd(nint self, ulong end);

    /// <summary>The <c>ges_timeline_element_roll_start</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_roll_start")]
    private static partial int GesTimelineElementRollStart(nint self, ulong start);

    /// <summary>The <c>ges_timeline_element_set_child_property_by_pspec</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_child_property_by_pspec")]
    private static partial void GesTimelineElementSetChildPropertyByPspec(nint self, nint pspec, Gst.GObject.GValueNative* value);

    /// <summary>The <c>ges_timeline_element_set_child_property_full</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_child_property_full")]
    private static partial int GesTimelineElementSetChildPropertyFull(nint self, byte* propertyName, Gst.GObject.GValueNative* value, nint* error);

    /// <summary>The <c>ges_timeline_element_set_duration</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_duration")]
    private static partial int GesTimelineElementSetDuration(nint self, ulong duration);

    /// <summary>The <c>ges_timeline_element_set_inpoint</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_inpoint")]
    private static partial int GesTimelineElementSetInpoint(nint self, ulong inpoint);

    /// <summary>The <c>ges_timeline_element_set_max_duration</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_max_duration")]
    private static partial int GesTimelineElementSetMaxDuration(nint self, ulong maxduration);

    /// <summary>The <c>ges_timeline_element_set_name</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_name")]
    private static partial int GesTimelineElementSetName(nint self, byte* name);

    /// <summary>The <c>ges_timeline_element_set_parent</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_parent")]
    private static partial int GesTimelineElementSetParent(nint self, nint parent);

    /// <summary>The <c>ges_timeline_element_set_priority</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_priority")]
    private static partial int GesTimelineElementSetPriority(nint self, uint priority);

    /// <summary>The <c>ges_timeline_element_set_start</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_start")]
    private static partial int GesTimelineElementSetStart(nint self, ulong start);

    /// <summary>The <c>ges_timeline_element_set_timeline</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_set_timeline")]
    private static partial int GesTimelineElementSetTimeline(nint self, nint timeline);

    /// <summary>The <c>ges_timeline_element_trim</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_trim")]
    private static partial int GesTimelineElementTrim(nint self, ulong start);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESTimelineElement</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_timeline_element_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GESTimelineElement</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : TimelineElement
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
