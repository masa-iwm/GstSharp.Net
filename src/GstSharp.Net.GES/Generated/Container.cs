// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>
/// A #GESContainer is a timeline element that controls other
/// #GESTimelineElement-s, which are its children. In particular, it is
/// responsible for maintaining the relative #GESTimelineElement:start and
/// #GESTimelineElement:duration times of its children. Therefore, if a
/// container is temporally adjusted or moved to a new layer, it may
/// accordingly adjust and move its children. Similarly, a change in one of
/// its children may prompt the parent to correspondingly change its
/// siblings.
/// </summary>
public abstract unsafe partial class Container : GES.TimelineElement, GES.IExtractable, GES.IMetaContainer
{
    /// <summary>Wraps a native <c>GESContainer</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESContainer</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Container(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Adds a timeline element to the container. The element will now be a
    /// child of the container (and the container will be the
    /// #GESTimelineElement:parent of the added element), which means that it
    /// is now controlled by the container. This may change the properties of
    /// the child or the container, depending on the subclass.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Additionally, the children properties of the newly added element will
    /// be shared with the container, meaning they can also be read and set
    /// using ges_timeline_element_get_child_property() and
    /// ges_timeline_element_set_child_property() on the container.
    /// </para>
    /// </remarks>
    /// <param name="child">The <c>child</c> argument.</param>
    /// <returns>%TRUE if @child was successfully added to @container.</returns>
    public bool Add(GES.TimelineElement child)
    {
        ArgumentNullException.ThrowIfNull(child);
        int nativeResult = GesContainerAdd(Handle, child.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(child);
        return nativeResult != 0;
    }

    /// <summary>Edits the container within its timeline.</summary>
    /// <remarks>
    /// <para>
    /// GStreamer ignores this list. ges_timeline_element_edit forwards to
    /// ges_timeline_element_edit_full with NULL and never reads it
    /// (ges-timeline-element.c:2533-2543, where the upstream FIXME says so), and the
    /// two deprecated wrappers forward to it (ges-container.c:1063-1070,
    /// ges-track-element.c:1823-1831). Pass null.
    /// This overload takes the new priority as an <see langword="int"/> while the
    /// member it does not hide, <c>GES.TimelineElement.Edit</c>, takes it as a
    /// <see langword="long"/>: an integer literal binds this one, and an argument
    /// of type <see langword="long"/> reaches the other.
    /// </para>
    /// </remarks>
    /// <param name="layers">
    /// The <c>layers</c> argument.
    /// The call reads the list while it runs and copies whatever it keeps. A
    /// temporary native list is built for the call and released when it returns,
    /// and an empty sequence is passed as the null pointer, which is how C spells
    /// the empty list.
    /// </param>
    /// <param name="newLayerPriority">The <c>newLayerPriority</c> argument.</param>
    /// <param name="mode">The <c>mode</c> argument.</param>
    /// <param name="edge">The <c>edge</c> argument.</param>
    /// <param name="position">The <c>position</c> argument.</param>
    /// <returns>%TRUE if the edit of @container completed, %FALSE on failure.</returns>
    [Obsolete("use #ges_timeline_element_edit instead. (deprecated since 1.18)")]
    public bool Edit(System.Collections.Generic.IEnumerable<GES.Layer>? layers, int newLayerPriority, GES.EditMode mode, GES.Edge edge, ulong position)
    {
        using Gst.Interop.GListScope layersScope = Gst.Interop.GMarshal.AllocList(layers, singly: false);
        int nativeResult = GesContainerEdit(Handle, layersScope.Head, newLayerPriority, (int)mode, (int)edge, position);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Get the list of timeline elements contained in the container. If
    /// @recursive is %TRUE, and the container contains other containers as
    /// children, then their children will be added to the list, in addition to
    /// themselves, and so on.
    /// </summary>
    /// <param name="recursive">The <c>recursive</c> argument.</param>
    /// <returns>
    /// The list of
    /// #GESTimelineElement-s contained in @container.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.TimelineElement> GetChildren(bool recursive)
    {
        nint nativeResult = GesContainerGetChildren(Handle, recursive ? 1 : 0);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.TimelineElement> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.TimelineElement>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Removes a timeline element from the container. The element will no
    /// longer be controlled by the container.
    /// </summary>
    /// <param name="child">The <c>child</c> argument.</param>
    /// <returns>%TRUE if @child was successfully removed from @container.</returns>
    public bool Remove(GES.TimelineElement child)
    {
        ArgumentNullException.ThrowIfNull(child);
        int nativeResult = GesContainerRemove(Handle, child.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(child);
        return nativeResult != 0;
    }

    /// <summary>
    /// Groups the containers into a single container by merging them. The
    /// containers must all belong to the same #GESTimelineElement:timeline.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If the elements are all #GESClip-s then this method will attempt to
    /// combine them all into a single #GESClip. This should succeed if they:
    /// share the same #GESTimelineElement:start, #GESTimelineElement:duration
    /// and #GESTimelineElement:in-point; exist in the same layer; and all of
    /// the sources share the same #GESAsset. If this fails, or one of the
    /// elements is not a #GESClip, this method will try to create a #GESGroup
    /// instead.
    /// </para>
    /// <para>
    /// A list of one answers that element's own wrapper rather than a new group,
    /// and takes no new reference (ges-container.c:1007-1014, upstream FIXME). A
    /// null or empty list answers a new, empty group (ges-group.c:461). Where the
    /// members are clips, the clips after the first are merged into the first and
    /// removed from their layer (ges-clip.c:2238-2330).
    /// </para>
    /// </remarks>
    /// <param name="containers">
    /// The <c>containers</c> argument.
    /// The call reads the list while it runs and copies whatever it keeps. A
    /// temporary native list is built for the call and released when it returns,
    /// and an empty sequence is passed as the null pointer, which is how C spells
    /// the empty list.
    /// </param>
    /// <returns>
    /// The container created by merging
    /// @containers, or %NULL if they could not be merged into a single
    /// container.
    /// </returns>
    public static GES.Container? Group(System.Collections.Generic.IEnumerable<GES.Container>? containers)
    {
        using Gst.Interop.GListScope containersScope = Gst.Interop.GMarshal.AllocList(containers, singly: false);
        nint nativeResult = GesContainerGroup(containersScope.Head);
        return Gst.GObject.Object.FromNative<GES.Container>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>
    /// The span of the container's children's #GESTimelineElement:priority
    /// values, which is the number of integers that lie between (inclusive)
    /// the minimum and maximum priorities found amongst the container's
    /// children (maximum - minimum + 1).
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public uint Height
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("height");
            return holder.GetUInt();
        }
    }

    /// <summary>The arguments of the <c>child-added</c> signal of <c>GESContainer</c>.</summary>
    public sealed class ChildAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ChildAddedSignalArgs"/> class.</summary>
        /// <param name="element">The child that was added</param>
        internal ChildAddedSignalArgs(GES.TimelineElement element)
        {
            Element = element;
        }

        /// <summary>The child that was added</summary>
        public GES.TimelineElement Element { get; }
    }

    /// <summary>
    /// Will be emitted after a child is added to the container. Usually,
    /// you should connect with g_signal_connect_after() since the signal
    /// may be stopped internally.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Container.ChildAddedSignalArgs> ChildAdded
    {
        add => GES.SignalConnections.Add(this, "child-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&ChildAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "child-added", value);
    }

    /// <summary>The native handler of the <c>child-added</c> signal of <c>GESContainer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ChildAddedTrampoline(nint instance, nint element, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Container.ChildAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.TimelineElement elementValue = Gst.GObject.Object.FromNative<GES.TimelineElement>(element, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The child-added signal of GESContainer passed no element.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Container.ChildAddedSignalArgs(elementValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>child-removed</c> signal of <c>GESContainer</c>.</summary>
    public sealed class ChildRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ChildRemovedSignalArgs"/> class.</summary>
        /// <param name="element">The child that was removed</param>
        internal ChildRemovedSignalArgs(GES.TimelineElement element)
        {
            Element = element;
        }

        /// <summary>The child that was removed</summary>
        public GES.TimelineElement Element { get; }
    }

    /// <summary>Will be emitted after a child is removed from the container.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Container.ChildRemovedSignalArgs> ChildRemoved
    {
        add => GES.SignalConnections.Add(this, "child-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&ChildRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "child-removed", value);
    }

    /// <summary>The native handler of the <c>child-removed</c> signal of <c>GESContainer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ChildRemovedTrampoline(nint instance, nint element, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Container.ChildRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.TimelineElement elementValue = Gst.GObject.Object.FromNative<GES.TimelineElement>(element, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The child-removed signal of GESContainer passed no element.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Container.ChildRemovedSignalArgs(elementValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>ges_container_add</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_container_add")]
    private static partial int GesContainerAdd(nint container, nint child);

    /// <summary>The <c>ges_container_edit</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_container_edit")]
    private static partial int GesContainerEdit(nint container, nint layers, int newLayerPriority, int mode, int edge, ulong position);

    /// <summary>The <c>ges_container_get_children</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_container_get_children")]
    private static partial nint GesContainerGetChildren(nint container, int recursive);

    /// <summary>The <c>ges_container_remove</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_container_remove")]
    private static partial int GesContainerRemove(nint container, nint child);

    /// <summary>The <c>ges_container_group</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_container_group")]
    private static partial nint GesContainerGroup(nint containers);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESContainer</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_container_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GESContainer</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : Container
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
