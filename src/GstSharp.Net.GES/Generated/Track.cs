// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>
/// A #GESTrack acts an output source for a #GESTimeline. Each one
/// essentially provides an additional #GstPad for the timeline, with
/// #GESTrack:restriction-caps capabilities. Internally, a track
/// wraps an #nlecomposition filtered by a #capsfilter.
/// </summary>
/// <remarks>
/// <para>
/// A track will contain a number of #GESTrackElement-s, and its role is
/// to select and activate these elements according to their timings when
/// the timeline in played. For example, a track would activate a
/// #GESSource when its #GESTimelineElement:start is reached by outputting
/// its data for its #GESTimelineElement:duration. Similarly, a
/// #GESOperation would be activated by applying its effect to the source
/// data, starting from its #GESTimelineElement:start time and lasting for
/// its #GESTimelineElement:duration.
/// </para>
/// <para>
/// For most users, it will usually be sufficient to add newly created
/// tracks to a timeline, but never directly add an element to a track.
/// Whenever a #GESClip is added to a timeline, the clip adds its
/// elements to the timeline's tracks and assumes responsibility for
/// updating them.
/// </para>
/// </remarks>
public unsafe partial class Track : Gst.Bin, GES.IMetaContainer, Gst.IChildProxy
{
    /// <summary>Wraps a native <c>GESTrack</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESTrack</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Track(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new track with the given track-type and caps.</summary>
    /// <remarks>
    /// <para>
    /// If @type is #GES_TRACK_TYPE_VIDEO, and @caps is a subset of
    /// "video/x-raw(ANY)", then a #GESVideoTrack is created. This will
    /// automatically choose a gap creation method suitable for video data. You
    /// will likely want to set #GESTrack:restriction-caps separately. You may
    /// prefer to use the ges_video_track_new() method instead.
    /// </para>
    /// <para>
    /// If @type is #GES_TRACK_TYPE_AUDIO, and @caps is a subset of
    /// "audio/x-raw(ANY)", then a #GESAudioTrack is created. This will
    /// automatically choose a gap creation method suitable for audio data, and
    /// will set the #GESTrack:restriction-caps to the default for
    /// #GESAudioTrack. You may prefer to use the ges_audio_track_new() method
    /// instead.
    /// </para>
    /// <para>
    /// Otherwise, a plain #GESTrack is returned. You will likely want to set
    /// the #GESTrack:restriction-caps and call
    /// ges_track_set_create_element_for_gap_func() on the returned track.
    /// </para>
    /// <para>
    /// The <c>caps</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="type">The #GESTrack:track-type for the track</param>
    /// <param name="caps">
    /// The #GESTrack:caps for the track
    /// The call consumes it: <paramref name="caps"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>A new track.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="caps"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// <paramref name="caps"/> was disposed.
    /// </exception>
    public static GES.Track New(GES.TrackType type, Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        nint capsNative = caps.Handle;
        nint capsOwned = Gst.GstNative.MiniObjectRef(capsNative);
        nint nativeResult = GesTrackNew((int)type, capsOwned);
        caps.Dispose();
        return Gst.GObject.Object.FromNative<GES.Track>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_track_new returned no value.");
    }

    /// <summary>See ges_track_add_element(), which also gives an error.</summary>
    /// <param name="object">The element to add</param>
    /// <returns>%TRUE if @object was successfully added to @track.</returns>
    public bool AddElement(GES.TrackElement @object)
    {
        ArgumentNullException.ThrowIfNull(@object);
        int nativeResult = GesTrackAddElement(Handle, @object.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(@object);
        return nativeResult != 0;
    }

    /// <summary>
    /// Adds the given track element to the track, which takes ownership of the
    /// element.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that this can fail if it would break a configuration rule of the
    /// track's #GESTimeline.
    /// </para>
    /// <para>Note that a #GESTrackElement can only be added to one track.</para>
    /// </remarks>
    /// <param name="object">The element to add</param>
    /// <returns>%TRUE if @object was successfully added to @track.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool AddElementFull(GES.TrackElement @object)
    {
        ArgumentNullException.ThrowIfNull(@object);
        nint errorNative = 0;
        int nativeResult = GesTrackAddElementFull(Handle, @object.Handle, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(@object);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Commits all the pending changes for the elements contained in the
    /// track.
    /// </summary>
    /// <remarks>
    /// <para>
    /// When changes are made to the timing or priority of elements within a
    /// track, they are not directly executed for the underlying
    /// #nlecomposition and its children. This method will finally execute
    /// these changes so they are reflected in the data output of the track.
    /// </para>
    /// <para>
    /// Any pending changes will be executed in the backend. The
    /// #GESTimeline::commited signal will be emitted once this has completed.
    /// </para>
    /// <para>
    /// Note that ges_timeline_commit() will call this method on all of its
    /// tracks, so you are unlikely to need to use this directly.
    /// </para>
    /// </remarks>
    /// <returns>
    /// %TRUE if pending changes were committed, or %FALSE if nothing
    /// needed to be committed.
    /// </returns>
    public bool Commit()
    {
        int nativeResult = GesTrackCommit(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get the #GESTrack:caps of the track.</summary>
    /// <returns>
    /// The caps of @track.
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Caps? GetCaps()
    {
        nint nativeResult = GesTrackGetCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>
    /// Gets the track elements contained in the track. The returned list is
    /// sorted by the element's #GESTimelineElement:priority and
    /// #GESTimelineElement:start.
    /// </summary>
    /// <returns>
    /// A list of
    /// all the #GESTrackElement-s in @track.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.TrackElement> GetElements()
    {
        nint nativeResult = GesTrackGetElements(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.TrackElement> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.TrackElement>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Gets the #GESTrack:mixing of the track.</summary>
    /// <returns>Whether @track is mixing.</returns>
    public bool GetMixing()
    {
        int nativeResult = GesTrackGetMixing(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Gets the #GESTrack:restriction-caps of the track.</summary>
    /// <returns>The restriction-caps of @track.</returns>
    public Gst.Caps? GetRestrictionCaps()
    {
        nint nativeResult = GesTrackGetRestrictionCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the timeline this track belongs to.</summary>
    /// <returns>
    /// The timeline that @track belongs to, or %NULL if
    /// it does not belong to a timeline.
    /// </returns>
    public GES.Timeline? GetTimeline()
    {
        nint nativeResult = GesTrackGetTimeline(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.Timeline>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>See ges_track_remove_element_full(), which also returns an error.</summary>
    /// <param name="object">The element to remove</param>
    /// <returns>%TRUE if @object was successfully removed from @track.</returns>
    public bool RemoveElement(GES.TrackElement @object)
    {
        ArgumentNullException.ThrowIfNull(@object);
        int nativeResult = GesTrackRemoveElement(Handle, @object.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(@object);
        return nativeResult != 0;
    }

    /// <summary>
    /// Removes the given track element from the track, which revokes
    /// ownership of the element.
    /// </summary>
    /// <param name="object">The element to remove</param>
    /// <returns>%TRUE if @object was successfully removed from @track.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool RemoveElementFull(GES.TrackElement @object)
    {
        ArgumentNullException.ThrowIfNull(@object);
        nint errorNative = 0;
        int nativeResult = GesTrackRemoveElementFull(Handle, @object.Handle, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(@object);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>Sets the #GESTrack:mixing for the track.</summary>
    /// <param name="mixing">Whether @track should be mixing</param>
    public void SetMixing(bool mixing)
    {
        GesTrackSetMixing(Handle, mixing ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the #GESTrack:restriction-caps for the track.</summary>
    /// <remarks>
    /// <para>
    /// &gt; **NOTE**: Restriction caps are **not** taken into account when
    /// &gt; using #GESPipeline:mode=#GES_PIPELINE_MODE_SMART_RENDER.
    /// </para>
    /// </remarks>
    /// <param name="caps">The new restriction-caps for @track</param>
    public void SetRestrictionCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        GesTrackSetRestrictionCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
    }

    /// <summary>
    /// Informs the track that it belongs to the given timeline. Calling this
    /// does not actually add the track to the timeline. For that, you should
    /// use ges_timeline_add_track(), which will also take care of informing
    /// the track that it belongs to the timeline. As such, there is no need
    /// for you to call this method.
    /// </summary>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    public void SetTimeline(GES.Timeline timeline)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        GesTrackSetTimeline(Handle, timeline.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
    }

    /// <summary>
    /// Updates the #GESTrack:restriction-caps of the track using the fields
    /// found in the given caps. Each of the #GstStructure-s in @caps is
    /// compared against the existing structure with the same index in the
    /// current #GESTrack:restriction-caps. If there is no corresponding
    /// existing structure at that index, then the new structure is simply
    /// copied to that index. Otherwise, any fields in the new structure are
    /// copied into the existing structure. This will replace existing values,
    /// and may introduce new ones, but any fields 'missing' in the new
    /// structure are left unchanged in the existing structure.
    /// </summary>
    /// <remarks>
    /// <para>
    /// For example, if the existing #GESTrack:restriction-caps are
    /// "video/x-raw, width=480, height=360", and the updating caps is
    /// "video/x-raw, format=I420, width=500; video/x-bayer, width=400", then
    /// the new #GESTrack:restriction-caps after calling this will be
    /// "video/x-raw, width=500, height=360, format=I420; video/x-bayer,
    /// width=400".
    /// </para>
    /// </remarks>
    /// <param name="caps">The caps to update the restriction-caps with</param>
    public void UpdateRestrictionCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        GesTrackUpdateRestrictionCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
    }

    /// <summary>
    /// Whether the track should support the mixing of #GESLayer data, such
    /// as composing the video data of each layer (when part of the video
    /// data is transparent, the next layer will become visible) or adding
    /// together the audio data. As such, for audio and video tracks, you'll
    /// likely want to keep this set to %TRUE.
    /// </summary>
    public bool Mixing
    {
        get => GetMixing();
        set => SetMixing(value);
    }

    /// <summary>Current duration of the track</summary>
    /// <remarks>
    /// <para>Default value: O</para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public ulong Duration
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("duration");
            return holder.GetUInt64();
        }
    }

    /// <summary>The #nlecomposition:id of the underlying #nlecomposition.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public string? Id
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("id");
            return holder.GetString();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("id");
            holder.SetString(value);
            SetPropertyValue("id", in holder);
        }
    }

    /// <summary>
    /// The track type of the track. This controls the type of
    /// #GESTrackElement-s that can be added to the track. This should
    /// match with the track's #GESTrack:caps.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Once a track has been added to a #GESTimeline, you should not change
    /// this.
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>The property is construct-only and therefore read-only here.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public GES.TrackType TrackType
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("track-type");
            return (GES.TrackType)holder.GetFlags();
        }
    }

    /// <summary>
    /// This signal will be emitted once the changes initiated by
    /// ges_track_commit() have been executed in the backend. In particular,
    /// this will be emitted whenever the underlying #nlecomposition has been
    /// committed (see #nlecomposition::commited).
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler Committed
    {
        add => GES.SignalConnections.Add(this, "commited", (nint)(delegate* unmanaged[Cdecl]<nint, nint, void>)&CommittedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "commited", value);
    }

    /// <summary>The native handler of the <c>commited</c> signal of <c>GESTrack</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void CommittedTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler>(userData) is not { } handler)
            {
                return;
            }

            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>track-element-added</c> signal of <c>GESTrack</c>.</summary>
    public sealed class TrackElementAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="TrackElementAddedSignalArgs"/> class.</summary>
        /// <param name="effect">The element that was added</param>
        internal TrackElementAddedSignalArgs(GES.TrackElement effect)
        {
            Effect = effect;
        }

        /// <summary>The element that was added</summary>
        public GES.TrackElement Effect { get; }
    }

    /// <summary>Will be emitted after a track element is added to the track.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Track.TrackElementAddedSignalArgs> TrackElementAdded
    {
        add => GES.SignalConnections.Add(this, "track-element-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&TrackElementAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "track-element-added", value);
    }

    /// <summary>The native handler of the <c>track-element-added</c> signal of <c>GESTrack</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void TrackElementAddedTrampoline(nint instance, nint effect, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Track.TrackElementAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.TrackElement effectValue = Gst.GObject.Object.FromNative<GES.TrackElement>(effect, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The track-element-added signal of GESTrack passed no effect.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Track.TrackElementAddedSignalArgs(effectValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>track-element-removed</c> signal of <c>GESTrack</c>.</summary>
    public sealed class TrackElementRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="TrackElementRemovedSignalArgs"/> class.</summary>
        /// <param name="effect">The element that was removed</param>
        internal TrackElementRemovedSignalArgs(GES.TrackElement effect)
        {
            Effect = effect;
        }

        /// <summary>The element that was removed</summary>
        public GES.TrackElement Effect { get; }
    }

    /// <summary>Will be emitted after a track element is removed from the track.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Track.TrackElementRemovedSignalArgs> TrackElementRemoved
    {
        add => GES.SignalConnections.Add(this, "track-element-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&TrackElementRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "track-element-removed", value);
    }

    /// <summary>The native handler of the <c>track-element-removed</c> signal of <c>GESTrack</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void TrackElementRemovedTrampoline(nint instance, nint effect, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Track.TrackElementRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.TrackElement effectValue = Gst.GObject.Object.FromNative<GES.TrackElement>(effect, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The track-element-removed signal of GESTrack passed no effect.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Track.TrackElementRemovedSignalArgs(effectValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>ges_track_new</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_new")]
    private static partial nint GesTrackNew(int type, nint caps);

    /// <summary>The <c>ges_track_add_element</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_add_element")]
    private static partial int GesTrackAddElement(nint track, nint @object);

    /// <summary>The <c>ges_track_add_element_full</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_add_element_full")]
    private static partial int GesTrackAddElementFull(nint track, nint @object, nint* error);

    /// <summary>The <c>ges_track_commit</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_commit")]
    private static partial int GesTrackCommit(nint track);

    /// <summary>The <c>ges_track_get_caps</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_get_caps")]
    private static partial nint GesTrackGetCaps(nint track);

    /// <summary>The <c>ges_track_get_elements</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_get_elements")]
    private static partial nint GesTrackGetElements(nint track);

    /// <summary>The <c>ges_track_get_mixing</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_get_mixing")]
    private static partial int GesTrackGetMixing(nint track);

    /// <summary>The <c>ges_track_get_restriction_caps</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_get_restriction_caps")]
    private static partial nint GesTrackGetRestrictionCaps(nint track);

    /// <summary>The <c>ges_track_get_timeline</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_get_timeline")]
    private static partial nint GesTrackGetTimeline(nint track);

    /// <summary>The <c>ges_track_remove_element</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_remove_element")]
    private static partial int GesTrackRemoveElement(nint track, nint @object);

    /// <summary>The <c>ges_track_remove_element_full</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_remove_element_full")]
    private static partial int GesTrackRemoveElementFull(nint track, nint @object, nint* error);

    /// <summary>The <c>ges_track_set_mixing</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_set_mixing")]
    private static partial void GesTrackSetMixing(nint track, int mixing);

    /// <summary>The <c>ges_track_set_restriction_caps</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_set_restriction_caps")]
    private static partial void GesTrackSetRestrictionCaps(nint track, nint caps);

    /// <summary>The <c>ges_track_set_timeline</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_set_timeline")]
    private static partial void GesTrackSetTimeline(nint track, nint timeline);

    /// <summary>The <c>ges_track_update_restriction_caps</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_track_update_restriction_caps")]
    private static partial void GesTrackUpdateRestrictionCaps(nint track, nint caps);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESTrack</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_track_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Track(handle, transfer);
}
