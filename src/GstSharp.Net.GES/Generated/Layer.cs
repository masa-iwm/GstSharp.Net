// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>#GESLayer-s are responsible for collecting and ordering #GESClip-s.</summary>
/// <remarks>
/// <para>
/// A layer within a timeline will have an associated priority,
/// corresponding to their index within the timeline. A layer with the
/// index/priority 0 will have the highest priority and the layer with the
/// largest index will have the lowest priority (the order of priorities,
/// in this sense, is the _reverse_ of the numerical ordering of the
/// indices). ges_timeline_move_layer() should be used if you wish to
/// change how layers are prioritised in a timeline.
/// </para>
/// <para>
/// Layers with higher priorities will have their content priorities
/// over content from lower priority layers, similar to how layers are
/// used in image editing. For example, if two separate layers both
/// display video content, then the layer with the higher priority will
/// have its images shown first. The other layer will only have its image
/// shown if the higher priority layer has no content at the given
/// playtime, or is transparent in some way. Audio content in separate
/// layers will simply play in addition.
/// </para>
/// </remarks>
public unsafe partial class Layer : Gst.GObject.InitiallyUnowned, GES.IExtractable, GES.IMetaContainer
{
    /// <summary>Wraps a native <c>GESLayer</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESLayer</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Layer(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new layer.</summary>
    /// <returns>A new layer.</returns>
    public static GES.Layer New()
    {
        nint nativeResult = GesLayerNew();
        return Gst.GObject.Object.FromNative<GES.Layer>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_layer_new returned no value.");
    }

    /// <summary>See ges_layer_add_asset_full(), which also gives an error.</summary>
    /// <param name="asset">The asset to extract the new clip from</param>
    /// <param name="start">
    /// The #GESTimelineElement:start value to set on the new clip
    /// If `start == #GST_CLOCK_TIME_NONE`, it will be added to the end
    /// of @layer, i.e. it will be set to @layer's duration
    /// </param>
    /// <param name="inpoint">
    /// The #GESTimelineElement:in-point value to set on the new
    /// clip
    /// </param>
    /// <param name="duration">
    /// The #GESTimelineElement:duration value to set on the new
    /// clip
    /// </param>
    /// <param name="trackTypes">
    /// The #GESClip:supported-formats to set on the the new
    /// clip, or #GES_TRACK_TYPE_UNKNOWN to use the default
    /// </param>
    /// <returns>The newly created clip.</returns>
    public GES.Clip? AddAsset(GES.Asset asset, Gst.ClockTime start, Gst.ClockTime inpoint, Gst.ClockTime duration, GES.TrackType trackTypes)
    {
        ArgumentNullException.ThrowIfNull(asset);
        nint nativeResult = GesLayerAddAsset(Handle, asset.Handle, start.Nanoseconds, inpoint.Nanoseconds, duration.Nanoseconds, (int)trackTypes);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(asset);
        return Gst.GObject.Object.FromNative<GES.Clip>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>
    /// Extracts a new clip from an asset and adds it to the layer with
    /// the given properties.
    /// </summary>
    /// <param name="asset">The asset to extract the new clip from</param>
    /// <param name="start">
    /// The #GESTimelineElement:start value to set on the new clip
    /// If `start == #GST_CLOCK_TIME_NONE`, it will be added to the end
    /// of @layer, i.e. it will be set to @layer's duration
    /// </param>
    /// <param name="inpoint">
    /// The #GESTimelineElement:in-point value to set on the new
    /// clip
    /// </param>
    /// <param name="duration">
    /// The #GESTimelineElement:duration value to set on the new
    /// clip
    /// </param>
    /// <param name="trackTypes">
    /// The #GESClip:supported-formats to set on the the new
    /// clip, or #GES_TRACK_TYPE_UNKNOWN to use the default
    /// </param>
    /// <returns>The newly created clip.</returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public GES.Clip AddAssetFull(GES.Asset asset, Gst.ClockTime start, Gst.ClockTime inpoint, Gst.ClockTime duration, GES.TrackType trackTypes)
    {
        ArgumentNullException.ThrowIfNull(asset);
        nint errorNative = 0;
        nint nativeResult = GesLayerAddAssetFull(Handle, asset.Handle, start.Nanoseconds, inpoint.Nanoseconds, duration.Nanoseconds, (int)trackTypes, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(asset);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return Gst.GObject.Object.FromNative<GES.Clip>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_layer_add_asset_full returned no value.");
    }

    /// <summary>See ges_layer_add_clip_full(), which also gives an error.</summary>
    /// <param name="clip">The clip to add</param>
    /// <returns>
    /// %TRUE if @clip was properly added to @layer, or %FALSE
    /// if @layer refused to add @clip.
    /// </returns>
    public bool AddClip(GES.Clip clip)
    {
        ArgumentNullException.ThrowIfNull(clip);
        int nativeResult = GesLayerAddClip(Handle, clip.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(clip);
        return nativeResult != 0;
    }

    /// <summary>
    /// Adds the given clip to the layer. If the method succeeds, the layer
    /// will take ownership of the clip.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This method will fail and return %FALSE if @clip already resides in
    /// some layer. It can also fail if the additional clip breaks some
    /// compositional rules (see #GESTimelineElement).
    /// </para>
    /// </remarks>
    /// <param name="clip">The clip to add</param>
    /// <returns>
    /// %TRUE if @clip was properly added to @layer, or %FALSE
    /// if @layer refused to add @clip.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool AddClipFull(GES.Clip clip)
    {
        ArgumentNullException.ThrowIfNull(clip);
        nint errorNative = 0;
        int nativeResult = GesLayerAddClipFull(Handle, clip.Handle, &errorNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(clip);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets whether the layer is active for the given track. See
    /// ges_layer_set_active_for_tracks().
    /// </summary>
    /// <param name="track">The #GESTrack to check if @layer is currently active for</param>
    /// <returns>%TRUE if @layer is active for @track, or %FALSE otherwise.</returns>
    public bool GetActiveForTrack(GES.Track track)
    {
        ArgumentNullException.ThrowIfNull(track);
        int nativeResult = GesLayerGetActiveForTrack(Handle, track.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(track);
        return nativeResult != 0;
    }

    /// <summary>Gets the #GESLayer:auto-transition of the layer.</summary>
    /// <returns>%TRUE if transitions are automatically added to @layer.</returns>
    public bool GetAutoTransition()
    {
        int nativeResult = GesLayerGetAutoTransition(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get the #GESClip-s contained in this layer.</summary>
    /// <returns>
    /// A list of clips in
    /// @layer.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Clip> GetClips()
    {
        nint nativeResult = GesLayerGetClips(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.Clip> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Clip>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Gets the clips within the layer that appear between @start and @end.</summary>
    /// <param name="start">Start of the interval</param>
    /// <param name="end">End of the interval</param>
    /// <returns>
    /// A list of #GESClip-s
    /// that intersect the interval `[start, end)` in @layer.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<GES.Clip> GetClipsInInterval(Gst.ClockTime start, Gst.ClockTime end)
    {
        nint nativeResult = GesLayerGetClipsInInterval(Handle, start.Nanoseconds, end.Nanoseconds);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<GES.Clip> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<GES.Clip>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Retrieves the duration of the layer, which is the difference
    /// between the start of the layer (always time 0) and the end (which will
    /// be the end time of the final clip).
    /// </summary>
    /// <returns>The duration of @layer.</returns>
    public Gst.ClockTime GetDuration()
    {
        ulong nativeResult = GesLayerGetDuration(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the priority of the layer. When inside a timeline, this is its
    /// index in the timeline. See ges_timeline_move_layer().
    /// </summary>
    /// <returns>The priority of @layer within its timeline.</returns>
    public uint GetPriority()
    {
        uint nativeResult = GesLayerGetPriority(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Gets the timeline that the layer is a part of.</summary>
    /// <returns>
    /// The timeline that @layer
    /// is currently part of, or %NULL if it is not associated with any
    /// timeline.
    /// </returns>
    public GES.Timeline? GetTimeline()
    {
        nint nativeResult = GesLayerGetTimeline(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<GES.Timeline>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>
    /// Convenience method to check if the layer is empty (doesn't contain
    /// any #GESClip), or not.
    /// </summary>
    /// <returns>
    /// %TRUE if @layer is empty, %FALSE if it contains at least
    /// one clip.
    /// </returns>
    public bool IsEmpty()
    {
        int nativeResult = GesLayerIsEmpty(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Removes the given clip from the layer.</summary>
    /// <param name="clip">The clip to remove</param>
    /// <returns>
    /// %TRUE if @clip was removed from @layer, or %FALSE if the
    /// operation failed.
    /// </returns>
    public bool RemoveClip(GES.Clip clip)
    {
        ArgumentNullException.ThrowIfNull(clip);
        int nativeResult = GesLayerRemoveClip(Handle, clip.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(clip);
        return nativeResult != 0;
    }

    /// <summary>
    /// Activate or deactivate track elements in @tracks (or in all tracks if @tracks
    /// is %NULL).
    /// </summary>
    /// <remarks>
    /// <para>
    /// When a layer is deactivated for a track, all the #GESTrackElement-s in
    /// the track that belong to a #GESClip in the layer will no longer be
    /// active in the track, regardless of their individual
    /// #GESTrackElement:active value.
    /// </para>
    /// <para>
    /// Note that by default a layer will be active for all of its
    /// timeline's tracks.
    /// </para>
    /// <para>
    /// A null list means every track of the timeline (ges-layer.c:1083). Every
    /// track named has to belong to the timeline of this layer; one that does not
    /// makes the call answer false (ges-layer.c:1089).
    /// </para>
    /// </remarks>
    /// <param name="active">Whether elements in @tracks should be active or not</param>
    /// <param name="tracks">
    /// The list of
    /// tracks @layer should be (de-)active in, or %NULL to include all the tracks
    /// in the @layer's timeline
    /// The call reads the list while it runs and copies whatever it keeps. A
    /// temporary native list is built for the call and released when it returns,
    /// and an empty sequence is passed as the null pointer, which is how C spells
    /// the empty list.
    /// </param>
    /// <returns>%TRUE if the operation worked %FALSE otherwise.</returns>
    public bool SetActiveForTracks(bool active, System.Collections.Generic.IEnumerable<GES.Track>? tracks)
    {
        using Gst.Interop.GListScope tracksScope = Gst.Interop.GMarshal.AllocList(tracks, singly: false);
        int nativeResult = GesLayerSetActiveForTracks(Handle, active ? 1 : 0, tracksScope.Head);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets #GESLayer:auto-transition for the layer. Use
    /// ges_timeline_set_auto_transition() if you want all layers within a
    /// #GESTimeline to have #GESLayer:auto-transition set to %TRUE. Use this
    /// method if you want different values for different layers (and make sure
    /// to keep #GESTimeline:auto-transition as %FALSE for the corresponding
    /// timeline).
    /// </summary>
    /// <param name="autoTransition">
    /// Whether transitions should be automatically added to
    /// the layer
    /// </param>
    public void SetAutoTransition(bool autoTransition)
    {
        GesLayerSetAutoTransition(Handle, autoTransition ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the layer to the given priority. See #GESLayer:priority.</summary>
    /// <param name="priority">The priority to set</param>
    [Obsolete("use #ges_timeline_move_layer instead. This deprecation means that you will not need to handle layer priorities at all yourself, GES will make sure there is never 'gaps' between layer priorities. (deprecated since 1.16.0)")]
    public void SetPriority(uint priority)
    {
        GesLayerSetPriority(Handle, priority);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>ges_layer_set_timeline</c> function.</summary>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    public void SetTimeline(GES.Timeline timeline)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        GesLayerSetTimeline(Handle, timeline.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
    }

    /// <summary>
    /// Whether to automatically create a #GESTransitionClip whenever two
    /// #GESSource-s that both belong to a #GESClip in the layer overlap.
    /// See #GESTimeline for what counts as an overlap.
    /// </summary>
    /// <remarks>
    /// <para>
    /// When a layer is added to a #GESTimeline, if this property is left as
    /// %FALSE, but the timeline's #GESTimeline:auto-transition is %TRUE, it
    /// will be set to %TRUE as well.
    /// </para>
    /// </remarks>
    public bool AutoTransition
    {
        get => GetAutoTransition();
        set => SetAutoTransition(value);
    }

    /// <summary>
    /// The priority of the layer in the #GESTimeline. 0 is the highest
    /// priority. Conceptually, a timeline is a stack of layers,
    /// and the priority of the layer represents its position in the stack. Two
    /// layers should not have the same priority within a given GESTimeline.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that the timeline needs to be committed (with #ges_timeline_commit)
    /// for the change to be taken into account.
    /// </para>
    /// </remarks>
    [Obsolete("use #ges_timeline_move_layer instead. This deprecation means that you will not need to handle layer priorities at all yourself, GES will make sure there is never 'gaps' between layer priorities. (deprecated since 1.16.0)")]
    public uint Priority
    {
        get => GetPriority();
        set => SetPriority(value);
    }

    /// <summary>The arguments of the <c>active-changed</c> signal of <c>GESLayer</c>.</summary>
    public sealed class ActiveChangedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ActiveChangedSignalArgs"/> class.</summary>
        /// <param name="active">Whether @layer has been made active or de-active in the @tracks</param>
        /// <param name="tracks">
        /// A list of #GESTrack
        /// which have been activated or deactivated
        /// </param>
        internal ActiveChangedSignalArgs(bool active, GES.Track[] tracks)
        {
            Active = active;
            Tracks = tracks;
        }

        /// <summary>Whether @layer has been made active or de-active in the @tracks</summary>
        public bool Active { get; }

        /// <summary>
        /// A list of #GESTrack
        /// which have been activated or deactivated
        /// </summary>
        /// <remarks>
        /// A snapshot: the elements are read out of the array the emitter passes
        /// before the handler runs, and the library frees its own array when the
        /// emission ends. The array here is the handler's own and may be kept; the
        /// objects in it are the usual borrowed wrappers. An emission that carries
        /// no array is the empty array.
        /// </remarks>
        public GES.Track[] Tracks { get; }
    }

    /// <summary>
    /// Will be emitted whenever the layer is activated or deactivated
    /// for some #GESTrack. See ges_layer_set_active_for_tracks().
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Layer.ActiveChangedSignalArgs> ActiveChanged
    {
        add => GES.SignalConnections.Add(this, "active-changed", (nint)(delegate* unmanaged[Cdecl]<nint, int, nint, nint, void>)&ActiveChangedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "active-changed", value);
    }

    /// <summary>The native handler of the <c>active-changed</c> signal of <c>GESLayer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ActiveChangedTrampoline(nint instance, int active, nint tracks, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Layer.ActiveChangedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            bool activeValue = active != 0;
            GES.Track[] tracksValue = Gst.GLib.PtrArray.ToArray<GES.Track>(tracks);
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Layer.ActiveChangedSignalArgs(activeValue, tracksValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>clip-added</c> signal of <c>GESLayer</c>.</summary>
    public sealed class ClipAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ClipAddedSignalArgs"/> class.</summary>
        /// <param name="clip">The clip that was added</param>
        internal ClipAddedSignalArgs(GES.Clip clip)
        {
            Clip = clip;
        }

        /// <summary>The clip that was added</summary>
        public GES.Clip Clip { get; }
    }

    /// <summary>Will be emitted after the clip is added to the layer.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Layer.ClipAddedSignalArgs> ClipAdded
    {
        add => GES.SignalConnections.Add(this, "clip-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&ClipAddedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "clip-added", value);
    }

    /// <summary>The native handler of the <c>clip-added</c> signal of <c>GESLayer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ClipAddedTrampoline(nint instance, nint clip, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Layer.ClipAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Clip clipValue = Gst.GObject.Object.FromNative<GES.Clip>(clip, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The clip-added signal of GESLayer passed no clip.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Layer.ClipAddedSignalArgs(clipValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>clip-removed</c> signal of <c>GESLayer</c>.</summary>
    public sealed class ClipRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ClipRemovedSignalArgs"/> class.</summary>
        /// <param name="clip">The clip that was removed</param>
        internal ClipRemovedSignalArgs(GES.Clip clip)
        {
            Clip = clip;
        }

        /// <summary>The clip that was removed</summary>
        public GES.Clip Clip { get; }
    }

    /// <summary>Will be emitted after the clip is removed from the layer.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<GES.Layer.ClipRemovedSignalArgs> ClipRemoved
    {
        add => GES.SignalConnections.Add(this, "clip-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&ClipRemovedTrampoline, value);
        remove => GES.SignalConnections.Remove(this, "clip-removed", value);
    }

    /// <summary>The native handler of the <c>clip-removed</c> signal of <c>GESLayer</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ClipRemovedTrampoline(nint instance, nint clip, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<GES.Layer.ClipRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            GES.Clip clipValue = Gst.GObject.Object.FromNative<GES.Clip>(clip, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The clip-removed signal of GESLayer passed no clip.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new GES.Layer.ClipRemovedSignalArgs(clipValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>ges_layer_new</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_new")]
    private static partial nint GesLayerNew();

    /// <summary>The <c>ges_layer_add_asset</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_add_asset")]
    private static partial nint GesLayerAddAsset(nint layer, nint asset, ulong start, ulong inpoint, ulong duration, int trackTypes);

    /// <summary>The <c>ges_layer_add_asset_full</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_add_asset_full")]
    private static partial nint GesLayerAddAssetFull(nint layer, nint asset, ulong start, ulong inpoint, ulong duration, int trackTypes, nint* error);

    /// <summary>The <c>ges_layer_add_clip</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_add_clip")]
    private static partial int GesLayerAddClip(nint layer, nint clip);

    /// <summary>The <c>ges_layer_add_clip_full</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_add_clip_full")]
    private static partial int GesLayerAddClipFull(nint layer, nint clip, nint* error);

    /// <summary>The <c>ges_layer_get_active_for_track</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_active_for_track")]
    private static partial int GesLayerGetActiveForTrack(nint layer, nint track);

    /// <summary>The <c>ges_layer_get_auto_transition</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_auto_transition")]
    private static partial int GesLayerGetAutoTransition(nint layer);

    /// <summary>The <c>ges_layer_get_clips</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_clips")]
    private static partial nint GesLayerGetClips(nint layer);

    /// <summary>The <c>ges_layer_get_clips_in_interval</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_clips_in_interval")]
    private static partial nint GesLayerGetClipsInInterval(nint layer, ulong start, ulong end);

    /// <summary>The <c>ges_layer_get_duration</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_duration")]
    private static partial ulong GesLayerGetDuration(nint layer);

    /// <summary>The <c>ges_layer_get_priority</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_priority")]
    private static partial uint GesLayerGetPriority(nint layer);

    /// <summary>The <c>ges_layer_get_timeline</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_timeline")]
    private static partial nint GesLayerGetTimeline(nint layer);

    /// <summary>The <c>ges_layer_is_empty</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_is_empty")]
    private static partial int GesLayerIsEmpty(nint layer);

    /// <summary>The <c>ges_layer_remove_clip</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_remove_clip")]
    private static partial int GesLayerRemoveClip(nint layer, nint clip);

    /// <summary>The <c>ges_layer_set_active_for_tracks</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_set_active_for_tracks")]
    private static partial int GesLayerSetActiveForTracks(nint layer, int active, nint tracks);

    /// <summary>The <c>ges_layer_set_auto_transition</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_set_auto_transition")]
    private static partial void GesLayerSetAutoTransition(nint layer, int autoTransition);

    /// <summary>The <c>ges_layer_set_priority</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_set_priority")]
    private static partial void GesLayerSetPriority(nint layer, uint priority);

    /// <summary>The <c>ges_layer_set_timeline</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_layer_set_timeline")]
    private static partial void GesLayerSetTimeline(nint layer, nint timeline);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESLayer</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_layer_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Layer(handle, transfer);
}
