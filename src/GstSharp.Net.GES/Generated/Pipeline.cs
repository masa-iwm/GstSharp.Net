// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>
/// A #GESPipeline can take an audio-video #GESTimeline and conveniently
/// link its #GESTrack-s to an internal #playsink element, for
/// preview/playback, and an internal #encodebin element, for rendering.
/// You can switch between these modes using ges_pipeline_set_mode().
/// </summary>
/// <remarks>
/// <para>
/// You can choose the specific audio and video sinks used for previewing
/// the timeline by setting the #GESPipeline:audio-sink and
/// #GESPipeline:video-sink properties.
/// </para>
/// <para>
/// You can set the encoding and save location used in rendering by calling
/// ges_pipeline_set_render_settings().
/// </para>
/// </remarks>
public unsafe partial class Pipeline : Gst.Pipeline, Gst.IChildProxy, Gst.Video.IVideoOverlay
{
    /// <summary>Wraps a native <c>GESPipeline</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESPipeline</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Pipeline(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new pipeline.</summary>
    /// <returns>The newly created pipeline.</returns>
    public static GES.Pipeline New()
    {
        nint nativeResult = GesPipelineNew();
        return Gst.GObject.Object.FromNative<GES.Pipeline>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("ges_pipeline_new returned no value.");
    }

    /// <summary>Gets the #GESPipeline:mode of the pipeline.</summary>
    /// <returns>The current mode of @pipeline.</returns>
    public GES.PipelineFlags GetMode()
    {
        int nativeResult = GesPipelineGetMode(Handle);
        System.GC.KeepAlive(this);
        return (GES.PipelineFlags)nativeResult;
    }

    /// <summary>
    /// Gets a sample from the pipeline of the currently displayed image in
    /// preview, in the specified format.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that if you use "ANY" caps for @caps, then the current format of
    /// the image is used. You can retrieve these caps from the returned sample
    /// with gst_sample_get_caps().
    /// </para>
    /// </remarks>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>
    /// A sample of @self's current image preview in
    /// the format given by @caps, or %NULL if an error prevented fetching the
    /// sample.
    /// </returns>
    public Gst.Sample? GetThumbnail(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        nint nativeResult = GesPipelineGetThumbnail(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return Gst.Sample.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets a sample from the pipeline of the currently displayed image in
    /// preview, in the 24-bit "RGB" format and of the desired width and
    /// height.
    /// </summary>
    /// <remarks>
    /// <para>See ges_pipeline_get_thumbnail().</para>
    /// </remarks>
    /// <param name="width">The <c>width</c> argument.</param>
    /// <param name="height">The <c>height</c> argument.</param>
    /// <returns>
    /// A sample of @self's current image preview in
    /// the "RGB" format, scaled to @width and @height, or %NULL if an error
    /// prevented fetching the sample.
    /// </returns>
    public Gst.Sample? GetThumbnailRgb24(int width, int height)
    {
        nint nativeResult = GesPipelineGetThumbnailRgb24(Handle, width, height);
        System.GC.KeepAlive(this);
        return Gst.Sample.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Gets the #GESPipeline:audio-sink of the pipeline.</summary>
    /// <returns>The audio sink used by @self for preview.</returns>
    public Gst.Element? PreviewGetAudioSink()
    {
        nint nativeResult = GesPipelinePreviewGetAudioSink(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Element>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Gets the #GESPipeline:video-sink of the pipeline.</summary>
    /// <returns>The video sink used by @self for preview.</returns>
    public Gst.Element? PreviewGetVideoSink()
    {
        nint nativeResult = GesPipelinePreviewGetVideoSink(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Element>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Sets the #GESPipeline:audio-sink of the pipeline.</summary>
    /// <param name="sink">The <c>sink</c> argument.</param>
    public void PreviewSetAudioSink(Gst.Element? sink)
    {
        GesPipelinePreviewSetAudioSink(Handle, sink is null ? 0 : sink.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sink);
    }

    /// <summary>Sets the #GESPipeline:video-sink of the pipeline.</summary>
    /// <param name="sink">The <c>sink</c> argument.</param>
    public void PreviewSetVideoSink(Gst.Element? sink)
    {
        GesPipelinePreviewSetVideoSink(Handle, sink is null ? 0 : sink.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sink);
    }

    /// <summary>
    /// Saves the currently displayed image of the pipeline in preview to the
    /// given location, in the specified dimensions and format.
    /// </summary>
    /// <param name="width">The <c>width</c> argument.</param>
    /// <param name="height">The <c>height</c> argument.</param>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="location">The <c>location</c> argument.</param>
    /// <returns>
    /// %TRUE if @self's current image preview was successfully saved
    /// to @location using the given @format, @height and @width.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public bool SaveThumbnail(int width, int height, string format, string location)
    {
        ArgumentNullException.ThrowIfNull(format);
        System.Span<byte> formatBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope formatScope = Gst.Interop.GMarshal.StackUtf8(format, formatBuffer);
        ArgumentNullException.ThrowIfNull(location);
        System.Span<byte> locationBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope locationScope = Gst.Interop.GMarshal.StackUtf8(location, locationBuffer);
        nint errorNative = 0;
        int nativeResult = GesPipelineSaveThumbnail(Handle, width, height, formatScope.Pointer, locationScope.Pointer, &errorNative);
        System.GC.KeepAlive(this);
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return nativeResult != 0;
    }

    /// <summary>Sets the #GESPipeline:mode of the pipeline.</summary>
    /// <remarks>
    /// <para>
    /// Note that the pipeline will be set to #GST_STATE_NULL during this call to
    /// perform the necessary changes. You will need to set the state again yourself
    /// after calling this.
    /// </para>
    /// <para>
    /// &gt; **NOTE**: [Rendering settings](ges_pipeline_set_render_settings) need to be
    /// &gt; set before setting @mode to #GES_PIPELINE_MODE_RENDER or
    /// &gt; #GES_PIPELINE_MODE_SMART_RENDER, the call to this method will fail
    /// &gt; otherwise.
    /// </para>
    /// </remarks>
    /// <param name="mode">The <c>mode</c> argument.</param>
    /// <returns>%TRUE if the mode of @pipeline was successfully set to @mode.</returns>
    public bool SetMode(GES.PipelineFlags mode)
    {
        int nativeResult = GesPipelineSetMode(Handle, (int)mode);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Specifies encoding setting to be used by the pipeline to render its
    /// #GESPipeline:timeline, and where the result should be written to.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This method **must** be called before setting the pipeline mode to
    /// #GES_PIPELINE_MODE_RENDER.
    /// </para>
    /// </remarks>
    /// <param name="outputUri">The <c>outputUri</c> argument.</param>
    /// <param name="profile">The <c>profile</c> argument.</param>
    /// <returns>%TRUE if the settings were successfully set on @pipeline.</returns>
    public bool SetRenderSettings(string outputUri, Gst.Pbutils.EncodingProfile profile)
    {
        ArgumentNullException.ThrowIfNull(outputUri);
        System.Span<byte> outputUriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope outputUriScope = Gst.Interop.GMarshal.StackUtf8(outputUri, outputUriBuffer);
        ArgumentNullException.ThrowIfNull(profile);
        int nativeResult = GesPipelineSetRenderSettings(Handle, outputUriScope.Pointer, profile.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(profile);
        return nativeResult != 0;
    }

    /// <summary>
    /// Takes the given timeline and sets it as the #GESPipeline:timeline for
    /// the pipeline.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that you should only call this method once on a given pipeline
    /// because a pipeline can not have its #GESPipeline:timeline changed after
    /// it has been set.
    /// </para>
    /// </remarks>
    /// <param name="timeline">The <c>timeline</c> argument.</param>
    /// <returns>%TRUE if @timeline was successfully given to @pipeline.</returns>
    public bool SetTimeline(GES.Timeline timeline)
    {
        ArgumentNullException.ThrowIfNull(timeline);
        int nativeResult = GesPipelineSetTimeline(Handle, timeline.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(timeline);
        return nativeResult != 0;
    }

    /// <summary>
    /// The pipeline's mode. In preview mode (for audio or video, or both)
    /// the pipeline can display the timeline's content to an end user. In
    /// rendering mode the pipeline can encode the timeline's content and
    /// save it to a file.
    /// </summary>
    public GES.PipelineFlags Mode => GetMode();

    /// <summary>The <c>ges_pipeline_new</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_new")]
    private static partial nint GesPipelineNew();

    /// <summary>The <c>ges_pipeline_get_mode</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_get_mode")]
    private static partial int GesPipelineGetMode(nint pipeline);

    /// <summary>The <c>ges_pipeline_get_thumbnail</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_get_thumbnail")]
    private static partial nint GesPipelineGetThumbnail(nint self, nint caps);

    /// <summary>The <c>ges_pipeline_get_thumbnail_rgb24</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_get_thumbnail_rgb24")]
    private static partial nint GesPipelineGetThumbnailRgb24(nint self, int width, int height);

    /// <summary>The <c>ges_pipeline_preview_get_audio_sink</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_preview_get_audio_sink")]
    private static partial nint GesPipelinePreviewGetAudioSink(nint self);

    /// <summary>The <c>ges_pipeline_preview_get_video_sink</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_preview_get_video_sink")]
    private static partial nint GesPipelinePreviewGetVideoSink(nint self);

    /// <summary>The <c>ges_pipeline_preview_set_audio_sink</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_preview_set_audio_sink")]
    private static partial void GesPipelinePreviewSetAudioSink(nint self, nint sink);

    /// <summary>The <c>ges_pipeline_preview_set_video_sink</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_preview_set_video_sink")]
    private static partial void GesPipelinePreviewSetVideoSink(nint self, nint sink);

    /// <summary>The <c>ges_pipeline_save_thumbnail</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_save_thumbnail")]
    private static partial int GesPipelineSaveThumbnail(nint self, int width, int height, byte* format, byte* location, nint* error);

    /// <summary>The <c>ges_pipeline_set_mode</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_set_mode")]
    private static partial int GesPipelineSetMode(nint pipeline, int mode);

    /// <summary>The <c>ges_pipeline_set_render_settings</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_set_render_settings")]
    private static partial int GesPipelineSetRenderSettings(nint pipeline, byte* outputUri, nint profile);

    /// <summary>The <c>ges_pipeline_set_timeline</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_set_timeline")]
    private static partial int GesPipelineSetTimeline(nint pipeline, nint timeline);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESPipeline</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_pipeline_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Pipeline(handle, transfer);
}
