// <auto-generated/>
// Generated by GstSharp.Generator from GES-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace GES;

/// <summary>
/// Any GStreamer filter can be used as effects in GES. The only restriction we
/// have is that effects element should have a single [sinkpad](GST_PAD_SINK)
/// (which will be requested if necessary) and a single [srcpad](GST_PAD_SRC).
/// </summary>
/// <remarks>
/// <para>
/// Note that `gesaudiomixer` and `gescompositor` can be used as effects even
/// though they can have several sinkpads.
/// </para>
/// <para>## GES specific effects:</para>
/// <para>
/// * **`gesvideoscale`**: GES implements a specific scaling bin that allows
///   specifying where scaling will happen inside the chain of effects. By
///   default scaling can happen either in the source (if the source doesn't have
///   a specific size, like `videotestsrc` or [mixing](ges_track_set_mixing) has
///   been disabled) or in the mixing element otherwise, when adding that element
///   as an effect, GES guarantees that the scaling will happen in it. This can
///   be useful for example if you want to crop the video before scaling or apply
///   rounding corners to the video after scaling, etc...
/// </para>
/// <para>
/// &gt; Note: GES always adds converters (`audioconvert ! audioresample !
/// &gt; audioconvert` for audio effects and `videoconvert` for video effects) to
/// &gt; make it simpler for end users.
/// </para>
/// </remarks>
public unsafe partial class Effect : GES.BaseEffect, GES.IExtractable, GES.IMetaContainer
{
    /// <summary>Wraps a native <c>GESEffect</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GESEffect</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Effect(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Creates a new #GESEffect from the description of the bin. It should be
    /// possible to determine the type of the effect through the element
    /// 'klass' metadata of the GstElements that will be created.
    /// In that corner case, you should use:
    /// #ges_asset_request (GES_TYPE_EFFECT, "audio your ! bin ! description", NULL);
    /// and extract that asset to be in full control.
    /// </summary>
    /// <param name="binDescription">The <c>binDescription</c> argument.</param>
    /// <returns>
    /// a newly created #GESEffect, or %NULL if something went
    /// wrong.
    /// </returns>
    public static GES.Effect? New(string binDescription)
    {
        ArgumentNullException.ThrowIfNull(binDescription);
        System.Span<byte> binDescriptionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope binDescriptionScope = Gst.Interop.GMarshal.StackUtf8(binDescription, binDescriptionBuffer);
        nint nativeResult = GesEffectNew(binDescriptionScope.Pointer);
        return Gst.GObject.Object.FromNative<GES.Effect>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>
    /// The description of the effect bin with a gst-launch-style
    /// pipeline description.
    /// </summary>
    /// <remarks>
    /// <para>Example: "videobalance saturation=1.5 hue=+0.5"</para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>The property is construct-only and therefore read-only here.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public string? BinDescription
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("bin-description");
            return holder.GetString();
        }
    }

    /// <summary>The <c>ges_effect_new</c> entry point.</summary>
    [LibraryImport("GES", EntryPoint = "ges_effect_new")]
    private static partial nint GesEffectNew(byte* binDescription);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GESEffect</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GES", EntryPoint = "ges_effect_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Effect(handle, transfer);
}
