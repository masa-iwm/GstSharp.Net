// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>
/// This base class is for video encoders turning raw video into
/// encoded video data.
/// </summary>
/// <remarks>
/// <para>GstVideoEncoder and subclass should cooperate as follows.</para>
/// <para>## Configuration</para>
/// <para>
///   * Initially, GstVideoEncoder calls @start when the encoder element
///     is activated, which allows subclass to perform any global setup.
///   * GstVideoEncoder calls @set_format to inform subclass of the format
///     of input video data that it is about to receive.  Subclass should
///     setup for encoding and configure base class as appropriate
///     (e.g. latency). While unlikely, it might be called more than once,
///     if changing input parameters require reconfiguration.  Baseclass
///     will ensure that processing of current configuration is finished.
///   * GstVideoEncoder calls @stop at end of all processing.
/// </para>
/// <para>## Data processing</para>
/// <para>
///     * Base class collects input data and metadata into a frame and hands
///       this to subclass' @handle_frame.
/// </para>
/// <para>
///     * If codec processing results in encoded data, subclass should call
///       @gst_video_encoder_finish_frame to have encoded data pushed
///       downstream.
/// </para>
/// <para>
///     * If implemented, baseclass calls subclass @pre_push just prior to
///       pushing to allow subclasses to modify some metadata on the buffer.
///       If it returns GST_FLOW_OK, the buffer is pushed downstream.
/// </para>
/// <para>
///     * GstVideoEncoderClass will handle both srcpad and sinkpad events.
///       Sink events will be passed to subclass if @event callback has been
///       provided.
/// </para>
/// <para>## Shutdown phase</para>
/// <para>
///   * GstVideoEncoder class calls @stop to inform the subclass that data
///     parsing will be stopped.
/// </para>
/// <para>
/// Subclass is responsible for providing pad template caps for
/// source and sink pads. The pads need to be named "sink" and "src". It should
/// also be able to provide fixed src pad caps in @getcaps by the time it calls
/// @gst_video_encoder_finish_frame.
/// </para>
/// <para>Things that subclass need to take care of:</para>
/// <para>
///   * Provide pad templates
///   * Provide source pad caps before pushing the first buffer
///   * Accept data in @handle_frame and provide encoded results to
///      @gst_video_encoder_finish_frame.
/// </para>
/// <para>
/// The #GstVideoEncoder:qos property will enable the Quality-of-Service
/// features of the encoder which gather statistics about the real-time
/// performance of the downstream elements. If enabled, subclasses can
/// use gst_video_encoder_get_max_encode_time() to check if input frames
/// are already late and drop them right away to give a chance to the
/// pipeline to catch up.
/// </para>
/// </remarks>
public abstract unsafe partial class VideoEncoder : Gst.Element, Gst.IPreset
{
    /// <summary>Wraps a native <c>GstVideoEncoder</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstVideoEncoder</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected VideoEncoder(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Helper function that allocates a buffer to hold an encoded video frame
    /// for @encoder's current #GstVideoCodecState.
    /// </summary>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>allocated buffer</returns>
    public Gst.Buffer AllocateOutputBuffer(nuint size)
    {
        nint nativeResult = GstVideoEncoderAllocateOutputBuffer(Handle, size);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_encoder_allocate_output_buffer returned no value.");
    }

    /// <summary>
    /// Helper function that allocates a buffer to hold an encoded video frame for @encoder's
    /// current #GstVideoCodecState.  Subclass should already have configured video
    /// state and set src pad caps.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The buffer allocated here is owned by the frame and you should only
    /// keep references to the frame, not the buffer.
    /// </para>
    /// </remarks>
    /// <param name="frame">The <c>frame</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>%GST_FLOW_OK if an output buffer could be allocated</returns>
    public Gst.FlowReturn AllocateOutputFrame(Gst.Video.VideoCodecFrame frame, nuint size)
    {
        ArgumentNullException.ThrowIfNull(frame);
        int nativeResult = GstVideoEncoderAllocateOutputFrame(Handle, frame.Handle, size);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Removes @frame from the list of pending frames, releases it
    /// and posts a QoS message with the frame's details on the bus.
    /// Similar to calling gst_video_encoder_finish_frame() without a buffer
    /// attached to @frame, but this function additionally stores events
    /// from @frame as pending, to be pushed out alongside the next frame
    /// submitted via gst_video_encoder_finish_frame().
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// <para>Available since GStreamer 1.26.</para>
    /// </remarks>
    /// <param name="frame">
    /// The <c>frame</c> argument.
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public void DropFrame(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        GstVideoEncoderDropFrame(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
    }

    /// <summary>
    /// @frame must have a valid encoded data buffer, whose metadata fields
    /// are then appropriately set according to frame data or no buffer at
    /// all if the frame should be dropped.
    /// It is subsequently pushed downstream or provided to @pre_push.
    /// In any case, the frame is considered finished and released.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @frame does not have a buffer attached, it will be dropped, and
    /// a QoS message will be posted on the bus. Events from @frame will be
    /// pushed out immediately.
    /// </para>
    /// <para>
    /// After calling this function the output buffer of the frame is to be
    /// considered read-only. This function will also change the metadata
    /// of the buffer.
    /// </para>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="frame">
    /// The <c>frame</c> argument.
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn resulting from sending data downstream</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public Gst.FlowReturn FinishFrame(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        int nativeResult = GstVideoEncoderFinishFrame(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// If multiple subframes are produced for one input frame then use this method
    /// for each subframe, except for the last one. Before calling this function,
    /// you need to fill frame-&gt;output_buffer with the encoded buffer to push.
    /// </summary>
    /// <remarks>
    /// <para>
    /// You must call #gst_video_encoder_finish_frame() for the last sub-frame
    /// to tell the encoder that the frame has been fully encoded.
    /// </para>
    /// <para>
    /// This function will change the metadata of @frame and frame-&gt;output_buffer
    /// will be pushed downstream.
    /// </para>
    /// </remarks>
    /// <param name="frame">The <c>frame</c> argument.</param>
    /// <returns>a #GstFlowReturn resulting from pushing the buffer downstream.</returns>
    public Gst.FlowReturn FinishSubframe(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        int nativeResult = GstVideoEncoderFinishSubframe(Handle, frame.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Lets #GstVideoEncoder sub-classes to know the memory @allocator
    /// used by the base class and its @params.
    /// </summary>
    /// <remarks>
    /// <para>Unref the @allocator after use it.</para>
    /// </remarks>
    /// <param name="allocator">The <c>allocator</c> argument.</param>
    /// <param name="params">
    /// The <c>@params</c> argument.
    /// The binding allocates the storage; on return the caller owns
    /// <paramref name="params"/> and disposes it.
    /// </param>
    public void GetAllocator(out Gst.Allocator? allocator, out Gst.AllocationParams @params)
    {
        nint instanceHandle = Handle;
        nint allocatorNative = default;
        nint @paramsNative = GstAllocationParamsNew();
        GstVideoEncoderGetAllocator(instanceHandle, &allocatorNative, @paramsNative);
        System.GC.KeepAlive(this);
        @params = Gst.AllocationParams.FromNative(@paramsNative, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_allocation_params_new returned no value.");
        allocator = Gst.GObject.Object.FromNative<Gst.Allocator>(allocatorNative, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get a pending unfinished #GstVideoCodecFrame</summary>
    /// <param name="frameNumber">The <c>frameNumber</c> argument.</param>
    /// <returns>pending unfinished #GstVideoCodecFrame identified by @frame_number.</returns>
    public Gst.Video.VideoCodecFrame? GetFrame(int frameNumber)
    {
        nint nativeResult = GstVideoEncoderGetFrame(Handle, frameNumber);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoCodecFrame.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get all pending unfinished #GstVideoCodecFrame</summary>
    /// <returns>pending unfinished #GstVideoCodecFrame.</returns>
    public System.Collections.Generic.IReadOnlyList<Gst.Video.VideoCodecFrame> GetFrames()
    {
        nint nativeResult = GstVideoEncoderGetFrames(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<Gst.Video.VideoCodecFrame> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.Video.VideoCodecFrame.FromNative(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Query the configured encoding latency. Results will be returned via
    /// @min_latency and @max_latency.
    /// </summary>
    /// <param name="minLatency">The <c>minLatency</c> argument.</param>
    /// <param name="maxLatency">The <c>maxLatency</c> argument.</param>
    public void GetLatency(out Gst.ClockTime minLatency, out Gst.ClockTime maxLatency)
    {
        ulong minLatencyNative = default;
        ulong maxLatencyNative = default;
        GstVideoEncoderGetLatency(Handle, &minLatencyNative, &maxLatencyNative);
        System.GC.KeepAlive(this);
        minLatency = new Gst.ClockTime(minLatencyNative);
        maxLatency = new Gst.ClockTime(maxLatencyNative);
    }

    /// <summary>
    /// Determines maximum possible encoding time for @frame that will
    /// allow it to encode and arrive in time (as determined by QoS events).
    /// In particular, a negative result means encoding in time is no longer possible
    /// and should therefore occur as soon/skippy as possible.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If no QoS events have been received from downstream, or if
    /// #GstVideoEncoder:qos is disabled this function returns #G_MAXINT64.
    /// </para>
    /// </remarks>
    /// <param name="frame">The <c>frame</c> argument.</param>
    /// <returns>max decoding time.</returns>
    public long GetMaxEncodeTime(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        long nativeResult = GstVideoEncoderGetMaxEncodeTime(Handle, frame.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return nativeResult;
    }

    /// <summary>
    /// Returns the minimum force-keyunit interval, see gst_video_encoder_set_min_force_key_unit_interval()
    /// for more details.
    /// </summary>
    /// <returns>the minimum force-keyunit interval</returns>
    public Gst.ClockTime GetMinForceKeyUnitInterval()
    {
        ulong nativeResult = GstVideoEncoderGetMinForceKeyUnitInterval(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Get the oldest unfinished pending #GstVideoCodecFrame</summary>
    /// <returns>oldest unfinished pending #GstVideoCodecFrame</returns>
    public Gst.Video.VideoCodecFrame? GetOldestFrame()
    {
        nint nativeResult = GstVideoEncoderGetOldestFrame(Handle);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoCodecFrame.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the current #GstVideoCodecState</summary>
    /// <returns>#GstVideoCodecState describing format of video data.</returns>
    public Gst.Video.VideoCodecState? GetOutputState()
    {
        nint nativeResult = GstVideoEncoderGetOutputState(Handle);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoCodecState.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Checks if @encoder is currently configured to handle Quality-of-Service
    /// events from downstream.
    /// </summary>
    /// <returns>%TRUE if the encoder is configured to perform Quality-of-Service.</returns>
    public bool IsQosEnabled()
    {
        int nativeResult = GstVideoEncoderIsQosEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets the video encoder tags and how they should be merged with any
    /// upstream stream tags. This will override any tags previously-set
    /// with gst_video_encoder_merge_tags().
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that this is provided for convenience, and the subclass is
    /// not required to use this and can still do tag handling on its own.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="tags">The <c>tags</c> argument.</param>
    /// <param name="mode">The <c>mode</c> argument.</param>
    public void MergeTags(Gst.TagList? tags, Gst.TagMergeMode mode)
    {
        GstVideoEncoderMergeTags(Handle, tags is null ? 0 : tags.Handle, (int)mode);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(tags);
    }

    /// <summary>
    /// Negotiate with downstream elements to currently configured #GstVideoCodecState.
    /// Unmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if
    /// negotiate fails.
    /// </summary>
    /// <returns>%TRUE if the negotiation succeeded, else %FALSE.</returns>
    public bool Negotiate()
    {
        int nativeResult = GstVideoEncoderNegotiate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns caps that express @caps (or sink template caps if @caps == NULL)
    /// restricted to resolution/format/... combinations supported by downstream
    /// elements (e.g. muxers).
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <param name="filter">The <c>filter</c> argument.</param>
    /// <returns>a #GstCaps owned by caller</returns>
    public Gst.Caps ProxyGetcaps(Gst.Caps? caps, Gst.Caps? filter)
    {
        nint nativeResult = GstVideoEncoderProxyGetcaps(Handle, caps is null ? 0 : caps.Handle, filter is null ? 0 : filter.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        System.GC.KeepAlive(filter);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_encoder_proxy_getcaps returned no value.");
    }

    /// <summary>
    /// Removes @frame from list of pending frames and releases it, similar
    /// to calling gst_video_encoder_finish_frame() without a buffer attached
    /// to the frame, but does not post a QoS message or do any additional
    /// processing. Events from @frame are moved to the pending events list.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// <para>Available since GStreamer 1.26.</para>
    /// </remarks>
    /// <param name="frame">
    /// The <c>frame</c> argument.
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public void ReleaseFrame(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        GstVideoEncoderReleaseFrame(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
    }

    /// <summary>
    /// Informs baseclass of encoding latency. If the provided values changed from
    /// previously provided ones, this will also post a LATENCY message on the bus
    /// so the pipeline can reconfigure its global latency.
    /// </summary>
    /// <param name="minLatency">The <c>minLatency</c> argument.</param>
    /// <param name="maxLatency">The <c>maxLatency</c> argument.</param>
    public void SetLatency(Gst.ClockTime minLatency, Gst.ClockTime maxLatency)
    {
        GstVideoEncoderSetLatency(Handle, minLatency.Nanoseconds, maxLatency.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the minimum interval for requesting keyframes based on force-keyunit
    /// events. Setting this to 0 will allow to handle every event, setting this to
    /// %GST_CLOCK_TIME_NONE causes force-keyunit events to be ignored.
    /// </summary>
    /// <param name="interval">The <c>interval</c> argument.</param>
    public void SetMinForceKeyUnitInterval(Gst.ClockTime interval)
    {
        GstVideoEncoderSetMinForceKeyUnitInterval(Handle, interval.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>Request minimal value for PTS passed to handle_frame.</summary>
    /// <remarks>
    /// <para>
    /// For streams with reordered frames this can be used to ensure that there
    /// is enough time to accommodate first DTS, which may be less than first PTS
    /// </para>
    /// </remarks>
    /// <param name="minPts">The <c>minPts</c> argument.</param>
    public void SetMinPts(Gst.ClockTime minPts)
    {
        GstVideoEncoderSetMinPts(Handle, minPts.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Creates a new #GstVideoCodecState with the specified caps as the output state
    /// for the encoder.
    /// Any previously set output state on @encoder will be replaced by the newly
    /// created one.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The specified @caps should not contain any resolution, pixel-aspect-ratio,
    /// framerate, codec-data, .... Those should be specified instead in the returned
    /// #GstVideoCodecState.
    /// </para>
    /// <para>
    /// If the subclass wishes to copy over existing fields (like pixel aspect ratio,
    /// or framerate) from an existing #GstVideoCodecState, it can be provided as a
    /// @reference.
    /// </para>
    /// <para>
    /// If the subclass wishes to override some fields from the output state (like
    /// pixel-aspect-ratio or framerate) it can do so on the returned #GstVideoCodecState.
    /// </para>
    /// <para>
    /// The new output state will only take effect (set on pads and buffers) starting
    /// from the next call to #gst_video_encoder_finish_frame().
    /// </para>
    /// <para>
    /// The <c>caps</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="caps">
    /// The <c>caps</c> argument.
    /// The call consumes it: <paramref name="caps"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <param name="reference">The <c>reference</c> argument.</param>
    /// <returns>the newly configured output state.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="caps"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="caps"/> was disposed.
    /// </exception>
    public Gst.Video.VideoCodecState? SetOutputState(Gst.Caps caps, Gst.Video.VideoCodecState? reference)
    {
        ArgumentNullException.ThrowIfNull(caps);
        nint instanceHandle = Handle;
        nint capsNative = caps.Handle;
        nint referenceNative = reference is null ? 0 : reference.Handle;
        nint capsOwned = Gst.GstNative.MiniObjectRef(capsNative);
        nint nativeResult = GstVideoEncoderSetOutputState(instanceHandle, capsOwned, referenceNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(reference);
        caps.Dispose();
        return Gst.Video.VideoCodecState.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Configures @encoder to handle Quality-of-Service events from downstream.</summary>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetQosEnabled(bool enabled)
    {
        GstVideoEncoderSetQosEnabled(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Minimum interval between force-keyunit requests in nanoseconds. See
    /// gst_video_encoder_set_min_force_key_unit_interval() for more details.
    /// </summary>
    public Gst.ClockTime MinForceKeyUnitInterval
    {
        get => GetMinForceKeyUnitInterval();
        set => SetMinForceKeyUnitInterval(value);
    }

    /// <summary>The <c>qos</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool Qos
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("qos");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("qos");
            holder.SetBoolean(value);
            SetPropertyValue("qos", in holder);
        }
    }

    /// <summary>The <c>gst_video_encoder_allocate_output_buffer</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_allocate_output_buffer")]
    private static partial nint GstVideoEncoderAllocateOutputBuffer(nint encoder, nuint size);

    /// <summary>The <c>gst_video_encoder_allocate_output_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_allocate_output_frame")]
    private static partial int GstVideoEncoderAllocateOutputFrame(nint encoder, nint frame, nuint size);

    /// <summary>The <c>gst_video_encoder_drop_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_drop_frame")]
    private static partial void GstVideoEncoderDropFrame(nint encoder, nint frame);

    /// <summary>The <c>gst_video_encoder_finish_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_finish_frame")]
    private static partial int GstVideoEncoderFinishFrame(nint encoder, nint frame);

    /// <summary>The <c>gst_video_encoder_finish_subframe</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_finish_subframe")]
    private static partial int GstVideoEncoderFinishSubframe(nint encoder, nint frame);

    /// <summary>The <c>gst_video_encoder_get_allocator</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_allocator")]
    private static partial void GstVideoEncoderGetAllocator(nint encoder, nint* allocator, nint @params);

    /// <summary>The <c>gst_video_encoder_get_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_frame")]
    private static partial nint GstVideoEncoderGetFrame(nint encoder, int frameNumber);

    /// <summary>The <c>gst_video_encoder_get_frames</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_frames")]
    private static partial nint GstVideoEncoderGetFrames(nint encoder);

    /// <summary>The <c>gst_video_encoder_get_latency</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_latency")]
    private static partial void GstVideoEncoderGetLatency(nint encoder, ulong* minLatency, ulong* maxLatency);

    /// <summary>The <c>gst_video_encoder_get_max_encode_time</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_max_encode_time")]
    private static partial long GstVideoEncoderGetMaxEncodeTime(nint encoder, nint frame);

    /// <summary>The <c>gst_video_encoder_get_min_force_key_unit_interval</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_min_force_key_unit_interval")]
    private static partial ulong GstVideoEncoderGetMinForceKeyUnitInterval(nint encoder);

    /// <summary>The <c>gst_video_encoder_get_oldest_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_oldest_frame")]
    private static partial nint GstVideoEncoderGetOldestFrame(nint encoder);

    /// <summary>The <c>gst_video_encoder_get_output_state</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_output_state")]
    private static partial nint GstVideoEncoderGetOutputState(nint encoder);

    /// <summary>The <c>gst_video_encoder_is_qos_enabled</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_is_qos_enabled")]
    private static partial int GstVideoEncoderIsQosEnabled(nint encoder);

    /// <summary>The <c>gst_video_encoder_merge_tags</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_merge_tags")]
    private static partial void GstVideoEncoderMergeTags(nint encoder, nint tags, int mode);

    /// <summary>The <c>gst_video_encoder_negotiate</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_negotiate")]
    private static partial int GstVideoEncoderNegotiate(nint encoder);

    /// <summary>The <c>gst_video_encoder_proxy_getcaps</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_proxy_getcaps")]
    private static partial nint GstVideoEncoderProxyGetcaps(nint enc, nint caps, nint filter);

    /// <summary>The <c>gst_video_encoder_release_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_release_frame")]
    private static partial void GstVideoEncoderReleaseFrame(nint encoder, nint frame);

    /// <summary>The <c>gst_video_encoder_set_latency</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_set_latency")]
    private static partial void GstVideoEncoderSetLatency(nint encoder, ulong minLatency, ulong maxLatency);

    /// <summary>The <c>gst_video_encoder_set_min_force_key_unit_interval</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_set_min_force_key_unit_interval")]
    private static partial void GstVideoEncoderSetMinForceKeyUnitInterval(nint encoder, ulong interval);

    /// <summary>The <c>gst_video_encoder_set_min_pts</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_set_min_pts")]
    private static partial void GstVideoEncoderSetMinPts(nint encoder, ulong minPts);

    /// <summary>The <c>gst_video_encoder_set_output_state</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_set_output_state")]
    private static partial nint GstVideoEncoderSetOutputState(nint encoder, nint caps, nint reference);

    /// <summary>The <c>gst_video_encoder_set_qos_enabled</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_set_qos_enabled")]
    private static partial void GstVideoEncoderSetQosEnabled(nint encoder, int enabled);

    /// <summary>The <c>gst_allocation_params_new</c> entry point, which allocates the storage of a caller allocated out parameter.</summary>
    /// <returns>A new, zeroed instance the caller owns.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_allocation_params_new")]
    private static partial nint GstAllocationParamsNew();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstVideoEncoder</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_encoder_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstVideoEncoder</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : VideoEncoder
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
