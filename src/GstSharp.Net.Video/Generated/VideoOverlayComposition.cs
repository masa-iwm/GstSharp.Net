// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>Functions to create and handle overlay compositions on video buffers.</summary>
/// <remarks>
/// <para>
/// An overlay composition describes one or more overlay rectangles to be
/// blended on top of a video buffer.
/// </para>
/// <para>This API serves two main purposes:</para>
/// <para>
/// * it can be used to attach overlay information (subtitles or logos)
///   to non-raw video buffers such as GL/VAAPI/VDPAU surfaces. The actual
///   blending of the overlay can then be done by e.g. the video sink that
///   processes these non-raw buffers.
/// </para>
/// <para>
/// * it can also be used to blend overlay rectangles on top of raw video
///   buffers, thus consolidating blending functionality for raw video in
///   one place.
/// </para>
/// <para>
/// Together, this allows existing overlay elements to easily handle raw
/// and non-raw video as input in without major changes (once the overlays
/// have been put into a #GstVideoOverlayComposition object anyway) - for raw
/// video the overlay can just use the blending function to blend the data
/// on top of the video, and for surface buffers it can just attach them to
/// the buffer and let the sink render the overlays.
/// </para>
/// </remarks>
public sealed unsafe partial class VideoOverlayComposition : Gst.MiniObject
{
    /// <summary>Wraps a native <c>GstVideoOverlayComposition</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal VideoOverlayComposition(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Wraps a native <c>GstVideoOverlayComposition</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static VideoOverlayComposition? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>
    /// Creates a new video overlay composition object to hold one or more
    /// overlay rectangles.
    /// </summary>
    /// <remarks>
    /// <para>Note that since 1.20 this allows to pass %NULL for @rectangle.</para>
    /// </remarks>
    /// <param name="rectangle">The <c>rectangle</c> argument.</param>
    /// <returns>
    /// a new #GstVideoOverlayComposition. Unref with
    ///     gst_video_overlay_composition_unref() when no longer needed.
    /// </returns>
    public static Gst.Video.VideoOverlayComposition New(Gst.Video.VideoOverlayRectangle? rectangle)
    {
        nint nativeResult = GstVideoOverlayCompositionNew(rectangle is null ? 0 : rectangle.Handle);
        return Gst.Video.VideoOverlayComposition.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_overlay_composition_new returned no value.");
    }

    /// <summary>
    /// Adds an overlay rectangle to an existing overlay composition object. This
    /// must be done right after creating the overlay composition.
    /// </summary>
    /// <param name="rectangle">The <c>rectangle</c> argument.</param>
    public void AddRectangle(Gst.Video.VideoOverlayRectangle rectangle)
    {
        ArgumentNullException.ThrowIfNull(rectangle);
        GstVideoOverlayCompositionAddRectangle(Handle, rectangle.Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Blends the overlay rectangles in @comp on top of the raw video data
    /// contained in @video_buf. The data in @video_buf must be writable and
    /// mapped appropriately.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Since @video_buf data is read and will be modified, it ought be
    /// mapped with flag GST_MAP_READWRITE.
    /// </para>
    /// </remarks>
    /// <param name="videoBuf">The <c>videoBuf</c> argument.</param>
    /// <returns>The result of <c>gst_video_overlay_composition_blend</c>.</returns>
    public bool Blend(Gst.Video.VideoFrame videoBuf)
    {
        ArgumentNullException.ThrowIfNull(videoBuf);
        int nativeResult = GstVideoOverlayCompositionBlend(Handle, videoBuf.Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Makes a copy of @comp and all contained rectangles, so that it is possible
    /// to modify the composition and contained rectangles (e.g. add additional
    /// rectangles or change the render co-ordinates or render dimension). The
    /// actual overlay pixel data buffers contained in the rectangles are not
    /// copied.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This should be avoided unless rectangles need to be modified because it
    /// invalidates caching in sinks and compositor elements. To add extra rectangles
    /// it is rather recommended to add an extra composition meta using
    /// gst_buffer_add_video_overlay_composition_meta().
    /// </para>
    /// </remarks>
    /// <returns>
    /// a new #GstVideoOverlayComposition equivalent
    ///     to @comp.
    /// </returns>
    public Gst.Video.VideoOverlayComposition Copy()
    {
        nint nativeResult = GstVideoOverlayCompositionCopy(Handle);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoOverlayComposition.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_overlay_composition_copy returned no value.");
    }

    /// <summary>Returns the @n-th #GstVideoOverlayRectangle contained in @comp.</summary>
    /// <param name="n">The <c>n</c> argument.</param>
    /// <returns>
    /// the @n-th rectangle, or NULL if @n is out of
    ///     bounds. Will not return a new reference, the caller will need to
    ///     obtain her own reference using gst_video_overlay_rectangle_ref()
    ///     if needed.
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Video.VideoOverlayRectangle? GetRectangle(uint n)
    {
        nint nativeResult = GstVideoOverlayCompositionGetRectangle(Handle, n);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoOverlayRectangle.FromNative(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>
    /// Returns the sequence number of this composition. Sequence numbers are
    /// monotonically increasing and unique for overlay compositions and rectangles
    /// (meaning there will never be a rectangle with the same sequence number as
    /// a composition).
    /// </summary>
    /// <returns>the sequence number of @comp</returns>
    public uint GetSeqnum()
    {
        uint nativeResult = GstVideoOverlayCompositionGetSeqnum(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Returns the number of #GstVideoOverlayRectangle&lt;!-- --&gt;s contained in @comp.</summary>
    /// <returns>the number of rectangles</returns>
    public uint NRectangles()
    {
        uint nativeResult = GstVideoOverlayCompositionNRectangles(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>The <c>gst_video_overlay_composition_new</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_new")]
    private static partial nint GstVideoOverlayCompositionNew(nint rectangle);

    /// <summary>The <c>gst_video_overlay_composition_add_rectangle</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_add_rectangle")]
    private static partial void GstVideoOverlayCompositionAddRectangle(nint comp, nint rectangle);

    /// <summary>The <c>gst_video_overlay_composition_blend</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_blend")]
    private static partial int GstVideoOverlayCompositionBlend(nint comp, nint videoBuf);

    /// <summary>The <c>gst_video_overlay_composition_copy</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_copy")]
    private static partial nint GstVideoOverlayCompositionCopy(nint comp);

    /// <summary>The <c>gst_video_overlay_composition_get_rectangle</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_get_rectangle")]
    private static partial nint GstVideoOverlayCompositionGetRectangle(nint comp, uint n);

    /// <summary>The <c>gst_video_overlay_composition_get_seqnum</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_get_seqnum")]
    private static partial uint GstVideoOverlayCompositionGetSeqnum(nint comp);

    /// <summary>The <c>gst_video_overlay_composition_n_rectangles</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_n_rectangles")]
    private static partial uint GstVideoOverlayCompositionNRectangles(nint comp);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstVideoOverlayComposition</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_overlay_composition_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new VideoOverlayComposition(handle, transfer);
}
