// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>@field_count must be 0 for progressive video and 1 or 2 for interlaced.</summary>
/// <remarks>
/// <para>A representation of a SMPTE time code.</para>
/// <para>
/// @hours must be positive and less than 24. Will wrap around otherwise.
/// @minutes and @seconds must be positive and less than 60.
/// @frames must be less than or equal to @config.fps_n / @config.fps_d
/// These values are *NOT* automatically normalized.
/// </para>
/// </remarks>
public sealed unsafe partial class VideoTimeCode : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstVideoTimeCode</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal VideoTimeCode(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a native <c>GstVideoTimeCode</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static VideoTimeCode? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>The <c>gst_video_time_code_new_empty</c> function.</summary>
    /// <returns>a new empty, invalid #GstVideoTimeCode</returns>
    public static Gst.Video.VideoTimeCode NewEmpty()
    {
        nint nativeResult = GstVideoTimeCodeNewEmpty();
        return Gst.Video.VideoTimeCode.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_time_code_new_empty returned no value.");
    }

    /// <summary>The <c>gst_video_time_code_new_from_string</c> function.</summary>
    /// <param name="tcStr">The <c>tcStr</c> argument.</param>
    /// <returns>
    /// a new #GstVideoTimeCode from the given string or %NULL
    ///   if the string could not be passed.
    /// </returns>
    public static Gst.Video.VideoTimeCode? NewFromString(string tcStr)
    {
        ArgumentNullException.ThrowIfNull(tcStr);
        System.Span<byte> tcStrBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope tcStrScope = Gst.Interop.GMarshal.StackUtf8(tcStr, tcStrBuffer);
        nint nativeResult = GstVideoTimeCodeNewFromString(tcStrScope.Pointer);
        return Gst.Video.VideoTimeCode.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Adds or subtracts @frames amount of frames to @tc. tc needs to
    /// contain valid data, as verified by gst_video_time_code_is_valid().
    /// </summary>
    /// <param name="frames">The <c>frames</c> argument.</param>
    public void AddFrames(long frames)
    {
        GstVideoTimeCodeAddFrames(Handle, frames);
    }

    /// <summary>
    /// This makes a component-wise addition of @tc_inter to @tc. For example,
    /// adding ("01:02:03:04", "00:01:00:00") will return "01:03:03:04".
    /// When it comes to drop-frame timecodes,
    /// adding ("00:00:00;00", "00:01:00:00") will return "00:01:00;02"
    /// because of drop-frame oddities. However,
    /// adding ("00:09:00;02", "00:01:00:00") will return "00:10:00;00"
    /// because this time we can have an exact minute.
    /// </summary>
    /// <param name="tcInter">The <c>tcInter</c> argument.</param>
    /// <returns>
    /// A new #GstVideoTimeCode with @tc_inter added or %NULL
    ///   if the interval can't be added.
    /// </returns>
    public Gst.Video.VideoTimeCode? AddInterval(Gst.Video.VideoTimeCodeInterval tcInter)
    {
        ArgumentNullException.ThrowIfNull(tcInter);
        nint nativeResult = GstVideoTimeCodeAddInterval(Handle, tcInter.Handle);
        return Gst.Video.VideoTimeCode.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Initializes @tc with empty/zero/NULL values and frees any memory
    /// it might currently use.
    /// </summary>
    public void Clear()
    {
        GstVideoTimeCodeClear(Handle);
    }

    /// <summary>
    /// Compares @tc1 and @tc2. If both have latest daily jam information, it is
    /// taken into account. Otherwise, it is assumed that the daily jam of both
    /// @tc1 and @tc2 was at the same time. Both time codes must be valid.
    /// </summary>
    /// <param name="tc2">The <c>tc2</c> argument.</param>
    /// <returns>1 if @tc1 is after @tc2, -1 if @tc1 is before @tc2, 0 otherwise.</returns>
    public int Compare(Gst.Video.VideoTimeCode tc2)
    {
        ArgumentNullException.ThrowIfNull(tc2);
        int nativeResult = GstVideoTimeCodeCompare(Handle, tc2.Handle);
        return nativeResult;
    }

    /// <summary>The <c>gst_video_time_code_copy</c> function.</summary>
    /// <returns>a new #GstVideoTimeCode with the same values as @tc.</returns>
    public Gst.Video.VideoTimeCode Copy()
    {
        nint nativeResult = GstVideoTimeCodeCopy(Handle);
        return Gst.Video.VideoTimeCode.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_time_code_copy returned no value.");
    }

    /// <summary>The <c>gst_video_time_code_frames_since_daily_jam</c> function.</summary>
    /// <returns>how many frames have passed since the daily jam of @tc.</returns>
    public ulong FramesSinceDailyJam()
    {
        ulong nativeResult = GstVideoTimeCodeFramesSinceDailyJam(Handle);
        return nativeResult;
    }

    /// <summary>Frees @tc.</summary>
    public void Free()
    {
        GstVideoTimeCodeFree(Handle);
    }

    /// <summary>Adds one frame to @tc.</summary>
    public void IncrementFrame()
    {
        GstVideoTimeCodeIncrementFrame(Handle);
    }

    /// <summary>The <c>gst_video_time_code_is_valid</c> function.</summary>
    /// <returns>
    /// whether @tc is a valid timecode (supported frame rate,
    /// hours/minutes/seconds/frames not overflowing)
    /// </returns>
    public bool IsValid()
    {
        int nativeResult = GstVideoTimeCodeIsValid(Handle);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_video_time_code_nsec_since_daily_jam</c> function.</summary>
    /// <returns>how many nsec have passed since the daily jam of @tc.</returns>
    public ulong NsecSinceDailyJam()
    {
        ulong nativeResult = GstVideoTimeCodeNsecSinceDailyJam(Handle);
        return nativeResult;
    }

    /// <summary>The <c>gst_video_time_code_to_string</c> function.</summary>
    /// <returns>
    /// the SMPTE ST 2059-1:2015 string representation of @tc. That will
    /// take the form hh:mm:ss:ff. The last separator (between seconds and frames)
    /// may vary:
    /// </returns>
    public override string ToString()
    {
        nint nativeResult = GstVideoTimeCodeToString(Handle);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult)
            ?? throw new InvalidOperationException("gst_video_time_code_to_string returned no value.");
    }

    /// <summary>The <c>gst_video_time_code_new_empty</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_new_empty")]
    private static partial nint GstVideoTimeCodeNewEmpty();

    /// <summary>The <c>gst_video_time_code_new_from_string</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_new_from_string")]
    private static partial nint GstVideoTimeCodeNewFromString(byte* tcStr);

    /// <summary>The <c>gst_video_time_code_add_frames</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_add_frames")]
    private static partial void GstVideoTimeCodeAddFrames(nint tc, long frames);

    /// <summary>The <c>gst_video_time_code_add_interval</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_add_interval")]
    private static partial nint GstVideoTimeCodeAddInterval(nint tc, nint tcInter);

    /// <summary>The <c>gst_video_time_code_clear</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_clear")]
    private static partial void GstVideoTimeCodeClear(nint tc);

    /// <summary>The <c>gst_video_time_code_compare</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_compare")]
    private static partial int GstVideoTimeCodeCompare(nint tc1, nint tc2);

    /// <summary>The <c>gst_video_time_code_copy</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_copy")]
    private static partial nint GstVideoTimeCodeCopy(nint tc);

    /// <summary>The <c>gst_video_time_code_frames_since_daily_jam</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_frames_since_daily_jam")]
    private static partial ulong GstVideoTimeCodeFramesSinceDailyJam(nint tc);

    /// <summary>The <c>gst_video_time_code_free</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_free")]
    private static partial void GstVideoTimeCodeFree(nint tc);

    /// <summary>The <c>gst_video_time_code_increment_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_increment_frame")]
    private static partial void GstVideoTimeCodeIncrementFrame(nint tc);

    /// <summary>The <c>gst_video_time_code_is_valid</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_is_valid")]
    private static partial int GstVideoTimeCodeIsValid(nint tc);

    /// <summary>The <c>gst_video_time_code_nsec_since_daily_jam</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_nsec_since_daily_jam")]
    private static partial ulong GstVideoTimeCodeNsecSinceDailyJam(nint tc);

    /// <summary>The <c>gst_video_time_code_to_string</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_to_string")]
    private static partial nint GstVideoTimeCodeToString(nint tc);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstVideoTimeCode</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_time_code_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new VideoTimeCode(handle, transfer);
}
