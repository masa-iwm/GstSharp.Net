// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>The <c>GstVideoConverter</c> record.</summary>
public sealed unsafe partial class VideoConverter
{
    /// <summary>The native instance.</summary>
    internal nint Handle;

    /// <summary>Wraps a native <c>GstVideoConverter</c>.</summary>
    /// <param name="handle">The native instance.</param>
    internal VideoConverter(nint handle) => Handle = handle;

    /// <summary>Wraps a native <c>GstVideoConverter</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    /// <remarks>
    /// The wrapper of an opaque record is a bare pointer holder: the gir
    /// describes no way of releasing one, so it does not take part in the
    /// ownership of what it points at.
    /// </remarks>
    internal static VideoConverter? FromNative(nint handle) =>
        handle == 0 ? null : new(handle);

    /// <summary>Convert the pixels of @src into @dest using @convert.</summary>
    /// <remarks>
    /// <para>
    /// If #GST_VIDEO_CONVERTER_OPT_ASYNC_TASKS is %TRUE then this function will
    /// return immediately and needs to be followed by a call to
    /// gst_video_converter_frame_finish().
    /// </para>
    /// </remarks>
    /// <param name="src">a #GstVideoFrame</param>
    /// <param name="dest">a #GstVideoFrame</param>
    public void Frame(Gst.Video.VideoFrame src, Gst.Video.VideoFrame dest)
    {
        ArgumentNullException.ThrowIfNull(src);
        ArgumentNullException.ThrowIfNull(dest);
        GstVideoConverterFrame(Handle, src.Handle, dest.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(src);
        System.GC.KeepAlive(dest);
    }

    /// <summary>
    /// Wait for a previous async conversion performed using
    /// gst_video_converter_frame() to complete.
    /// </summary>
    public void FrameFinish()
    {
        GstVideoConverterFrameFinish(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Free @convert</summary>
    public void Free()
    {
        GstVideoConverterFree(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Get the current configuration of @convert.</summary>
    /// <returns>
    /// a #GstStructure that remains valid for as long as @convert is valid
    ///   or until gst_video_converter_set_config() is called.
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Structure GetConfig()
    {
        nint nativeResult = GstVideoConverterGetConfig(Handle);
        System.GC.KeepAlive(this);
        return Gst.Structure.FromNative(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_video_converter_get_config returned no value.");
    }

    /// <summary>Retrieve the input format of @convert.</summary>
    /// <returns>
    /// a #GstVideoInfo
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Video.VideoInfo GetInInfo()
    {
        nint nativeResult = GstVideoConverterGetInInfo(Handle);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoInfo.FromNative(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_video_converter_get_in_info returned no value.");
    }

    /// <summary>Retrieve the output format of @convert.</summary>
    /// <returns>
    /// a #GstVideoInfo
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Video.VideoInfo GetOutInfo()
    {
        nint nativeResult = GstVideoConverterGetOutInfo(Handle);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoInfo.FromNative(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_video_converter_get_out_info returned no value.");
    }

    /// <summary>Set @config as extra configuration for @convert.</summary>
    /// <remarks>
    /// <para>
    /// If the parameters in @config can not be set exactly, this function returns
    /// %FALSE and will try to update as much state as possible. The new state can
    /// then be retrieved and refined with gst_video_converter_get_config().
    /// </para>
    /// <para>
    /// Look at the `GST_VIDEO_CONVERTER_OPT_*` fields to check valid configuration
    /// option and values.
    /// </para>
    /// <para>
    /// The <c>config</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="config">
    /// a #GstStructure
    /// The call consumes it: <paramref name="config"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>%TRUE when @config could be set.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="config"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="config"/> was disposed.
    /// </exception>
    public bool SetConfig(Gst.Structure config)
    {
        ArgumentNullException.ThrowIfNull(config);
        nint instanceHandle = Handle;
        nint configNative = config.Handle;
        nuint configType = config.BoxedType.Value;
        nint configOwned = Gst.Interop.GObjectNative.BoxedCopy(configType, configNative);
        int nativeResult = GstVideoConverterSetConfig(instanceHandle, configOwned);
        System.GC.KeepAlive(this);
        config.Dispose();
        return nativeResult != 0;
    }

    /// <summary>Transform the GstMeta of @src into @dest using @convert.</summary>
    /// <remarks>
    /// <para>Available since GStreamer 1.28.</para>
    /// </remarks>
    /// <param name="src">a #GstBuffer</param>
    /// <param name="dest">a writable #GstBuffer</param>
    /// <returns>TRUE if any meta was copied</returns>
    public bool TransformMetas(Gst.Buffer src, Gst.Buffer dest)
    {
        ArgumentNullException.ThrowIfNull(src);
        ArgumentNullException.ThrowIfNull(dest);
        int nativeResult = GstVideoConverterTransformMetas(Handle, src.Handle, dest.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(src);
        System.GC.KeepAlive(dest);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_video_converter_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_frame")]
    private static partial void GstVideoConverterFrame(nint convert, nint src, nint dest);

    /// <summary>The <c>gst_video_converter_frame_finish</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_frame_finish")]
    private static partial void GstVideoConverterFrameFinish(nint convert);

    /// <summary>The <c>gst_video_converter_free</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_free")]
    private static partial void GstVideoConverterFree(nint convert);

    /// <summary>The <c>gst_video_converter_get_config</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_get_config")]
    private static partial nint GstVideoConverterGetConfig(nint convert);

    /// <summary>The <c>gst_video_converter_get_in_info</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_get_in_info")]
    private static partial nint GstVideoConverterGetInInfo(nint convert);

    /// <summary>The <c>gst_video_converter_get_out_info</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_get_out_info")]
    private static partial nint GstVideoConverterGetOutInfo(nint convert);

    /// <summary>The <c>gst_video_converter_set_config</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_set_config")]
    private static partial int GstVideoConverterSetConfig(nint convert, nint config);

    /// <summary>The <c>gst_video_converter_transform_metas</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_converter_transform_metas")]
    private static partial int GstVideoConverterTransformMetas(nint convert, nint src, nint dest);
}
