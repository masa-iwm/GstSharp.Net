// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>
/// Information describing image properties. This information can be filled
/// in from GstCaps with gst_video_info_from_caps(). The information is also used
/// to store the specific video info when mapping a video frame with
/// gst_video_frame_map().
/// </summary>
/// <remarks>
/// <para>Use the provided macros to access the info in this structure.</para>
/// </remarks>
public sealed unsafe partial class VideoInfo : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstVideoInfo</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal VideoInfo(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a native <c>GstVideoInfo</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static VideoInfo? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>
    /// Allocate a new #GstVideoInfo that is also initialized with
    /// gst_video_info_init().
    /// </summary>
    /// <returns>a new #GstVideoInfo. free with gst_video_info_free().</returns>
    public static Gst.Video.VideoInfo New()
    {
        nint nativeResult = GstVideoInfoNew();
        return Gst.Video.VideoInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_info_new returned no value.");
    }

    /// <summary>Parse @caps to generate a #GstVideoInfo.</summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>A #GstVideoInfo, or %NULL if @caps couldn't be parsed</returns>
    public static Gst.Video.VideoInfo? NewFromCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        nint nativeResult = GstVideoInfoNewFromCaps(caps.Handle);
        return Gst.Video.VideoInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Adjust the offset and stride fields in @info so that the padding and
    /// stride alignment in @align is respected.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Extra padding will be added to the right side when stride alignment padding
    /// is required and @align will be updated with the new padding values.
    /// </para>
    /// </remarks>
    /// <param name="align">The <c>align</c> argument.</param>
    /// <returns>
    /// %FALSE if alignment could not be applied, e.g. because the
    ///   size of a frame can't be represented as a 32 bit integer (Since: 1.12)
    /// </returns>
    public bool Align(Gst.Video.VideoAlignment align)
    {
        Gst.Video.VideoAlignment alignNative = align;
        int nativeResult = GstVideoInfoAlign(Handle, &alignNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Extra padding will be added to the right side when stride alignment padding
    /// is required and @align will be updated with the new padding values.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This variant of gst_video_info_align() provides the updated size, in bytes,
    /// of each video plane after the alignment, including all horizontal and vertical
    /// paddings.
    /// </para>
    /// <para>
    /// In case of GST_VIDEO_INTERLACE_MODE_ALTERNATE info, the returned sizes are the
    /// ones used to hold a single field, not the full frame.
    /// </para>
    /// </remarks>
    /// <param name="align">The <c>align</c> argument.</param>
    /// <param name="planeSize">The <c>planeSize</c> argument.</param>
    /// <returns>
    /// %FALSE if alignment could not be applied, e.g. because the
    ///   size of a frame can't be represented as a 32 bit integer
    /// </returns>
    public bool AlignFull(Gst.Video.VideoAlignment align, out nuint planeSize)
    {
        Gst.Video.VideoAlignment alignNative = align;
        nuint planeSizeNative = default;
        int nativeResult = GstVideoInfoAlignFull(Handle, &alignNative, &planeSizeNative);
        planeSize = planeSizeNative;
        return nativeResult != 0;
    }

    /// <summary>
    /// Converts among various #GstFormat types.  This function handles
    /// GST_FORMAT_BYTES, GST_FORMAT_TIME, and GST_FORMAT_DEFAULT.  For
    /// raw video, GST_FORMAT_DEFAULT corresponds to video frames.  This
    /// function can be used to handle pad queries of the type GST_QUERY_CONVERT.
    /// </summary>
    /// <param name="srcFormat">The <c>srcFormat</c> argument.</param>
    /// <param name="srcValue">The <c>srcValue</c> argument.</param>
    /// <param name="destFormat">The <c>destFormat</c> argument.</param>
    /// <param name="destValue">The <c>destValue</c> argument.</param>
    /// <returns>TRUE if the conversion was successful.</returns>
    public bool Convert(Gst.Format srcFormat, long srcValue, Gst.Format destFormat, out long destValue)
    {
        long destValueNative = default;
        int nativeResult = GstVideoInfoConvert(Handle, (int)srcFormat, srcValue, (int)destFormat, &destValueNative);
        destValue = destValueNative;
        return nativeResult != 0;
    }

    /// <summary>Copy a GstVideoInfo structure.</summary>
    /// <returns>a new #GstVideoInfo. free with gst_video_info_free.</returns>
    public Gst.Video.VideoInfo Copy()
    {
        nint nativeResult = GstVideoInfoCopy(Handle);
        return Gst.Video.VideoInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_info_copy returned no value.");
    }

    /// <summary>
    /// Free a GstVideoInfo structure previously allocated with gst_video_info_new()
    /// or gst_video_info_copy().
    /// </summary>
    public void Free()
    {
        GstVideoInfoFree(Handle);
    }

    /// <summary>Compares two #GstVideoInfo and returns whether they are equal or not</summary>
    /// <param name="other">The <c>other</c> argument.</param>
    /// <returns>%TRUE if @info and @other are equal, else %FALSE.</returns>
    public bool IsEqual(Gst.Video.VideoInfo other)
    {
        ArgumentNullException.ThrowIfNull(other);
        int nativeResult = GstVideoInfoIsEqual(Handle, other.Handle);
        return nativeResult != 0;
    }

    /// <summary>Set the default info for a video frame of @format and @width and @height.</summary>
    /// <remarks>
    /// <para>
    /// Note: This initializes @info first, no values are preserved. This function
    /// does not set the offsets correctly for interlaced vertically
    /// subsampled formats.
    /// </para>
    /// </remarks>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="width">The <c>width</c> argument.</param>
    /// <param name="height">The <c>height</c> argument.</param>
    /// <returns>
    /// %FALSE if the returned video info is invalid, e.g. because the
    ///   size of a frame can't be represented as a 32 bit integer (Since: 1.12)
    /// </returns>
    public bool SetFormat(Gst.Video.VideoFormat format, uint width, uint height)
    {
        int nativeResult = GstVideoInfoSetFormat(Handle, (int)format, width, height);
        return nativeResult != 0;
    }

    /// <summary>
    /// Same as #gst_video_info_set_format but also allowing to set the interlaced
    /// mode.
    /// </summary>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="mode">The <c>mode</c> argument.</param>
    /// <param name="width">The <c>width</c> argument.</param>
    /// <param name="height">The <c>height</c> argument.</param>
    /// <returns>
    /// %FALSE if the returned video info is invalid, e.g. because the
    ///   size of a frame can't be represented as a 32 bit integer.
    /// </returns>
    public bool SetInterlacedFormat(Gst.Video.VideoFormat format, Gst.Video.VideoInterlaceMode mode, uint width, uint height)
    {
        int nativeResult = GstVideoInfoSetInterlacedFormat(Handle, (int)format, (int)mode, width, height);
        return nativeResult != 0;
    }

    /// <summary>Convert the values of @info into a #GstCaps.</summary>
    /// <returns>a new #GstCaps containing the info of @info.</returns>
    public Gst.Caps ToCaps()
    {
        nint nativeResult = GstVideoInfoToCaps(Handle);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_info_to_caps returned no value.");
    }

    /// <summary>Parse @caps and update @info.</summary>
    /// <param name="info">The <c>info</c> argument.</param>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>TRUE if @caps could be parsed</returns>
    public static bool FromCaps(out Gst.Video.VideoInfo? info, Gst.Caps caps)
    {
        nint infoNative = default;
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstVideoInfoFromCaps(&infoNative, caps.Handle);
        info = Gst.Video.VideoInfo.FromNative(infoNative, Gst.Interop.Transfer.None);
        return nativeResult != 0;
    }

    /// <summary>Initialize @info with default values.</summary>
    /// <param name="info">The <c>info</c> argument.</param>
    public static void Init(out Gst.Video.VideoInfo? info)
    {
        nint infoNative = default;
        GstVideoInfoInit(&infoNative);
        info = Gst.Video.VideoInfo.FromNative(infoNative, Gst.Interop.Transfer.None);
    }

    /// <summary>The <c>gst_video_info_new</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_new")]
    private static partial nint GstVideoInfoNew();

    /// <summary>The <c>gst_video_info_new_from_caps</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_new_from_caps")]
    private static partial nint GstVideoInfoNewFromCaps(nint caps);

    /// <summary>The <c>gst_video_info_align</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_align")]
    private static partial int GstVideoInfoAlign(nint info, Gst.Video.VideoAlignment* align);

    /// <summary>The <c>gst_video_info_align_full</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_align_full")]
    private static partial int GstVideoInfoAlignFull(nint info, Gst.Video.VideoAlignment* align, nuint* planeSize);

    /// <summary>The <c>gst_video_info_convert</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_convert")]
    private static partial int GstVideoInfoConvert(nint info, int srcFormat, long srcValue, int destFormat, long* destValue);

    /// <summary>The <c>gst_video_info_copy</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_copy")]
    private static partial nint GstVideoInfoCopy(nint info);

    /// <summary>The <c>gst_video_info_free</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_free")]
    private static partial void GstVideoInfoFree(nint info);

    /// <summary>The <c>gst_video_info_is_equal</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_is_equal")]
    private static partial int GstVideoInfoIsEqual(nint info, nint other);

    /// <summary>The <c>gst_video_info_set_format</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_set_format")]
    private static partial int GstVideoInfoSetFormat(nint info, int format, uint width, uint height);

    /// <summary>The <c>gst_video_info_set_interlaced_format</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_set_interlaced_format")]
    private static partial int GstVideoInfoSetInterlacedFormat(nint info, int format, int mode, uint width, uint height);

    /// <summary>The <c>gst_video_info_to_caps</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_to_caps")]
    private static partial nint GstVideoInfoToCaps(nint info);

    /// <summary>The <c>gst_video_info_from_caps</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_from_caps")]
    private static partial int GstVideoInfoFromCaps(nint* info, nint caps);

    /// <summary>The <c>gst_video_info_init</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_init")]
    private static partial void GstVideoInfoInit(nint* info);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstVideoInfo</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_info_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new VideoInfo(handle, transfer);
}
