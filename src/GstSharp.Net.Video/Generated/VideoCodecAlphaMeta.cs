// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>
/// This meta is primarily for internal use in GStreamer elements to support
/// VP8/VP9 transparent video stored into WebM or Matroska containers, or
/// transparent static AV1 images. Nothing prevents you from using this meta
/// for custom purposes, but it generally can't be used to easily to add support
/// for alpha channels to CODECs or formats that don't support that out of the
/// box.
/// </summary>
public sealed unsafe partial class VideoCodecAlphaMeta
{
    /// <summary>The native instance.</summary>
    internal nint Handle;

    /// <summary>Wraps a native <c>GstVideoCodecAlphaMeta</c>.</summary>
    /// <param name="handle">The native instance.</param>
    internal VideoCodecAlphaMeta(nint handle) => Handle = handle;

    /// <summary>Reads the <c>buffer</c> field of <c>GstVideoCodecAlphaMeta</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// </remarks>
    /// <returns>the encoded alpha frame</returns>
    public Gst.Buffer? GetBuffer()
    {
        Gst.Buffer? value = Gst.Buffer.FromNative(((VideoCodecAlphaMetaRaw*)Handle)->Buffer, Gst.Interop.Transfer.None);
        System.GC.KeepAlive(this);
        return value;
    }

    /// <summary>Wraps a native <c>GstVideoCodecAlphaMeta</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    /// <remarks>
    /// The wrapper of an opaque record is a bare pointer holder: the gir
    /// describes no way of releasing one, so it does not take part in the
    /// ownership of what it points at.
    /// </remarks>
    internal static VideoCodecAlphaMeta? FromNative(nint handle) =>
        handle == 0 ? null : new(handle);

    /// <summary>The <c>gst_video_codec_alpha_meta_get_info</c> function.</summary>
    /// <returns>#GstMetaInfo pointer that describes #GstVideoCodecAlphaMeta.</returns>
    public static Gst.MetaInfo GetInfo()
    {
        nint nativeResult = GstVideoCodecAlphaMetaGetInfo();
        return Gst.MetaInfo.FromNative(nativeResult)
            ?? throw new InvalidOperationException("gst_video_codec_alpha_meta_get_info returned no value.");
    }

    /// <summary>The <c>gst_video_codec_alpha_meta_get_info</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_codec_alpha_meta_get_info")]
    private static partial nint GstVideoCodecAlphaMetaGetInfo();
}

/// <summary>The native layout of <c>GstVideoCodecAlphaMeta</c>.</summary>
/// <remarks>
/// <para>
/// The mirror is only ever read through a pointer into memory that GStreamer
/// owns; it is never allocated, assigned or copied.
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
internal unsafe struct VideoCodecAlphaMetaRaw
{
    /// <summary>The <c>meta</c> field.</summary>
    internal Gst.MetaRaw Meta;

    /// <summary>The <c>buffer</c> field.</summary>
    internal nint Buffer;
}
