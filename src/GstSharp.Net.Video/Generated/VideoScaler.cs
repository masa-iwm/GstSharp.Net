// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>
/// #GstVideoScaler is a utility object for rescaling and resampling
/// video frames using various interpolation / sampling methods.
/// </summary>
public sealed unsafe partial class VideoScaler
{
    /// <summary>The native instance.</summary>
    internal nint Handle;

    /// <summary>Wraps a native <c>GstVideoScaler</c>.</summary>
    /// <param name="handle">The native instance.</param>
    internal VideoScaler(nint handle) => Handle = handle;

    /// <summary>Wraps a native <c>GstVideoScaler</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    /// <remarks>
    /// The wrapper of an opaque record is a bare pointer holder: the gir
    /// describes no way of releasing one, so it does not take part in the
    /// ownership of what it points at.
    /// </remarks>
    internal static VideoScaler? FromNative(nint handle) =>
        handle == 0 ? null : new(handle);

    /// <summary>
    /// Scale a rectangle of pixels in @src with @src_stride to @dest with
    /// @dest_stride using the horizontal scaler @hscaler and the vertical
    /// scaler @vscale.
    /// </summary>
    /// <remarks>
    /// <para>
    /// One or both of @hscale and @vscale can be NULL to only perform scaling in
    /// one dimension or do a copy without scaling.
    /// </para>
    /// <para>@x and @y are the coordinates in the destination image to process.</para>
    /// </remarks>
    /// <param name="vscale">The <c>vscale</c> argument.</param>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="src">The <c>src</c> argument.</param>
    /// <param name="srcStride">The <c>srcStride</c> argument.</param>
    /// <param name="dest">The <c>dest</c> argument.</param>
    /// <param name="destStride">The <c>destStride</c> argument.</param>
    /// <param name="x">The <c>x</c> argument.</param>
    /// <param name="y">The <c>y</c> argument.</param>
    /// <param name="width">The <c>width</c> argument.</param>
    /// <param name="height">The <c>height</c> argument.</param>
    public void _2d(Gst.Video.VideoScaler vscale, Gst.Video.VideoFormat format, nint src, int srcStride, nint dest, int destStride, uint x, uint y, uint width, uint height)
    {
        ArgumentNullException.ThrowIfNull(vscale);
        GstVideoScaler2d(Handle, vscale.Handle, (int)format, src, srcStride, dest, destStride, x, y, width, height);
        System.GC.KeepAlive(this);
    }

    /// <summary>Free a previously allocated #GstVideoScaler @scale.</summary>
    public void Free()
    {
        GstVideoScalerFree(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// For a given pixel at @out_offset, get the first required input pixel at
    /// @in_offset and the @n_taps filter coefficients.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that for interlaced content, @in_offset needs to be incremented with
    /// 2 to get the next input line.
    /// </para>
    /// </remarks>
    /// <param name="outOffset">The <c>outOffset</c> argument.</param>
    /// <param name="inOffset">The <c>inOffset</c> argument.</param>
    /// <returns>an array of @n_taps gdouble values with filter coefficients.</returns>
    public double[]? GetCoeff(uint outOffset, out uint inOffset)
    {
        uint inOffsetNative = default;
        uint nTapsNative = default;
        nint nativeResult = GstVideoScalerGetCoeff(Handle, outOffset, &inOffsetNative, &nTapsNative);
        System.GC.KeepAlive(this);
        inOffset = inOffsetNative;
        double[]? result = null;
        if (nativeResult != 0)
        {
            result = new double[(int)nTapsNative];
            new System.ReadOnlySpan<double>((void*)nativeResult, (int)nTapsNative).CopyTo(result);
        }
        return result;
    }

    /// <summary>Get the maximum number of taps for @scale.</summary>
    /// <returns>the maximum number of taps</returns>
    public uint GetMaxTaps()
    {
        uint nativeResult = GstVideoScalerGetMaxTaps(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Horizontally scale the pixels in @src to @dest, starting from @dest_offset
    /// for @width samples.
    /// </summary>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="src">The <c>src</c> argument.</param>
    /// <param name="dest">The <c>dest</c> argument.</param>
    /// <param name="destOffset">The <c>destOffset</c> argument.</param>
    /// <param name="width">The <c>width</c> argument.</param>
    public void Horizontal(Gst.Video.VideoFormat format, nint src, nint dest, uint destOffset, uint width)
    {
        GstVideoScalerHorizontal(Handle, (int)format, src, dest, destOffset, width);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Vertically combine @width pixels in the lines in @src_lines to @dest.
    /// @dest is the location of the target line at @dest_offset and
    /// @srcs are the input lines for @dest_offset.
    /// </summary>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="srcLines">The <c>srcLines</c> argument.</param>
    /// <param name="dest">The <c>dest</c> argument.</param>
    /// <param name="destOffset">The <c>destOffset</c> argument.</param>
    /// <param name="width">The <c>width</c> argument.</param>
    public void Vertical(Gst.Video.VideoFormat format, nint srcLines, nint dest, uint destOffset, uint width)
    {
        GstVideoScalerVertical(Handle, (int)format, srcLines, dest, destOffset, width);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_video_scaler_2d</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_scaler_2d")]
    private static partial void GstVideoScaler2d(nint hscale, nint vscale, int format, nint src, int srcStride, nint dest, int destStride, uint x, uint y, uint width, uint height);

    /// <summary>The <c>gst_video_scaler_free</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_scaler_free")]
    private static partial void GstVideoScalerFree(nint scale);

    /// <summary>The <c>gst_video_scaler_get_coeff</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_scaler_get_coeff")]
    private static partial nint GstVideoScalerGetCoeff(nint scale, uint outOffset, uint* inOffset, uint* nTaps);

    /// <summary>The <c>gst_video_scaler_get_max_taps</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_scaler_get_max_taps")]
    private static partial uint GstVideoScalerGetMaxTaps(nint scale);

    /// <summary>The <c>gst_video_scaler_horizontal</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_scaler_horizontal")]
    private static partial void GstVideoScalerHorizontal(nint scale, int format, nint src, nint dest, uint destOffset, uint width);

    /// <summary>The <c>gst_video_scaler_vertical</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_scaler_vertical")]
    private static partial void GstVideoScalerVertical(nint scale, int format, nint srcLines, nint dest, uint destOffset, uint width);
}
