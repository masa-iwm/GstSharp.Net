// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>
/// VideoAggregator can accept AYUV, ARGB and BGRA video streams. For each of the requested
/// sink pads it will compare the incoming geometry and framerate to define the
/// output parameters. Indeed output video frames will have the geometry of the
/// biggest incoming video stream and the framerate of the fastest incoming one.
/// </summary>
/// <remarks>
/// <para>VideoAggregator will do colorspace conversion.</para>
/// <para>
/// Zorder for each input stream can be configured on the
/// #GstVideoAggregatorPad.
/// </para>
/// </remarks>
public abstract unsafe partial class VideoAggregator : Gst.Base.Aggregator
{
    /// <summary>Wraps a native <c>GstVideoAggregator</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstVideoAggregator</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected VideoAggregator(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// The returned #GstTaskPool is used internally for performing parallel
    /// video format conversions/scaling/etc during the
    /// #GstVideoAggregatorPadClass::prepare_frame_start() process.
    /// Subclasses can add their own operation to perform using the returned
    /// #GstTaskPool during #GstVideoAggregatorClass::aggregate_frames().
    /// </summary>
    /// <returns>
    /// the #GstTaskPool that can be used by subclasses
    ///     for performing concurrent operations
    /// </returns>
    public Gst.TaskPool GetExecutionTaskPool()
    {
        nint nativeResult = GstVideoAggregatorGetExecutionTaskPool(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.TaskPool>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_aggregator_get_execution_task_pool returned no value.");
    }

    /// <summary>The <c>gst_video_aggregator_get_execution_task_pool</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_aggregator_get_execution_task_pool")]
    private static partial nint GstVideoAggregatorGetExecutionTaskPool(nint vagg);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstVideoAggregator</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_aggregator_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstVideoAggregator</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : VideoAggregator
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
