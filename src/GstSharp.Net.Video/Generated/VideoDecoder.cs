// <auto-generated/>
// Generated by GstSharp.Generator from GstVideo-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Video;

/// <summary>
/// This base class is for video decoders turning encoded data into raw video
/// frames.
/// </summary>
/// <remarks>
/// <para>
/// The GstVideoDecoder base class and derived subclasses should cooperate as
/// follows:
/// </para>
/// <para>## Configuration</para>
/// <para>
///   * Initially, GstVideoDecoder calls @start when the decoder element
///     is activated, which allows the subclass to perform any global setup.
/// </para>
/// <para>
///   * GstVideoDecoder calls @set_format to inform the subclass of caps
///     describing input video data that it is about to receive, including
///     possibly configuration data.
///     While unlikely, it might be called more than once, if changing input
///     parameters require reconfiguration.
/// </para>
/// <para>
///   * Incoming data buffers are processed as needed, described in Data
///     Processing below.
/// </para>
/// <para>  * GstVideoDecoder calls @stop at end of all processing.</para>
/// <para>## Data processing</para>
/// <para>
///   * The base class gathers input data, and optionally allows subclass
///     to parse this into subsequently manageable chunks, typically
///     corresponding to and referred to as 'frames'.
/// </para>
/// <para>
///   * Each input frame is provided in turn to the subclass' @handle_frame
///     callback.
///   * When the subclass enables the subframe mode with `gst_video_decoder_set_subframe_mode`,
///     the base class will provide to the subclass the same input frame with
///     different input buffers to the subclass @handle_frame
///     callback. During this call, the subclass needs to take
///     ownership of the input_buffer as @GstVideoCodecFrame.input_buffer
///     will have been changed before the next subframe buffer is received.
///     The subclass will call `gst_video_decoder_have_last_subframe`
///     when a new input frame can be created by the base class.
///     Every subframe will share the same @GstVideoCodecFrame.output_buffer
///     to write the decoding result. The subclass is responsible to protect
///     its access.
/// </para>
/// <para>
///   * If codec processing results in decoded data, the subclass should call
///     @gst_video_decoder_finish_frame to have decoded data pushed
///     downstream. In subframe mode
///     the subclass should call @gst_video_decoder_finish_subframe until the
///     last subframe where it should call @gst_video_decoder_finish_frame.
///     The subclass can detect the last subframe using GST_VIDEO_BUFFER_FLAG_MARKER
///     on buffers or using its own logic to collect the subframes.
///     In case of decoding failure, the subclass must call
///     @gst_video_decoder_drop_frame or @gst_video_decoder_drop_subframe,
///     to allow the base class to do timestamp and offset tracking, and possibly
///     to requeue the frame for a later attempt in the case of reverse playback.
/// </para>
/// <para>## Shutdown phase</para>
/// <para>
///   * The GstVideoDecoder class calls @stop to inform the subclass that data
///     parsing will be stopped.
/// </para>
/// <para>## Additional Notes</para>
/// <para>  * Seeking/Flushing</para>
/// <para>
///     * When the pipeline is seeked or otherwise flushed, the subclass is
///       informed via a call to its @reset callback, with the hard parameter
///       set to true. This indicates the subclass should drop any internal data
///       queues and timestamps and prepare for a fresh set of buffers to arrive
///       for parsing and decoding.
/// </para>
/// <para>  * End Of Stream</para>
/// <para>
///     * At end-of-stream, the subclass @parse function may be called some final
///       times with the at_eos parameter set to true, indicating that the element
///       should not expect any more data to be arriving, and it should parse and
///       remaining frames and call gst_video_decoder_have_frame() if possible.
/// </para>
/// <para>
/// The subclass is responsible for providing pad template caps for
/// source and sink pads. The pads need to be named "sink" and "src". It also
/// needs to provide information about the output caps, when they are known.
/// This may be when the base class calls the subclass' @set_format function,
/// though it might be during decoding, before calling
/// @gst_video_decoder_finish_frame. This is done via
/// @gst_video_decoder_set_output_state
/// </para>
/// <para>
/// The subclass is also responsible for providing (presentation) timestamps
/// (likely based on corresponding input ones).  If that is not applicable
/// or possible, the base class provides limited framerate based interpolation.
/// </para>
/// <para>
/// Similarly, the base class provides some limited (legacy) seeking support
/// if specifically requested by the subclass, as full-fledged support
/// should rather be left to upstream demuxer, parser or alike.  This simple
/// approach caters for seeking and duration reporting using estimated input
/// bitrates. To enable it, a subclass should call
/// @gst_video_decoder_set_estimate_rate to enable handling of incoming
/// byte-streams.
/// </para>
/// <para>
/// The base class provides some support for reverse playback, in particular
/// in case incoming data is not packetized or upstream does not provide
/// fragments on keyframe boundaries.  However, the subclass should then be
/// prepared for the parsing and frame processing stage to occur separately
/// (in normal forward processing, the latter immediately follows the former),
/// The subclass also needs to ensure the parsing stage properly marks
/// keyframes, unless it knows the upstream elements will do so properly for
/// incoming data.
/// </para>
/// <para>The bare minimum that a functional subclass needs to implement is:</para>
/// <para>
///   * Provide pad templates
///   * Inform the base class of output caps via
///      @gst_video_decoder_set_output_state
/// </para>
/// <para>
///   * Parse input data, if it is not considered packetized from upstream
///      Data will be provided to @parse which should invoke
///      @gst_video_decoder_add_to_frame and @gst_video_decoder_have_frame to
///      separate the data belonging to each video frame.
/// </para>
/// <para>
///   * Accept data in @handle_frame and provide decoded results to
///      @gst_video_decoder_finish_frame, or call @gst_video_decoder_drop_frame.
/// </para>
/// </remarks>
public abstract unsafe partial class VideoDecoder : Gst.Element
{
    /// <summary>Wraps a native <c>GstVideoDecoder</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstVideoDecoder</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected VideoDecoder(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Removes next @n_bytes of input data and adds it to currently parsed frame.</summary>
    /// <param name="nBytes">the number of bytes to add</param>
    public void AddToFrame(int nBytes)
    {
        GstVideoDecoderAddToFrame(Handle, nBytes);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Helper function that allocates a buffer to hold a video frame for @decoder's
    /// current #GstVideoCodecState.
    /// </summary>
    /// <remarks>
    /// <para>
    /// You should use gst_video_decoder_allocate_output_frame() instead of this
    /// function, if possible at all.
    /// </para>
    /// </remarks>
    /// <returns>
    /// allocated buffer, or NULL if no buffer could be
    ///     allocated (e.g. when downstream is flushing or shutting down)
    /// </returns>
    public Gst.Buffer? AllocateOutputBuffer()
    {
        nint nativeResult = GstVideoDecoderAllocateOutputBuffer(Handle);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Helper function that allocates a buffer to hold a video frame for @decoder's
    /// current #GstVideoCodecState.  Subclass should already have configured video
    /// state and set src pad caps.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The buffer allocated here is owned by the frame and you should only
    /// keep references to the frame, not the buffer.
    /// </para>
    /// </remarks>
    /// <param name="frame">a #GstVideoCodecFrame</param>
    /// <returns>%GST_FLOW_OK if an output buffer could be allocated</returns>
    public Gst.FlowReturn AllocateOutputFrame(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        int nativeResult = GstVideoDecoderAllocateOutputFrame(Handle, frame.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Same as #gst_video_decoder_allocate_output_frame except it allows passing
    /// #GstBufferPoolAcquireParams to the sub call gst_buffer_pool_acquire_buffer.
    /// </summary>
    /// <param name="frame">a #GstVideoCodecFrame</param>
    /// <param name="params">a #GstBufferPoolAcquireParams</param>
    /// <returns>%GST_FLOW_OK if an output buffer could be allocated</returns>
    public Gst.FlowReturn AllocateOutputFrameWithParams(Gst.Video.VideoCodecFrame frame, Gst.BufferPoolAcquireParams @params)
    {
        ArgumentNullException.ThrowIfNull(frame);
        Gst.BufferPoolAcquireParams @paramsNative = @params;
        int nativeResult = GstVideoDecoderAllocateOutputFrameWithParams(Handle, frame.Handle, &@paramsNative);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Similar to gst_video_decoder_finish_frame(), but drops @frame in any
    /// case and posts a QoS message with the frame's details on the bus.
    /// In any case, the frame is considered finished and released.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="frame">
    /// the #GstVideoCodecFrame to drop
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn, usually GST_FLOW_OK.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public Gst.FlowReturn DropFrame(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        int nativeResult = GstVideoDecoderDropFrame(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Drops input data.
    /// The frame is not considered finished until the whole frame
    /// is finished or dropped by the subclass.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="frame">
    /// the #GstVideoCodecFrame
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn, usually GST_FLOW_OK.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public Gst.FlowReturn DropSubframe(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        int nativeResult = GstVideoDecoderDropSubframe(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// @frame should have a valid decoded data buffer, whose metadata fields
    /// are then appropriately set according to frame data and pushed downstream.
    /// If no output data is provided, @frame is considered skipped.
    /// In any case, the frame is considered finished and released.
    /// </summary>
    /// <remarks>
    /// <para>
    /// After calling this function the output buffer of the frame is to be
    /// considered read-only. This function will also change the metadata
    /// of the buffer.
    /// </para>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="frame">
    /// a decoded #GstVideoCodecFrame
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn resulting from sending data downstream</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public Gst.FlowReturn FinishFrame(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        int nativeResult = GstVideoDecoderFinishFrame(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Indicate that a subframe has been finished to be decoded
    /// by the subclass. This method should be called for all subframes
    /// except the last subframe where @gst_video_decoder_finish_frame
    /// should be called instead.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="frame">
    /// the #GstVideoCodecFrame
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn, usually GST_FLOW_OK.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public Gst.FlowReturn FinishSubframe(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        int nativeResult = GstVideoDecoderFinishSubframe(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Lets #GstVideoDecoder sub-classes to know the memory @allocator
    /// used by the base class and its @params.
    /// </summary>
    /// <remarks>
    /// <para>Unref the @allocator after use it.</para>
    /// </remarks>
    /// <param name="allocator">
    /// the #GstAllocator
    /// used
    /// </param>
    /// <param name="params">
    /// the
    /// #GstAllocationParams of @allocator
    /// The binding allocates the storage; on return the caller owns
    /// <paramref name="params"/> and disposes it.
    /// </param>
    public void GetAllocator(out Gst.Allocator? allocator, out Gst.AllocationParams @params)
    {
        nint instanceHandle = Handle;
        nint allocatorNative = default;
        nint @paramsNative = GstAllocationParamsNew();
        GstVideoDecoderGetAllocator(instanceHandle, &allocatorNative, @paramsNative);
        System.GC.KeepAlive(this);
        @params = Gst.AllocationParams.FromNative(@paramsNative, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_allocation_params_new returned no value.");
        allocator = Gst.GObject.Object.FromNative<Gst.Allocator>(allocatorNative, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_video_decoder_get_buffer_pool</c> function.</summary>
    /// <returns>
    /// the instance of the #GstBufferPool used
    /// by the decoder; free it after use it
    /// </returns>
    public Gst.BufferPool? GetBufferPool()
    {
        nint nativeResult = GstVideoDecoderGetBufferPool(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.BufferPool>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_video_decoder_get_estimate_rate</c> function.</summary>
    /// <returns>currently configured byte to time conversion setting</returns>
    public int GetEstimateRate()
    {
        int nativeResult = GstVideoDecoderGetEstimateRate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get a pending unfinished #GstVideoCodecFrame</summary>
    /// <param name="frameNumber">system_frame_number of a frame</param>
    /// <returns>pending unfinished #GstVideoCodecFrame identified by @frame_number.</returns>
    public Gst.Video.VideoCodecFrame? GetFrame(int frameNumber)
    {
        nint nativeResult = GstVideoDecoderGetFrame(Handle, frameNumber);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoCodecFrame.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get all pending unfinished #GstVideoCodecFrame</summary>
    /// <returns>pending unfinished #GstVideoCodecFrame.</returns>
    public System.Collections.Generic.IReadOnlyList<Gst.Video.VideoCodecFrame> GetFrames()
    {
        nint nativeResult = GstVideoDecoderGetFrames(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<Gst.Video.VideoCodecFrame> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.Video.VideoCodecFrame.FromNative(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Queries the number of the last subframe received by
    /// the decoder baseclass in the @frame.
    /// </summary>
    /// <param name="frame">the #GstVideoCodecFrame to update</param>
    /// <returns>the current subframe index received in subframe mode, 1 otherwise.</returns>
    public uint GetInputSubframeIndex(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        uint nativeResult = GstVideoDecoderGetInputSubframeIndex(Handle, frame.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return nativeResult;
    }

    /// <summary>
    /// Query the configured decoder latency. Results will be returned via
    /// @min_latency and @max_latency.
    /// </summary>
    /// <param name="minLatency">
    /// address of variable in which to store the
    ///     configured minimum latency, or %NULL
    /// </param>
    /// <param name="maxLatency">
    /// address of variable in which to store the
    ///     configured mximum latency, or %NULL
    /// </param>
    public void GetLatency(out Gst.ClockTime minLatency, out Gst.ClockTime maxLatency)
    {
        ulong minLatencyNative = default;
        ulong maxLatencyNative = default;
        GstVideoDecoderGetLatency(Handle, &minLatencyNative, &maxLatencyNative);
        System.GC.KeepAlive(this);
        minLatency = new Gst.ClockTime(minLatencyNative);
        maxLatency = new Gst.ClockTime(maxLatencyNative);
    }

    /// <summary>
    /// Determines maximum possible decoding time for @frame that will
    /// allow it to decode and arrive in time (as determined by QoS events).
    /// In particular, a negative result means decoding in time is no longer possible
    /// and should therefore occur as soon/skippy as possible.
    /// </summary>
    /// <param name="frame">a #GstVideoCodecFrame</param>
    /// <returns>max decoding time.</returns>
    public long GetMaxDecodeTime(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        long nativeResult = GstVideoDecoderGetMaxDecodeTime(Handle, frame.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return nativeResult;
    }

    /// <summary>The <c>gst_video_decoder_get_max_errors</c> function.</summary>
    /// <returns>currently configured decoder tolerated error count.</returns>
    public int GetMaxErrors()
    {
        int nativeResult = GstVideoDecoderGetMaxErrors(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Queries decoder required format handling.</summary>
    /// <returns>%TRUE if required format handling is enabled.</returns>
    public bool GetNeedsFormat()
    {
        int nativeResult = GstVideoDecoderGetNeedsFormat(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Queries if the decoder requires a sync point before it starts outputting
    /// data in the beginning.
    /// </summary>
    /// <returns>%TRUE if a sync point is required in the beginning.</returns>
    public bool GetNeedsSyncPoint()
    {
        int nativeResult = GstVideoDecoderGetNeedsSyncPoint(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get the oldest pending unfinished #GstVideoCodecFrame</summary>
    /// <returns>oldest pending unfinished #GstVideoCodecFrame.</returns>
    public Gst.Video.VideoCodecFrame? GetOldestFrame()
    {
        nint nativeResult = GstVideoDecoderGetOldestFrame(Handle);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoCodecFrame.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the #GstVideoCodecState currently describing the output stream.</summary>
    /// <returns>#GstVideoCodecState describing format of video data.</returns>
    public Gst.Video.VideoCodecState? GetOutputState()
    {
        nint nativeResult = GstVideoDecoderGetOutputState(Handle);
        System.GC.KeepAlive(this);
        return Gst.Video.VideoCodecState.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Queries whether input data is considered packetized or not by the
    /// base class.
    /// </summary>
    /// <returns>TRUE if input data is considered packetized.</returns>
    public bool GetPacketized()
    {
        int nativeResult = GstVideoDecoderGetPacketized(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns the number of bytes previously added to the current frame
    /// by calling gst_video_decoder_add_to_frame().
    /// </summary>
    /// <returns>The number of bytes pending for the current frame</returns>
    public nuint GetPendingFrameSize()
    {
        nuint nativeResult = GstVideoDecoderGetPendingFrameSize(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Queries the number of subframes in the frame processed by
    /// the decoder baseclass.
    /// </summary>
    /// <param name="frame">the #GstVideoCodecFrame to update</param>
    /// <returns>the current subframe processed received in subframe mode.</returns>
    public uint GetProcessedSubframeIndex(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        uint nativeResult = GstVideoDecoderGetProcessedSubframeIndex(Handle, frame.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return nativeResult;
    }

    /// <summary>The <c>gst_video_decoder_get_qos_proportion</c> function.</summary>
    /// <returns>The current QoS proportion.</returns>
    public double GetQosProportion()
    {
        double nativeResult = GstVideoDecoderGetQosProportion(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Queries whether input data is considered as subframes or not by the
    /// base class. If FALSE, each input buffer will be considered as a full
    /// frame.
    /// </summary>
    /// <returns>TRUE if input data is considered as sub frames.</returns>
    public bool GetSubframeMode()
    {
        int nativeResult = GstVideoDecoderGetSubframeMode(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gathers all data collected for currently parsed frame, gathers corresponding
    /// metadata and passes it along for further processing, i.e. @handle_frame.
    /// </summary>
    /// <returns>a #GstFlowReturn</returns>
    public Gst.FlowReturn HaveFrame()
    {
        int nativeResult = GstVideoDecoderHaveFrame(Handle);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Indicates that the last subframe has been processed by the decoder
    /// in @frame. This will release the current frame in video decoder
    /// allowing to receive new frames from upstream elements. This method
    /// must be called in the subclass @handle_frame callback.
    /// </summary>
    /// <param name="frame">the #GstVideoCodecFrame to update</param>
    /// <returns>a #GstFlowReturn, usually GST_FLOW_OK.</returns>
    public Gst.FlowReturn HaveLastSubframe(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        int nativeResult = GstVideoDecoderHaveLastSubframe(Handle, frame.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Sets the audio decoder tags and how they should be merged with any
    /// upstream stream tags. This will override any tags previously-set
    /// with gst_audio_decoder_merge_tags().
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that this is provided for convenience, and the subclass is
    /// not required to use this and can still do tag handling on its own.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="tags">
    /// a #GstTagList to merge, or NULL to unset
    ///     previously-set tags
    /// </param>
    /// <param name="mode">the #GstTagMergeMode to use, usually #GST_TAG_MERGE_REPLACE</param>
    public void MergeTags(Gst.TagList? tags, Gst.TagMergeMode mode)
    {
        GstVideoDecoderMergeTags(Handle, tags is null ? 0 : tags.Handle, (int)mode);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(tags);
    }

    /// <summary>
    /// Negotiate with downstream elements to currently configured #GstVideoCodecState.
    /// Unmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if
    /// negotiate fails.
    /// </summary>
    /// <returns>%TRUE if the negotiation succeeded, else %FALSE.</returns>
    public bool Negotiate()
    {
        int nativeResult = GstVideoDecoderNegotiate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns caps that express @caps (or sink template caps if @caps == NULL)
    /// restricted to resolution/format/... combinations supported by downstream
    /// elements.
    /// </summary>
    /// <param name="caps">initial caps</param>
    /// <param name="filter">filter caps</param>
    /// <returns>a #GstCaps owned by caller</returns>
    public Gst.Caps ProxyGetcaps(Gst.Caps? caps, Gst.Caps? filter)
    {
        nint nativeResult = GstVideoDecoderProxyGetcaps(Handle, caps is null ? 0 : caps.Handle, filter is null ? 0 : filter.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        System.GC.KeepAlive(filter);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_video_decoder_proxy_getcaps returned no value.");
    }

    /// <summary>
    /// Similar to gst_video_decoder_drop_frame(), but simply releases @frame
    /// without any processing other than removing it from list of pending frames,
    /// after which it is considered finished and released.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>frame</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="frame">
    /// the #GstVideoCodecFrame to release
    /// The call consumes it: <paramref name="frame"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="frame"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="frame"/> was disposed.
    /// </exception>
    public void ReleaseFrame(Gst.Video.VideoCodecFrame frame)
    {
        ArgumentNullException.ThrowIfNull(frame);
        nint instanceHandle = Handle;
        nint frameNative = frame.Handle;
        nuint frameType = frame.BoxedType.Value;
        nint frameOwned = Gst.Interop.GObjectNative.BoxedCopy(frameType, frameNative);
        GstVideoDecoderReleaseFrame(instanceHandle, frameOwned);
        System.GC.KeepAlive(this);
        frame.Dispose();
    }

    /// <summary>
    /// Allows the #GstVideoDecoder subclass to request from the base class that
    /// a new sync should be requested from upstream, and that @frame was the frame
    /// when the subclass noticed that a new sync point is required. A reason for
    /// the subclass to do this could be missing reference frames, for example.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The base class will then request a new sync point from upstream as long as
    /// the time that passed since the last one is exceeding
    /// #GstVideoDecoder:min-force-key-unit-interval.
    /// </para>
    /// <para>
    /// The subclass can signal via @flags how the frames until the next sync point
    /// should be handled:
    /// </para>
    /// <para>
    ///   * If %GST_VIDEO_DECODER_REQUEST_SYNC_POINT_DISCARD_INPUT is selected then
    ///     all following input frames until the next sync point are discarded.
    ///     This can be useful if the lack of a sync point will prevent all further
    ///     decoding and the decoder implementation is not very robust in handling
    ///     missing references frames.
    ///   * If %GST_VIDEO_DECODER_REQUEST_SYNC_POINT_CORRUPT_OUTPUT is selected
    ///     then all output frames following @frame are marked as corrupted via
    ///     %GST_BUFFER_FLAG_CORRUPTED. Corrupted frames can be automatically
    ///     dropped by the base class, see #GstVideoDecoder:discard-corrupted-frames.
    ///     Subclasses can manually mark frames as corrupted via %GST_VIDEO_CODEC_FRAME_FLAG_CORRUPTED
    ///     before calling gst_video_decoder_finish_frame().
    /// </para>
    /// </remarks>
    /// <param name="frame">a #GstVideoCodecFrame</param>
    /// <param name="flags">#GstVideoDecoderRequestSyncPointFlags</param>
    public void RequestSyncPoint(Gst.Video.VideoCodecFrame frame, Gst.Video.VideoDecoderRequestSyncPointFlags flags)
    {
        ArgumentNullException.ThrowIfNull(frame);
        GstVideoDecoderRequestSyncPoint(Handle, frame.Handle, (int)flags);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(frame);
    }

    /// <summary>Allows baseclass to perform byte to time estimated conversion.</summary>
    /// <param name="enabled">whether to enable byte to time conversion</param>
    public void SetEstimateRate(bool enabled)
    {
        GstVideoDecoderSetEstimateRate(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Same as #gst_video_decoder_set_output_state() but also allows you to also set
    /// the interlacing mode.
    /// </summary>
    /// <param name="fmt">a #GstVideoFormat</param>
    /// <param name="interlaceMode">A #GstVideoInterlaceMode</param>
    /// <param name="width">The width in pixels</param>
    /// <param name="height">The height in pixels</param>
    /// <param name="reference">An optional reference #GstVideoCodecState</param>
    /// <returns>the newly configured output state.</returns>
    public Gst.Video.VideoCodecState? SetInterlacedOutputState(Gst.Video.VideoFormat fmt, Gst.Video.VideoInterlaceMode interlaceMode, uint width, uint height, Gst.Video.VideoCodecState? reference)
    {
        nint nativeResult = GstVideoDecoderSetInterlacedOutputState(Handle, (int)fmt, (int)interlaceMode, width, height, reference is null ? 0 : reference.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(reference);
        return Gst.Video.VideoCodecState.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Lets #GstVideoDecoder sub-classes tell the baseclass what the decoder latency
    /// is. If the provided values changed from previously provided ones, this will
    /// also post a LATENCY message on the bus so the pipeline can reconfigure its
    /// global latency.
    /// </summary>
    /// <param name="minLatency">minimum latency</param>
    /// <param name="maxLatency">maximum latency</param>
    public void SetLatency(Gst.ClockTime minLatency, Gst.ClockTime maxLatency)
    {
        GstVideoDecoderSetLatency(Handle, minLatency.Nanoseconds, maxLatency.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets numbers of tolerated decoder errors, where a tolerated one is then only
    /// warned about, but more than tolerated will lead to fatal error.  You can set
    /// -1 for never returning fatal errors. Default is set to
    /// GST_VIDEO_DECODER_MAX_ERRORS.
    /// </summary>
    /// <remarks>
    /// <para>The '-1' option was added in 1.4</para>
    /// </remarks>
    /// <param name="num">max tolerated errors</param>
    public void SetMaxErrors(int num)
    {
        GstVideoDecoderSetMaxErrors(Handle, num);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configures decoder format needs.  If enabled, subclass needs to be
    /// negotiated with format caps before it can process any data.  It will then
    /// never be handed any data before it has been configured.
    /// Otherwise, it might be handed data without having been configured and
    /// is then expected being able to do so either by default
    /// or based on the input data.
    /// </summary>
    /// <param name="enabled">new state</param>
    public void SetNeedsFormat(bool enabled)
    {
        GstVideoDecoderSetNeedsFormat(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configures whether the decoder requires a sync point before it starts
    /// outputting data in the beginning. If enabled, the base class will discard
    /// all non-sync point frames in the beginning and after a flush and does not
    /// pass it to the subclass.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If the first frame is not a sync point, the base class will request a sync
    /// point via the force-key-unit event.
    /// </para>
    /// </remarks>
    /// <param name="enabled">new state</param>
    public void SetNeedsSyncPoint(bool enabled)
    {
        GstVideoDecoderSetNeedsSyncPoint(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Creates a new #GstVideoCodecState with the specified @fmt, @width and @height
    /// as the output state for the decoder.
    /// Any previously set output state on @decoder will be replaced by the newly
    /// created one.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If the subclass wishes to copy over existing fields (like pixel aspec ratio,
    /// or framerate) from an existing #GstVideoCodecState, it can be provided as a
    /// @reference.
    /// </para>
    /// <para>
    /// If the subclass wishes to override some fields from the output state (like
    /// pixel-aspect-ratio or framerate) it can do so on the returned #GstVideoCodecState.
    /// </para>
    /// <para>
    /// The new output state will only take effect (set on pads and buffers) starting
    /// from the next call to #gst_video_decoder_finish_frame().
    /// </para>
    /// </remarks>
    /// <param name="fmt">a #GstVideoFormat</param>
    /// <param name="width">The width in pixels</param>
    /// <param name="height">The height in pixels</param>
    /// <param name="reference">An optional reference #GstVideoCodecState</param>
    /// <returns>the newly configured output state.</returns>
    public Gst.Video.VideoCodecState? SetOutputState(Gst.Video.VideoFormat fmt, uint width, uint height, Gst.Video.VideoCodecState? reference)
    {
        nint nativeResult = GstVideoDecoderSetOutputState(Handle, (int)fmt, width, height, reference is null ? 0 : reference.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(reference);
        return Gst.Video.VideoCodecState.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Allows baseclass to consider input data as packetized or not. If the
    /// input is packetized, then the @parse method will not be called.
    /// </summary>
    /// <param name="packetized">whether the input data should be considered as packetized.</param>
    public void SetPacketized(bool packetized)
    {
        GstVideoDecoderSetPacketized(Handle, packetized ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If this is set to TRUE, it informs the base class that the subclass
    /// can receive the data at a granularity lower than one frame.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that in this mode, the subclass has two options. It can either
    /// require the presence of a GST_VIDEO_BUFFER_FLAG_MARKER to mark the
    /// end of a frame. Or it can operate in such a way that it will decode
    /// a single frame at a time. In this second case, every buffer that
    /// arrives to the element is considered part of the same frame until
    /// gst_video_decoder_finish_frame() is called.
    /// </para>
    /// <para>
    /// In either case, the same #GstVideoCodecFrame will be passed to the
    /// GstVideoDecoderClass:handle_frame vmethod repeatedly with a
    /// different GstVideoCodecFrame:input_buffer every time until the end of the
    /// frame has been signaled using either method.
    /// This method must be called during the decoder subclass @set_format call.
    /// </para>
    /// </remarks>
    /// <param name="subframeMode">whether the input data should be considered as subframes.</param>
    public void SetSubframeMode(bool subframeMode)
    {
        GstVideoDecoderSetSubframeMode(Handle, subframeMode ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Lets #GstVideoDecoder sub-classes decide if they want the sink pad
    /// to use the default pad query handler to reply to accept-caps queries.
    /// </summary>
    /// <remarks>
    /// <para>
    /// By setting this to true it is possible to further customize the default
    /// handler with %GST_PAD_SET_ACCEPT_INTERSECT and
    /// %GST_PAD_SET_ACCEPT_TEMPLATE
    /// </para>
    /// </remarks>
    /// <param name="use">if the default pad accept-caps query handling should be used</param>
    public void SetUseDefaultPadAcceptcaps(bool use)
    {
        GstVideoDecoderSetUseDefaultPadAcceptcaps(Handle, use ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Maximum number of tolerated consecutive decode errors. See
    /// gst_video_decoder_set_max_errors() for more details.
    /// </summary>
    public int MaxErrors
    {
        get => GetMaxErrors();
        set => SetMaxErrors(value);
    }

    /// <summary>
    /// GstVideoDecoderRequestSyncPointFlags to use for the automatically
    /// requested sync points if `automatic-request-sync-points` is enabled.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public Gst.Video.VideoDecoderRequestSyncPointFlags AutomaticRequestSyncPointFlags
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("automatic-request-sync-point-flags");
            return (Gst.Video.VideoDecoderRequestSyncPointFlags)holder.GetFlags();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("automatic-request-sync-point-flags");
            holder.SetFlags((uint)value);
            SetPropertyValue("automatic-request-sync-point-flags", in holder);
        }
    }

    /// <summary>
    /// If set to %TRUE the decoder will automatically request sync points when
    /// it seems like a good idea, e.g. if the first frames are not key frames or
    /// if packet loss was reported by upstream.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool AutomaticRequestSyncPoints
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("automatic-request-sync-points");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("automatic-request-sync-points");
            holder.SetBoolean(value);
            SetPropertyValue("automatic-request-sync-points", in holder);
        }
    }

    /// <summary>
    /// If set to %TRUE the decoder will discard frames that are marked as
    /// corrupted instead of outputting them.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool DiscardCorruptedFrames
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("discard-corrupted-frames");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("discard-corrupted-frames");
            holder.SetBoolean(value);
            SetPropertyValue("discard-corrupted-frames", in holder);
        }
    }

    /// <summary>
    /// Minimum interval between force-key-unit events sent upstream by the
    /// decoder. Setting this to 0 will cause every event to be handled, setting
    /// this to %GST_CLOCK_TIME_NONE will cause every event to be ignored.
    /// </summary>
    /// <remarks>
    /// <para>
    /// See gst_video_event_new_upstream_force_key_unit() for more details about
    /// force-key-unit events.
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public ulong MinForceKeyUnitInterval
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("min-force-key-unit-interval");
            return holder.GetUInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("min-force-key-unit-interval");
            holder.SetUInt64(value);
            SetPropertyValue("min-force-key-unit-interval", in holder);
        }
    }

    /// <summary>
    /// If set to %TRUE the decoder will handle QoS events received
    /// from downstream elements.
    /// This includes dropping output frames which are detected as late
    /// using the metrics reported by those events.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool Qos
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("qos");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("qos");
            holder.SetBoolean(value);
            SetPropertyValue("qos", in holder);
        }
    }

    /// <summary>The <c>gst_video_decoder_add_to_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_add_to_frame")]
    private static partial void GstVideoDecoderAddToFrame(nint decoder, int nBytes);

    /// <summary>The <c>gst_video_decoder_allocate_output_buffer</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_allocate_output_buffer")]
    private static partial nint GstVideoDecoderAllocateOutputBuffer(nint decoder);

    /// <summary>The <c>gst_video_decoder_allocate_output_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_allocate_output_frame")]
    private static partial int GstVideoDecoderAllocateOutputFrame(nint decoder, nint frame);

    /// <summary>The <c>gst_video_decoder_allocate_output_frame_with_params</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_allocate_output_frame_with_params")]
    private static partial int GstVideoDecoderAllocateOutputFrameWithParams(nint decoder, nint frame, Gst.BufferPoolAcquireParams* @params);

    /// <summary>The <c>gst_video_decoder_drop_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_drop_frame")]
    private static partial int GstVideoDecoderDropFrame(nint dec, nint frame);

    /// <summary>The <c>gst_video_decoder_drop_subframe</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_drop_subframe")]
    private static partial int GstVideoDecoderDropSubframe(nint dec, nint frame);

    /// <summary>The <c>gst_video_decoder_finish_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_finish_frame")]
    private static partial int GstVideoDecoderFinishFrame(nint decoder, nint frame);

    /// <summary>The <c>gst_video_decoder_finish_subframe</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_finish_subframe")]
    private static partial int GstVideoDecoderFinishSubframe(nint decoder, nint frame);

    /// <summary>The <c>gst_video_decoder_get_allocator</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_allocator")]
    private static partial void GstVideoDecoderGetAllocator(nint decoder, nint* allocator, nint @params);

    /// <summary>The <c>gst_video_decoder_get_buffer_pool</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_buffer_pool")]
    private static partial nint GstVideoDecoderGetBufferPool(nint decoder);

    /// <summary>The <c>gst_video_decoder_get_estimate_rate</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_estimate_rate")]
    private static partial int GstVideoDecoderGetEstimateRate(nint dec);

    /// <summary>The <c>gst_video_decoder_get_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_frame")]
    private static partial nint GstVideoDecoderGetFrame(nint decoder, int frameNumber);

    /// <summary>The <c>gst_video_decoder_get_frames</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_frames")]
    private static partial nint GstVideoDecoderGetFrames(nint decoder);

    /// <summary>The <c>gst_video_decoder_get_input_subframe_index</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_input_subframe_index")]
    private static partial uint GstVideoDecoderGetInputSubframeIndex(nint decoder, nint frame);

    /// <summary>The <c>gst_video_decoder_get_latency</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_latency")]
    private static partial void GstVideoDecoderGetLatency(nint decoder, ulong* minLatency, ulong* maxLatency);

    /// <summary>The <c>gst_video_decoder_get_max_decode_time</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_max_decode_time")]
    private static partial long GstVideoDecoderGetMaxDecodeTime(nint decoder, nint frame);

    /// <summary>The <c>gst_video_decoder_get_max_errors</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_max_errors")]
    private static partial int GstVideoDecoderGetMaxErrors(nint dec);

    /// <summary>The <c>gst_video_decoder_get_needs_format</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_needs_format")]
    private static partial int GstVideoDecoderGetNeedsFormat(nint dec);

    /// <summary>The <c>gst_video_decoder_get_needs_sync_point</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_needs_sync_point")]
    private static partial int GstVideoDecoderGetNeedsSyncPoint(nint dec);

    /// <summary>The <c>gst_video_decoder_get_oldest_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_oldest_frame")]
    private static partial nint GstVideoDecoderGetOldestFrame(nint decoder);

    /// <summary>The <c>gst_video_decoder_get_output_state</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_output_state")]
    private static partial nint GstVideoDecoderGetOutputState(nint decoder);

    /// <summary>The <c>gst_video_decoder_get_packetized</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_packetized")]
    private static partial int GstVideoDecoderGetPacketized(nint decoder);

    /// <summary>The <c>gst_video_decoder_get_pending_frame_size</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_pending_frame_size")]
    private static partial nuint GstVideoDecoderGetPendingFrameSize(nint decoder);

    /// <summary>The <c>gst_video_decoder_get_processed_subframe_index</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_processed_subframe_index")]
    private static partial uint GstVideoDecoderGetProcessedSubframeIndex(nint decoder, nint frame);

    /// <summary>The <c>gst_video_decoder_get_qos_proportion</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_qos_proportion")]
    private static partial double GstVideoDecoderGetQosProportion(nint decoder);

    /// <summary>The <c>gst_video_decoder_get_subframe_mode</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_subframe_mode")]
    private static partial int GstVideoDecoderGetSubframeMode(nint decoder);

    /// <summary>The <c>gst_video_decoder_have_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_have_frame")]
    private static partial int GstVideoDecoderHaveFrame(nint decoder);

    /// <summary>The <c>gst_video_decoder_have_last_subframe</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_have_last_subframe")]
    private static partial int GstVideoDecoderHaveLastSubframe(nint decoder, nint frame);

    /// <summary>The <c>gst_video_decoder_merge_tags</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_merge_tags")]
    private static partial void GstVideoDecoderMergeTags(nint decoder, nint tags, int mode);

    /// <summary>The <c>gst_video_decoder_negotiate</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_negotiate")]
    private static partial int GstVideoDecoderNegotiate(nint decoder);

    /// <summary>The <c>gst_video_decoder_proxy_getcaps</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_proxy_getcaps")]
    private static partial nint GstVideoDecoderProxyGetcaps(nint decoder, nint caps, nint filter);

    /// <summary>The <c>gst_video_decoder_release_frame</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_release_frame")]
    private static partial void GstVideoDecoderReleaseFrame(nint dec, nint frame);

    /// <summary>The <c>gst_video_decoder_request_sync_point</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_request_sync_point")]
    private static partial void GstVideoDecoderRequestSyncPoint(nint dec, nint frame, int flags);

    /// <summary>The <c>gst_video_decoder_set_estimate_rate</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_estimate_rate")]
    private static partial void GstVideoDecoderSetEstimateRate(nint dec, int enabled);

    /// <summary>The <c>gst_video_decoder_set_interlaced_output_state</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_interlaced_output_state")]
    private static partial nint GstVideoDecoderSetInterlacedOutputState(nint decoder, int fmt, int interlaceMode, uint width, uint height, nint reference);

    /// <summary>The <c>gst_video_decoder_set_latency</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_latency")]
    private static partial void GstVideoDecoderSetLatency(nint decoder, ulong minLatency, ulong maxLatency);

    /// <summary>The <c>gst_video_decoder_set_max_errors</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_max_errors")]
    private static partial void GstVideoDecoderSetMaxErrors(nint dec, int num);

    /// <summary>The <c>gst_video_decoder_set_needs_format</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_needs_format")]
    private static partial void GstVideoDecoderSetNeedsFormat(nint dec, int enabled);

    /// <summary>The <c>gst_video_decoder_set_needs_sync_point</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_needs_sync_point")]
    private static partial void GstVideoDecoderSetNeedsSyncPoint(nint dec, int enabled);

    /// <summary>The <c>gst_video_decoder_set_output_state</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_output_state")]
    private static partial nint GstVideoDecoderSetOutputState(nint decoder, int fmt, uint width, uint height, nint reference);

    /// <summary>The <c>gst_video_decoder_set_packetized</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_packetized")]
    private static partial void GstVideoDecoderSetPacketized(nint decoder, int packetized);

    /// <summary>The <c>gst_video_decoder_set_subframe_mode</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_subframe_mode")]
    private static partial void GstVideoDecoderSetSubframeMode(nint decoder, int subframeMode);

    /// <summary>The <c>gst_video_decoder_set_use_default_pad_acceptcaps</c> entry point.</summary>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_set_use_default_pad_acceptcaps")]
    private static partial void GstVideoDecoderSetUseDefaultPadAcceptcaps(nint decoder, int use);

    /// <summary>The <c>gst_allocation_params_new</c> entry point, which allocates the storage of a caller allocated out parameter.</summary>
    /// <returns>A new, zeroed instance the caller owns.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_allocation_params_new")]
    private static partial nint GstAllocationParamsNew();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstVideoDecoder</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstVideo", EntryPoint = "gst_video_decoder_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstVideoDecoder</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : VideoDecoder
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
