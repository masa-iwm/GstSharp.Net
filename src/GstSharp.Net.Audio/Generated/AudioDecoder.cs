// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// This base class is for audio decoders turning encoded data into
/// raw audio samples.
/// </summary>
/// <remarks>
/// <para>GstAudioDecoder and subclass should cooperate as follows.</para>
/// <para>## Configuration</para>
/// <para>
///   * Initially, GstAudioDecoder calls @start when the decoder element
///     is activated, which allows subclass to perform any global setup.
///     Base class (context) parameters can already be set according to subclass
///     capabilities (or possibly upon receive more information in subsequent
///     @set_format).
///   * GstAudioDecoder calls @set_format to inform subclass of the format
///     of input audio data that it is about to receive.
///     While unlikely, it might be called more than once, if changing input
///     parameters require reconfiguration.
///   * GstAudioDecoder calls @stop at end of all processing.
/// </para>
/// <para>
/// As of configuration stage, and throughout processing, GstAudioDecoder
/// provides various (context) parameters, e.g. describing the format of
/// output audio data (valid when output caps have been set) or current parsing state.
/// Conversely, subclass can and should configure context to inform
/// base class of its expectation w.r.t. buffer handling.
/// </para>
/// <para>
/// ## Data processing
///     * Base class gathers input data, and optionally allows subclass
///       to parse this into subsequently manageable (as defined by subclass)
///       chunks.  Such chunks are subsequently referred to as 'frames',
///       though they may or may not correspond to 1 (or more) audio format frame.
///     * Input frame is provided to subclass' @handle_frame.
///     * If codec processing results in decoded data, subclass should call
///       @gst_audio_decoder_finish_frame to have decoded data pushed
///       downstream.
///     * Just prior to actually pushing a buffer downstream,
///       it is passed to @pre_push.  Subclass should either use this callback
///       to arrange for additional downstream pushing or otherwise ensure such
///       custom pushing occurs after at least a method call has finished since
///       setting src pad caps.
///     * During the parsing process GstAudioDecoderClass will handle both
///       srcpad and sinkpad events. Sink events will be passed to subclass
///       if @event callback has been provided.
/// </para>
/// <para>## Shutdown phase</para>
/// <para>
///   * GstAudioDecoder class calls @stop to inform the subclass that data
///     parsing will be stopped.
/// </para>
/// <para>
/// Subclass is responsible for providing pad template caps for
/// source and sink pads. The pads need to be named "sink" and "src". It also
/// needs to set the fixed caps on srcpad, when the format is ensured.  This
/// is typically when base class calls subclass' @set_format function, though
/// it might be delayed until calling @gst_audio_decoder_finish_frame.
/// </para>
/// <para>
/// In summary, above process should have subclass concentrating on
/// codec data processing while leaving other matters to base class,
/// such as most notably timestamp handling.  While it may exert more control
/// in this area (see e.g. @pre_push), it is very much not recommended.
/// </para>
/// <para>
/// In particular, base class will try to arrange for perfect output timestamps
/// as much as possible while tracking upstream timestamps.
/// To this end, if deviation between the next ideal expected perfect timestamp
/// and upstream exceeds #GstAudioDecoder:tolerance, then resync to upstream
/// occurs (which would happen always if the tolerance mechanism is disabled).
/// </para>
/// <para>
/// In non-live pipelines, baseclass can also (configurably) arrange for
/// output buffer aggregation which may help to redue large(r) numbers of
/// small(er) buffers being pushed and processed downstream. Note that this
/// feature is only available if the buffer layout is interleaved. For planar
/// buffers, the decoder implementation is fully responsible for the output
/// buffer size.
/// </para>
/// <para>
/// On the other hand, it should be noted that baseclass only provides limited
/// seeking support (upon explicit subclass request), as full-fledged support
/// should rather be left to upstream demuxer, parser or alike.  This simple
/// approach caters for seeking and duration reporting using estimated input
/// bitrates.
/// </para>
/// <para>Things that subclass need to take care of:</para>
/// <para>
///   * Provide pad templates
///   * Set source pad caps when appropriate
///   * Set user-configurable properties to sane defaults for format and
///      implementing codec at hand, and convey some subclass capabilities and
///      expectations in context.
/// </para>
/// <para>
///   * Accept data in @handle_frame and provide encoded results to
///      @gst_audio_decoder_finish_frame.  If it is prepared to perform
///      PLC, it should also accept NULL data in @handle_frame and provide for
///      data for indicated duration.
/// </para>
/// </remarks>
public abstract unsafe partial class AudioDecoder : Gst.Element
{
    /// <summary>Wraps a native <c>GstAudioDecoder</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal AudioDecoder(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Helper function that allocates a buffer to hold an audio frame
    /// for @dec's current output format.
    /// </summary>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>allocated buffer</returns>
    public Gst.Buffer AllocateOutputBuffer(nuint size)
    {
        nint nativeResult = GstAudioDecoderAllocateOutputBuffer(Handle, size);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_decoder_allocate_output_buffer returned no value.");
    }

    /// <summary>The <c>gst_audio_decoder_get_audio_info</c> function.</summary>
    /// <returns>
    /// a #GstAudioInfo describing the input audio format
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Audio.AudioInfo GetAudioInfo()
    {
        nint nativeResult = GstAudioDecoderGetAudioInfo(Handle);
        System.GC.KeepAlive(this);
        return Gst.Audio.AudioInfo.FromNative(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_audio_decoder_get_audio_info returned no value.");
    }

    /// <summary>The <c>gst_audio_decoder_get_delay</c> function.</summary>
    /// <returns>currently configured decoder delay</returns>
    public int GetDelay()
    {
        int nativeResult = GstAudioDecoderGetDelay(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Queries decoder drain handling.</summary>
    /// <returns>TRUE if drainable handling is enabled.</returns>
    public bool GetDrainable()
    {
        int nativeResult = GstAudioDecoderGetDrainable(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_audio_decoder_get_estimate_rate</c> function.</summary>
    /// <returns>currently configured byte to time conversion setting</returns>
    public int GetEstimateRate()
    {
        int nativeResult = GstAudioDecoderGetEstimateRate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Sets the variables pointed to by @min and @max to the currently configured
    /// latency.
    /// </summary>
    /// <param name="min">The <c>min</c> argument.</param>
    /// <param name="max">The <c>max</c> argument.</param>
    public void GetLatency(out Gst.ClockTime min, out Gst.ClockTime max)
    {
        ulong minNative = default;
        ulong maxNative = default;
        GstAudioDecoderGetLatency(Handle, &minNative, &maxNative);
        System.GC.KeepAlive(this);
        min = new Gst.ClockTime(minNative);
        max = new Gst.ClockTime(maxNative);
    }

    /// <summary>The <c>gst_audio_decoder_get_max_errors</c> function.</summary>
    /// <returns>currently configured decoder tolerated error count.</returns>
    public int GetMaxErrors()
    {
        int nativeResult = GstAudioDecoderGetMaxErrors(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Queries decoder's latency aggregation.</summary>
    /// <returns>aggregation latency.</returns>
    public Gst.ClockTime GetMinLatency()
    {
        ulong nativeResult = GstAudioDecoderGetMinLatency(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Queries decoder required format handling.</summary>
    /// <returns>TRUE if required format handling is enabled.</returns>
    public bool GetNeedsFormat()
    {
        int nativeResult = GstAudioDecoderGetNeedsFormat(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Return current parsing (sync and eos) state.</summary>
    /// <param name="sync">The <c>sync</c> argument.</param>
    /// <param name="eos">The <c>eos</c> argument.</param>
    public void GetParseState(out bool sync, out bool eos)
    {
        int syncNative = default;
        int eosNative = default;
        GstAudioDecoderGetParseState(Handle, &syncNative, &eosNative);
        System.GC.KeepAlive(this);
        sync = syncNative != 0;
        eos = eosNative != 0;
    }

    /// <summary>Queries decoder packet loss concealment handling.</summary>
    /// <returns>TRUE if packet loss concealment is enabled.</returns>
    public bool GetPlc()
    {
        int nativeResult = GstAudioDecoderGetPlc(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_audio_decoder_get_plc_aware</c> function.</summary>
    /// <returns>currently configured plc handling</returns>
    public int GetPlcAware()
    {
        int nativeResult = GstAudioDecoderGetPlcAware(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Queries current audio jitter tolerance threshold.</summary>
    /// <returns>decoder audio jitter tolerance threshold.</returns>
    public Gst.ClockTime GetTolerance()
    {
        ulong nativeResult = GstAudioDecoderGetTolerance(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Sets the audio decoder tags and how they should be merged with any
    /// upstream stream tags. This will override any tags previously-set
    /// with gst_audio_decoder_merge_tags().
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that this is provided for convenience, and the subclass is
    /// not required to use this and can still do tag handling on its own.
    /// </para>
    /// </remarks>
    /// <param name="tags">The <c>tags</c> argument.</param>
    /// <param name="mode">The <c>mode</c> argument.</param>
    public void MergeTags(Gst.TagList? tags, Gst.TagMergeMode mode)
    {
        GstAudioDecoderMergeTags(Handle, tags is null ? 0 : tags.Handle, (int)mode);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(tags);
    }

    /// <summary>
    /// Negotiate with downstream elements to currently configured #GstAudioInfo.
    /// Unmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if
    /// negotiate fails.
    /// </summary>
    /// <returns>%TRUE if the negotiation succeeded, else %FALSE.</returns>
    public bool Negotiate()
    {
        int nativeResult = GstAudioDecoderNegotiate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns caps that express @caps (or sink template caps if @caps == NULL)
    /// restricted to rate/channels/... combinations supported by downstream
    /// elements.
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <param name="filter">The <c>filter</c> argument.</param>
    /// <returns>a #GstCaps owned by caller</returns>
    public Gst.Caps ProxyGetcaps(Gst.Caps? caps, Gst.Caps? filter)
    {
        nint nativeResult = GstAudioDecoderProxyGetcaps(Handle, caps is null ? 0 : caps.Handle, filter is null ? 0 : filter.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        System.GC.KeepAlive(filter);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_decoder_proxy_getcaps returned no value.");
    }

    /// <summary>
    /// Sets a caps in allocation query which are different from the set
    /// pad's caps. Use this function before calling
    /// gst_audio_decoder_negotiate(). Setting to %NULL the allocation
    /// query will use the caps from the pad.
    /// </summary>
    /// <param name="allocationCaps">The <c>allocationCaps</c> argument.</param>
    public void SetAllocationCaps(Gst.Caps? allocationCaps)
    {
        GstAudioDecoderSetAllocationCaps(Handle, allocationCaps is null ? 0 : allocationCaps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(allocationCaps);
    }

    /// <summary>
    /// Configures decoder drain handling.  If drainable, subclass might
    /// be handed a NULL buffer to have it return any leftover decoded data.
    /// Otherwise, it is not considered so capable and will only ever be passed
    /// real data.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetDrainable(bool enabled)
    {
        GstAudioDecoderSetDrainable(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Allows baseclass to perform byte to time estimated conversion.</summary>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetEstimateRate(bool enabled)
    {
        GstAudioDecoderSetEstimateRate(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets decoder latency. If the provided values changed from
    /// previously provided ones, this will also post a LATENCY message on the bus
    /// so the pipeline can reconfigure its global latency.
    /// </summary>
    /// <param name="min">The <c>min</c> argument.</param>
    /// <param name="max">The <c>max</c> argument.</param>
    public void SetLatency(Gst.ClockTime min, Gst.ClockTime max)
    {
        GstAudioDecoderSetLatency(Handle, min.Nanoseconds, max.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets numbers of tolerated decoder errors, where a tolerated one is then only
    /// warned about, but more than tolerated will lead to fatal error. You can set
    /// -1 for never returning fatal errors. Default is set to
    /// GST_AUDIO_DECODER_MAX_ERRORS.
    /// </summary>
    /// <param name="num">The <c>num</c> argument.</param>
    public void SetMaxErrors(int num)
    {
        GstAudioDecoderSetMaxErrors(Handle, num);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets decoder minimum aggregation latency.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="num">The <c>num</c> argument.</param>
    public void SetMinLatency(Gst.ClockTime num)
    {
        GstAudioDecoderSetMinLatency(Handle, num.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configures decoder format needs.  If enabled, subclass needs to be
    /// negotiated with format caps before it can process any data.  It will then
    /// never be handed any data before it has been configured.
    /// Otherwise, it might be handed data without having been configured and
    /// is then expected being able to do so either by default
    /// or based on the input data.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetNeedsFormat(bool enabled)
    {
        GstAudioDecoderSetNeedsFormat(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configure output caps on the srcpad of @dec. Similar to
    /// gst_audio_decoder_set_output_format(), but allows subclasses to specify
    /// output caps that can't be expressed via #GstAudioInfo e.g. caps that have
    /// caps features.
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>%TRUE on success.</returns>
    public bool SetOutputCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstAudioDecoderSetOutputCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>Configure output info on the srcpad of @dec.</summary>
    /// <param name="info">The <c>info</c> argument.</param>
    /// <returns>%TRUE on success.</returns>
    public bool SetOutputFormat(Gst.Audio.AudioInfo info)
    {
        ArgumentNullException.ThrowIfNull(info);
        int nativeResult = GstAudioDecoderSetOutputFormat(Handle, info.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(info);
        return nativeResult != 0;
    }

    /// <summary>
    /// Enable or disable decoder packet loss concealment, provided subclass
    /// and codec are capable and allow handling plc.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetPlc(bool enabled)
    {
        GstAudioDecoderSetPlc(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Indicates whether or not subclass handles packet loss concealment (plc).</summary>
    /// <param name="plc">The <c>plc</c> argument.</param>
    public void SetPlcAware(bool plc)
    {
        GstAudioDecoderSetPlcAware(Handle, plc ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Configures decoder audio jitter tolerance threshold.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="tolerance">The <c>tolerance</c> argument.</param>
    public void SetTolerance(Gst.ClockTime tolerance)
    {
        GstAudioDecoderSetTolerance(Handle, tolerance.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Lets #GstAudioDecoder sub-classes decide if they want the sink pad
    /// to use the default pad query handler to reply to accept-caps queries.
    /// </summary>
    /// <remarks>
    /// <para>
    /// By setting this to true it is possible to further customize the default
    /// handler with %GST_PAD_SET_ACCEPT_INTERSECT and
    /// %GST_PAD_SET_ACCEPT_TEMPLATE
    /// </para>
    /// </remarks>
    /// <param name="use">The <c>use</c> argument.</param>
    public void SetUseDefaultPadAcceptcaps(bool use)
    {
        GstAudioDecoderSetUseDefaultPadAcceptcaps(Handle, use ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Maximum number of tolerated consecutive decode errors. See
    /// gst_audio_decoder_set_max_errors() for more details.
    /// </summary>
    public int MaxErrors
    {
        get => GetMaxErrors();
        set => SetMaxErrors(value);
    }

    /// <summary>The <c>min-latency</c> property.</summary>
    public Gst.ClockTime MinLatency
    {
        get => GetMinLatency();
        set => SetMinLatency(value);
    }

    /// <summary>The <c>plc</c> property.</summary>
    public bool Plc
    {
        get => GetPlc();
        set => SetPlc(value);
    }

    /// <summary>The <c>tolerance</c> property.</summary>
    public Gst.ClockTime Tolerance
    {
        get => GetTolerance();
        set => SetTolerance(value);
    }

    /// <summary>The <c>gst_audio_decoder_allocate_output_buffer</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_allocate_output_buffer")]
    private static partial nint GstAudioDecoderAllocateOutputBuffer(nint dec, nuint size);

    /// <summary>The <c>gst_audio_decoder_get_audio_info</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_audio_info")]
    private static partial nint GstAudioDecoderGetAudioInfo(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_delay</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_delay")]
    private static partial int GstAudioDecoderGetDelay(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_drainable</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_drainable")]
    private static partial int GstAudioDecoderGetDrainable(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_estimate_rate</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_estimate_rate")]
    private static partial int GstAudioDecoderGetEstimateRate(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_latency</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_latency")]
    private static partial void GstAudioDecoderGetLatency(nint dec, ulong* min, ulong* max);

    /// <summary>The <c>gst_audio_decoder_get_max_errors</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_max_errors")]
    private static partial int GstAudioDecoderGetMaxErrors(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_min_latency</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_min_latency")]
    private static partial ulong GstAudioDecoderGetMinLatency(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_needs_format</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_needs_format")]
    private static partial int GstAudioDecoderGetNeedsFormat(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_parse_state</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_parse_state")]
    private static partial void GstAudioDecoderGetParseState(nint dec, int* sync, int* eos);

    /// <summary>The <c>gst_audio_decoder_get_plc</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_plc")]
    private static partial int GstAudioDecoderGetPlc(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_plc_aware</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_plc_aware")]
    private static partial int GstAudioDecoderGetPlcAware(nint dec);

    /// <summary>The <c>gst_audio_decoder_get_tolerance</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_tolerance")]
    private static partial ulong GstAudioDecoderGetTolerance(nint dec);

    /// <summary>The <c>gst_audio_decoder_merge_tags</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_merge_tags")]
    private static partial void GstAudioDecoderMergeTags(nint dec, nint tags, int mode);

    /// <summary>The <c>gst_audio_decoder_negotiate</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_negotiate")]
    private static partial int GstAudioDecoderNegotiate(nint dec);

    /// <summary>The <c>gst_audio_decoder_proxy_getcaps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_proxy_getcaps")]
    private static partial nint GstAudioDecoderProxyGetcaps(nint decoder, nint caps, nint filter);

    /// <summary>The <c>gst_audio_decoder_set_allocation_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_allocation_caps")]
    private static partial void GstAudioDecoderSetAllocationCaps(nint dec, nint allocationCaps);

    /// <summary>The <c>gst_audio_decoder_set_drainable</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_drainable")]
    private static partial void GstAudioDecoderSetDrainable(nint dec, int enabled);

    /// <summary>The <c>gst_audio_decoder_set_estimate_rate</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_estimate_rate")]
    private static partial void GstAudioDecoderSetEstimateRate(nint dec, int enabled);

    /// <summary>The <c>gst_audio_decoder_set_latency</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_latency")]
    private static partial void GstAudioDecoderSetLatency(nint dec, ulong min, ulong max);

    /// <summary>The <c>gst_audio_decoder_set_max_errors</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_max_errors")]
    private static partial void GstAudioDecoderSetMaxErrors(nint dec, int num);

    /// <summary>The <c>gst_audio_decoder_set_min_latency</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_min_latency")]
    private static partial void GstAudioDecoderSetMinLatency(nint dec, ulong num);

    /// <summary>The <c>gst_audio_decoder_set_needs_format</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_needs_format")]
    private static partial void GstAudioDecoderSetNeedsFormat(nint dec, int enabled);

    /// <summary>The <c>gst_audio_decoder_set_output_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_output_caps")]
    private static partial int GstAudioDecoderSetOutputCaps(nint dec, nint caps);

    /// <summary>The <c>gst_audio_decoder_set_output_format</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_output_format")]
    private static partial int GstAudioDecoderSetOutputFormat(nint dec, nint info);

    /// <summary>The <c>gst_audio_decoder_set_plc</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_plc")]
    private static partial void GstAudioDecoderSetPlc(nint dec, int enabled);

    /// <summary>The <c>gst_audio_decoder_set_plc_aware</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_plc_aware")]
    private static partial void GstAudioDecoderSetPlcAware(nint dec, int plc);

    /// <summary>The <c>gst_audio_decoder_set_tolerance</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_tolerance")]
    private static partial void GstAudioDecoderSetTolerance(nint dec, ulong tolerance);

    /// <summary>The <c>gst_audio_decoder_set_use_default_pad_acceptcaps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_set_use_default_pad_acceptcaps")]
    private static partial void GstAudioDecoderSetUseDefaultPadAcceptcaps(nint decoder, int use);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioDecoder</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_decoder_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstAudioDecoder</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : AudioDecoder
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
