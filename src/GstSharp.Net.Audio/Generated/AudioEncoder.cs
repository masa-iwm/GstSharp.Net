// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// This base class is for audio encoders turning raw audio samples into
/// encoded audio data.
/// </summary>
/// <remarks>
/// <para>GstAudioEncoder and subclass should cooperate as follows.</para>
/// <para>## Configuration</para>
/// <para>
///   * Initially, GstAudioEncoder calls @start when the encoder element
///     is activated, which allows subclass to perform any global setup.
/// </para>
/// <para>
///   * GstAudioEncoder calls @set_format to inform subclass of the format
///     of input audio data that it is about to receive.  Subclass should
///     setup for encoding and configure various base class parameters
///     appropriately, notably those directing desired input data handling.
///     While unlikely, it might be called more than once, if changing input
///     parameters require reconfiguration.
/// </para>
/// <para>  * GstAudioEncoder calls @stop at end of all processing.</para>
/// <para>
/// As of configuration stage, and throughout processing, GstAudioEncoder
/// maintains various parameters that provide required context,
/// e.g. describing the format of input audio data.
/// Conversely, subclass can and should configure these context parameters
/// to inform base class of its expectation w.r.t. buffer handling.
/// </para>
/// <para>## Data processing</para>
/// <para>
///     * Base class gathers input sample data (as directed by the context's
///       frame_samples and frame_max) and provides this to subclass' @handle_frame.
///     * If codec processing results in encoded data, subclass should call
///       gst_audio_encoder_finish_frame() to have encoded data pushed
///       downstream. Alternatively, it might also call
///       gst_audio_encoder_finish_frame() (with a NULL buffer and some number of
///       dropped samples) to indicate dropped (non-encoded) samples.
///     * Just prior to actually pushing a buffer downstream,
///       it is passed to @pre_push.
///     * During the parsing process GstAudioEncoderClass will handle both
///       srcpad and sinkpad events. Sink events will be passed to subclass
///       if @event callback has been provided.
/// </para>
/// <para>## Shutdown phase</para>
/// <para>
///   * GstAudioEncoder class calls @stop to inform the subclass that data
///     parsing will be stopped.
/// </para>
/// <para>
/// Subclass is responsible for providing pad template caps for
/// source and sink pads. The pads need to be named "sink" and "src". It also
/// needs to set the fixed caps on srcpad, when the format is ensured.  This
/// is typically when base class calls subclass' @set_format function, though
/// it might be delayed until calling @gst_audio_encoder_finish_frame.
/// </para>
/// <para>
/// In summary, above process should have subclass concentrating on
/// codec data processing while leaving other matters to base class,
/// such as most notably timestamp handling.  While it may exert more control
/// in this area (see e.g. @pre_push), it is very much not recommended.
/// </para>
/// <para>
/// In particular, base class will either favor tracking upstream timestamps
/// (at the possible expense of jitter) or aim to arrange for a perfect stream of
/// output timestamps, depending on #GstAudioEncoder:perfect-timestamp.
/// However, in the latter case, the input may not be so perfect or ideal, which
/// is handled as follows.  An input timestamp is compared with the expected
/// timestamp as dictated by input sample stream and if the deviation is less
/// than #GstAudioEncoder:tolerance, the deviation is discarded.
/// Otherwise, it is considered a discontuinity and subsequent output timestamp
/// is resynced to the new position after performing configured discontinuity
/// processing.  In the non-perfect-timestamp case, an upstream variation
/// exceeding tolerance only leads to marking DISCONT on subsequent outgoing
/// (while timestamps are adjusted to upstream regardless of variation).
/// While DISCONT is also marked in the perfect-timestamp case, this one
/// optionally (see #GstAudioEncoder:hard-resync)
/// performs some additional steps, such as clipping of (early) input samples
/// or draining all currently remaining input data, depending on the direction
/// of the discontuinity.
/// </para>
/// <para>
/// If perfect timestamps are arranged, it is also possible to request baseclass
/// (usually set by subclass) to provide additional buffer metadata (in OFFSET
/// and OFFSET_END) fields according to granule defined semantics currently
/// needed by oggmux.  Specifically, OFFSET is set to granulepos (= sample count
/// including buffer) and OFFSET_END to corresponding timestamp (as determined
/// by same sample count and sample rate).
/// </para>
/// <para>Things that subclass need to take care of:</para>
/// <para>
///   * Provide pad templates
///   * Set source pad caps when appropriate
///   * Inform base class of buffer processing needs using context's
///      frame_samples and frame_bytes.
///   * Set user-configurable properties to sane defaults for format and
///      implementing codec at hand, e.g. those controlling timestamp behaviour
///      and discontinuity processing.
///   * Accept data in @handle_frame and provide encoded results to
///      gst_audio_encoder_finish_frame().
/// </para>
/// </remarks>
public abstract unsafe partial class AudioEncoder : Gst.Element, Gst.IPreset
{
    /// <summary>Wraps a native <c>GstAudioEncoder</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstAudioEncoder</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected AudioEncoder(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Helper function that allocates a buffer to hold an encoded audio frame
    /// for @enc's current output format.
    /// </summary>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>allocated buffer</returns>
    public Gst.Buffer AllocateOutputBuffer(nuint size)
    {
        nint nativeResult = GstAudioEncoderAllocateOutputBuffer(Handle, size);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_encoder_allocate_output_buffer returned no value.");
    }

    /// <summary>
    /// Collects encoded data and pushes encoded data downstream.
    /// Source pad caps must be set when this is called.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @samples &lt; 0, then best estimate is all samples provided to encoder
    /// (subclass) so far.  @buf may be NULL, in which case next number of @samples
    /// are considered discarded, e.g. as a result of discontinuous transmission,
    /// and a discontinuity is marked.
    /// </para>
    /// <para>
    /// Note that samples received in #GstAudioEncoderClass.handle_frame()
    /// may be invalidated by a call to this function.
    /// </para>
    /// <para>
    /// The <c>buffer</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="buffer">
    /// The <c>buffer</c> argument.
    /// The call consumes it: <paramref name="buffer"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// It may be <see langword="null"/>, which is the absence of a payload and leaves
    /// nothing to consume.
    /// </param>
    /// <param name="samples">The <c>samples</c> argument.</param>
    /// <returns>a #GstFlowReturn that should be escalated to caller (of caller)</returns>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="buffer"/> was disposed.
    /// </exception>
    public Gst.FlowReturn FinishFrame(Gst.Buffer? buffer, int samples)
    {
        nint instanceHandle = Handle;
        nint bufferNative = buffer is null ? 0 : buffer.Handle;
        nint bufferOwned = buffer is null ? 0 : Gst.GstNative.MiniObjectRef(bufferNative);
        int nativeResult = GstAudioEncoderFinishFrame(instanceHandle, bufferOwned, samples);
        System.GC.KeepAlive(this);
        buffer?.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Lets #GstAudioEncoder sub-classes to know the memory @allocator
    /// used by the base class and its @params.
    /// </summary>
    /// <remarks>
    /// <para>Unref the @allocator after use it.</para>
    /// </remarks>
    /// <param name="allocator">The <c>allocator</c> argument.</param>
    /// <param name="params">
    /// The <c>@params</c> argument.
    /// The binding allocates the storage; on return the caller owns
    /// <paramref name="params"/> and disposes it.
    /// </param>
    public void GetAllocator(out Gst.Allocator? allocator, out Gst.AllocationParams @params)
    {
        nint instanceHandle = Handle;
        nint allocatorNative = default;
        nint @paramsNative = GstAllocationParamsNew();
        GstAudioEncoderGetAllocator(instanceHandle, &allocatorNative, @paramsNative);
        System.GC.KeepAlive(this);
        @params = Gst.AllocationParams.FromNative(@paramsNative, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_allocation_params_new returned no value.");
        allocator = Gst.GObject.Object.FromNative<Gst.Allocator>(allocatorNative, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_audio_encoder_get_audio_info</c> function.</summary>
    /// <returns>
    /// a #GstAudioInfo describing the input audio format
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Audio.AudioInfo GetAudioInfo()
    {
        nint nativeResult = GstAudioEncoderGetAudioInfo(Handle);
        System.GC.KeepAlive(this);
        return Gst.Audio.AudioInfo.FromNative(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_audio_encoder_get_audio_info returned no value.");
    }

    /// <summary>Queries encoder drain handling.</summary>
    /// <returns>TRUE if drainable handling is enabled.</returns>
    public bool GetDrainable()
    {
        int nativeResult = GstAudioEncoderGetDrainable(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_audio_encoder_get_frame_max</c> function.</summary>
    /// <returns>currently configured maximum handled frames</returns>
    public int GetFrameMax()
    {
        int nativeResult = GstAudioEncoderGetFrameMax(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>The <c>gst_audio_encoder_get_frame_samples_max</c> function.</summary>
    /// <returns>currently maximum requested samples per frame</returns>
    public int GetFrameSamplesMax()
    {
        int nativeResult = GstAudioEncoderGetFrameSamplesMax(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>The <c>gst_audio_encoder_get_frame_samples_min</c> function.</summary>
    /// <returns>currently minimum requested samples per frame</returns>
    public int GetFrameSamplesMin()
    {
        int nativeResult = GstAudioEncoderGetFrameSamplesMin(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Queries encoder hard minimum handling.</summary>
    /// <returns>TRUE if hard minimum handling is enabled.</returns>
    public bool GetHardMin()
    {
        int nativeResult = GstAudioEncoderGetHardMin(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_audio_encoder_get_hard_resync</c> function.</summary>
    /// <returns>The result of <c>gst_audio_encoder_get_hard_resync</c>.</returns>
    public bool GetHardResync()
    {
        int nativeResult = GstAudioEncoderGetHardResync(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets the variables pointed to by @min and @max to the currently configured
    /// latency.
    /// </summary>
    /// <param name="min">The <c>min</c> argument.</param>
    /// <param name="max">The <c>max</c> argument.</param>
    public void GetLatency(out Gst.ClockTime min, out Gst.ClockTime max)
    {
        ulong minNative = default;
        ulong maxNative = default;
        GstAudioEncoderGetLatency(Handle, &minNative, &maxNative);
        System.GC.KeepAlive(this);
        min = new Gst.ClockTime(minNative);
        max = new Gst.ClockTime(maxNative);
    }

    /// <summary>The <c>gst_audio_encoder_get_lookahead</c> function.</summary>
    /// <returns>currently configured encoder lookahead</returns>
    public int GetLookahead()
    {
        int nativeResult = GstAudioEncoderGetLookahead(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Queries if the encoder will handle granule marking.</summary>
    /// <returns>TRUE if granule marking is enabled.</returns>
    public bool GetMarkGranule()
    {
        int nativeResult = GstAudioEncoderGetMarkGranule(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Queries encoder perfect timestamp behaviour.</summary>
    /// <returns>TRUE if perfect timestamp setting enabled.</returns>
    public bool GetPerfectTimestamp()
    {
        int nativeResult = GstAudioEncoderGetPerfectTimestamp(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Queries current audio jitter tolerance threshold.</summary>
    /// <returns>encoder audio jitter tolerance threshold.</returns>
    public Gst.ClockTime GetTolerance()
    {
        ulong nativeResult = GstAudioEncoderGetTolerance(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Sets the audio encoder tags and how they should be merged with any
    /// upstream stream tags. This will override any tags previously-set
    /// with gst_audio_encoder_merge_tags().
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that this is provided for convenience, and the subclass is
    /// not required to use this and can still do tag handling on its own.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="tags">The <c>tags</c> argument.</param>
    /// <param name="mode">The <c>mode</c> argument.</param>
    public void MergeTags(Gst.TagList? tags, Gst.TagMergeMode mode)
    {
        GstAudioEncoderMergeTags(Handle, tags is null ? 0 : tags.Handle, (int)mode);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(tags);
    }

    /// <summary>
    /// Negotiate with downstream elements to currently configured #GstCaps.
    /// Unmark GST_PAD_FLAG_NEED_RECONFIGURE in any case. But mark it again if
    /// negotiate fails.
    /// </summary>
    /// <returns>%TRUE if the negotiation succeeded, else %FALSE.</returns>
    public bool Negotiate()
    {
        int nativeResult = GstAudioEncoderNegotiate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns caps that express @caps (or sink template caps if @caps == NULL)
    /// restricted to channel/rate combinations supported by downstream elements
    /// (e.g. muxers).
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <param name="filter">The <c>filter</c> argument.</param>
    /// <returns>a #GstCaps owned by caller</returns>
    public Gst.Caps ProxyGetcaps(Gst.Caps? caps, Gst.Caps? filter)
    {
        nint nativeResult = GstAudioEncoderProxyGetcaps(Handle, caps is null ? 0 : caps.Handle, filter is null ? 0 : filter.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        System.GC.KeepAlive(filter);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_encoder_proxy_getcaps returned no value.");
    }

    /// <summary>
    /// Sets a caps in allocation query which are different from the set
    /// pad's caps. Use this function before calling
    /// gst_audio_encoder_negotiate(). Setting to %NULL the allocation
    /// query will use the caps from the pad.
    /// </summary>
    /// <param name="allocationCaps">The <c>allocationCaps</c> argument.</param>
    public void SetAllocationCaps(Gst.Caps? allocationCaps)
    {
        GstAudioEncoderSetAllocationCaps(Handle, allocationCaps is null ? 0 : allocationCaps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(allocationCaps);
    }

    /// <summary>
    /// Configures encoder drain handling.  If drainable, subclass might
    /// be handed a NULL buffer to have it return any leftover encoded data.
    /// Otherwise, it is not considered so capable and will only ever be passed
    /// real data.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetDrainable(bool enabled)
    {
        GstAudioEncoderSetDrainable(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets max number of frames accepted at once (assumed minimally 1).
    /// Requires @frame_samples_min and @frame_samples_max to be the equal.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note: This value will be reset to 0 every time before
    /// #GstAudioEncoderClass.set_format() is called.
    /// </para>
    /// </remarks>
    /// <param name="num">The <c>num</c> argument.</param>
    public void SetFrameMax(int num)
    {
        GstAudioEncoderSetFrameMax(Handle, num);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets number of samples (per channel) subclass needs to be handed,
    /// at most or will be handed all available if 0.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If an exact number of samples is required, gst_audio_encoder_set_frame_samples_min()
    /// must be called with the same number.
    /// </para>
    /// <para>
    /// Note: This value will be reset to 0 every time before
    /// #GstAudioEncoderClass.set_format() is called.
    /// </para>
    /// </remarks>
    /// <param name="num">The <c>num</c> argument.</param>
    public void SetFrameSamplesMax(int num)
    {
        GstAudioEncoderSetFrameSamplesMax(Handle, num);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets number of samples (per channel) subclass needs to be handed,
    /// at least or will be handed all available if 0.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If an exact number of samples is required, gst_audio_encoder_set_frame_samples_max()
    /// must be called with the same number.
    /// </para>
    /// <para>
    /// Note: This value will be reset to 0 every time before
    /// #GstAudioEncoderClass.set_format() is called.
    /// </para>
    /// </remarks>
    /// <param name="num">The <c>num</c> argument.</param>
    public void SetFrameSamplesMin(int num)
    {
        GstAudioEncoderSetFrameSamplesMin(Handle, num);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configures encoder hard minimum handling.  If enabled, subclass
    /// will never be handed less samples than it configured, which otherwise
    /// might occur near end-of-data handling.  Instead, the leftover samples
    /// will simply be discarded.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetHardMin(bool enabled)
    {
        GstAudioEncoderSetHardMin(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_audio_encoder_set_hard_resync</c> function.</summary>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetHardResync(bool enabled)
    {
        GstAudioEncoderSetHardResync(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Set the codec headers to be sent downstream whenever requested.</summary>
    /// <param name="headers">
    /// The <c>headers</c> argument.
    /// The call takes the list over. The binding hands it a native list of its own
    /// and one reference per element, and releases neither afterwards - the callee
    /// owns both from the moment the call is made, including when it answers false.
    /// Your own objects keep their references and stay usable.
    /// </param>
    public void SetHeaders(System.Collections.Generic.IEnumerable<Gst.Buffer>? headers)
    {
        nint instanceHandle = Handle;
        nint headersOwned = Gst.Interop.GMarshal.ConsumeList(headers, singly: false);
        GstAudioEncoderSetHeaders(instanceHandle, headersOwned);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets encoder latency. If the provided values changed from
    /// previously provided ones, this will also post a LATENCY message on the bus
    /// so the pipeline can reconfigure its global latency.
    /// </summary>
    /// <param name="min">The <c>min</c> argument.</param>
    /// <param name="max">The <c>max</c> argument.</param>
    public void SetLatency(Gst.ClockTime min, Gst.ClockTime max)
    {
        GstAudioEncoderSetLatency(Handle, min.Nanoseconds, max.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets encoder lookahead (in units of input rate samples)</summary>
    /// <remarks>
    /// <para>
    /// Note: This value will be reset to 0 every time before
    /// #GstAudioEncoderClass.set_format() is called.
    /// </para>
    /// </remarks>
    /// <param name="num">The <c>num</c> argument.</param>
    public void SetLookahead(int num)
    {
        GstAudioEncoderSetLookahead(Handle, num);
        System.GC.KeepAlive(this);
    }

    /// <summary>Enable or disable encoder granule handling.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetMarkGranule(bool enabled)
    {
        GstAudioEncoderSetMarkGranule(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Configure output caps on the srcpad of @enc.</summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>%TRUE on success.</returns>
    public bool SetOutputFormat(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstAudioEncoderSetOutputFormat(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>Enable or disable encoder perfect output timestamp preference.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetPerfectTimestamp(bool enabled)
    {
        GstAudioEncoderSetPerfectTimestamp(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Configures encoder audio jitter tolerance threshold.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="tolerance">The <c>tolerance</c> argument.</param>
    public void SetTolerance(Gst.ClockTime tolerance)
    {
        GstAudioEncoderSetTolerance(Handle, tolerance.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>hard-resync</c> property.</summary>
    public bool HardResync
    {
        get => GetHardResync();
        set => SetHardResync(value);
    }

    /// <summary>The <c>mark-granule</c> property.</summary>
    public bool MarkGranule => GetMarkGranule();

    /// <summary>The <c>perfect-timestamp</c> property.</summary>
    public bool PerfectTimestamp
    {
        get => GetPerfectTimestamp();
        set => SetPerfectTimestamp(value);
    }

    /// <summary>The <c>tolerance</c> property.</summary>
    public Gst.ClockTime Tolerance
    {
        get => GetTolerance();
        set => SetTolerance(value);
    }

    /// <summary>The <c>gst_audio_encoder_allocate_output_buffer</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_allocate_output_buffer")]
    private static partial nint GstAudioEncoderAllocateOutputBuffer(nint enc, nuint size);

    /// <summary>The <c>gst_audio_encoder_finish_frame</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_finish_frame")]
    private static partial int GstAudioEncoderFinishFrame(nint enc, nint buffer, int samples);

    /// <summary>The <c>gst_audio_encoder_get_allocator</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_allocator")]
    private static partial void GstAudioEncoderGetAllocator(nint enc, nint* allocator, nint @params);

    /// <summary>The <c>gst_audio_encoder_get_audio_info</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_audio_info")]
    private static partial nint GstAudioEncoderGetAudioInfo(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_drainable</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_drainable")]
    private static partial int GstAudioEncoderGetDrainable(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_frame_max</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_frame_max")]
    private static partial int GstAudioEncoderGetFrameMax(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_frame_samples_max</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_frame_samples_max")]
    private static partial int GstAudioEncoderGetFrameSamplesMax(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_frame_samples_min</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_frame_samples_min")]
    private static partial int GstAudioEncoderGetFrameSamplesMin(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_hard_min</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_hard_min")]
    private static partial int GstAudioEncoderGetHardMin(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_hard_resync</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_hard_resync")]
    private static partial int GstAudioEncoderGetHardResync(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_latency</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_latency")]
    private static partial void GstAudioEncoderGetLatency(nint enc, ulong* min, ulong* max);

    /// <summary>The <c>gst_audio_encoder_get_lookahead</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_lookahead")]
    private static partial int GstAudioEncoderGetLookahead(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_mark_granule</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_mark_granule")]
    private static partial int GstAudioEncoderGetMarkGranule(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_perfect_timestamp</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_perfect_timestamp")]
    private static partial int GstAudioEncoderGetPerfectTimestamp(nint enc);

    /// <summary>The <c>gst_audio_encoder_get_tolerance</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_tolerance")]
    private static partial ulong GstAudioEncoderGetTolerance(nint enc);

    /// <summary>The <c>gst_audio_encoder_merge_tags</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_merge_tags")]
    private static partial void GstAudioEncoderMergeTags(nint enc, nint tags, int mode);

    /// <summary>The <c>gst_audio_encoder_negotiate</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_negotiate")]
    private static partial int GstAudioEncoderNegotiate(nint enc);

    /// <summary>The <c>gst_audio_encoder_proxy_getcaps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_proxy_getcaps")]
    private static partial nint GstAudioEncoderProxyGetcaps(nint enc, nint caps, nint filter);

    /// <summary>The <c>gst_audio_encoder_set_allocation_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_allocation_caps")]
    private static partial void GstAudioEncoderSetAllocationCaps(nint enc, nint allocationCaps);

    /// <summary>The <c>gst_audio_encoder_set_drainable</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_drainable")]
    private static partial void GstAudioEncoderSetDrainable(nint enc, int enabled);

    /// <summary>The <c>gst_audio_encoder_set_frame_max</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_frame_max")]
    private static partial void GstAudioEncoderSetFrameMax(nint enc, int num);

    /// <summary>The <c>gst_audio_encoder_set_frame_samples_max</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_frame_samples_max")]
    private static partial void GstAudioEncoderSetFrameSamplesMax(nint enc, int num);

    /// <summary>The <c>gst_audio_encoder_set_frame_samples_min</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_frame_samples_min")]
    private static partial void GstAudioEncoderSetFrameSamplesMin(nint enc, int num);

    /// <summary>The <c>gst_audio_encoder_set_hard_min</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_hard_min")]
    private static partial void GstAudioEncoderSetHardMin(nint enc, int enabled);

    /// <summary>The <c>gst_audio_encoder_set_hard_resync</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_hard_resync")]
    private static partial void GstAudioEncoderSetHardResync(nint enc, int enabled);

    /// <summary>The <c>gst_audio_encoder_set_headers</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_headers")]
    private static partial void GstAudioEncoderSetHeaders(nint enc, nint headers);

    /// <summary>The <c>gst_audio_encoder_set_latency</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_latency")]
    private static partial void GstAudioEncoderSetLatency(nint enc, ulong min, ulong max);

    /// <summary>The <c>gst_audio_encoder_set_lookahead</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_lookahead")]
    private static partial void GstAudioEncoderSetLookahead(nint enc, int num);

    /// <summary>The <c>gst_audio_encoder_set_mark_granule</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_mark_granule")]
    private static partial void GstAudioEncoderSetMarkGranule(nint enc, int enabled);

    /// <summary>The <c>gst_audio_encoder_set_output_format</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_output_format")]
    private static partial int GstAudioEncoderSetOutputFormat(nint enc, nint caps);

    /// <summary>The <c>gst_audio_encoder_set_perfect_timestamp</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_perfect_timestamp")]
    private static partial void GstAudioEncoderSetPerfectTimestamp(nint enc, int enabled);

    /// <summary>The <c>gst_audio_encoder_set_tolerance</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_set_tolerance")]
    private static partial void GstAudioEncoderSetTolerance(nint enc, ulong tolerance);

    /// <summary>The <c>gst_allocation_params_new</c> entry point, which allocates the storage of a caller allocated out parameter.</summary>
    /// <returns>A new, zeroed instance the caller owns.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_allocation_params_new")]
    private static partial nint GstAllocationParamsNew();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioEncoder</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_encoder_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstAudioEncoder</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : AudioEncoder
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
