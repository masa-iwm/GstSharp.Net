// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// Information describing audio properties. This information can be filled
/// in from GstCaps with gst_audio_info_from_caps().
/// </summary>
/// <remarks>
/// <para>Use the provided macros to access the info in this structure.</para>
/// </remarks>
public sealed unsafe partial class AudioInfo : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstAudioInfo</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal AudioInfo(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a <c>GstAudioInfo</c> that GStreamer keeps owning, for the length of one call.</summary>
    /// <param name="handle">The value that is lent to managed code.</param>
    /// <returns>The wrapper, which holds nothing of its own.</returns>
    /// <remarks>
    /// This is how the override of a virtual method receives a <c>transfer none</c>
    /// boxed value. No <c>g_boxed_copy</c> is taken, so every write the override
    /// makes lands in the instance the caller of the slot reads back, and disposing
    /// the wrapper only detaches it: a wrapper kept past the call throws instead of
    /// freeing a value it never owned. See <see cref="Gst.Interop.Borrowed"/>.
    /// </remarks>
    internal static AudioInfo Borrow(nint handle) => new(new Gst.Interop.Borrowed(handle));

    /// <summary>Wraps a <c>GstAudioInfo</c> the caller keeps owning.</summary>
    /// <param name="borrowed">The value that is lent to managed code.</param>
    private AudioInfo(Gst.Interop.Borrowed borrowed)
        : base(borrowed, new Gst.GObject.GType(GetGType()))
    {
    }

    /// <summary>additional audio flags</summary>
    public Gst.Audio.AudioFlags Flags
    {
        get
        {
            Gst.Audio.AudioFlags value = ((AudioInfoRaw*)Handle)->Flags;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>audio layout</summary>
    public Gst.Audio.AudioLayout Layout
    {
        get
        {
            Gst.Audio.AudioLayout value = ((AudioInfoRaw*)Handle)->Layout;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>the audio sample rate</summary>
    public int Rate
    {
        get
        {
            int value = ((AudioInfoRaw*)Handle)->Rate;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>the number of channels</summary>
    public int Channels
    {
        get
        {
            int value = ((AudioInfoRaw*)Handle)->Channels;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>
    /// the number of bytes for one frame, this is the size of one
    ///         sample * @channels
    /// </summary>
    public int Bpf
    {
        get
        {
            int value = ((AudioInfoRaw*)Handle)->Bpf;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>the positions for each channel</summary>
    public Gst.Audio.AudioInfo.PositionArray Position
    {
        get
        {
            Gst.Audio.AudioInfo.PositionArray value = ((AudioInfoRaw*)Handle)->Position;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>Inline storage of the 64 elements of the <c>position</c> field of <c>GstAudioInfo</c>.</summary>
    [InlineArray(64)]
    public struct PositionArray
    {
        private Gst.Audio.AudioChannelPosition _element0;
    }

    /// <summary>Wraps a native <c>GstAudioInfo</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static AudioInfo? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>
    /// Allocate a new #GstAudioInfo that is also initialized with
    /// gst_audio_info_init().
    /// </summary>
    /// <returns>a new #GstAudioInfo. free with gst_audio_info_free().</returns>
    public static Gst.Audio.AudioInfo New()
    {
        nint nativeResult = GstAudioInfoNew();
        return Gst.Audio.AudioInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_info_new returned no value.");
    }

    /// <summary>Parse @caps to generate a #GstAudioInfo.</summary>
    /// <param name="caps">a #GstCaps</param>
    /// <returns>A #GstAudioInfo, or %NULL if @caps couldn't be parsed</returns>
    public static Gst.Audio.AudioInfo? NewFromCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        nint nativeResult = GstAudioInfoNewFromCaps(caps.Handle);
        System.GC.KeepAlive(caps);
        return Gst.Audio.AudioInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Converts among various #GstFormat types.  This function handles
    /// GST_FORMAT_BYTES, GST_FORMAT_TIME, and GST_FORMAT_DEFAULT.  For
    /// raw audio, GST_FORMAT_DEFAULT corresponds to audio frames.  This
    /// function can be used to handle pad queries of the type GST_QUERY_CONVERT.
    /// </summary>
    /// <param name="srcFmt">#GstFormat of the @src_val</param>
    /// <param name="srcVal">value to convert</param>
    /// <param name="destFmt">#GstFormat of the @dest_val</param>
    /// <param name="destVal">pointer to destination value</param>
    /// <returns>TRUE if the conversion was successful.</returns>
    public bool Convert(Gst.Format srcFmt, long srcVal, Gst.Format destFmt, out long destVal)
    {
        long destValNative = default;
        int nativeResult = GstAudioInfoConvert(Handle, (int)srcFmt, srcVal, (int)destFmt, &destValNative);
        System.GC.KeepAlive(this);
        destVal = destValNative;
        return nativeResult != 0;
    }

    /// <summary>Copy a GstAudioInfo structure.</summary>
    /// <returns>a new #GstAudioInfo. free with gst_audio_info_free.</returns>
    public Gst.Audio.AudioInfo Copy()
    {
        nint nativeResult = GstAudioInfoCopy(Handle);
        System.GC.KeepAlive(this);
        return Gst.Audio.AudioInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_info_copy returned no value.");
    }

    /// <summary>Compares two #GstAudioInfo and returns whether they are equal or not</summary>
    /// <param name="other">a #GstAudioInfo</param>
    /// <returns>%TRUE if @info and @other are equal, else %FALSE.</returns>
    public bool IsEqual(Gst.Audio.AudioInfo other)
    {
        ArgumentNullException.ThrowIfNull(other);
        int nativeResult = GstAudioInfoIsEqual(Handle, other.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(other);
        return nativeResult != 0;
    }

    /// <summary>Set the default info for the audio info of @format and @rate and @channels.</summary>
    /// <remarks>
    /// <para>Note: This initializes @info first, no values are preserved.</para>
    /// </remarks>
    /// <param name="format">the format</param>
    /// <param name="rate">the samplerate</param>
    /// <param name="channels">the number of channels</param>
    /// <param name="position">
    /// the channel positions
    /// The C declaration sizes this buffer at 64 elements; pass exactly 64, or an empty span for <c>NULL</c>.
    /// </param>
    /// <exception cref="ArgumentException">
    /// <paramref name="position"/> does not have exactly 64 elements and is not empty.
    /// </exception>
    public void SetFormat(Gst.Audio.AudioFormat format, int rate, int channels, System.ReadOnlySpan<Gst.Audio.AudioChannelPosition> position)
    {
        if (position.Length != 64 && position.Length != 0)
        {
            throw new ArgumentException(
                "position must have exactly 64 elements, or none at all.",
                nameof(position));
        }
        fixed (Gst.Audio.AudioChannelPosition* positionPointer = position)
        {
            GstAudioInfoSetFormat(Handle, (int)format, rate, channels, positionPointer);
            System.GC.KeepAlive(this);
        }
    }

    /// <summary>Convert the values of @info into a #GstCaps.</summary>
    /// <returns>
    /// the new #GstCaps containing the
    ///          info of @info.
    /// </returns>
    public Gst.Caps ToCaps()
    {
        nint nativeResult = GstAudioInfoToCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_info_to_caps returned no value.");
    }

    /// <summary>The <c>gst_audio_info_new</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_new")]
    private static partial nint GstAudioInfoNew();

    /// <summary>The <c>gst_audio_info_new_from_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_new_from_caps")]
    private static partial nint GstAudioInfoNewFromCaps(nint caps);

    /// <summary>The <c>gst_audio_info_convert</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_convert")]
    private static partial int GstAudioInfoConvert(nint info, int srcFmt, long srcVal, int destFmt, long* destVal);

    /// <summary>The <c>gst_audio_info_copy</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_copy")]
    private static partial nint GstAudioInfoCopy(nint info);

    /// <summary>The <c>gst_audio_info_is_equal</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_is_equal")]
    private static partial int GstAudioInfoIsEqual(nint info, nint other);

    /// <summary>The <c>gst_audio_info_set_format</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_set_format")]
    private static partial void GstAudioInfoSetFormat(nint info, int format, int rate, int channels, Gst.Audio.AudioChannelPosition* position);

    /// <summary>The <c>gst_audio_info_to_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_to_caps")]
    private static partial nint GstAudioInfoToCaps(nint info);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioInfo</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_info_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new AudioInfo(handle, transfer);
}

/// <summary>The native layout of <c>GstAudioInfo</c>.</summary>
/// <remarks>
/// <para>
/// The mirror is only ever read through a pointer into memory that GStreamer
/// owns; it is never allocated, assigned or copied.
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
internal unsafe struct AudioInfoRaw
{
    /// <summary>The <c>finfo</c> field.</summary>
    internal nint Finfo;

    /// <summary>The <c>flags</c> field.</summary>
    internal Gst.Audio.AudioFlags Flags;

    /// <summary>The <c>layout</c> field.</summary>
    internal Gst.Audio.AudioLayout Layout;

    /// <summary>The <c>rate</c> field.</summary>
    internal int Rate;

    /// <summary>The <c>channels</c> field.</summary>
    internal int Channels;

    /// <summary>The <c>bpf</c> field.</summary>
    internal int Bpf;

    /// <summary>The <c>position</c> field.</summary>
    internal Gst.Audio.AudioInfo.PositionArray Position;

    /// <summary>The <c>_gst_reserved</c> field.</summary>
    internal GstReservedArray GstReserved;

    /// <summary>Inline storage of the 4 elements of the <c>_gst_reserved</c> field.</summary>
    [InlineArray(4)]
    internal struct GstReservedArray
    {
        private nint _element0;
    }
}
