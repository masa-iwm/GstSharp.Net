// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// Subclasses must use (a subclass of) #GstAudioAggregatorPad for both
/// their source and sink pads,
/// gst_element_class_add_static_pad_template_with_gtype() is a convenient
/// helper.
/// </summary>
/// <remarks>
/// <para>
/// #GstAudioAggregator can perform conversion on the data arriving
/// on its sink pads, based on the format expected downstream: in order
/// to enable that behaviour, the GType of the sink pads must either be
/// a (subclass of) #GstAudioAggregatorConvertPad to use the default
/// #GstAudioConverter implementation, or a subclass of #GstAudioAggregatorPad
/// implementing #GstAudioAggregatorPadClass.convert_buffer.
/// </para>
/// <para>
/// To allow for the output caps to change, the mechanism is the same as
/// above, with the GType of the source pad.
/// </para>
/// <para>See #GstAudioMixer for an example.</para>
/// <para>
/// When conversion is enabled, #GstAudioAggregator will accept
/// any type of raw audio caps and perform conversion
/// on the data arriving on its sink pads, with whatever downstream
/// expects as the target format.
/// </para>
/// <para>
/// In case downstream caps are not fully fixated, it will use
/// the first configured sink pad to finish fixating its source pad
/// caps.
/// </para>
/// <para>
/// A notable exception for now is the sample rate, sink pads must
/// have the same sample rate as either the downstream requirement,
/// or the first configured pad, or a combination of both (when
/// downstream specifies a range or a set of acceptable rates).
/// </para>
/// <para>
/// The #GstAggregator::samples-selected signal is provided with some
/// additional information about the output buffer:
/// - "offset"  G_TYPE_UINT64   Offset in samples since segment start
///   for the position that is next to be filled in the output buffer.
/// - "frames"  G_TYPE_UINT   Number of frames per output buffer.
/// </para>
/// <para>
/// In addition the gst_aggregator_peek_next_sample() function returns
/// additional information in the info #GstStructure of the returned sample:
/// - "output-offset"  G_TYPE_UINT64   Sample offset in output segment relative to
///   the output segment's start where the current position of this input
///   buffer would be placed
/// - "position"  G_TYPE_UINT   current position in the input buffer in samples
/// - "size"  G_TYPE_UINT   size of the input buffer in samples
/// </para>
/// </remarks>
public abstract unsafe partial class AudioAggregator : Gst.Base.Aggregator
{
    /// <summary>Wraps a native <c>GstAudioAggregator</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstAudioAggregator</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected AudioAggregator(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>The <c>gst_audio_aggregator_set_sink_caps</c> function.</summary>
    /// <param name="pad">The <c>pad</c> argument.</param>
    /// <param name="caps">The <c>caps</c> argument.</param>
    public void SetSinkCaps(Gst.Audio.AudioAggregatorPad pad, Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(pad);
        ArgumentNullException.ThrowIfNull(caps);
        GstAudioAggregatorSetSinkCaps(Handle, pad.Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pad);
        System.GC.KeepAlive(caps);
    }

    /// <summary>The <c>alignment-threshold</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public ulong AlignmentThreshold
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("alignment-threshold");
            return holder.GetUInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("alignment-threshold");
            holder.SetUInt64(value);
            SetPropertyValue("alignment-threshold", in holder);
        }
    }

    /// <summary>The <c>discont-wait</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public ulong DiscontWait
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("discont-wait");
            return holder.GetUInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("discont-wait");
            holder.SetUInt64(value);
            SetPropertyValue("discont-wait", in holder);
        }
    }

    /// <summary>
    /// Causes the element to aggregate on a timeout even when no live source is
    /// connected to its sinks. See #GstAggregator:min-upstream-latency for a
    /// companion property: in the vast majority of cases where you plan to plug in
    /// live sources with a non-zero latency, you should set it to a non-zero value.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>The property is construct-only and therefore read-only here.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public bool ForceLive
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("force-live");
            return holder.GetBoolean();
        }
    }

    /// <summary>
    /// Don't wait for inactive pads when live. An inactive pad
    /// is a pad that hasn't yet received a buffer, but that has
    /// been waited on at least once.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The purpose of this property is to avoid aggregating on
    /// timeout when new pads are requested in advance of receiving
    /// data flow, for example the user may decide to connect it later,
    /// but wants to configure it already.
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool IgnoreInactivePads
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("ignore-inactive-pads");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("ignore-inactive-pads");
            holder.SetBoolean(value);
            SetPropertyValue("ignore-inactive-pads", in holder);
        }
    }

    /// <summary>The <c>output-buffer-duration</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public ulong OutputBufferDuration
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("output-buffer-duration");
            return holder.GetUInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("output-buffer-duration");
            holder.SetUInt64(value);
            SetPropertyValue("output-buffer-duration", in holder);
        }
    }

    /// <summary>The <c>gst_audio_aggregator_set_sink_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_aggregator_set_sink_caps")]
    private static partial void GstAudioAggregatorSetSinkCaps(nint aagg, nint pad, nint caps);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioAggregator</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_aggregator_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstAudioAggregator</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : AudioAggregator
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
