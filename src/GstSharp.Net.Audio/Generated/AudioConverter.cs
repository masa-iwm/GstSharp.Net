// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// This object is used to convert audio samples from one format to another.
/// The object can perform conversion of:
/// </summary>
/// <remarks>
/// <para> * audio format with optional dithering and noise shaping</para>
/// <para> * audio samplerate</para>
/// <para> * audio channels and channel layout</para>
/// </remarks>
public sealed unsafe partial class AudioConverter : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstAudioConverter</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal AudioConverter(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a native <c>GstAudioConverter</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static AudioConverter? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>
    /// Convenience wrapper around gst_audio_converter_samples(), which will
    /// perform allocation of the output buffer based on the result from
    /// gst_audio_converter_get_out_frames().
    /// </summary>
    /// <param name="flags">The <c>flags</c> argument.</param>
    /// <param name="in">input data</param>
    /// <param name="out">
    /// a pointer where
    ///  the output data will be written
    /// </param>
    /// <returns>%TRUE is the conversion could be performed.</returns>
    public bool Convert(Gst.Audio.AudioConverterFlags flags, System.Span<byte> @in, out byte[]? @out)
    {
        nint @outNative = default;
        nuint outSizeNative = default;
        fixed (byte* @inPointer = @in)
        {
            int nativeResult = GstAudioConverterConvert(Handle, (int)flags, @inPointer, (nuint)@in.Length, &@outNative, &outSizeNative);
            System.GC.KeepAlive(this);
            @out = null;
            if (@outNative != 0)
            {
                @out = new byte[(int)outSizeNative];
                new System.ReadOnlySpan<byte>((void*)@outNative, (int)outSizeNative).CopyTo(@out);
                Gst.Interop.GMarshal.Free(@outNative);
            }
            return nativeResult != 0;
        }
    }

    /// <summary>Get the current configuration of @convert.</summary>
    /// <param name="inRate">The <c>inRate</c> argument.</param>
    /// <param name="outRate">The <c>outRate</c> argument.</param>
    /// <returns>
    ///   a #GstStructure that remains valid for as long as @convert is valid
    ///   or until gst_audio_converter_update_config() is called.
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Structure GetConfig(out int inRate, out int outRate)
    {
        int inRateNative = default;
        int outRateNative = default;
        nint nativeResult = GstAudioConverterGetConfig(Handle, &inRateNative, &outRateNative);
        System.GC.KeepAlive(this);
        inRate = inRateNative;
        outRate = outRateNative;
        return Gst.Structure.FromNative(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_audio_converter_get_config returned no value.");
    }

    /// <summary>
    /// Calculate how many input frames are currently needed by @convert to produce
    /// @out_frames of output frames.
    /// </summary>
    /// <param name="outFrames">The <c>outFrames</c> argument.</param>
    /// <returns>the number of input frames</returns>
    public nuint GetInFrames(nuint outFrames)
    {
        nuint nativeResult = GstAudioConverterGetInFrames(Handle, outFrames);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Get the maximum number of input frames that the converter would
    /// need before producing output.
    /// </summary>
    /// <returns>
    /// the latency of @convert as expressed in the number of
    /// frames.
    /// </returns>
    public nuint GetMaxLatency()
    {
        nuint nativeResult = GstAudioConverterGetMaxLatency(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Calculate how many output frames can be produced when @in_frames input
    /// frames are given to @convert.
    /// </summary>
    /// <param name="inFrames">The <c>inFrames</c> argument.</param>
    /// <returns>the number of output frames</returns>
    public nuint GetOutFrames(nuint inFrames)
    {
        nuint nativeResult = GstAudioConverterGetOutFrames(Handle, inFrames);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Returns whether the audio converter will operate in passthrough mode.
    /// The return value would be typically input to gst_base_transform_set_passthrough()
    /// </summary>
    /// <returns>%TRUE when no conversion will actually occur.</returns>
    public bool IsPassthrough()
    {
        int nativeResult = GstAudioConverterIsPassthrough(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Reset @convert to the state it was when it was first created, clearing
    /// any history it might currently have.
    /// </summary>
    public void Reset()
    {
        GstAudioConverterReset(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Returns whether the audio converter can perform the conversion in-place.
    /// The return value would be typically input to gst_base_transform_set_in_place()
    /// </summary>
    /// <returns>%TRUE when the conversion can be done in place.</returns>
    public bool SupportsInplace()
    {
        int nativeResult = GstAudioConverterSupportsInplace(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_audio_converter_convert</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_convert")]
    private static partial int GstAudioConverterConvert(nint convert, int flags, byte* @in, nuint inSize, nint* @out, nuint* outSize);

    /// <summary>The <c>gst_audio_converter_get_config</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_get_config")]
    private static partial nint GstAudioConverterGetConfig(nint convert, int* inRate, int* outRate);

    /// <summary>The <c>gst_audio_converter_get_in_frames</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_get_in_frames")]
    private static partial nuint GstAudioConverterGetInFrames(nint convert, nuint outFrames);

    /// <summary>The <c>gst_audio_converter_get_max_latency</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_get_max_latency")]
    private static partial nuint GstAudioConverterGetMaxLatency(nint convert);

    /// <summary>The <c>gst_audio_converter_get_out_frames</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_get_out_frames")]
    private static partial nuint GstAudioConverterGetOutFrames(nint convert, nuint inFrames);

    /// <summary>The <c>gst_audio_converter_is_passthrough</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_is_passthrough")]
    private static partial int GstAudioConverterIsPassthrough(nint convert);

    /// <summary>The <c>gst_audio_converter_reset</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_reset")]
    private static partial void GstAudioConverterReset(nint convert);

    /// <summary>The <c>gst_audio_converter_supports_inplace</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_supports_inplace")]
    private static partial int GstAudioConverterSupportsInplace(nint convert);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioConverter</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_converter_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new AudioConverter(handle, transfer);
}
