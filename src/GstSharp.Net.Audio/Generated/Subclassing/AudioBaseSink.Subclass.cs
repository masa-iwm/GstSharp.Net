// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <content>The subclassing surface of <c>GstAudioBaseSink</c>.</content>
public unsafe partial class AudioBaseSink
{
    /// <summary>
    /// Constructs the managed subclass that
    /// <see cref="Gst.GObject.SubclassType.NewInstance()"/> created an instance of.
    /// </summary>
    /// <param name="args">The instance, as the registration handed it out.</param>
    /// <exception cref="System.ArgumentException">The instance is not a <c>GstAudioBaseSink</c>.</exception>
    protected AudioBaseSink(Gst.GObject.SubclassCtorArgs args)
        : this(args.HandleFor(GetGType()), args.Transfer)
    {
    }

    /// <summary>
    /// Gets the declaration of <c>GstAudioBaseSink.create_ringbuffer</c>, for a subclass that
    /// overrides <see cref="OnCreateRingbuffer"/>.
    /// </summary>
    public static Gst.GObject.VfuncOverride CreateRingbufferOverride { get; } = new(
        &GetGType,
        Gst.Audio.AudioBaseSinkClassRaw.CreateRingbufferOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, nint>)&CreateRingbufferTrampoline);

    /// <summary>
    /// Gets the declaration of <c>GstAudioBaseSink.payload</c>, for a subclass that
    /// overrides <see cref="OnPayload"/>.
    /// </summary>
    public static Gst.GObject.VfuncOverride PayloadOverride { get; } = new(
        &GetGType,
        Gst.Audio.AudioBaseSinkClassRaw.PayloadOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint>)&PayloadTrampoline);

    /// <summary>Registers a managed subclass of <c>GstAudioBaseSink</c> with GObject.</summary>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// It <b>has to</b> add a pad template named <c>sink</c>.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GstAudioBaseSink</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass(
        string typeName,
        Action<Gst.GObject.ClassConfig> configureClass,
        params Gst.GObject.VfuncOverride[] overrides) =>
        DefineSubclassCore(typeName, configureClass, overrides, null);

    /// <summary>Registers a managed subclass of <c>GstAudioBaseSink</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// It <b>has to</b> add a pad template named <c>sink</c>.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GstAudioBaseSink</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ClassConfig> configureClass,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : AudioBaseSink, Gst.GObject.IManagedSubclass<TSelf> =>
        DefineSubclassCore(typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args));

    private static Gst.GObject.SubclassType DefineSubclassCore(
        string typeName,
        Action<Gst.GObject.ClassConfig> configureClass,
        Gst.GObject.VfuncOverride[] overrides,
        Func<Gst.GObject.SubclassCtorArgs, Gst.GObject.Object>? wrapFactory)
    {
        ArgumentNullException.ThrowIfNull(configureClass);
        ArgumentNullException.ThrowIfNull(overrides);

        bool declaredCreateRingbuffer = false;
        foreach (Gst.GObject.VfuncOverride candidate in overrides)
        {
            if (candidate.Function == CreateRingbufferOverride.Function)
            {
                declaredCreateRingbuffer = true;
                break;
            }
        }

        if (!declaredCreateRingbuffer)
        {
            throw new ArgumentException(
                "A managed GstAudioBaseSink has to declare CreateRingbufferOverride: without a ring buffer the element cannot leave the NULL state.",
                nameof(overrides));
        }

        Gst.GObject.SubclassType type = Gst.GObject.SubclassType.Define(
            new Gst.GObject.GType(GetGType()), typeName, configureClass, overrides, wrapFactory);
        type.RequirePadTemplate("sink");
        return type;
    }

    /// <summary>
    /// Create and return the #GstAudioRingBuffer for @sink. This function will
    /// call the ::create_ringbuffer vmethod and will set @sink as the parent of
    /// the returned buffer (see gst_object_set_parent()).
    /// </summary>
    /// <remarks>
    /// <para>The ring buffer is answered floating: the sink parents it and becomes its only owner,
    /// which is why the answer is not referenced on the way out. Keep no reference of your own
    /// to it - read it back from the element instead.</para>
    /// </remarks>
    /// <returns>
    /// The new ringbuffer of @sink.
    /// The answer is borrowed: no reference is added for the caller, so the
    /// override has to keep the object alive by other means.
    /// </returns>
    protected virtual Gst.Audio.AudioRingBuffer? OnCreateRingbuffer() =>
        ChainUpCreateRingbuffer();

    /// <summary>
    /// payload data in a format suitable to write to the sink. If no
    ///           payloading is required, returns a reffed copy of the original
    ///           buffer, else returns the payloaded buffer with all other metadata
    ///           copied.
    /// </summary>
    /// <remarks>
    /// <para>Answering nothing fails the render: the sink posts an error on the bus and drops the
    /// buffer, so a sink with nothing to payload answers the buffer it was given.</para>
    /// </remarks>
    /// <param name="buffer">
    /// The <c>buffer</c> argument.
    /// The element lends this for the duration of the call; keep a copy to retain it.
    /// </param>
    /// <returns>
    /// What <c>payload</c> answers.
    /// The object you return is handed to the element; copy or ref it first if
    /// you need it afterwards. The wrapper is detached by the return and throws
    /// from then on, exactly like the wrapper of an argument the slot consumed.
    /// </returns>
    protected virtual Gst.Buffer? OnPayload(Gst.Buffer buffer) =>
        ChainUpPayload(buffer);

    /// <summary>Runs the implementation of <c>create_ringbuffer</c> below the managed override.</summary>
    /// <remarks>
    /// <para>The ring buffer is answered floating: the sink parents it and becomes its only owner,
    /// which is why the answer is not referenced on the way out. Keep no reference of your own
    /// to it - read it back from the element instead.</para>
    /// </remarks>
    /// <returns>
    /// The new ringbuffer of @sink.
    /// The answer is borrowed: no reference is added for the caller, so the
    /// override has to keep the object alive by other means.
    /// </returns>
    protected Gst.Audio.AudioRingBuffer? ChainUpCreateRingbuffer()
    {
        nint resultNative = ChainUpCreateRingbuffer(Handle);
        Gst.Audio.AudioRingBuffer? result = Gst.GObject.Object.FromNative<Gst.Audio.AudioRingBuffer>(resultNative, Gst.Interop.Transfer.None);
        GC.KeepAlive(this);
        return result;
    }

    /// <summary>Runs the implementation of <c>payload</c> below the managed override.</summary>
    /// <remarks>
    /// <para>Answering nothing fails the render: the sink posts an error on the bus and drops the
    /// buffer, so a sink with nothing to payload answers the buffer it was given.</para>
    /// </remarks>
    /// <param name="buffer">
    /// The <c>buffer</c> argument.
    /// The element lends this for the duration of the call; keep a copy to retain it.
    /// </param>
    /// <returns>
    /// What <c>payload</c> answers.
    /// The object you return is handed to the element; copy or ref it first if
    /// you need it afterwards. The wrapper is detached by the return and throws
    /// from then on, exactly like the wrapper of an argument the slot consumed.
    /// </returns>
    protected Gst.Buffer? ChainUpPayload(Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        nint resultNative = ChainUpPayload(Handle, buffer.Handle);
        Gst.Buffer? result = Gst.Buffer.FromNative(resultNative, Gst.Interop.Transfer.Full);
        GC.KeepAlive(this);
        GC.KeepAlive(buffer);
        return result;
    }

    private static nint ChainUpCreateRingbuffer(nint sink)
    {
        delegate* unmanaged[Cdecl]<nint, nint> slot =
            (delegate* unmanaged[Cdecl]<nint, nint>)ParentClassOf(sink)->CreateRingbuffer;

        if (slot is null)
        {
            return nint.Zero;
        }

        return slot(sink);
    }

    private static nint ChainUpPayload(nint sink, nint buffer)
    {
        delegate* unmanaged[Cdecl]<nint, nint, nint> slot =
            (delegate* unmanaged[Cdecl]<nint, nint, nint>)ParentClassOf(sink)->Payload;

        if (slot is null)
        {
            // Payloading is what a sink does to data a device wants in another
            // shape, and there is nothing below the override that wants one: the
            // buffer is answered as it arrived, with the reference its caller takes
            // over. That is what gst_audio_base_sink_render does for a NULL slot -
            // it renders the buffer it already holds - and the gir says the same in
            // so many words: a reffed copy of the original buffer.
            return Gst.GstNative.MiniObjectRef(buffer);
        }

        return slot(sink, buffer);
    }

    /// <summary>
    /// Returns the class the registration captured, which is the one an override
    /// chains up through.
    /// </summary>
    /// <param name="instance">The native <c>GstAudioBaseSink</c>.</param>
    /// <returns>The parent class of the managed subclass.</returns>
    private static Gst.Audio.AudioBaseSinkClassRaw* ParentClassOf(nint instance) =>
        (Gst.Audio.AudioBaseSinkClassRaw*)Gst.GObject.SubclassRegistry.DescriptorFor(instance).ParentClass;

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static nint CreateRingbufferTrampoline(nint sink)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(sink) is not AudioBaseSink managed)
            {
                return ChainUpCreateRingbuffer(sink);
            }

            Gst.Audio.AudioRingBuffer? result = managed.OnCreateRingbuffer();
            return result is null ? nint.Zero : result.Handle;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return nint.Zero;
        }
    }

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static nint PayloadTrampoline(nint sink, nint buffer)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(sink) is not AudioBaseSink managed)
            {
                return ChainUpPayload(sink, buffer);
            }

            using Gst.Buffer? bufferValue = buffer == nint.Zero ? null : Gst.Buffer.Borrow(buffer);
            Gst.Buffer? result = managed.OnPayload(bufferValue!);
            return result is null ? nint.Zero : result.HandOver();
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return nint.Zero;
        }
    }
}
