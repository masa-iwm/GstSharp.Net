// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// Provides a base class for CD digital audio (CDDA) sources, which handles
/// things like seeking, querying, discid calculation, tags, and buffer
/// timestamping.
/// </summary>
/// <remarks>
/// <para>## Using GstAudioCdSrc-based elements in applications</para>
/// <para>
/// GstAudioCdSrc registers two #GstFormat&lt;!-- --&gt;s of its own, namely
/// the "track" format and the "sector" format. Applications will usually
/// only find the "track" format interesting. You can retrieve that #GstFormat
/// for use in seek events or queries with gst_format_get_by_nick("track").
/// </para>
/// <para>
/// In order to query the number of tracks, for example, an application would
/// set the CDDA source element to READY or PAUSED state and then query the
/// the number of tracks via gst_element_query_duration() using the track
/// format acquired above. Applications can query the currently playing track
/// in the same way.
/// </para>
/// <para>
/// Alternatively, applications may retrieve the currently playing track and
/// the total number of tracks from the taglist that will posted on the bus
/// whenever the CD is opened or the currently playing track changes. The
/// taglist will contain GST_TAG_TRACK_NUMBER and GST_TAG_TRACK_COUNT tags.
/// </para>
/// <para>
/// Applications playing back CD audio using playbin and cdda://n URIs should
/// issue a seek command in track format to change between tracks, rather than
/// setting a new cdda://n+1 URI on playbin (as setting a new URI on playbin
/// involves closing and re-opening the CD device, which is much much slower).
/// </para>
/// <para>## Tags and meta-information</para>
/// <para>
/// CDDA sources will automatically emit a number of tags, details about which
/// can be found in the libgsttag documentation. Those tags are:
/// #GST_TAG_CDDA_CDDB_DISCID, #GST_TAG_CDDA_CDDB_DISCID_FULL,
/// #GST_TAG_CDDA_MUSICBRAINZ_DISCID, #GST_TAG_CDDA_MUSICBRAINZ_DISCID_FULL,
/// among others.
/// </para>
/// <para>## Tracks and Table of Contents (TOC)</para>
/// <para>
/// Applications will be informed of the available tracks via a TOC message
/// on the pipeline's #GstBus. The #GstToc will contain a #GstTocEntry for
/// each track, with information about each track. The duration for each
/// track can be retrieved via the #GST_TAG_DURATION tag from each entry's
/// tag list, or calculated via gst_toc_entry_get_start_stop_times().
/// The track entries in the TOC will be sorted by track number.
/// </para>
/// </remarks>
public unsafe partial class AudioCdSrc : Gst.Base.PushSrc, Gst.IURIHandler
{
    /// <summary>Wraps a native <c>GstAudioCdSrc</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal AudioCdSrc(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// CDDA sources use this function from their start vfunc to announce the
    /// available data and audio tracks to the base source class. The caller
    /// should allocate @track on the stack, the base source will do a shallow
    /// copy of the structure (and take ownership of the taglist if there is one).
    /// </summary>
    /// <param name="track">The <c>track</c> argument.</param>
    /// <returns>FALSE on error, otherwise TRUE.</returns>
    public bool AddTrack(Gst.Audio.AudioCdSrcTrack track)
    {
        Gst.Audio.AudioCdSrcTrack trackNative = track;
        int nativeResult = GstAudioCdSrcAddTrack(Handle, &trackNative);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_audio_cd_src_add_track</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_cd_src_add_track")]
    private static partial int GstAudioCdSrcAddTrack(nint src, Gst.Audio.AudioCdSrcTrack* track);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioCdSrc</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_cd_src_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new AudioCdSrc(handle, transfer);
}
