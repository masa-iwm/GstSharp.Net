// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>Information describing DSD audio properties.</summary>
/// <remarks>
/// <para>
/// In DSD, the "sample format" is the bit. Unlike PCM, there are no further
/// "sample formats" in DSD. However, in software, DSD bits are grouped into
/// bytes (since dealing with individual bits is impractical), and these bytes
/// in turn are grouped into words. This becomes relevant when interleaving
/// channels and transmitting DSD data through audio APIs. The different
/// types of grouping DSD bytes are referred to as the "DSD grouping forma"
/// or just "DSD format". #GstDsdFormat has a list of valid ways of grouping
/// DSD bytes into words.
/// </para>
/// <para>
/// DSD rates are equivalent to PCM sample rates, except that they specify
/// how many DSD bytes are consumed per second. This refers to the bytes per
/// second _per channel_; the rate does not change when the number of channel
/// changes. (Strictly speaking, it would be more correct to measure the
/// *bits* per second, since the bit is the DSD "sample format", but it is
/// more practical to use bytes.) In DSD, bit rates are always an integer
/// multiple of the CD audio rate (44100) or the DAT rate (48000). DSD64-44x
/// is 44100 * 64 = 2822400 bits per second, or 352800 bytes per second
/// (the latter would be used in this info structure). DSD64-48x is
/// 48000 * 64 = 3072000 bits per second, or 384000 bytes per second.
/// #GST_DSD_MAKE_DSD_RATE_44x can be used for specifying DSD-44x rates,
/// *and #GST_DSD_MAKE_DSD_RATE_48x can be used for specifying DSD-48x ones.
/// Also, since DSD-48x is less well known, when the multiplier is given
/// without the 44x/48x specifier, 44x is typically implied.
/// </para>
/// <para>
/// It is important to know that in DSD, different format widths correspond
/// to different playtimes. That is, a word with 32 DSD bits covers two times
/// as much playtime as a word with 16 DSD bits. This is in contrast to PCM,
/// where one word (= one PCM sample) always covers a time period of 1/samplerate,
/// no matter how many bits a PCM sample is made of. For this reason, DSD
/// and PCM widths and strides cannot be used the same way.
/// </para>
/// <para>
/// Multiple channels are arranged in DSD data either interleaved or non-
/// interleaved. This is similar to PCM. Interleaved layouts rotate between
/// channels and words. First, word 0 of channel 0 is present. Then word
/// 0 of channel 1 follows. Then word 0 of channel 2 etc. until all
/// channels are through, then comes word 1 of channel 0 etc.
/// </para>
/// <para>
/// Non-interleaved data is planar. First, all words of channel 0 are
/// present, then all words of channel 1 etc. Unlike interleaved data,
/// non-interleaved data can be sparse, that is, there can be space in
/// between the planes. the @positions array specifies the plane offsets.
/// </para>
/// <para>
/// In uncommon cases, the DSD bits in the data bytes can be stored in reverse
/// order. For example, normally, in DSDU8, the first byte contains DSD bits
/// 0 to 7, and the most significant bit of that byte is DSD bit 0. If this
/// order is reversed, then bit 7 is the first one instead. In that ase,
/// @reversed_bytes is set to TRUE.
/// </para>
/// <para>Use the provided macros to access the info in this structure.</para>
/// </remarks>
public sealed unsafe partial class DsdInfo : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstDsdInfo</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal DsdInfo(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a <c>GstDsdInfo</c> that GStreamer keeps owning, for the length of one call.</summary>
    /// <param name="handle">The value that is lent to managed code.</param>
    /// <returns>The wrapper, which holds nothing of its own.</returns>
    /// <remarks>
    /// This is how the override of a virtual method receives a <c>transfer none</c>
    /// boxed value. No <c>g_boxed_copy</c> is taken, so every write the override
    /// makes lands in the instance the caller of the slot reads back, and disposing
    /// the wrapper only detaches it: a wrapper kept past the call throws instead of
    /// freeing a value it never owned. See <see cref="Gst.Interop.Borrowed"/>.
    /// </remarks>
    internal static DsdInfo Borrow(nint handle) => new(new Gst.Interop.Borrowed(handle));

    /// <summary>Wraps a <c>GstDsdInfo</c> the caller keeps owning.</summary>
    /// <param name="borrowed">The value that is lent to managed code.</param>
    private DsdInfo(Gst.Interop.Borrowed borrowed)
        : base(borrowed, new Gst.GObject.GType(GetGType()))
    {
    }

    /// <summary>DSD grouping format</summary>
    public Gst.Audio.DsdFormat Format
    {
        get
        {
            Gst.Audio.DsdFormat value = ((DsdInfoRaw*)Handle)->Format;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>DSD rate</summary>
    public int Rate
    {
        get
        {
            int value = ((DsdInfoRaw*)Handle)->Rate;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>number of channels (must be at least 1)</summary>
    public int Channels
    {
        get
        {
            int value = ((DsdInfoRaw*)Handle)->Channels;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>audio layout</summary>
    public Gst.Audio.AudioLayout Layout
    {
        get
        {
            Gst.Audio.AudioLayout value = ((DsdInfoRaw*)Handle)->Layout;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>
    /// true if the DSD bits in the data bytes are reversed,
    ///   that is, the least significant bit comes first
    /// </summary>
    public bool ReversedBytes
    {
        get
        {
            bool value = ((DsdInfoRaw*)Handle)->ReversedBytes != 0;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>positions for each channel</summary>
    public Gst.Audio.DsdInfo.PositionsArray Positions
    {
        get
        {
            Gst.Audio.DsdInfo.PositionsArray value = ((DsdInfoRaw*)Handle)->Positions;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>The <c>flags</c> field of <c>GstDsdInfo</c>.</summary>
    public Gst.Audio.AudioFlags Flags
    {
        get
        {
            Gst.Audio.AudioFlags value = ((DsdInfoRaw*)Handle)->Flags;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>Inline storage of the 64 elements of the <c>positions</c> field of <c>GstDsdInfo</c>.</summary>
    [InlineArray(64)]
    public struct PositionsArray
    {
        private Gst.Audio.AudioChannelPosition _element0;
    }

    /// <summary>Wraps a native <c>GstDsdInfo</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static DsdInfo? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>
    /// Allocate a new #GstDsdInfo that is also initialized with
    /// gst_dsd_info_init().
    /// </summary>
    /// <returns>a new #GstDsdInfo. free with gst_dsd_info_free().</returns>
    public static Gst.Audio.DsdInfo New()
    {
        nint nativeResult = GstDsdInfoNew();
        return Gst.Audio.DsdInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_dsd_info_new returned no value.");
    }

    /// <summary>Parse @caps to generate a #GstDsdInfo.</summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>A #GstDsdInfo, or %NULL if @caps couldn't be parsed</returns>
    public static Gst.Audio.DsdInfo NewFromCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        nint nativeResult = GstDsdInfoNewFromCaps(caps.Handle);
        System.GC.KeepAlive(caps);
        return Gst.Audio.DsdInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_dsd_info_new_from_caps returned no value.");
    }

    /// <summary>Copy a GstDsdInfo structure.</summary>
    /// <returns>a new #GstDsdInfo. free with gst_dsd_info_free.</returns>
    public Gst.Audio.DsdInfo Copy()
    {
        nint nativeResult = GstDsdInfoCopy(Handle);
        System.GC.KeepAlive(this);
        return Gst.Audio.DsdInfo.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_dsd_info_copy returned no value.");
    }

    /// <summary>Compares two #GstDsdInfo and returns whether they are equal or not</summary>
    /// <param name="other">The <c>other</c> argument.</param>
    /// <returns>%TRUE if @info and @other are equal, else %FALSE.</returns>
    public bool IsEqual(Gst.Audio.DsdInfo other)
    {
        ArgumentNullException.ThrowIfNull(other);
        int nativeResult = GstDsdInfoIsEqual(Handle, other.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(other);
        return nativeResult != 0;
    }

    /// <summary>Set the default info for the DSD info of @format and @rate and @channels.</summary>
    /// <remarks>
    /// <para>Note: This initializes @info first, no values are preserved.</para>
    /// </remarks>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="rate">The <c>rate</c> argument.</param>
    /// <param name="channels">The <c>channels</c> argument.</param>
    /// <param name="positions">
    /// the channel positions
    /// The C declaration sizes this buffer at 64 elements; pass exactly 64, or an empty span for <c>NULL</c>.
    /// </param>
    /// <exception cref="ArgumentException">
    /// <paramref name="positions"/> does not have exactly 64 elements and is not empty.
    /// </exception>
    public void SetFormat(Gst.Audio.DsdFormat format, int rate, int channels, System.ReadOnlySpan<Gst.Audio.AudioChannelPosition> positions)
    {
        if (positions.Length != 64 && positions.Length != 0)
        {
            throw new ArgumentException(
                "positions must have exactly 64 elements, or none at all.",
                nameof(positions));
        }
        fixed (Gst.Audio.AudioChannelPosition* positionsPointer = positions)
        {
            GstDsdInfoSetFormat(Handle, (int)format, rate, channels, positionsPointer);
            System.GC.KeepAlive(this);
        }
    }

    /// <summary>Convert the values of @info into a #GstCaps.</summary>
    /// <returns>
    /// the new #GstCaps containing the
    ///          info of @info.
    /// </returns>
    public Gst.Caps ToCaps()
    {
        nint nativeResult = GstDsdInfoToCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_dsd_info_to_caps returned no value.");
    }

    /// <summary>The <c>gst_dsd_info_new</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_dsd_info_new")]
    private static partial nint GstDsdInfoNew();

    /// <summary>The <c>gst_dsd_info_new_from_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_dsd_info_new_from_caps")]
    private static partial nint GstDsdInfoNewFromCaps(nint caps);

    /// <summary>The <c>gst_dsd_info_copy</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_dsd_info_copy")]
    private static partial nint GstDsdInfoCopy(nint info);

    /// <summary>The <c>gst_dsd_info_is_equal</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_dsd_info_is_equal")]
    private static partial int GstDsdInfoIsEqual(nint info, nint other);

    /// <summary>The <c>gst_dsd_info_set_format</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_dsd_info_set_format")]
    private static partial void GstDsdInfoSetFormat(nint info, int format, int rate, int channels, Gst.Audio.AudioChannelPosition* positions);

    /// <summary>The <c>gst_dsd_info_to_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_dsd_info_to_caps")]
    private static partial nint GstDsdInfoToCaps(nint info);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstDsdInfo</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_dsd_info_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new DsdInfo(handle, transfer);
}

/// <summary>The native layout of <c>GstDsdInfo</c>.</summary>
/// <remarks>
/// <para>
/// The mirror is only ever read through a pointer into memory that GStreamer
/// owns; it is never allocated, assigned or copied.
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
internal unsafe struct DsdInfoRaw
{
    /// <summary>The <c>format</c> field.</summary>
    internal Gst.Audio.DsdFormat Format;

    /// <summary>The <c>rate</c> field.</summary>
    internal int Rate;

    /// <summary>The <c>channels</c> field.</summary>
    internal int Channels;

    /// <summary>The <c>layout</c> field.</summary>
    internal Gst.Audio.AudioLayout Layout;

    // <c>gboolean</c> is a 32 bit integer; every non zero value is true.
    /// <summary>The <c>reversed_bytes</c> field.</summary>
    internal int ReversedBytes;

    /// <summary>The <c>positions</c> field.</summary>
    internal Gst.Audio.DsdInfo.PositionsArray Positions;

    /// <summary>The <c>flags</c> field.</summary>
    internal Gst.Audio.AudioFlags Flags;

    /// <summary>The <c>_gst_reserved</c> field.</summary>
    internal GstReservedArray GstReserved;

    /// <summary>Inline storage of the 4 elements of the <c>_gst_reserved</c> field.</summary>
    [InlineArray(4)]
    internal struct GstReservedArray
    {
        private nint _element0;
    }
}
