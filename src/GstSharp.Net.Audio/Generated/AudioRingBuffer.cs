// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// This object is the base class for audio ringbuffers used by the base
/// audio source and sink classes.
/// </summary>
/// <remarks>
/// <para>
/// The ringbuffer abstracts a circular buffer of data. One reader and
/// one writer can operate on the data from different threads in a lockfree
/// manner. The base class is sufficiently flexible to be used as an
/// abstraction for DMA based ringbuffers as well as a pure software
/// implementations.
/// </para>
/// </remarks>
public abstract unsafe partial class AudioRingBuffer : Gst.Object
{
    /// <summary>Wraps a native <c>GstAudioRingBuffer</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstAudioRingBuffer</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected AudioRingBuffer(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Allocate the resources for the ringbuffer. This function fills
    /// in the data pointer of the ring buffer with a valid #GstBuffer
    /// to which samples can be written.
    /// </summary>
    /// <param name="spec">The <c>spec</c> argument.</param>
    /// <returns>TRUE if the device could be acquired, FALSE on error.</returns>
    public bool Acquire(Gst.Audio.AudioRingBufferSpec spec)
    {
        ArgumentNullException.ThrowIfNull(spec);
        int nativeResult = GstAudioRingBufferAcquire(Handle, spec.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(spec);
        return nativeResult != 0;
    }

    /// <summary>Activate @buf to start or stop pulling data.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="active">The <c>active</c> argument.</param>
    /// <returns>
    /// TRUE if the device could be activated in the requested mode,
    /// FALSE on error.
    /// </returns>
    public bool Activate(bool active)
    {
        int nativeResult = GstAudioRingBufferActivate(Handle, active ? 1 : 0);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Subclasses should call this function to notify the fact that
    /// @advance segments are now processed by the device.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="advance">The <c>advance</c> argument.</param>
    public void Advance(uint advance)
    {
        GstAudioRingBufferAdvance(Handle, advance);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Clear the given segment of the buffer with silence samples.
    /// This function is used by subclasses.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="segment">The <c>segment</c> argument.</param>
    public void Clear(int segment)
    {
        GstAudioRingBufferClear(Handle, segment);
        System.GC.KeepAlive(this);
    }

    /// <summary>Clear all samples from the ringbuffer.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    public void ClearAll()
    {
        GstAudioRingBufferClearAll(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Close the audio device associated with the ring buffer. The ring buffer
    /// should already have been released via gst_audio_ring_buffer_release().
    /// </summary>
    /// <returns>TRUE if the device could be closed, FALSE on error.</returns>
    public bool CloseDevice()
    {
        int nativeResult = GstAudioRingBufferCloseDevice(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Convert @src_val in @src_fmt to the equivalent value in @dest_fmt. The result
    /// will be put in @dest_val.
    /// </summary>
    /// <param name="srcFmt">The <c>srcFmt</c> argument.</param>
    /// <param name="srcVal">The <c>srcVal</c> argument.</param>
    /// <param name="destFmt">The <c>destFmt</c> argument.</param>
    /// <param name="destVal">The <c>destVal</c> argument.</param>
    /// <returns>TRUE if the conversion succeeded.</returns>
    public bool Convert(Gst.Format srcFmt, long srcVal, Gst.Format destFmt, out long destVal)
    {
        long destValNative = default;
        int nativeResult = GstAudioRingBufferConvert(Handle, (int)srcFmt, srcVal, (int)destFmt, &destValNative);
        System.GC.KeepAlive(this);
        destVal = destValNative;
        return nativeResult != 0;
    }

    /// <summary>
    /// Get the number of samples queued in the audio device. This is
    /// usually less than the segment size but can be bigger when the
    /// implementation uses another internal buffer between the audio
    /// device.
    /// </summary>
    /// <remarks>
    /// <para>
    /// For playback ringbuffers this is the amount of samples transferred from the
    /// ringbuffer to the device but still not played.
    /// </para>
    /// <para>
    /// For capture ringbuffers this is the amount of samples in the device that are
    /// not yet transferred to the ringbuffer.
    /// </para>
    /// </remarks>
    /// <returns>The number of samples queued in the audio device.</returns>
    public uint Delay()
    {
        uint nativeResult = GstAudioRingBufferDelay(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Checks the status of the device associated with the ring buffer.</summary>
    /// <returns>TRUE if the device was open, FALSE if it was closed.</returns>
    public bool DeviceIsOpen()
    {
        int nativeResult = GstAudioRingBufferDeviceIsOpen(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Gets the current segment base number of the ringbuffer.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// <para>Available since GStreamer 1.26.</para>
    /// </remarks>
    /// <returns>Current segment base number of the ringbuffer.</returns>
    public ulong GetSegbase()
    {
        ulong nativeResult = GstAudioRingBufferGetSegbase(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Gets the current segment number of the ringbuffer.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// <para>Available since GStreamer 1.26.</para>
    /// </remarks>
    /// <returns>Current segment number of the ringbuffer.</returns>
    public ulong GetSegdone()
    {
        ulong nativeResult = GstAudioRingBufferGetSegdone(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Check if the ringbuffer is acquired and ready to use.</summary>
    /// <returns>TRUE if the ringbuffer is acquired, FALSE on error.</returns>
    public bool IsAcquired()
    {
        int nativeResult = GstAudioRingBufferIsAcquired(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Check if @buf is activated.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <returns>TRUE if the device is active.</returns>
    public bool IsActive()
    {
        int nativeResult = GstAudioRingBufferIsActive(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Check if @buf is flushing.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <returns>TRUE if the device is flushing.</returns>
    public bool IsFlushing()
    {
        int nativeResult = GstAudioRingBufferIsFlushing(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Tell the ringbuffer that it is allowed to start playback when
    /// the ringbuffer is filled with samples.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="allowed">The <c>allowed</c> argument.</param>
    public void MayStart(bool allowed)
    {
        GstAudioRingBufferMayStart(Handle, allowed ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Open the audio device associated with the ring buffer. Does not perform any
    /// setup on the device. You must open the device before acquiring the ring
    /// buffer.
    /// </summary>
    /// <returns>TRUE if the device could be opened, FALSE on error.</returns>
    public bool OpenDevice()
    {
        int nativeResult = GstAudioRingBufferOpenDevice(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Pause processing samples from the ringbuffer.</summary>
    /// <returns>TRUE if the device could be paused, FALSE on error.</returns>
    public bool Pause()
    {
        int nativeResult = GstAudioRingBufferPause(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns a pointer to memory where the data from segment @segment
    /// can be found. This function is mostly used by subclasses.
    /// </summary>
    /// <param name="segment">The <c>segment</c> argument.</param>
    /// <param name="readptr">    the pointer to the memory where samples can be read</param>
    /// <returns>FALSE if the buffer is not started.</returns>
    public bool PrepareRead(out int segment, out byte[]? readptr)
    {
        int segmentNative = default;
        nint readptrNative = default;
        int lenNative = default;
        int nativeResult = GstAudioRingBufferPrepareRead(Handle, &segmentNative, &readptrNative, &lenNative);
        System.GC.KeepAlive(this);
        segment = segmentNative;
        readptr = null;
        if (readptrNative != 0)
        {
            readptr = new byte[(int)lenNative];
            new System.ReadOnlySpan<byte>((void*)readptrNative, (int)lenNative).CopyTo(readptr);
            Gst.Interop.GMarshal.Free(readptrNative);
        }
        return nativeResult != 0;
    }

    /// <summary>
    /// Read @len samples from the ringbuffer into the memory pointed
    /// to by @data.
    /// The first sample should be read from position @sample in
    /// the ringbuffer.
    /// </summary>
    /// <remarks>
    /// <para>
    /// @len should not be a multiple of the segment size of the ringbuffer
    /// although it is recommended.
    /// </para>
    /// <para>@timestamp will return the timestamp associated with the data returned.</para>
    /// </remarks>
    /// <param name="sample">The <c>sample</c> argument.</param>
    /// <param name="data">where the data should be read</param>
    /// <param name="timestamp">The <c>timestamp</c> argument.</param>
    /// <returns>
    /// The number of samples read from the ringbuffer or -1 on
    /// error.
    /// </returns>
    public uint Read(ulong sample, System.Span<byte> data, out Gst.ClockTime timestamp)
    {
        ulong timestampNative = default;
        fixed (byte* dataPointer = data)
        {
            uint nativeResult = GstAudioRingBufferRead(Handle, sample, dataPointer, (uint)data.Length, &timestampNative);
            System.GC.KeepAlive(this);
            timestamp = new Gst.ClockTime(timestampNative);
            return nativeResult;
        }
    }

    /// <summary>Free the resources of the ringbuffer.</summary>
    /// <returns>TRUE if the device could be released, FALSE on error.</returns>
    public bool Release()
    {
        int nativeResult = GstAudioRingBufferRelease(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Get the number of samples that were processed by the ringbuffer
    /// since it was last started. This does not include the number of samples not
    /// yet processed (see gst_audio_ring_buffer_delay()).
    /// </summary>
    /// <returns>The number of samples processed by the ringbuffer.</returns>
    public ulong SamplesDone()
    {
        ulong nativeResult = GstAudioRingBufferSamplesDone(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Mark the ringbuffer as errored after it has started.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    public void SetErrored()
    {
        GstAudioRingBufferSetErrored(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Set the ringbuffer to flushing mode or normal mode.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="flushing">The <c>flushing</c> argument.</param>
    public void SetFlushing(bool flushing)
    {
        GstAudioRingBufferSetFlushing(Handle, flushing ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Make sure that the next sample written to the device is
    /// accounted for as being the @sample sample written to the
    /// device. This value will be used in reporting the current
    /// sample position of the ringbuffer.
    /// </summary>
    /// <remarks>
    /// <para>This function will also clear the buffer with silence.</para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="sample">The <c>sample</c> argument.</param>
    public void SetSample(ulong sample)
    {
        GstAudioRingBufferSetSample(Handle, sample);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the current segment number of the ringbuffer.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// <para>Available since GStreamer 1.26.</para>
    /// </remarks>
    /// <param name="segdone">The <c>segdone</c> argument.</param>
    public void SetSegdone(ulong segdone)
    {
        GstAudioRingBufferSetSegdone(Handle, segdone);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_audio_ring_buffer_set_timestamp</c> function.</summary>
    /// <param name="readseg">The <c>readseg</c> argument.</param>
    /// <param name="timestamp">The <c>timestamp</c> argument.</param>
    public void SetTimestamp(int readseg, Gst.ClockTime timestamp)
    {
        GstAudioRingBufferSetTimestamp(Handle, readseg, timestamp.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>Start processing samples from the ringbuffer.</summary>
    /// <returns>TRUE if the device could be started, FALSE on error.</returns>
    public bool Start()
    {
        int nativeResult = GstAudioRingBufferStart(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Stop processing samples from the ringbuffer.</summary>
    /// <returns>TRUE if the device could be stopped, FALSE on error.</returns>
    public bool Stop()
    {
        int nativeResult = GstAudioRingBufferStop(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Print debug info about the buffer sized in @spec to the debug log.</summary>
    /// <param name="spec">The <c>spec</c> argument.</param>
    public static void DebugSpecBuff(Gst.Audio.AudioRingBufferSpec spec)
    {
        ArgumentNullException.ThrowIfNull(spec);
        GstAudioRingBufferDebugSpecBuff(spec.Handle);
        System.GC.KeepAlive(spec);
    }

    /// <summary>Print debug info about the parsed caps in @spec to the debug log.</summary>
    /// <param name="spec">The <c>spec</c> argument.</param>
    public static void DebugSpecCaps(Gst.Audio.AudioRingBufferSpec spec)
    {
        ArgumentNullException.ThrowIfNull(spec);
        GstAudioRingBufferDebugSpecCaps(spec.Handle);
        System.GC.KeepAlive(spec);
    }

    /// <summary>Parse @caps into @spec.</summary>
    /// <param name="spec">The <c>spec</c> argument.</param>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>TRUE if the caps could be parsed.</returns>
    public static bool ParseCaps(Gst.Audio.AudioRingBufferSpec spec, Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(spec);
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstAudioRingBufferParseCaps(spec.Handle, caps.Handle);
        System.GC.KeepAlive(spec);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_audio_ring_buffer_acquire</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_acquire")]
    private static partial int GstAudioRingBufferAcquire(nint buf, nint spec);

    /// <summary>The <c>gst_audio_ring_buffer_activate</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_activate")]
    private static partial int GstAudioRingBufferActivate(nint buf, int active);

    /// <summary>The <c>gst_audio_ring_buffer_advance</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_advance")]
    private static partial void GstAudioRingBufferAdvance(nint buf, uint advance);

    /// <summary>The <c>gst_audio_ring_buffer_clear</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_clear")]
    private static partial void GstAudioRingBufferClear(nint buf, int segment);

    /// <summary>The <c>gst_audio_ring_buffer_clear_all</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_clear_all")]
    private static partial void GstAudioRingBufferClearAll(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_close_device</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_close_device")]
    private static partial int GstAudioRingBufferCloseDevice(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_convert</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_convert")]
    private static partial int GstAudioRingBufferConvert(nint buf, int srcFmt, long srcVal, int destFmt, long* destVal);

    /// <summary>The <c>gst_audio_ring_buffer_delay</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_delay")]
    private static partial uint GstAudioRingBufferDelay(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_device_is_open</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_device_is_open")]
    private static partial int GstAudioRingBufferDeviceIsOpen(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_get_segbase</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_get_segbase")]
    private static partial ulong GstAudioRingBufferGetSegbase(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_get_segdone</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_get_segdone")]
    private static partial ulong GstAudioRingBufferGetSegdone(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_is_acquired</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_is_acquired")]
    private static partial int GstAudioRingBufferIsAcquired(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_is_active</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_is_active")]
    private static partial int GstAudioRingBufferIsActive(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_is_flushing</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_is_flushing")]
    private static partial int GstAudioRingBufferIsFlushing(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_may_start</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_may_start")]
    private static partial void GstAudioRingBufferMayStart(nint buf, int allowed);

    /// <summary>The <c>gst_audio_ring_buffer_open_device</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_open_device")]
    private static partial int GstAudioRingBufferOpenDevice(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_pause</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_pause")]
    private static partial int GstAudioRingBufferPause(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_prepare_read</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_prepare_read")]
    private static partial int GstAudioRingBufferPrepareRead(nint buf, int* segment, nint* readptr, int* len);

    /// <summary>The <c>gst_audio_ring_buffer_read</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_read")]
    private static partial uint GstAudioRingBufferRead(nint buf, ulong sample, byte* data, uint len, ulong* timestamp);

    /// <summary>The <c>gst_audio_ring_buffer_release</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_release")]
    private static partial int GstAudioRingBufferRelease(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_samples_done</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_samples_done")]
    private static partial ulong GstAudioRingBufferSamplesDone(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_set_errored</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_set_errored")]
    private static partial void GstAudioRingBufferSetErrored(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_set_flushing</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_set_flushing")]
    private static partial void GstAudioRingBufferSetFlushing(nint buf, int flushing);

    /// <summary>The <c>gst_audio_ring_buffer_set_sample</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_set_sample")]
    private static partial void GstAudioRingBufferSetSample(nint buf, ulong sample);

    /// <summary>The <c>gst_audio_ring_buffer_set_segdone</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_set_segdone")]
    private static partial void GstAudioRingBufferSetSegdone(nint buf, ulong segdone);

    /// <summary>The <c>gst_audio_ring_buffer_set_timestamp</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_set_timestamp")]
    private static partial void GstAudioRingBufferSetTimestamp(nint buf, int readseg, ulong timestamp);

    /// <summary>The <c>gst_audio_ring_buffer_start</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_start")]
    private static partial int GstAudioRingBufferStart(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_stop</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_stop")]
    private static partial int GstAudioRingBufferStop(nint buf);

    /// <summary>The <c>gst_audio_ring_buffer_debug_spec_buff</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_debug_spec_buff")]
    private static partial void GstAudioRingBufferDebugSpecBuff(nint spec);

    /// <summary>The <c>gst_audio_ring_buffer_debug_spec_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_debug_spec_caps")]
    private static partial void GstAudioRingBufferDebugSpecCaps(nint spec);

    /// <summary>The <c>gst_audio_ring_buffer_parse_caps</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_parse_caps")]
    private static partial int GstAudioRingBufferParseCaps(nint spec, nint caps);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioRingBuffer</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_ring_buffer_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstAudioRingBuffer</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : AudioRingBuffer
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
