// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// #GstAudioClock makes it easy for elements to implement a #GstClock, they
/// simply need to provide a function that returns the current clock time.
/// </summary>
/// <remarks>
/// <para>This object is internally used to implement the clock in #GstAudioBaseSink.</para>
/// </remarks>
public unsafe partial class AudioClock : Gst.SystemClock
{
    /// <summary>Wraps a native <c>GstAudioClock</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstAudioClock</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected AudioClock(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Create a new #GstAudioClock instance. Whenever the clock time should be
    /// calculated it will call @func with @user_data. When @func returns
    /// #GST_CLOCK_TIME_NONE, the clock will return the last reported time.
    /// </summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <param name="func">a function</param>
    /// <returns>a new #GstAudioClock casted to a #GstClock.</returns>
    public static Gst.Clock New(string name, Gst.Audio.AudioClockGetTimeFunc func)
    {
        ArgumentNullException.ThrowIfNull(name);
        ArgumentNullException.ThrowIfNull(func);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        nint nativeResult = GstAudioClockNew(nameScope.Pointer, Gst.Audio.AudioClockGetTimeFuncTrampoline.Pointer, funcState.UserData, (nint)Gst.Interop.CallbackHandle.DestroyNotify);
        return Gst.GObject.Object.FromNative<Gst.Clock>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_audio_clock_new returned no value.");
    }

    /// <summary>Adjust @time with the internal offset of the audio clock.</summary>
    /// <param name="time">The <c>time</c> argument.</param>
    /// <returns>@time adjusted with the internal offset.</returns>
    public Gst.ClockTime Adjust(Gst.ClockTime time)
    {
        ulong nativeResult = GstAudioClockAdjust(Handle, time.Nanoseconds);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Report the time as returned by the #GstAudioClockGetTimeFunc without applying
    /// any offsets.
    /// </summary>
    /// <returns>the time as reported by the time function of the audio clock</returns>
    public new Gst.ClockTime GetTime()
    {
        ulong nativeResult = GstAudioClockGetTime(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Invalidate the clock function. Call this function when the provided
    /// #GstAudioClockGetTimeFunc cannot be called anymore, for example, when the
    /// user_data becomes invalid.
    /// </summary>
    /// <remarks>
    /// <para>
    /// After calling this function, @clock will return the last returned time for
    /// the rest of its lifetime.
    /// </para>
    /// </remarks>
    public void Invalidate()
    {
        GstAudioClockInvalidate(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Inform @clock that future calls to #GstAudioClockGetTimeFunc will return values
    /// starting from @time. The clock will update an internal offset to make sure that
    /// future calls to internal_time will return an increasing result as required by
    /// the #GstClock object.
    /// </summary>
    /// <param name="time">The <c>time</c> argument.</param>
    public void Reset(Gst.ClockTime time)
    {
        GstAudioClockReset(Handle, time.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_audio_clock_new</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_clock_new")]
    private static partial nint GstAudioClockNew(byte* name, nint func, nint userData, nint destroyNotify);

    /// <summary>The <c>gst_audio_clock_adjust</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_clock_adjust")]
    private static partial ulong GstAudioClockAdjust(nint clock, ulong time);

    /// <summary>The <c>gst_audio_clock_get_time</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_clock_get_time")]
    private static partial ulong GstAudioClockGetTime(nint clock);

    /// <summary>The <c>gst_audio_clock_invalidate</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_clock_invalidate")]
    private static partial void GstAudioClockInvalidate(nint clock);

    /// <summary>The <c>gst_audio_clock_reset</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_clock_reset")]
    private static partial void GstAudioClockReset(nint clock, ulong time);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAudioClock</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_audio_clock_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new AudioClock(handle, transfer);
}
