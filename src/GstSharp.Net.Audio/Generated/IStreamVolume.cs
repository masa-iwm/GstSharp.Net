// <auto-generated/>
// Generated by GstSharp.Generator from GstAudio-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Audio;

/// <summary>
/// This interface is implemented by elements that provide a stream volume. Examples for
/// such elements are #volume and #playbin.
/// </summary>
/// <remarks>
/// <para>
/// Applications can use this interface to get or set the current stream volume. For this
/// the "volume" #GObject property can be used or the helper functions gst_stream_volume_set_volume()
/// and gst_stream_volume_get_volume(). This volume is always a linear factor, i.e. 0.0 is muted
/// 1.0 is 100%. For showing the volume in a GUI it might make sense to convert it to
/// a different format by using gst_stream_volume_convert_volume(). Volume sliders should usually
/// use a cubic volume.
/// </para>
/// <para>
/// Separate from the volume the stream can also be muted by the "mute" #GObject property or
/// gst_stream_volume_set_mute() and gst_stream_volume_get_mute().
/// </para>
/// <para>
/// Elements that provide some kind of stream volume should implement the "volume" and
/// "mute" #GObject properties and handle setting and getting of them properly.
/// The volume property is defined to be a linear volume factor.
/// </para>
/// </remarks>
public interface IStreamVolume
{
    /// <summary>Gets the native instance that implements the interface.</summary>
    nint Handle { get; }
}

/// <summary>The methods of <c>GstStreamVolume</c>.</summary>
public static unsafe partial class StreamVolumeExtensions
{
    /// <summary>The <c>gst_stream_volume_get_mute</c> function.</summary>
    /// <param name="volume">#GstStreamVolume that should be used</param>
    /// <returns>Returns %TRUE if the stream is muted</returns>
    public static bool GetMute(this Gst.Audio.IStreamVolume volume)
    {
        ArgumentNullException.ThrowIfNull(volume);
        int nativeResult = GstStreamVolumeGetMute(volume.Handle);
        System.GC.KeepAlive(volume);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_stream_volume_get_volume</c> function.</summary>
    /// <param name="volume">#GstStreamVolume that should be used</param>
    /// <param name="format">#GstStreamVolumeFormat which should be returned</param>
    /// <returns>The current stream volume as linear factor</returns>
    public static double GetVolume(this Gst.Audio.IStreamVolume volume, Gst.Audio.StreamVolumeFormat format)
    {
        ArgumentNullException.ThrowIfNull(volume);
        double nativeResult = GstStreamVolumeGetVolume(volume.Handle, (int)format);
        System.GC.KeepAlive(volume);
        return nativeResult;
    }

    /// <summary>The <c>gst_stream_volume_set_mute</c> function.</summary>
    /// <param name="volume">#GstStreamVolume that should be used</param>
    /// <param name="mute">Mute state that should be set</param>
    public static void SetMute(this Gst.Audio.IStreamVolume volume, bool mute)
    {
        ArgumentNullException.ThrowIfNull(volume);
        GstStreamVolumeSetMute(volume.Handle, mute ? 1 : 0);
        System.GC.KeepAlive(volume);
    }

    /// <summary>The <c>gst_stream_volume_set_volume</c> function.</summary>
    /// <param name="volume">#GstStreamVolume that should be used</param>
    /// <param name="format">#GstStreamVolumeFormat of @val</param>
    /// <param name="val">Linear volume factor that should be set</param>
    public static void SetVolume(this Gst.Audio.IStreamVolume volume, Gst.Audio.StreamVolumeFormat format, double val)
    {
        ArgumentNullException.ThrowIfNull(volume);
        GstStreamVolumeSetVolume(volume.Handle, (int)format, val);
        System.GC.KeepAlive(volume);
    }

    /// <summary>The <c>gst_stream_volume_convert_volume</c> function.</summary>
    /// <param name="from">#GstStreamVolumeFormat to convert from</param>
    /// <param name="to">#GstStreamVolumeFormat to convert to</param>
    /// <param name="val">Volume in @from format that should be converted</param>
    /// <returns>the converted volume</returns>
    public static double ConvertVolume(Gst.Audio.StreamVolumeFormat from, Gst.Audio.StreamVolumeFormat to, double val)
    {
        double nativeResult = GstStreamVolumeConvertVolume((int)from, (int)to, val);
        return nativeResult;
    }

    /// <summary>The <c>gst_stream_volume_get_mute</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_stream_volume_get_mute")]
    private static partial int GstStreamVolumeGetMute(nint volume);

    /// <summary>The <c>gst_stream_volume_get_volume</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_stream_volume_get_volume")]
    private static partial double GstStreamVolumeGetVolume(nint volume, int format);

    /// <summary>The <c>gst_stream_volume_set_mute</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_stream_volume_set_mute")]
    private static partial void GstStreamVolumeSetMute(nint volume, int mute);

    /// <summary>The <c>gst_stream_volume_set_volume</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_stream_volume_set_volume")]
    private static partial void GstStreamVolumeSetVolume(nint volume, int format, double val);

    /// <summary>The <c>gst_stream_volume_convert_volume</c> entry point.</summary>
    [LibraryImport("GstAudio", EntryPoint = "gst_stream_volume_convert_volume")]
    private static partial double GstStreamVolumeConvertVolume(int from, int to, double val);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstStreamVolume</c> under.</summary>
    /// <returns>The type of the <c>GstStreamVolume</c> interface.</returns>
    [LibraryImport("GstAudio", EntryPoint = "gst_stream_volume_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Presents a <see cref="Gst.GObject.Object"/> as <see cref="Gst.Audio.IStreamVolume"/>, once the runtime has checked the type.</summary>
    internal sealed class Adapter : Gst.Audio.IStreamVolume
    {
        private readonly Gst.GObject.Object _owner;

        /// <summary>Initialises the view of an object.</summary>
        /// <param name="owner">The wrapper that the view reads its handle from.</param>
        internal Adapter(Gst.GObject.Object owner) => _owner = owner;

        /// <inheritdoc/>
        public nint Handle => _owner.Handle;
    }

    /// <summary>Creates the view of an object, for the type registry.</summary>
    /// <param name="owner">The wrapper to present as the interface.</param>
    /// <returns>The new view.</returns>
    internal static object CreateAdapter(Gst.GObject.Object owner) => new Adapter(owner);
}
