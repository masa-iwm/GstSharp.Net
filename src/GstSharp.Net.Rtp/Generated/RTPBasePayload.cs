// <auto-generated/>
// Generated by GstSharp.Generator from GstRtp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Rtp;

/// <summary>Provides a base class for RTP payloaders</summary>
public abstract unsafe partial class RTPBasePayload : Gst.Element
{
    /// <summary>Wraps a native <c>GstRTPBasePayload</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstRTPBasePayload</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected RTPBasePayload(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Allocate a new #GstBuffer with enough data to hold an RTP packet with
    /// minimum @csrc_count CSRCs, a payload length of @payload_len and padding of
    /// @pad_len. If @payload has #GstRTPBasePayload:source-info %TRUE additional
    /// CSRCs may be allocated and filled with RTP source information.
    /// </summary>
    /// <param name="payloadLen">the length of the payload</param>
    /// <param name="padLen">the amount of padding</param>
    /// <param name="csrcCount">the minimum number of CSRC entries</param>
    /// <returns>
    /// A newly allocated buffer that can hold an RTP packet with given
    /// parameters.
    /// </returns>
    public Gst.Buffer AllocateOutputBuffer(uint payloadLen, byte padLen, byte csrcCount)
    {
        nint nativeResult = GstRtpBasePayloadAllocateOutputBuffer(Handle, payloadLen, padLen, csrcCount);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_rtp_base_payload_allocate_output_buffer returned no value.");
    }

    /// <summary>
    /// Count the total number of RTP sources found in the meta of @buffer, which
    /// will be automically added by gst_rtp_base_payload_allocate_output_buffer().
    /// If #GstRTPBasePayload:source-info is %FALSE the count will be 0.
    /// </summary>
    /// <param name="buffer">a #GstBuffer, typically the buffer to payload</param>
    /// <returns>The number of sources.</returns>
    public uint GetSourceCount(Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        uint nativeResult = GstRtpBasePayloadGetSourceCount(Handle, buffer.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(buffer);
        return nativeResult;
    }

    /// <summary>
    /// Check if the packet with @size and @duration would exceed the configured
    /// maximum size.
    /// </summary>
    /// <param name="size">the size of the packet</param>
    /// <param name="duration">the duration of the packet</param>
    /// <returns>
    /// %TRUE if the packet of @size and @duration would exceed the
    /// configured MTU or max_ptime.
    /// </returns>
    public bool IsFilled(uint size, Gst.ClockTime duration)
    {
        int nativeResult = GstRtpBasePayloadIsFilled(Handle, size, duration.Nanoseconds);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Queries whether the payloader will add contributing sources (CSRCs) to the
    /// RTP header from #GstRTPSourceMeta.
    /// </summary>
    /// <returns>%TRUE if source-info is enabled.</returns>
    public bool IsSourceInfoEnabled()
    {
        int nativeResult = GstRtpBasePayloadIsSourceInfoEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Push @buffer to the peer element of the payloader. The SSRC, payload type,
    /// seqnum and timestamp of the RTP buffer will be updated first.
    /// </summary>
    /// <remarks>
    /// <para>This function takes ownership of @buffer.</para>
    /// <para>
    /// The <c>buffer</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="buffer">
    /// a #GstBuffer
    /// The call consumes it: <paramref name="buffer"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="buffer"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="buffer"/> was disposed.
    /// </exception>
    public Gst.FlowReturn Push(Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        nint instanceHandle = Handle;
        nint bufferNative = buffer.Handle;
        nint bufferOwned = Gst.GstNative.MiniObjectRef(bufferNative);
        int nativeResult = GstRtpBasePayloadPush(instanceHandle, bufferOwned);
        System.GC.KeepAlive(this);
        buffer.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Push @list to the peer element of the payloader. The SSRC, payload type,
    /// seqnum and timestamp of the RTP buffer will be updated first.
    /// </summary>
    /// <remarks>
    /// <para>This function takes ownership of @list.</para>
    /// <para>
    /// The <c>list</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="list">
    /// a #GstBufferList
    /// The call consumes it: <paramref name="list"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="list"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="list"/> was disposed.
    /// </exception>
    public Gst.FlowReturn PushList(Gst.BufferList list)
    {
        ArgumentNullException.ThrowIfNull(list);
        nint instanceHandle = Handle;
        nint listNative = list.Handle;
        nint listOwned = Gst.GstNative.MiniObjectRef(listNative);
        int nativeResult = GstRtpBasePayloadPushList(instanceHandle, listOwned);
        System.GC.KeepAlive(this);
        list.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Set the rtp options of the payloader. These options will be set in the caps
    /// of the payloader. Subclasses must call this method before calling
    /// gst_rtp_base_payload_push() or gst_rtp_base_payload_set_outcaps().
    /// </summary>
    /// <param name="media">the media type (typically "audio" or "video")</param>
    /// <param name="dynamic">if the payload type is dynamic</param>
    /// <param name="encodingName">the encoding name</param>
    /// <param name="clockRate">the clock rate of the media</param>
    public void SetOptions(string media, bool dynamic, string encodingName, uint clockRate)
    {
        ArgumentNullException.ThrowIfNull(media);
        System.Span<byte> mediaBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope mediaScope = Gst.Interop.GMarshal.StackUtf8(media, mediaBuffer);
        ArgumentNullException.ThrowIfNull(encodingName);
        System.Span<byte> encodingNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope encodingNameScope = Gst.Interop.GMarshal.StackUtf8(encodingName, encodingNameBuffer);
        GstRtpBasePayloadSetOptions(Handle, mediaScope.Pointer, dynamic ? 1 : 0, encodingNameScope.Pointer, clockRate);
        System.GC.KeepAlive(this);
    }

    /// <summary>Configure the output caps with the optional fields.</summary>
    /// <param name="s">a #GstStructure with the caps fields</param>
    /// <returns>%TRUE if the caps could be set.</returns>
    public bool SetOutcapsStructure(Gst.Structure? s)
    {
        int nativeResult = GstRtpBasePayloadSetOutcapsStructure(Handle, s is null ? 0 : s.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(s);
        return nativeResult != 0;
    }

    /// <summary>
    /// Enable or disable adding contributing sources to RTP packets from
    /// #GstRTPSourceMeta.
    /// </summary>
    /// <param name="enable">whether to add contributing sources to RTP packets</param>
    public void SetSourceInfoEnabled(bool enable)
    {
        GstRtpBasePayloadSetSourceInfoEnabled(Handle, enable ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If enabled, the payloader will automatically try to enable all the
    /// RTP header extensions provided in the src caps, saving the application
    /// the need to handle these extensions manually using the
    /// GstRTPBasePayload::request-extension: signal.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool AutoHeaderExtension
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("auto-header-extension");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("auto-header-extension");
            holder.SetBoolean(value);
            SetPropertyValue("auto-header-extension", in holder);
        }
    }

    /// <summary>The <c>max-ptime</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public long MaxPtime
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("max-ptime");
            return holder.GetInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("max-ptime");
            holder.SetInt64(value);
            SetPropertyValue("max-ptime", in holder);
        }
    }

    /// <summary>Minimum duration of the packet data in ns (can't go above MTU)</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public long MinPtime
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("min-ptime");
            return holder.GetInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("min-ptime");
            holder.SetInt64(value);
            SetPropertyValue("min-ptime", in holder);
        }
    }

    /// <summary>The <c>mtu</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public uint Mtu
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("mtu");
            return holder.GetUInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("mtu");
            holder.SetUInt(value);
            SetPropertyValue("mtu", in holder);
        }
    }

    /// <summary>
    /// Make the payloader timestamp packets according to the Rate-Control=no
    /// behaviour specified in the ONVIF replay spec.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool OnvifNoRateControl
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("onvif-no-rate-control");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("onvif-no-rate-control");
            holder.SetBoolean(value);
            SetPropertyValue("onvif-no-rate-control", in holder);
        }
    }

    /// <summary>
    /// Try to use the offset fields to generate perfect RTP timestamps. When this
    /// option is disabled, RTP timestamps are generated from GST_BUFFER_PTS of
    /// each payloaded buffer. The PTSes of buffers may not necessarily increment
    /// with the amount of data in each input buffer, consider e.g. the case where
    /// the buffer arrives from a network which means that the PTS is unrelated to
    /// the amount of data. Because the RTP timestamps are generated from
    /// GST_BUFFER_PTS this can result in RTP timestamps that also don't increment
    /// with the amount of data in the payloaded packet. To circumvent this it is
    /// possible to set the perfect rtptime option enabled. When this option is
    /// enabled the payloader will increment the RTP timestamps based on
    /// GST_BUFFER_OFFSET which relates to the amount of data in each packet
    /// rather than the GST_BUFFER_PTS of each buffer and therefore the RTP
    /// timestamps will more closely correlate with the amount of data in each
    /// buffer. Currently GstRTPBasePayload is limited to handling perfect RTP
    /// timestamps for audio streams.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool PerfectRtptime
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("perfect-rtptime");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("perfect-rtptime");
            holder.SetBoolean(value);
            SetPropertyValue("perfect-rtptime", in holder);
        }
    }

    /// <summary>The <c>pt</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public uint Pt
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("pt");
            return holder.GetUInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("pt");
            holder.SetUInt(value);
            SetPropertyValue("pt", in holder);
        }
    }

    /// <summary>Force buffers to be multiples of this duration in ns (0 disables)</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public long PtimeMultiple
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("ptime-multiple");
            return holder.GetInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("ptime-multiple");
            holder.SetInt64(value);
            SetPropertyValue("ptime-multiple", in holder);
        }
    }

    /// <summary>
    /// Make the RTP packets' timestamps be scaled with the segment's rate
    /// (corresponding to RTSP speed parameter). Disabling this property means
    /// the timestamps will not be affected by the set delivery speed (RTSP speed).
    /// </summary>
    /// <remarks>
    /// <para>
    /// Example: A server wants to allow streaming a recorded video in double
    /// speed but still have the timestamps correspond to the position in the
    /// video. This is achieved by the client setting RTSP Speed to 2 while the
    /// server has this property disabled.
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool ScaleRtptime
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("scale-rtptime");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("scale-rtptime");
            holder.SetBoolean(value);
            SetPropertyValue("scale-rtptime", in holder);
        }
    }

    /// <summary>The <c>seqnum</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public uint Seqnum
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("seqnum");
            return holder.GetUInt();
        }
    }

    /// <summary>The <c>seqnum-offset</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public int SeqnumOffset
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("seqnum-offset");
            return holder.GetInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("seqnum-offset");
            holder.SetInt(value);
            SetPropertyValue("seqnum-offset", in holder);
        }
    }

    /// <summary>
    /// Enable writing the CSRC field in allocated RTP header based on RTP source
    /// information found in the input buffer's #GstRTPSourceMeta.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool SourceInfo
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("source-info");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("source-info");
            holder.SetBoolean(value);
            SetPropertyValue("source-info", in holder);
        }
    }

    /// <summary>The <c>ssrc</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public uint Ssrc
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("ssrc");
            return holder.GetUInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("ssrc");
            holder.SetUInt(value);
            SetPropertyValue("ssrc", in holder);
        }
    }

    /// <summary>
    /// Various payloader statistics retrieved atomically (and are therefore
    /// synchroized with each other), these can be used e.g. to generate an
    /// RTP-Info header. This property return a GstStructure named
    /// application/x-rtp-payload-stats containing the following fields relating to
    /// the last processed buffer and current state of the stream being payloaded:
    /// </summary>
    /// <remarks>
    /// <para>
    ///   * `clock-rate` :#G_TYPE_UINT, clock-rate of the stream
    ///   * `running-time` :#G_TYPE_UINT64, running time
    ///   * `seqnum` :#G_TYPE_UINT, sequence number, same as #GstRTPBasePayload:seqnum
    ///   * `timestamp` :#G_TYPE_UINT, RTP timestamp, same as #GstRTPBasePayload:timestamp
    ///   * `ssrc` :#G_TYPE_UINT, The SSRC in use
    ///   * `pt` :#G_TYPE_UINT, The Payload type in use, same as #GstRTPBasePayload:pt
    ///   * `seqnum-offset` :#G_TYPE_UINT, The current offset added to the seqnum
    ///   * `timestamp-offset` :#G_TYPE_UINT, The current offset added to the timestamp
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>
    /// Reading builds a wrapper that owns a copy of the value: dispose it when
    /// you are done with it.
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public Gst.Structure? Stats
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("stats");
            return holder.GetBoxed<Gst.Structure>();
        }
    }

    /// <summary>The <c>timestamp</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public uint Timestamp
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("timestamp");
            return holder.GetUInt();
        }
    }

    /// <summary>The <c>timestamp-offset</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public uint TimestampOffset
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("timestamp-offset");
            return holder.GetUInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("timestamp-offset");
            holder.SetUInt(value);
            SetPropertyValue("timestamp-offset", in holder);
        }
    }

    /// <summary>The arguments of the <c>request-extension</c> signal of <c>GstRTPBasePayload</c>.</summary>
    public sealed class RequestExtensionSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="RequestExtensionSignalArgs"/> class.</summary>
        /// <param name="extId">the extension id being requested</param>
        /// <param name="extUri">the extension URI being requested</param>
        internal RequestExtensionSignalArgs(uint extId, string extUri)
        {
            ExtId = extId;
            ExtUri = extUri;
        }

        /// <summary>the extension id being requested</summary>
        public uint ExtId { get; }

        /// <summary>the extension URI being requested</summary>
        public string ExtUri { get; }
    }

    /// <summary>The handler of the <c>request-extension</c> signal of <c>GstRTPBasePayload</c>.</summary>
    /// <remarks>
    /// The value the handler returns is handed to native code with a reference
    /// minted for it, so the wrapper the handler holds stays usable and is still
    /// the caller's to manage. Returning <see langword="null"/> answers no value,
    /// and what the emission makes of that is the contract of the signal, stated
    /// in its own returns documentation: it may go on to another handler, to the
    /// class handler, or read the empty answer as the result. That choice belongs
    /// to the emitting library and can differ between GStreamer versions.
    /// </remarks>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>the #GstRTPHeaderExtension for @ext_id, or %NULL</returns>
    public delegate Gst.Rtp.RTPHeaderExtension? RequestExtensionHandler(object? sender, Gst.Rtp.RTPBasePayload.RequestExtensionSignalArgs args);

    /// <summary>
    /// The returned @ext must be configured with the correct @ext_id and with the
    /// necessary attributes as required by the extension implementation.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.Rtp.RTPBasePayload.RequestExtensionHandler RequestExtension
    {
        add => Gst.Rtp.SignalConnections.Add(this, "request-extension", (nint)(delegate* unmanaged[Cdecl]<nint, uint, byte*, nint, nint>)&RequestExtensionTrampoline, value);
        remove => Gst.Rtp.SignalConnections.Remove(this, "request-extension", value);
    }

    /// <summary>The native handler of the <c>request-extension</c> signal of <c>GstRTPBasePayload</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static nint RequestExtensionTrampoline(nint instance, uint extId, byte* extUri, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.Rtp.RTPBasePayload.RequestExtensionHandler>(userData) is not { } handler)
            {
                return default;
            }

            uint extIdValue = extId;
            string extUriValue = Gst.Interop.GMarshal.PtrToStringUtf8((nint)extUri)
                ?? throw new InvalidOperationException("The request-extension signal of GstRTPBasePayload passed no ext_uri.");
            Gst.Rtp.RTPHeaderExtension? result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Rtp.RTPBasePayload.RequestExtensionSignalArgs(extIdValue, extUriValue));
            nint owned = result is null ? 0 : Gst.Interop.GObjectNative.ObjectRef(result.Handle);
            System.GC.KeepAlive(result);
            return owned;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The <c>gst_rtp_base_payload_allocate_output_buffer</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_allocate_output_buffer")]
    private static partial nint GstRtpBasePayloadAllocateOutputBuffer(nint payload, uint payloadLen, byte padLen, byte csrcCount);

    /// <summary>The <c>gst_rtp_base_payload_get_source_count</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_get_source_count")]
    private static partial uint GstRtpBasePayloadGetSourceCount(nint payload, nint buffer);

    /// <summary>The <c>gst_rtp_base_payload_is_filled</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_is_filled")]
    private static partial int GstRtpBasePayloadIsFilled(nint payload, uint size, ulong duration);

    /// <summary>The <c>gst_rtp_base_payload_is_source_info_enabled</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_is_source_info_enabled")]
    private static partial int GstRtpBasePayloadIsSourceInfoEnabled(nint payload);

    /// <summary>The <c>gst_rtp_base_payload_push</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_push")]
    private static partial int GstRtpBasePayloadPush(nint payload, nint buffer);

    /// <summary>The <c>gst_rtp_base_payload_push_list</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_push_list")]
    private static partial int GstRtpBasePayloadPushList(nint payload, nint list);

    /// <summary>The <c>gst_rtp_base_payload_set_options</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_set_options")]
    private static partial void GstRtpBasePayloadSetOptions(nint payload, byte* media, int dynamic, byte* encodingName, uint clockRate);

    /// <summary>The <c>gst_rtp_base_payload_set_outcaps_structure</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_set_outcaps_structure")]
    private static partial int GstRtpBasePayloadSetOutcapsStructure(nint payload, nint s);

    /// <summary>The <c>gst_rtp_base_payload_set_source_info_enabled</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_set_source_info_enabled")]
    private static partial void GstRtpBasePayloadSetSourceInfoEnabled(nint payload, int enable);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRTPBasePayload</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_payload_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstRTPBasePayload</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : RTPBasePayload
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
