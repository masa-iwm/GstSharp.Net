// <auto-generated/>
// Generated by GstSharp.Generator from GstRtp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Rtp;

/// <summary>Provides a base class for RTP depayloaders</summary>
/// <remarks>
/// <para>
/// In order to handle RTP header extensions correctly if the
/// depayloader aggregates multiple RTP packet payloads into one output
/// buffer this class provides the function
/// gst_rtp_base_depayload_set_aggregate_hdrext_enabled(). If the
/// aggregation is enabled the virtual functions
/// @GstRTPBaseDepayload.process or
/// @GstRTPBaseDepayload.process_rtp_packet must tell the base class
/// what happens to the current RTP packet. By default the base class
/// assumes that the packet payload is used with the next output
/// buffer.
/// </para>
/// <para>
/// If the RTP packet will not be used with an output buffer
/// gst_rtp_base_depayload_dropped() must be called. A typical
/// situation would be if we are waiting for a keyframe.
/// </para>
/// <para>
/// If the RTP packet will be used but not with the current output
/// buffer but with the next one gst_rtp_base_depayload_delayed() must
/// be called. This may happen if the current RTP packet signals the
/// start of a new output buffer and the currently processed output
/// buffer will be pushed first. The undelay happens implicitly once
/// the current buffer has been pushed or
/// gst_rtp_base_depayload_flush() has been called.
/// </para>
/// <para>
/// If gst_rtp_base_depayload_flush() is called all RTP packets that
/// have not been dropped since the last output buffer are dropped,
/// e.g. if an output buffer is discarded due to malformed data. This
/// may or may not include the current RTP packet depending on the 2nd
/// parameter @keep_current.
/// </para>
/// <para>
/// Be aware that in case gst_rtp_base_depayload_push_list() is used
/// each buffer will see the same list of RTP header extensions.
/// </para>
/// </remarks>
public abstract unsafe partial class RTPBaseDepayload : Gst.Element
{
    /// <summary>Wraps a native <c>GstRTPBaseDepayload</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstRTPBaseDepayload</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected RTPBaseDepayload(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Called from @GstRTPBaseDepayload.process or
    /// @GstRTPBaseDepayload.process_rtp_packet when the depayloader needs
    /// to keep the current input RTP header for use with the next output
    /// buffer.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The delayed buffer will remain until the end of processing the
    /// current output buffer and then enqueued for processing with the
    /// next output buffer.
    /// </para>
    /// <para>
    /// A typical use-case is when the depayloader implementation will
    /// start a new output buffer for the current input RTP buffer but push
    /// the current output buffer first.
    /// </para>
    /// <para>Must be called with the stream lock held.</para>
    /// </remarks>
    public void Delayed()
    {
        GstRtpBaseDepayloadDelayed(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Called from @GstRTPBaseDepayload.process or
    /// @GstRTPBaseDepayload.process_rtp_packet if the depayloader does not
    /// use the current buffer for the output buffer. This will either drop
    /// the delayed buffer or the last buffer from the header extension
    /// cache.
    /// </summary>
    /// <remarks>
    /// <para>
    /// A typical use-case is when the depayloader implementation is
    /// dropping an input RTP buffer while waiting for the first keyframe.
    /// </para>
    /// <para>Must be called with the stream lock held.</para>
    /// </remarks>
    public void Dropped()
    {
        GstRtpBaseDepayloadDropped(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If @GstRTPBaseDepayload.process or
    /// @GstRTPBaseDepayload.process_rtp_packet drop an output buffer this
    /// function tells the base class to flush header extension cache as
    /// well.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This will not drop an input RTP header marked as delayed from
    /// gst_rtp_base_depayload_delayed().
    /// </para>
    /// <para>
    /// If @keep_current is %TRUE the current input RTP header will be kept
    /// and enqueued after flushing the previous input RTP headers.
    /// </para>
    /// <para>
    /// A typical use-case for @keep_current is when the depayloader
    /// implementation invalidates the current output buffer and starts a
    /// new one with the current RTP input buffer.
    /// </para>
    /// <para>Must be called with the stream lock held.</para>
    /// </remarks>
    /// <param name="keepCurrent">if the current RTP buffer shall be kept</param>
    public void Flush(bool keepCurrent)
    {
        GstRtpBaseDepayloadFlush(Handle, keepCurrent ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Queries whether header extensions will be aggregated per depayloaded buffers.</summary>
    /// <returns>%TRUE if aggregate-header-extension is enabled.</returns>
    public bool IsAggregateHdrextEnabled()
    {
        int nativeResult = GstRtpBaseDepayloadIsAggregateHdrextEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Queries whether #GstRTPSourceMeta will be added to depayloaded buffers.</summary>
    /// <returns>%TRUE if source-info is enabled.</returns>
    public bool IsSourceInfoEnabled()
    {
        int nativeResult = GstRtpBaseDepayloadIsSourceInfoEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Push @out_buf to the peer of @filter. This function takes ownership of
    /// @out_buf.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function will by default apply the last incoming timestamp on
    /// the outgoing buffer when it didn't have a timestamp already.
    /// </para>
    /// <para>
    /// The <c>outBuf</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="outBuf">
    /// a #GstBuffer
    /// The call consumes it: <paramref name="outBuf"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="outBuf"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="outBuf"/> was disposed.
    /// </exception>
    public Gst.FlowReturn Push(Gst.Buffer outBuf)
    {
        ArgumentNullException.ThrowIfNull(outBuf);
        nint instanceHandle = Handle;
        nint outBufNative = outBuf.Handle;
        nint outBufOwned = Gst.GstNative.MiniObjectRef(outBufNative);
        int nativeResult = GstRtpBaseDepayloadPush(instanceHandle, outBufOwned);
        System.GC.KeepAlive(this);
        outBuf.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Push @out_list to the peer of @filter. This function takes ownership of
    /// @out_list.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>outList</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="outList">
    /// a #GstBufferList
    /// The call consumes it: <paramref name="outList"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="outList"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="outList"/> was disposed.
    /// </exception>
    public Gst.FlowReturn PushList(Gst.BufferList outList)
    {
        ArgumentNullException.ThrowIfNull(outList);
        nint instanceHandle = Handle;
        nint outListNative = outList.Handle;
        nint outListOwned = Gst.GstNative.MiniObjectRef(outListNative);
        int nativeResult = GstRtpBaseDepayloadPushList(instanceHandle, outListOwned);
        System.GC.KeepAlive(this);
        outList.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>Enable or disable aggregating header extensions.</summary>
    /// <param name="enable">whether to aggregate header extensions per output buffer</param>
    public void SetAggregateHdrextEnabled(bool enable)
    {
        GstRtpBaseDepayloadSetAggregateHdrextEnabled(Handle, enable ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Enable or disable adding #GstRTPSourceMeta to depayloaded buffers.</summary>
    /// <param name="enable">whether to add meta about RTP sources to buffer</param>
    public void SetSourceInfoEnabled(bool enable)
    {
        GstRtpBaseDepayloadSetSourceInfoEnabled(Handle, enable ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If enabled, the depayloader will automatically try to enable all the
    /// RTP header extensions provided in the sink caps, saving the application
    /// the need to handle these extensions manually using the
    /// GstRTPBaseDepayload::request-extension: signal.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool AutoHeaderExtension
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("auto-header-extension");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("auto-header-extension");
            holder.SetBoolean(value);
            SetPropertyValue("auto-header-extension", in holder);
        }
    }

    /// <summary>Max seqnum reorder before the sender is assumed to have restarted.</summary>
    /// <remarks>
    /// <para>
    /// When max-reorder is set to 0 all reordered/duplicate packets are
    /// considered coming from a restarted sender.
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public int MaxReorder
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("max-reorder");
            return holder.GetInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("max-reorder");
            holder.SetInt(value);
            SetPropertyValue("max-reorder", in holder);
        }
    }

    /// <summary>Add RTP source information found in RTP header as meta to output buffer.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool SourceInfo
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("source-info");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("source-info");
            holder.SetBoolean(value);
            SetPropertyValue("source-info", in holder);
        }
    }

    /// <summary>
    /// Various depayloader statistics retrieved atomically (and are therefore
    /// synchroized with each other). This property return a GstStructure named
    /// application/x-rtp-depayload-stats containing the following fields relating to
    /// the last processed buffer and current state of the stream being depayloaded:
    /// </summary>
    /// <remarks>
    /// <para>
    ///   * `clock-rate`: #G_TYPE_UINT, clock-rate of the stream
    ///   * `npt-start`: #G_TYPE_UINT64, time of playback start
    ///   * `npt-stop`: #G_TYPE_UINT64, time of playback stop
    ///   * `play-speed`: #G_TYPE_DOUBLE, the playback speed
    ///   * `play-scale`: #G_TYPE_DOUBLE, the playback scale
    ///   * `running-time-dts`: #G_TYPE_UINT64, the last running-time of the
    ///      last DTS
    ///   * `running-time-pts`: #G_TYPE_UINT64, the last running-time of the
    ///      last PTS
    ///   * `seqnum`: #G_TYPE_UINT, the last seen seqnum
    ///   * `timestamp`: #G_TYPE_UINT, the last seen RTP timestamp
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>
    /// Reading builds a wrapper that owns a copy of the value: dispose it when
    /// you are done with it.
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public Gst.Structure? Stats
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("stats");
            return holder.GetBoxed<Gst.Structure>();
        }
    }

    /// <summary>The arguments of the <c>request-extension</c> signal of <c>GstRTPBaseDepayload</c>.</summary>
    public sealed class RequestExtensionSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="RequestExtensionSignalArgs"/> class.</summary>
        /// <param name="extId">the extension id being requested</param>
        /// <param name="extUri">the extension URI being requested</param>
        internal RequestExtensionSignalArgs(uint extId, string? extUri)
        {
            ExtId = extId;
            ExtUri = extUri;
        }

        /// <summary>the extension id being requested</summary>
        public uint ExtId { get; }

        /// <summary>the extension URI being requested</summary>
        public string? ExtUri { get; }
    }

    /// <summary>The handler of the <c>request-extension</c> signal of <c>GstRTPBaseDepayload</c>.</summary>
    /// <remarks>
    /// The value the handler returns is handed to native code with a reference
    /// minted for it, so the wrapper the handler holds stays usable and is still
    /// the caller's to manage. Returning <see langword="null"/> answers no value,
    /// and what the emission makes of that is the contract of the signal, stated
    /// in its own returns documentation: it may go on to another handler, to the
    /// class handler, or read the empty answer as the result. That choice belongs
    /// to the emitting library and can differ between GStreamer versions.
    /// </remarks>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>the #GstRTPHeaderExtension for @ext_id, or %NULL</returns>
    public delegate Gst.Rtp.RTPHeaderExtension? RequestExtensionHandler(object? sender, Gst.Rtp.RTPBaseDepayload.RequestExtensionSignalArgs args);

    /// <summary>
    /// The returned @ext must be configured with the correct @ext_id and with the
    /// necessary attributes as required by the extension implementation.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.Rtp.RTPBaseDepayload.RequestExtensionHandler RequestExtension
    {
        add => Gst.Rtp.SignalConnections.Add(this, "request-extension", (nint)(delegate* unmanaged[Cdecl]<nint, uint, byte*, nint, nint>)&RequestExtensionTrampoline, value);
        remove => Gst.Rtp.SignalConnections.Remove(this, "request-extension", value);
    }

    /// <summary>The native handler of the <c>request-extension</c> signal of <c>GstRTPBaseDepayload</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static nint RequestExtensionTrampoline(nint instance, uint extId, byte* extUri, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.Rtp.RTPBaseDepayload.RequestExtensionHandler>(userData) is not { } handler)
            {
                return default;
            }

            uint extIdValue = extId;
            string? extUriValue = Gst.Interop.GMarshal.PtrToStringUtf8((nint)extUri);
            Gst.Rtp.RTPHeaderExtension? result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Rtp.RTPBaseDepayload.RequestExtensionSignalArgs(extIdValue, extUriValue));
            nint owned = result is null ? 0 : Gst.Interop.GObjectNative.ObjectRef(result.Handle);
            System.GC.KeepAlive(result);
            return owned;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The <c>gst_rtp_base_depayload_delayed</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_delayed")]
    private static partial void GstRtpBaseDepayloadDelayed(nint depayload);

    /// <summary>The <c>gst_rtp_base_depayload_dropped</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_dropped")]
    private static partial void GstRtpBaseDepayloadDropped(nint depayload);

    /// <summary>The <c>gst_rtp_base_depayload_flush</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_flush")]
    private static partial void GstRtpBaseDepayloadFlush(nint depayload, int keepCurrent);

    /// <summary>The <c>gst_rtp_base_depayload_is_aggregate_hdrext_enabled</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_is_aggregate_hdrext_enabled")]
    private static partial int GstRtpBaseDepayloadIsAggregateHdrextEnabled(nint depayload);

    /// <summary>The <c>gst_rtp_base_depayload_is_source_info_enabled</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_is_source_info_enabled")]
    private static partial int GstRtpBaseDepayloadIsSourceInfoEnabled(nint depayload);

    /// <summary>The <c>gst_rtp_base_depayload_push</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_push")]
    private static partial int GstRtpBaseDepayloadPush(nint filter, nint outBuf);

    /// <summary>The <c>gst_rtp_base_depayload_push_list</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_push_list")]
    private static partial int GstRtpBaseDepayloadPushList(nint filter, nint outList);

    /// <summary>The <c>gst_rtp_base_depayload_set_aggregate_hdrext_enabled</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_set_aggregate_hdrext_enabled")]
    private static partial void GstRtpBaseDepayloadSetAggregateHdrextEnabled(nint depayload, int enable);

    /// <summary>The <c>gst_rtp_base_depayload_set_source_info_enabled</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_set_source_info_enabled")]
    private static partial void GstRtpBaseDepayloadSetSourceInfoEnabled(nint depayload, int enable);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRTPBaseDepayload</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_depayload_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstRTPBaseDepayload</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : RTPBaseDepayload
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
