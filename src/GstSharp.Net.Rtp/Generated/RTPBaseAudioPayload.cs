// <auto-generated/>
// Generated by GstSharp.Generator from GstRtp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Rtp;

/// <summary>
/// Provides a base class for audio RTP payloaders for frame or sample based
/// audio codecs (constant bitrate)
/// </summary>
/// <remarks>
/// <para>
/// This class derives from GstRTPBasePayload. It can be used for payloading
/// audio codecs. It will only work with constant bitrate codecs. It supports
/// both frame based and sample based codecs. It takes care of packing up the
/// audio data into RTP packets and filling up the headers accordingly. The
/// payloading is done based on the maximum MTU (mtu) and the maximum time per
/// packet (max-ptime). The general idea is to divide large data buffers into
/// smaller RTP packets. The RTP packet size is the minimum of either the MTU,
/// max-ptime (if set) or available data. The RTP packet size is always larger or
/// equal to min-ptime (if set). If min-ptime is not set, any residual data is
/// sent in a last RTP packet. In the case of frame based codecs, the resulting
/// RTP packets always contain full frames.
/// </para>
/// <para>## Usage</para>
/// <para>
/// To use this base class, your child element needs to call either
/// gst_rtp_base_audio_payload_set_frame_based() or
/// gst_rtp_base_audio_payload_set_sample_based(). This is usually done in the
/// element's `_init()` function. Then, the child element must call either
/// gst_rtp_base_audio_payload_set_frame_options(),
/// gst_rtp_base_audio_payload_set_sample_options() or
/// gst_rtp_base_audio_payload_set_samplebits_options. Since
/// GstRTPBaseAudioPayload derives from GstRTPBasePayload, the child element
/// must set any variables or call/override any functions required by that base
/// class. The child element does not need to override any other functions
/// specific to GstRTPBaseAudioPayload.
/// </para>
/// </remarks>
public unsafe partial class RTPBaseAudioPayload : Gst.Rtp.RTPBasePayload
{
    /// <summary>Wraps a native <c>GstRTPBaseAudioPayload</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstRTPBaseAudioPayload</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected RTPBaseAudioPayload(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Create an RTP buffer and store @payload_len bytes of the adapter as the
    /// payload. Set the timestamp on the new buffer to @timestamp before pushing
    /// the buffer downstream.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @payload_len is -1, all pending bytes will be flushed. If @timestamp is
    /// -1, the timestamp will be calculated automatically.
    /// </para>
    /// </remarks>
    /// <param name="payloadLen">length of payload</param>
    /// <param name="timestamp">a #GstClockTime</param>
    /// <returns>a #GstFlowReturn</returns>
    public Gst.FlowReturn Flush(uint payloadLen, Gst.ClockTime timestamp)
    {
        int nativeResult = GstRtpBaseAudioPayloadFlush(Handle, payloadLen, timestamp.Nanoseconds);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>Gets the internal adapter used by the depayloader.</summary>
    /// <returns>a #GstAdapter.</returns>
    public Gst.Base.Adapter GetAdapter()
    {
        nint nativeResult = GstRtpBaseAudioPayloadGetAdapter(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Base.Adapter>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_rtp_base_audio_payload_get_adapter returned no value.");
    }

    /// <summary>
    /// Create an RTP buffer and store @payload_len bytes of @data as the
    /// payload. Set the timestamp on the new buffer to @timestamp before pushing
    /// the buffer downstream.
    /// </summary>
    /// <param name="data">data to set as payload</param>
    /// <param name="timestamp">a #GstClockTime</param>
    /// <returns>a #GstFlowReturn</returns>
    public Gst.FlowReturn Push(System.ReadOnlySpan<byte> data, Gst.ClockTime timestamp)
    {
        fixed (byte* dataPointer = data)
        {
            int nativeResult = GstRtpBaseAudioPayloadPush(Handle, dataPointer, (uint)data.Length, timestamp.Nanoseconds);
            System.GC.KeepAlive(this);
            return (Gst.FlowReturn)nativeResult;
        }
    }

    /// <summary>
    /// Tells #GstRTPBaseAudioPayload that the child element is for a frame based
    /// audio codec
    /// </summary>
    public void SetFrameBased()
    {
        GstRtpBaseAudioPayloadSetFrameBased(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the options for frame based audio codecs.</summary>
    /// <param name="frameDuration">The duraction of an audio frame in milliseconds.</param>
    /// <param name="frameSize">The size of an audio frame in bytes.</param>
    public void SetFrameOptions(int frameDuration, int frameSize)
    {
        GstRtpBaseAudioPayloadSetFrameOptions(Handle, frameDuration, frameSize);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Tells #GstRTPBaseAudioPayload that the child element is for a sample based
    /// audio codec
    /// </summary>
    public void SetSampleBased()
    {
        GstRtpBaseAudioPayloadSetSampleBased(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the options for sample based audio codecs.</summary>
    /// <param name="sampleSize">Size per sample in bytes.</param>
    public void SetSampleOptions(int sampleSize)
    {
        GstRtpBaseAudioPayloadSetSampleOptions(Handle, sampleSize);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the options for sample based audio codecs.</summary>
    /// <param name="sampleSize">Size per sample in bits.</param>
    public void SetSamplebitsOptions(int sampleSize)
    {
        GstRtpBaseAudioPayloadSetSamplebitsOptions(Handle, sampleSize);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>buffer-list</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool BufferList
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("buffer-list");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("buffer-list");
            holder.SetBoolean(value);
            SetPropertyValue("buffer-list", in holder);
        }
    }

    /// <summary>The <c>gst_rtp_base_audio_payload_flush</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_flush")]
    private static partial int GstRtpBaseAudioPayloadFlush(nint baseaudiopayload, uint payloadLen, ulong timestamp);

    /// <summary>The <c>gst_rtp_base_audio_payload_get_adapter</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_get_adapter")]
    private static partial nint GstRtpBaseAudioPayloadGetAdapter(nint rtpbaseaudiopayload);

    /// <summary>The <c>gst_rtp_base_audio_payload_push</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_push")]
    private static partial int GstRtpBaseAudioPayloadPush(nint baseaudiopayload, byte* data, uint payloadLen, ulong timestamp);

    /// <summary>The <c>gst_rtp_base_audio_payload_set_frame_based</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_set_frame_based")]
    private static partial void GstRtpBaseAudioPayloadSetFrameBased(nint rtpbaseaudiopayload);

    /// <summary>The <c>gst_rtp_base_audio_payload_set_frame_options</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_set_frame_options")]
    private static partial void GstRtpBaseAudioPayloadSetFrameOptions(nint rtpbaseaudiopayload, int frameDuration, int frameSize);

    /// <summary>The <c>gst_rtp_base_audio_payload_set_sample_based</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_set_sample_based")]
    private static partial void GstRtpBaseAudioPayloadSetSampleBased(nint rtpbaseaudiopayload);

    /// <summary>The <c>gst_rtp_base_audio_payload_set_sample_options</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_set_sample_options")]
    private static partial void GstRtpBaseAudioPayloadSetSampleOptions(nint rtpbaseaudiopayload, int sampleSize);

    /// <summary>The <c>gst_rtp_base_audio_payload_set_samplebits_options</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_set_samplebits_options")]
    private static partial void GstRtpBaseAudioPayloadSetSamplebitsOptions(nint rtpbaseaudiopayload, int sampleSize);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRTPBaseAudioPayload</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_base_audio_payload_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new RTPBaseAudioPayload(handle, transfer);
}
