// <auto-generated/>
// Generated by GstSharp.Generator from GstRtp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Rtp;

/// <summary>Instance struct for a RTP Audio/Video header extension.</summary>
public abstract unsafe partial class RTPHeaderExtension : Gst.Element
{
    /// <summary>Wraps a native <c>GstRTPHeaderExtension</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstRTPHeaderExtension</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected RTPHeaderExtension(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Retrieve the direction</summary>
    /// <returns>The direction</returns>
    public Gst.Rtp.RTPHeaderExtensionDirection GetDirection()
    {
        int nativeResult = GstRtpHeaderExtensionGetDirection(Handle);
        System.GC.KeepAlive(this);
        return (Gst.Rtp.RTPHeaderExtensionDirection)nativeResult;
    }

    /// <summary>The <c>gst_rtp_header_extension_get_id</c> function.</summary>
    /// <returns>the RTP extension id configured on @ext</returns>
    public uint GetId()
    {
        uint nativeResult = GstRtpHeaderExtensionGetId(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// This is used to know how much data a certain header extension will need for
    /// both allocating the resulting data, and deciding how much payload data can
    /// be generated.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Implementations should return as accurate a value as is possible using the
    /// information given in the input @buffer.
    /// </para>
    /// </remarks>
    /// <param name="inputMeta">The <c>inputMeta</c> argument.</param>
    /// <returns>the maximum size of the data written by this extension</returns>
    public nuint GetMaxSize(Gst.Buffer inputMeta)
    {
        ArgumentNullException.ThrowIfNull(inputMeta);
        nuint nativeResult = GstRtpHeaderExtensionGetMaxSize(Handle, inputMeta.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(inputMeta);
        return nativeResult;
    }

    /// <summary>The <c>gst_rtp_header_extension_get_sdp_caps_field_name</c> function.</summary>
    /// <returns>the #GstStructure field name used in SDP-like #GstCaps for this @ext configuration</returns>
    public string? GetSdpCapsFieldName()
    {
        nint nativeResult = GstRtpHeaderExtensionGetSdpCapsFieldName(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult);
    }

    /// <summary>The <c>gst_rtp_header_extension_get_supported_flags</c> function.</summary>
    /// <returns>the flags supported by this instance of @ext</returns>
    public Gst.Rtp.RTPHeaderExtensionFlags GetSupportedFlags()
    {
        int nativeResult = GstRtpHeaderExtensionGetSupportedFlags(Handle);
        System.GC.KeepAlive(this);
        return (Gst.Rtp.RTPHeaderExtensionFlags)nativeResult;
    }

    /// <summary>The <c>gst_rtp_header_extension_get_uri</c> function.</summary>
    /// <returns>the RTP extension URI for this object</returns>
    public string? GetUri()
    {
        nint nativeResult = GstRtpHeaderExtensionGetUri(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>Read the RTP header extension from @data.</summary>
    /// <param name="readFlags">The <c>readFlags</c> argument.</param>
    /// <param name="data">location to read the rtp header extension from</param>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <returns>whether the extension could be read from @data</returns>
    public bool Read(Gst.Rtp.RTPHeaderExtensionFlags readFlags, System.ReadOnlySpan<byte> data, Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        fixed (byte* dataPointer = data)
        {
            int nativeResult = GstRtpHeaderExtensionRead(Handle, (int)readFlags, dataPointer, (nuint)data.Length, buffer.Handle);
            System.GC.KeepAlive(this);
            System.GC.KeepAlive(buffer);
            return nativeResult != 0;
        }
    }

    /// <summary>
    /// gst_rtp_header_extension_set_id() must have been called with a valid
    /// extension id that is contained in these caps.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The only current known caps format is based on the SDP standard as produced
    /// by gst_sdp_media_attributes_to_caps().
    /// </para>
    /// </remarks>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>whether the @caps could be successfully set on @ext.</returns>
    public bool SetAttributesFromCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstRtpHeaderExtensionSetAttributesFromCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// gst_rtp_header_extension_set_id() must have been called with a valid
    /// extension id that is contained in these caps.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The only current known caps format is based on the SDP standard as produced
    /// by gst_sdp_media_attributes_to_caps().
    /// </para>
    /// </remarks>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>
    /// whether the configured attributes on @ext can successfully be set on
    /// 	@caps
    /// </returns>
    public bool SetCapsFromAttributes(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstRtpHeaderExtensionSetCapsFromAttributes(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// Helper implementation for GstRTPExtensionClass::set_caps_from_attributes
    /// that sets the @ext uri on caps with the specified extension id as required
    /// for sdp #GstCaps.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Requires that the extension does not have any attributes or direction
    /// advertised in @caps.
    /// </para>
    /// </remarks>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <param name="attributes">The <c>attributes</c> argument.</param>
    /// <returns>whether the @ext attributes could be set on @caps.</returns>
    public bool SetCapsFromAttributesHelper(Gst.Caps caps, string attributes)
    {
        ArgumentNullException.ThrowIfNull(caps);
        ArgumentNullException.ThrowIfNull(attributes);
        System.Span<byte> attributesBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope attributesScope = Gst.Interop.GMarshal.StackUtf8(attributes, attributesBuffer);
        int nativeResult = GstRtpHeaderExtensionSetCapsFromAttributesHelper(Handle, caps.Handle, attributesScope.Pointer);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// Set the direction that this header extension should be used in.
    /// If #GST_RTP_HEADER_EXTENSION_DIRECTION_INHERITED is included, the
    /// direction will not be included in the caps (as it shouldn't be in the
    /// extmap line in the SDP).
    /// </summary>
    /// <param name="direction">The <c>direction</c> argument.</param>
    public void SetDirection(Gst.Rtp.RTPHeaderExtensionDirection direction)
    {
        GstRtpHeaderExtensionSetDirection(Handle, (int)direction);
        System.GC.KeepAlive(this);
    }

    /// <summary>sets the RTP extension id on @ext</summary>
    /// <param name="extId">The <c>extId</c> argument.</param>
    public void SetId(uint extId)
    {
        GstRtpHeaderExtensionSetId(Handle, extId);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Passes RTP payloader's sink (i.e. not payloaded) @caps to the header
    /// extension.
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>Whether @caps could be read successfully</returns>
    public bool SetNonRtpSinkCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstRtpHeaderExtensionSetNonRtpSinkCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// Call this function in a subclass from #GstRTPHeaderExtensionClass::read to
    /// tell the depayloader whether the data just parsed from RTP packet require
    /// updating its src (non-RTP) caps. If @state is TRUE, #GstRTPBaseDepayload will
    /// eventually invoke gst_rtp_header_extension_update_non_rtp_src_caps() to
    /// have the caps update applied. Applying the update also flips the internal
    /// "wants update" flag back to FALSE.
    /// </summary>
    /// <param name="state">The <c>state</c> argument.</param>
    public void SetWantsUpdateNonRtpSrcCaps(bool state)
    {
        GstRtpHeaderExtensionSetWantsUpdateNonRtpSrcCaps(Handle, state ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Updates depayloader src caps based on the information received in RTP header.
    /// @caps must be writable as this function may modify them.
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>whether @caps were modified successfully</returns>
    public bool UpdateNonRtpSrcCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstRtpHeaderExtensionUpdateNonRtpSrcCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// Call this function after gst_rtp_header_extension_read() to check if
    /// the depayloader's src caps need updating with data received in the last RTP
    /// packet.
    /// </summary>
    /// <returns>Whether @ext wants to update depayloader's src caps.</returns>
    public bool WantsUpdateNonRtpSrcCaps()
    {
        int nativeResult = GstRtpHeaderExtensionWantsUpdateNonRtpSrcCaps(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Writes the RTP header extension to @data using information available from
    /// the @input_meta.  @data will be sized to be at least the value returned
    /// from gst_rtp_header_extension_get_max_size().
    /// </summary>
    /// <param name="inputMeta">The <c>inputMeta</c> argument.</param>
    /// <param name="writeFlags">The <c>writeFlags</c> argument.</param>
    /// <param name="output">The <c>output</c> argument.</param>
    /// <param name="data">location to write the rtp header extension into</param>
    /// <returns>the size of the data written, &lt; 0 on failure</returns>
    public nint Write(Gst.Buffer inputMeta, Gst.Rtp.RTPHeaderExtensionFlags writeFlags, Gst.Buffer output, System.Span<byte> data)
    {
        ArgumentNullException.ThrowIfNull(inputMeta);
        ArgumentNullException.ThrowIfNull(output);
        fixed (byte* dataPointer = data)
        {
            nint nativeResult = GstRtpHeaderExtensionWrite(Handle, inputMeta.Handle, (int)writeFlags, output.Handle, dataPointer, (nuint)data.Length);
            System.GC.KeepAlive(this);
            System.GC.KeepAlive(inputMeta);
            System.GC.KeepAlive(output);
            return nativeResult;
        }
    }

    /// <summary>The <c>gst_rtp_header_extension_create_from_uri</c> function.</summary>
    /// <param name="uri">The <c>uri</c> argument.</param>
    /// <returns>the #GstRTPHeaderExtension for @uri or %NULL</returns>
    public static Gst.Rtp.RTPHeaderExtension? CreateFromUri(string uri)
    {
        ArgumentNullException.ThrowIfNull(uri);
        System.Span<byte> uriBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope uriScope = Gst.Interop.GMarshal.StackUtf8(uri, uriBuffer);
        nint nativeResult = GstRtpHeaderExtensionCreateFromUri(uriScope.Pointer);
        return Gst.GObject.Object.FromNative<Gst.Rtp.RTPHeaderExtension>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_rtp_header_extension_get_direction</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_get_direction")]
    private static partial int GstRtpHeaderExtensionGetDirection(nint ext);

    /// <summary>The <c>gst_rtp_header_extension_get_id</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_get_id")]
    private static partial uint GstRtpHeaderExtensionGetId(nint ext);

    /// <summary>The <c>gst_rtp_header_extension_get_max_size</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_get_max_size")]
    private static partial nuint GstRtpHeaderExtensionGetMaxSize(nint ext, nint inputMeta);

    /// <summary>The <c>gst_rtp_header_extension_get_sdp_caps_field_name</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_get_sdp_caps_field_name")]
    private static partial nint GstRtpHeaderExtensionGetSdpCapsFieldName(nint ext);

    /// <summary>The <c>gst_rtp_header_extension_get_supported_flags</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_get_supported_flags")]
    private static partial int GstRtpHeaderExtensionGetSupportedFlags(nint ext);

    /// <summary>The <c>gst_rtp_header_extension_get_uri</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_get_uri")]
    private static partial nint GstRtpHeaderExtensionGetUri(nint ext);

    /// <summary>The <c>gst_rtp_header_extension_read</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_read")]
    private static partial int GstRtpHeaderExtensionRead(nint ext, int readFlags, byte* data, nuint size, nint buffer);

    /// <summary>The <c>gst_rtp_header_extension_set_attributes_from_caps</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_set_attributes_from_caps")]
    private static partial int GstRtpHeaderExtensionSetAttributesFromCaps(nint ext, nint caps);

    /// <summary>The <c>gst_rtp_header_extension_set_caps_from_attributes</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_set_caps_from_attributes")]
    private static partial int GstRtpHeaderExtensionSetCapsFromAttributes(nint ext, nint caps);

    /// <summary>The <c>gst_rtp_header_extension_set_caps_from_attributes_helper</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_set_caps_from_attributes_helper")]
    private static partial int GstRtpHeaderExtensionSetCapsFromAttributesHelper(nint ext, nint caps, byte* attributes);

    /// <summary>The <c>gst_rtp_header_extension_set_direction</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_set_direction")]
    private static partial void GstRtpHeaderExtensionSetDirection(nint ext, int direction);

    /// <summary>The <c>gst_rtp_header_extension_set_id</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_set_id")]
    private static partial void GstRtpHeaderExtensionSetId(nint ext, uint extId);

    /// <summary>The <c>gst_rtp_header_extension_set_non_rtp_sink_caps</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_set_non_rtp_sink_caps")]
    private static partial int GstRtpHeaderExtensionSetNonRtpSinkCaps(nint ext, nint caps);

    /// <summary>The <c>gst_rtp_header_extension_set_wants_update_non_rtp_src_caps</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_set_wants_update_non_rtp_src_caps")]
    private static partial void GstRtpHeaderExtensionSetWantsUpdateNonRtpSrcCaps(nint ext, int state);

    /// <summary>The <c>gst_rtp_header_extension_update_non_rtp_src_caps</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_update_non_rtp_src_caps")]
    private static partial int GstRtpHeaderExtensionUpdateNonRtpSrcCaps(nint ext, nint caps);

    /// <summary>The <c>gst_rtp_header_extension_wants_update_non_rtp_src_caps</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_wants_update_non_rtp_src_caps")]
    private static partial int GstRtpHeaderExtensionWantsUpdateNonRtpSrcCaps(nint ext);

    /// <summary>The <c>gst_rtp_header_extension_write</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_write")]
    private static partial nint GstRtpHeaderExtensionWrite(nint ext, nint inputMeta, int writeFlags, nint output, byte* data, nuint size);

    /// <summary>The <c>gst_rtp_header_extension_create_from_uri</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_create_from_uri")]
    private static partial nint GstRtpHeaderExtensionCreateFromUri(byte* uri);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRTPHeaderExtension</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtp_header_extension_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstRTPHeaderExtension</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : RTPHeaderExtension
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
