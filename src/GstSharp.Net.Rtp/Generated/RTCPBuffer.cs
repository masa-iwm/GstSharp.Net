// <auto-generated/>
// Generated by GstSharp.Generator from GstRtp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Rtp;

/// <summary>Note: The API in this module is not yet declared stable.</summary>
/// <remarks>
/// <para>
/// The GstRTPCBuffer helper functions makes it easy to parse and create regular
/// #GstBuffer objects that contain compound RTCP packets. These buffers are typically
/// of 'application/x-rtcp' #GstCaps.
/// </para>
/// <para>
/// An RTCP buffer consists of 1 or more #GstRTCPPacket structures that you can
/// retrieve with gst_rtcp_buffer_get_first_packet(). #GstRTCPPacket acts as a pointer
/// into the RTCP buffer; you can move to the next packet with
/// gst_rtcp_packet_move_to_next().
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
public unsafe partial struct RTCPBuffer
{
    /// <summary>The <c>buffer</c> field of <c>GstRTCPBuffer</c>.</summary>
    public nint BufferPtr;

    /// <summary>The <c>map</c> field of <c>GstRTCPBuffer</c>.</summary>
    public Gst.MapInfo Map;

    /// <summary>Reads the <c>buffer</c> field of <c>GstRTCPBuffer</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    /// <returns>The <c>buffer</c> field of <c>GstRTCPBuffer</c>.</returns>
    public Gst.Buffer? GetBuffer()
    {
        Gst.Buffer? value = Gst.Buffer.FromNative(BufferPtr, Gst.Interop.Transfer.None);
        return value;
    }

    /// <summary>
    /// Add a new packet of @type to @rtcp. @packet will point to the newly created
    /// packet.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Mutates this instance; call it on a variable, not on a copy returned by a
    /// property.
    /// </para>
    /// </remarks>
    /// <param name="type">The <c>type</c> argument.</param>
    /// <param name="packet">The <c>packet</c> argument.</param>
    /// <returns>
    /// %TRUE if the packet could be created. This function returns %FALSE
    /// if the max mtu is exceeded for the buffer.
    /// </returns>
    public bool AddPacket(Gst.Rtp.RTCPType type, out Gst.Rtp.RTCPPacket packet)
    {
        Gst.Rtp.RTCPPacket packetNative = default;
        fixed (Gst.Rtp.RTCPBuffer* self = &this)
        {
            int nativeResult = GstRtcpBufferAddPacket(self, (int)type, &packetNative);
            packet = packetNative;
            return nativeResult != 0;
        }
    }

    /// <summary>
    /// Initialize a new #GstRTCPPacket pointer that points to the first packet in
    /// @rtcp.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Mutates this instance; call it on a variable, not on a copy returned by a
    /// property.
    /// </para>
    /// </remarks>
    /// <param name="packet">The <c>packet</c> argument.</param>
    /// <returns>TRUE if the packet existed in @rtcp.</returns>
    public bool GetFirstPacket(out Gst.Rtp.RTCPPacket packet)
    {
        Gst.Rtp.RTCPPacket packetNative = default;
        fixed (Gst.Rtp.RTCPBuffer* self = &this)
        {
            int nativeResult = GstRtcpBufferGetFirstPacket(self, &packetNative);
            packet = packetNative;
            return nativeResult != 0;
        }
    }

    /// <summary>Get the number of RTCP packets in @rtcp.</summary>
    /// <remarks>
    /// <para>
    /// Mutates this instance; call it on a variable, not on a copy returned by a
    /// property.
    /// </para>
    /// </remarks>
    /// <returns>the number of RTCP packets in @rtcp.</returns>
    public uint GetPacketCount()
    {
        fixed (Gst.Rtp.RTCPBuffer* self = &this)
        {
            uint nativeResult = GstRtcpBufferGetPacketCount(self);
            return nativeResult;
        }
    }

    /// <summary>
    /// Finish @rtcp after being constructed. This function is usually called
    /// after gst_rtcp_buffer_map() and after adding the RTCP items to the new buffer.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The function adjusts the size of @rtcp with the total length of all the
    /// added packets.
    /// </para>
    /// <para>
    /// Mutates this instance; call it on a variable, not on a copy returned by a
    /// property.
    /// </para>
    /// </remarks>
    /// <returns>The result of <c>gst_rtcp_buffer_unmap</c>.</returns>
    public bool Unmap()
    {
        fixed (Gst.Rtp.RTCPBuffer* self = &this)
        {
            int nativeResult = GstRtcpBufferUnmap(self);
            return nativeResult != 0;
        }
    }

    /// <summary>
    /// Open @buffer for reading or writing, depending on @flags. The resulting RTCP
    /// buffer state is stored in @rtcp.
    /// </summary>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <param name="flags">The <c>flags</c> argument.</param>
    /// <param name="rtcp">The <c>rtcp</c> argument.</param>
    /// <returns>The result of <c>gst_rtcp_buffer_map</c>.</returns>
    public static bool MapBuffer(Gst.Buffer buffer, Gst.MapFlags flags, out Gst.Rtp.RTCPBuffer rtcp)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        Gst.Rtp.RTCPBuffer rtcpNative = default;
        int nativeResult = GstRtcpBufferMap(buffer.Handle, (int)flags, &rtcpNative);
        System.GC.KeepAlive(buffer);
        rtcp = rtcpNative;
        return nativeResult != 0;
    }

    /// <summary>
    /// Create a new buffer for constructing RTCP packets. The packet will have a
    /// maximum size of @mtu.
    /// </summary>
    /// <param name="mtu">The <c>mtu</c> argument.</param>
    /// <returns>A newly allocated buffer.</returns>
    public static Gst.Buffer New(uint mtu)
    {
        nint nativeResult = GstRtcpBufferNew(mtu);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_rtcp_buffer_new returned no value.");
    }

    /// <summary>
    /// Create a new buffer and set the data to a copy of @len
    /// bytes of @data and the size to @len. The data will be freed when the buffer
    /// is freed.
    /// </summary>
    /// <param name="data">data for the new buffer</param>
    /// <returns>A newly allocated buffer with a copy of @data and of size @len.</returns>
    public static Gst.Buffer NewCopyData(System.ReadOnlySpan<byte> data)
    {
        fixed (byte* dataPointer = data)
        {
            nint nativeResult = GstRtcpBufferNewCopyData(dataPointer, (uint)data.Length);
            return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full)
                ?? throw new InvalidOperationException("gst_rtcp_buffer_new_copy_data returned no value.");
        }
    }

    /// <summary>
    /// Create a new buffer and set the data and size of the buffer to @data and @len
    /// respectively. @data will be freed when the buffer is unreffed, so this
    /// function transfers ownership of @data to the new buffer.
    /// </summary>
    /// <param name="data">data for the new buffer</param>
    /// <returns>A newly allocated buffer with @data and of size @len.</returns>
    public static Gst.Buffer NewTakeData(System.Span<byte> data)
    {
        fixed (byte* dataPointer = data)
        {
            nint nativeResult = GstRtcpBufferNewTakeData(dataPointer, (uint)data.Length);
            return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full)
                ?? throw new InvalidOperationException("gst_rtcp_buffer_new_take_data returned no value.");
        }
    }

    /// <summary>
    /// Check if the data pointed to by @buffer is a valid RTCP packet using
    /// gst_rtcp_buffer_validate_data().
    /// </summary>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <returns>TRUE if @buffer is a valid RTCP packet.</returns>
    public static bool Validate(Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        int nativeResult = GstRtcpBufferValidate(buffer.Handle);
        System.GC.KeepAlive(buffer);
        return nativeResult != 0;
    }

    /// <summary>
    /// Check if the @data and @size point to the data of a valid compound,
    /// non-reduced size RTCP packet.
    /// Use this function to validate a packet before using the other functions in
    /// this module.
    /// </summary>
    /// <param name="data">the data to validate</param>
    /// <returns>TRUE if the data points to a valid RTCP packet.</returns>
    public static bool ValidateData(System.Span<byte> data)
    {
        fixed (byte* dataPointer = data)
        {
            int nativeResult = GstRtcpBufferValidateData(dataPointer, (uint)data.Length);
            return nativeResult != 0;
        }
    }

    /// <summary>
    /// Check if the @data and @size point to the data of a valid RTCP packet.
    /// Use this function to validate a packet before using the other functions in
    /// this module.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function is updated to support reduced size rtcp packets according to
    /// RFC 5506 and will validate full compound RTCP packets as well as reduced
    /// size RTCP packets.
    /// </para>
    /// </remarks>
    /// <param name="data">the data to validate</param>
    /// <returns>TRUE if the data points to a valid RTCP packet.</returns>
    public static bool ValidateDataReduced(System.Span<byte> data)
    {
        fixed (byte* dataPointer = data)
        {
            int nativeResult = GstRtcpBufferValidateDataReduced(dataPointer, (uint)data.Length);
            return nativeResult != 0;
        }
    }

    /// <summary>
    /// Check if the data pointed to by @buffer is a valid RTCP packet using
    /// gst_rtcp_buffer_validate_reduced().
    /// </summary>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <returns>TRUE if @buffer is a valid RTCP packet.</returns>
    public static bool ValidateReduced(Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        int nativeResult = GstRtcpBufferValidateReduced(buffer.Handle);
        System.GC.KeepAlive(buffer);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_rtcp_buffer_add_packet</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_add_packet")]
    private static partial int GstRtcpBufferAddPacket(Gst.Rtp.RTCPBuffer* rtcp, int type, Gst.Rtp.RTCPPacket* packet);

    /// <summary>The <c>gst_rtcp_buffer_get_first_packet</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_get_first_packet")]
    private static partial int GstRtcpBufferGetFirstPacket(Gst.Rtp.RTCPBuffer* rtcp, Gst.Rtp.RTCPPacket* packet);

    /// <summary>The <c>gst_rtcp_buffer_get_packet_count</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_get_packet_count")]
    private static partial uint GstRtcpBufferGetPacketCount(Gst.Rtp.RTCPBuffer* rtcp);

    /// <summary>The <c>gst_rtcp_buffer_unmap</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_unmap")]
    private static partial int GstRtcpBufferUnmap(Gst.Rtp.RTCPBuffer* rtcp);

    /// <summary>The <c>gst_rtcp_buffer_map</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_map")]
    private static partial int GstRtcpBufferMap(nint buffer, int flags, Gst.Rtp.RTCPBuffer* rtcp);

    /// <summary>The <c>gst_rtcp_buffer_new</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_new")]
    private static partial nint GstRtcpBufferNew(uint mtu);

    /// <summary>The <c>gst_rtcp_buffer_new_copy_data</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_new_copy_data")]
    private static partial nint GstRtcpBufferNewCopyData(byte* data, uint len);

    /// <summary>The <c>gst_rtcp_buffer_new_take_data</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_new_take_data")]
    private static partial nint GstRtcpBufferNewTakeData(byte* data, uint len);

    /// <summary>The <c>gst_rtcp_buffer_validate</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_validate")]
    private static partial int GstRtcpBufferValidate(nint buffer);

    /// <summary>The <c>gst_rtcp_buffer_validate_data</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_validate_data")]
    private static partial int GstRtcpBufferValidateData(byte* data, uint len);

    /// <summary>The <c>gst_rtcp_buffer_validate_data_reduced</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_validate_data_reduced")]
    private static partial int GstRtcpBufferValidateDataReduced(byte* data, uint len);

    /// <summary>The <c>gst_rtcp_buffer_validate_reduced</c> entry point.</summary>
    [LibraryImport("GstRtp", EntryPoint = "gst_rtcp_buffer_validate_reduced")]
    private static partial int GstRtcpBufferValidateReduced(nint buffer);
}
