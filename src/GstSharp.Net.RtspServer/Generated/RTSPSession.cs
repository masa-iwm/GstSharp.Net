// <auto-generated/>
// Generated by GstSharp.Generator from GstRtspServer-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.RtspServer;

/// <summary>
/// Session information kept by the server for a specific client.
/// One client session, identified with a session id, can handle multiple medias
/// identified with the url of a media.
/// </summary>
public unsafe partial class RTSPSession : Gst.GObject.Object
{
    /// <summary>Wraps a native <c>GstRTSPSession</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstRTSPSession</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected RTSPSession(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Create a new #GstRTSPSession instance with @sessionid.</summary>
    /// <param name="sessionid">a session id</param>
    /// <returns>a new #GstRTSPSession</returns>
    public static Gst.RtspServer.RTSPSession New(string sessionid)
    {
        ArgumentNullException.ThrowIfNull(sessionid);
        System.Span<byte> sessionidBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope sessionidScope = Gst.Interop.GMarshal.StackUtf8(sessionid, sessionidBuffer);
        nint nativeResult = GstRtspSessionNew(sessionidScope.Pointer);
        return Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSession>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_rtsp_session_new returned no value.");
    }

    /// <summary>
    /// Allow @session to expire. This method must be called an equal
    /// amount of time as gst_rtsp_session_prevent_expire().
    /// </summary>
    public void AllowExpire()
    {
        GstRtspSessionAllowExpire(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Gets the session media for @path, increasing its reference count. @matched
    /// will contain the number of matched characters of @path.
    /// </summary>
    /// <param name="path">the path for the media</param>
    /// <param name="matched">the amount of matched characters</param>
    /// <returns>
    /// the configuration for @path in @sess,
    /// should be unreferenced when no longer needed.
    /// </returns>
    public Gst.RtspServer.RTSPSessionMedia? DupMedia(string path, out int matched)
    {
        ArgumentNullException.ThrowIfNull(path);
        System.Span<byte> pathBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope pathScope = Gst.Interop.GMarshal.StackUtf8(path, pathBuffer);
        int matchedNative = default;
        nint nativeResult = GstRtspSessionDupMedia(Handle, pathScope.Pointer, &matchedNative);
        System.GC.KeepAlive(this);
        matched = matchedNative;
        return Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSessionMedia>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Call @func for each media in @sess. The result value of @func determines
    /// what happens to the media. @func will be called with @sess
    /// locked so no further actions on @sess can be performed from @func.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @func returns #GST_RTSP_FILTER_REMOVE, the media will be removed from
    /// @sess.
    /// </para>
    /// <para>If @func returns #GST_RTSP_FILTER_KEEP, the media will remain in @sess.</para>
    /// <para>
    /// If @func returns #GST_RTSP_FILTER_REF, the media will remain in @sess but
    /// will also be added with an additional ref to the result #GList of this
    /// function..
    /// </para>
    /// <para>When @func is %NULL, #GST_RTSP_FILTER_REF will be assumed for all media.</para>
    /// </remarks>
    /// <param name="func">a callback</param>
    /// <returns>
    /// a GList with all
    /// media for which @func returned #GST_RTSP_FILTER_REF. After usage, each
    /// element in the #GList should be unreffed before the list is freed.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.RtspServer.RTSPSessionMedia> Filter(Gst.RtspServer.RTSPSessionFilterFunc? func)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle funcState = func is null ? default : Gst.Interop.CallbackHandle.Alloc(func);
        try
        {
            nint nativeResult = GstRtspSessionFilter(instanceHandle, func is null ? 0 : Gst.RtspServer.RTSPSessionFilterFuncTrampoline.Pointer, funcState.UserData);
            System.GC.KeepAlive(this);
            nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
            System.Collections.Generic.List<Gst.RtspServer.RTSPSessionMedia> result = new(nativeItems.Length);
            foreach (nint nativeItem in nativeItems)
            {
                if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSessionMedia>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
                {
                    result.Add(adopted);
                }
            }

            return result;
        }
        finally
        {
            funcState.Free();
        }
    }

    /// <summary>Get the string that can be placed in the Session header field.</summary>
    /// <returns>
    /// the Session header of @session.
    /// g_free() after usage.
    /// </returns>
    public string? GetHeader()
    {
        nint nativeResult = GstRtspSessionGetHeader(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult);
    }

    /// <summary>
    /// Gets the session media for @path. @matched will contain the number of matched
    /// characters of @path.
    /// </summary>
    /// <param name="path">the path for the media</param>
    /// <param name="matched">the amount of matched characters</param>
    /// <returns>the configuration for @path in @sess.</returns>
    public Gst.RtspServer.RTSPSessionMedia? GetMedia(string path, out int matched)
    {
        ArgumentNullException.ThrowIfNull(path);
        System.Span<byte> pathBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope pathScope = Gst.Interop.GMarshal.StackUtf8(path, pathBuffer);
        int matchedNative = default;
        nint nativeResult = GstRtspSessionGetMedia(Handle, pathScope.Pointer, &matchedNative);
        System.GC.KeepAlive(this);
        matched = matchedNative;
        return Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSessionMedia>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Get the sessionid of @session.</summary>
    /// <returns>
    /// the sessionid of @session.
    /// The value remains valid as long as @session is alive.
    /// </returns>
    public string? GetSessionid()
    {
        nint nativeResult = GstRtspSessionGetSessionid(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>Get the timeout value of @session.</summary>
    /// <returns>the timeout of @session in seconds.</returns>
    public uint GetTimeout()
    {
        uint nativeResult = GstRtspSessionGetTimeout(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Check if @session timeout out.</summary>
    /// <param name="now">the current monotonic time</param>
    /// <returns>%TRUE if @session timed out</returns>
    public bool IsExpiredUsec(long now)
    {
        int nativeResult = GstRtspSessionIsExpiredUsec(Handle, now);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Manage the media object @obj in @sess. @path will be used to retrieve this
    /// media from the session with gst_rtsp_session_get_media().
    /// </summary>
    /// <remarks>
    /// <para>Ownership is taken from @media.</para>
    /// <para>
    /// The <c>media</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it. A
    /// GObject wrapper is interned, so disposing it gives the object up for the
    /// whole process rather than for one holder: after this call there is no
    /// wrapper for that object anywhere.
    /// <see cref="Gst.GObject.Object.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="path">the path for the media</param>
    /// <param name="media">
    /// a #GstRTSPMedia
    /// The call consumes it: <paramref name="media"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a new @GstRTSPSessionMedia object.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="media"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="media"/> was disposed.
    /// </exception>
    public Gst.RtspServer.RTSPSessionMedia ManageMedia(string path, Gst.RtspServer.RTSPMedia media)
    {
        ArgumentNullException.ThrowIfNull(path);
        ArgumentNullException.ThrowIfNull(media);
        nint instanceHandle = Handle;
        nint mediaNative = media.Handle;
        System.Span<byte> pathBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope pathScope = Gst.Interop.GMarshal.StackUtf8(path, pathBuffer);
        nint mediaOwned = Gst.Interop.GObjectNative.ObjectRef(mediaNative);
        nint nativeResult = GstRtspSessionManageMedia(instanceHandle, pathScope.Pointer, mediaOwned);
        System.GC.KeepAlive(this);
        media.Dispose();
        return Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSessionMedia>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_rtsp_session_manage_media returned no value.");
    }

    /// <summary>Get the amount of milliseconds till the session will expire.</summary>
    /// <param name="now">the current monotonic time</param>
    /// <returns>the amount of milliseconds since the session will time out.</returns>
    public int NextTimeoutUsec(long now)
    {
        int nativeResult = GstRtspSessionNextTimeoutUsec(Handle, now);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Prevent @session from expiring.</summary>
    public void PreventExpire()
    {
        GstRtspSessionPreventExpire(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Release the managed @media in @sess, freeing the memory allocated by it.</summary>
    /// <param name="media">a #GstRTSPMedia</param>
    /// <returns>%TRUE if there are more media session left in @sess.</returns>
    public bool ReleaseMedia(Gst.RtspServer.RTSPSessionMedia media)
    {
        ArgumentNullException.ThrowIfNull(media);
        int nativeResult = GstRtspSessionReleaseMedia(Handle, media.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(media);
        return nativeResult != 0;
    }

    /// <summary>
    /// Configure @session for a timeout of @timeout seconds. The session will be
    /// cleaned up when there is no activity for @timeout seconds.
    /// </summary>
    /// <param name="timeout">the new timeout</param>
    public void SetTimeout(uint timeout)
    {
        GstRtspSessionSetTimeout(Handle, timeout);
        System.GC.KeepAlive(this);
    }

    /// <summary>Update the last_access time of the session to the current time.</summary>
    public void Touch()
    {
        GstRtspSessionTouch(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>sessionid</c> property.</summary>
    public string? Sessionid => GetSessionid();

    /// <summary>The <c>timeout</c> property.</summary>
    public uint Timeout
    {
        get => GetTimeout();
        set => SetTimeout(value);
    }

    /// <summary>The <c>extra-timeout</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public uint ExtraTimeout
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("extra-timeout");
            return holder.GetUInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("extra-timeout");
            holder.SetUInt(value);
            SetPropertyValue("extra-timeout", in holder);
        }
    }

    /// <summary>The <c>timeout-always-visible</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool TimeoutAlwaysVisible
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("timeout-always-visible");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("timeout-always-visible");
            holder.SetBoolean(value);
            SetPropertyValue("timeout-always-visible", in holder);
        }
    }

    /// <summary>The <c>gst_rtsp_session_new</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_new")]
    private static partial nint GstRtspSessionNew(byte* sessionid);

    /// <summary>The <c>gst_rtsp_session_allow_expire</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_allow_expire")]
    private static partial void GstRtspSessionAllowExpire(nint session);

    /// <summary>The <c>gst_rtsp_session_dup_media</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_dup_media")]
    private static partial nint GstRtspSessionDupMedia(nint sess, byte* path, int* matched);

    /// <summary>The <c>gst_rtsp_session_filter</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_filter")]
    private static partial nint GstRtspSessionFilter(nint sess, nint func, nint userData);

    /// <summary>The <c>gst_rtsp_session_get_header</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_get_header")]
    private static partial nint GstRtspSessionGetHeader(nint session);

    /// <summary>The <c>gst_rtsp_session_get_media</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_get_media")]
    private static partial nint GstRtspSessionGetMedia(nint sess, byte* path, int* matched);

    /// <summary>The <c>gst_rtsp_session_get_sessionid</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_get_sessionid")]
    private static partial nint GstRtspSessionGetSessionid(nint session);

    /// <summary>The <c>gst_rtsp_session_get_timeout</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_get_timeout")]
    private static partial uint GstRtspSessionGetTimeout(nint session);

    /// <summary>The <c>gst_rtsp_session_is_expired_usec</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_is_expired_usec")]
    private static partial int GstRtspSessionIsExpiredUsec(nint session, long now);

    /// <summary>The <c>gst_rtsp_session_manage_media</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_manage_media")]
    private static partial nint GstRtspSessionManageMedia(nint sess, byte* path, nint media);

    /// <summary>The <c>gst_rtsp_session_next_timeout_usec</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_next_timeout_usec")]
    private static partial int GstRtspSessionNextTimeoutUsec(nint session, long now);

    /// <summary>The <c>gst_rtsp_session_prevent_expire</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_prevent_expire")]
    private static partial void GstRtspSessionPreventExpire(nint session);

    /// <summary>The <c>gst_rtsp_session_release_media</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_release_media")]
    private static partial int GstRtspSessionReleaseMedia(nint sess, nint media);

    /// <summary>The <c>gst_rtsp_session_set_timeout</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_set_timeout")]
    private static partial void GstRtspSessionSetTimeout(nint session, uint timeout);

    /// <summary>The <c>gst_rtsp_session_touch</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_touch")]
    private static partial void GstRtspSessionTouch(nint session);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRTSPSession</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new RTSPSession(handle, transfer);
}
