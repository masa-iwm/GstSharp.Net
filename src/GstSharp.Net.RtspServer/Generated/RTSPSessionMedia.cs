// <auto-generated/>
// Generated by GstSharp.Generator from GstRtspServer-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.RtspServer;

/// <summary>State of a client session regarding a specific media identified by path.</summary>
public unsafe partial class RTSPSessionMedia : Gst.GObject.Object
{
    /// <summary>Wraps a native <c>GstRTSPSessionMedia</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstRTSPSessionMedia</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected RTSPSessionMedia(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Create a new #GstRTSPSessionMedia that manages the streams
    /// in @media for @path. @media should be prepared.
    /// </summary>
    /// <remarks>
    /// <para>Ownership is taken of @media.</para>
    /// <para>
    /// Where the documentation above tells the caller to give the argument up —
    /// not to use it after the call, or to take a reference of its own first —
    /// that is the rule for a C caller, whose own reference the call took: this
    /// binding is not that caller.
    /// The call takes <paramref name="media"/> over: the library is handed a
    /// reference of its own, minted for this call, and keeps it for as long as it
    /// needs the object. This wrapper keeps the reference it holds, so it stays
    /// usable after the call and the handlers connected to it keep firing.
    /// When the call refuses the argument — the <see langword="null"/> the C answers is
    /// that refusal — the reference minted for it is released again, so nothing
    /// is leaked.
    /// </para>
    /// </remarks>
    /// <param name="path">the path</param>
    /// <param name="media">
    /// the #GstRTSPMedia
    /// The call is handed a reference of its own: <paramref name="media"/> stays
    /// usable after this method returns.
    /// </param>
    /// <returns>a new #GstRTSPSessionMedia.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="media"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// <paramref name="media"/> was disposed.
    /// </exception>
    public static Gst.RtspServer.RTSPSessionMedia New(string path, Gst.RtspServer.RTSPMedia media)
    {
        ArgumentNullException.ThrowIfNull(path);
        ArgumentNullException.ThrowIfNull(media);
        nint mediaNative = media.Handle;
        System.Span<byte> pathBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope pathScope = Gst.Interop.GMarshal.StackUtf8(path, pathBuffer);
        nint mediaOwned = Gst.Interop.GObjectNative.ObjectRef(mediaNative);
        nint nativeResult = GstRtspSessionMediaNew(pathScope.Pointer, mediaOwned);
        if (nativeResult == 0)
        {
            // The call refused the argument, which is the C stating that it did
            // not take the reference minted for it. Releasing it here is what
            // keeps a refusal from leaking one; the result is handed on unchanged.
            Gst.Interop.GObjectNative.ObjectUnref(mediaOwned);
        }
        Gst.RtspServer.RTSPSessionMedia result = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSessionMedia>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_rtsp_session_media_new returned no value.");
        System.GC.KeepAlive(media);
        return result;
    }

    /// <summary>
    /// Fill @range with the next available min and max channels for
    /// interleaved transport.
    /// </summary>
    /// <param name="range">a #GstRTSPRange</param>
    /// <returns>%TRUE on success.</returns>
    public bool AllocChannels(out Gst.Rtsp.RTSPRange range)
    {
        Gst.Rtsp.RTSPRange rangeNative = default;
        int nativeResult = GstRtspSessionMediaAllocChannels(Handle, &rangeNative);
        range = rangeNative;
        bool result = nativeResult != 0;
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>Get the base_time of the #GstRTSPMedia in @media</summary>
    /// <returns>the base_time of the media.</returns>
    public Gst.ClockTime GetBaseTime()
    {
        ulong nativeResult = GstRtspSessionMediaGetBaseTime(Handle);
        Gst.ClockTime result = new Gst.ClockTime(nativeResult);
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>Get the #GstRTSPMedia that was used when constructing @media</summary>
    /// <returns>
    /// the #GstRTSPMedia of @media.
    /// Remains valid as long as @media is valid.
    /// </returns>
    public Gst.RtspServer.RTSPMedia? GetMedia()
    {
        nint nativeResult = GstRtspSessionMediaGetMedia(Handle);
        Gst.RtspServer.RTSPMedia? result = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPMedia>(nativeResult, Gst.Interop.Transfer.None);
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>
    /// Retrieve the RTP-Info header string for all streams in @media
    /// with configured transports.
    /// </summary>
    /// <returns>
    /// The RTP-Info as a string or
    /// %NULL when no RTP-Info could be generated, g_free() after usage.
    /// </returns>
    public string? GetRtpinfo()
    {
        nint nativeResult = GstRtspSessionMediaGetRtpinfo(Handle);
        string? result = Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult);
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>Get the current RTSP state of @media.</summary>
    /// <returns>the current RTSP state of @media.</returns>
    public Gst.Rtsp.RTSPState GetRtspState()
    {
        int nativeResult = GstRtspSessionMediaGetRtspState(Handle);
        Gst.Rtsp.RTSPState result = (Gst.Rtsp.RTSPState)nativeResult;
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>Get a previously created #GstRTSPStreamTransport for the stream at @idx.</summary>
    /// <param name="idx">the stream index</param>
    /// <returns>
    /// a #GstRTSPStreamTransport that is
    /// valid until the session of @media is unreffed.
    /// </returns>
    public Gst.RtspServer.RTSPStreamTransport? GetTransport(uint idx)
    {
        nint nativeResult = GstRtspSessionMediaGetTransport(Handle, idx);
        Gst.RtspServer.RTSPStreamTransport? result = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPStreamTransport>(nativeResult, Gst.Interop.Transfer.None);
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>
    /// Check if the path of @media matches @path. It @path matches, the amount of
    /// matched characters is returned in @matched.
    /// </summary>
    /// <param name="path">a path</param>
    /// <param name="matched">the amount of matched characters of @path</param>
    /// <returns>%TRUE when @path matches the path of @media.</returns>
    public bool Matches(string path, out int matched)
    {
        ArgumentNullException.ThrowIfNull(path);
        System.Span<byte> pathBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope pathScope = Gst.Interop.GMarshal.StackUtf8(path, pathBuffer);
        int matchedNative = default;
        int nativeResult = GstRtspSessionMediaMatches(Handle, pathScope.Pointer, &matchedNative);
        matched = matchedNative;
        bool result = nativeResult != 0;
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>Set the RTSP state of @media to @state.</summary>
    /// <param name="state">a #GstRTSPState</param>
    public void SetRtspState(Gst.Rtsp.RTSPState state)
    {
        GstRtspSessionMediaSetRtspState(Handle, (int)state);
        System.GC.KeepAlive(this);
    }

    /// <summary>Tell the media object @media to change to @state.</summary>
    /// <param name="state">the new state</param>
    /// <returns>%TRUE on success.</returns>
    public bool SetState(Gst.State state)
    {
        int nativeResult = GstRtspSessionMediaSetState(Handle, (int)state);
        bool result = nativeResult != 0;
        System.GC.KeepAlive(this);
        return result;
    }

    /// <summary>The <c>gst_rtsp_session_media_new</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_new")]
    private static partial nint GstRtspSessionMediaNew(byte* path, nint media);

    /// <summary>The <c>gst_rtsp_session_media_alloc_channels</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_alloc_channels")]
    private static partial int GstRtspSessionMediaAllocChannels(nint media, Gst.Rtsp.RTSPRange* range);

    /// <summary>The <c>gst_rtsp_session_media_get_base_time</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_get_base_time")]
    private static partial ulong GstRtspSessionMediaGetBaseTime(nint media);

    /// <summary>The <c>gst_rtsp_session_media_get_media</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_get_media")]
    private static partial nint GstRtspSessionMediaGetMedia(nint media);

    /// <summary>The <c>gst_rtsp_session_media_get_rtpinfo</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_get_rtpinfo")]
    private static partial nint GstRtspSessionMediaGetRtpinfo(nint media);

    /// <summary>The <c>gst_rtsp_session_media_get_rtsp_state</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_get_rtsp_state")]
    private static partial int GstRtspSessionMediaGetRtspState(nint media);

    /// <summary>The <c>gst_rtsp_session_media_get_transport</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_get_transport")]
    private static partial nint GstRtspSessionMediaGetTransport(nint media, uint idx);

    /// <summary>The <c>gst_rtsp_session_media_matches</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_matches")]
    private static partial int GstRtspSessionMediaMatches(nint media, byte* path, int* matched);

    /// <summary>The <c>gst_rtsp_session_media_set_rtsp_state</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_set_rtsp_state")]
    private static partial void GstRtspSessionMediaSetRtspState(nint media, int state);

    /// <summary>The <c>gst_rtsp_session_media_set_state</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_set_state")]
    private static partial int GstRtspSessionMediaSetState(nint media, int state);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRTSPSessionMedia</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_session_media_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new RTSPSessionMedia(handle, transfer);
}
