// <auto-generated/>
// Generated by GstSharp.Generator from GstRtspServer-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.RtspServer;

/// <summary>
/// The opaque permissions structure. It is used to define the permissions
/// of objects in different roles.
/// </summary>
public sealed unsafe partial class RTSPPermissions : Gst.MiniObject
{
    /// <summary>Wraps a native <c>GstRTSPPermissions</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal RTSPPermissions(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Wraps a <c>GstRTSPPermissions</c> that GStreamer keeps owning, for the length of one call.</summary>
    /// <param name="handle">The mini object that is lent to managed code.</param>
    /// <returns>The wrapper, which holds no reference of its own.</returns>
    /// <remarks>
    /// This is how the override of a virtual method receives a <c>transfer none</c>
    /// mini object. The wrapper takes no reference, so the object stays as writable
    /// as GStreamer handed it over, and disposing the wrapper only detaches it: a
    /// wrapper kept past the call throws instead of releasing a reference it never
    /// took. See <see cref="Gst.Interop.Borrowed"/>.
    /// </remarks>
    internal static RTSPPermissions Borrow(nint handle) => new(new Gst.Interop.Borrowed(handle));

    /// <summary>Wraps a <c>GstRTSPPermissions</c> the caller keeps owning.</summary>
    /// <param name="borrowed">The mini object that is lent to managed code.</param>
    private RTSPPermissions(Gst.Interop.Borrowed borrowed)
        : base(borrowed)
    {
    }

    /// <summary>Wraps a native <c>GstRTSPPermissions</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static RTSPPermissions? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>Create a new empty Authorization permissions.</summary>
    /// <returns>a new empty authorization permissions.</returns>
    public static Gst.RtspServer.RTSPPermissions New()
    {
        nint nativeResult = GstRtspPermissionsNew();
        return Gst.RtspServer.RTSPPermissions.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_rtsp_permissions_new returned no value.");
    }

    /// <summary>Add a new @permission for @role to @permissions with the access in @allowed.</summary>
    /// <param name="role">The <c>role</c> argument.</param>
    /// <param name="permission">The <c>permission</c> argument.</param>
    /// <param name="allowed">The <c>allowed</c> argument.</param>
    public void AddPermissionForRole(string role, string permission, bool allowed)
    {
        ArgumentNullException.ThrowIfNull(role);
        System.Span<byte> roleBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope roleScope = Gst.Interop.GMarshal.StackUtf8(role, roleBuffer);
        ArgumentNullException.ThrowIfNull(permission);
        System.Span<byte> permissionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope permissionScope = Gst.Interop.GMarshal.StackUtf8(permission, permissionBuffer);
        GstRtspPermissionsAddPermissionForRole(Handle, roleScope.Pointer, permissionScope.Pointer, allowed ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Add a new @role to @permissions without any permissions. You can add
    /// permissions for the role with gst_rtsp_permissions_add_permission_for_role().
    /// </summary>
    /// <param name="role">The <c>role</c> argument.</param>
    public void AddRole(string role)
    {
        ArgumentNullException.ThrowIfNull(role);
        System.Span<byte> roleBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope roleScope = Gst.Interop.GMarshal.StackUtf8(role, roleBuffer);
        GstRtspPermissionsAddRoleEmpty(Handle, roleScope.Pointer);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Add a new role to @permissions based on @structure, for example
    /// given a role named `tester`, which should be granted a permission named
    /// `permission1`, the structure could be created with:
    /// </summary>
    /// <remarks>
    /// <para>
    /// <code>
    /// gst_structure_new ("tester", "permission1", G_TYPE_BOOLEAN, TRUE, NULL);
    /// </code>
    /// </para>
    /// </remarks>
    /// <param name="structure">The <c>structure</c> argument.</param>
    public void AddRoleFromStructure(Gst.Structure structure)
    {
        ArgumentNullException.ThrowIfNull(structure);
        GstRtspPermissionsAddRoleFromStructure(Handle, structure.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(structure);
    }

    /// <summary>Get all permissions for @role in @permissions.</summary>
    /// <param name="role">The <c>role</c> argument.</param>
    /// <returns>
    /// the structure with permissions for @role. It
    /// remains valid for as long as @permissions is valid.
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Structure? GetRole(string role)
    {
        ArgumentNullException.ThrowIfNull(role);
        System.Span<byte> roleBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope roleScope = Gst.Interop.GMarshal.StackUtf8(role, roleBuffer);
        nint nativeResult = GstRtspPermissionsGetRole(Handle, roleScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.Structure.FromNative(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Check if @role in @permissions is given permission for @permission.</summary>
    /// <param name="role">The <c>role</c> argument.</param>
    /// <param name="permission">The <c>permission</c> argument.</param>
    /// <returns>%TRUE if @role is allowed @permission.</returns>
    public bool IsAllowed(string role, string permission)
    {
        ArgumentNullException.ThrowIfNull(role);
        System.Span<byte> roleBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope roleScope = Gst.Interop.GMarshal.StackUtf8(role, roleBuffer);
        ArgumentNullException.ThrowIfNull(permission);
        System.Span<byte> permissionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope permissionScope = Gst.Interop.GMarshal.StackUtf8(permission, permissionBuffer);
        int nativeResult = GstRtspPermissionsIsAllowed(Handle, roleScope.Pointer, permissionScope.Pointer);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Remove all permissions for @role in @permissions.</summary>
    /// <param name="role">The <c>role</c> argument.</param>
    public void RemoveRole(string role)
    {
        ArgumentNullException.ThrowIfNull(role);
        System.Span<byte> roleBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope roleScope = Gst.Interop.GMarshal.StackUtf8(role, roleBuffer);
        GstRtspPermissionsRemoveRole(Handle, roleScope.Pointer);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_rtsp_permissions_new</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_new")]
    private static partial nint GstRtspPermissionsNew();

    /// <summary>The <c>gst_rtsp_permissions_add_permission_for_role</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_add_permission_for_role")]
    private static partial void GstRtspPermissionsAddPermissionForRole(nint permissions, byte* role, byte* permission, int allowed);

    /// <summary>The <c>gst_rtsp_permissions_add_role_empty</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_add_role_empty")]
    private static partial void GstRtspPermissionsAddRoleEmpty(nint permissions, byte* role);

    /// <summary>The <c>gst_rtsp_permissions_add_role_from_structure</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_add_role_from_structure")]
    private static partial void GstRtspPermissionsAddRoleFromStructure(nint permissions, nint structure);

    /// <summary>The <c>gst_rtsp_permissions_get_role</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_get_role")]
    private static partial nint GstRtspPermissionsGetRole(nint permissions, byte* role);

    /// <summary>The <c>gst_rtsp_permissions_is_allowed</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_is_allowed")]
    private static partial int GstRtspPermissionsIsAllowed(nint permissions, byte* role, byte* permission);

    /// <summary>The <c>gst_rtsp_permissions_remove_role</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_remove_role")]
    private static partial void GstRtspPermissionsRemoveRole(nint permissions, byte* role);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRTSPPermissions</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_permissions_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new RTSPPermissions(handle, transfer);
}

/// <summary>The native layout of <c>GstRTSPPermissions</c>.</summary>
/// <remarks>
/// <para>
/// The mirror is only ever read through a pointer into memory that GStreamer
/// owns; it is never allocated, assigned or copied.
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
internal unsafe struct RTSPPermissionsRaw
{
    /// <summary>The <c>mini_object</c> field.</summary>
    internal Gst.MiniObjectRaw MiniObject;
}
