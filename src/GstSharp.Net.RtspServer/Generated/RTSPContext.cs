// <auto-generated/>
// Generated by GstSharp.Generator from GstRtspServer-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace Gst.RtspServer;

/// <summary>Information passed around containing the context of a request.</summary>
[StructLayout(LayoutKind.Sequential)]
public unsafe partial struct RTSPContext
{
    /// <summary>the server</summary>
    public nint ServerPtr;

    /// <summary>the connection</summary>
    public nint ConnPtr;

    /// <summary>the client</summary>
    public nint ClientPtr;

    /// <summary>the complete request</summary>
    public nint RequestPtr;

    /// <summary>the complete url parsed from @request</summary>
    public nint UriPtr;

    // <c>GstRTSPMethod</c> is not generated in this module; the field keeps its underlying type.
    /// <summary>the parsed method of @uri</summary>
    public int Method;

    /// <summary>the current auth object or %NULL</summary>
    public nint AuthPtr;

    /// <summary>authorisation token</summary>
    public nint TokenPtr;

    /// <summary>the session, can be %NULL</summary>
    public nint SessionPtr;

    /// <summary>the session media for the url can be %NULL</summary>
    public nint SessmediaPtr;

    /// <summary>the media factory for the url, can be %NULL</summary>
    public nint FactoryPtr;

    /// <summary>the media for the url can be %NULL</summary>
    public nint MediaPtr;

    /// <summary>the stream for the url can be %NULL</summary>
    public nint StreamPtr;

    /// <summary>the response</summary>
    public nint ResponsePtr;

    /// <summary>the stream transport, can be %NULL</summary>
    public nint TransPtr;

    /// <summary>The <c>_gst_reserved</c> field of <c>GstRTSPContext</c>.</summary>
    private GstReservedArray _gstReserved;

    /// <summary>Inline storage of the 3 elements of the <c>_gst_reserved</c> field of <c>GstRTSPContext</c>.</summary>
    [InlineArray(3)]
    private struct GstReservedArray
    {
        private nint _element0;
    }

    /// <summary>the server</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPServer? Server
    {
        get
        {
            Gst.RtspServer.RTSPServer? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPServer>(ServerPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>the connection</summary>
    /// <remarks>
    /// <para>
    /// The value is borrowed from the structure that declares the field: the
    /// wrapper of an opaque record takes no part in the ownership of what it
    /// points at, there is nothing to dispose, and it is only good for as long
    /// as the structure that holds the field is.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.Rtsp.RTSPConnection? Conn
    {
        get
        {
            Gst.Rtsp.RTSPConnection? value = Gst.Rtsp.RTSPConnection.FromNative(ConnPtr);
            return value;
        }
    }

    /// <summary>the client</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPClient? Client
    {
        get
        {
            Gst.RtspServer.RTSPClient? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPClient>(ClientPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>Reads the <c>request</c> field of <c>GstRTSPContext</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    /// <returns>the complete request</returns>
    public Gst.Rtsp.RTSPMessage? GetRequest()
    {
        Gst.Rtsp.RTSPMessage? value = Gst.Rtsp.RTSPMessage.FromNative(RequestPtr, Gst.Interop.Transfer.None);
        return value;
    }

    /// <summary>Reads the <c>uri</c> field of <c>GstRTSPContext</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    /// <returns>the complete url parsed from @request</returns>
    public Gst.Rtsp.RTSPUrl? GetUri()
    {
        Gst.Rtsp.RTSPUrl? value = Gst.Rtsp.RTSPUrl.FromNative(UriPtr, Gst.Interop.Transfer.None);
        return value;
    }

    /// <summary>the current auth object or %NULL</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPAuth? Auth
    {
        get
        {
            Gst.RtspServer.RTSPAuth? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPAuth>(AuthPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>Reads the <c>token</c> field of <c>GstRTSPContext</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    /// <returns>authorisation token</returns>
    public Gst.RtspServer.RTSPToken? GetToken()
    {
        Gst.RtspServer.RTSPToken? value = Gst.RtspServer.RTSPToken.FromNative(TokenPtr, Gst.Interop.Transfer.None);
        return value;
    }

    /// <summary>the session, can be %NULL</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPSession? Session
    {
        get
        {
            Gst.RtspServer.RTSPSession? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSession>(SessionPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>the session media for the url can be %NULL</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPSessionMedia? Sessmedia
    {
        get
        {
            Gst.RtspServer.RTSPSessionMedia? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPSessionMedia>(SessmediaPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>the media factory for the url, can be %NULL</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPMediaFactory? Factory
    {
        get
        {
            Gst.RtspServer.RTSPMediaFactory? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPMediaFactory>(FactoryPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>the media for the url can be %NULL</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPMedia? Media
    {
        get
        {
            Gst.RtspServer.RTSPMedia? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPMedia>(MediaPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>the stream for the url can be %NULL</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPStream? Stream
    {
        get
        {
            Gst.RtspServer.RTSPStream? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPStream>(StreamPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>Reads the <c>response</c> field of <c>GstRTSPContext</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    /// <returns>the response</returns>
    public Gst.Rtsp.RTSPMessage? GetResponse()
    {
        Gst.Rtsp.RTSPMessage? value = Gst.Rtsp.RTSPMessage.FromNative(ResponsePtr, Gst.Interop.Transfer.None);
        return value;
    }

    /// <summary>the stream transport, can be %NULL</summary>
    /// <remarks>
    /// <para>
    /// The object is read out of the structure at the moment of the call. The
    /// wrapper owns a reference of its own and stays valid afterwards; it is
    /// the instance every other lookup of the same object hands out, so
    /// disposing it releases the reference for all of them.
    /// </para>
    /// <para>
    /// The structure this is read out of is storage the caller holds. A library
    /// call that fills one for the length of a single scope - a mapping, or a
    /// metadata transform - leaves nothing behind in it, so the read has to
    /// happen inside that scope.
    /// </para>
    /// </remarks>
    public Gst.RtspServer.RTSPStreamTransport? Trans
    {
        get
        {
            Gst.RtspServer.RTSPStreamTransport? value = Gst.GObject.Object.FromNative<Gst.RtspServer.RTSPStreamTransport>(TransPtr, Gst.Interop.Transfer.None);
            return value;
        }
    }

    /// <summary>
    /// Pops @ctx off the context stack (verifying that @ctx
    /// is on the top of the stack).
    /// </summary>
    /// <remarks>
    /// <para>
    /// Mutates this instance; call it on a variable, not on a copy returned by a
    /// property.
    /// </para>
    /// </remarks>
    public void PopCurrent()
    {
        fixed (Gst.RtspServer.RTSPContext* self = &this)
        {
            GstRtspContextPopCurrent(self);
        }
    }

    /// <summary>
    /// Pushes @ctx onto the context stack. The current
    /// context can then be received using gst_rtsp_context_get_current().
    /// </summary>
    /// <remarks>
    /// <para>
    /// Mutates this instance; call it on a variable, not on a copy returned by a
    /// property.
    /// </para>
    /// </remarks>
    public void PushCurrent()
    {
        fixed (Gst.RtspServer.RTSPContext* self = &this)
        {
            GstRtspContextPushCurrent(self);
        }
    }

    /// <summary>Set the token for @ctx.</summary>
    /// <remarks>
    /// <para>
    /// Mutates this instance; call it on a variable, not on a copy returned by a
    /// property.
    /// </para>
    /// </remarks>
    /// <param name="token">a #GstRTSPToken</param>
    public void SetToken(Gst.RtspServer.RTSPToken token)
    {
        ArgumentNullException.ThrowIfNull(token);
        fixed (Gst.RtspServer.RTSPContext* self = &this)
        {
            GstRtspContextSetToken(self, token.Handle);
            System.GC.KeepAlive(token);
        }
    }

    /// <summary>The <c>gst_rtsp_context_pop_current</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_context_pop_current")]
    private static partial void GstRtspContextPopCurrent(Gst.RtspServer.RTSPContext* ctx);

    /// <summary>The <c>gst_rtsp_context_push_current</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_context_push_current")]
    private static partial void GstRtspContextPushCurrent(Gst.RtspServer.RTSPContext* ctx);

    /// <summary>The <c>gst_rtsp_context_set_token</c> entry point.</summary>
    [LibraryImport("GstRtspServer", EntryPoint = "gst_rtsp_context_set_token")]
    private static partial void GstRtspContextSetToken(Gst.RtspServer.RTSPContext* ctx, nint token);
}
