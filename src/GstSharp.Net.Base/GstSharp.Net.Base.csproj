<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Gst.Base</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../GstSharp.Net/GstSharp.Net.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The ABI probes read the class struct mirrors of this module and assert
         them against the running library, which is what they are there for. -->
    <InternalsVisibleTo Include="GstSharp.IntegrationTests" />
    <InternalsVisibleTo Include="GstSharp.Net.App" />
    <InternalsVisibleTo Include="GstSharp.Net.Audio" />
    <InternalsVisibleTo Include="GstSharp.Net.Video" />
    <InternalsVisibleTo Include="GstSharp.Net.Pbutils" />
  </ItemGroup>

</Project>
