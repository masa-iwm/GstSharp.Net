// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <summary>
/// #GstBaseSink is the base class for sink elements in GStreamer, such as
/// xvimagesink or filesink. It is a layer on top of #GstElement that provides a
/// simplified interface to plugin writers. #GstBaseSink handles many details
/// for you, for example: preroll, clock synchronization, state changes,
/// activation in push or pull mode, and queries.
/// </summary>
/// <remarks>
/// <para>
/// In most cases, when writing sink elements, there is no need to implement
/// class methods from #GstElement or to set functions on pads, because the
/// #GstBaseSink infrastructure should be sufficient.
/// </para>
/// <para>
/// #GstBaseSink provides support for exactly one sink pad, which should be
/// named "sink". A sink implementation (subclass of #GstBaseSink) should
/// install a pad template in its class_init function, like so:
/// |[&lt;!-- language="C" --&gt;
/// static void
/// my_element_class_init (GstMyElementClass *klass)
/// {
///   GstElementClass *gstelement_class = GST_ELEMENT_CLASS (klass);
/// </para>
/// <para>
///   // sinktemplate should be a #GstStaticPadTemplate with direction
///   // %GST_PAD_SINK and name "sink"
///   gst_element_class_add_static_pad_template (gstelement_class, &amp;sinktemplate);
/// </para>
/// <para>
///   gst_element_class_set_static_metadata (gstelement_class,
///       "Sink name",
///       "Sink",
///       "My Sink element",
///       "The author &lt;my.sink@my.email&gt;");
/// }
/// ]|
/// </para>
/// <para>
/// #GstBaseSink will handle the prerolling correctly. This means that it will
/// return %GST_STATE_CHANGE_ASYNC from a state change to PAUSED until the first
/// buffer arrives in this element. The base class will call the
/// #GstBaseSinkClass::preroll vmethod with this preroll buffer and will then
/// commit the state change to the next asynchronously pending state.
/// </para>
/// <para>
/// When the element is set to PLAYING, #GstBaseSink will synchronise on the
/// clock using the times returned from #GstBaseSinkClass::get_times. If this
/// function returns %GST_CLOCK_TIME_NONE for the start time, no synchronisation
/// will be done. Synchronisation can be disabled entirely by setting the object
/// #GstBaseSink:sync property to %FALSE.
/// </para>
/// <para>
/// After synchronisation the virtual method #GstBaseSinkClass::render will be
/// called. Subclasses should minimally implement this method.
/// </para>
/// <para>
/// Subclasses that synchronise on the clock in the #GstBaseSinkClass::render
/// method are supported as well. These classes typically receive a buffer in
/// the render method and can then potentially block on the clock while
/// rendering. A typical example is an audiosink.
/// These subclasses can use gst_base_sink_wait_preroll() to perform the
/// blocking wait.
/// </para>
/// <para>
/// Upon receiving the EOS event in the PLAYING state, #GstBaseSink will wait
/// for the clock to reach the time indicated by the stop time of the last
/// #GstBaseSinkClass::get_times call before posting an EOS message. When the
/// element receives EOS in PAUSED, preroll completes, the event is queued and an
/// EOS message is posted when going to PLAYING.
/// </para>
/// <para>
/// #GstBaseSink will internally use the %GST_EVENT_SEGMENT events to schedule
/// synchronisation and clipping of buffers. Buffers that fall completely outside
/// of the current segment are dropped. Buffers that fall partially in the
/// segment are rendered (and prerolled). Subclasses should do any subbuffer
/// clipping themselves when needed.
/// </para>
/// <para>
/// #GstBaseSink will by default report the current playback position in
/// %GST_FORMAT_TIME based on the current clock time and segment information.
/// If no clock has been set on the element, the query will be forwarded
/// upstream.
/// </para>
/// <para>
/// The #GstBaseSinkClass::set_caps function will be called when the subclass
/// should configure itself to process a specific media type.
/// </para>
/// <para>
/// The #GstBaseSinkClass::start and #GstBaseSinkClass::stop virtual methods
/// will be called when resources should be allocated. Any
/// #GstBaseSinkClass::preroll, #GstBaseSinkClass::render and
/// #GstBaseSinkClass::set_caps function will be called between the
/// #GstBaseSinkClass::start and #GstBaseSinkClass::stop calls.
/// </para>
/// <para>
/// The #GstBaseSinkClass::event virtual method will be called when an event is
/// received by #GstBaseSink. Normally this method should only be overridden by
/// very specific elements (such as file sinks) which need to handle the
/// newsegment event specially.
/// </para>
/// <para>
/// The #GstBaseSinkClass::unlock method is called when the elements should
/// unblock any blocking operations they perform in the
/// #GstBaseSinkClass::render method. This is mostly useful when the
/// #GstBaseSinkClass::render method performs a blocking write on a file
/// descriptor, for example.
/// </para>
/// <para>
/// The #GstBaseSink:max-lateness property affects how the sink deals with
/// buffers that arrive too late in the sink. A buffer arrives too late in the
/// sink when the presentation time (as a combination of the last segment, buffer
/// timestamp and element base_time) plus the duration is before the current
/// time of the clock.
/// If the frame is later than max-lateness, the sink will drop the buffer
/// without calling the render method.
/// This feature is disabled if sync is disabled, the
/// #GstBaseSinkClass::get_times method does not return a valid start time or
/// max-lateness is set to -1 (the default).
/// Subclasses can use gst_base_sink_set_max_lateness() to configure the
/// max-lateness value.
/// </para>
/// <para>
/// The #GstBaseSink:qos property will enable the quality-of-service features of
/// the basesink which gather statistics about the real-time performance of the
/// clock synchronisation. For each buffer received in the sink, statistics are
/// gathered and a QOS event is sent upstream with these numbers. This
/// information can then be used by upstream elements to reduce their processing
/// rate, for example.
/// </para>
/// <para>
/// The #GstBaseSink:async property can be used to instruct the sink to never
/// perform an ASYNC state change. This feature is mostly usable when dealing
/// with non-synchronized streams or sparse streams.
/// </para>
/// </remarks>
public abstract unsafe partial class BaseSink : Gst.Element
{
    /// <summary>Wraps a native <c>GstBaseSink</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstBaseSink</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected BaseSink(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Get the number of bytes that the sink will pull when it is operating in pull
    /// mode.
    /// </summary>
    /// <returns>the number of bytes @sink will pull in pull mode.</returns>
    public uint GetBlocksize()
    {
        uint nativeResult = GstBaseSinkGetBlocksize(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Checks if @sink is currently configured to drop buffers which are outside
    /// the current segment
    /// </summary>
    /// <returns>
    /// %TRUE if the sink is configured to drop buffers outside the
    /// current segment.
    /// </returns>
    public bool GetDropOutOfSegment()
    {
        int nativeResult = GstBaseSinkGetDropOutOfSegment(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Get the last sample that arrived in the sink and was used for preroll or for
    /// rendering. This property can be used to generate thumbnails.
    /// </summary>
    /// <remarks>
    /// <para>The #GstCaps on the sample can be used to determine the type of the buffer.</para>
    /// <para>Free-function: gst_sample_unref</para>
    /// </remarks>
    /// <returns>
    /// a #GstSample. gst_sample_unref() after
    ///     usage.  This function returns %NULL when no buffer has arrived in the
    ///     sink yet or when the sink is not in PAUSED or PLAYING.
    /// </returns>
    public Gst.Sample? GetLastSample()
    {
        nint nativeResult = GstBaseSinkGetLastSample(Handle);
        System.GC.KeepAlive(this);
        return Gst.Sample.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the currently configured latency.</summary>
    /// <returns>The configured latency.</returns>
    public Gst.ClockTime GetLatency()
    {
        ulong nativeResult = GstBaseSinkGetLatency(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Get the maximum amount of bits per second that the sink will render.</summary>
    /// <returns>the maximum number of bits per second @sink will render.</returns>
    public ulong GetMaxBitrate()
    {
        ulong nativeResult = GstBaseSinkGetMaxBitrate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Gets the max lateness value. See gst_base_sink_set_max_lateness() for
    /// more details.
    /// </summary>
    /// <returns>
    /// The maximum time in nanoseconds that a buffer can be late
    /// before it is dropped and not rendered. A value of -1 means an
    /// unlimited time.
    /// </returns>
    public long GetMaxLateness()
    {
        long nativeResult = GstBaseSinkGetMaxLateness(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Get the processing deadline of @sink. see
    /// gst_base_sink_set_processing_deadline() for more information about
    /// the processing deadline.
    /// </summary>
    /// <returns>the processing deadline</returns>
    public Gst.ClockTime GetProcessingDeadline()
    {
        ulong nativeResult = GstBaseSinkGetProcessingDeadline(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the render delay of @sink. see gst_base_sink_set_render_delay() for more
    /// information about the render delay.
    /// </summary>
    /// <returns>the render delay of @sink.</returns>
    public Gst.ClockTime GetRenderDelay()
    {
        ulong nativeResult = GstBaseSinkGetRenderDelay(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Return various #GstBaseSink statistics. This function returns a #GstStructure
    /// with name `application/x-gst-base-sink-stats` with the following fields:
    /// </summary>
    /// <remarks>
    /// <para>
    /// - "average-rate" G_TYPE_DOUBLE   average frame rate
    /// - "dropped" G_TYPE_UINT64   Number of dropped frames
    /// - "rendered" G_TYPE_UINT64   Number of rendered frames
    /// </para>
    /// </remarks>
    /// <returns>pointer to #GstStructure</returns>
    public Gst.Structure GetStats()
    {
        nint nativeResult = GstBaseSinkGetStats(Handle);
        System.GC.KeepAlive(this);
        return Gst.Structure.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_base_sink_get_stats returned no value.");
    }

    /// <summary>
    /// Checks if @sink is currently configured to synchronize against the
    /// clock.
    /// </summary>
    /// <returns>%TRUE if the sink is configured to synchronize against the clock.</returns>
    public bool GetSync()
    {
        int nativeResult = GstBaseSinkGetSync(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Get the time that will be inserted between frames to control the
    /// maximum buffers per second.
    /// </summary>
    /// <returns>the number of nanoseconds @sink will put between frames.</returns>
    public ulong GetThrottleTime()
    {
        ulong nativeResult = GstBaseSinkGetThrottleTime(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the synchronisation offset of @sink.</summary>
    /// <returns>The synchronisation offset.</returns>
    public long GetTsOffset()
    {
        long nativeResult = GstBaseSinkGetTsOffset(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Checks if @sink is currently configured to perform asynchronous state
    /// changes to PAUSED.
    /// </summary>
    /// <returns>
    /// %TRUE if the sink is configured to perform asynchronous state
    /// changes.
    /// </returns>
    public bool IsAsyncEnabled()
    {
        int nativeResult = GstBaseSinkIsAsyncEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks if @sink is currently configured to store the last received sample in
    /// the last-sample property.
    /// </summary>
    /// <returns>%TRUE if the sink is configured to store the last received sample.</returns>
    public bool IsLastSampleEnabled()
    {
        int nativeResult = GstBaseSinkIsLastSampleEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks if @sink is currently configured to send Quality-of-Service events
    /// upstream.
    /// </summary>
    /// <returns>%TRUE if the sink is configured to perform Quality-of-Service.</returns>
    public bool IsQosEnabled()
    {
        int nativeResult = GstBaseSinkIsQosEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Query the sink for the latency parameters. The latency will be queried from
    /// the upstream elements. @live will be %TRUE if @sink is configured to
    /// synchronize against the clock. @upstream_live will be %TRUE if an upstream
    /// element is live.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If both @live and @upstream_live are %TRUE, the sink will want to compensate
    /// for the latency introduced by the upstream elements by setting the
    /// @min_latency to a strictly positive value.
    /// </para>
    /// <para>This function is mostly used by subclasses.</para>
    /// </remarks>
    /// <param name="live">The <c>live</c> argument.</param>
    /// <param name="upstreamLive">The <c>upstreamLive</c> argument.</param>
    /// <param name="minLatency">The <c>minLatency</c> argument.</param>
    /// <param name="maxLatency">The <c>maxLatency</c> argument.</param>
    /// <returns>%TRUE if the query succeeded.</returns>
    public bool QueryLatency(out bool live, out bool upstreamLive, out Gst.ClockTime minLatency, out Gst.ClockTime maxLatency)
    {
        int liveNative = default;
        int upstreamLiveNative = default;
        ulong minLatencyNative = default;
        ulong maxLatencyNative = default;
        int nativeResult = GstBaseSinkQueryLatency(Handle, &liveNative, &upstreamLiveNative, &minLatencyNative, &maxLatencyNative);
        System.GC.KeepAlive(this);
        live = liveNative != 0;
        upstreamLive = upstreamLiveNative != 0;
        minLatency = new Gst.ClockTime(minLatencyNative);
        maxLatency = new Gst.ClockTime(maxLatencyNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Configures @sink to perform all state changes asynchronously. When async is
    /// disabled, the sink will immediately go to PAUSED instead of waiting for a
    /// preroll buffer. This feature is useful if the sink does not synchronize
    /// against the clock or when it is dealing with sparse streams.
    /// </summary>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetAsyncEnabled(bool enabled)
    {
        GstBaseSinkSetAsyncEnabled(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the number of bytes that the sink will pull when it is operating in pull
    /// mode.
    /// </summary>
    /// <param name="blocksize">The <c>blocksize</c> argument.</param>
    public void SetBlocksize(uint blocksize)
    {
        GstBaseSinkSetBlocksize(Handle, blocksize);
        System.GC.KeepAlive(this);
    }

    /// <summary>Configure @sink to drop buffers which are outside the current segment</summary>
    /// <param name="dropOutOfSegment">The <c>dropOutOfSegment</c> argument.</param>
    public void SetDropOutOfSegment(bool dropOutOfSegment)
    {
        GstBaseSinkSetDropOutOfSegment(Handle, dropOutOfSegment ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configures @sink to store the last received sample in the last-sample
    /// property.
    /// </summary>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetLastSampleEnabled(bool enabled)
    {
        GstBaseSinkSetLastSampleEnabled(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Set the maximum amount of bits per second that the sink will render.</summary>
    /// <param name="maxBitrate">The <c>maxBitrate</c> argument.</param>
    public void SetMaxBitrate(ulong maxBitrate)
    {
        GstBaseSinkSetMaxBitrate(Handle, maxBitrate);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the new max lateness value to @max_lateness. This value is
    /// used to decide if a buffer should be dropped or not based on the
    /// buffer timestamp and the current clock time. A value of -1 means
    /// an unlimited time.
    /// </summary>
    /// <param name="maxLateness">The <c>maxLateness</c> argument.</param>
    public void SetMaxLateness(long maxLateness)
    {
        GstBaseSinkSetMaxLateness(Handle, maxLateness);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Maximum amount of time (in nanoseconds) that the pipeline can take
    /// for processing the buffer. This is added to the latency of live
    /// pipelines.
    /// </summary>
    /// <remarks>
    /// <para>This function is usually called by subclasses.</para>
    /// </remarks>
    /// <param name="processingDeadline">The <c>processingDeadline</c> argument.</param>
    public void SetProcessingDeadline(Gst.ClockTime processingDeadline)
    {
        GstBaseSinkSetProcessingDeadline(Handle, processingDeadline.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>Configures @sink to send Quality-of-Service events upstream.</summary>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetQosEnabled(bool enabled)
    {
        GstBaseSinkSetQosEnabled(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the render delay in @sink to @delay. The render delay is the time
    /// between actual rendering of a buffer and its synchronisation time. Some
    /// devices might delay media rendering which can be compensated for with this
    /// function.
    /// </summary>
    /// <remarks>
    /// <para>
    /// After calling this function, this sink will report additional latency and
    /// other sinks will adjust their latency to delay the rendering of their media.
    /// </para>
    /// <para>This function is usually called by subclasses.</para>
    /// </remarks>
    /// <param name="delay">The <c>delay</c> argument.</param>
    public void SetRenderDelay(Gst.ClockTime delay)
    {
        GstBaseSinkSetRenderDelay(Handle, delay.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configures @sink to synchronize on the clock or not. When
    /// @sync is %FALSE, incoming samples will be played as fast as
    /// possible. If @sync is %TRUE, the timestamps of the incoming
    /// buffers will be used to schedule the exact render time of its
    /// contents.
    /// </summary>
    /// <param name="sync">The <c>sync</c> argument.</param>
    public void SetSync(bool sync)
    {
        GstBaseSinkSetSync(Handle, sync ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the time that will be inserted between rendered buffers. This
    /// can be used to control the maximum buffers per second that the sink
    /// will render.
    /// </summary>
    /// <param name="throttle">The <c>throttle</c> argument.</param>
    public void SetThrottleTime(ulong throttle)
    {
        GstBaseSinkSetThrottleTime(Handle, throttle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Adjust the synchronisation of @sink with @offset. A negative value will
    /// render buffers earlier than their timestamp. A positive value will delay
    /// rendering. This function can be used to fix playback of badly timestamped
    /// buffers.
    /// </summary>
    /// <param name="offset">The <c>offset</c> argument.</param>
    public void SetTsOffset(long offset)
    {
        GstBaseSinkSetTsOffset(Handle, offset);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// This function will wait for preroll to complete and will then block until @time
    /// is reached. It is usually called by subclasses that use their own internal
    /// synchronisation but want to let some synchronization (like EOS) be handled
    /// by the base class.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function should only be called with the PREROLL_LOCK held (like when
    /// receiving an EOS event in the ::event vmethod or when handling buffers in
    /// ::render).
    /// </para>
    /// <para>
    /// The @time argument should be the running_time of when the timeout should happen
    /// and will be adjusted with any latency and offset configured in the sink.
    /// </para>
    /// </remarks>
    /// <param name="time">The <c>time</c> argument.</param>
    /// <param name="jitter">The <c>jitter</c> argument.</param>
    /// <returns>#GstFlowReturn</returns>
    public Gst.FlowReturn Wait(Gst.ClockTime time, out long jitter)
    {
        long jitterNative = default;
        int nativeResult = GstBaseSinkWait(Handle, time.Nanoseconds, &jitterNative);
        System.GC.KeepAlive(this);
        jitter = jitterNative;
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// This function will block until @time is reached. It is usually called by
    /// subclasses that use their own internal synchronisation.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @time is not valid, no synchronisation is done and %GST_CLOCK_BADTIME is
    /// returned. Likewise, if synchronisation is disabled in the element or there
    /// is no clock, no synchronisation is done and %GST_CLOCK_BADTIME is returned.
    /// </para>
    /// <para>
    /// This function should only be called with the PREROLL_LOCK held, like when
    /// receiving an EOS event in the #GstBaseSinkClass::event vmethod or when
    /// receiving a buffer in
    /// the #GstBaseSinkClass::render vmethod.
    /// </para>
    /// <para>
    /// The @time argument should be the running_time of when this method should
    /// return and is not adjusted with any latency or offset configured in the
    /// sink.
    /// </para>
    /// </remarks>
    /// <param name="time">The <c>time</c> argument.</param>
    /// <param name="jitter">The <c>jitter</c> argument.</param>
    /// <returns>#GstClockReturn</returns>
    public Gst.ClockReturn WaitClock(Gst.ClockTime time, out long jitter)
    {
        long jitterNative = default;
        int nativeResult = GstBaseSinkWaitClock(Handle, time.Nanoseconds, &jitterNative);
        System.GC.KeepAlive(this);
        jitter = jitterNative;
        return (Gst.ClockReturn)nativeResult;
    }

    /// <summary>
    /// If the #GstBaseSinkClass::render method performs its own synchronisation
    /// against the clock it must unblock when going from PLAYING to the PAUSED state
    /// and call this method before continuing to render the remaining data.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If the #GstBaseSinkClass::render method can block on something else than
    /// the clock, it must also be ready to unblock immediately on
    /// the #GstBaseSinkClass::unlock method and cause the
    /// #GstBaseSinkClass::render method to immediately call this function.
    /// In this case, the subclass must be prepared to continue rendering where it
    /// left off if this function returns %GST_FLOW_OK.
    /// </para>
    /// <para>
    /// This function will block until a state change to PLAYING happens (in which
    /// case this function returns %GST_FLOW_OK) or the processing must be stopped due
    /// to a state change to READY or a FLUSH event (in which case this function
    /// returns %GST_FLOW_FLUSHING).
    /// </para>
    /// <para>
    /// This function should only be called with the PREROLL_LOCK held, like in the
    /// render function.
    /// </para>
    /// </remarks>
    /// <returns>
    /// %GST_FLOW_OK if the preroll completed and processing can
    /// continue. Any other return value should be returned from the render vmethod.
    /// </returns>
    public Gst.FlowReturn WaitPreroll()
    {
        int nativeResult = GstBaseSinkWaitPreroll(Handle);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>The amount of bytes to pull when operating in pull mode.</summary>
    public uint Blocksize
    {
        get => GetBlocksize();
        set => SetBlocksize(value);
    }

    /// <summary>
    /// Control the maximum amount of bits that will be rendered per second.
    /// Setting this property to a value bigger than 0 will make the sink delay
    /// rendering of the buffers when it would exceed to max-bitrate.
    /// </summary>
    public ulong MaxBitrate
    {
        get => GetMaxBitrate();
        set => SetMaxBitrate(value);
    }

    /// <summary>The <c>max-lateness</c> property.</summary>
    public long MaxLateness
    {
        get => GetMaxLateness();
        set => SetMaxLateness(value);
    }

    /// <summary>
    /// Maximum amount of time (in nanoseconds) that the pipeline can take
    /// for processing the buffer. This is added to the latency of live
    /// pipelines.
    /// </summary>
    public Gst.ClockTime ProcessingDeadline
    {
        get => GetProcessingDeadline();
        set => SetProcessingDeadline(value);
    }

    /// <summary>
    /// The additional delay between synchronisation and actual rendering of the
    /// media. This property will add additional latency to the device in order to
    /// make other sinks compensate for the delay.
    /// </summary>
    public Gst.ClockTime RenderDelay
    {
        get => GetRenderDelay();
        set => SetRenderDelay(value);
    }

    /// <summary>The <c>sync</c> property.</summary>
    public bool Sync
    {
        get => GetSync();
        set => SetSync(value);
    }

    /// <summary>
    /// The time to insert between buffers. This property can be used to control
    /// the maximum amount of buffers per second to render. Setting this property
    /// to a value bigger than 0 will make the sink create THROTTLE QoS events.
    /// </summary>
    public ulong ThrottleTime
    {
        get => GetThrottleTime();
        set => SetThrottleTime(value);
    }

    /// <summary>
    /// Controls the final synchronisation, a negative value will render the buffer
    /// earlier while a positive value delays playback. This property can be
    /// used to fix synchronisation in bad files.
    /// </summary>
    public long TsOffset
    {
        get => GetTsOffset();
        set => SetTsOffset(value);
    }

    /// <summary>The <c>gst_base_sink_get_blocksize</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_blocksize")]
    private static partial uint GstBaseSinkGetBlocksize(nint sink);

    /// <summary>The <c>gst_base_sink_get_drop_out_of_segment</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_drop_out_of_segment")]
    private static partial int GstBaseSinkGetDropOutOfSegment(nint sink);

    /// <summary>The <c>gst_base_sink_get_last_sample</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_last_sample")]
    private static partial nint GstBaseSinkGetLastSample(nint sink);

    /// <summary>The <c>gst_base_sink_get_latency</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_latency")]
    private static partial ulong GstBaseSinkGetLatency(nint sink);

    /// <summary>The <c>gst_base_sink_get_max_bitrate</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_max_bitrate")]
    private static partial ulong GstBaseSinkGetMaxBitrate(nint sink);

    /// <summary>The <c>gst_base_sink_get_max_lateness</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_max_lateness")]
    private static partial long GstBaseSinkGetMaxLateness(nint sink);

    /// <summary>The <c>gst_base_sink_get_processing_deadline</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_processing_deadline")]
    private static partial ulong GstBaseSinkGetProcessingDeadline(nint sink);

    /// <summary>The <c>gst_base_sink_get_render_delay</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_render_delay")]
    private static partial ulong GstBaseSinkGetRenderDelay(nint sink);

    /// <summary>The <c>gst_base_sink_get_stats</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_stats")]
    private static partial nint GstBaseSinkGetStats(nint sink);

    /// <summary>The <c>gst_base_sink_get_sync</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_sync")]
    private static partial int GstBaseSinkGetSync(nint sink);

    /// <summary>The <c>gst_base_sink_get_throttle_time</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_throttle_time")]
    private static partial ulong GstBaseSinkGetThrottleTime(nint sink);

    /// <summary>The <c>gst_base_sink_get_ts_offset</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_ts_offset")]
    private static partial long GstBaseSinkGetTsOffset(nint sink);

    /// <summary>The <c>gst_base_sink_is_async_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_is_async_enabled")]
    private static partial int GstBaseSinkIsAsyncEnabled(nint sink);

    /// <summary>The <c>gst_base_sink_is_last_sample_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_is_last_sample_enabled")]
    private static partial int GstBaseSinkIsLastSampleEnabled(nint sink);

    /// <summary>The <c>gst_base_sink_is_qos_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_is_qos_enabled")]
    private static partial int GstBaseSinkIsQosEnabled(nint sink);

    /// <summary>The <c>gst_base_sink_query_latency</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_query_latency")]
    private static partial int GstBaseSinkQueryLatency(nint sink, int* live, int* upstreamLive, ulong* minLatency, ulong* maxLatency);

    /// <summary>The <c>gst_base_sink_set_async_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_async_enabled")]
    private static partial void GstBaseSinkSetAsyncEnabled(nint sink, int enabled);

    /// <summary>The <c>gst_base_sink_set_blocksize</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_blocksize")]
    private static partial void GstBaseSinkSetBlocksize(nint sink, uint blocksize);

    /// <summary>The <c>gst_base_sink_set_drop_out_of_segment</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_drop_out_of_segment")]
    private static partial void GstBaseSinkSetDropOutOfSegment(nint sink, int dropOutOfSegment);

    /// <summary>The <c>gst_base_sink_set_last_sample_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_last_sample_enabled")]
    private static partial void GstBaseSinkSetLastSampleEnabled(nint sink, int enabled);

    /// <summary>The <c>gst_base_sink_set_max_bitrate</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_max_bitrate")]
    private static partial void GstBaseSinkSetMaxBitrate(nint sink, ulong maxBitrate);

    /// <summary>The <c>gst_base_sink_set_max_lateness</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_max_lateness")]
    private static partial void GstBaseSinkSetMaxLateness(nint sink, long maxLateness);

    /// <summary>The <c>gst_base_sink_set_processing_deadline</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_processing_deadline")]
    private static partial void GstBaseSinkSetProcessingDeadline(nint sink, ulong processingDeadline);

    /// <summary>The <c>gst_base_sink_set_qos_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_qos_enabled")]
    private static partial void GstBaseSinkSetQosEnabled(nint sink, int enabled);

    /// <summary>The <c>gst_base_sink_set_render_delay</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_render_delay")]
    private static partial void GstBaseSinkSetRenderDelay(nint sink, ulong delay);

    /// <summary>The <c>gst_base_sink_set_sync</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_sync")]
    private static partial void GstBaseSinkSetSync(nint sink, int sync);

    /// <summary>The <c>gst_base_sink_set_throttle_time</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_throttle_time")]
    private static partial void GstBaseSinkSetThrottleTime(nint sink, ulong throttle);

    /// <summary>The <c>gst_base_sink_set_ts_offset</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_set_ts_offset")]
    private static partial void GstBaseSinkSetTsOffset(nint sink, long offset);

    /// <summary>The <c>gst_base_sink_wait</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_wait")]
    private static partial int GstBaseSinkWait(nint sink, ulong time, long* jitter);

    /// <summary>The <c>gst_base_sink_wait_clock</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_wait_clock")]
    private static partial int GstBaseSinkWaitClock(nint sink, ulong time, long* jitter);

    /// <summary>The <c>gst_base_sink_wait_preroll</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_wait_preroll")]
    private static partial int GstBaseSinkWaitPreroll(nint sink);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstBaseSink</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstBase", EntryPoint = "gst_base_sink_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstBaseSink</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : BaseSink
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
