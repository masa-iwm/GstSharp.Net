// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <summary>
/// Manages a set of pads that operate in collect mode. This means that control
/// is given to the manager of this object when all pads have data.
/// </summary>
/// <remarks>
/// <para>
///   * Collectpads are created with gst_collect_pads_new(). A callback should then
///     be installed with gst_collect_pads_set_function ().
/// </para>
/// <para>
///   * Pads are added to the collection with gst_collect_pads_add_pad()/
///     gst_collect_pads_remove_pad(). The pad has to be a sinkpad. When added,
///     the chain, event and query functions of the pad are overridden. The
///     element_private of the pad is used to store private information for the
///     collectpads.
/// </para>
/// <para>
///   * For each pad, data is queued in the _chain function or by
///     performing a pull_range.
/// </para>
/// <para>  * When data is queued on all pads in waiting mode, the callback function is called.</para>
/// <para>
///   * Data can be dequeued from the pad with the gst_collect_pads_pop() method.
///     One can peek at the data with the gst_collect_pads_peek() function.
///     These functions will return %NULL if the pad received an EOS event. When all
///     pads return %NULL from a gst_collect_pads_peek(), the element can emit an EOS
///     event itself.
/// </para>
/// <para>
///   * Data can also be dequeued in byte units using the gst_collect_pads_available(),
///     gst_collect_pads_read_buffer() and gst_collect_pads_flush() calls.
/// </para>
/// <para>
///   * Elements should call gst_collect_pads_start() and gst_collect_pads_stop() in
///     their state change functions to start and stop the processing of the collectpads.
///     The gst_collect_pads_stop() call should be called before calling the parent
///     element state change function in the PAUSED_TO_READY state change to ensure
///     no pad is blocked and the element can finish streaming.
/// </para>
/// <para>
///   * gst_collect_pads_set_waiting() sets a pad to waiting or non-waiting mode.
///     CollectPads element is not waiting for data to be collected on non-waiting pads.
///     Thus these pads may but need not have data when the callback is called.
///     All pads are in waiting mode by default.
/// </para>
/// </remarks>
public unsafe partial class CollectPads : Gst.Object
{
    /// <summary>Wraps a native <c>GstCollectPads</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal CollectPads(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Create a new instance of #GstCollectPads.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <returns>a new #GstCollectPads, or %NULL in case of an error.</returns>
    public static Gst.Base.CollectPads New()
    {
        nint nativeResult = GstCollectPadsNew();
        return Gst.GObject.Object.FromNative<Gst.Base.CollectPads>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_collect_pads_new returned no value.");
    }

    /// <summary>
    /// Query how much bytes can be read from each queued buffer. This means
    /// that the result of this call is the maximum number of bytes that can
    /// be read from each of the pads.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function should be called with @pads STREAM_LOCK held, such as
    /// in the callback.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <returns>
    /// The maximum number of bytes queued on all pads. This function
    /// returns 0 if a pad has no queued buffer.
    /// </returns>
    public uint Available()
    {
        uint nativeResult = GstCollectPadsAvailable(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Convenience clipping function that converts incoming buffer's timestamp
    /// to running time, or clips the buffer if outside configured segment.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Since 1.6, this clipping function also sets the DTS parameter of the
    /// GstCollectData structure. This version of the running time DTS can be
    /// negative. G_MININT64 is used to indicate invalid value.
    /// </para>
    /// </remarks>
    /// <param name="cdata">The <c>cdata</c> argument.</param>
    /// <param name="buf">The <c>buf</c> argument.</param>
    /// <param name="outbuf">The <c>outbuf</c> argument.</param>
    /// <param name="userData">The <c>userData</c> argument.</param>
    /// <returns>The result of <c>gst_collect_pads_clip_running_time</c>.</returns>
    public Gst.FlowReturn ClipRunningTime(Gst.Base.CollectData cdata, Gst.Buffer buf, out Gst.Buffer? outbuf, nint userData)
    {
        ArgumentNullException.ThrowIfNull(cdata);
        ArgumentNullException.ThrowIfNull(buf);
        nint outbufNative = default;
        int nativeResult = GstCollectPadsClipRunningTime(Handle, cdata.Handle, buf.Handle, &outbufNative, userData);
        System.GC.KeepAlive(this);
        outbuf = Gst.Buffer.FromNative(outbufNative, Gst.Interop.Transfer.Full);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Default #GstCollectPads event handling that elements should always
    /// chain up to to ensure proper operation.  Element might however indicate
    /// event should not be forwarded downstream.
    /// </summary>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <param name="event">The <c>@event</c> argument.</param>
    /// <param name="discard">The <c>discard</c> argument.</param>
    /// <returns>The result of <c>gst_collect_pads_event_default</c>.</returns>
    public bool EventDefault(Gst.Base.CollectData data, Gst.Event @event, bool discard)
    {
        ArgumentNullException.ThrowIfNull(data);
        ArgumentNullException.ThrowIfNull(@event);
        int nativeResult = GstCollectPadsEventDefault(Handle, data.Handle, @event.Handle, discard ? 1 : 0);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Flush @size bytes from the pad @data.</summary>
    /// <remarks>
    /// <para>
    /// This function should be called with @pads STREAM_LOCK held, such as
    /// in the callback.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>
    /// The number of bytes flushed This can be less than @size and
    /// is 0 if the pad was end-of-stream.
    /// </returns>
    public uint Flush(Gst.Base.CollectData data, uint size)
    {
        ArgumentNullException.ThrowIfNull(data);
        uint nativeResult = GstCollectPadsFlush(Handle, data.Handle, size);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Peek at the buffer currently queued in @data. This function
    /// should be called with the @pads STREAM_LOCK held, such as in the callback
    /// handler.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <returns>
    /// The buffer in @data or %NULL if no
    /// buffer is queued. should unref the buffer after usage.
    /// </returns>
    public Gst.Buffer? Peek(Gst.Base.CollectData data)
    {
        ArgumentNullException.ThrowIfNull(data);
        nint nativeResult = GstCollectPadsPeek(Handle, data.Handle);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Pop the buffer currently queued in @data. This function
    /// should be called with the @pads STREAM_LOCK held, such as in the callback
    /// handler.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <returns>
    /// The buffer in @data or %NULL if no
    /// buffer was queued. You should unref the buffer after usage.
    /// </returns>
    public Gst.Buffer? Pop(Gst.Base.CollectData data)
    {
        ArgumentNullException.ThrowIfNull(data);
        nint nativeResult = GstCollectPadsPop(Handle, data.Handle);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Default #GstCollectPads query handling that elements should always
    /// chain up to to ensure proper operation.  Element might however indicate
    /// query should not be forwarded downstream.
    /// </summary>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <param name="query">The <c>query</c> argument.</param>
    /// <param name="discard">The <c>discard</c> argument.</param>
    /// <returns>The result of <c>gst_collect_pads_query_default</c>.</returns>
    public bool QueryDefault(Gst.Base.CollectData data, Gst.Query query, bool discard)
    {
        ArgumentNullException.ThrowIfNull(data);
        ArgumentNullException.ThrowIfNull(query);
        int nativeResult = GstCollectPadsQueryDefault(Handle, data.Handle, query.Handle, discard ? 1 : 0);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get a subbuffer of @size bytes from the given pad @data.</summary>
    /// <remarks>
    /// <para>
    /// This function should be called with @pads STREAM_LOCK held, such as in the
    /// callback.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>
    /// A sub buffer. The size of the buffer can
    /// be less that requested. A return of %NULL signals that the pad is
    /// end-of-stream. Unref the buffer after use.
    /// </returns>
    public Gst.Buffer? ReadBuffer(Gst.Base.CollectData data, uint size)
    {
        ArgumentNullException.ThrowIfNull(data);
        nint nativeResult = GstCollectPadsReadBuffer(Handle, data.Handle, size);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Remove a pad from the collection of collect pads. This function will also
    /// free the #GstCollectData and all the resources that were allocated with
    /// gst_collect_pads_add_pad().
    /// </summary>
    /// <remarks>
    /// <para>The pad will be deactivated automatically when @pads is stopped.</para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="pad">The <c>pad</c> argument.</param>
    /// <returns>%TRUE if the pad could be removed.</returns>
    public bool RemovePad(Gst.Pad pad)
    {
        ArgumentNullException.ThrowIfNull(pad);
        int nativeResult = GstCollectPadsRemovePad(Handle, pad.Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Set the timestamp comparison function.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="func">the function to set</param>
    public void SetCompareFunction(Gst.Base.CollectPadsCompareFunction func)
    {
        ArgumentNullException.ThrowIfNull(func);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        try
        {
            GstCollectPadsSetCompareFunction(Handle, Gst.Base.CollectPadsCompareFunctionTrampoline.Pointer, funcState.UserData);
            System.GC.KeepAlive(this);
        }
        finally
        {
            funcState.Free();
        }
    }

    /// <summary>
    /// Set the event callback function and user data that will be called when
    /// collectpads has received an event originating from one of the collected
    /// pads.  If the event being processed is a serialized one, this callback is
    /// called with @pads STREAM_LOCK held, otherwise not.  As this lock should be
    /// held when calling a number of CollectPads functions, it should be acquired
    /// if so (unusually) needed.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="func">the function to set</param>
    public void SetEventFunction(Gst.Base.CollectPadsEventFunction func)
    {
        ArgumentNullException.ThrowIfNull(func);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        try
        {
            GstCollectPadsSetEventFunction(Handle, Gst.Base.CollectPadsEventFunctionTrampoline.Pointer, funcState.UserData);
            System.GC.KeepAlive(this);
        }
        finally
        {
            funcState.Free();
        }
    }

    /// <summary>
    /// Install a flush function that is called when the internal
    /// state of all pads should be flushed as part of flushing seek
    /// handling. See #GstCollectPadsFlushFunction for more info.
    /// </summary>
    /// <param name="func">flush function to install</param>
    public void SetFlushFunction(Gst.Base.CollectPadsFlushFunction func)
    {
        ArgumentNullException.ThrowIfNull(func);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        try
        {
            GstCollectPadsSetFlushFunction(Handle, Gst.Base.CollectPadsFlushFunctionTrampoline.Pointer, funcState.UserData);
            System.GC.KeepAlive(this);
        }
        finally
        {
            funcState.Free();
        }
    }

    /// <summary>
    /// Change the flushing state of all the pads in the collection. No pad
    /// is able to accept anymore data when @flushing is %TRUE. Calling this
    /// function with @flushing %FALSE makes @pads accept data again.
    /// Caller must ensure that downstream streaming (thread) is not blocked,
    /// e.g. by sending a FLUSH_START downstream.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="flushing">The <c>flushing</c> argument.</param>
    public void SetFlushing(bool flushing)
    {
        GstCollectPadsSetFlushing(Handle, flushing ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// CollectPads provides a default collection algorithm that will determine
    /// the oldest buffer available on all of its pads, and then delegate
    /// to a configured callback.
    /// However, if circumstances are more complicated and/or more control
    /// is desired, this sets a callback that will be invoked instead when
    /// all the pads added to the collection have buffers queued.
    /// Evidently, this callback is not compatible with
    /// gst_collect_pads_set_buffer_function() callback.
    /// If this callback is set, the former will be unset.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="func">the function to set</param>
    public void SetFunction(Gst.Base.CollectPadsFunction func)
    {
        ArgumentNullException.ThrowIfNull(func);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        try
        {
            GstCollectPadsSetFunction(Handle, Gst.Base.CollectPadsFunctionTrampoline.Pointer, funcState.UserData);
            System.GC.KeepAlive(this);
        }
        finally
        {
            funcState.Free();
        }
    }

    /// <summary>
    /// Set the query callback function and user data that will be called after
    /// collectpads has received a query originating from one of the collected
    /// pads.  If the query being processed is a serialized one, this callback is
    /// called with @pads STREAM_LOCK held, otherwise not.  As this lock should be
    /// held when calling a number of CollectPads functions, it should be acquired
    /// if so (unusually) needed.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="func">the function to set</param>
    public void SetQueryFunction(Gst.Base.CollectPadsQueryFunction func)
    {
        ArgumentNullException.ThrowIfNull(func);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        try
        {
            GstCollectPadsSetQueryFunction(Handle, Gst.Base.CollectPadsQueryFunctionTrampoline.Pointer, funcState.UserData);
            System.GC.KeepAlive(this);
        }
        finally
        {
            funcState.Free();
        }
    }

    /// <summary>
    /// Sets a pad to waiting or non-waiting mode, if at least this pad
    /// has not been created with locked waiting state,
    /// in which case nothing happens.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function should be called with @pads STREAM_LOCK held, such as
    /// in the callback.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <param name="waiting">The <c>waiting</c> argument.</param>
    public void SetWaiting(Gst.Base.CollectData data, bool waiting)
    {
        ArgumentNullException.ThrowIfNull(data);
        GstCollectPadsSetWaiting(Handle, data.Handle, waiting ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Default #GstCollectPads event handling for the src pad of elements.
    /// Elements can chain up to this to let flushing seek event handling
    /// be done by #GstCollectPads.
    /// </summary>
    /// <param name="pad">The <c>pad</c> argument.</param>
    /// <param name="event">The <c>@event</c> argument.</param>
    /// <returns>The result of <c>gst_collect_pads_src_event_default</c>.</returns>
    public bool SrcEventDefault(Gst.Pad pad, Gst.Event @event)
    {
        ArgumentNullException.ThrowIfNull(pad);
        ArgumentNullException.ThrowIfNull(@event);
        int nativeResult = GstCollectPadsSrcEventDefault(Handle, pad.Handle, @event.Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Starts the processing of data in the collect_pads.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    public void Start()
    {
        GstCollectPadsStart(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Stops the processing of data in the collect_pads. this function
    /// will also unblock any blocking operations.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    public void Stop()
    {
        GstCollectPadsStop(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Get a subbuffer of @size bytes from the given pad @data. Flushes the amount
    /// of read bytes.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function should be called with @pads STREAM_LOCK held, such as in the
    /// callback.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>
    /// A sub buffer. The size of the buffer can
    /// be less that requested. A return of %NULL signals that the pad is
    /// end-of-stream. Unref the buffer after use.
    /// </returns>
    public Gst.Buffer? TakeBuffer(Gst.Base.CollectData data, uint size)
    {
        ArgumentNullException.ThrowIfNull(data);
        nint nativeResult = GstCollectPadsTakeBuffer(Handle, data.Handle, size);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_collect_pads_new</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_new")]
    private static partial nint GstCollectPadsNew();

    /// <summary>The <c>gst_collect_pads_available</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_available")]
    private static partial uint GstCollectPadsAvailable(nint pads);

    /// <summary>The <c>gst_collect_pads_clip_running_time</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_clip_running_time")]
    private static partial int GstCollectPadsClipRunningTime(nint pads, nint cdata, nint buf, nint* outbuf, nint userData);

    /// <summary>The <c>gst_collect_pads_event_default</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_event_default")]
    private static partial int GstCollectPadsEventDefault(nint pads, nint data, nint @event, int discard);

    /// <summary>The <c>gst_collect_pads_flush</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_flush")]
    private static partial uint GstCollectPadsFlush(nint pads, nint data, uint size);

    /// <summary>The <c>gst_collect_pads_peek</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_peek")]
    private static partial nint GstCollectPadsPeek(nint pads, nint data);

    /// <summary>The <c>gst_collect_pads_pop</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_pop")]
    private static partial nint GstCollectPadsPop(nint pads, nint data);

    /// <summary>The <c>gst_collect_pads_query_default</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_query_default")]
    private static partial int GstCollectPadsQueryDefault(nint pads, nint data, nint query, int discard);

    /// <summary>The <c>gst_collect_pads_read_buffer</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_read_buffer")]
    private static partial nint GstCollectPadsReadBuffer(nint pads, nint data, uint size);

    /// <summary>The <c>gst_collect_pads_remove_pad</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_remove_pad")]
    private static partial int GstCollectPadsRemovePad(nint pads, nint pad);

    /// <summary>The <c>gst_collect_pads_set_compare_function</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_set_compare_function")]
    private static partial void GstCollectPadsSetCompareFunction(nint pads, nint func, nint userData);

    /// <summary>The <c>gst_collect_pads_set_event_function</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_set_event_function")]
    private static partial void GstCollectPadsSetEventFunction(nint pads, nint func, nint userData);

    /// <summary>The <c>gst_collect_pads_set_flush_function</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_set_flush_function")]
    private static partial void GstCollectPadsSetFlushFunction(nint pads, nint func, nint userData);

    /// <summary>The <c>gst_collect_pads_set_flushing</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_set_flushing")]
    private static partial void GstCollectPadsSetFlushing(nint pads, int flushing);

    /// <summary>The <c>gst_collect_pads_set_function</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_set_function")]
    private static partial void GstCollectPadsSetFunction(nint pads, nint func, nint userData);

    /// <summary>The <c>gst_collect_pads_set_query_function</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_set_query_function")]
    private static partial void GstCollectPadsSetQueryFunction(nint pads, nint func, nint userData);

    /// <summary>The <c>gst_collect_pads_set_waiting</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_set_waiting")]
    private static partial void GstCollectPadsSetWaiting(nint pads, nint data, int waiting);

    /// <summary>The <c>gst_collect_pads_src_event_default</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_src_event_default")]
    private static partial int GstCollectPadsSrcEventDefault(nint pads, nint pad, nint @event);

    /// <summary>The <c>gst_collect_pads_start</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_start")]
    private static partial void GstCollectPadsStart(nint pads);

    /// <summary>The <c>gst_collect_pads_stop</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_stop")]
    private static partial void GstCollectPadsStop(nint pads);

    /// <summary>The <c>gst_collect_pads_take_buffer</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_take_buffer")]
    private static partial nint GstCollectPadsTakeBuffer(nint pads, nint data, uint size);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstCollectPads</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstBase", EntryPoint = "gst_collect_pads_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new CollectPads(handle, transfer);
}
