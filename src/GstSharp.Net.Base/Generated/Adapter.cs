// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <summary>
/// This class is for elements that receive buffers in an undesired size.
/// While for example raw video contains one image per buffer, the same is not
/// true for a lot of other formats, especially those that come directly from
/// a file. So if you have undefined buffer sizes and require a specific size,
/// this object is for you.
/// </summary>
/// <remarks>
/// <para>
/// An adapter is created with gst_adapter_new(). It can be freed again with
/// g_object_unref().
/// </para>
/// <para>
/// The theory of operation is like this: All buffers received are put
/// into the adapter using gst_adapter_push() and the data is then read back
/// in chunks of the desired size using gst_adapter_map()/gst_adapter_unmap()
/// and/or gst_adapter_copy(). After the data has been processed, it is freed
/// using gst_adapter_unmap().
/// </para>
/// <para>
/// Other methods such as gst_adapter_take() and gst_adapter_take_buffer()
/// combine gst_adapter_map() and gst_adapter_unmap() in one method and are
/// potentially more convenient for some use cases.
/// </para>
/// <para>
/// For example, a sink pad's chain function that needs to pass data to a library
/// in 512-byte chunks could be implemented like this:
/// |[&lt;!-- language="C" --&gt;
/// static GstFlowReturn
/// sink_pad_chain (GstPad *pad, GstObject *parent, GstBuffer *buffer)
/// {
///   MyElement *this;
///   GstAdapter *adapter;
///   GstFlowReturn ret = GST_FLOW_OK;
/// </para>
/// <para>  this = MY_ELEMENT (parent);</para>
/// <para>  adapter = this-&gt;adapter;</para>
/// <para>
///   // put buffer into adapter
///   gst_adapter_push (adapter, buffer);
/// </para>
/// <para>
///   // while we can read out 512 bytes, process them
///   while (gst_adapter_available (adapter) &gt;= 512 &amp;&amp; ret == GST_FLOW_OK) {
///     const guint8 *data = gst_adapter_map (adapter, 512);
///     // use flowreturn as an error value
///     ret = my_library_foo (data);
///     gst_adapter_unmap (adapter);
///     gst_adapter_flush (adapter, 512);
///   }
///   return ret;
/// }
/// ]|
/// </para>
/// <para>
/// For another example, a simple element inside GStreamer that uses #GstAdapter
/// is the libvisual element.
/// </para>
/// <para>
/// An element using #GstAdapter in its sink pad chain function should ensure that
/// when the FLUSH_STOP event is received, that any queued data is cleared using
/// gst_adapter_clear(). Data should also be cleared or processed on EOS and
/// when changing state from %GST_STATE_PAUSED to %GST_STATE_READY.
/// </para>
/// <para>
/// Also check the GST_BUFFER_FLAG_DISCONT flag on the buffer. Some elements might
/// need to clear the adapter after a discontinuity.
/// </para>
/// <para>
/// The adapter will keep track of the timestamps of the buffers
/// that were pushed. The last seen timestamp before the current position
/// can be queried with gst_adapter_prev_pts(). This function can
/// optionally return the number of bytes between the start of the buffer that
/// carried the timestamp and the current adapter position. The distance is
/// useful when dealing with, for example, raw audio samples because it allows
/// you to calculate the timestamp of the current adapter position by using the
/// last seen timestamp and the amount of bytes since.  Additionally, the
/// gst_adapter_prev_pts_at_offset() can be used to determine the last
/// seen timestamp at a particular offset in the adapter.
/// </para>
/// <para>
/// The adapter will also keep track of the offset of the buffers
/// (#GST_BUFFER_OFFSET) that were pushed. The last seen offset before the
/// current position can be queried with gst_adapter_prev_offset(). This function
/// can optionally return the number of bytes between the start of the buffer
/// that carried the offset and the current adapter position.
/// </para>
/// <para>
/// Additionally the adapter also keeps track of the PTS, DTS and buffer offset
/// at the last discontinuity, which can be retrieved with
/// gst_adapter_pts_at_discont(), gst_adapter_dts_at_discont() and
/// gst_adapter_offset_at_discont(). The number of bytes that were consumed
/// since then can be queried with gst_adapter_distance_from_discont().
/// </para>
/// <para>
/// A last thing to note is that while #GstAdapter is pretty optimized,
/// merging buffers still might be an operation that requires a `malloc()` and
/// `memcpy()` operation, and these operations are not the fastest. Because of
/// this, some functions like gst_adapter_available_fast() are provided to help
/// speed up such cases should you want to. To avoid repeated memory allocations,
/// gst_adapter_copy() can be used to copy data into a (statically allocated)
/// user provided buffer.
/// </para>
/// <para>
/// #GstAdapter is not MT safe. All operations on an adapter must be serialized by
/// the caller. This is not normally a problem, however, as the normal use case
/// of #GstAdapter is inside one pad's chain function, in which case access is
/// serialized via the pad's STREAM_LOCK.
/// </para>
/// <para>
/// Note that gst_adapter_push() takes ownership of the buffer passed. Use
/// gst_buffer_ref() before pushing it into the adapter if you still want to
/// access the buffer later. The adapter will never modify the data in the
/// buffer pushed in it.
/// </para>
/// </remarks>
public unsafe partial class Adapter : Gst.GObject.Object
{
    /// <summary>Wraps a native <c>GstAdapter</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal Adapter(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new #GstAdapter. Free with g_object_unref().</summary>
    /// <returns>a new #GstAdapter</returns>
    public static Gst.Base.Adapter New()
    {
        nint nativeResult = GstAdapterNew();
        return Gst.GObject.Object.FromNative<Gst.Base.Adapter>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_adapter_new returned no value.");
    }

    /// <summary>
    /// Gets the maximum amount of bytes available, that is it returns the maximum
    /// value that can be supplied to gst_adapter_map() without that function
    /// returning %NULL.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Calling gst_adapter_map() with the amount of bytes returned by this function
    /// may require expensive operations (like copying the data into a temporary
    /// buffer) in some cases.
    /// </para>
    /// </remarks>
    /// <returns>number of bytes available in @adapter</returns>
    public nuint Available()
    {
        nuint nativeResult = GstAdapterAvailable(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Gets the maximum number of bytes that can be retrieved in a single map
    /// operation without merging buffers.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Calling gst_adapter_map() with the amount of bytes returned by this function
    /// will never require any expensive operations (like copying the data into a
    /// temporary buffer).
    /// </para>
    /// </remarks>
    /// <returns>
    /// number of bytes that are available in @adapter without expensive
    /// operations
    /// </returns>
    public nuint AvailableFast()
    {
        nuint nativeResult = GstAdapterAvailableFast(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Removes all buffers from @adapter.</summary>
    public void Clear()
    {
        GstAdapterClear(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Get the distance in bytes since the last buffer with the
    /// %GST_BUFFER_FLAG_DISCONT flag.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The distance will be reset to 0 for all buffers with
    /// %GST_BUFFER_FLAG_DISCONT on them, and then calculated for all other
    /// following buffers based on their size.
    /// </para>
    /// </remarks>
    /// <returns>The offset. Can be %GST_BUFFER_OFFSET_NONE.</returns>
    public ulong DistanceFromDiscont()
    {
        ulong nativeResult = GstAdapterDistanceFromDiscont(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Get the DTS that was on the last buffer with the GST_BUFFER_FLAG_DISCONT
    /// flag, or GST_CLOCK_TIME_NONE.
    /// </summary>
    /// <returns>The DTS at the last discont or GST_CLOCK_TIME_NONE.</returns>
    public Gst.ClockTime DtsAtDiscont()
    {
        ulong nativeResult = GstAdapterDtsAtDiscont(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Flushes the first @flush bytes in the @adapter. The caller must ensure that
    /// at least this many bytes are available.
    /// </summary>
    /// <remarks>
    /// <para>See also: gst_adapter_map(), gst_adapter_unmap()</para>
    /// </remarks>
    /// <param name="flush">The <c>flush</c> argument.</param>
    public void Flush(nuint flush)
    {
        GstAdapterFlush(Handle, flush);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Returns a #GstBuffer containing the first @nbytes of the @adapter, but
    /// does not flush them from the adapter. See gst_adapter_take_buffer()
    /// for details.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Caller owns a reference to the returned buffer. gst_buffer_unref() after
    /// usage.
    /// </para>
    /// <para>Free-function: gst_buffer_unref</para>
    /// </remarks>
    /// <param name="nbytes">The <c>nbytes</c> argument.</param>
    /// <returns>
    /// a #GstBuffer containing the first
    ///     @nbytes of the adapter, or %NULL if @nbytes bytes are not available.
    ///     gst_buffer_unref() when no longer needed.
    /// </returns>
    public Gst.Buffer? GetBuffer(nuint nbytes)
    {
        nint nativeResult = GstAdapterGetBuffer(Handle, nbytes);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Returns a #GstBuffer containing the first @nbytes of the @adapter, but
    /// does not flush them from the adapter. See gst_adapter_take_buffer_fast()
    /// for details.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Caller owns a reference to the returned buffer. gst_buffer_unref() after
    /// usage.
    /// </para>
    /// <para>Free-function: gst_buffer_unref</para>
    /// </remarks>
    /// <param name="nbytes">The <c>nbytes</c> argument.</param>
    /// <returns>
    /// a #GstBuffer containing the first
    ///     @nbytes of the adapter, or %NULL if @nbytes bytes are not available.
    ///     gst_buffer_unref() when no longer needed.
    /// </returns>
    public Gst.Buffer? GetBufferFast(nuint nbytes)
    {
        nint nativeResult = GstAdapterGetBufferFast(Handle, nbytes);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Returns a #GstBufferList of buffers containing the first @nbytes bytes of
    /// the @adapter but does not flush them from the adapter. See
    /// gst_adapter_take_buffer_list() for details.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Caller owns the returned list. Call gst_buffer_list_unref() to free
    /// the list after usage.
    /// </para>
    /// </remarks>
    /// <param name="nbytes">The <c>nbytes</c> argument.</param>
    /// <returns>
    /// a #GstBufferList of buffers containing
    ///     the first @nbytes of the adapter, or %NULL if @nbytes bytes are not
    ///     available
    /// </returns>
    public Gst.BufferList? GetBufferList(nuint nbytes)
    {
        nint nativeResult = GstAdapterGetBufferList(Handle, nbytes);
        System.GC.KeepAlive(this);
        return Gst.BufferList.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Scan for pattern @pattern with applied mask @mask in the adapter data,
    /// starting from offset @offset.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The bytes in @pattern and @mask are interpreted left-to-right, regardless
    /// of endianness.  All four bytes of the pattern must be present in the
    /// adapter for it to match, even if the first or last bytes are masked out.
    /// </para>
    /// <para>
    /// It is an error to call this function without making sure that there is
    /// enough data (offset+size bytes) in the adapter.
    /// </para>
    /// <para>
    /// This function calls gst_adapter_masked_scan_uint32_peek() passing %NULL
    /// for value.
    /// </para>
    /// </remarks>
    /// <param name="mask">The <c>mask</c> argument.</param>
    /// <param name="pattern">The <c>pattern</c> argument.</param>
    /// <param name="offset">The <c>offset</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <returns>offset of the first match, or -1 if no match was found.</returns>
    public nint MaskedScanUint32(uint mask, uint pattern, nuint offset, nuint size)
    {
        nint nativeResult = GstAdapterMaskedScanUint32(Handle, mask, pattern, offset, size);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Scan for pattern @pattern with applied mask @mask in the adapter data,
    /// starting from offset @offset.  If a match is found, the value that matched
    /// is returned through @value, otherwise @value is left untouched.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The bytes in @pattern and @mask are interpreted left-to-right, regardless
    /// of endianness.  All four bytes of the pattern must be present in the
    /// adapter for it to match, even if the first or last bytes are masked out.
    /// </para>
    /// <para>
    /// It is an error to call this function without making sure that there is
    /// enough data (offset+size bytes) in the adapter.
    /// </para>
    /// </remarks>
    /// <param name="mask">The <c>mask</c> argument.</param>
    /// <param name="pattern">The <c>pattern</c> argument.</param>
    /// <param name="offset">The <c>offset</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="value">The <c>value</c> argument.</param>
    /// <returns>offset of the first match, or -1 if no match was found.</returns>
    public nint MaskedScanUint32Peek(uint mask, uint pattern, nuint offset, nuint size, out uint value)
    {
        uint valueNative = default;
        nint nativeResult = GstAdapterMaskedScanUint32Peek(Handle, mask, pattern, offset, size, &valueNative);
        System.GC.KeepAlive(this);
        value = valueNative;
        return nativeResult;
    }

    /// <summary>
    /// Get the offset that was on the last buffer with the GST_BUFFER_FLAG_DISCONT
    /// flag, or GST_BUFFER_OFFSET_NONE.
    /// </summary>
    /// <returns>The offset at the last discont or GST_BUFFER_OFFSET_NONE.</returns>
    public ulong OffsetAtDiscont()
    {
        ulong nativeResult = GstAdapterOffsetAtDiscont(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Get the dts that was before the current byte in the adapter. When
    /// @distance is given, the amount of bytes between the dts and the current
    /// position is returned.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The dts is reset to GST_CLOCK_TIME_NONE and the distance is set to 0 when
    /// the adapter is first created or when it is cleared. This also means that before
    /// the first byte with a dts is added to the adapter, the dts
    /// and distance returned are GST_CLOCK_TIME_NONE and 0 respectively.
    /// </para>
    /// </remarks>
    /// <param name="distance">The <c>distance</c> argument.</param>
    /// <returns>The previously seen dts.</returns>
    public Gst.ClockTime PrevDts(out ulong distance)
    {
        ulong distanceNative = default;
        ulong nativeResult = GstAdapterPrevDts(Handle, &distanceNative);
        System.GC.KeepAlive(this);
        distance = distanceNative;
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the dts that was before the byte at offset @offset in the adapter. When
    /// @distance is given, the amount of bytes between the dts and the current
    /// position is returned.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The dts is reset to GST_CLOCK_TIME_NONE and the distance is set to 0 when
    /// the adapter is first created or when it is cleared. This also means that before
    /// the first byte with a dts is added to the adapter, the dts
    /// and distance returned are GST_CLOCK_TIME_NONE and 0 respectively.
    /// </para>
    /// </remarks>
    /// <param name="offset">The <c>offset</c> argument.</param>
    /// <param name="distance">The <c>distance</c> argument.</param>
    /// <returns>The previously seen dts at given offset.</returns>
    public Gst.ClockTime PrevDtsAtOffset(nuint offset, out ulong distance)
    {
        ulong distanceNative = default;
        ulong nativeResult = GstAdapterPrevDtsAtOffset(Handle, offset, &distanceNative);
        System.GC.KeepAlive(this);
        distance = distanceNative;
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the offset that was before the current byte in the adapter. When
    /// @distance is given, the amount of bytes between the offset and the current
    /// position is returned.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The offset is reset to GST_BUFFER_OFFSET_NONE and the distance is set to 0
    /// when the adapter is first created or when it is cleared. This also means that
    /// before the first byte with an offset is added to the adapter, the offset
    /// and distance returned are GST_BUFFER_OFFSET_NONE and 0 respectively.
    /// </para>
    /// </remarks>
    /// <param name="distance">The <c>distance</c> argument.</param>
    /// <returns>The previous seen offset.</returns>
    public ulong PrevOffset(out ulong distance)
    {
        ulong distanceNative = default;
        ulong nativeResult = GstAdapterPrevOffset(Handle, &distanceNative);
        System.GC.KeepAlive(this);
        distance = distanceNative;
        return nativeResult;
    }

    /// <summary>
    /// Get the pts that was before the current byte in the adapter. When
    /// @distance is given, the amount of bytes between the pts and the current
    /// position is returned.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The pts is reset to GST_CLOCK_TIME_NONE and the distance is set to 0 when
    /// the adapter is first created or when it is cleared. This also means that before
    /// the first byte with a pts is added to the adapter, the pts
    /// and distance returned are GST_CLOCK_TIME_NONE and 0 respectively.
    /// </para>
    /// </remarks>
    /// <param name="distance">The <c>distance</c> argument.</param>
    /// <returns>The previously seen pts.</returns>
    public Gst.ClockTime PrevPts(out ulong distance)
    {
        ulong distanceNative = default;
        ulong nativeResult = GstAdapterPrevPts(Handle, &distanceNative);
        System.GC.KeepAlive(this);
        distance = distanceNative;
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the pts that was before the byte at offset @offset in the adapter. When
    /// @distance is given, the amount of bytes between the pts and the current
    /// position is returned.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The pts is reset to GST_CLOCK_TIME_NONE and the distance is set to 0 when
    /// the adapter is first created or when it is cleared. This also means that before
    /// the first byte with a pts is added to the adapter, the pts
    /// and distance returned are GST_CLOCK_TIME_NONE and 0 respectively.
    /// </para>
    /// </remarks>
    /// <param name="offset">The <c>offset</c> argument.</param>
    /// <param name="distance">The <c>distance</c> argument.</param>
    /// <returns>The previously seen pts at given offset.</returns>
    public Gst.ClockTime PrevPtsAtOffset(nuint offset, out ulong distance)
    {
        ulong distanceNative = default;
        ulong nativeResult = GstAdapterPrevPtsAtOffset(Handle, offset, &distanceNative);
        System.GC.KeepAlive(this);
        distance = distanceNative;
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the PTS that was on the last buffer with the GST_BUFFER_FLAG_DISCONT
    /// flag, or GST_CLOCK_TIME_NONE.
    /// </summary>
    /// <returns>The PTS at the last discont or GST_CLOCK_TIME_NONE.</returns>
    public Gst.ClockTime PtsAtDiscont()
    {
        ulong nativeResult = GstAdapterPtsAtDiscont(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Returns a #GstBuffer containing the first @nbytes bytes of the
    /// @adapter. The returned bytes will be flushed from the adapter.
    /// This function is potentially more performant than
    /// gst_adapter_take() since it can reuse the memory in pushed buffers
    /// by subbuffering or merging. This function will always return a
    /// buffer with a single memory region.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that no assumptions should be made as to whether certain buffer
    /// flags such as the DISCONT flag are set on the returned buffer, or not.
    /// The caller needs to explicitly set or unset flags that should be set or
    /// unset.
    /// </para>
    /// <para>
    /// Likewise, no assumptions should be made about timestamps and offset of the
    /// returned buffer. The caller should use gst_adapter_prev_pts(),
    /// gst_adapter_prev_dts(), and gst_adapter_prev_offset() to obtain the relevant
    /// information.
    /// </para>
    /// <para>
    /// The returned buffer might not be writable, use gst_buffer_make_writable()
    /// if you need to change e.g. flags or timestamps.
    /// </para>
    /// <para>
    /// Since 1.6 this will also copy over all GstMeta of the input buffers except
    /// for meta with the %GST_META_FLAG_POOLED flag or with the "memory" tag.
    /// </para>
    /// <para>
    /// Caller owns a reference to the returned buffer. gst_buffer_unref() after
    /// usage.
    /// </para>
    /// <para>Free-function: gst_buffer_unref</para>
    /// </remarks>
    /// <param name="nbytes">The <c>nbytes</c> argument.</param>
    /// <returns>
    /// a #GstBuffer containing the first
    ///     @nbytes of the adapter, or %NULL if @nbytes bytes are not available.
    ///     gst_buffer_unref() when no longer needed.
    /// </returns>
    public Gst.Buffer? TakeBuffer(nuint nbytes)
    {
        nint nativeResult = GstAdapterTakeBuffer(Handle, nbytes);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Returns a #GstBuffer containing the first @nbytes of the @adapter.
    /// The returned bytes will be flushed from the adapter.  This function
    /// is potentially more performant than gst_adapter_take_buffer() since
    /// it can reuse the memory in pushed buffers by subbuffering or
    /// merging. Unlike gst_adapter_take_buffer(), the returned buffer may
    /// be composed of multiple non-contiguous #GstMemory objects, no
    /// copies are made.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that no assumptions should be made as to whether certain buffer
    /// flags such as the DISCONT flag are set on the returned buffer, or not.
    /// The caller needs to explicitly set or unset flags that should be set or
    /// unset.
    /// </para>
    /// <para>
    /// This will also copy over all GstMeta of the input buffers except
    /// for meta with the %GST_META_FLAG_POOLED flag or with the "memory" tag.
    /// </para>
    /// <para>
    /// This function can return buffer up to the return value of
    /// gst_adapter_available() without making copies if possible.
    /// </para>
    /// <para>
    /// Caller owns a reference to the returned buffer. gst_buffer_unref() after
    /// usage.
    /// </para>
    /// <para>Free-function: gst_buffer_unref</para>
    /// </remarks>
    /// <param name="nbytes">The <c>nbytes</c> argument.</param>
    /// <returns>
    /// a #GstBuffer containing the first
    ///     @nbytes of the adapter, or %NULL if @nbytes bytes are not available.
    ///     gst_buffer_unref() when no longer needed.
    /// </returns>
    public Gst.Buffer? TakeBufferFast(nuint nbytes)
    {
        nint nativeResult = GstAdapterTakeBufferFast(Handle, nbytes);
        System.GC.KeepAlive(this);
        return Gst.Buffer.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Returns a #GstBufferList of buffers containing the first @nbytes bytes of
    /// the @adapter. The returned bytes will be flushed from the adapter.
    /// When the caller can deal with individual buffers, this function is more
    /// performant because no memory should be copied.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Caller owns the returned list. Call gst_buffer_list_unref() to free
    /// the list after usage.
    /// </para>
    /// </remarks>
    /// <param name="nbytes">The <c>nbytes</c> argument.</param>
    /// <returns>
    /// a #GstBufferList of buffers containing
    ///     the first @nbytes of the adapter, or %NULL if @nbytes bytes are not
    ///     available
    /// </returns>
    public Gst.BufferList? TakeBufferList(nuint nbytes)
    {
        nint nativeResult = GstAdapterTakeBufferList(Handle, nbytes);
        System.GC.KeepAlive(this);
        return Gst.BufferList.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_adapter_new</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_new")]
    private static partial nint GstAdapterNew();

    /// <summary>The <c>gst_adapter_available</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_available")]
    private static partial nuint GstAdapterAvailable(nint adapter);

    /// <summary>The <c>gst_adapter_available_fast</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_available_fast")]
    private static partial nuint GstAdapterAvailableFast(nint adapter);

    /// <summary>The <c>gst_adapter_clear</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_clear")]
    private static partial void GstAdapterClear(nint adapter);

    /// <summary>The <c>gst_adapter_distance_from_discont</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_distance_from_discont")]
    private static partial ulong GstAdapterDistanceFromDiscont(nint adapter);

    /// <summary>The <c>gst_adapter_dts_at_discont</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_dts_at_discont")]
    private static partial ulong GstAdapterDtsAtDiscont(nint adapter);

    /// <summary>The <c>gst_adapter_flush</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_flush")]
    private static partial void GstAdapterFlush(nint adapter, nuint flush);

    /// <summary>The <c>gst_adapter_get_buffer</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_get_buffer")]
    private static partial nint GstAdapterGetBuffer(nint adapter, nuint nbytes);

    /// <summary>The <c>gst_adapter_get_buffer_fast</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_get_buffer_fast")]
    private static partial nint GstAdapterGetBufferFast(nint adapter, nuint nbytes);

    /// <summary>The <c>gst_adapter_get_buffer_list</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_get_buffer_list")]
    private static partial nint GstAdapterGetBufferList(nint adapter, nuint nbytes);

    /// <summary>The <c>gst_adapter_masked_scan_uint32</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_masked_scan_uint32")]
    private static partial nint GstAdapterMaskedScanUint32(nint adapter, uint mask, uint pattern, nuint offset, nuint size);

    /// <summary>The <c>gst_adapter_masked_scan_uint32_peek</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_masked_scan_uint32_peek")]
    private static partial nint GstAdapterMaskedScanUint32Peek(nint adapter, uint mask, uint pattern, nuint offset, nuint size, uint* value);

    /// <summary>The <c>gst_adapter_offset_at_discont</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_offset_at_discont")]
    private static partial ulong GstAdapterOffsetAtDiscont(nint adapter);

    /// <summary>The <c>gst_adapter_prev_dts</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_prev_dts")]
    private static partial ulong GstAdapterPrevDts(nint adapter, ulong* distance);

    /// <summary>The <c>gst_adapter_prev_dts_at_offset</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_prev_dts_at_offset")]
    private static partial ulong GstAdapterPrevDtsAtOffset(nint adapter, nuint offset, ulong* distance);

    /// <summary>The <c>gst_adapter_prev_offset</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_prev_offset")]
    private static partial ulong GstAdapterPrevOffset(nint adapter, ulong* distance);

    /// <summary>The <c>gst_adapter_prev_pts</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_prev_pts")]
    private static partial ulong GstAdapterPrevPts(nint adapter, ulong* distance);

    /// <summary>The <c>gst_adapter_prev_pts_at_offset</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_prev_pts_at_offset")]
    private static partial ulong GstAdapterPrevPtsAtOffset(nint adapter, nuint offset, ulong* distance);

    /// <summary>The <c>gst_adapter_pts_at_discont</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_pts_at_discont")]
    private static partial ulong GstAdapterPtsAtDiscont(nint adapter);

    /// <summary>The <c>gst_adapter_take_buffer</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_take_buffer")]
    private static partial nint GstAdapterTakeBuffer(nint adapter, nuint nbytes);

    /// <summary>The <c>gst_adapter_take_buffer_fast</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_take_buffer_fast")]
    private static partial nint GstAdapterTakeBufferFast(nint adapter, nuint nbytes);

    /// <summary>The <c>gst_adapter_take_buffer_list</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_take_buffer_list")]
    private static partial nint GstAdapterTakeBufferList(nint adapter, nuint nbytes);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAdapter</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstBase", EntryPoint = "gst_adapter_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Adapter(handle, transfer);
}
