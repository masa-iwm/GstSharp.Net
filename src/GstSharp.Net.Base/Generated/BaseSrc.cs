// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <summary>
/// This is a generic base class for source elements. The following
/// types of sources are supported:
/// </summary>
/// <remarks>
/// <para>
///   * random access sources like files
///   * seekable sources
///   * live sources
/// </para>
/// <para>
/// The source can be configured to operate in any #GstFormat with the
/// gst_base_src_set_format() method. The currently set format determines
/// the format of the internal #GstSegment and any %GST_EVENT_SEGMENT
/// events. The default format for #GstBaseSrc is %GST_FORMAT_BYTES.
/// </para>
/// <para>
/// #GstBaseSrc always supports push mode scheduling. If the following
/// conditions are met, it also supports pull mode scheduling:
/// </para>
/// <para>
///   * The format is set to %GST_FORMAT_BYTES (default).
///   * #GstBaseSrcClass::is_seekable returns %TRUE.
/// </para>
/// <para>
/// If all the conditions are met for operating in pull mode, #GstBaseSrc is
/// automatically seekable in push mode as well. The following conditions must
/// be met to make the element seekable in push mode when the format is not
/// %GST_FORMAT_BYTES:
/// </para>
/// <para>
/// * #GstBaseSrcClass::is_seekable returns %TRUE.
/// * #GstBaseSrcClass::query can convert all supported seek formats to the
///   internal format as set with gst_base_src_set_format().
/// * #GstBaseSrcClass::do_seek is implemented, performs the seek and returns
///    %TRUE.
/// </para>
/// <para>
/// When the element does not meet the requirements to operate in pull mode, the
/// offset and length in the #GstBaseSrcClass::create method should be ignored.
/// It is recommended to subclass #GstPushSrc instead, in this situation. If the
/// element can operate in pull mode but only with specific offsets and
/// lengths, it is allowed to generate an error when the wrong values are passed
/// to the #GstBaseSrcClass::create function.
/// </para>
/// <para>
/// #GstBaseSrc has support for live sources. Live sources are sources that when
/// paused discard data, such as audio or video capture devices. A typical live
/// source also produces data at a fixed rate and thus provides a clock to publish
/// this rate.
/// Use gst_base_src_set_live() to activate the live source mode.
/// </para>
/// <para>
/// A live source does not produce data in the PAUSED state. This means that the
/// #GstBaseSrcClass::create method will not be called in PAUSED but only in
/// PLAYING. To signal the pipeline that the element will not produce data, the
/// return value from the READY to PAUSED state will be
/// %GST_STATE_CHANGE_NO_PREROLL.
/// </para>
/// <para>
/// A typical live source will timestamp the buffers it creates with the
/// current running time of the pipeline. This is one reason why a live source
/// can only produce data in the PLAYING state, when the clock is actually
/// distributed and running.
/// </para>
/// <para>
/// Live sources that synchronize and block on the clock (an audio source, for
/// example) can use gst_base_src_wait_playing() when the
/// #GstBaseSrcClass::create function was interrupted by a state change to
/// PAUSED.
/// </para>
/// <para>
/// The #GstBaseSrcClass::get_times method can be used to implement pseudo-live
/// sources. It only makes sense to implement the #GstBaseSrcClass::get_times
/// function if the source is a live source. The #GstBaseSrcClass::get_times
/// function should return timestamps starting from 0, as if it were a non-live
/// source. The base class will make sure that the timestamps are transformed
/// into the current running_time. The base source will then wait for the
/// calculated running_time before pushing out the buffer.
/// </para>
/// <para>
/// For live sources, the base class will by default report a latency of 0.
/// For pseudo live sources, the base class will by default measure the difference
/// between the first buffer timestamp and the start time of get_times and will
/// report this value as the latency.
/// Subclasses should override the query function when this behaviour is not
/// acceptable.
/// </para>
/// <para>
/// There is only support in #GstBaseSrc for exactly one source pad, which
/// should be named "src". A source implementation (subclass of #GstBaseSrc)
/// should install a pad template in its class_init function, like so:
/// |[&lt;!-- language="C" --&gt;
/// static void
/// my_element_class_init (GstMyElementClass *klass)
/// {
///   GstElementClass *gstelement_class = GST_ELEMENT_CLASS (klass);
///   // srctemplate should be a #GstStaticPadTemplate with direction
///   // %GST_PAD_SRC and name "src"
///   gst_element_class_add_static_pad_template (gstelement_class, &amp;srctemplate);
/// </para>
/// <para>
///   gst_element_class_set_static_metadata (gstelement_class,
///      "Source name",
///      "Source",
///      "My Source element",
///      "The author &lt;my.sink@my.email&gt;");
/// }
/// ]|
/// </para>
/// <para>## Controlled shutdown of live sources in applications</para>
/// <para>
/// Applications that record from a live source may want to stop recording
/// in a controlled way, so that the recording is stopped, but the data
/// already in the pipeline is processed to the end (remember that many live
/// sources would go on recording forever otherwise). For that to happen the
/// application needs to make the source stop recording and send an EOS
/// event down the pipeline. The application would then wait for an
/// EOS message posted on the pipeline's bus to know when all data has
/// been processed and the pipeline can safely be stopped.
/// </para>
/// <para>
/// An application may send an EOS event to a source element to make it
/// perform the EOS logic (send EOS event downstream or post a
/// %GST_MESSAGE_SEGMENT_DONE on the bus). This can typically be done
/// with the gst_element_send_event() function on the element or its parent bin.
/// </para>
/// <para>
/// After the EOS has been sent to the element, the application should wait for
/// an EOS message to be posted on the pipeline's bus. Once this EOS message is
/// received, it may safely shut down the entire pipeline.
/// </para>
/// </remarks>
public abstract unsafe partial class BaseSrc : Gst.Element
{
    /// <summary>Wraps a native <c>GstBaseSrc</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstBaseSrc</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected BaseSrc(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Lets #GstBaseSrc sub-classes to know the memory @allocator
    /// used by the base class and its @params.
    /// </summary>
    /// <remarks>
    /// <para>Unref the @allocator after usage.</para>
    /// </remarks>
    /// <param name="allocator">The <c>allocator</c> argument.</param>
    /// <param name="params">
    /// The <c>@params</c> argument.
    /// The binding allocates the storage; on return the caller owns
    /// <paramref name="params"/> and disposes it.
    /// </param>
    public void GetAllocator(out Gst.Allocator? allocator, out Gst.AllocationParams @params)
    {
        nint instanceHandle = Handle;
        nint allocatorNative = default;
        nint @paramsNative = GstAllocationParamsNew();
        GstBaseSrcGetAllocator(instanceHandle, &allocatorNative, @paramsNative);
        System.GC.KeepAlive(this);
        @params = Gst.AllocationParams.FromNative(@paramsNative, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_allocation_params_new returned no value.");
        allocator = Gst.GObject.Object.FromNative<Gst.Allocator>(allocatorNative, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the number of bytes that @src will push out with each buffer.</summary>
    /// <returns>the number of bytes pushed with each buffer.</returns>
    public uint GetBlocksize()
    {
        uint nativeResult = GstBaseSrcGetBlocksize(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>The <c>gst_base_src_get_buffer_pool</c> function.</summary>
    /// <returns>
    /// the instance of the #GstBufferPool used
    /// by the src; unref it after usage.
    /// </returns>
    public Gst.BufferPool? GetBufferPool()
    {
        nint nativeResult = GstBaseSrcGetBufferPool(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.BufferPool>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Query if @src timestamps outgoing buffers based on the current running_time.</summary>
    /// <returns>%TRUE if the base class will automatically timestamp outgoing buffers.</returns>
    public bool GetDoTimestamp()
    {
        int nativeResult = GstBaseSrcGetDoTimestamp(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get the current async behaviour of @src. See also gst_base_src_set_async().</summary>
    /// <returns>%TRUE if @src is operating in async mode.</returns>
    public bool IsAsync()
    {
        int nativeResult = GstBaseSrcIsAsync(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Check if an element is in live mode.</summary>
    /// <returns>%TRUE if element is in live mode.</returns>
    public bool IsLive()
    {
        int nativeResult = GstBaseSrcIsLive(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Negotiates src pad caps with downstream elements.
    /// Unmarks GST_PAD_FLAG_NEED_RECONFIGURE in any case. But marks it again
    /// if #GstBaseSrcClass::negotiate fails.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Do not call this in the #GstBaseSrcClass::fill vmethod. Call this in
    /// #GstBaseSrcClass::create or in #GstBaseSrcClass::alloc, _before_ any
    /// buffer is allocated.
    /// </para>
    /// </remarks>
    /// <returns>%TRUE if the negotiation succeeded, else %FALSE.</returns>
    public bool Negotiate()
    {
        int nativeResult = GstBaseSrcNegotiate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Prepare a new seamless segment for emission downstream. This function must
    /// only be called by derived sub-classes, and only from the #GstBaseSrcClass::create function,
    /// as the stream-lock needs to be held.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The format for the new segment will be the current format of the source, as
    /// configured with gst_base_src_set_format()
    /// </para>
    /// </remarks>
    /// <param name="start">The <c>start</c> argument.</param>
    /// <param name="stop">The <c>stop</c> argument.</param>
    /// <param name="time">The <c>time</c> argument.</param>
    /// <returns>%TRUE if preparation of the seamless segment succeeded.</returns>
    [Obsolete("Use gst_base_src_new_segment() (deprecated since 1.18)")]
    public bool NewSeamlessSegment(long start, long stop, long time)
    {
        int nativeResult = GstBaseSrcNewSeamlessSegment(Handle, start, stop, time);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Prepare a new segment for emission downstream. This function must
    /// only be called by derived sub-classes, and only from the #GstBaseSrcClass::create function,
    /// as the stream-lock needs to be held.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The format for the @segment must be identical with the current format
    /// of the source, as configured with gst_base_src_set_format().
    /// </para>
    /// <para>
    /// The format of @src must not be %GST_FORMAT_UNDEFINED and the format
    /// should be configured via gst_base_src_set_format() before calling this method.
    /// </para>
    /// </remarks>
    /// <param name="segment">The <c>segment</c> argument.</param>
    /// <returns>%TRUE if preparation of new segment succeeded.</returns>
    public bool NewSegment(Gst.Segment segment)
    {
        ArgumentNullException.ThrowIfNull(segment);
        int nativeResult = GstBaseSrcNewSegment(Handle, segment.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(segment);
        return nativeResult != 0;
    }

    /// <summary>
    /// Send a new segment downstream. This function must
    /// only be called by derived sub-classes, and only from the #GstBaseSrcClass::create function,
    /// as the stream-lock needs to be held.
    /// This method also requires that an out caps has been configured, so
    /// gst_base_src_set_caps() needs to have been called before.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The format for the @segment must be identical with the current format
    /// of the source, as configured with gst_base_src_set_format().
    /// </para>
    /// <para>
    /// The format of @src must not be %GST_FORMAT_UNDEFINED and the format
    /// should be configured via gst_base_src_set_format() before calling this method.
    /// </para>
    /// <para>
    /// This is a variant of gst_base_src_new_segment() sending the segment right away,
    /// which can be useful to ensure events ordering.
    /// </para>
    /// </remarks>
    /// <param name="segment">The <c>segment</c> argument.</param>
    /// <returns>%TRUE if sending of new segment succeeded.</returns>
    public bool PushSegment(Gst.Segment segment)
    {
        ArgumentNullException.ThrowIfNull(segment);
        int nativeResult = GstBaseSrcPushSegment(Handle, segment.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(segment);
        return nativeResult != 0;
    }

    /// <summary>
    /// Query the source for the latency parameters. @live will be %TRUE when @src is
    /// configured as a live source. @min_latency and @max_latency will be set
    /// to the difference between the running time and the timestamp of the first
    /// buffer.
    /// </summary>
    /// <remarks>
    /// <para>This function is mostly used by subclasses.</para>
    /// </remarks>
    /// <param name="live">The <c>live</c> argument.</param>
    /// <param name="minLatency">The <c>minLatency</c> argument.</param>
    /// <param name="maxLatency">The <c>maxLatency</c> argument.</param>
    /// <returns>%TRUE if the query succeeded.</returns>
    public bool QueryLatency(out bool live, out Gst.ClockTime minLatency, out Gst.ClockTime maxLatency)
    {
        int liveNative = default;
        ulong minLatencyNative = default;
        ulong maxLatencyNative = default;
        int nativeResult = GstBaseSrcQueryLatency(Handle, &liveNative, &minLatencyNative, &maxLatencyNative);
        System.GC.KeepAlive(this);
        live = liveNative != 0;
        minLatency = new Gst.ClockTime(minLatencyNative);
        maxLatency = new Gst.ClockTime(maxLatencyNative);
        return nativeResult != 0;
    }

    /// <summary>
    /// Configure async behaviour in @src, no state change will block. The open,
    /// close, start, stop, play and pause virtual methods will be executed in a
    /// different thread and are thus allowed to perform blocking operations. Any
    /// blocking operation should be unblocked with the unlock vmethod.
    /// </summary>
    /// <param name="async">The <c>async</c> argument.</param>
    public void SetAsync(bool async)
    {
        GstBaseSrcSetAsync(Handle, async ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If @automatic_eos is %TRUE, @src will automatically go EOS if a buffer
    /// after the total size is returned. By default this is %TRUE but sources
    /// that can't return an authoritative size and only know that they're EOS
    /// when trying to read more should set this to %FALSE.
    /// </summary>
    /// <remarks>
    /// <para>
    /// When @src operates in %GST_FORMAT_TIME, #GstBaseSrc will send an EOS
    /// when a buffer outside of the currently configured segment is pushed if
    /// @automatic_eos is %TRUE. Since 1.16, if @automatic_eos is %FALSE an
    /// EOS will be pushed only when the #GstBaseSrcClass::create implementation
    /// returns %GST_FLOW_EOS.
    /// </para>
    /// </remarks>
    /// <param name="automaticEos">The <c>automaticEos</c> argument.</param>
    public void SetAutomaticEos(bool automaticEos)
    {
        GstBaseSrcSetAutomaticEos(Handle, automaticEos ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the number of bytes that @src will push out with each buffer. When
    /// @blocksize is set to -1, a default length will be used.
    /// </summary>
    /// <param name="blocksize">The <c>blocksize</c> argument.</param>
    public void SetBlocksize(uint blocksize)
    {
        GstBaseSrcSetBlocksize(Handle, blocksize);
        System.GC.KeepAlive(this);
    }

    /// <summary>Set new caps on the basesrc source pad.</summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>%TRUE if the caps could be set</returns>
    public bool SetCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstBaseSrcSetCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// Configure @src to automatically timestamp outgoing buffers based on the
    /// current running_time of the pipeline. This property is mostly useful for live
    /// sources.
    /// </summary>
    /// <param name="timestamp">The <c>timestamp</c> argument.</param>
    public void SetDoTimestamp(bool timestamp)
    {
        GstBaseSrcSetDoTimestamp(Handle, timestamp ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If not @dynamic, size is only updated when needed, such as when trying to
    /// read past current tracked size.  Otherwise, size is checked for upon each
    /// read.
    /// </summary>
    /// <param name="dynamic">The <c>dynamic</c> argument.</param>
    public void SetDynamicSize(bool dynamic)
    {
        GstBaseSrcSetDynamicSize(Handle, dynamic ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the default format of the source. This will be the format used
    /// for sending SEGMENT events and for performing seeks.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If a format of GST_FORMAT_BYTES is set, the element will be able to
    /// operate in pull mode if the #GstBaseSrcClass::is_seekable returns %TRUE.
    /// </para>
    /// <para>This function must only be called in states &lt; %GST_STATE_PAUSED.</para>
    /// </remarks>
    /// <param name="format">The <c>format</c> argument.</param>
    public void SetFormat(Gst.Format format)
    {
        GstBaseSrcSetFormat(Handle, (int)format);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If the element listens to a live source, @live should
    /// be set to %TRUE.
    /// </summary>
    /// <remarks>
    /// <para>
    /// A live source will not produce data in the PAUSED state and
    /// will therefore not be able to participate in the PREROLL phase
    /// of a pipeline. To signal this fact to the application and the
    /// pipeline, the state change return value of the live source will
    /// be GST_STATE_CHANGE_NO_PREROLL.
    /// </para>
    /// </remarks>
    /// <param name="live">The <c>live</c> argument.</param>
    public void SetLive(bool live)
    {
        GstBaseSrcSetLive(Handle, live ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Complete an asynchronous start operation. When the subclass overrides the
    /// start method, it should call gst_base_src_start_complete() when the start
    /// operation completes either from the same thread or from an asynchronous
    /// helper thread.
    /// </summary>
    /// <param name="ret">The <c>ret</c> argument.</param>
    public void StartComplete(Gst.FlowReturn ret)
    {
        GstBaseSrcStartComplete(Handle, (int)ret);
        System.GC.KeepAlive(this);
    }

    /// <summary>Wait until the start operation completes.</summary>
    /// <returns>a #GstFlowReturn.</returns>
    public Gst.FlowReturn StartWait()
    {
        int nativeResult = GstBaseSrcStartWait(Handle);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Subclasses can call this from their create virtual method implementation
    /// to submit a buffer list to be pushed out later. This is useful in
    /// cases where the create function wants to produce multiple buffers to be
    /// pushed out in one go in form of a #GstBufferList, which can reduce overhead
    /// drastically, especially for packetised inputs (for data streams where
    /// the packetisation/chunking is not important it is usually more efficient
    /// to return larger buffers instead).
    /// </summary>
    /// <remarks>
    /// <para>
    /// Subclasses that use this function from their create function must return
    /// %GST_FLOW_OK and no buffer from their create virtual method implementation.
    /// If a buffer is returned after a buffer list has also been submitted via this
    /// function the behaviour is undefined.
    /// </para>
    /// <para>
    /// Subclasses must only call this function once per create function call and
    /// subclasses must only call this function when the source operates in push
    /// mode.
    /// </para>
    /// <para>
    /// The <c>bufferList</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="bufferList">
    /// The <c>bufferList</c> argument.
    /// The call consumes it: <paramref name="bufferList"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="bufferList"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="bufferList"/> was disposed.
    /// </exception>
    public void SubmitBufferList(Gst.BufferList bufferList)
    {
        ArgumentNullException.ThrowIfNull(bufferList);
        nint instanceHandle = Handle;
        nint bufferListNative = bufferList.Handle;
        nint bufferListOwned = Gst.GstNative.MiniObjectRef(bufferListNative);
        GstBaseSrcSubmitBufferList(instanceHandle, bufferListOwned);
        System.GC.KeepAlive(this);
        bufferList.Dispose();
    }

    /// <summary>
    /// If the #GstBaseSrcClass::create method performs its own synchronisation
    /// against the clock it must unblock when going from PLAYING to the PAUSED state
    /// and call this method before continuing to produce the remaining data.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function will block until a state change to PLAYING happens (in which
    /// case this function returns %GST_FLOW_OK) or the processing must be stopped due
    /// to a state change to READY or a FLUSH event (in which case this function
    /// returns %GST_FLOW_FLUSHING).
    /// </para>
    /// </remarks>
    /// <returns>
    /// %GST_FLOW_OK if @src is PLAYING and processing can
    /// continue. Any other return value should be returned from the create vmethod.
    /// </returns>
    public Gst.FlowReturn WaitPlaying()
    {
        int nativeResult = GstBaseSrcWaitPlaying(Handle);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>The <c>blocksize</c> property.</summary>
    public uint Blocksize
    {
        get => GetBlocksize();
        set => SetBlocksize(value);
    }

    /// <summary>The <c>do-timestamp</c> property.</summary>
    public bool DoTimestamp
    {
        get => GetDoTimestamp();
        set => SetDoTimestamp(value);
    }

    /// <summary>See gst_base_src_set_automatic_eos()</summary>
    /// <remarks>
    /// <para>
    /// This property has no C getter; it is read through the GObject property
    /// system (<c>g_object_get_property</c>) and written through
    /// <see cref="SetAutomaticEos"/>.
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public bool AutomaticEos
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("automatic-eos");
            return holder.GetBoolean();
        }

        set => SetAutomaticEos(value);
    }

    /// <summary>The <c>num-buffers</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public int NumBuffers
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("num-buffers");
            return holder.GetInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("num-buffers");
            holder.SetInt(value);
            SetPropertyValue("num-buffers", in holder);
        }
    }

    /// <summary>The <c>typefind</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool Typefind
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("typefind");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("typefind");
            holder.SetBoolean(value);
            SetPropertyValue("typefind", in holder);
        }
    }

    /// <summary>The <c>gst_base_src_get_allocator</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_get_allocator")]
    private static partial void GstBaseSrcGetAllocator(nint src, nint* allocator, nint @params);

    /// <summary>The <c>gst_base_src_get_blocksize</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_get_blocksize")]
    private static partial uint GstBaseSrcGetBlocksize(nint src);

    /// <summary>The <c>gst_base_src_get_buffer_pool</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_get_buffer_pool")]
    private static partial nint GstBaseSrcGetBufferPool(nint src);

    /// <summary>The <c>gst_base_src_get_do_timestamp</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_get_do_timestamp")]
    private static partial int GstBaseSrcGetDoTimestamp(nint src);

    /// <summary>The <c>gst_base_src_is_async</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_is_async")]
    private static partial int GstBaseSrcIsAsync(nint src);

    /// <summary>The <c>gst_base_src_is_live</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_is_live")]
    private static partial int GstBaseSrcIsLive(nint src);

    /// <summary>The <c>gst_base_src_negotiate</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_negotiate")]
    private static partial int GstBaseSrcNegotiate(nint src);

    /// <summary>The <c>gst_base_src_new_seamless_segment</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_new_seamless_segment")]
    private static partial int GstBaseSrcNewSeamlessSegment(nint src, long start, long stop, long time);

    /// <summary>The <c>gst_base_src_new_segment</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_new_segment")]
    private static partial int GstBaseSrcNewSegment(nint src, nint segment);

    /// <summary>The <c>gst_base_src_push_segment</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_push_segment")]
    private static partial int GstBaseSrcPushSegment(nint src, nint segment);

    /// <summary>The <c>gst_base_src_query_latency</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_query_latency")]
    private static partial int GstBaseSrcQueryLatency(nint src, int* live, ulong* minLatency, ulong* maxLatency);

    /// <summary>The <c>gst_base_src_set_async</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_async")]
    private static partial void GstBaseSrcSetAsync(nint src, int async);

    /// <summary>The <c>gst_base_src_set_automatic_eos</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_automatic_eos")]
    private static partial void GstBaseSrcSetAutomaticEos(nint src, int automaticEos);

    /// <summary>The <c>gst_base_src_set_blocksize</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_blocksize")]
    private static partial void GstBaseSrcSetBlocksize(nint src, uint blocksize);

    /// <summary>The <c>gst_base_src_set_caps</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_caps")]
    private static partial int GstBaseSrcSetCaps(nint src, nint caps);

    /// <summary>The <c>gst_base_src_set_do_timestamp</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_do_timestamp")]
    private static partial void GstBaseSrcSetDoTimestamp(nint src, int timestamp);

    /// <summary>The <c>gst_base_src_set_dynamic_size</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_dynamic_size")]
    private static partial void GstBaseSrcSetDynamicSize(nint src, int dynamic);

    /// <summary>The <c>gst_base_src_set_format</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_format")]
    private static partial void GstBaseSrcSetFormat(nint src, int format);

    /// <summary>The <c>gst_base_src_set_live</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_set_live")]
    private static partial void GstBaseSrcSetLive(nint src, int live);

    /// <summary>The <c>gst_base_src_start_complete</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_start_complete")]
    private static partial void GstBaseSrcStartComplete(nint basesrc, int ret);

    /// <summary>The <c>gst_base_src_start_wait</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_start_wait")]
    private static partial int GstBaseSrcStartWait(nint basesrc);

    /// <summary>The <c>gst_base_src_submit_buffer_list</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_submit_buffer_list")]
    private static partial void GstBaseSrcSubmitBufferList(nint src, nint bufferList);

    /// <summary>The <c>gst_base_src_wait_playing</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_wait_playing")]
    private static partial int GstBaseSrcWaitPlaying(nint src);

    /// <summary>The <c>gst_allocation_params_new</c> entry point, which allocates the storage of a caller allocated out parameter.</summary>
    /// <returns>A new, zeroed instance the caller owns.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_allocation_params_new")]
    private static partial nint GstAllocationParamsNew();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstBaseSrc</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstBase", EntryPoint = "gst_base_src_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstBaseSrc</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : BaseSrc
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
