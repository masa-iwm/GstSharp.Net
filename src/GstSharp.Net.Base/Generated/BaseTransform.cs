// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <summary>
/// This base class is for filter elements that process data. Elements
/// that are suitable for implementation using #GstBaseTransform are ones
/// where the size and caps of the output is known entirely from the input
/// caps and buffer sizes. These include elements that directly transform
/// one buffer into another, modify the contents of a buffer in-place, as
/// well as elements that collate multiple input buffers into one output buffer,
/// or that expand one input buffer into multiple output buffers. See below
/// for more concrete use cases.
/// </summary>
/// <remarks>
/// <para>It provides for:</para>
/// <para>
/// * one sinkpad and one srcpad
/// * Possible formats on sink and source pad implemented
///   with custom transform_caps function. By default uses
///   same format on sink and source.
/// </para>
/// <para>
/// * Handles state changes
/// * Does flushing
/// * Push mode
/// * Pull mode if the sub-class transform can operate on arbitrary data
/// </para>
/// <para># Use Cases</para>
/// <para>## Passthrough mode</para>
/// <para>
///   * Element has no interest in modifying the buffer. It may want to inspect it,
///     in which case the element should have a transform_ip function. If there
///     is no transform_ip function in passthrough mode, the buffer is pushed
///     intact.
/// </para>
/// <para>
///   * The #GstBaseTransformClass.passthrough_on_same_caps variable
///     will automatically set/unset passthrough based on whether the
///     element negotiates the same caps on both pads.
/// </para>
/// <para>
///   * #GstBaseTransformClass.passthrough_on_same_caps on an element that
///     doesn't implement a transform_caps function is useful for elements that
///     only inspect data (such as level)
/// </para>
/// <para>  * Example elements</para>
/// <para>
///     * Level
///     * Videoscale, audioconvert, videoconvert, audioresample in certain modes.
/// </para>
/// <para>## Modifications in-place - input buffer and output buffer are the same thing.</para>
/// <para>
/// * The element must implement a transform_ip function.
/// * Output buffer size must &lt;= input buffer size
/// * If the always_in_place flag is set, non-writable buffers will be copied
///   and passed to the transform_ip function, otherwise a new buffer will be
///   created and the transform function called.
/// </para>
/// <para>
/// * Incoming writable buffers will be passed to the transform_ip function
///   immediately.
/// * only implementing transform_ip and not transform implies always_in_place = %TRUE
/// </para>
/// <para>
///   * Example elements:
///     * Volume
///     * Audioconvert in certain modes (signed/unsigned conversion)
///     * videoconvert in certain modes (endianness swapping)
/// </para>
/// <para>## Modifications only to the caps/metadata of a buffer</para>
/// <para>
/// * The element does not require writable data, but non-writable buffers
///   should be subbuffered so that the meta-information can be replaced.
/// </para>
/// <para>
/// * Elements wishing to operate in this mode should replace the
///   prepare_output_buffer method to create subbuffers of the input buffer
///   and set always_in_place to %TRUE
/// </para>
/// <para>
/// * Example elements
///   * Capsfilter when setting caps on outgoing buffers that have
///     none.
///   * identity when it is going to re-timestamp buffers by
///     datarate.
/// </para>
/// <para>
/// ## Normal mode
///   * always_in_place flag is not set, or there is no transform_ip function
///   * Element will receive an input buffer and output buffer to operate on.
///   * Output buffer is allocated by calling the prepare_output_buffer function.
///   * Example elements:
///     * Videoscale, videoconvert, audioconvert when doing
///     scaling/conversions
/// </para>
/// <para>
/// ## Special output buffer allocations
///   * Elements which need to do special allocation of their output buffers
///     beyond allocating output buffers via the negotiated allocator or
///     buffer pool should implement the prepare_output_buffer method.
/// </para>
/// <para>
///   * Example elements:
///     * efence
/// </para>
/// <para># Sub-class settable flags on GstBaseTransform</para>
/// <para>* passthrough</para>
/// <para>
///   * Implies that in the current configuration, the sub-class is not interested in modifying the buffers.
///   * Elements which are always in passthrough mode whenever the same caps has been negotiated on both pads can set the class variable passthrough_on_same_caps to have this behaviour automatically.
/// </para>
/// <para>
/// * always_in_place
///   * Determines whether a non-writable buffer will be copied before passing
///     to the transform_ip function.
/// </para>
/// <para>
///   * Implied %TRUE if no transform function is implemented.
///   * Implied %FALSE if ONLY transform function is implemented.
/// </para>
/// </remarks>
public abstract unsafe partial class BaseTransform : Gst.Element
{
    /// <summary>Wraps a native <c>GstBaseTransform</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal BaseTransform(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>The <c>gst_base_transform_get_buffer_pool</c> function.</summary>
    /// <returns>
    /// the instance of the #GstBufferPool used
    /// by @trans; free it after use
    /// </returns>
    public Gst.BufferPool? GetBufferPool()
    {
        nint nativeResult = GstBaseTransformGetBufferPool(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.BufferPool>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>See if @trans is configured as a in_place transform.</summary>
    /// <returns>%TRUE if the transform is configured in in_place mode.</returns>
    public bool IsInPlace()
    {
        int nativeResult = GstBaseTransformIsInPlace(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>See if @trans is configured as a passthrough transform.</summary>
    /// <returns>%TRUE if the transform is configured in passthrough mode.</returns>
    public bool IsPassthrough()
    {
        int nativeResult = GstBaseTransformIsPassthrough(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Queries if the transform will handle QoS.</summary>
    /// <returns>%TRUE if QoS is enabled.</returns>
    public bool IsQosEnabled()
    {
        int nativeResult = GstBaseTransformIsQosEnabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Negotiates src pad caps with downstream elements if the source pad is
    /// marked as needing reconfiguring. Unmarks GST_PAD_FLAG_NEED_RECONFIGURE in
    /// any case. But marks it again if negotiation fails.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Do not call this in the #GstBaseTransformClass::transform or
    /// #GstBaseTransformClass::transform_ip vmethod. Call this in
    /// #GstBaseTransformClass::submit_input_buffer,
    /// #GstBaseTransformClass::prepare_output_buffer or in
    /// #GstBaseTransformClass::generate_output _before_ any output buffer is
    /// allocated.
    /// </para>
    /// <para>
    /// It will be default be called when handling an ALLOCATION query or at the
    /// very beginning of the default #GstBaseTransformClass::submit_input_buffer
    /// implementation.
    /// </para>
    /// </remarks>
    /// <returns>%TRUE if the negotiation succeeded, else %FALSE.</returns>
    public bool Reconfigure()
    {
        int nativeResult = GstBaseTransformReconfigure(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Instructs @trans to request renegotiation upstream. This function is
    /// typically called after properties on the transform were set that
    /// influence the input format.
    /// </summary>
    public void ReconfigureSink()
    {
        GstBaseTransformReconfigureSink(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Instructs @trans to renegotiate a new downstream transform on the next
    /// buffer. This function is typically called after properties on the transform
    /// were set that influence the output format.
    /// </summary>
    public void ReconfigureSrc()
    {
        GstBaseTransformReconfigureSrc(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If @gap_aware is %FALSE (the default), output buffers will have the
    /// %GST_BUFFER_FLAG_GAP flag unset.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If set to %TRUE, the element must handle output buffers with this flag set
    /// correctly, i.e. it can assume that the buffer contains neutral data but must
    /// unset the flag if the output is no neutral data.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="gapAware">The <c>gapAware</c> argument.</param>
    public void SetGapAware(bool gapAware)
    {
        GstBaseTransformSetGapAware(Handle, gapAware ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Determines whether a non-writable buffer will be copied before passing
    /// to the transform_ip function.
    /// </summary>
    /// <remarks>
    /// <para>
    ///   * Always %TRUE if no transform function is implemented.
    ///   * Always %FALSE if ONLY transform function is implemented.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="inPlace">The <c>inPlace</c> argument.</param>
    public void SetInPlace(bool inPlace)
    {
        GstBaseTransformSetInPlace(Handle, inPlace ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set passthrough mode for this filter by default. This is mostly
    /// useful for filters that do not care about negotiation.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Always %TRUE for filters which don't implement either a transform
    /// or transform_ip or generate_output method.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="passthrough">The <c>passthrough</c> argument.</param>
    public void SetPassthrough(bool passthrough)
    {
        GstBaseTransformSetPassthrough(Handle, passthrough ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// If @prefer_passthrough is %TRUE (the default), @trans will check and
    /// prefer passthrough caps from the list of caps returned by the
    /// transform_caps vmethod.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If set to %FALSE, the element must order the caps returned from the
    /// transform_caps function in such a way that the preferred format is
    /// first in the list. This can be interesting for transforms that can do
    /// passthrough transforms but prefer to do something else, like a
    /// capsfilter.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="preferPassthrough">The <c>preferPassthrough</c> argument.</param>
    public void SetPreferPassthrough(bool preferPassthrough)
    {
        GstBaseTransformSetPreferPassthrough(Handle, preferPassthrough ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>Enable or disable QoS handling in the transform.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public void SetQosEnabled(bool enabled)
    {
        GstBaseTransformSetQosEnabled(Handle, enabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the QoS parameters in the transform. This function is called internally
    /// when a QOS event is received but subclasses can provide custom information
    /// when needed.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="proportion">The <c>proportion</c> argument.</param>
    /// <param name="diff">The <c>diff</c> argument.</param>
    /// <param name="timestamp">The <c>timestamp</c> argument.</param>
    public void UpdateQos(double proportion, long diff, Gst.ClockTime timestamp)
    {
        GstBaseTransformUpdateQos(Handle, proportion, diff, timestamp.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Updates the srcpad caps and sends the caps downstream. This function
    /// can be used by subclasses when they have already negotiated their caps
    /// but found a change in them (or computed new information). This way,
    /// they can notify downstream about that change without losing any
    /// buffer.
    /// </summary>
    /// <param name="updatedCaps">The <c>updatedCaps</c> argument.</param>
    /// <returns>%TRUE if the caps could be sent downstream %FALSE otherwise</returns>
    public bool UpdateSrcCaps(Gst.Caps updatedCaps)
    {
        ArgumentNullException.ThrowIfNull(updatedCaps);
        int nativeResult = GstBaseTransformUpdateSrcCaps(Handle, updatedCaps.Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_base_transform_get_buffer_pool</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_get_buffer_pool")]
    private static partial nint GstBaseTransformGetBufferPool(nint trans);

    /// <summary>The <c>gst_base_transform_is_in_place</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_is_in_place")]
    private static partial int GstBaseTransformIsInPlace(nint trans);

    /// <summary>The <c>gst_base_transform_is_passthrough</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_is_passthrough")]
    private static partial int GstBaseTransformIsPassthrough(nint trans);

    /// <summary>The <c>gst_base_transform_is_qos_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_is_qos_enabled")]
    private static partial int GstBaseTransformIsQosEnabled(nint trans);

    /// <summary>The <c>gst_base_transform_reconfigure</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_reconfigure")]
    private static partial int GstBaseTransformReconfigure(nint trans);

    /// <summary>The <c>gst_base_transform_reconfigure_sink</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_reconfigure_sink")]
    private static partial void GstBaseTransformReconfigureSink(nint trans);

    /// <summary>The <c>gst_base_transform_reconfigure_src</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_reconfigure_src")]
    private static partial void GstBaseTransformReconfigureSrc(nint trans);

    /// <summary>The <c>gst_base_transform_set_gap_aware</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_set_gap_aware")]
    private static partial void GstBaseTransformSetGapAware(nint trans, int gapAware);

    /// <summary>The <c>gst_base_transform_set_in_place</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_set_in_place")]
    private static partial void GstBaseTransformSetInPlace(nint trans, int inPlace);

    /// <summary>The <c>gst_base_transform_set_passthrough</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_set_passthrough")]
    private static partial void GstBaseTransformSetPassthrough(nint trans, int passthrough);

    /// <summary>The <c>gst_base_transform_set_prefer_passthrough</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_set_prefer_passthrough")]
    private static partial void GstBaseTransformSetPreferPassthrough(nint trans, int preferPassthrough);

    /// <summary>The <c>gst_base_transform_set_qos_enabled</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_set_qos_enabled")]
    private static partial void GstBaseTransformSetQosEnabled(nint trans, int enabled);

    /// <summary>The <c>gst_base_transform_update_qos</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_update_qos")]
    private static partial void GstBaseTransformUpdateQos(nint trans, double proportion, long diff, ulong timestamp);

    /// <summary>The <c>gst_base_transform_update_src_caps</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_update_src_caps")]
    private static partial int GstBaseTransformUpdateSrcCaps(nint trans, nint updatedCaps);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstBaseTransform</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstBase", EntryPoint = "gst_base_transform_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstBaseTransform</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : BaseTransform
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
