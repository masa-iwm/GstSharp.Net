// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <summary>
/// Utility struct to help handling #GstFlowReturn combination. Useful for
/// #GstElement&lt;!-- --&gt;s that have multiple source pads and need to combine
/// the different #GstFlowReturn for those pads.
/// </summary>
/// <remarks>
/// <para>
/// #GstFlowCombiner works by using the last #GstFlowReturn for all #GstPad
/// it has in its list and computes the combined return value and provides
/// it to the caller.
/// </para>
/// <para>
/// To add a new pad to the #GstFlowCombiner use gst_flow_combiner_add_pad().
/// The new #GstPad is stored with a default value of %GST_FLOW_OK.
/// </para>
/// <para>In case you want a #GstPad to be removed, use gst_flow_combiner_remove_pad().</para>
/// <para>
/// Please be aware that this struct isn't thread safe as its designed to be
///  used by demuxers, those usually will have a single thread operating it.
/// </para>
/// <para>These functions will take refs on the passed #GstPad&lt;!-- --&gt;s.</para>
/// <para>
/// Aside from reducing the user's code size, the main advantage of using this
/// helper struct is to follow the standard rules for #GstFlowReturn combination.
/// These rules are:
/// </para>
/// <para>
/// * %GST_FLOW_EOS: only if all returns are EOS too
/// * %GST_FLOW_NOT_LINKED: only if all returns are NOT_LINKED too
/// * %GST_FLOW_ERROR or below: if at least one returns an error return
/// * %GST_FLOW_NOT_NEGOTIATED: if at least one returns a not-negotiated return
/// * %GST_FLOW_FLUSHING: if at least one returns flushing
/// * %GST_FLOW_OK: otherwise
/// </para>
/// <para>
/// %GST_FLOW_ERROR or below, GST_FLOW_NOT_NEGOTIATED and GST_FLOW_FLUSHING are
/// returned immediately from the gst_flow_combiner_update_flow() function.
/// </para>
/// </remarks>
public sealed unsafe partial class FlowCombiner : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstFlowCombiner</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal FlowCombiner(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a <c>GstFlowCombiner</c> that GStreamer keeps owning, for the length of one call.</summary>
    /// <param name="handle">The value that is lent to managed code.</param>
    /// <returns>The wrapper, which holds nothing of its own.</returns>
    /// <remarks>
    /// This is how the override of a virtual method receives a <c>transfer none</c>
    /// boxed value. No <c>g_boxed_copy</c> is taken, so every write the override
    /// makes lands in the instance the caller of the slot reads back, and disposing
    /// the wrapper only detaches it: a wrapper kept past the call throws instead of
    /// freeing a value it never owned. See <see cref="Gst.Interop.Borrowed"/>.
    /// </remarks>
    internal static FlowCombiner Borrow(nint handle) => new(new Gst.Interop.Borrowed(handle));

    /// <summary>Wraps a <c>GstFlowCombiner</c> the caller keeps owning.</summary>
    /// <param name="borrowed">The value that is lent to managed code.</param>
    private FlowCombiner(Gst.Interop.Borrowed borrowed)
        : base(borrowed, new Gst.GObject.GType(GetGType()))
    {
    }

    /// <summary>Wraps a native <c>GstFlowCombiner</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static FlowCombiner? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>Creates a new #GstFlowCombiner, use gst_flow_combiner_free() to free it.</summary>
    /// <returns>A new #GstFlowCombiner</returns>
    public static Gst.Base.FlowCombiner New()
    {
        nint nativeResult = GstFlowCombinerNew();
        return Gst.Base.FlowCombiner.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_flow_combiner_new returned no value.");
    }

    /// <summary>Adds a new #GstPad to the #GstFlowCombiner.</summary>
    /// <param name="pad">the #GstPad that is being added</param>
    public void AddPad(Gst.Pad pad)
    {
        ArgumentNullException.ThrowIfNull(pad);
        GstFlowCombinerAddPad(Handle, pad.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pad);
    }

    /// <summary>Removes all pads from a #GstFlowCombiner and resets it to its initial state.</summary>
    public void Clear()
    {
        GstFlowCombinerClear(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Removes a #GstPad from the #GstFlowCombiner.</summary>
    /// <param name="pad">the #GstPad to remove</param>
    public void RemovePad(Gst.Pad pad)
    {
        ArgumentNullException.ThrowIfNull(pad);
        GstFlowCombinerRemovePad(Handle, pad.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pad);
    }

    /// <summary>Reset flow combiner and all pads to their initial state without removing pads.</summary>
    public void Reset()
    {
        GstFlowCombinerReset(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Computes the combined flow return for the pads in it.</summary>
    /// <remarks>
    /// <para>
    /// The #GstFlowReturn parameter should be the last flow return update for a pad
    /// in this #GstFlowCombiner. It will use this value to be able to shortcut some
    /// combinations and avoid looking over all pads again. e.g. The last combined
    /// return is the same as the latest obtained #GstFlowReturn.
    /// </para>
    /// </remarks>
    /// <param name="fret">the latest #GstFlowReturn received for a pad in this #GstFlowCombiner</param>
    /// <returns>The combined #GstFlowReturn</returns>
    public Gst.FlowReturn UpdateFlow(Gst.FlowReturn fret)
    {
        int nativeResult = GstFlowCombinerUpdateFlow(Handle, (int)fret);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Sets the provided pad's last flow return to provided value and computes
    /// the combined flow return for the pads in it.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The #GstFlowReturn parameter should be the last flow return update for a pad
    /// in this #GstFlowCombiner. It will use this value to be able to shortcut some
    /// combinations and avoid looking over all pads again. e.g. The last combined
    /// return is the same as the latest obtained #GstFlowReturn.
    /// </para>
    /// </remarks>
    /// <param name="pad">the #GstPad whose #GstFlowReturn to update</param>
    /// <param name="fret">the latest #GstFlowReturn received for a pad in this #GstFlowCombiner</param>
    /// <returns>The combined #GstFlowReturn</returns>
    public Gst.FlowReturn UpdatePadFlow(Gst.Pad pad, Gst.FlowReturn fret)
    {
        ArgumentNullException.ThrowIfNull(pad);
        int nativeResult = GstFlowCombinerUpdatePadFlow(Handle, pad.Handle, (int)fret);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pad);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>The <c>gst_flow_combiner_new</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_new")]
    private static partial nint GstFlowCombinerNew();

    /// <summary>The <c>gst_flow_combiner_add_pad</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_add_pad")]
    private static partial void GstFlowCombinerAddPad(nint combiner, nint pad);

    /// <summary>The <c>gst_flow_combiner_clear</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_clear")]
    private static partial void GstFlowCombinerClear(nint combiner);

    /// <summary>The <c>gst_flow_combiner_remove_pad</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_remove_pad")]
    private static partial void GstFlowCombinerRemovePad(nint combiner, nint pad);

    /// <summary>The <c>gst_flow_combiner_reset</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_reset")]
    private static partial void GstFlowCombinerReset(nint combiner);

    /// <summary>The <c>gst_flow_combiner_update_flow</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_update_flow")]
    private static partial int GstFlowCombinerUpdateFlow(nint combiner, int fret);

    /// <summary>The <c>gst_flow_combiner_update_pad_flow</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_update_pad_flow")]
    private static partial int GstFlowCombinerUpdatePadFlow(nint combiner, nint pad, int fret);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstFlowCombiner</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstBase", EntryPoint = "gst_flow_combiner_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new FlowCombiner(handle, transfer);
}
