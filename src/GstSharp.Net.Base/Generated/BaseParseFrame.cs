// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <summary>
/// Frame (context) data passed to each frame parsing virtual methods.  In
/// addition to providing the data to be checked for a valid frame or an already
/// identified frame, it conveys additional metadata or control information
/// from and to the subclass w.r.t. the particular frame in question (rather
/// than global parameters).  Some of these may apply to each parsing stage, others
/// only to some a particular one.  These parameters are effectively zeroed at start
/// of each frame's processing, i.e. parsing virtual method invocation sequence.
/// </summary>
public sealed unsafe partial class BaseParseFrame : Gst.GObject.Boxed
{
    /// <summary>Wraps a native <c>GstBaseParseFrame</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal BaseParseFrame(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, new Gst.GObject.GType(GetGType()), transfer)
    {
    }

    /// <summary>Wraps a <c>GstBaseParseFrame</c> that GStreamer keeps owning, for the length of one call.</summary>
    /// <param name="handle">The value that is lent to managed code.</param>
    /// <returns>The wrapper, which holds nothing of its own.</returns>
    /// <remarks>
    /// This is how the override of a virtual method receives a <c>transfer none</c>
    /// boxed value. No <c>g_boxed_copy</c> is taken, so every write the override
    /// makes lands in the instance the caller of the slot reads back, and disposing
    /// the wrapper only detaches it: a wrapper kept past the call throws instead of
    /// freeing a value it never owned. See <see cref="Gst.Interop.Borrowed"/>.
    /// </remarks>
    internal static BaseParseFrame Borrow(nint handle) => new(new Gst.Interop.Borrowed(handle));

    /// <summary>Wraps a <c>GstBaseParseFrame</c> the caller keeps owning.</summary>
    /// <param name="borrowed">The value that is lent to managed code.</param>
    private BaseParseFrame(Gst.Interop.Borrowed borrowed)
        : base(borrowed, new Gst.GObject.GType(GetGType()))
    {
    }

    /// <summary>Reads the <c>buffer</c> field of <c>GstBaseParseFrame</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// </remarks>
    /// <returns>input data to be parsed for frames.</returns>
    public Gst.Buffer? GetBuffer()
    {
        Gst.Buffer? value = Gst.Buffer.FromNative(((BaseParseFrameRaw*)Handle)->Buffer, Gst.Interop.Transfer.None);
        System.GC.KeepAlive(this);
        return value;
    }

    /// <summary>Reads the <c>out_buffer</c> field of <c>GstBaseParseFrame</c>.</summary>
    /// <remarks>
    /// <para>
    /// The value is read out of the structure at the moment of the call. What
    /// comes back owns a reference of its own - a mini object is referenced, a
    /// boxed value copied - so the caller disposes it, which is why this is a
    /// method rather than a property.
    /// </para>
    /// </remarks>
    /// <returns>output data.</returns>
    public Gst.Buffer? GetOutBuffer()
    {
        Gst.Buffer? value = Gst.Buffer.FromNative(((BaseParseFrameRaw*)Handle)->OutBuffer, Gst.Interop.Transfer.None);
        System.GC.KeepAlive(this);
        return value;
    }

    /// <summary>
    /// a combination of input and output #GstBaseParseFrameFlags that
    ///  convey additional context to subclass or allow subclass to tune
    ///  subsequent #GstBaseParse actions.
    /// </summary>
    public uint Flags
    {
        get
        {
            uint value = ((BaseParseFrameRaw*)Handle)->Flags;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>
    /// media specific offset of input frame
    ///   Note that a converter may have a different one on the frame's buffer.
    /// </summary>
    public ulong Offset
    {
        get
        {
            ulong value = ((BaseParseFrameRaw*)Handle)->Offset;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>
    /// subclass can set this to indicates the metadata overhead
    ///   for the given frame, which is then used to enable more accurate bitrate
    ///   computations. If this is -1, it is assumed that this frame should be
    ///   skipped in bitrate calculation.
    /// </summary>
    public int Overhead
    {
        get
        {
            int value = ((BaseParseFrameRaw*)Handle)->Overhead;
            System.GC.KeepAlive(this);
            return value;
        }
    }

    /// <summary>Wraps a native <c>GstBaseParseFrame</c>, mapping the null pointer onto <see langword="null"/>.</summary>
    /// <param name="handle">The native instance, or <c>0</c>.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The wrapper, or <see langword="null"/> when <paramref name="handle"/> is <c>0</c>.</returns>
    internal static BaseParseFrame? FromNative(nint handle, Gst.Interop.Transfer transfer) =>
        handle == 0 ? null : new(handle, transfer);

    /// <summary>
    /// Allocates a new #GstBaseParseFrame. This function is mainly for bindings,
    /// elements written in C should usually allocate the frame on the stack and
    /// then use gst_base_parse_frame_init() to initialise it.
    /// </summary>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <param name="flags">The <c>flags</c> argument.</param>
    /// <param name="overhead">The <c>overhead</c> argument.</param>
    /// <returns>
    /// a newly-allocated #GstBaseParseFrame. Free with
    ///     gst_base_parse_frame_free() when no longer needed.
    /// </returns>
    public static Gst.Base.BaseParseFrame New(Gst.Buffer buffer, Gst.Base.BaseParseFrameFlags flags, int overhead)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        nint nativeResult = GstBaseParseFrameNew(buffer.Handle, (int)flags, overhead);
        System.GC.KeepAlive(buffer);
        return Gst.Base.BaseParseFrame.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_base_parse_frame_new returned no value.");
    }

    /// <summary>Copies a #GstBaseParseFrame.</summary>
    /// <returns>A copy of @frame</returns>
    public Gst.Base.BaseParseFrame Copy()
    {
        nint nativeResult = GstBaseParseFrameCopy(Handle);
        System.GC.KeepAlive(this);
        return Gst.Base.BaseParseFrame.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_base_parse_frame_copy returned no value.");
    }

    /// <summary>
    /// Sets a #GstBaseParseFrame to initial state.  Currently this means
    /// all public fields are zero-ed and a private flag is set to make
    /// sure gst_base_parse_frame_free() only frees the contents but not
    /// the actual frame. Use this function to initialise a #GstBaseParseFrame
    /// allocated on the stack.
    /// </summary>
    public void Init()
    {
        GstBaseParseFrameInit(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_base_parse_frame_new</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_parse_frame_new")]
    private static partial nint GstBaseParseFrameNew(nint buffer, int flags, int overhead);

    /// <summary>The <c>gst_base_parse_frame_copy</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_parse_frame_copy")]
    private static partial nint GstBaseParseFrameCopy(nint frame);

    /// <summary>The <c>gst_base_parse_frame_init</c> entry point.</summary>
    [LibraryImport("GstBase", EntryPoint = "gst_base_parse_frame_init")]
    private static partial void GstBaseParseFrameInit(nint frame);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstBaseParseFrame</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstBase", EntryPoint = "gst_base_parse_frame_get_type")]
    internal static partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new BaseParseFrame(handle, transfer);
}

/// <summary>The native layout of <c>GstBaseParseFrame</c>.</summary>
/// <remarks>
/// <para>
/// The mirror is only ever read through a pointer into memory that GStreamer
/// owns; it is never allocated, assigned or copied.
/// </para>
/// </remarks>
[StructLayout(LayoutKind.Sequential)]
internal unsafe struct BaseParseFrameRaw
{
    /// <summary>The <c>buffer</c> field.</summary>
    internal nint Buffer;

    /// <summary>The <c>out_buffer</c> field.</summary>
    internal nint OutBuffer;

    /// <summary>The <c>flags</c> field.</summary>
    internal uint Flags;

    /// <summary>The <c>offset</c> field.</summary>
    internal ulong Offset;

    /// <summary>The <c>overhead</c> field.</summary>
    internal int Overhead;

    /// <summary>The <c>size</c> field.</summary>
    internal int Size;

    /// <summary>The <c>_gst_reserved_i</c> field.</summary>
    internal GstReservedIArray GstReservedI;

    /// <summary>The <c>_gst_reserved_p</c> field.</summary>
    internal GstReservedPArray GstReservedP;

    /// <summary>The <c>_private_flags</c> field.</summary>
    internal uint PrivateFlags;

    /// <summary>Inline storage of the 2 elements of the <c>_gst_reserved_i</c> field.</summary>
    [InlineArray(2)]
    internal struct GstReservedIArray
    {
        private uint _element0;
    }

    /// <summary>Inline storage of the 2 elements of the <c>_gst_reserved_p</c> field.</summary>
    [InlineArray(2)]
    internal struct GstReservedPArray
    {
        private nint _element0;
    }
}
