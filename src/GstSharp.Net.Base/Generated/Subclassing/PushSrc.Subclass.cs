// <auto-generated/>
// Generated by GstSharp.Generator from GstBase-1.0.gir. Do not edit.

#nullable enable

using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace Gst.Base;

/// <content>The subclassing surface of <c>GstPushSrc</c>.</content>
public unsafe partial class PushSrc
{
    /// <summary>
    /// Constructs the managed subclass that
    /// <see cref="Gst.GObject.SubclassType.NewInstance()"/> created an instance of.
    /// </summary>
    /// <param name="args">The instance, as the registration handed it out.</param>
    /// <exception cref="System.ArgumentException">The instance is not a <c>GstPushSrc</c>.</exception>
    protected PushSrc(Gst.GObject.SubclassCtorArgs args)
        : this(args.HandleFor(GetGType()), args.Transfer)
    {
    }

    /// <summary>
    /// Gets the declaration of <c>GstPushSrc.create</c>, for a subclass that
    /// overrides <see cref="OnCreate"/>.
    /// </summary>
    public static new Gst.GObject.VfuncOverride CreateOverride { get; } = new(
        &GetGType,
        Gst.Base.PushSrcClassRaw.CreateOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, nint*, int>)&CreateTrampoline);

    /// <summary>
    /// Gets the declaration of <c>GstPushSrc.alloc</c>, for a subclass that
    /// overrides <see cref="OnAlloc"/>.
    /// </summary>
    public static new Gst.GObject.VfuncOverride AllocOverride { get; } = new(
        &GetGType,
        Gst.Base.PushSrcClassRaw.AllocOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, nint*, int>)&AllocTrampoline);

    /// <summary>
    /// Gets the declaration of <c>GstPushSrc.fill</c>, for a subclass that
    /// overrides <see cref="OnFill"/>.
    /// </summary>
    public static new Gst.GObject.VfuncOverride FillOverride { get; } = new(
        &GetGType,
        Gst.Base.PushSrcClassRaw.FillOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, nint, int>)&FillTrampoline);

    /// <summary>Registers a managed subclass of <c>GstPushSrc</c> with GObject.</summary>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// It <b>has to</b> add a pad template named <c>src</c>.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GstPushSrc</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass(
        string typeName,
        Action<Gst.GObject.ClassConfig> configureClass,
        params Gst.GObject.VfuncOverride[] overrides) =>
        DefineSubclassCore(typeName, configureClass, overrides, null, null);

    /// <summary>Registers a managed subclass of <c>GstPushSrc</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// It <b>has to</b> add a pad template named <c>src</c>.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GstPushSrc</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ClassConfig> configureClass,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : PushSrc, Gst.GObject.IManagedSubclass<TSelf> =>
        DefineSubclassCore(typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), null);

    /// <summary>Registers a managed subclass of <c>GstPushSrc</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// It <b>has to</b> add a pad template named <c>src</c>.
    /// </param>
    /// <param name="options">
    /// The optional parts of the registration, the interfaces the subclass
    /// implements above all. Interfaces can be declared here and nowhere else:
    /// GObject refuses to attach one once the class of the type is being
    /// initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, a declared slot belongs to a
    /// class that <c>GstPushSrc</c> does not derive from, an interface is declared
    /// twice, or the parent type implements it already.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static new Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ClassConfig> configureClass,
        Gst.GObject.SubclassOptions options,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : PushSrc, Gst.GObject.IManagedSubclass<TSelf>
    {
        ArgumentNullException.ThrowIfNull(options);

        return DefineSubclassCore(
            typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), options);
    }

    private static Gst.GObject.SubclassType DefineSubclassCore(
        string typeName,
        Action<Gst.GObject.ClassConfig> configureClass,
        Gst.GObject.VfuncOverride[] overrides,
        Func<Gst.GObject.SubclassCtorArgs, Gst.GObject.Object>? wrapFactory,
        Gst.GObject.SubclassOptions? options)
    {
        ArgumentNullException.ThrowIfNull(configureClass);

        Gst.GObject.SubclassType type = Gst.GObject.SubclassType.Define(
            new Gst.GObject.GType(GetGType()), typeName, configureClass, overrides, wrapFactory, options,
            requiredPadTemplates: new[] { "src" });
        return type;
    }

    /// <summary>
    /// Ask the subclass to create a buffer, the default implementation will call alloc if
    /// no allocated @buf is provided and then call fill.
    /// </summary>
    /// <remarks>
    /// <para>Answering Ok without a buffer is not a way to produce nothing: the base class posts an
    /// element error on the bus for a NULL buffer, so a source with nothing to hand out answers
    /// a flow result that says so - Eos, Flushing or an error.</para>
    /// </remarks>
    /// <param name="buffer">
    /// The <c>buffer</c> argument.
    /// What the override leaves here is handed to the caller with one added
    /// reference; the wrapper keeps its own.
    /// </param>
    /// <returns>What <c>create</c> answers.</returns>
    protected virtual Gst.FlowReturn OnCreate(out Gst.Buffer? buffer) =>
        ChainUpCreate(out buffer);

    /// <summary>Allocate memory for a buffer.</summary>
    /// <param name="buf">
    /// The <c>buf</c> argument.
    /// What the override leaves here is handed to the caller with one added
    /// reference; the wrapper keeps its own.
    /// </param>
    /// <returns>What <c>alloc</c> answers.</returns>
    protected virtual Gst.FlowReturn OnAlloc(out Gst.Buffer? buf) =>
        ChainUpAlloc(out buf);

    /// <summary>Ask the subclass to fill the buffer with data.</summary>
    /// <param name="buf">
    /// The <c>buf</c> argument.
    /// The element lends this for the duration of the call; keep a copy to retain it.
    /// </param>
    /// <returns>What <c>fill</c> answers.</returns>
    protected virtual Gst.FlowReturn OnFill(Gst.Buffer buf) =>
        ChainUpFill(buf);

    /// <summary>Runs the implementation of <c>create</c> below the managed override.</summary>
    /// <remarks>
    /// <para>Answering Ok without a buffer is not a way to produce nothing: the base class posts an
    /// element error on the bus for a NULL buffer, so a source with nothing to hand out answers
    /// a flow result that says so - Eos, Flushing or an error.</para>
    /// </remarks>
    /// <param name="buffer">
    /// The <c>buffer</c> argument.
    /// What the override leaves here is handed to the caller with one added
    /// reference; the wrapper keeps its own.
    /// </param>
    /// <returns>What <c>create</c> answers.</returns>
    protected Gst.FlowReturn ChainUpCreate(out Gst.Buffer? buffer)
    {
        nint bufferNative = nint.Zero;
        Gst.FlowReturn result = ChainUpCreate(Handle, &bufferNative);
        GC.KeepAlive(this);
        buffer = bufferNative == nint.Zero ? null : Gst.Buffer.FromNative(bufferNative, Gst.Interop.Transfer.Full);
        return result;
    }

    /// <summary>Runs the implementation of <c>alloc</c> below the managed override.</summary>
    /// <param name="buf">
    /// The <c>buf</c> argument.
    /// What the override leaves here is handed to the caller with one added
    /// reference; the wrapper keeps its own.
    /// </param>
    /// <returns>What <c>alloc</c> answers.</returns>
    protected Gst.FlowReturn ChainUpAlloc(out Gst.Buffer? buf)
    {
        nint bufNative = nint.Zero;
        Gst.FlowReturn result = ChainUpAlloc(Handle, &bufNative);
        GC.KeepAlive(this);
        buf = bufNative == nint.Zero ? null : Gst.Buffer.FromNative(bufNative, Gst.Interop.Transfer.Full);
        return result;
    }

    /// <summary>Runs the implementation of <c>fill</c> below the managed override.</summary>
    /// <param name="buf">
    /// The <c>buf</c> argument.
    /// The element lends this for the duration of the call; keep a copy to retain it.
    /// </param>
    /// <returns>What <c>fill</c> answers.</returns>
    protected Gst.FlowReturn ChainUpFill(Gst.Buffer buf)
    {
        ArgumentNullException.ThrowIfNull(buf);
        Gst.FlowReturn result = ChainUpFill(Handle, buf.Handle);
        GC.KeepAlive(this);
        GC.KeepAlive(buf);
        return result;
    }

    private static Gst.FlowReturn ChainUpCreate(nint src, nint* buffer)
    {
        delegate* unmanaged[Cdecl]<nint, nint*, int> slot =
            (delegate* unmanaged[Cdecl]<nint, nint*, int>)ParentClassOf(src)->Create;

        if (slot is null)
        {
            *buffer = default;
            return Gst.FlowReturn.NotSupported;
        }

        return (Gst.FlowReturn)slot(src, buffer);
    }

    private static Gst.FlowReturn ChainUpAlloc(nint src, nint* buf)
    {
        delegate* unmanaged[Cdecl]<nint, nint*, int> slot =
            (delegate* unmanaged[Cdecl]<nint, nint*, int>)ParentClassOf(src)->Alloc;

        if (slot is null)
        {
            // gst_push_src_alloc falls back to gst_base_src_default_alloc, the pooled
            // allocation, when the GstPushSrcClass slot is NULL. No class of the library
            // sets that slot, so the chain-up has to reach that implementation itself.
            return Gst.Base.BaseSrcDefaults.Alloc(src, buf);
        }

        return (Gst.FlowReturn)slot(src, buf);
    }

    private static Gst.FlowReturn ChainUpFill(nint src, nint buf)
    {
        delegate* unmanaged[Cdecl]<nint, nint, int> slot =
            (delegate* unmanaged[Cdecl]<nint, nint, int>)ParentClassOf(src)->Fill;

        if (slot is null)
        {
            return Gst.FlowReturn.NotSupported;
        }

        return (Gst.FlowReturn)slot(src, buf);
    }

    /// <summary>
    /// Returns the class the registration captured, which is the one an override
    /// chains up through.
    /// </summary>
    /// <param name="instance">The native <c>GstPushSrc</c>.</param>
    /// <returns>The parent class of the managed subclass.</returns>
    private static Gst.Base.PushSrcClassRaw* ParentClassOf(nint instance) =>
        (Gst.Base.PushSrcClassRaw*)Gst.GObject.SubclassRegistry.DescriptorFor(instance).ParentClass;

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static int CreateTrampoline(nint src, nint* buffer)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(src) is not PushSrc managed)
            {
                return (int)(ChainUpCreate(src, buffer));
            }

            Gst.Buffer? bufferValue = null;

            try
            {
                Gst.FlowReturn result = managed.OnCreate(out bufferValue);

                if (result == Gst.FlowReturn.Ok)
                {
                    nint bufferHandle = bufferValue is null ? nint.Zero : bufferValue.Handle;
                    if (bufferHandle != nint.Zero)
                    {
                        Gst.GstNative.MiniObjectRef(bufferHandle);
                    }
                    *buffer = bufferHandle;
                }
                return (int)(result);
            }
            finally
            {
                bufferValue?.Dispose();
            }
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return (int)Gst.FlowReturn.Error;
        }
    }

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static int AllocTrampoline(nint src, nint* buf)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(src) is not PushSrc managed)
            {
                return (int)(ChainUpAlloc(src, buf));
            }

            Gst.Buffer? bufValue = null;

            try
            {
                Gst.FlowReturn result = managed.OnAlloc(out bufValue);

                if (result == Gst.FlowReturn.Ok)
                {
                    nint bufHandle = bufValue is null ? nint.Zero : bufValue.Handle;
                    if (bufHandle != nint.Zero)
                    {
                        Gst.GstNative.MiniObjectRef(bufHandle);
                    }
                    *buf = bufHandle;
                }
                return (int)(result);
            }
            finally
            {
                bufValue?.Dispose();
            }
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return (int)Gst.FlowReturn.Error;
        }
    }

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static int FillTrampoline(nint src, nint buf)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(src) is not PushSrc managed)
            {
                return (int)(ChainUpFill(src, buf));
            }

            using Gst.Buffer? bufValue = buf == nint.Zero ? null : Gst.Buffer.Borrow(buf);
            return (int)(managed.OnFill(bufValue!));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return (int)Gst.FlowReturn.Error;
        }
    }
}
