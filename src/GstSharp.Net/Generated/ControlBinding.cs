// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// A base class for value mapping objects that attaches control sources to #GObject
/// properties. Such an object is taking one or more #GstControlSource instances,
/// combines them and maps the resulting value to the type and value range of the
/// bound property.
/// </summary>
public abstract unsafe partial class ControlBinding : Gst.Object
{
    /// <summary>Wraps a native <c>GstControlBinding</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstControlBinding</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected ControlBinding(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Gets the value for the given controlled property at the requested time.</summary>
    /// <param name="timestamp">the time the control-change should be read from</param>
    /// <returns>
    /// the GValue of the property at the given time,
    /// or %NULL if the property isn't controlled.
    /// Ownership is transferred: dispose the value. It is empty when the call
    /// returns <c>NULL</c>, and disposing an empty value does nothing.
    /// </returns>
    public Gst.GObject.Value GetValue(Gst.ClockTime timestamp)
    {
        nint nativeResult = GstControlBindingGetValue(Handle, timestamp.Nanoseconds);
        System.GC.KeepAlive(this);
        return Gst.GObject.Value.TakeOwnership(nativeResult);
    }

    /// <summary>Checks if the control binding is disabled.</summary>
    /// <returns>%TRUE if the binding is inactive</returns>
    public bool IsDisabled()
    {
        int nativeResult = GstControlBindingIsDisabled(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// This function is used to disable a control binding for some time, i.e.
    /// gst_object_sync_values() will do nothing.
    /// </summary>
    /// <param name="disabled">
    /// boolean that specifies whether to disable the controller
    /// or not.
    /// </param>
    public void SetDisabled(bool disabled)
    {
        GstControlBindingSetDisabled(Handle, disabled ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the property of the @object, according to the #GstControlSources that
    /// handles it and for the given timestamp.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If this function fails, it is most likely the application developers fault.
    /// Most probably the control sources are not setup correctly.
    /// </para>
    /// </remarks>
    /// <param name="object">the object that has controlled properties</param>
    /// <param name="timestamp">the time that should be processed</param>
    /// <param name="lastSync">the last time this was called</param>
    /// <returns>
    /// %TRUE if the controller value could be applied to the object
    /// property, %FALSE otherwise
    /// </returns>
    public bool SyncValues(Gst.Object @object, Gst.ClockTime timestamp, Gst.ClockTime lastSync)
    {
        ArgumentNullException.ThrowIfNull(@object);
        int nativeResult = GstControlBindingSyncValues(Handle, @object.Handle, timestamp.Nanoseconds, lastSync.Nanoseconds);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(@object);
        return nativeResult != 0;
    }

    /// <summary>The <c>name</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>The property is construct-only and therefore read-only here.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public string? PropertyName
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("name");
            return holder.GetString();
        }
    }

    /// <summary>The <c>object</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>The property is construct-only and therefore read-only here.</para>
    /// <para>
    /// Reading hands back the interned wrapper of the object, which the binding
    /// keeps; it is not the reader's to dispose.
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public Gst.Object? Object
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("object");
            return (Gst.Object?)holder.GetObject();
        }
    }

    /// <summary>The <c>gst_control_binding_get_value</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_control_binding_get_value")]
    private static partial nint GstControlBindingGetValue(nint binding, ulong timestamp);

    /// <summary>The <c>gst_control_binding_is_disabled</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_control_binding_is_disabled")]
    private static partial int GstControlBindingIsDisabled(nint binding);

    /// <summary>The <c>gst_control_binding_set_disabled</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_control_binding_set_disabled")]
    private static partial void GstControlBindingSetDisabled(nint binding, int disabled);

    /// <summary>The <c>gst_control_binding_sync_values</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_control_binding_sync_values")]
    private static partial int GstControlBindingSyncValues(nint binding, nint @object, ulong timestamp, ulong lastSync);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstControlBinding</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_control_binding_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstControlBinding</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : ControlBinding
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
