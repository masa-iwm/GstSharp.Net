// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// The #GstBus is an object responsible for delivering #GstMessage packets in
/// a first-in first-out way from the streaming threads (see #GstTask) to the
/// application.
/// </summary>
/// <remarks>
/// <para>
/// Since the application typically only wants to deal with delivery of these
/// messages from one thread, the GstBus will marshall the messages between
/// different threads. This is important since the actual streaming of media
/// is done in another thread than the application.
/// </para>
/// <para>
/// The GstBus provides support for #GSource based notifications. This makes it
/// possible to handle the delivery in the glib #GMainLoop.
/// </para>
/// <para>
/// The #GSource callback function gst_bus_async_signal_func() can be used to
/// convert all bus messages into signal emissions.
/// </para>
/// <para>
/// A message is posted on the bus with the gst_bus_post() method. With the
/// gst_bus_peek() and gst_bus_pop() methods one can look at or retrieve a
/// previously posted message.
/// </para>
/// <para>
/// The bus can be polled with the gst_bus_poll() method. This methods blocks
/// up to the specified timeout value until one of the specified messages types
/// is posted on the bus. The application can then gst_bus_pop() the messages
/// from the bus to handle them.
/// Alternatively the application can register an asynchronous bus function
/// using gst_bus_add_watch_full() or gst_bus_add_watch(). This function will
/// install a #GSource in the default glib main loop and will deliver messages
/// a short while after they have been posted. Note that the main loop should
/// be running for the asynchronous callbacks.
/// </para>
/// <para>
/// It is also possible to get messages from the bus without any thread
/// marshalling with the gst_bus_set_sync_handler() method. This makes it
/// possible to react to a message in the same thread that posted the
/// message on the bus. This should only be used if the application is able
/// to deal with messages from different threads.
/// </para>
/// <para>Every #GstPipeline has one bus.</para>
/// <para>
/// Note that a #GstPipeline will set its bus into flushing state when changing
/// from READY to NULL state.
/// </para>
/// </remarks>
public unsafe partial class Bus : Gst.Object
{
    /// <summary>Wraps a native <c>GstBus</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstBus</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Bus(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new #GstBus instance.</summary>
    /// <returns>a new #GstBus instance</returns>
    public static Gst.Bus New()
    {
        nint nativeResult = GstBusNew();
        return Gst.GObject.Object.FromNative<Gst.Bus>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bus_new returned no value.");
    }

    /// <summary>
    /// Adds a bus signal watch to the default main context with the default priority
    /// ( %G_PRIORITY_DEFAULT ). It is also possible to use a non-default
    /// main context set up using g_main_context_push_thread_default() (before
    /// one had to create a bus watch source and attach it to the desired main
    /// context 'manually').
    /// </summary>
    /// <remarks>
    /// <para>
    /// After calling this statement, the bus will emit the "message" signal for each
    /// message posted on the bus.
    /// </para>
    /// <para>
    /// This function may be called multiple times. To clean up, the caller is
    /// responsible for calling gst_bus_remove_signal_watch() as many times as this
    /// function is called.
    /// </para>
    /// </remarks>
    public void AddSignalWatch()
    {
        GstBusAddSignalWatch(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Adds a bus signal watch to the default main context with the given @priority
    /// (e.g. %G_PRIORITY_DEFAULT). It is also possible to use a non-default main
    /// context set up using g_main_context_push_thread_default()
    /// (before one had to create a bus watch source and attach it to the desired
    /// main context 'manually').
    /// </summary>
    /// <remarks>
    /// <para>
    /// After calling this statement, the bus will emit the "message" signal for each
    /// message posted on the bus when the #GMainLoop is running.
    /// </para>
    /// <para>
    /// This function may be called multiple times. To clean up, the caller is
    /// responsible for calling gst_bus_remove_signal_watch() as many times as this
    /// function is called.
    /// </para>
    /// <para>
    /// There can only be a single bus watch per bus, you must remove any signal
    /// watch before you can set another type of watch.
    /// </para>
    /// </remarks>
    /// <param name="priority">The <c>priority</c> argument.</param>
    public void AddSignalWatchFull(int priority)
    {
        GstBusAddSignalWatchFull(Handle, priority);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Adds a bus watch to the default main context with the given @priority (e.g.
    /// %G_PRIORITY_DEFAULT). It is also possible to use a non-default  main
    /// context set up using g_main_context_push_thread_default() (before
    /// one had to create a bus watch source and attach it to the desired main
    /// context 'manually').
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function is used to receive asynchronous messages in the main loop.
    /// There can only be a single bus watch per bus, you must remove it before you
    /// can set a new one.
    /// </para>
    /// <para>The bus watch will only work if a #GMainLoop is being run.</para>
    /// <para>
    /// When @func is called, the message belongs to the caller; if you want to
    /// keep a copy of it, call gst_message_ref() before leaving @func.
    /// </para>
    /// <para>
    /// The watch can be removed using gst_bus_remove_watch() or by returning %FALSE
    /// from @func. If the watch was added to the default main context it is also
    /// possible to remove the watch using g_source_remove().
    /// </para>
    /// <para>
    /// The bus watch will take its own reference to the @bus, so it is safe to unref
    /// @bus using gst_object_unref() after setting the bus watch.
    /// </para>
    /// </remarks>
    /// <param name="priority">The <c>priority</c> argument.</param>
    /// <param name="func">A function to call when a message is received.</param>
    /// <returns>The event source id or 0 if @bus already got an event source.</returns>
    public uint AddWatch(int priority, Gst.BusFunc func)
    {
        nint instanceHandle = Handle;
        ArgumentNullException.ThrowIfNull(func);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        uint nativeResult = GstBusAddWatchFull(instanceHandle, priority, Gst.BusFuncTrampoline.Pointer, funcState.UserData, (nint)Gst.Interop.CallbackHandle.DestroyNotify);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// A helper #GstBusFunc that can be used to convert all asynchronous messages
    /// into signals.
    /// </summary>
    /// <param name="message">The <c>message</c> argument.</param>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <returns>%TRUE</returns>
    public bool AsyncSignalFunc(Gst.Message message, nint data)
    {
        ArgumentNullException.ThrowIfNull(message);
        int nativeResult = GstBusAsyncSignalFunc(Handle, message.Handle, data);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(message);
        return nativeResult != 0;
    }

    /// <summary>
    /// Instructs GStreamer to stop emitting the "sync-message" signal for this bus.
    /// See gst_bus_enable_sync_message_emission() for more information.
    /// </summary>
    /// <remarks>
    /// <para>
    /// In the event that multiple pieces of code have called
    /// gst_bus_enable_sync_message_emission(), the sync-message emissions will only
    /// be stopped after all calls to gst_bus_enable_sync_message_emission() were
    /// "cancelled" by calling this function. In this way the semantics are exactly
    /// the same as gst_object_ref() that which calls enable should also call
    /// disable.
    /// </para>
    /// </remarks>
    public void DisableSyncMessageEmission()
    {
        GstBusDisableSyncMessageEmission(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Instructs GStreamer to emit the "sync-message" signal after running the bus's
    /// sync handler. This function is here so that code can ensure that they can
    /// synchronously receive messages without having to affect what the bin's sync
    /// handler is.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function may be called multiple times. To clean up, the caller is
    /// responsible for calling gst_bus_disable_sync_message_emission() as many times
    /// as this function is called.
    /// </para>
    /// <para>
    /// While this function looks similar to gst_bus_add_signal_watch(), it is not
    /// exactly the same -- this function enables *synchronous* emission of
    /// signals when messages arrive; gst_bus_add_signal_watch() adds an idle callback
    /// to pop messages off the bus *asynchronously*. The sync-message signal
    /// comes from the thread of whatever object posted the message; the "message"
    /// signal is marshalled to the main thread via the #GMainLoop.
    /// </para>
    /// </remarks>
    public void EnableSyncMessageEmission()
    {
        GstBusEnableSyncMessageEmission(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Checks if there are pending messages on the bus that
    /// should be handled.
    /// </summary>
    /// <returns>
    /// %TRUE if there are messages on the bus to be handled, %FALSE
    /// otherwise.
    /// </returns>
    public bool HavePending()
    {
        int nativeResult = GstBusHavePending(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Peeks the message on the top of the bus' queue. The message will remain
    /// on the bus' message queue.
    /// </summary>
    /// <returns>
    /// the #GstMessage that is on the
    ///     bus, or %NULL if the bus is empty.
    /// </returns>
    public Gst.Message? Peek()
    {
        nint nativeResult = GstBusPeek(Handle);
        System.GC.KeepAlive(this);
        return Gst.Message.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Polls the bus for messages. Will block while waiting for messages to come.
    /// You can specify a maximum time to poll with the @timeout parameter. If
    /// @timeout is negative, this function will block indefinitely.
    /// </summary>
    /// <remarks>
    /// <para>
    /// All messages not in @events will be popped off the bus and will be ignored.
    /// It is not possible to use message enums beyond #GST_MESSAGE_EXTENDED in the
    /// @events mask
    /// </para>
    /// <para>
    /// Because poll is implemented using the "message" signal enabled by
    /// gst_bus_add_signal_watch(), calling gst_bus_poll() will cause the "message"
    /// signal to be emitted for every message that poll sees. Thus a "message"
    /// signal handler will see the same messages that this function sees -- neither
    /// will steal messages from the other.
    /// </para>
    /// <para>
    /// This function will run a #GMainLoop from the default main context when
    /// polling.
    /// </para>
    /// <para>
    /// You should never use this function, since it is pure evil. This is
    /// especially true for GUI applications based on Gtk+ or Qt, but also for any
    /// other non-trivial application that uses the GLib main loop. As this function
    /// runs a GLib main loop, any callback attached to the default GLib main
    /// context may be invoked. This could be timeouts, GUI events, I/O events etc.;
    /// even if gst_bus_poll() is called with a 0 timeout. Any of these callbacks
    /// may do things you do not expect, e.g. destroy the main application window or
    /// some other resource; change other application state; display a dialog and
    /// run another main loop until the user clicks it away. In short, using this
    /// function may add a lot of complexity to your code through unexpected
    /// re-entrancy and unexpected changes to your application's state.
    /// </para>
    /// <para>
    /// For 0 timeouts use gst_bus_pop_filtered() instead of this function; for
    /// other short timeouts use gst_bus_timed_pop_filtered(); everything else is
    /// better handled by setting up an asynchronous bus watch and doing things
    /// from there.
    /// </para>
    /// </remarks>
    /// <param name="events">The <c>events</c> argument.</param>
    /// <param name="timeout">The <c>timeout</c> argument.</param>
    /// <returns>
    /// the message that was received,
    ///     or %NULL if the poll timed out.
    /// </returns>
    public Gst.Message? Poll(Gst.MessageType events, Gst.ClockTime timeout)
    {
        nint nativeResult = GstBusPoll(Handle, (uint)events, timeout.Nanoseconds);
        System.GC.KeepAlive(this);
        return Gst.Message.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Gets a message from the bus.</summary>
    /// <returns>
    /// the #GstMessage that is on the
    ///     bus, or %NULL if the bus is empty.
    /// </returns>
    public Gst.Message? Pop()
    {
        nint nativeResult = GstBusPop(Handle);
        System.GC.KeepAlive(this);
        return Gst.Message.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets a message matching @type from the bus.  Will discard all messages on
    /// the bus that do not match @type and that have been posted before the first
    /// message that does match @type.  If there is no message matching @type on
    /// the bus, all messages will be discarded. It is not possible to use message
    /// enums beyond #GST_MESSAGE_EXTENDED in the @events mask.
    /// </summary>
    /// <param name="types">The <c>types</c> argument.</param>
    /// <returns>
    /// the next #GstMessage matching
    ///     @type that is on the bus, or %NULL if the bus is empty or there
    ///     is no message matching @type.
    /// </returns>
    public Gst.Message? PopFiltered(Gst.MessageType types)
    {
        nint nativeResult = GstBusPopFiltered(Handle, (uint)types);
        System.GC.KeepAlive(this);
        return Gst.Message.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Posts a message on the given bus. Ownership of the message
    /// is taken by the bus.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The <c>message</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="message">
    /// The <c>message</c> argument.
    /// The call consumes it: <paramref name="message"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>%TRUE if the message could be posted, %FALSE if the bus is flushing.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="message"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="message"/> was disposed.
    /// </exception>
    public bool Post(Gst.Message message)
    {
        ArgumentNullException.ThrowIfNull(message);
        nint instanceHandle = Handle;
        nint messageNative = message.Handle;
        nint messageOwned = Gst.GstNative.MiniObjectRef(messageNative);
        int nativeResult = GstBusPost(instanceHandle, messageOwned);
        System.GC.KeepAlive(this);
        message.Dispose();
        return nativeResult != 0;
    }

    /// <summary>Removes a signal watch previously added with gst_bus_add_signal_watch().</summary>
    public void RemoveSignalWatch()
    {
        GstBusRemoveSignalWatch(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Removes an installed bus watch from @bus.</summary>
    /// <returns>%TRUE on success or %FALSE if @bus has no event source.</returns>
    public bool RemoveWatch()
    {
        int nativeResult = GstBusRemoveWatch(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// If @flushing, flushes out and unrefs any messages queued in the bus. Releases
    /// references to the message origin objects. Will flush future messages until
    /// gst_bus_set_flushing() sets @flushing to %FALSE.
    /// </summary>
    /// <param name="flushing">The <c>flushing</c> argument.</param>
    public void SetFlushing(bool flushing)
    {
        GstBusSetFlushing(Handle, flushing ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// A helper #GstBusSyncHandler that can be used to convert all synchronous
    /// messages into signals.
    /// </summary>
    /// <param name="message">The <c>message</c> argument.</param>
    /// <param name="data">The <c>data</c> argument.</param>
    /// <returns>%GST_BUS_PASS</returns>
    public Gst.BusSyncReply SyncSignalHandler(Gst.Message message, nint data)
    {
        ArgumentNullException.ThrowIfNull(message);
        int nativeResult = GstBusSyncSignalHandler(Handle, message.Handle, data);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(message);
        return (Gst.BusSyncReply)nativeResult;
    }

    /// <summary>Gets a message from the bus, waiting up to the specified timeout.</summary>
    /// <remarks>
    /// <para>
    /// If @timeout is 0, this function behaves like gst_bus_pop(). If @timeout is
    /// #GST_CLOCK_TIME_NONE, this function will block forever until a message was
    /// posted on the bus.
    /// </para>
    /// </remarks>
    /// <param name="timeout">The <c>timeout</c> argument.</param>
    /// <returns>
    /// the #GstMessage that is on the
    ///     bus after the specified timeout or %NULL if the bus is empty
    ///     after the timeout expired.
    /// </returns>
    public Gst.Message? TimedPop(Gst.ClockTime timeout)
    {
        nint nativeResult = GstBusTimedPop(Handle, timeout.Nanoseconds);
        System.GC.KeepAlive(this);
        return Gst.Message.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets a message from the bus whose type matches the message type mask @types,
    /// waiting up to the specified timeout (and discarding any messages that do not
    /// match the mask provided).
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @timeout is 0, this function behaves like gst_bus_pop_filtered(). If
    /// @timeout is #GST_CLOCK_TIME_NONE, this function will block forever until a
    /// matching message was posted on the bus.
    /// </para>
    /// </remarks>
    /// <param name="timeout">The <c>timeout</c> argument.</param>
    /// <param name="types">The <c>types</c> argument.</param>
    /// <returns>
    /// a #GstMessage matching the
    ///     filter in @types, or %NULL if no matching message was found on
    ///     the bus until the timeout expired.
    /// </returns>
    public Gst.Message? TimedPopFiltered(Gst.ClockTime timeout, Gst.MessageType types)
    {
        nint nativeResult = GstBusTimedPopFiltered(Handle, timeout.Nanoseconds, (uint)types);
        System.GC.KeepAlive(this);
        return Gst.Message.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The arguments of the <c>message</c> signal of <c>GstBus</c>.</summary>
    public sealed class MessageSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="MessageSignalArgs"/> class.</summary>
        /// <param name="message">the message that has been posted asynchronously</param>
        internal MessageSignalArgs(Gst.Message message)
        {
            Message = message;
        }

        /// <summary>the message that has been posted asynchronously</summary>
        /// <remarks>
        /// The value is only valid while the handler runs: the wrapper is disposed
        /// once it returns. Read out of it what is needed, or copy it where the
        /// type offers a copy.
        /// </remarks>
        public Gst.Message Message { get; }
    }

    /// <summary>
    /// A message has been posted on the bus. This signal is emitted from a
    /// #GSource added to the mainloop. this signal will only be emitted when
    /// there is a #GMainLoop running.
    /// </summary>
    /// <remarks>
    /// The signal is detailed. The handler is connected to <c>message</c>
    /// without a detail, so it runs for every detail of the signal.
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Bus.MessageSignalArgs> Message
    {
        add => Gst.SignalConnections.Add(this, "message", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&MessageTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "message", value);
    }

    /// <summary>The native handler of the <c>message</c> signal of <c>GstBus</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void MessageTrampoline(nint instance, nint message, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Bus.MessageSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            using Gst.Message messageValue = Gst.Message.FromNative(message, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The message signal of GstBus passed no message.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Bus.MessageSignalArgs(messageValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>sync-message</c> signal of <c>GstBus</c>.</summary>
    public sealed class SyncMessageSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="SyncMessageSignalArgs"/> class.</summary>
        /// <param name="message">the message that has been posted synchronously</param>
        internal SyncMessageSignalArgs(Gst.Message message)
        {
            Message = message;
        }

        /// <summary>the message that has been posted synchronously</summary>
        /// <remarks>
        /// The value is only valid while the handler runs: the wrapper is disposed
        /// once it returns. Read out of it what is needed, or copy it where the
        /// type offers a copy.
        /// </remarks>
        public Gst.Message Message { get; }
    }

    /// <summary>
    /// A message has been posted on the bus. This signal is emitted from the
    /// thread that posted the message so one has to be careful with locking.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This signal will not be emitted by default, you have to call
    /// gst_bus_enable_sync_message_emission() before.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The signal is detailed. The handler is connected to <c>sync-message</c>
    /// without a detail, so it runs for every detail of the signal.
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Bus.SyncMessageSignalArgs> SyncMessage
    {
        add => Gst.SignalConnections.Add(this, "sync-message", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&SyncMessageTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "sync-message", value);
    }

    /// <summary>The native handler of the <c>sync-message</c> signal of <c>GstBus</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void SyncMessageTrampoline(nint instance, nint message, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Bus.SyncMessageSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            using Gst.Message messageValue = Gst.Message.FromNative(message, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The sync-message signal of GstBus passed no message.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Bus.SyncMessageSignalArgs(messageValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_bus_new</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_new")]
    private static partial nint GstBusNew();

    /// <summary>The <c>gst_bus_add_signal_watch</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_add_signal_watch")]
    private static partial void GstBusAddSignalWatch(nint bus);

    /// <summary>The <c>gst_bus_add_signal_watch_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_add_signal_watch_full")]
    private static partial void GstBusAddSignalWatchFull(nint bus, int priority);

    /// <summary>The <c>gst_bus_add_watch_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_add_watch_full")]
    private static partial uint GstBusAddWatchFull(nint bus, int priority, nint func, nint userData, nint notify);

    /// <summary>The <c>gst_bus_async_signal_func</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_async_signal_func")]
    private static partial int GstBusAsyncSignalFunc(nint bus, nint message, nint data);

    /// <summary>The <c>gst_bus_disable_sync_message_emission</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_disable_sync_message_emission")]
    private static partial void GstBusDisableSyncMessageEmission(nint bus);

    /// <summary>The <c>gst_bus_enable_sync_message_emission</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_enable_sync_message_emission")]
    private static partial void GstBusEnableSyncMessageEmission(nint bus);

    /// <summary>The <c>gst_bus_have_pending</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_have_pending")]
    private static partial int GstBusHavePending(nint bus);

    /// <summary>The <c>gst_bus_peek</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_peek")]
    private static partial nint GstBusPeek(nint bus);

    /// <summary>The <c>gst_bus_poll</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_poll")]
    private static partial nint GstBusPoll(nint bus, uint events, ulong timeout);

    /// <summary>The <c>gst_bus_pop</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_pop")]
    private static partial nint GstBusPop(nint bus);

    /// <summary>The <c>gst_bus_pop_filtered</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_pop_filtered")]
    private static partial nint GstBusPopFiltered(nint bus, uint types);

    /// <summary>The <c>gst_bus_post</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_post")]
    private static partial int GstBusPost(nint bus, nint message);

    /// <summary>The <c>gst_bus_remove_signal_watch</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_remove_signal_watch")]
    private static partial void GstBusRemoveSignalWatch(nint bus);

    /// <summary>The <c>gst_bus_remove_watch</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_remove_watch")]
    private static partial int GstBusRemoveWatch(nint bus);

    /// <summary>The <c>gst_bus_set_flushing</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_set_flushing")]
    private static partial void GstBusSetFlushing(nint bus, int flushing);

    /// <summary>The <c>gst_bus_sync_signal_handler</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_sync_signal_handler")]
    private static partial int GstBusSyncSignalHandler(nint bus, nint message, nint data);

    /// <summary>The <c>gst_bus_timed_pop</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_timed_pop")]
    private static partial nint GstBusTimedPop(nint bus, ulong timeout);

    /// <summary>The <c>gst_bus_timed_pop_filtered</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bus_timed_pop_filtered")]
    private static partial nint GstBusTimedPopFiltered(nint bus, ulong timeout, uint types);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstBus</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_bus_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Bus(handle, transfer);
}
