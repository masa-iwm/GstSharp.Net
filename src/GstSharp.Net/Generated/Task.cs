// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// #GstTask is used by #GstElement and #GstPad to provide the data passing
/// threads in a #GstPipeline.
/// </summary>
/// <remarks>
/// <para>
/// A #GstPad will typically start a #GstTask to push or pull data to/from the
/// peer pads. Most source elements start a #GstTask to push data. In some cases
/// a demuxer element can start a #GstTask to pull data from a peer element. This
/// is typically done when the demuxer can perform random access on the upstream
/// peer element for improved performance.
/// </para>
/// <para>
/// Although convenience functions exist on #GstPad to start/pause/stop tasks, it
/// might sometimes be needed to create a #GstTask manually if it is not related to
/// a #GstPad.
/// </para>
/// <para>
/// Before the #GstTask can be run, it needs a #GRecMutex that can be set with
/// gst_task_set_lock().
/// </para>
/// <para>
/// The task can be started, paused and stopped with gst_task_start(), gst_task_pause()
/// and gst_task_stop() respectively or with the gst_task_set_state() function.
/// </para>
/// <para>
/// A #GstTask will repeatedly call the #GstTaskFunction with the user data
/// that was provided when creating the task with gst_task_new(). While calling
/// the function it will acquire the provided lock. The provided lock is released
/// when the task pauses or stops.
/// </para>
/// <para>
/// Stopping a task with gst_task_stop() will not immediately make sure the task is
/// not running anymore. Use gst_task_join() to make sure the task is completely
/// stopped and the thread is stopped.
/// </para>
/// <para>
/// After creating a #GstTask, use gst_object_unref() to free its resources. This can
/// only be done when the task is not running anymore.
/// </para>
/// <para>
/// Task functions can send a #GstMessage to send out-of-band data to the
/// application. The application can receive messages from the #GstBus in its
/// mainloop.
/// </para>
/// <para>
/// For debugging purposes, the task will configure its object name as the thread
/// name on Linux. Please note that the object name should be configured before the
/// task is started; changing the object name after the task has been started, has
/// no effect on the thread name.
/// </para>
/// </remarks>
public unsafe partial class Task : Gst.Object
{
    /// <summary>Wraps a native <c>GstTask</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstTask</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Task(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Create a new Task that will repeatedly call the provided @func
    /// with @user_data as a parameter. Typically the task will run in
    /// a new thread.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The function cannot be changed after the task has been created. You
    /// must create a new #GstTask to change the function.
    /// </para>
    /// <para>
    /// This function will not yet create and start a thread. Use gst_task_start() or
    /// gst_task_pause() to create and start the GThread.
    /// </para>
    /// <para>
    /// Before the task can be used, a #GRecMutex must be configured using the
    /// gst_task_set_lock() function. This lock will always be acquired while
    /// @func is called.
    /// </para>
    /// </remarks>
    /// <param name="func">The #GstTaskFunction to use</param>
    /// <returns>A new #GstTask.</returns>
    public static Gst.Task New(Gst.TaskFunction func)
    {
        ArgumentNullException.ThrowIfNull(func);
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        nint nativeResult = GstTaskNew(Gst.TaskFunctionTrampoline.Pointer, funcState.UserData, (nint)Gst.Interop.CallbackHandle.DestroyNotify);
        return Gst.GObject.Object.FromNative<Gst.Task>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_task_new returned no value.");
    }

    /// <summary>
    /// Get the #GstTaskPool that this task will use for its streaming
    /// threads.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <returns>
    /// the #GstTaskPool used by @task. gst_object_unref()
    /// after usage.
    /// </returns>
    public Gst.TaskPool GetPool()
    {
        nint nativeResult = GstTaskGetPool(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.TaskPool>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_task_get_pool returned no value.");
    }

    /// <summary>Get the current state of the task.</summary>
    /// <returns>The #GstTaskState of the task</returns>
    public Gst.TaskState GetState()
    {
        int nativeResult = GstTaskGetState(Handle);
        System.GC.KeepAlive(this);
        return (Gst.TaskState)nativeResult;
    }

    /// <summary>
    /// Joins @task. After this call, it is safe to unref the task
    /// and clean up the lock set with gst_task_set_lock().
    /// </summary>
    /// <remarks>
    /// <para>The task will automatically be stopped with this call.</para>
    /// <para>
    /// This function cannot be called from within a task function as this
    /// would cause a deadlock. The function will detect this and print a
    /// g_warning.
    /// </para>
    /// </remarks>
    /// <returns>%TRUE if the task could be joined.</returns>
    public bool Join()
    {
        int nativeResult = GstTaskJoin(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Pauses @task. This method can also be called on a task in the
    /// stopped state, in which case a thread will be started and will remain
    /// in the paused state. This function does not wait for the task to complete
    /// the paused state.
    /// </summary>
    /// <returns>%TRUE if the task could be paused.</returns>
    public bool Pause()
    {
        int nativeResult = GstTaskPause(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Resume @task in case it was paused. If the task was stopped, it will
    /// remain in that state and this function will return %FALSE.
    /// </summary>
    /// <returns>%TRUE if the task could be resumed.</returns>
    public bool Resume()
    {
        int nativeResult = GstTaskResume(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Set @pool as the new GstTaskPool for @task. Any new streaming threads that
    /// will be created by @task will now use @pool.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="pool">The <c>pool</c> argument.</param>
    public void SetPool(Gst.TaskPool pool)
    {
        ArgumentNullException.ThrowIfNull(pool);
        GstTaskSetPool(Handle, pool.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(pool);
    }

    /// <summary>Sets the state of @task to @state.</summary>
    /// <remarks>
    /// <para>
    /// The @task must have a lock associated with it using
    /// gst_task_set_lock() when going to GST_TASK_STARTED or GST_TASK_PAUSED or
    /// this function will return %FALSE.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="state">The <c>state</c> argument.</param>
    /// <returns>%TRUE if the state could be changed.</returns>
    public bool SetState(Gst.TaskState state)
    {
        int nativeResult = GstTaskSetState(Handle, (int)state);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Starts @task. The @task must have a lock associated with it using
    /// gst_task_set_lock() or this function will return %FALSE.
    /// </summary>
    /// <returns>%TRUE if the task could be started.</returns>
    public bool Start()
    {
        int nativeResult = GstTaskStart(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Stops @task. This method merely schedules the task to stop and
    /// will not wait for the task to have completely stopped. Use
    /// gst_task_join() to stop and wait for completion.
    /// </summary>
    /// <returns>%TRUE if the task could be stopped.</returns>
    public bool Stop()
    {
        int nativeResult = GstTaskStop(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Wait for all tasks to be stopped. This is mainly used internally
    /// to ensure proper cleanup of internal data structures in test suites.
    /// </summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    public static void CleanupAll()
    {
        GstTaskCleanupAll();
    }

    /// <summary>The <c>gst_task_new</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_new")]
    private static partial nint GstTaskNew(nint func, nint userData, nint notify);

    /// <summary>The <c>gst_task_get_pool</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_get_pool")]
    private static partial nint GstTaskGetPool(nint task);

    /// <summary>The <c>gst_task_get_state</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_get_state")]
    private static partial int GstTaskGetState(nint task);

    /// <summary>The <c>gst_task_join</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_join")]
    private static partial int GstTaskJoin(nint task);

    /// <summary>The <c>gst_task_pause</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_pause")]
    private static partial int GstTaskPause(nint task);

    /// <summary>The <c>gst_task_resume</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_resume")]
    private static partial int GstTaskResume(nint task);

    /// <summary>The <c>gst_task_set_pool</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_set_pool")]
    private static partial void GstTaskSetPool(nint task, nint pool);

    /// <summary>The <c>gst_task_set_state</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_set_state")]
    private static partial int GstTaskSetState(nint task, int state);

    /// <summary>The <c>gst_task_start</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_start")]
    private static partial int GstTaskStart(nint task);

    /// <summary>The <c>gst_task_stop</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_stop")]
    private static partial int GstTaskStop(nint task);

    /// <summary>The <c>gst_task_cleanup_all</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_task_cleanup_all")]
    private static partial void GstTaskCleanupAll();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstTask</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_task_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Task(handle, transfer);
}
