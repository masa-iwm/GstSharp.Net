// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>This is a base class for anything that can be added to a #GstPlugin.</summary>
public abstract unsafe partial class PluginFeature : Gst.Object
{
    /// <summary>Wraps a native <c>GstPluginFeature</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstPluginFeature</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected PluginFeature(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Checks whether the given plugin feature is at least the required version.</summary>
    /// <remarks>
    /// <para>
    /// Note: Since version 1.24 this function no longer returns %TRUE if the
    /// version is a git development version (e.g. 1.23.0.1) and the check is
    /// for the "next" micro version, that is it will no longer return %TRUE for
    /// e.g. 1.23.0.1 if the check is for 1.23.1. It is still possible to parse
    /// the nano version from the string and do this check that way if needed.
    /// </para>
    /// </remarks>
    /// <param name="minMajor">minimum required major version</param>
    /// <param name="minMinor">minimum required minor version</param>
    /// <param name="minMicro">minimum required micro version</param>
    /// <returns>
    /// %TRUE if the plugin feature has at least
    ///  the required version, otherwise %FALSE.
    /// </returns>
    public bool CheckVersion(uint minMajor, uint minMinor, uint minMicro)
    {
        int nativeResult = GstPluginFeatureCheckVersion(Handle, minMajor, minMinor, minMicro);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get the plugin that provides this feature.</summary>
    /// <returns>
    /// the plugin that provides this
    ///     feature, or %NULL.  Unref with gst_object_unref() when no
    ///     longer needed.
    /// </returns>
    public Gst.Plugin? GetPlugin()
    {
        nint nativeResult = GstPluginFeatureGetPlugin(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Plugin>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the name of the plugin that provides this feature.</summary>
    /// <returns>
    /// the name of the plugin that provides this
    ///     feature, or %NULL if the feature is not associated with a
    ///     plugin.
    /// </returns>
    public string? GetPluginName()
    {
        nint nativeResult = GstPluginFeatureGetPluginName(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>Gets the rank of a plugin feature.</summary>
    /// <returns>The rank of the feature</returns>
    public uint GetRank()
    {
        uint nativeResult = GstPluginFeatureGetRank(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Loads the plugin containing @feature if it's not already loaded. @feature is
    /// unaffected; use the return value instead.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Normally this function is used like this:
    /// |[&lt;!-- language="C" --&gt;
    /// GstPluginFeature *loaded_feature;
    /// </para>
    /// <para>
    /// loaded_feature = gst_plugin_feature_load (feature);
    /// // presumably, we're no longer interested in the potentially-unloaded feature
    /// gst_object_unref (feature);
    /// feature = loaded_feature;
    /// ]|
    /// </para>
    /// </remarks>
    /// <returns>
    /// a reference to the loaded
    /// feature, or %NULL on error
    /// </returns>
    public Gst.PluginFeature? Load()
    {
        nint nativeResult = GstPluginFeatureLoad(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.PluginFeature>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Specifies a rank for a plugin feature, so that autoplugging uses
    /// the most appropriate feature.
    /// </summary>
    /// <param name="rank">rank value - higher number means more priority rank</param>
    public void SetRank(uint rank)
    {
        GstPluginFeatureSetRank(Handle, rank);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Copies the list of features. Caller should call @gst_plugin_feature_list_free
    /// when done with the list.
    /// </summary>
    /// <param name="list">
    /// list
    ///     of #GstPluginFeature
    /// The call reads the list while it runs and copies whatever it keeps. A
    /// temporary native list is built for the call and released when it returns,
    /// and an empty sequence is passed as the null pointer, which is how C spells
    /// the empty list.
    /// </param>
    /// <returns>
    /// a copy of @list,
    ///     with each feature's reference count incremented.
    /// </returns>
    public static System.Collections.Generic.IReadOnlyList<Gst.PluginFeature> ListCopy(System.Collections.Generic.IEnumerable<Gst.PluginFeature>? list)
    {
        using Gst.Interop.GListScope listScope = Gst.Interop.GMarshal.AllocList(list, singly: false);
        nint nativeResult = GstPluginFeatureListCopy(listScope.Head);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<Gst.PluginFeature> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.PluginFeature>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Debug the plugin feature names in @list.</summary>
    /// <param name="list">
    /// a #GList of
    ///     plugin features
    /// The call reads the list while it runs and copies whatever it keeps. A
    /// temporary native list is built for the call and released when it returns,
    /// and an empty sequence is passed as the null pointer, which is how C spells
    /// the empty list.
    /// </param>
    public static void ListDebug(System.Collections.Generic.IEnumerable<Gst.PluginFeature>? list)
    {
        using Gst.Interop.GListScope listScope = Gst.Interop.GMarshal.AllocList(list, singly: false);
        GstPluginFeatureListDebug(listScope.Head);
    }

    /// <summary>
    /// Compares the two given #GstPluginFeature instances. This function can be
    /// used as a #GCompareFunc when sorting by rank and then by name.
    /// </summary>
    /// <param name="p1">a #GstPluginFeature</param>
    /// <param name="p2">a #GstPluginFeature</param>
    /// <returns>
    /// negative value if the rank of p1 &gt; the rank of p2 or the ranks are
    /// equal but the name of p1 comes before the name of p2; zero if the rank
    /// and names are equal; positive value if the rank of p1 &lt; the rank of p2 or the
    /// ranks are equal but the name of p2 comes before the name of p1
    /// </returns>
    public static int RankCompareFunc(nint p1, nint p2)
    {
        int nativeResult = GstPluginFeatureRankCompareFunc(p1, p2);
        return nativeResult;
    }

    /// <summary>The <c>gst_plugin_feature_check_version</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_check_version")]
    private static partial int GstPluginFeatureCheckVersion(nint feature, uint minMajor, uint minMinor, uint minMicro);

    /// <summary>The <c>gst_plugin_feature_get_plugin</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_get_plugin")]
    private static partial nint GstPluginFeatureGetPlugin(nint feature);

    /// <summary>The <c>gst_plugin_feature_get_plugin_name</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_get_plugin_name")]
    private static partial nint GstPluginFeatureGetPluginName(nint feature);

    /// <summary>The <c>gst_plugin_feature_get_rank</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_get_rank")]
    private static partial uint GstPluginFeatureGetRank(nint feature);

    /// <summary>The <c>gst_plugin_feature_load</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_load")]
    private static partial nint GstPluginFeatureLoad(nint feature);

    /// <summary>The <c>gst_plugin_feature_set_rank</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_set_rank")]
    private static partial void GstPluginFeatureSetRank(nint feature, uint rank);

    /// <summary>The <c>gst_plugin_feature_list_copy</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_list_copy")]
    private static partial nint GstPluginFeatureListCopy(nint list);

    /// <summary>The <c>gst_plugin_feature_list_debug</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_list_debug")]
    private static partial void GstPluginFeatureListDebug(nint list);

    /// <summary>The <c>gst_plugin_feature_rank_compare_func</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_rank_compare_func")]
    private static partial int GstPluginFeatureRankCompareFunc(nint p1, nint p2);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstPluginFeature</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_feature_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstPluginFeature</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : PluginFeature
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
