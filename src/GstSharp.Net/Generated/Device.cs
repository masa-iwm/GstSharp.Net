// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// #GstDevice are objects representing a device, they contain
/// relevant metadata about the device, such as its class and the #GstCaps
/// representing the media types it can produce or handle.
/// </summary>
/// <remarks>
/// <para>
/// #GstDevice are created by #GstDeviceProvider objects which can be
/// aggregated by #GstDeviceMonitor objects.
/// </para>
/// </remarks>
public abstract unsafe partial class Device : Gst.Object
{
    /// <summary>Wraps a native <c>GstDevice</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal Device(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Creates the element with all of the required parameters set to use
    /// this device.
    /// </summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>
    /// a new #GstElement configured to use
    /// this device
    /// </returns>
    public Gst.Element? CreateElement(string? name)
    {
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstDeviceCreateElement(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Element>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Getter for the #GstCaps that this device supports.</summary>
    /// <returns>
    /// The #GstCaps supported by this device. Unref with
    /// gst_caps_unref() when done.
    /// </returns>
    public Gst.Caps? GetCaps()
    {
        nint nativeResult = GstDeviceGetCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets the "class" of a device. This is a "/" separated list of
    /// classes that represent this device. They are a subset of the
    /// classes of the #GstDeviceProvider that produced this device.
    /// </summary>
    /// <returns>The device class. Free with g_free() after use.</returns>
    public string GetDeviceClass()
    {
        nint nativeResult = GstDeviceGetDeviceClass(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult)
            ?? throw new InvalidOperationException("gst_device_get_device_class returned no value.");
    }

    /// <summary>Gets the user-friendly name of the device.</summary>
    /// <returns>The device name. Free with g_free() after use.</returns>
    public string GetDisplayName()
    {
        nint nativeResult = GstDeviceGetDisplayName(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult)
            ?? throw new InvalidOperationException("gst_device_get_display_name returned no value.");
    }

    /// <summary>Gets the extra properties of a device.</summary>
    /// <returns>
    /// The extra properties or %NULL when there are none.
    ///          Free with gst_structure_free() after use.
    /// </returns>
    public Gst.Structure? GetProperties()
    {
        nint nativeResult = GstDeviceGetProperties(Handle);
        System.GC.KeepAlive(this);
        return Gst.Structure.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Check if @device matches all of the given classes</summary>
    /// <param name="classes">The <c>classes</c> argument.</param>
    /// <returns>%TRUE if @device matches.</returns>
    public bool HasClasses(string classes)
    {
        ArgumentNullException.ThrowIfNull(classes);
        System.Span<byte> classesBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope classesScope = Gst.Interop.GMarshal.StackUtf8(classes, classesBuffer);
        int nativeResult = GstDeviceHasClasses(Handle, classesScope.Pointer);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Tries to reconfigure an existing element to use the device. If this
    /// function fails, then one must destroy the element and create a new one
    /// using gst_device_create_element().
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note: This should only be implemented for elements can change their
    /// device in the PLAYING state.
    /// </para>
    /// </remarks>
    /// <param name="element">The <c>element</c> argument.</param>
    /// <returns>
    /// %TRUE if the element could be reconfigured to use this device,
    /// %FALSE otherwise.
    /// </returns>
    public bool ReconfigureElement(Gst.Element element)
    {
        ArgumentNullException.ThrowIfNull(element);
        int nativeResult = GstDeviceReconfigureElement(Handle, element.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(element);
        return nativeResult != 0;
    }

    /// <summary>The <c>caps</c> property.</summary>
    public Gst.Caps? Caps => GetCaps();

    /// <summary>The <c>device-class</c> property.</summary>
    public string DeviceClass => GetDeviceClass();

    /// <summary>The <c>display-name</c> property.</summary>
    public string DisplayName => GetDisplayName();

    /// <summary>The <c>properties</c> property.</summary>
    public Gst.Structure? Properties => GetProperties();

    /// <summary>Raised for the <c>removed</c> signal of <c>GstDevice</c>.</summary>
    public event System.EventHandler Removed
    {
        add => Gst.SignalConnections.Add(this, "removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, void>)&RemovedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "removed", value);
    }

    /// <summary>The native handler of the <c>removed</c> signal of <c>GstDevice</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void RemovedTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler>(userData) is not { } handler)
            {
                return;
            }

            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_device_create_element</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_create_element")]
    private static partial nint GstDeviceCreateElement(nint device, byte* name);

    /// <summary>The <c>gst_device_get_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_get_caps")]
    private static partial nint GstDeviceGetCaps(nint device);

    /// <summary>The <c>gst_device_get_device_class</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_get_device_class")]
    private static partial nint GstDeviceGetDeviceClass(nint device);

    /// <summary>The <c>gst_device_get_display_name</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_get_display_name")]
    private static partial nint GstDeviceGetDisplayName(nint device);

    /// <summary>The <c>gst_device_get_properties</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_get_properties")]
    private static partial nint GstDeviceGetProperties(nint device);

    /// <summary>The <c>gst_device_has_classes</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_has_classes")]
    private static partial int GstDeviceHasClasses(nint device, byte* classes);

    /// <summary>The <c>gst_device_reconfigure_element</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_reconfigure_element")]
    private static partial int GstDeviceReconfigureElement(nint device, nint element);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstDevice</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_device_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstDevice</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : Device
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
