// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>One registry holds the metadata of a set of plugins.</summary>
/// <remarks>
/// <para>&lt;emphasis role="bold"&gt;Design:&lt;/emphasis&gt;</para>
/// <para>
/// The #GstRegistry object is a list of plugins and some functions for dealing
/// with them. Each #GstPlugin is matched 1-1 with a file on disk, and may or may
/// not be loaded at a given time.
/// </para>
/// <para>
/// The primary source, at all times, of plugin information is each plugin file
/// itself. Thus, if an application wants information about a particular plugin,
/// or wants to search for a feature that satisfies given criteria, the primary
/// means of doing so is to load every plugin and look at the resulting
/// information that is gathered in the default registry. Clearly, this is a time
/// consuming process, so we cache information in the registry file. The format
/// and location of the cache file is internal to gstreamer.
/// </para>
/// <para>
/// On startup, plugins are searched for in the plugin search path. The following
/// locations are checked in this order:
/// </para>
/// <para>
/// * location from --gst-plugin-path commandline option.
/// * the GST_PLUGIN_PATH environment variable.
/// * the GST_PLUGIN_SYSTEM_PATH environment variable.
/// * default locations (if GST_PLUGIN_SYSTEM_PATH is not set).
///   Those default locations are:
///   `$XDG_DATA_HOME/gstreamer-$GST_API_VERSION/plugins/`
///   and `$prefix/libs/gstreamer-$GST_API_VERSION/`.
///   [$XDG_DATA_HOME](http://standards.freedesktop.org/basedir-spec/basedir-spec-latest.html) defaults to
///   `$HOME/.local/share`.
/// </para>
/// <para>
/// The registry cache file is loaded from
/// `$XDG_CACHE_HOME/gstreamer-$GST_API_VERSION/registry-$ARCH.bin`
/// (where $XDG_CACHE_HOME defaults to `$HOME/.cache`) or the file listed in the `GST_REGISTRY`
/// env var. One reason to change the registry location is for testing.
/// </para>
/// <para>
/// For each plugin that is found in the plugin search path, there could be 3
/// possibilities for cached information:
/// </para>
/// <para>
///   * the cache may not contain information about a given file.
///   * the cache may have stale information.
///   * the cache may have current information.
/// </para>
/// <para>
/// In the first two cases, the plugin is loaded and the cache updated. In
/// addition to these cases, the cache may have entries for plugins that are not
/// relevant to the current process. These are marked as not available to the
/// current process. If the cache is updated for whatever reason, it is marked
/// dirty.
/// </para>
/// <para>
/// A dirty cache is written out at the end of initialization. Each entry is
/// checked to make sure the information is minimally valid. If not, the entry is
/// simply dropped.
/// </para>
/// <para>## Implementation notes:</para>
/// <para>
/// The "cache" and "registry" are different concepts and can represent
/// different sets of plugins. For various reasons, at init time, the cache is
/// stored in the default registry, and plugins not relevant to the current
/// process are marked with the %GST_PLUGIN_FLAG_CACHED bit. These plugins are
/// removed at the end of initialization.
/// </para>
/// </remarks>
public unsafe partial class Registry : Gst.Object
{
    /// <summary>Wraps a native <c>GstRegistry</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstRegistry</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Registry(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Add the feature to the registry. The feature-added signal will be emitted.</summary>
    /// <remarks>
    /// <para>
    /// @feature's reference count will be incremented, and any floating
    /// reference will be removed (see gst_object_ref_sink())
    /// </para>
    /// </remarks>
    /// <param name="feature">The <c>feature</c> argument.</param>
    /// <returns>%TRUE on success.</returns>
    public bool AddFeature(Gst.PluginFeature feature)
    {
        ArgumentNullException.ThrowIfNull(feature);
        int nativeResult = GstRegistryAddFeature(Handle, feature.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(feature);
        return nativeResult != 0;
    }

    /// <summary>Add the plugin to the registry. The plugin-added signal will be emitted.</summary>
    /// <remarks>
    /// <para>
    /// @plugin's reference count will be incremented, and any floating
    /// reference will be removed (see gst_object_ref_sink())
    /// </para>
    /// </remarks>
    /// <param name="plugin">The <c>plugin</c> argument.</param>
    /// <returns>%TRUE on success.</returns>
    public bool AddPlugin(Gst.Plugin plugin)
    {
        ArgumentNullException.ThrowIfNull(plugin);
        int nativeResult = GstRegistryAddPlugin(Handle, plugin.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(plugin);
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks whether a plugin feature by the given name exists in
    /// @registry and whether its version is at least the
    /// version required.
    /// </summary>
    /// <param name="featureName">The <c>featureName</c> argument.</param>
    /// <param name="minMajor">The <c>minMajor</c> argument.</param>
    /// <param name="minMinor">The <c>minMinor</c> argument.</param>
    /// <param name="minMicro">The <c>minMicro</c> argument.</param>
    /// <returns>
    /// %TRUE if the feature could be found and the version is
    /// the same as the required version or newer, and %FALSE otherwise.
    /// </returns>
    public bool CheckFeatureVersion(string featureName, uint minMajor, uint minMinor, uint minMicro)
    {
        ArgumentNullException.ThrowIfNull(featureName);
        System.Span<byte> featureNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope featureNameScope = Gst.Interop.GMarshal.StackUtf8(featureName, featureNameBuffer);
        int nativeResult = GstRegistryCheckFeatureVersion(Handle, featureNameScope.Pointer, minMajor, minMinor, minMicro);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Runs a filter against all features of the plugins in the registry
    /// and returns a GList with the results.
    /// If the first flag is set, only the first match is
    /// returned (as a list with a single object).
    /// </summary>
    /// <param name="filter">the filter to use</param>
    /// <param name="first">The <c>first</c> argument.</param>
    /// <returns>
    /// a #GList of
    ///     #GstPluginFeature. Use gst_plugin_feature_list_free() after usage.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.PluginFeature> FeatureFilter(Gst.PluginFeatureFilter filter, bool first)
    {
        ArgumentNullException.ThrowIfNull(filter);
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle filterState = Gst.Interop.CallbackHandle.Alloc(filter);
        try
        {
            nint nativeResult = GstRegistryFeatureFilter(instanceHandle, Gst.PluginFeatureFilterTrampoline.Pointer, first ? 1 : 0, filterState.UserData);
            System.GC.KeepAlive(this);
            nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
            System.Collections.Generic.List<Gst.PluginFeature> result = new(nativeItems.Length);
            foreach (nint nativeItem in nativeItems)
            {
                if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.PluginFeature>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
                {
                    result.Add(adopted);
                }
            }

            return result;
        }
        finally
        {
            filterState.Free();
        }
    }

    /// <summary>Find the pluginfeature with the given name and type in the registry.</summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <param name="type">The <c>type</c> argument.</param>
    /// <returns>
    /// the pluginfeature with the
    ///     given name and type or %NULL if the plugin was not
    ///     found. gst_object_unref() after usage.
    /// </returns>
    public Gst.PluginFeature? FindFeature(string name, Gst.GObject.GType type)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstRegistryFindFeature(Handle, nameScope.Pointer, type.Value);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.PluginFeature>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Find the plugin with the given name in the registry.
    /// The plugin will be reffed; caller is responsible for unreffing.
    /// </summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>
    /// the plugin with the given name
    ///     or %NULL if the plugin was not found. gst_object_unref() after
    ///     usage.
    /// </returns>
    public Gst.Plugin? FindPlugin(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstRegistryFindPlugin(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Plugin>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Retrieves a #GList of #GstPluginFeature of @type.</summary>
    /// <param name="type">The <c>type</c> argument.</param>
    /// <returns>
    /// a #GList of
    ///     #GstPluginFeature of @type. Use gst_plugin_feature_list_free() after use
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.PluginFeature> GetFeatureList(Gst.GObject.GType type)
    {
        nint nativeResult = GstRegistryGetFeatureList(Handle, type.Value);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<Gst.PluginFeature> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.PluginFeature>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Retrieves a #GList of features of the plugin with name @name.</summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>
    /// a #GList of
    ///     #GstPluginFeature. Use gst_plugin_feature_list_free() after usage.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.PluginFeature> GetFeatureListByPlugin(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstRegistryGetFeatureListByPlugin(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<Gst.PluginFeature> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.PluginFeature>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Returns the registry's feature list cookie. This changes
    /// every time a feature is added or removed from the registry.
    /// </summary>
    /// <returns>the feature list cookie.</returns>
    public uint GetFeatureListCookie()
    {
        uint nativeResult = GstRegistryGetFeatureListCookie(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Get a copy of all plugins registered in the given registry. The refcount
    /// of each element in the list in incremented.
    /// </summary>
    /// <returns>
    /// a #GList of #GstPlugin.
    ///     Use gst_plugin_list_free() after usage.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.Plugin> GetPluginList()
    {
        nint nativeResult = GstRegistryGetPluginList(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<Gst.Plugin> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.Plugin>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>
    /// Look up a plugin in the given registry with the given filename.
    /// If found, plugin is reffed.
    /// </summary>
    /// <param name="filename">The <c>filename</c> argument.</param>
    /// <returns>
    /// the #GstPlugin if found, or
    ///     %NULL if not.  gst_object_unref() after usage.
    /// </returns>
    public Gst.Plugin? Lookup(string filename)
    {
        ArgumentNullException.ThrowIfNull(filename);
        System.Span<byte> filenameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope filenameScope = Gst.Interop.GMarshal.StackUtf8(filename, filenameBuffer);
        nint nativeResult = GstRegistryLookup(Handle, filenameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Plugin>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Find a #GstPluginFeature with @name in @registry.</summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>
    /// a #GstPluginFeature with its refcount incremented,
    ///     use gst_object_unref() after usage.
    /// </returns>
    public Gst.PluginFeature? LookupFeature(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstRegistryLookupFeature(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.PluginFeature>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Runs a filter against all plugins in the registry and returns a #GList with
    /// the results. If the first flag is set, only the first match is
    /// returned (as a list with a single object).
    /// Every plugin is reffed; use gst_plugin_list_free() after use, which
    /// will unref again.
    /// </summary>
    /// <param name="filter">the filter to use</param>
    /// <param name="first">The <c>first</c> argument.</param>
    /// <returns>
    /// a #GList of #GstPlugin.
    ///     Use gst_plugin_list_free() after usage.
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.Plugin> PluginFilter(Gst.PluginFilter filter, bool first)
    {
        ArgumentNullException.ThrowIfNull(filter);
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle filterState = Gst.Interop.CallbackHandle.Alloc(filter);
        try
        {
            nint nativeResult = GstRegistryPluginFilter(instanceHandle, Gst.PluginFilterTrampoline.Pointer, first ? 1 : 0, filterState.UserData);
            System.GC.KeepAlive(this);
            nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
            System.Collections.Generic.List<Gst.Plugin> result = new(nativeItems.Length);
            foreach (nint nativeItem in nativeItems)
            {
                if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.Plugin>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
                {
                    result.Add(adopted);
                }
            }

            return result;
        }
        finally
        {
            filterState.Free();
        }
    }

    /// <summary>Remove the feature from the registry.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="feature">The <c>feature</c> argument.</param>
    public void RemoveFeature(Gst.PluginFeature feature)
    {
        ArgumentNullException.ThrowIfNull(feature);
        GstRegistryRemoveFeature(Handle, feature.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(feature);
    }

    /// <summary>Remove the plugin from the registry.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="plugin">The <c>plugin</c> argument.</param>
    public void RemovePlugin(Gst.Plugin plugin)
    {
        ArgumentNullException.ThrowIfNull(plugin);
        GstRegistryRemovePlugin(Handle, plugin.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(plugin);
    }

    /// <summary>
    /// Scan the given path for plugins to add to the registry. The syntax of the
    /// path is specific to the registry.
    /// </summary>
    /// <param name="path">The <c>path</c> argument.</param>
    /// <returns>%TRUE if registry changed</returns>
    public bool ScanPath(string path)
    {
        ArgumentNullException.ThrowIfNull(path);
        System.Span<byte> pathBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope pathScope = Gst.Interop.GMarshal.StackUtf8(path, pathBuffer);
        int nativeResult = GstRegistryScanPath(Handle, pathScope.Pointer);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// By default GStreamer will perform scanning and rebuilding of the
    /// registry file using a helper child process.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Applications might want to disable this behaviour with the
    /// gst_registry_fork_set_enabled() function, in which case new plugins
    /// are scanned (and loaded) into the application process.
    /// </para>
    /// </remarks>
    /// <returns>
    /// %TRUE if GStreamer will use the child helper process when
    /// rebuilding the registry.
    /// </returns>
    public static bool ForkIsEnabled()
    {
        int nativeResult = GstRegistryForkIsEnabled();
        return nativeResult != 0;
    }

    /// <summary>
    /// Applications might want to disable/enable spawning of a child helper process
    /// when rebuilding the registry. See gst_registry_fork_is_enabled() for more
    /// information.
    /// </summary>
    /// <param name="enabled">The <c>enabled</c> argument.</param>
    public static void ForkSetEnabled(bool enabled)
    {
        GstRegistryForkSetEnabled(enabled ? 1 : 0);
    }

    /// <summary>
    /// Retrieves the singleton plugin registry. The caller does not own a
    /// reference on the registry, as it is alive as long as GStreamer is
    /// initialized.
    /// </summary>
    /// <returns>the #GstRegistry.</returns>
    public static Gst.Registry Get()
    {
        nint nativeResult = GstRegistryGet();
        return Gst.GObject.Object.FromNative<Gst.Registry>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_registry_get returned no value.");
    }

    /// <summary>The arguments of the <c>feature-added</c> signal of <c>GstRegistry</c>.</summary>
    public sealed class FeatureAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="FeatureAddedSignalArgs"/> class.</summary>
        /// <param name="feature">the feature that has been added</param>
        internal FeatureAddedSignalArgs(Gst.PluginFeature feature)
        {
            Feature = feature;
        }

        /// <summary>the feature that has been added</summary>
        public Gst.PluginFeature Feature { get; }
    }

    /// <summary>
    /// Signals that a feature has been added to the registry (possibly
    /// replacing a previously-added one by the same name)
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Registry.FeatureAddedSignalArgs> FeatureAdded
    {
        add => Gst.SignalConnections.Add(this, "feature-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&FeatureAddedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "feature-added", value);
    }

    /// <summary>The native handler of the <c>feature-added</c> signal of <c>GstRegistry</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void FeatureAddedTrampoline(nint instance, nint feature, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Registry.FeatureAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.PluginFeature featureValue = Gst.GObject.Object.FromNative<Gst.PluginFeature>(feature, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The feature-added signal of GstRegistry passed no feature.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Registry.FeatureAddedSignalArgs(featureValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>plugin-added</c> signal of <c>GstRegistry</c>.</summary>
    public sealed class PluginAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="PluginAddedSignalArgs"/> class.</summary>
        /// <param name="plugin">the plugin that has been added</param>
        internal PluginAddedSignalArgs(Gst.Plugin plugin)
        {
            Plugin = plugin;
        }

        /// <summary>the plugin that has been added</summary>
        public Gst.Plugin Plugin { get; }
    }

    /// <summary>
    /// Signals that a plugin has been added to the registry (possibly
    /// replacing a previously-added one by the same name)
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Registry.PluginAddedSignalArgs> PluginAdded
    {
        add => Gst.SignalConnections.Add(this, "plugin-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&PluginAddedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "plugin-added", value);
    }

    /// <summary>The native handler of the <c>plugin-added</c> signal of <c>GstRegistry</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void PluginAddedTrampoline(nint instance, nint plugin, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Registry.PluginAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Plugin pluginValue = Gst.GObject.Object.FromNative<Gst.Plugin>(plugin, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The plugin-added signal of GstRegistry passed no plugin.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Registry.PluginAddedSignalArgs(pluginValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_registry_add_feature</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_add_feature")]
    private static partial int GstRegistryAddFeature(nint registry, nint feature);

    /// <summary>The <c>gst_registry_add_plugin</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_add_plugin")]
    private static partial int GstRegistryAddPlugin(nint registry, nint plugin);

    /// <summary>The <c>gst_registry_check_feature_version</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_check_feature_version")]
    private static partial int GstRegistryCheckFeatureVersion(nint registry, byte* featureName, uint minMajor, uint minMinor, uint minMicro);

    /// <summary>The <c>gst_registry_feature_filter</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_feature_filter")]
    private static partial nint GstRegistryFeatureFilter(nint registry, nint filter, int first, nint userData);

    /// <summary>The <c>gst_registry_find_feature</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_find_feature")]
    private static partial nint GstRegistryFindFeature(nint registry, byte* name, nuint type);

    /// <summary>The <c>gst_registry_find_plugin</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_find_plugin")]
    private static partial nint GstRegistryFindPlugin(nint registry, byte* name);

    /// <summary>The <c>gst_registry_get_feature_list</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_get_feature_list")]
    private static partial nint GstRegistryGetFeatureList(nint registry, nuint type);

    /// <summary>The <c>gst_registry_get_feature_list_by_plugin</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_get_feature_list_by_plugin")]
    private static partial nint GstRegistryGetFeatureListByPlugin(nint registry, byte* name);

    /// <summary>The <c>gst_registry_get_feature_list_cookie</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_get_feature_list_cookie")]
    private static partial uint GstRegistryGetFeatureListCookie(nint registry);

    /// <summary>The <c>gst_registry_get_plugin_list</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_get_plugin_list")]
    private static partial nint GstRegistryGetPluginList(nint registry);

    /// <summary>The <c>gst_registry_lookup</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_lookup")]
    private static partial nint GstRegistryLookup(nint registry, byte* filename);

    /// <summary>The <c>gst_registry_lookup_feature</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_lookup_feature")]
    private static partial nint GstRegistryLookupFeature(nint registry, byte* name);

    /// <summary>The <c>gst_registry_plugin_filter</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_plugin_filter")]
    private static partial nint GstRegistryPluginFilter(nint registry, nint filter, int first, nint userData);

    /// <summary>The <c>gst_registry_remove_feature</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_remove_feature")]
    private static partial void GstRegistryRemoveFeature(nint registry, nint feature);

    /// <summary>The <c>gst_registry_remove_plugin</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_remove_plugin")]
    private static partial void GstRegistryRemovePlugin(nint registry, nint plugin);

    /// <summary>The <c>gst_registry_scan_path</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_scan_path")]
    private static partial int GstRegistryScanPath(nint registry, byte* path);

    /// <summary>The <c>gst_registry_fork_is_enabled</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_fork_is_enabled")]
    private static partial int GstRegistryForkIsEnabled();

    /// <summary>The <c>gst_registry_fork_set_enabled</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_fork_set_enabled")]
    private static partial void GstRegistryForkSetEnabled(int enabled);

    /// <summary>The <c>gst_registry_get</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_registry_get")]
    private static partial nint GstRegistryGet();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstRegistry</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_registry_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Registry(handle, transfer);
}
