// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// This interface offers methods to query and manipulate parameter preset sets.
/// A preset is a bunch of property settings, together with meta data and a name.
/// The name of a preset serves as key for subsequent method calls to manipulate
/// single presets.
/// All instances of one type will share the list of presets. The list is created
/// on demand, if presets are not used, the list is not created.
/// </summary>
/// <remarks>
/// <para>
/// The interface comes with a default implementation that serves most plugins.
/// Wrapper plugins will override most methods to implement support for the
/// native preset format of those wrapped plugins.
/// One method that is useful to be overridden is gst_preset_get_property_names().
/// With that one can control which properties are saved and in which order.
/// When implementing support for read-only presets, one should set the vmethods
/// for gst_preset_save_preset() and gst_preset_delete_preset() to %NULL.
/// Applications can use gst_preset_is_editable() to check for that.
/// </para>
/// <para>
/// The default implementation supports presets located in a system directory,
/// application specific directory and in the users home directory. When getting
/// a list of presets individual presets are read and overlaid in 1) system,
/// 2) application and 3) user order. Whenever an earlier entry is newer, the
/// later entries will be updated. Since 1.8 you can also provide extra paths
/// where to find presets through the GST_PRESET_PATH environment variable.
/// Presets found in those paths will be considered as "app presets".
/// </para>
/// </remarks>
public interface IPreset
{
    /// <summary>Gets the native instance that implements the interface.</summary>
    nint Handle { get; }
}

/// <summary>The methods of <c>GstPreset</c>.</summary>
public static unsafe partial class PresetExtensions
{
    /// <summary>Delete the given preset.</summary>
    /// <param name="preset">a #GObject that implements #GstPreset</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>%TRUE for success, %FALSE if e.g. there is no preset with that @name</returns>
    public static bool DeletePreset(this Gst.IPreset preset, string name)
    {
        ArgumentNullException.ThrowIfNull(preset);
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        int nativeResult = GstPresetDeletePreset(preset.Handle, nameScope.Pointer);
        System.GC.KeepAlive(preset);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets the @value for an existing meta data @tag. Meta data @tag names can be
    /// something like e.g. "comment". Returned values need to be released when done.
    /// </summary>
    /// <param name="preset">a #GObject that implements #GstPreset</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <param name="tag">The <c>tag</c> argument.</param>
    /// <param name="value">The <c>value</c> argument.</param>
    /// <returns>
    /// %TRUE for success, %FALSE if e.g. there is no preset with that @name
    /// or no value for the given @tag
    /// </returns>
    public static bool GetMeta(this Gst.IPreset preset, string name, string tag, out string? value)
    {
        ArgumentNullException.ThrowIfNull(preset);
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        ArgumentNullException.ThrowIfNull(tag);
        System.Span<byte> tagBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope tagScope = Gst.Interop.GMarshal.StackUtf8(tag, tagBuffer);
        nint valueNative = default;
        int nativeResult = GstPresetGetMeta(preset.Handle, nameScope.Pointer, tagScope.Pointer, &valueNative);
        System.GC.KeepAlive(preset);
        value = Gst.Interop.GMarshal.PtrToStringUtf8AndFree(valueNative);
        return nativeResult != 0;
    }

    /// <summary>Check if one can add new presets, change existing ones and remove presets.</summary>
    /// <param name="preset">a #GObject that implements #GstPreset</param>
    /// <returns>%TRUE if presets are editable or %FALSE if they are static</returns>
    public static bool IsEditable(this Gst.IPreset preset)
    {
        ArgumentNullException.ThrowIfNull(preset);
        int nativeResult = GstPresetIsEditable(preset.Handle);
        System.GC.KeepAlive(preset);
        return nativeResult != 0;
    }

    /// <summary>Load the given preset.</summary>
    /// <param name="preset">a #GObject that implements #GstPreset</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>%TRUE for success, %FALSE if e.g. there is no preset with that @name</returns>
    public static bool LoadPreset(this Gst.IPreset preset, string name)
    {
        ArgumentNullException.ThrowIfNull(preset);
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        int nativeResult = GstPresetLoadPreset(preset.Handle, nameScope.Pointer);
        System.GC.KeepAlive(preset);
        return nativeResult != 0;
    }

    /// <summary>
    /// Renames a preset. If there is already a preset by the @new_name it will be
    /// overwritten.
    /// </summary>
    /// <param name="preset">a #GObject that implements #GstPreset</param>
    /// <param name="oldName">The <c>oldName</c> argument.</param>
    /// <param name="newName">The <c>newName</c> argument.</param>
    /// <returns>%TRUE for success, %FALSE if e.g. there is no preset with @old_name</returns>
    public static bool RenamePreset(this Gst.IPreset preset, string oldName, string newName)
    {
        ArgumentNullException.ThrowIfNull(preset);
        ArgumentNullException.ThrowIfNull(oldName);
        System.Span<byte> oldNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope oldNameScope = Gst.Interop.GMarshal.StackUtf8(oldName, oldNameBuffer);
        ArgumentNullException.ThrowIfNull(newName);
        System.Span<byte> newNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope newNameScope = Gst.Interop.GMarshal.StackUtf8(newName, newNameBuffer);
        int nativeResult = GstPresetRenamePreset(preset.Handle, oldNameScope.Pointer, newNameScope.Pointer);
        System.GC.KeepAlive(preset);
        return nativeResult != 0;
    }

    /// <summary>
    /// Save the current object settings as a preset under the given name. If there
    /// is already a preset by this @name it will be overwritten.
    /// </summary>
    /// <param name="preset">a #GObject that implements #GstPreset</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>%TRUE for success, %FALSE</returns>
    public static bool SavePreset(this Gst.IPreset preset, string name)
    {
        ArgumentNullException.ThrowIfNull(preset);
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        int nativeResult = GstPresetSavePreset(preset.Handle, nameScope.Pointer);
        System.GC.KeepAlive(preset);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets a new @value for an existing meta data item or adds a new item. Meta
    /// data @tag names can be something like e.g. "comment". Supplying %NULL for the
    /// @value will unset an existing value.
    /// </summary>
    /// <param name="preset">a #GObject that implements #GstPreset</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <param name="tag">The <c>tag</c> argument.</param>
    /// <param name="value">The <c>value</c> argument.</param>
    /// <returns>%TRUE for success, %FALSE if e.g. there is no preset with that @name</returns>
    public static bool SetMeta(this Gst.IPreset preset, string name, string tag, string? value)
    {
        ArgumentNullException.ThrowIfNull(preset);
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        ArgumentNullException.ThrowIfNull(tag);
        System.Span<byte> tagBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope tagScope = Gst.Interop.GMarshal.StackUtf8(tag, tagBuffer);
        System.Span<byte> valueBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope valueScope = Gst.Interop.GMarshal.StackUtf8(value, valueBuffer);
        int nativeResult = GstPresetSetMeta(preset.Handle, nameScope.Pointer, tagScope.Pointer, valueScope.Pointer);
        System.GC.KeepAlive(preset);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets the directory for application specific presets if set by the
    /// application.
    /// </summary>
    /// <returns>
    /// the directory or %NULL, don't free or modify
    /// the string
    /// </returns>
    public static string? GetAppDir()
    {
        nint nativeResult = GstPresetGetAppDir();
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>
    /// Sets an extra directory as an absolute path that should be considered when
    /// looking for presets. Any presets in the application dir will shadow the
    /// system presets.
    /// </summary>
    /// <param name="appDir">The <c>appDir</c> argument.</param>
    /// <returns>%TRUE for success, %FALSE if the dir already has been set</returns>
    public static bool SetAppDir(string appDir)
    {
        ArgumentNullException.ThrowIfNull(appDir);
        System.Span<byte> appDirBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope appDirScope = Gst.Interop.GMarshal.StackUtf8(appDir, appDirBuffer);
        int nativeResult = GstPresetSetAppDir(appDirScope.Pointer);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_preset_delete_preset</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_delete_preset")]
    private static partial int GstPresetDeletePreset(nint preset, byte* name);

    /// <summary>The <c>gst_preset_get_meta</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_get_meta")]
    private static partial int GstPresetGetMeta(nint preset, byte* name, byte* tag, nint* value);

    /// <summary>The <c>gst_preset_is_editable</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_is_editable")]
    private static partial int GstPresetIsEditable(nint preset);

    /// <summary>The <c>gst_preset_load_preset</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_load_preset")]
    private static partial int GstPresetLoadPreset(nint preset, byte* name);

    /// <summary>The <c>gst_preset_rename_preset</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_rename_preset")]
    private static partial int GstPresetRenamePreset(nint preset, byte* oldName, byte* newName);

    /// <summary>The <c>gst_preset_save_preset</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_save_preset")]
    private static partial int GstPresetSavePreset(nint preset, byte* name);

    /// <summary>The <c>gst_preset_set_meta</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_set_meta")]
    private static partial int GstPresetSetMeta(nint preset, byte* name, byte* tag, byte* value);

    /// <summary>The <c>gst_preset_get_app_dir</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_get_app_dir")]
    private static partial nint GstPresetGetAppDir();

    /// <summary>The <c>gst_preset_set_app_dir</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_preset_set_app_dir")]
    private static partial int GstPresetSetAppDir(byte* appDir);
}
