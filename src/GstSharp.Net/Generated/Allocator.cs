// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// Memory is usually created by allocators with a gst_allocator_alloc()
/// method call. When %NULL is used as the allocator, the default allocator will
/// be used.
/// </summary>
/// <remarks>
/// <para>
/// New allocators can be registered with gst_allocator_register().
/// Allocators are identified by name and can be retrieved with
/// gst_allocator_find(). gst_allocator_set_default() can be used to change the
/// default allocator.
/// </para>
/// <para>
/// New memory can be created with gst_memory_new_wrapped() that wraps the memory
/// allocated elsewhere.
/// </para>
/// </remarks>
public abstract unsafe partial class Allocator : Gst.Object
{
    /// <summary>Wraps a native <c>GstAllocator</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal Allocator(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Use @allocator to allocate a new memory block with memory that is at least
    /// @size big.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The optional @params can specify the prefix and padding for the memory. If
    /// %NULL is passed, no flags, no extra prefix/padding and a default alignment is
    /// used.
    /// </para>
    /// <para>
    /// The prefix/padding will be filled with 0 if flags contains
    /// #GST_MEMORY_FLAG_ZERO_PREFIXED and #GST_MEMORY_FLAG_ZERO_PADDED respectively.
    /// </para>
    /// <para>When @allocator is %NULL, the default allocator will be used.</para>
    /// <para>
    /// The alignment in @params is given as a bitmask so that @align + 1 equals
    /// the amount of bytes to align to. For example, to align to 8 bytes,
    /// use an alignment of 7.
    /// </para>
    /// </remarks>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="params">The <c>@params</c> argument.</param>
    /// <returns>a new #GstMemory.</returns>
    public Gst.Memory? Alloc(nuint size, Gst.AllocationParams? @params)
    {
        nint nativeResult = GstAllocatorAlloc(Handle, size, @params is null ? 0 : @params.Handle);
        System.GC.KeepAlive(this);
        return Gst.Memory.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Find a previously registered allocator with @name. When @name is %NULL, the
    /// default allocator will be returned.
    /// </summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>
    /// a #GstAllocator or %NULL when
    /// the allocator with @name was not registered.
    /// </returns>
    public static Gst.Allocator? Find(string? name)
    {
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstAllocatorFind(nameScope.Pointer);
        return Gst.GObject.Object.FromNative<Gst.Allocator>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_allocator_alloc</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_allocator_alloc")]
    private static partial nint GstAllocatorAlloc(nint allocator, nuint size, nint @params);

    /// <summary>The <c>gst_allocator_find</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_allocator_find")]
    private static partial nint GstAllocatorFind(byte* name);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAllocator</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_allocator_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstAllocator</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : Allocator
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
