// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// A #GstBufferPool is an object that can be used to pre-allocate and recycle
/// buffers of the same size and with the same properties.
/// </summary>
/// <remarks>
/// <para>A #GstBufferPool is created with gst_buffer_pool_new().</para>
/// <para>
/// Once a pool is created, it needs to be configured. A call to
/// gst_buffer_pool_get_config() returns the current configuration structure from
/// the pool. With gst_buffer_pool_config_set_params() and
/// gst_buffer_pool_config_set_allocator() the bufferpool parameters and
/// allocator can be configured. Other properties can be configured in the pool
/// depending on the pool implementation.
/// </para>
/// <para>
/// A bufferpool can have extra options that can be enabled with
/// gst_buffer_pool_config_add_option(). The available options can be retrieved
/// with gst_buffer_pool_get_options(). Some options allow for additional
/// configuration properties to be set.
/// </para>
/// <para>
/// After the configuration structure has been configured,
/// gst_buffer_pool_set_config() updates the configuration in the pool. This can
/// fail when the configuration structure is not accepted.
/// </para>
/// <para>
/// After the pool has been configured, it can be activated with
/// gst_buffer_pool_set_active(). This will preallocate the configured resources
/// in the pool.
/// </para>
/// <para>
/// When the pool is active, gst_buffer_pool_acquire_buffer() can be used to
/// retrieve a buffer from the pool.
/// </para>
/// <para>
/// Buffers allocated from a bufferpool will automatically be returned to the
/// pool with gst_buffer_pool_release_buffer() when their refcount drops to 0.
/// </para>
/// <para>
/// The bufferpool can be deactivated again with gst_buffer_pool_set_active().
/// All further gst_buffer_pool_acquire_buffer() calls will return an error. When
/// all buffers are returned to the pool they will be freed.
/// </para>
/// </remarks>
public unsafe partial class BufferPool : Gst.Object
{
    /// <summary>Wraps a native <c>GstBufferPool</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstBufferPool</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected BufferPool(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new #GstBufferPool instance.</summary>
    /// <returns>a new #GstBufferPool instance</returns>
    public static Gst.BufferPool New()
    {
        nint nativeResult = GstBufferPoolNew();
        return Gst.GObject.Object.FromNative<Gst.BufferPool>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_buffer_pool_new returned no value.");
    }

    /// <summary>
    /// Acquires a buffer from @pool. @buffer should point to a memory location that
    /// can hold a pointer to the new buffer. When the pool is empty, this function
    /// will by default block until a buffer is released into the pool again or when
    /// the pool is set to flushing or deactivated.
    /// </summary>
    /// <remarks>
    /// <para>@params can contain optional parameters to influence the allocation.</para>
    /// </remarks>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <param name="params">The <c>@params</c> argument.</param>
    /// <returns>
    /// a #GstFlowReturn such as %GST_FLOW_FLUSHING when the pool is
    /// inactive.
    /// </returns>
    public Gst.FlowReturn AcquireBuffer(out Gst.Buffer? buffer, Gst.BufferPoolAcquireParams @params)
    {
        nint bufferNative = default;
        Gst.BufferPoolAcquireParams @paramsNative = @params;
        int nativeResult = GstBufferPoolAcquireBuffer(Handle, &bufferNative, &@paramsNative);
        System.GC.KeepAlive(this);
        buffer = Gst.Buffer.FromNative(bufferNative, Gst.Interop.Transfer.Full);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Gets a copy of the current configuration of the pool. This configuration
    /// can be modified and used for the gst_buffer_pool_set_config() call.
    /// </summary>
    /// <returns>a copy of the current configuration of @pool.</returns>
    public Gst.Structure GetConfig()
    {
        nint nativeResult = GstBufferPoolGetConfig(Handle);
        System.GC.KeepAlive(this);
        return Gst.Structure.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_buffer_pool_get_config returned no value.");
    }

    /// <summary>Checks if the bufferpool supports @option.</summary>
    /// <param name="option">The <c>option</c> argument.</param>
    /// <returns>%TRUE if the buffer pool contains @option.</returns>
    public bool HasOption(string option)
    {
        ArgumentNullException.ThrowIfNull(option);
        System.Span<byte> optionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope optionScope = Gst.Interop.GMarshal.StackUtf8(option, optionBuffer);
        int nativeResult = GstBufferPoolHasOption(Handle, optionScope.Pointer);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks if @pool is active. A pool can be activated with the
    /// gst_buffer_pool_set_active() call.
    /// </summary>
    /// <returns>%TRUE when the pool is active.</returns>
    public bool IsActive()
    {
        int nativeResult = GstBufferPoolIsActive(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Controls the active state of @pool. When the pool is inactive, new calls to
    /// gst_buffer_pool_acquire_buffer() will return with %GST_FLOW_FLUSHING.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Activating the bufferpool will preallocate all resources in the pool based on
    /// the configuration of the pool.
    /// </para>
    /// <para>
    /// Deactivating will free the resources again when there are no outstanding
    /// buffers. When there are outstanding buffers, they will be freed as soon as
    /// they are all returned to the pool.
    /// </para>
    /// </remarks>
    /// <param name="active">The <c>active</c> argument.</param>
    /// <returns>
    /// %FALSE when the pool was not configured or when preallocation of the
    /// buffers failed.
    /// </returns>
    public bool SetActive(bool active)
    {
        int nativeResult = GstBufferPoolSetActive(Handle, active ? 1 : 0);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Enables or disables the flushing state of a @pool without freeing or
    /// allocating buffers.
    /// </summary>
    /// <param name="flushing">The <c>flushing</c> argument.</param>
    public void SetFlushing(bool flushing)
    {
        GstBufferPoolSetFlushing(Handle, flushing ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Enables the option in @config. This will instruct the @bufferpool to enable
    /// the specified option on the buffers that it allocates.
    /// </summary>
    /// <remarks>
    /// <para>The options supported by @pool can be retrieved with gst_buffer_pool_get_options().</para>
    /// </remarks>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="option">The <c>option</c> argument.</param>
    public static void ConfigAddOption(Gst.Structure config, string option)
    {
        ArgumentNullException.ThrowIfNull(config);
        ArgumentNullException.ThrowIfNull(option);
        System.Span<byte> optionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope optionScope = Gst.Interop.GMarshal.StackUtf8(option, optionBuffer);
        GstBufferPoolConfigAddOption(config.Handle, optionScope.Pointer);
        System.GC.KeepAlive(config);
    }

    /// <summary>Gets the @allocator and @params from @config.</summary>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="allocator">The <c>allocator</c> argument.</param>
    /// <param name="params">
    /// The <c>@params</c> argument.
    /// The binding allocates the storage; on success the caller owns
    /// <paramref name="params"/> and disposes it. On failure it is
    /// <see langword="null"/>.
    /// </param>
    /// <returns>%TRUE, if the values are set.</returns>
    public static bool ConfigGetAllocator(Gst.Structure config, out Gst.Allocator? allocator, out Gst.AllocationParams? @params)
    {
        ArgumentNullException.ThrowIfNull(config);
        nint configNative = config.Handle;
        nint allocatorNative = default;
        nint @paramsNative = GstAllocationParamsNew();
        int nativeResult = GstBufferPoolConfigGetAllocator(configNative, &allocatorNative, @paramsNative);
        System.GC.KeepAlive(config);
        if (nativeResult != 0)
        {
            @params = Gst.AllocationParams.FromNative(@paramsNative, Gst.Interop.Transfer.Full);
        }
        else
        {
            // The call filled nothing, so the storage goes back through
            // the boxed free the wrapper disposes through.
            Gst.AllocationParams.FromNative(@paramsNative, Gst.Interop.Transfer.Full)?.Dispose();
            @params = null;
        }
        allocator = Gst.GObject.Object.FromNative<Gst.Allocator>(allocatorNative, Gst.Interop.Transfer.None);
        return nativeResult != 0;
    }

    /// <summary>
    /// Parses an available @config and gets the option at @index of the options API
    /// array.
    /// </summary>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="index">The <c>index</c> argument.</param>
    /// <returns>the option at @index.</returns>
    public static string? ConfigGetOption(Gst.Structure config, uint index)
    {
        ArgumentNullException.ThrowIfNull(config);
        nint nativeResult = GstBufferPoolConfigGetOption(config.Handle, index);
        System.GC.KeepAlive(config);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>Gets the configuration values from @config.</summary>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="minBuffers">The <c>minBuffers</c> argument.</param>
    /// <param name="maxBuffers">The <c>maxBuffers</c> argument.</param>
    /// <returns>%TRUE if all parameters could be fetched.</returns>
    public static bool ConfigGetParams(Gst.Structure config, out Gst.Caps? caps, out uint size, out uint minBuffers, out uint maxBuffers)
    {
        ArgumentNullException.ThrowIfNull(config);
        nint capsNative = default;
        uint sizeNative = default;
        uint minBuffersNative = default;
        uint maxBuffersNative = default;
        int nativeResult = GstBufferPoolConfigGetParams(config.Handle, &capsNative, &sizeNative, &minBuffersNative, &maxBuffersNative);
        System.GC.KeepAlive(config);
        caps = Gst.Caps.FromNative(capsNative, Gst.Interop.Transfer.None);
        size = sizeNative;
        minBuffers = minBuffersNative;
        maxBuffers = maxBuffersNative;
        return nativeResult != 0;
    }

    /// <summary>Checks if @config contains @option.</summary>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="option">The <c>option</c> argument.</param>
    /// <returns>%TRUE if the options array contains @option.</returns>
    public static bool ConfigHasOption(Gst.Structure config, string option)
    {
        ArgumentNullException.ThrowIfNull(config);
        ArgumentNullException.ThrowIfNull(option);
        System.Span<byte> optionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope optionScope = Gst.Interop.GMarshal.StackUtf8(option, optionBuffer);
        int nativeResult = GstBufferPoolConfigHasOption(config.Handle, optionScope.Pointer);
        System.GC.KeepAlive(config);
        return nativeResult != 0;
    }

    /// <summary>
    /// Retrieves the number of values currently stored in the options array of the
    /// @config structure.
    /// </summary>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <returns>the options array size as a #guint.</returns>
    public static uint ConfigNOptions(Gst.Structure config)
    {
        ArgumentNullException.ThrowIfNull(config);
        uint nativeResult = GstBufferPoolConfigNOptions(config.Handle);
        System.GC.KeepAlive(config);
        return nativeResult;
    }

    /// <summary>Sets the @allocator and @params on @config.</summary>
    /// <remarks>
    /// <para>
    /// One of @allocator and @params can be %NULL, but not both. When @allocator
    /// is %NULL, the default allocator of the pool will use the values in @param
    /// to perform its allocation. When @param is %NULL, the pool will use the
    /// provided @allocator with its default #GstAllocationParams.
    /// </para>
    /// <para>
    /// A call to gst_buffer_pool_set_config() can update the allocator and params
    /// with the values that it is able to do. Some pools are, for example, not able
    /// to operate with different allocators or cannot allocate with the values
    /// specified in @params. Use gst_buffer_pool_get_config() to get the currently
    /// used values.
    /// </para>
    /// </remarks>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="allocator">The <c>allocator</c> argument.</param>
    /// <param name="params">The <c>@params</c> argument.</param>
    public static void ConfigSetAllocator(Gst.Structure config, Gst.Allocator? allocator, Gst.AllocationParams? @params)
    {
        ArgumentNullException.ThrowIfNull(config);
        GstBufferPoolConfigSetAllocator(config.Handle, allocator is null ? 0 : allocator.Handle, @params is null ? 0 : @params.Handle);
        System.GC.KeepAlive(config);
        System.GC.KeepAlive(allocator);
        System.GC.KeepAlive(@params);
    }

    /// <summary>Configures @config with the given parameters.</summary>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="minBuffers">The <c>minBuffers</c> argument.</param>
    /// <param name="maxBuffers">The <c>maxBuffers</c> argument.</param>
    public static void ConfigSetParams(Gst.Structure config, Gst.Caps? caps, uint size, uint minBuffers, uint maxBuffers)
    {
        ArgumentNullException.ThrowIfNull(config);
        GstBufferPoolConfigSetParams(config.Handle, caps is null ? 0 : caps.Handle, size, minBuffers, maxBuffers);
        System.GC.KeepAlive(config);
        System.GC.KeepAlive(caps);
    }

    /// <summary>
    /// Validates that changes made to @config are still valid in the context of the
    /// expected parameters. This function is a helper that can be used to validate
    /// changes made by a pool to a config when gst_buffer_pool_set_config()
    /// returns %FALSE. This expects that @caps haven't changed and that
    /// @min_buffers aren't lower then what we initially expected.
    /// This does not check if options or allocator parameters are still valid,
    /// won't check if size have changed, since changing the size is valid to adapt
    /// padding.
    /// </summary>
    /// <param name="config">The <c>config</c> argument.</param>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="minBuffers">The <c>minBuffers</c> argument.</param>
    /// <param name="maxBuffers">The <c>maxBuffers</c> argument.</param>
    /// <returns>%TRUE, if the parameters are valid in this context.</returns>
    public static bool ConfigValidateParams(Gst.Structure config, Gst.Caps? caps, uint size, uint minBuffers, uint maxBuffers)
    {
        ArgumentNullException.ThrowIfNull(config);
        int nativeResult = GstBufferPoolConfigValidateParams(config.Handle, caps is null ? 0 : caps.Handle, size, minBuffers, maxBuffers);
        System.GC.KeepAlive(config);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>The <c>gst_buffer_pool_new</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_new")]
    private static partial nint GstBufferPoolNew();

    /// <summary>The <c>gst_buffer_pool_acquire_buffer</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_acquire_buffer")]
    private static partial int GstBufferPoolAcquireBuffer(nint pool, nint* buffer, Gst.BufferPoolAcquireParams* @params);

    /// <summary>The <c>gst_buffer_pool_get_config</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_get_config")]
    private static partial nint GstBufferPoolGetConfig(nint pool);

    /// <summary>The <c>gst_buffer_pool_has_option</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_has_option")]
    private static partial int GstBufferPoolHasOption(nint pool, byte* option);

    /// <summary>The <c>gst_buffer_pool_is_active</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_is_active")]
    private static partial int GstBufferPoolIsActive(nint pool);

    /// <summary>The <c>gst_buffer_pool_set_active</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_set_active")]
    private static partial int GstBufferPoolSetActive(nint pool, int active);

    /// <summary>The <c>gst_buffer_pool_set_flushing</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_set_flushing")]
    private static partial void GstBufferPoolSetFlushing(nint pool, int flushing);

    /// <summary>The <c>gst_buffer_pool_config_add_option</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_add_option")]
    private static partial void GstBufferPoolConfigAddOption(nint config, byte* option);

    /// <summary>The <c>gst_buffer_pool_config_get_allocator</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_get_allocator")]
    private static partial int GstBufferPoolConfigGetAllocator(nint config, nint* allocator, nint @params);

    /// <summary>The <c>gst_buffer_pool_config_get_option</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_get_option")]
    private static partial nint GstBufferPoolConfigGetOption(nint config, uint index);

    /// <summary>The <c>gst_buffer_pool_config_get_params</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_get_params")]
    private static partial int GstBufferPoolConfigGetParams(nint config, nint* caps, uint* size, uint* minBuffers, uint* maxBuffers);

    /// <summary>The <c>gst_buffer_pool_config_has_option</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_has_option")]
    private static partial int GstBufferPoolConfigHasOption(nint config, byte* option);

    /// <summary>The <c>gst_buffer_pool_config_n_options</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_n_options")]
    private static partial uint GstBufferPoolConfigNOptions(nint config);

    /// <summary>The <c>gst_buffer_pool_config_set_allocator</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_set_allocator")]
    private static partial void GstBufferPoolConfigSetAllocator(nint config, nint allocator, nint @params);

    /// <summary>The <c>gst_buffer_pool_config_set_params</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_set_params")]
    private static partial void GstBufferPoolConfigSetParams(nint config, nint caps, uint size, uint minBuffers, uint maxBuffers);

    /// <summary>The <c>gst_buffer_pool_config_validate_params</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_config_validate_params")]
    private static partial int GstBufferPoolConfigValidateParams(nint config, nint caps, uint size, uint minBuffers, uint maxBuffers);

    /// <summary>The <c>gst_allocation_params_new</c> entry point, which allocates the storage of a caller allocated out parameter.</summary>
    /// <returns>A new, zeroed instance the caller owns.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_allocation_params_new")]
    private static partial nint GstAllocationParamsNew();

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstBufferPool</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_buffer_pool_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new BufferPool(handle, transfer);
}
