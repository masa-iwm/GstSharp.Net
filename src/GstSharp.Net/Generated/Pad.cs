// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// A #GstElement is linked to other elements via "pads", which are extremely
/// light-weight generic link points.
/// </summary>
/// <remarks>
/// <para>
/// Pads have a #GstPadDirection, source pads produce data, sink pads consume
/// data.
/// </para>
/// <para>
/// Pads are typically created from a #GstPadTemplate with
/// gst_pad_new_from_template() and are then added to a #GstElement. This usually
/// happens when the element is created but it can also happen dynamically based
/// on the data that the element is processing or based on the pads that the
/// application requests.
/// </para>
/// <para>
/// Pads without pad templates can be created with gst_pad_new(),
/// which takes a direction and a name as an argument.  If the name is %NULL,
/// then a guaranteed unique name will be assigned to it.
/// </para>
/// <para>
/// A #GstElement creating a pad will typically use the various
/// gst_pad_set_*_function\() calls to register callbacks for events, queries or
/// dataflow on the pads.
/// </para>
/// <para>gst_pad_get_parent() will retrieve the #GstElement that owns the pad.</para>
/// <para>
/// After two pads are retrieved from an element by gst_element_get_static_pad(),
/// the pads can be linked with gst_pad_link(). (For quick links,
/// you can also use gst_element_link(), which will make the obvious
/// link for you if it's straightforward.). Pads can be unlinked again with
/// gst_pad_unlink(). gst_pad_get_peer() can be used to check what the pad is
/// linked to.
/// </para>
/// <para>
/// Before dataflow is possible on the pads, they need to be activated with
/// gst_pad_set_active().
/// </para>
/// <para>
/// gst_pad_query() and gst_pad_peer_query() can be used to query various
/// properties of the pad and the stream.
/// </para>
/// <para>
/// To send a #GstEvent on a pad, use gst_pad_send_event() and
/// gst_pad_push_event(). Some events will be sticky on the pad, meaning that
/// after they pass on the pad they can be queried later with
/// gst_pad_get_sticky_event() and gst_pad_sticky_events_foreach().
/// gst_pad_get_current_caps() and gst_pad_has_current_caps() are convenience
/// functions to query the current sticky CAPS event on a pad.
/// </para>
/// <para>
/// GstElements will use gst_pad_push() and gst_pad_pull_range() to push out
/// or pull in a buffer.
/// </para>
/// <para>
/// The dataflow, events and queries that happen on a pad can be monitored with
/// probes that can be installed with gst_pad_add_probe(). gst_pad_is_blocked()
/// can be used to check if a block probe is installed on the pad.
/// gst_pad_is_blocking() checks if the blocking probe is currently blocking the
/// pad. gst_pad_remove_probe() is used to remove a previously installed probe
/// and unblock blocking probes if any.
/// </para>
/// <para>
/// Pad have an offset that can be retrieved with gst_pad_get_offset(). This
/// offset will be applied to the running_time of all data passing over the pad.
/// gst_pad_set_offset() can be used to change the offset.
/// </para>
/// <para>
/// Convenience functions exist to start, pause and stop the task on a pad with
/// gst_pad_start_task(), gst_pad_pause_task() and gst_pad_stop_task()
/// respectively.
/// </para>
/// </remarks>
public unsafe partial class Pad : Gst.Object
{
    /// <summary>Wraps a native <c>GstPad</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstPad</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Pad(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Creates a new pad with the given name in the given direction.
    /// If name is %NULL, a guaranteed unique name (across all pads)
    /// will be assigned.
    /// This function makes a copy of the name so you can safely free the name.
    /// </summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <param name="direction">The <c>direction</c> argument.</param>
    /// <returns>a new #GstPad.</returns>
    public static Gst.Pad New(string? name, Gst.PadDirection direction)
    {
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstPadNew(nameScope.Pointer, (int)direction);
        return Gst.GObject.Object.FromNative<Gst.Pad>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_pad_new returned no value.");
    }

    /// <summary>
    /// Creates a new pad with the given name from the given static template.
    /// If name is %NULL, a guaranteed unique name (across all pads)
    /// will be assigned.
    /// This function makes a copy of the name so you can safely free the name.
    /// </summary>
    /// <param name="templ">The <c>templ</c> argument.</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>a new #GstPad.</returns>
    public static Gst.Pad NewFromStaticTemplate(Gst.StaticPadTemplate templ, string name)
    {
        ArgumentNullException.ThrowIfNull(templ);
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstPadNewFromStaticTemplate(templ.Handle, nameScope.Pointer);
        System.GC.KeepAlive(templ);
        return Gst.GObject.Object.FromNative<Gst.Pad>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_pad_new_from_static_template returned no value.");
    }

    /// <summary>
    /// Creates a new pad with the given name from the given template.
    /// If name is %NULL, a guaranteed unique name (across all pads)
    /// will be assigned.
    /// This function makes a copy of the name so you can safely free the name.
    /// </summary>
    /// <param name="templ">The <c>templ</c> argument.</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>a new #GstPad.</returns>
    public static Gst.Pad NewFromTemplate(Gst.PadTemplate templ, string? name)
    {
        ArgumentNullException.ThrowIfNull(templ);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstPadNewFromTemplate(templ.Handle, nameScope.Pointer);
        System.GC.KeepAlive(templ);
        return Gst.GObject.Object.FromNative<Gst.Pad>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_pad_new_from_template returned no value.");
    }

    /// <summary>
    /// Activates or deactivates the given pad in @mode via dispatching to the
    /// pad's activatemodefunc. For use from within pad activation functions only.
    /// </summary>
    /// <remarks>
    /// <para>If you don't know what this is, you probably don't want to call it.</para>
    /// </remarks>
    /// <param name="mode">The <c>mode</c> argument.</param>
    /// <param name="active">The <c>active</c> argument.</param>
    /// <returns>%TRUE if the operation was successful.</returns>
    public bool ActivateMode(Gst.PadMode mode, bool active)
    {
        int nativeResult = GstPadActivateMode(Handle, (int)mode, active ? 1 : 0);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Be notified of different states of pads. The provided callback is called for
    /// every state that matches @mask.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Probes are called in groups: First GST_PAD_PROBE_TYPE_BLOCK probes are
    /// called, then others, then finally GST_PAD_PROBE_TYPE_IDLE. The only
    /// exception here are GST_PAD_PROBE_TYPE_IDLE probes that are called
    /// immediately if the pad is already idle while calling gst_pad_add_probe().
    /// In each of the groups, probes are called in the order in which they were
    /// added.
    /// </para>
    /// </remarks>
    /// <param name="mask">The <c>mask</c> argument.</param>
    /// <param name="callback">
    /// #GstPadProbeCallback that will be called with
    ///           notifications of the pad state
    /// </param>
    /// <returns>
    /// an id or 0 if no probe is pending. The id can be used to remove the
    /// probe with gst_pad_remove_probe(). When using GST_PAD_PROBE_TYPE_IDLE it can
    /// happen that the probe can be run immediately and if the probe returns
    /// GST_PAD_PROBE_REMOVE this functions returns 0.
    /// </returns>
    public System.Runtime.InteropServices.CULong AddProbe(Gst.PadProbeType mask, Gst.PadProbeCallback callback)
    {
        ArgumentNullException.ThrowIfNull(callback);
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle callbackState = Gst.Interop.CallbackHandle.Alloc(callback);
        System.Runtime.InteropServices.CULong nativeResult = GstPadAddProbe(instanceHandle, (int)mask, Gst.PadProbeCallbackTrampoline.Pointer, callbackState.UserData, (nint)Gst.Interop.CallbackHandle.DestroyNotify);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Checks if the source pad and the sink pad are compatible so they can be
    /// linked.
    /// </summary>
    /// <param name="sinkpad">The <c>sinkpad</c> argument.</param>
    /// <returns>%TRUE if the pads can be linked.</returns>
    public bool CanLink(Gst.Pad sinkpad)
    {
        ArgumentNullException.ThrowIfNull(sinkpad);
        int nativeResult = GstPadCanLink(Handle, sinkpad.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sinkpad);
        return nativeResult != 0;
    }

    /// <summary>Chain a buffer to @pad.</summary>
    /// <remarks>
    /// <para>The function returns #GST_FLOW_FLUSHING if the pad was flushing.</para>
    /// <para>
    /// If the buffer type is not acceptable for @pad (as negotiated with a
    /// preceding GST_EVENT_CAPS event), this function returns
    /// #GST_FLOW_NOT_NEGOTIATED.
    /// </para>
    /// <para>
    /// The function proceeds calling the chain function installed on @pad (see
    /// gst_pad_set_chain_function()) and the return value of that function is
    /// returned to the caller. #GST_FLOW_NOT_SUPPORTED is returned if @pad has no
    /// chain function.
    /// </para>
    /// <para>
    /// In all cases, success or failure, the caller loses its reference to @buffer
    /// after calling this function.
    /// </para>
    /// <para>
    /// The <c>buffer</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="buffer">
    /// The <c>buffer</c> argument.
    /// The call consumes it: <paramref name="buffer"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn from the pad.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="buffer"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="buffer"/> was disposed.
    /// </exception>
    public Gst.FlowReturn Chain(Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        nint instanceHandle = Handle;
        nint bufferNative = buffer.Handle;
        nint bufferOwned = Gst.GstNative.MiniObjectRef(bufferNative);
        int nativeResult = GstPadChain(instanceHandle, bufferOwned);
        System.GC.KeepAlive(this);
        buffer.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>Chain a bufferlist to @pad.</summary>
    /// <remarks>
    /// <para>The function returns #GST_FLOW_FLUSHING if the pad was flushing.</para>
    /// <para>
    /// If @pad was not negotiated properly with a CAPS event, this function
    /// returns #GST_FLOW_NOT_NEGOTIATED.
    /// </para>
    /// <para>
    /// The function proceeds calling the chainlist function installed on @pad (see
    /// gst_pad_set_chain_list_function()) and the return value of that function is
    /// returned to the caller. #GST_FLOW_NOT_SUPPORTED is returned if @pad has no
    /// chainlist function.
    /// </para>
    /// <para>
    /// In all cases, success or failure, the caller loses its reference to @list
    /// after calling this function.
    /// </para>
    /// <para>MT safe.</para>
    /// <para>
    /// The <c>list</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="list">
    /// The <c>list</c> argument.
    /// The call consumes it: <paramref name="list"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn from the pad.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="list"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="list"/> was disposed.
    /// </exception>
    public Gst.FlowReturn ChainList(Gst.BufferList list)
    {
        ArgumentNullException.ThrowIfNull(list);
        nint instanceHandle = Handle;
        nint listNative = list.Handle;
        nint listOwned = Gst.GstNative.MiniObjectRef(listNative);
        int nativeResult = GstPadChainList(instanceHandle, listOwned);
        System.GC.KeepAlive(this);
        list.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Check and clear the #GST_PAD_FLAG_NEED_RECONFIGURE flag on @pad and return %TRUE
    /// if the flag was set.
    /// </summary>
    /// <returns>%TRUE is the GST_PAD_FLAG_NEED_RECONFIGURE flag was set on @pad.</returns>
    public bool CheckReconfigure()
    {
        int nativeResult = GstPadCheckReconfigure(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Creates a stream-id for the source #GstPad @pad by combining the
    /// upstream information with the optional @stream_id of the stream
    /// of @pad. @pad must have a parent #GstElement and which must have zero
    /// or one sinkpad. @stream_id can only be %NULL if the parent element
    /// of @pad has only a single source pad.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function generates an unique stream-id by getting the upstream
    /// stream-start event stream ID and appending @stream_id to it. If the
    /// element has no sinkpad it will generate an upstream stream-id by
    /// doing an URI query on the element and in the worst case just uses
    /// a random number. Source elements that don't implement the URI
    /// handler interface should ideally generate a unique, deterministic
    /// stream-id manually instead.
    /// </para>
    /// <para>
    /// Since stream IDs are sorted alphabetically, any numbers in the
    /// stream ID should be printed with a fixed number of characters,
    /// preceded by 0's, such as by using the format \%03u instead of \%u.
    /// </para>
    /// </remarks>
    /// <param name="parent">The <c>parent</c> argument.</param>
    /// <param name="streamId">The <c>streamId</c> argument.</param>
    /// <returns>A stream-id for @pad. g_free() after usage.</returns>
    public string CreateStreamId(Gst.Element parent, string? streamId)
    {
        ArgumentNullException.ThrowIfNull(parent);
        System.Span<byte> streamIdBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope streamIdScope = Gst.Interop.GMarshal.StackUtf8(streamId, streamIdBuffer);
        nint nativeResult = GstPadCreateStreamId(Handle, parent.Handle, streamIdScope.Pointer);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(parent);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult)
            ?? throw new InvalidOperationException("gst_pad_create_stream_id returned no value.");
    }

    /// <summary>Invokes the default event handler for the given pad.</summary>
    /// <remarks>
    /// <para>
    /// The EOS event will pause the task associated with @pad before it is forwarded
    /// to all internally linked pads,
    /// </para>
    /// <para>
    /// The event is sent to all pads internally linked to @pad. This function
    /// takes ownership of @event.
    /// </para>
    /// <para>
    /// The <c>event</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="parent">The <c>parent</c> argument.</param>
    /// <param name="event">
    /// The <c>@event</c> argument.
    /// The call consumes it: <paramref name="event"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>%TRUE if the event was sent successfully.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="event"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="event"/> was disposed.
    /// </exception>
    public bool EventDefault(Gst.Object? parent, Gst.Event @event)
    {
        ArgumentNullException.ThrowIfNull(@event);
        nint instanceHandle = Handle;
        nint parentNative = parent is null ? 0 : parent.Handle;
        nint @eventNative = @event.Handle;
        nint @eventOwned = Gst.GstNative.MiniObjectRef(@eventNative);
        int nativeResult = GstPadEventDefault(instanceHandle, parentNative, @eventOwned);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(parent);
        @event.Dispose();
        return nativeResult != 0;
    }

    /// <summary>
    /// Calls @forward for all internally linked pads of @pad. This function deals with
    /// dynamically changing internal pads and will make sure that the @forward
    /// function is only called once for each pad.
    /// </summary>
    /// <remarks>
    /// <para>When @forward returns %TRUE, no further pads will be processed.</para>
    /// </remarks>
    /// <param name="forward">a #GstPadForwardFunction</param>
    /// <returns>%TRUE if one of the dispatcher functions returned %TRUE.</returns>
    public bool Forward(Gst.PadForwardFunction forward)
    {
        ArgumentNullException.ThrowIfNull(forward);
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle forwardState = Gst.Interop.CallbackHandle.Alloc(forward);
        try
        {
            int nativeResult = GstPadForward(instanceHandle, Gst.PadForwardFunctionTrampoline.Pointer, forwardState.UserData);
            System.GC.KeepAlive(this);
            return nativeResult != 0;
        }
        finally
        {
            forwardState.Free();
        }
    }

    /// <summary>
    /// Gets the capabilities of the allowed media types that can flow through
    /// @pad and its peer.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The allowed capabilities is calculated as the intersection of the results of
    /// calling gst_pad_query_caps() on @pad and its peer. The caller owns a reference
    /// on the resulting caps.
    /// </para>
    /// </remarks>
    /// <returns>
    /// the allowed #GstCaps of the
    ///     pad link. Unref the caps when you no longer need it. This
    ///     function returns %NULL when @pad has no peer.
    /// </returns>
    public Gst.Caps? GetAllowedCaps()
    {
        nint nativeResult = GstPadGetAllowedCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets the capabilities currently configured on @pad with the last
    /// #GST_EVENT_CAPS event.
    /// </summary>
    /// <returns>
    /// the current caps of the pad with
    /// incremented ref-count or %NULL when pad has no caps. Unref after usage.
    /// </returns>
    public Gst.Caps? GetCurrentCaps()
    {
        nint nativeResult = GstPadGetCurrentCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets the direction of the pad. The direction of the pad is
    /// decided at construction time so this function does not take
    /// the LOCK.
    /// </summary>
    /// <returns>the #GstPadDirection of the pad.</returns>
    public Gst.PadDirection GetDirection()
    {
        int nativeResult = GstPadGetDirection(Handle);
        System.GC.KeepAlive(this);
        return (Gst.PadDirection)nativeResult;
    }

    /// <summary>
    /// Gets the private data of a pad.
    /// No locking is performed in this function.
    /// </summary>
    /// <returns>a #gpointer to the private data.</returns>
    public nint GetElementPrivate()
    {
        nint nativeResult = GstPadGetElementPrivate(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Gets the #GstFlowReturn return from the last data passed by this pad.</summary>
    /// <returns>The result of <c>gst_pad_get_last_flow_return</c>.</returns>
    public Gst.FlowReturn GetLastFlowReturn()
    {
        int nativeResult = GstPadGetLastFlowReturn(Handle);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Get the offset applied to the running time of @pad. @pad has to be a source
    /// pad.
    /// </summary>
    /// <returns>the offset.</returns>
    public long GetOffset()
    {
        long nativeResult = GstPadGetOffset(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Gets the template for @pad.</summary>
    /// <returns>
    /// the #GstPadTemplate from which
    ///     this pad was instantiated, or %NULL if this pad has no
    ///     template. Unref after usage.
    /// </returns>
    public Gst.PadTemplate? GetPadTemplate()
    {
        nint nativeResult = GstPadGetPadTemplate(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.PadTemplate>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Gets the capabilities for @pad's template.</summary>
    /// <returns>
    /// the #GstCaps of this pad template.
    /// Unref after usage.
    /// </returns>
    public Gst.Caps GetPadTemplateCaps()
    {
        nint nativeResult = GstPadGetPadTemplateCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_pad_get_pad_template_caps returned no value.");
    }

    /// <summary>
    /// Gets the parent of @pad, cast to a #GstElement. If a @pad has no parent or
    /// its parent is not an element, return %NULL.
    /// </summary>
    /// <returns>
    /// the parent of the pad. The
    /// caller has a reference on the parent, so unref when you're finished
    /// with it.
    /// </returns>
    public Gst.Element? GetParentElement()
    {
        nint nativeResult = GstPadGetParentElement(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Element>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets the peer of @pad. This function refs the peer pad so
    /// you need to unref it after use.
    /// </summary>
    /// <returns>the peer #GstPad. Unref after usage.</returns>
    public Gst.Pad? GetPeer()
    {
        nint nativeResult = GstPadGetPeer(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Pad>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// When @pad is flushing this function returns #GST_FLOW_FLUSHING
    /// immediately and @buffer is %NULL.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Calls the getrange function of @pad, see #GstPadGetRangeFunction for a
    /// description of a getrange function. If @pad has no getrange function
    /// installed (see gst_pad_set_getrange_function()) this function returns
    /// #GST_FLOW_NOT_SUPPORTED.
    /// </para>
    /// <para>
    /// If @buffer points to a variable holding %NULL, a valid new #GstBuffer will be
    /// placed in @buffer when this function returns #GST_FLOW_OK. The new buffer
    /// must be freed with gst_buffer_unref() after usage.
    /// </para>
    /// <para>
    /// When @buffer points to a variable that points to a valid #GstBuffer, the
    /// buffer will be filled with the result data when this function returns
    /// #GST_FLOW_OK. If the provided buffer is larger than @size, only
    /// @size bytes will be filled in the result buffer and its size will be updated
    /// accordingly.
    /// </para>
    /// <para>
    /// Note that less than @size bytes can be returned in @buffer when, for example,
    /// an EOS condition is near or when @buffer is not large enough to hold @size
    /// bytes. The caller should check the result buffer size to get the result size.
    /// </para>
    /// <para>
    /// When this function returns any other result value than #GST_FLOW_OK, @buffer
    /// will be unchanged.
    /// </para>
    /// <para>This is a lowlevel function. Usually gst_pad_pull_range() is used.</para>
    /// </remarks>
    /// <param name="offset">The <c>offset</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <returns>a #GstFlowReturn from the pad.</returns>
    public Gst.FlowReturn GetRange(ulong offset, uint size, out Gst.Buffer? buffer)
    {
        nint bufferNative = default;
        int nativeResult = GstPadGetRange(Handle, offset, size, &bufferNative);
        System.GC.KeepAlive(this);
        buffer = Gst.Buffer.FromNative(bufferNative, Gst.Interop.Transfer.Full);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// If there is a single internal link of the given pad, this function will
    /// return it. Otherwise, it will return NULL.
    /// </summary>
    /// <returns>
    /// a #GstPad, or %NULL if @pad has none
    /// or more than one internal links. Unref returned pad with
    /// gst_object_unref().
    /// </returns>
    public Gst.Pad? GetSingleInternalLink()
    {
        nint nativeResult = GstPadGetSingleInternalLink(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Pad>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Returns a new reference of the sticky event of type @event_type
    /// from the event.
    /// </summary>
    /// <param name="eventType">The <c>eventType</c> argument.</param>
    /// <param name="idx">The <c>idx</c> argument.</param>
    /// <returns>
    /// a #GstEvent of type
    /// @event_type or %NULL when no event of @event_type was on
    /// @pad. Unref after usage.
    /// </returns>
    public Gst.Event? GetStickyEvent(Gst.EventType eventType, uint idx)
    {
        nint nativeResult = GstPadGetStickyEvent(Handle, (int)eventType, idx);
        System.GC.KeepAlive(this);
        return Gst.Event.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Returns the current #GstStream for the @pad, or %NULL if none has been
    /// set yet, i.e. the pad has not received a stream-start event yet.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This is a convenience wrapper around gst_pad_get_sticky_event() and
    /// gst_event_parse_stream().
    /// </para>
    /// </remarks>
    /// <returns>
    /// the current #GstStream for @pad, or %NULL.
    ///     unref the returned stream when no longer needed.
    /// </returns>
    public Gst.Stream? GetStream()
    {
        nint nativeResult = GstPadGetStream(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Stream>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Returns the current stream-id for the @pad, or %NULL if none has been
    /// set yet, i.e. the pad has not received a stream-start event yet.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This is a convenience wrapper around gst_pad_get_sticky_event() and
    /// gst_event_parse_stream_start().
    /// </para>
    /// <para>
    /// The returned stream-id string should be treated as an opaque string, its
    /// contents should not be interpreted.
    /// </para>
    /// </remarks>
    /// <returns>
    /// a newly-allocated copy of the stream-id for
    ///     @pad, or %NULL.  g_free() the returned string when no longer
    ///     needed.
    /// </returns>
    public string? GetStreamId()
    {
        nint nativeResult = GstPadGetStreamId(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8AndFree(nativeResult);
    }

    /// <summary>
    /// Get @pad task state. If no task is currently
    /// set, #GST_TASK_STOPPED is returned.
    /// </summary>
    /// <returns>The current state of @pad's task.</returns>
    public Gst.TaskState GetTaskState()
    {
        int nativeResult = GstPadGetTaskState(Handle);
        System.GC.KeepAlive(this);
        return (Gst.TaskState)nativeResult;
    }

    /// <summary>Check if @pad has caps set on it with a #GST_EVENT_CAPS event.</summary>
    /// <returns>%TRUE when @pad has caps associated with it.</returns>
    public bool HasCurrentCaps()
    {
        int nativeResult = GstPadHasCurrentCaps(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Query if a pad is active</summary>
    /// <returns>%TRUE if the pad is active.</returns>
    public bool IsActive()
    {
        int nativeResult = GstPadIsActive(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks if the pad is blocked or not. This function returns the
    /// last requested state of the pad. It is not certain that the pad
    /// is actually blocking at this point (see gst_pad_is_blocking()).
    /// </summary>
    /// <returns>%TRUE if the pad is blocked.</returns>
    public bool IsBlocked()
    {
        int nativeResult = GstPadIsBlocked(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks if the pad is blocking or not. This is a guaranteed state
    /// of whether the pad is actually blocking on a #GstBuffer or a #GstEvent.
    /// </summary>
    /// <returns>%TRUE if the pad is blocking.</returns>
    public bool IsBlocking()
    {
        int nativeResult = GstPadIsBlocking(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Checks if a @pad is linked to another pad or not.</summary>
    /// <returns>%TRUE if the pad is linked, %FALSE otherwise.</returns>
    public bool IsLinked()
    {
        int nativeResult = GstPadIsLinked(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets an iterator for the pads to which the given pad is linked to inside
    /// of the parent element.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Each #GstPad element yielded by the iterator will have its refcount increased,
    /// so unref after use.
    /// </para>
    /// <para>Free-function: gst_iterator_free</para>
    /// </remarks>
    /// <returns>
    /// a new #GstIterator of #GstPad
    ///     or %NULL when the pad does not have an iterator function
    ///     configured. Use gst_iterator_free() after usage.
    /// </returns>
    public Gst.Iterator? IterateInternalLinks()
    {
        nint nativeResult = GstPadIterateInternalLinks(Handle);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Iterate the list of pads to which the given pad is linked to inside of
    /// the parent element.
    /// This is the default handler, and thus returns an iterator of all of the
    /// pads inside the parent element with opposite direction.
    /// </summary>
    /// <remarks>
    /// <para>The caller must free this iterator after use with gst_iterator_free().</para>
    /// </remarks>
    /// <param name="parent">The <c>parent</c> argument.</param>
    /// <returns>
    /// a #GstIterator of #GstPad, or %NULL if @pad
    /// has no parent. Unref each returned pad with gst_object_unref().
    /// </returns>
    public Gst.Iterator? IterateInternalLinksDefault(Gst.Object? parent)
    {
        nint nativeResult = GstPadIterateInternalLinksDefault(Handle, parent is null ? 0 : parent.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(parent);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Links the source pad and the sink pad.</summary>
    /// <param name="sinkpad">The <c>sinkpad</c> argument.</param>
    /// <returns>
    /// A result code indicating if the connection worked or
    ///          what went wrong.
    /// </returns>
    public Gst.PadLinkReturn Link(Gst.Pad sinkpad)
    {
        ArgumentNullException.ThrowIfNull(sinkpad);
        int nativeResult = GstPadLink(Handle, sinkpad.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sinkpad);
        return (Gst.PadLinkReturn)nativeResult;
    }

    /// <summary>Links the source pad and the sink pad.</summary>
    /// <remarks>
    /// <para>
    /// This variant of #gst_pad_link provides a more granular control on the
    /// checks being done when linking. While providing some considerable speedups
    /// the caller of this method must be aware that wrong usage of those flags
    /// can cause severe issues. Refer to the documentation of #GstPadLinkCheck
    /// for more information.
    /// </para>
    /// <para>MT Safe.</para>
    /// </remarks>
    /// <param name="sinkpad">The <c>sinkpad</c> argument.</param>
    /// <param name="flags">The <c>flags</c> argument.</param>
    /// <returns>
    /// A result code indicating if the connection worked or
    ///          what went wrong.
    /// </returns>
    public Gst.PadLinkReturn LinkFull(Gst.Pad sinkpad, Gst.PadLinkCheck flags)
    {
        ArgumentNullException.ThrowIfNull(sinkpad);
        int nativeResult = GstPadLinkFull(Handle, sinkpad.Handle, (int)flags);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sinkpad);
        return (Gst.PadLinkReturn)nativeResult;
    }

    /// <summary>Links @src to @sink, creating any #GstGhostPad's in between as necessary.</summary>
    /// <remarks>
    /// <para>
    /// This is a convenience function to save having to create and add intermediate
    /// #GstGhostPad's as required for linking across #GstBin boundaries.
    /// </para>
    /// <para>
    /// If @src or @sink pads don't have parent elements or do not share a common
    /// ancestor, the link will fail.
    /// </para>
    /// </remarks>
    /// <param name="sink">The <c>sink</c> argument.</param>
    /// <returns>whether the link succeeded.</returns>
    public bool LinkMaybeGhosting(Gst.Pad sink)
    {
        ArgumentNullException.ThrowIfNull(sink);
        int nativeResult = GstPadLinkMaybeGhosting(Handle, sink.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sink);
        return nativeResult != 0;
    }

    /// <summary>Links @src to @sink, creating any #GstGhostPad's in between as necessary.</summary>
    /// <remarks>
    /// <para>
    /// This is a convenience function to save having to create and add intermediate
    /// #GstGhostPad's as required for linking across #GstBin boundaries.
    /// </para>
    /// <para>
    /// If @src or @sink pads don't have parent elements or do not share a common
    /// ancestor, the link will fail.
    /// </para>
    /// <para>
    /// Calling gst_pad_link_maybe_ghosting_full() with
    /// @flags == %GST_PAD_LINK_CHECK_DEFAULT is the recommended way of linking
    /// pads with safety checks applied.
    /// </para>
    /// </remarks>
    /// <param name="sink">The <c>sink</c> argument.</param>
    /// <param name="flags">The <c>flags</c> argument.</param>
    /// <returns>whether the link succeeded.</returns>
    public bool LinkMaybeGhostingFull(Gst.Pad sink, Gst.PadLinkCheck flags)
    {
        ArgumentNullException.ThrowIfNull(sink);
        int nativeResult = GstPadLinkMaybeGhostingFull(Handle, sink.Handle, (int)flags);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sink);
        return nativeResult != 0;
    }

    /// <summary>
    /// Mark a pad for needing reconfiguration. The next call to
    /// gst_pad_check_reconfigure() will return %TRUE after this call.
    /// </summary>
    public void MarkReconfigure()
    {
        GstPadMarkReconfigure(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Check the #GST_PAD_FLAG_NEED_RECONFIGURE flag on @pad and return %TRUE
    /// if the flag was set.
    /// </summary>
    /// <returns>%TRUE is the GST_PAD_FLAG_NEED_RECONFIGURE flag is set on @pad.</returns>
    public bool NeedsReconfigure()
    {
        int nativeResult = GstPadNeedsReconfigure(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Pause the task of @pad. This function will also wait until the
    /// function executed by the task is finished if this function is not
    /// called from the task function.
    /// </summary>
    /// <returns>
    /// a %TRUE if the task could be paused or %FALSE when the pad
    /// has no task.
    /// </returns>
    public bool PauseTask()
    {
        int nativeResult = GstPadPauseTask(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Performs gst_pad_query() on the peer of @pad.</summary>
    /// <remarks>
    /// <para>
    /// The caller is responsible for both the allocation and deallocation of
    /// the query structure.
    /// </para>
    /// </remarks>
    /// <param name="query">The <c>query</c> argument.</param>
    /// <returns>
    /// %TRUE if the query could be performed. This function returns %FALSE
    /// if @pad has no peer.
    /// </returns>
    public bool PeerQuery(Gst.Query query)
    {
        ArgumentNullException.ThrowIfNull(query);
        int nativeResult = GstPadPeerQuery(Handle, query.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(query);
        return nativeResult != 0;
    }

    /// <summary>
    /// Check if the peer of @pad accepts @caps. If @pad has no peer, this function
    /// returns %TRUE.
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>%TRUE if the peer of @pad can accept the caps or @pad has no peer.</returns>
    public bool PeerQueryAcceptCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstPadPeerQueryAcceptCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets the capabilities of the peer connected to this pad. Similar to
    /// gst_pad_query_caps().
    /// </summary>
    /// <remarks>
    /// <para>
    /// When called on srcpads @filter contains the caps that
    /// upstream could produce in the order preferred by upstream. When
    /// called on sinkpads @filter contains the caps accepted by
    /// downstream in the preferred order. @filter might be %NULL but
    /// if it is not %NULL the returned caps will be a subset of @filter.
    /// </para>
    /// </remarks>
    /// <param name="filter">The <c>filter</c> argument.</param>
    /// <returns>
    /// the caps of the peer pad with incremented
    /// ref-count. When there is no peer pad, this function returns @filter or,
    /// when @filter is %NULL, ANY caps.
    /// </returns>
    public Gst.Caps PeerQueryCaps(Gst.Caps? filter)
    {
        nint nativeResult = GstPadPeerQueryCaps(Handle, filter is null ? 0 : filter.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(filter);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_pad_peer_query_caps returned no value.");
    }

    /// <summary>
    /// Queries the peer pad of a given sink pad to convert @src_val in @src_format
    /// to @dest_format.
    /// </summary>
    /// <param name="srcFormat">The <c>srcFormat</c> argument.</param>
    /// <param name="srcVal">The <c>srcVal</c> argument.</param>
    /// <param name="destFormat">The <c>destFormat</c> argument.</param>
    /// <param name="destVal">The <c>destVal</c> argument.</param>
    /// <returns>%TRUE if the query could be performed.</returns>
    public bool PeerQueryConvert(Gst.Format srcFormat, long srcVal, Gst.Format destFormat, out long destVal)
    {
        long destValNative = default;
        int nativeResult = GstPadPeerQueryConvert(Handle, (int)srcFormat, srcVal, (int)destFormat, &destValNative);
        System.GC.KeepAlive(this);
        destVal = destValNative;
        return nativeResult != 0;
    }

    /// <summary>Queries the peer pad of a given sink pad for the total stream duration.</summary>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="duration">The <c>duration</c> argument.</param>
    /// <returns>%TRUE if the query could be performed.</returns>
    public bool PeerQueryDuration(Gst.Format format, out long duration)
    {
        long durationNative = default;
        int nativeResult = GstPadPeerQueryDuration(Handle, (int)format, &durationNative);
        System.GC.KeepAlive(this);
        duration = durationNative;
        return nativeResult != 0;
    }

    /// <summary>Queries the peer of a given sink pad for the stream position.</summary>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="cur">The <c>cur</c> argument.</param>
    /// <returns>%TRUE if the query could be performed.</returns>
    public bool PeerQueryPosition(Gst.Format format, out long cur)
    {
        long curNative = default;
        int nativeResult = GstPadPeerQueryPosition(Handle, (int)format, &curNative);
        System.GC.KeepAlive(this);
        cur = curNative;
        return nativeResult != 0;
    }

    /// <summary>
    /// Checks if all internally linked pads of @pad accepts the caps in @query and
    /// returns the intersection of the results.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function is useful as a default accept caps query function for an element
    /// that can handle any stream format, but requires caps that are acceptable for
    /// all opposite pads.
    /// </para>
    /// </remarks>
    /// <param name="query">The <c>query</c> argument.</param>
    /// <returns>%TRUE if @query could be executed</returns>
    public bool ProxyQueryAcceptCaps(Gst.Query query)
    {
        ArgumentNullException.ThrowIfNull(query);
        int nativeResult = GstPadProxyQueryAcceptCaps(Handle, query.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(query);
        return nativeResult != 0;
    }

    /// <summary>
    /// Calls gst_pad_query_caps() for all internally linked pads of @pad and returns
    /// the intersection of the results.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function is useful as a default caps query function for an element
    /// that can handle any stream format, but requires all its pads to have
    /// the same caps.  Two such elements are tee and adder.
    /// </para>
    /// </remarks>
    /// <param name="query">The <c>query</c> argument.</param>
    /// <returns>%TRUE if @query could be executed</returns>
    public bool ProxyQueryCaps(Gst.Query query)
    {
        ArgumentNullException.ThrowIfNull(query);
        int nativeResult = GstPadProxyQueryCaps(Handle, query.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(query);
        return nativeResult != 0;
    }

    /// <summary>Pulls a @buffer from the peer pad or fills up a provided buffer.</summary>
    /// <remarks>
    /// <para>
    /// This function will first trigger the pad block signal if it was
    /// installed.
    /// </para>
    /// <para>
    /// When @pad is not linked #GST_FLOW_NOT_LINKED is returned else this
    /// function returns the result of gst_pad_get_range() on the peer pad.
    /// See gst_pad_get_range() for a list of return values and for the
    /// semantics of the arguments of this function.
    /// </para>
    /// <para>
    /// If @buffer points to a variable holding %NULL, a valid new #GstBuffer will be
    /// placed in @buffer when this function returns #GST_FLOW_OK. The new buffer
    /// must be freed with gst_buffer_unref() after usage. When this function
    /// returns any other result value, @buffer will still point to %NULL.
    /// </para>
    /// <para>
    /// When @buffer points to a variable that points to a valid #GstBuffer, the
    /// buffer will be filled with the result data when this function returns
    /// #GST_FLOW_OK. When this function returns any other result value,
    /// @buffer will be unchanged. If the provided buffer is larger than @size, only
    /// @size bytes will be filled in the result buffer and its size will be updated
    /// accordingly.
    /// </para>
    /// <para>
    /// Note that less than @size bytes can be returned in @buffer when, for example,
    /// an EOS condition is near or when @buffer is not large enough to hold @size
    /// bytes. The caller should check the result buffer size to get the result size.
    /// </para>
    /// </remarks>
    /// <param name="offset">The <c>offset</c> argument.</param>
    /// <param name="size">The <c>size</c> argument.</param>
    /// <param name="buffer">The <c>buffer</c> argument.</param>
    /// <returns>a #GstFlowReturn from the peer pad.</returns>
    public Gst.FlowReturn PullRange(ulong offset, uint size, out Gst.Buffer? buffer)
    {
        nint bufferNative = default;
        int nativeResult = GstPadPullRange(Handle, offset, size, &bufferNative);
        System.GC.KeepAlive(this);
        buffer = Gst.Buffer.FromNative(bufferNative, Gst.Interop.Transfer.Full);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>Pushes a buffer to the peer of @pad.</summary>
    /// <remarks>
    /// <para>
    /// This function will call installed block probes before triggering any
    /// installed data probes.
    /// </para>
    /// <para>
    /// The function proceeds calling gst_pad_chain() on the peer pad and returns
    /// the value from that function. If @pad has no peer, #GST_FLOW_NOT_LINKED will
    /// be returned.
    /// </para>
    /// <para>
    /// In all cases, success or failure, the caller loses its reference to @buffer
    /// after calling this function.
    /// </para>
    /// <para>
    /// The <c>buffer</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="buffer">
    /// The <c>buffer</c> argument.
    /// The call consumes it: <paramref name="buffer"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn from the peer pad.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="buffer"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="buffer"/> was disposed.
    /// </exception>
    public Gst.FlowReturn Push(Gst.Buffer buffer)
    {
        ArgumentNullException.ThrowIfNull(buffer);
        nint instanceHandle = Handle;
        nint bufferNative = buffer.Handle;
        nint bufferOwned = Gst.GstNative.MiniObjectRef(bufferNative);
        int nativeResult = GstPadPush(instanceHandle, bufferOwned);
        System.GC.KeepAlive(this);
        buffer.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>Pushes a buffer list to the peer of @pad.</summary>
    /// <remarks>
    /// <para>
    /// This function will call installed block probes before triggering any
    /// installed data probes.
    /// </para>
    /// <para>
    /// The function proceeds calling the chain function on the peer pad and returns
    /// the value from that function. If @pad has no peer, #GST_FLOW_NOT_LINKED will
    /// be returned. If the peer pad does not have any installed chainlist function
    /// every group buffer of the list will be merged into a normal #GstBuffer and
    /// chained via gst_pad_chain().
    /// </para>
    /// <para>
    /// In all cases, success or failure, the caller loses its reference to @list
    /// after calling this function.
    /// </para>
    /// <para>
    /// The <c>list</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="list">
    /// The <c>list</c> argument.
    /// The call consumes it: <paramref name="list"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>a #GstFlowReturn from the peer pad.</returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="list"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="list"/> was disposed.
    /// </exception>
    public Gst.FlowReturn PushList(Gst.BufferList list)
    {
        ArgumentNullException.ThrowIfNull(list);
        nint instanceHandle = Handle;
        nint listNative = list.Handle;
        nint listOwned = Gst.GstNative.MiniObjectRef(listNative);
        int nativeResult = GstPadPushList(instanceHandle, listOwned);
        System.GC.KeepAlive(this);
        list.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Dispatches a query to a pad. The query should have been allocated by the
    /// caller via one of the type-specific allocation functions. The element that
    /// the pad belongs to is responsible for filling the query with an appropriate
    /// response, which should then be parsed with a type-specific query parsing
    /// function.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Again, the caller is responsible for both the allocation and deallocation of
    /// the query structure.
    /// </para>
    /// <para>Please also note that some queries might need a running pipeline to work.</para>
    /// </remarks>
    /// <param name="query">The <c>query</c> argument.</param>
    /// <returns>%TRUE if the query could be performed.</returns>
    public bool Query(Gst.Query query)
    {
        ArgumentNullException.ThrowIfNull(query);
        int nativeResult = GstPadQuery(Handle, query.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(query);
        return nativeResult != 0;
    }

    /// <summary>Check if the given pad accepts the caps.</summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    /// <returns>%TRUE if the pad can accept the caps.</returns>
    public bool QueryAcceptCaps(Gst.Caps caps)
    {
        ArgumentNullException.ThrowIfNull(caps);
        int nativeResult = GstPadQueryAcceptCaps(Handle, caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets the capabilities this pad can produce or consume.
    /// Note that this method doesn't necessarily return the caps set by sending a
    /// gst_event_new_caps() - use gst_pad_get_current_caps() for that instead.
    /// gst_pad_query_caps returns all possible caps a pad can operate with, using
    /// the pad's CAPS query function, If the query fails, this function will return
    /// @filter, if not %NULL, otherwise ANY.
    /// </summary>
    /// <remarks>
    /// <para>
    /// When called on sinkpads @filter contains the caps that
    /// upstream could produce in the order preferred by upstream. When
    /// called on srcpads @filter contains the caps accepted by
    /// downstream in the preferred order. @filter might be %NULL but
    /// if it is not %NULL the returned caps will be a subset of @filter.
    /// </para>
    /// <para>
    /// Note that this function does not return writable #GstCaps, use
    /// gst_caps_make_writable() before modifying the caps.
    /// </para>
    /// </remarks>
    /// <param name="filter">The <c>filter</c> argument.</param>
    /// <returns>the caps of the pad with incremented ref-count.</returns>
    public Gst.Caps QueryCaps(Gst.Caps? filter)
    {
        nint nativeResult = GstPadQueryCaps(Handle, filter is null ? 0 : filter.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(filter);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_pad_query_caps returned no value.");
    }

    /// <summary>Queries a pad to convert @src_val in @src_format to @dest_format.</summary>
    /// <param name="srcFormat">The <c>srcFormat</c> argument.</param>
    /// <param name="srcVal">The <c>srcVal</c> argument.</param>
    /// <param name="destFormat">The <c>destFormat</c> argument.</param>
    /// <param name="destVal">The <c>destVal</c> argument.</param>
    /// <returns>%TRUE if the query could be performed.</returns>
    public bool QueryConvert(Gst.Format srcFormat, long srcVal, Gst.Format destFormat, out long destVal)
    {
        long destValNative = default;
        int nativeResult = GstPadQueryConvert(Handle, (int)srcFormat, srcVal, (int)destFormat, &destValNative);
        System.GC.KeepAlive(this);
        destVal = destValNative;
        return nativeResult != 0;
    }

    /// <summary>
    /// Invokes the default query handler for the given pad.
    /// The query is sent to all pads internally linked to @pad. Note that
    /// if there are many possible sink pads that are internally linked to
    /// @pad, only one will be sent the query.
    /// Multi-sinkpad elements should implement custom query handlers.
    /// </summary>
    /// <param name="parent">The <c>parent</c> argument.</param>
    /// <param name="query">The <c>query</c> argument.</param>
    /// <returns>%TRUE if the query was performed successfully.</returns>
    public bool QueryDefault(Gst.Object? parent, Gst.Query query)
    {
        ArgumentNullException.ThrowIfNull(query);
        int nativeResult = GstPadQueryDefault(Handle, parent is null ? 0 : parent.Handle, query.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(parent);
        System.GC.KeepAlive(query);
        return nativeResult != 0;
    }

    /// <summary>Queries a pad for the total stream duration.</summary>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="duration">The <c>duration</c> argument.</param>
    /// <returns>%TRUE if the query could be performed.</returns>
    public bool QueryDuration(Gst.Format format, out long duration)
    {
        long durationNative = default;
        int nativeResult = GstPadQueryDuration(Handle, (int)format, &durationNative);
        System.GC.KeepAlive(this);
        duration = durationNative;
        return nativeResult != 0;
    }

    /// <summary>Queries a pad for the stream position.</summary>
    /// <param name="format">The <c>format</c> argument.</param>
    /// <param name="cur">The <c>cur</c> argument.</param>
    /// <returns>%TRUE if the query could be performed.</returns>
    public bool QueryPosition(Gst.Format format, out long cur)
    {
        long curNative = default;
        int nativeResult = GstPadQueryPosition(Handle, (int)format, &curNative);
        System.GC.KeepAlive(this);
        cur = curNative;
        return nativeResult != 0;
    }

    /// <summary>Remove the probe with @id from @pad.</summary>
    /// <remarks>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="id">The <c>id</c> argument.</param>
    public void RemoveProbe(System.Runtime.InteropServices.CULong id)
    {
        GstPadRemoveProbe(Handle, id);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the given activate function for @pad. The activate function will
    /// dispatch to gst_pad_activate_mode() to perform the actual activation.
    /// Only makes sense to set on sink pads.
    /// </summary>
    /// <remarks>
    /// <para>Call this function if your sink pad can start a pull-based task.</para>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="activate">the #GstPadActivateFunction to set.</param>
    public void SetActivateFunction(Gst.PadActivateFunction? activate)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle activateState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "activate", activate);
        GstPadSetActivateFunctionFull(instanceHandle, activate is null ? 0 : Gst.PadActivateFunctionTrampoline.Pointer, activateState.UserData, activate is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the given activate_mode function for the pad. An activate_mode function
    /// prepares the element for data passing.
    /// </summary>
    /// <remarks>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="activatemode">the #GstPadActivateModeFunction to set.</param>
    public void SetActivatemodeFunction(Gst.PadActivateModeFunction? activatemode)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle activatemodeState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "activatemode", activatemode);
        GstPadSetActivatemodeFunctionFull(instanceHandle, activatemode is null ? 0 : Gst.PadActivateModeFunctionTrampoline.Pointer, activatemodeState.UserData, activatemode is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Activates or deactivates the given pad.
    /// Normally called from within core state change functions.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @active, makes sure the pad is active. If it is already active, either in
    /// push or pull mode, just return. Otherwise dispatches to the pad's activate
    /// function to perform the actual activation.
    /// </para>
    /// <para>
    /// If not @active, calls gst_pad_activate_mode() with the pad's current mode
    /// and a %FALSE argument.
    /// </para>
    /// </remarks>
    /// <param name="active">The <c>active</c> argument.</param>
    /// <returns>%TRUE if the operation was successful.</returns>
    public bool SetActive(bool active)
    {
        int nativeResult = GstPadSetActive(Handle, active ? 1 : 0);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Sets the given chain function for the pad. The chain function is called to
    /// process a #GstBuffer input buffer. see #GstPadChainFunction for more details.
    /// </summary>
    /// <remarks>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="chain">the #GstPadChainFunction to set.</param>
    public void SetChainFunction(Gst.PadChainFunction? chain)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle chainState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "chain", chain);
        GstPadSetChainFunctionFull(instanceHandle, chain is null ? 0 : Gst.PadChainFunctionTrampoline.Pointer, chainState.UserData, chain is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the given chain list function for the pad. The chainlist function is
    /// called to process a #GstBufferList input buffer list. See
    /// #GstPadChainListFunction for more details.
    /// </summary>
    /// <remarks>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="chainlist">the #GstPadChainListFunction to set.</param>
    public void SetChainListFunction(Gst.PadChainListFunction? chainlist)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle chainlistState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "chain_list", chainlist);
        GstPadSetChainListFunctionFull(instanceHandle, chainlist is null ? 0 : Gst.PadChainListFunctionTrampoline.Pointer, chainlistState.UserData, chainlist is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the given private data gpointer on the pad.
    /// This function can only be used by the element that owns the pad.
    /// No locking is performed in this function.
    /// </summary>
    /// <param name="priv">The <c>priv</c> argument.</param>
    public void SetElementPrivate(nint priv)
    {
        GstPadSetElementPrivate(Handle, priv);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the given event handler for the pad.</summary>
    /// <remarks>
    /// <para>
    /// This writes the same storage as SetEventFunction (gstpad.c:1933-1937, :1979-1984): a pad
    /// carries one of the two handlers, not both, and the later call releases the state of the
    /// earlier one. Unsetting this handler leaves the event_wrap wrapper this setter installed
    /// as the plain event function of the pad (gstpad.c:1981-1982), and that wrapper
    /// dereferences the full function pointer the same call has just cleared, so follow
    /// SetEventFullFunction(null) with SetEventFunction(null) to take both off the pad.
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="event">the #GstPadEventFullFunction to set.</param>
    public void SetEventFullFunction(Gst.PadEventFullFunction? @event)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle @eventState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "event", @event);
        GstPadSetEventFullFunctionFull(instanceHandle, @event is null ? 0 : Gst.PadEventFullFunctionTrampoline.Pointer, @eventState.UserData, @event is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the given event handler for the pad.</summary>
    /// <remarks>
    /// <para>
    /// This writes the same storage as SetEventFullFunction (gstpad.c:1933-1937, :1979-1984): a
    /// pad carries one of the two handlers, not both, and the later call releases the state of
    /// the earlier one. Installing this handler while a full event handler is installed does
    /// not take that one out of the pad (gstpad.c:1935 leaves the full function pointer alone)
    /// and gst_pad_send_event_unchecked prefers it (gstpad.c:6170), so the handler set here is
    /// never reached: unset the full one first. GStreamer reads this function pointer without
    /// holding a lock (gstpad.c:4590-4594), so replacing or unsetting the handler while the pad
    /// is running races an invocation that is already under way: the handler being replaced may
    /// still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="event">the #GstPadEventFunction to set.</param>
    public void SetEventFunction(Gst.PadEventFunction? @event)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle @eventState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "event", @event);
        GstPadSetEventFunctionFull(instanceHandle, @event is null ? 0 : Gst.PadEventFunctionTrampoline.Pointer, @eventState.UserData, @event is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the given getrange function for the pad. The getrange function is
    /// called to produce a new #GstBuffer to start the processing pipeline. see
    /// #GstPadGetRangeFunction for a description of the getrange function.
    /// </summary>
    /// <remarks>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="get">the #GstPadGetRangeFunction to set.</param>
    public void SetGetrangeFunction(Gst.PadGetRangeFunction? get)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle getState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "getrange", get);
        GstPadSetGetrangeFunctionFull(instanceHandle, get is null ? 0 : Gst.PadGetRangeFunctionTrampoline.Pointer, getState.UserData, get is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>Sets the given internal link iterator function for the pad.</summary>
    /// <remarks>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="iterintlink">the #GstPadIterIntLinkFunction to set.</param>
    public void SetIterateInternalLinksFunction(Gst.PadIterIntLinkFunction? iterintlink)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle iterintlinkState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "iterate_internal_links", iterintlink);
        GstPadSetIterateInternalLinksFunctionFull(instanceHandle, iterintlink is null ? 0 : Gst.PadIterIntLinkFunctionTrampoline.Pointer, iterintlinkState.UserData, iterintlink is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the given link function for the pad. It will be called when
    /// the pad is linked with another pad.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The return value #GST_PAD_LINK_OK should be used when the connection can be
    /// made.
    /// </para>
    /// <para>
    /// The return value #GST_PAD_LINK_REFUSED should be used when the connection
    /// cannot be made for some reason.
    /// </para>
    /// <para>
    /// If @link is installed on a source pad, it should call the #GstPadLinkFunction
    /// of the peer sink pad, if present.
    /// </para>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="link">the #GstPadLinkFunction to set.</param>
    public void SetLinkFunction(Gst.PadLinkFunction? link)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle linkState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "link", link);
        GstPadSetLinkFunctionFull(instanceHandle, link is null ? 0 : Gst.PadLinkFunctionTrampoline.Pointer, linkState.UserData, link is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the offset that will be applied to the running time of @pad. Upon next
    /// buffer, every sticky events (notably segment) will be pushed again with
    /// their running time adjusted. For that reason this is only reliable on
    /// source pads.
    /// </summary>
    /// <param name="offset">The <c>offset</c> argument.</param>
    public void SetOffset(long offset)
    {
        GstPadSetOffset(Handle, offset);
        System.GC.KeepAlive(this);
    }

    /// <summary>Set the given query function for the pad.</summary>
    /// <remarks>
    /// <para>
    /// GStreamer reads this function pointer without holding a lock (gstpad.c:4590-4594), so
    /// replacing or unsetting the handler while the pad is running races an invocation that is
    /// already under way: the handler being replaced may still be executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="query">the #GstPadQueryFunction to set.</param>
    public void SetQueryFunction(Gst.PadQueryFunction? query)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle queryState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "query", query);
        GstPadSetQueryFunctionFull(instanceHandle, query is null ? 0 : Gst.PadQueryFunctionTrampoline.Pointer, queryState.UserData, query is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the given unlink function for the pad. It will be called
    /// when the pad is unlinked.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Note that the pad's lock is already held when the unlink
    /// function is called, so most pad functions cannot be called
    /// from within the callback.
    /// </para>
    /// <para>
    /// The object lock of the pad is already held when the unlink function runs
    /// (gstpad.c:2109-2121), so most other members of Gst.Pad must not be called from the
    /// handler. GStreamer reads this function pointer without holding a lock
    /// (gstpad.c:4590-4594), so replacing or unsetting the handler while the pad is running
    /// races an invocation that is already under way: the handler being replaced may still be
    /// executing when this returns.
    /// </para>
    /// </remarks>
    /// <param name="unlink">the #GstPadUnlinkFunction to set.</param>
    public void SetUnlinkFunction(Gst.PadUnlinkFunction? unlink)
    {
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle unlinkState = Gst.Interop.InstanceKeyedCallbacks.Install(instanceHandle, "unlink", unlink);
        GstPadSetUnlinkFunctionFull(instanceHandle, unlink is null ? 0 : Gst.PadUnlinkFunctionTrampoline.Pointer, unlinkState.UserData, unlink is null ? 0 : (nint)Gst.Interop.InstanceKeyedCallbacks.DestroyNotify);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Starts a task that repeatedly calls @func with @user_data. This function
    /// is mostly used in pad activation functions to start the dataflow.
    /// The #GST_PAD_STREAM_LOCK of @pad will automatically be acquired
    /// before @func is called.
    /// </summary>
    /// <param name="func">the task function to call</param>
    /// <returns>a %TRUE if the task could be started.</returns>
    public bool StartTask(Gst.TaskFunction func)
    {
        ArgumentNullException.ThrowIfNull(func);
        nint instanceHandle = Handle;
        Gst.Interop.CallbackHandle funcState = Gst.Interop.CallbackHandle.Alloc(func);
        int nativeResult = GstPadStartTask(instanceHandle, Gst.TaskFunctionTrampoline.Pointer, funcState.UserData, (nint)Gst.Interop.CallbackHandle.DestroyNotify);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Stop the task of @pad. This function will also make sure that the
    /// function executed by the task will effectively stop if not called
    /// from the GstTaskFunction.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function will deadlock if called from the GstTaskFunction of
    /// the task. Use gst_task_pause() instead.
    /// </para>
    /// <para>
    /// Regardless of whether the pad has a task, the stream lock is acquired and
    /// released so as to ensure that streaming through this pad has finished.
    /// </para>
    /// </remarks>
    /// <returns>a %TRUE if the task could be stopped or %FALSE on error.</returns>
    public bool StopTask()
    {
        int nativeResult = GstPadStopTask(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Store the sticky @event on @pad</summary>
    /// <param name="event">The <c>@event</c> argument.</param>
    /// <returns>
    /// #GST_FLOW_OK on success, #GST_FLOW_FLUSHING when the pad
    /// was flushing or #GST_FLOW_EOS when the pad was EOS.
    /// </returns>
    public Gst.FlowReturn StoreStickyEvent(Gst.Event @event)
    {
        ArgumentNullException.ThrowIfNull(@event);
        int nativeResult = GstPadStoreStickyEvent(Handle, @event.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(@event);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Unlinks the source pad from the sink pad. Will emit the #GstPad::unlinked
    /// signal on both pads.
    /// </summary>
    /// <param name="sinkpad">The <c>sinkpad</c> argument.</param>
    /// <returns>
    /// %TRUE if the pads were unlinked. This function returns %FALSE if
    /// the pads were not linked together.
    /// </returns>
    public bool Unlink(Gst.Pad sinkpad)
    {
        ArgumentNullException.ThrowIfNull(sinkpad);
        int nativeResult = GstPadUnlink(Handle, sinkpad.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sinkpad);
        return nativeResult != 0;
    }

    /// <summary>
    /// A helper function you can use that sets the FIXED_CAPS flag
    /// This way the default CAPS query will always return the negotiated caps
    /// or in case the pad is not negotiated, the padtemplate caps.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The negotiated caps are the caps of the last CAPS event that passed on the
    /// pad. Use this function on a pad that, once it negotiated to a CAPS, cannot
    /// be renegotiated to something else.
    /// </para>
    /// </remarks>
    public void UseFixedCaps()
    {
        GstPadUseFixedCaps(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Gets a string representing the given pad-link return.</summary>
    /// <param name="ret">The <c>ret</c> argument.</param>
    /// <returns>a static string with the name of the pad-link return.</returns>
    public static string LinkGetName(Gst.PadLinkReturn ret)
    {
        nint nativeResult = GstPadLinkGetName((int)ret);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_pad_link_get_name returned no value.");
    }

    /// <summary>The <c>direction</c> property.</summary>
    public Gst.PadDirection Direction => GetDirection();

    /// <summary>The offset that will be applied to the running time of the pad.</summary>
    public long Offset
    {
        get => GetOffset();
        set => SetOffset(value);
    }

    /// <summary>The <c>caps</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>
    /// Reading builds a wrapper that owns a reference of its own: dispose it
    /// when you are done with it.
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public Gst.Caps? Caps
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("caps");
            return holder.GetMiniObject<Gst.Caps>();
        }
    }

    /// <summary>The <c>template</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>
    /// Reading hands back the interned wrapper of the object, which the binding
    /// keeps; it is not the reader's to dispose.
    /// </para>
    /// <para>
    /// Writing takes a reference of its own, so the argument stays the caller's
    /// to dispose, and <see langword="null"/> clears the property.
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public Gst.PadTemplate? Template
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("template");
            return (Gst.PadTemplate?)holder.GetObject();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("template");
            holder.SetObject(value);
            SetPropertyValue("template", in holder);
        }
    }

    /// <summary>The arguments of the <c>linked</c> signal of <c>GstPad</c>.</summary>
    public sealed class LinkedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="LinkedSignalArgs"/> class.</summary>
        /// <param name="peer">the peer pad that has been connected</param>
        internal LinkedSignalArgs(Gst.Pad peer)
        {
            Peer = peer;
        }

        /// <summary>the peer pad that has been connected</summary>
        public Gst.Pad Peer { get; }
    }

    /// <summary>Signals that a pad has been linked to the peer pad.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Pad.LinkedSignalArgs> Linked
    {
        add => Gst.SignalConnections.Add(this, "linked", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&LinkedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "linked", value);
    }

    /// <summary>The native handler of the <c>linked</c> signal of <c>GstPad</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void LinkedTrampoline(nint instance, nint peer, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Pad.LinkedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Pad peerValue = Gst.GObject.Object.FromNative<Gst.Pad>(peer, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The linked signal of GstPad passed no peer.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Pad.LinkedSignalArgs(peerValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>unlinked</c> signal of <c>GstPad</c>.</summary>
    public sealed class UnlinkedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="UnlinkedSignalArgs"/> class.</summary>
        /// <param name="peer">the peer pad that has been disconnected</param>
        internal UnlinkedSignalArgs(Gst.Pad peer)
        {
            Peer = peer;
        }

        /// <summary>the peer pad that has been disconnected</summary>
        public Gst.Pad Peer { get; }
    }

    /// <summary>Signals that a pad has been unlinked from the peer pad.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Pad.UnlinkedSignalArgs> Unlinked
    {
        add => Gst.SignalConnections.Add(this, "unlinked", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&UnlinkedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "unlinked", value);
    }

    /// <summary>The native handler of the <c>unlinked</c> signal of <c>GstPad</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void UnlinkedTrampoline(nint instance, nint peer, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Pad.UnlinkedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Pad peerValue = Gst.GObject.Object.FromNative<Gst.Pad>(peer, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The unlinked signal of GstPad passed no peer.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Pad.UnlinkedSignalArgs(peerValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_pad_new</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_new")]
    private static partial nint GstPadNew(byte* name, int direction);

    /// <summary>The <c>gst_pad_new_from_static_template</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_new_from_static_template")]
    private static partial nint GstPadNewFromStaticTemplate(nint templ, byte* name);

    /// <summary>The <c>gst_pad_new_from_template</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_new_from_template")]
    private static partial nint GstPadNewFromTemplate(nint templ, byte* name);

    /// <summary>The <c>gst_pad_activate_mode</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_activate_mode")]
    private static partial int GstPadActivateMode(nint pad, int mode, int active);

    /// <summary>The <c>gst_pad_add_probe</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_add_probe")]
    private static partial System.Runtime.InteropServices.CULong GstPadAddProbe(nint pad, int mask, nint callback, nint userData, nint destroyData);

    /// <summary>The <c>gst_pad_can_link</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_can_link")]
    private static partial int GstPadCanLink(nint srcpad, nint sinkpad);

    /// <summary>The <c>gst_pad_chain</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_chain")]
    private static partial int GstPadChain(nint pad, nint buffer);

    /// <summary>The <c>gst_pad_chain_list</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_chain_list")]
    private static partial int GstPadChainList(nint pad, nint list);

    /// <summary>The <c>gst_pad_check_reconfigure</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_check_reconfigure")]
    private static partial int GstPadCheckReconfigure(nint pad);

    /// <summary>The <c>gst_pad_create_stream_id</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_create_stream_id")]
    private static partial nint GstPadCreateStreamId(nint pad, nint parent, byte* streamId);

    /// <summary>The <c>gst_pad_event_default</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_event_default")]
    private static partial int GstPadEventDefault(nint pad, nint parent, nint @event);

    /// <summary>The <c>gst_pad_forward</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_forward")]
    private static partial int GstPadForward(nint pad, nint forward, nint userData);

    /// <summary>The <c>gst_pad_get_allowed_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_allowed_caps")]
    private static partial nint GstPadGetAllowedCaps(nint pad);

    /// <summary>The <c>gst_pad_get_current_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_current_caps")]
    private static partial nint GstPadGetCurrentCaps(nint pad);

    /// <summary>The <c>gst_pad_get_direction</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_direction")]
    private static partial int GstPadGetDirection(nint pad);

    /// <summary>The <c>gst_pad_get_element_private</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_element_private")]
    private static partial nint GstPadGetElementPrivate(nint pad);

    /// <summary>The <c>gst_pad_get_last_flow_return</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_last_flow_return")]
    private static partial int GstPadGetLastFlowReturn(nint pad);

    /// <summary>The <c>gst_pad_get_offset</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_offset")]
    private static partial long GstPadGetOffset(nint pad);

    /// <summary>The <c>gst_pad_get_pad_template</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_pad_template")]
    private static partial nint GstPadGetPadTemplate(nint pad);

    /// <summary>The <c>gst_pad_get_pad_template_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_pad_template_caps")]
    private static partial nint GstPadGetPadTemplateCaps(nint pad);

    /// <summary>The <c>gst_pad_get_parent_element</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_parent_element")]
    private static partial nint GstPadGetParentElement(nint pad);

    /// <summary>The <c>gst_pad_get_peer</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_peer")]
    private static partial nint GstPadGetPeer(nint pad);

    /// <summary>The <c>gst_pad_get_range</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_range")]
    private static partial int GstPadGetRange(nint pad, ulong offset, uint size, nint* buffer);

    /// <summary>The <c>gst_pad_get_single_internal_link</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_single_internal_link")]
    private static partial nint GstPadGetSingleInternalLink(nint pad);

    /// <summary>The <c>gst_pad_get_sticky_event</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_sticky_event")]
    private static partial nint GstPadGetStickyEvent(nint pad, int eventType, uint idx);

    /// <summary>The <c>gst_pad_get_stream</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_stream")]
    private static partial nint GstPadGetStream(nint pad);

    /// <summary>The <c>gst_pad_get_stream_id</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_stream_id")]
    private static partial nint GstPadGetStreamId(nint pad);

    /// <summary>The <c>gst_pad_get_task_state</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_task_state")]
    private static partial int GstPadGetTaskState(nint pad);

    /// <summary>The <c>gst_pad_has_current_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_has_current_caps")]
    private static partial int GstPadHasCurrentCaps(nint pad);

    /// <summary>The <c>gst_pad_is_active</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_is_active")]
    private static partial int GstPadIsActive(nint pad);

    /// <summary>The <c>gst_pad_is_blocked</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_is_blocked")]
    private static partial int GstPadIsBlocked(nint pad);

    /// <summary>The <c>gst_pad_is_blocking</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_is_blocking")]
    private static partial int GstPadIsBlocking(nint pad);

    /// <summary>The <c>gst_pad_is_linked</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_is_linked")]
    private static partial int GstPadIsLinked(nint pad);

    /// <summary>The <c>gst_pad_iterate_internal_links</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_iterate_internal_links")]
    private static partial nint GstPadIterateInternalLinks(nint pad);

    /// <summary>The <c>gst_pad_iterate_internal_links_default</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_iterate_internal_links_default")]
    private static partial nint GstPadIterateInternalLinksDefault(nint pad, nint parent);

    /// <summary>The <c>gst_pad_link</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_link")]
    private static partial int GstPadLink(nint srcpad, nint sinkpad);

    /// <summary>The <c>gst_pad_link_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_link_full")]
    private static partial int GstPadLinkFull(nint srcpad, nint sinkpad, int flags);

    /// <summary>The <c>gst_pad_link_maybe_ghosting</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_link_maybe_ghosting")]
    private static partial int GstPadLinkMaybeGhosting(nint src, nint sink);

    /// <summary>The <c>gst_pad_link_maybe_ghosting_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_link_maybe_ghosting_full")]
    private static partial int GstPadLinkMaybeGhostingFull(nint src, nint sink, int flags);

    /// <summary>The <c>gst_pad_mark_reconfigure</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_mark_reconfigure")]
    private static partial void GstPadMarkReconfigure(nint pad);

    /// <summary>The <c>gst_pad_needs_reconfigure</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_needs_reconfigure")]
    private static partial int GstPadNeedsReconfigure(nint pad);

    /// <summary>The <c>gst_pad_pause_task</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_pause_task")]
    private static partial int GstPadPauseTask(nint pad);

    /// <summary>The <c>gst_pad_peer_query</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_peer_query")]
    private static partial int GstPadPeerQuery(nint pad, nint query);

    /// <summary>The <c>gst_pad_peer_query_accept_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_peer_query_accept_caps")]
    private static partial int GstPadPeerQueryAcceptCaps(nint pad, nint caps);

    /// <summary>The <c>gst_pad_peer_query_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_peer_query_caps")]
    private static partial nint GstPadPeerQueryCaps(nint pad, nint filter);

    /// <summary>The <c>gst_pad_peer_query_convert</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_peer_query_convert")]
    private static partial int GstPadPeerQueryConvert(nint pad, int srcFormat, long srcVal, int destFormat, long* destVal);

    /// <summary>The <c>gst_pad_peer_query_duration</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_peer_query_duration")]
    private static partial int GstPadPeerQueryDuration(nint pad, int format, long* duration);

    /// <summary>The <c>gst_pad_peer_query_position</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_peer_query_position")]
    private static partial int GstPadPeerQueryPosition(nint pad, int format, long* cur);

    /// <summary>The <c>gst_pad_proxy_query_accept_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_proxy_query_accept_caps")]
    private static partial int GstPadProxyQueryAcceptCaps(nint pad, nint query);

    /// <summary>The <c>gst_pad_proxy_query_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_proxy_query_caps")]
    private static partial int GstPadProxyQueryCaps(nint pad, nint query);

    /// <summary>The <c>gst_pad_pull_range</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_pull_range")]
    private static partial int GstPadPullRange(nint pad, ulong offset, uint size, nint* buffer);

    /// <summary>The <c>gst_pad_push</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_push")]
    private static partial int GstPadPush(nint pad, nint buffer);

    /// <summary>The <c>gst_pad_push_list</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_push_list")]
    private static partial int GstPadPushList(nint pad, nint list);

    /// <summary>The <c>gst_pad_query</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_query")]
    private static partial int GstPadQuery(nint pad, nint query);

    /// <summary>The <c>gst_pad_query_accept_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_query_accept_caps")]
    private static partial int GstPadQueryAcceptCaps(nint pad, nint caps);

    /// <summary>The <c>gst_pad_query_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_query_caps")]
    private static partial nint GstPadQueryCaps(nint pad, nint filter);

    /// <summary>The <c>gst_pad_query_convert</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_query_convert")]
    private static partial int GstPadQueryConvert(nint pad, int srcFormat, long srcVal, int destFormat, long* destVal);

    /// <summary>The <c>gst_pad_query_default</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_query_default")]
    private static partial int GstPadQueryDefault(nint pad, nint parent, nint query);

    /// <summary>The <c>gst_pad_query_duration</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_query_duration")]
    private static partial int GstPadQueryDuration(nint pad, int format, long* duration);

    /// <summary>The <c>gst_pad_query_position</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_query_position")]
    private static partial int GstPadQueryPosition(nint pad, int format, long* cur);

    /// <summary>The <c>gst_pad_remove_probe</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_remove_probe")]
    private static partial void GstPadRemoveProbe(nint pad, System.Runtime.InteropServices.CULong id);

    /// <summary>The <c>gst_pad_set_activate_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_activate_function_full")]
    private static partial void GstPadSetActivateFunctionFull(nint pad, nint activate, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_activatemode_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_activatemode_function_full")]
    private static partial void GstPadSetActivatemodeFunctionFull(nint pad, nint activatemode, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_active</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_active")]
    private static partial int GstPadSetActive(nint pad, int active);

    /// <summary>The <c>gst_pad_set_chain_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_chain_function_full")]
    private static partial void GstPadSetChainFunctionFull(nint pad, nint chain, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_chain_list_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_chain_list_function_full")]
    private static partial void GstPadSetChainListFunctionFull(nint pad, nint chainlist, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_element_private</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_element_private")]
    private static partial void GstPadSetElementPrivate(nint pad, nint priv);

    /// <summary>The <c>gst_pad_set_event_full_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_event_full_function_full")]
    private static partial void GstPadSetEventFullFunctionFull(nint pad, nint @event, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_event_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_event_function_full")]
    private static partial void GstPadSetEventFunctionFull(nint pad, nint @event, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_getrange_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_getrange_function_full")]
    private static partial void GstPadSetGetrangeFunctionFull(nint pad, nint get, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_iterate_internal_links_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_iterate_internal_links_function_full")]
    private static partial void GstPadSetIterateInternalLinksFunctionFull(nint pad, nint iterintlink, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_link_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_link_function_full")]
    private static partial void GstPadSetLinkFunctionFull(nint pad, nint link, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_offset</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_offset")]
    private static partial void GstPadSetOffset(nint pad, long offset);

    /// <summary>The <c>gst_pad_set_query_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_query_function_full")]
    private static partial void GstPadSetQueryFunctionFull(nint pad, nint query, nint userData, nint notify);

    /// <summary>The <c>gst_pad_set_unlink_function_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_set_unlink_function_full")]
    private static partial void GstPadSetUnlinkFunctionFull(nint pad, nint unlink, nint userData, nint notify);

    /// <summary>The <c>gst_pad_start_task</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_start_task")]
    private static partial int GstPadStartTask(nint pad, nint func, nint userData, nint notify);

    /// <summary>The <c>gst_pad_stop_task</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_stop_task")]
    private static partial int GstPadStopTask(nint pad);

    /// <summary>The <c>gst_pad_store_sticky_event</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_store_sticky_event")]
    private static partial int GstPadStoreStickyEvent(nint pad, nint @event);

    /// <summary>The <c>gst_pad_unlink</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_unlink")]
    private static partial int GstPadUnlink(nint srcpad, nint sinkpad);

    /// <summary>The <c>gst_pad_use_fixed_caps</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_use_fixed_caps")]
    private static partial void GstPadUseFixedCaps(nint pad);

    /// <summary>The <c>gst_pad_link_get_name</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pad_link_get_name")]
    private static partial nint GstPadLinkGetName(int ret);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstPad</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_pad_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Pad(handle, transfer);
}
