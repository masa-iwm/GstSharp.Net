// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;

namespace Gst;

/// <content>The subclassing surface of <c>GstDeviceProvider</c>.</content>
public unsafe partial class DeviceProvider
{
    /// <summary>
    /// Constructs the managed subclass that
    /// <see cref="Gst.GObject.SubclassType.NewInstance()"/> created an instance of.
    /// </summary>
    /// <param name="args">The instance, as the registration handed it out.</param>
    /// <exception cref="System.ArgumentException">The instance is not a <c>GstDeviceProvider</c>.</exception>
    protected DeviceProvider(Gst.GObject.SubclassCtorArgs args)
        : this(args.HandleFor(GetGType()), args.Transfer)
    {
    }

    /// <summary>
    /// Gets the declaration of <c>GstDeviceProvider.start</c>, for a subclass that
    /// overrides <see cref="OnStart"/>.
    /// </summary>
    public static Gst.GObject.VfuncOverride StartOverride { get; } = new(
        &GetGType,
        Gst.DeviceProviderClassRaw.StartOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, int>)&StartTrampoline);

    /// <summary>
    /// Gets the declaration of <c>GstDeviceProvider.stop</c>, for a subclass that
    /// overrides <see cref="OnStop"/>.
    /// </summary>
    public static Gst.GObject.VfuncOverride StopOverride { get; } = new(
        &GetGType,
        Gst.DeviceProviderClassRaw.StopOffset,
        (nint)(delegate* unmanaged[Cdecl]<nint, void>)&StopTrampoline);

    /// <summary>Registers a managed subclass of <c>GstDeviceProvider</c> with GObject.</summary>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GstDeviceProvider</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static Gst.GObject.SubclassType DefineSubclass(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        params Gst.GObject.VfuncOverride[] overrides) =>
        DefineSubclassCore(typeName, configureClass, overrides, null, null);

    /// <summary>Registers a managed subclass of <c>GstDeviceProvider</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, or a declared slot belongs to a
    /// class that <c>GstDeviceProvider</c> does not derive from.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : DeviceProvider, Gst.GObject.IManagedSubclass<TSelf> =>
        DefineSubclassCore(typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), null);

    /// <summary>Registers a managed subclass of <c>GstDeviceProvider</c> with GObject.</summary>
    /// <typeparam name="TSelf">
    /// The subclass itself, which states how its wrapper is built.
    /// </typeparam>
    /// <param name="typeName">The <c>GType</c> name, unique in the process.</param>
    /// <param name="configureClass">
    /// Describes the class while it is being initialised.
    /// </param>
    /// <param name="options">
    /// The optional parts of the registration, the interfaces the subclass
    /// implements above all. Interfaces can be declared here and nowhere else:
    /// GObject refuses to attach one once the class of the type is being
    /// initialised.
    /// </param>
    /// <param name="overrides">The slots the subclass takes over.</param>
    /// <returns>The registration.</returns>
    /// <remarks>
    /// An instance of the registered type that native code creates - one an element
    /// factory made, a pad a base class built from a template - is wrapped as
    /// <typeparamref name="TSelf"/> through
    /// <see cref="Gst.GObject.IManagedSubclass{TSelf}.CreateWrapper"/>, so the overrides
    /// of the subclass run for it. The non generic overload registers no such factory
    /// and its instances arrive as the nearest wrapped ancestor.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException">An argument is <see langword="null"/>.</exception>
    /// <exception cref="System.ArgumentException">
    /// The type name is not a legal <c>GType</c> name, a declared slot belongs to a
    /// class that <c>GstDeviceProvider</c> does not derive from, an interface is declared
    /// twice, or the parent type implements it already.
    /// </exception>
    /// <exception cref="System.InvalidOperationException">
    /// The type name is taken, or the class initialiser failed.
    /// </exception>
    public static Gst.GObject.SubclassType DefineSubclass<TSelf>(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        Gst.GObject.SubclassOptions options,
        params Gst.GObject.VfuncOverride[] overrides)
        where TSelf : DeviceProvider, Gst.GObject.IManagedSubclass<TSelf>
    {
        ArgumentNullException.ThrowIfNull(options);

        return DefineSubclassCore(
            typeName, configureClass, overrides, static args => TSelf.CreateWrapper(args), options);
    }

    private static Gst.GObject.SubclassType DefineSubclassCore(
        string typeName,
        Action<Gst.GObject.ObjectClassConfig>? configureClass,
        Gst.GObject.VfuncOverride[] overrides,
        Func<Gst.GObject.SubclassCtorArgs, Gst.GObject.Object>? wrapFactory,
        Gst.GObject.SubclassOptions? options)
    {
        Gst.GObject.SubclassType type = Gst.GObject.SubclassType.Define(
            new Gst.GObject.GType(GetGType()), typeName, configureClass, overrides, wrapFactory, options);
        return type;
    }

    /// <summary>
    /// Starts providering the devices. This will cause #GST_MESSAGE_DEVICE_ADDED
    /// and #GST_MESSAGE_DEVICE_REMOVED messages to be posted on the provider's bus
    /// when devices are added or removed from the system.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Since the #GstDeviceProvider is a singleton,
    /// gst_device_provider_start() may already have been called by another
    /// user of the object, gst_device_provider_stop() needs to be called the same
    /// number of times.
    /// </para>
    /// <para>
    /// After this function has been called, gst_device_provider_get_devices() will
    /// return the same objects that have been received from the
    /// #GST_MESSAGE_DEVICE_ADDED messages and will no longer probe.
    /// </para>
    /// <para>Declare this slot on every managed device provider, even one that has nothing to start.
    /// No class in the chain installs a start of its own - gstdeviceprovider.h:169-184 declares
    /// the slot and the base class leaves it NULL - so a chain-up reaches nothing and answers
    /// true, meaning nothing below refuses to start. The slot is also what keeps the provider
    /// safe: gst_device_provider_start calls klass-&gt;probe with no NULL check when
    /// klass-&gt;start is NULL (gstdeviceprovider.c:476-481), and a managed provider never
    /// installs probe, which is introspectable=0 and carries no managed surface, so a provider
    /// that declares stop alone ends a Start call in a call through a NULL function pointer
    /// rather than in an exception. Answer false to refuse to start; there is no error channel,
    /// the C slot is a plain gboolean.</para>
    /// </remarks>
    /// <returns>%TRUE if the device providering could be started</returns>
    protected virtual bool OnStart() =>
        ChainUpStart();

    /// <summary>
    /// Decreases the use-count by one. If the use count reaches zero, this
    /// #GstDeviceProvider will stop providering the devices. This needs to be
    /// called the same number of times that gst_device_provider_start() was called.
    /// </summary>
    /// <remarks>
    /// <para>Declaring this slot alone is unsafe: it leaves start NULL, which sends
    /// gst_device_provider_start into its unguarded klass-&gt;probe call
    /// (gstdeviceprovider.c:476-481) and crashes the process. Declare the start slot beside it.
    /// The dispatch of this one is guarded (gstdeviceprovider.c:522-536), so a provider that
    /// declares start alone is fine.</para>
    /// </remarks>
    protected virtual void OnStop() =>
        ChainUpStop();

    /// <summary>Runs the implementation of <c>start</c> below the managed override.</summary>
    /// <remarks>
    /// <para>Declare this slot on every managed device provider, even one that has nothing to start.
    /// No class in the chain installs a start of its own - gstdeviceprovider.h:169-184 declares
    /// the slot and the base class leaves it NULL - so a chain-up reaches nothing and answers
    /// true, meaning nothing below refuses to start. The slot is also what keeps the provider
    /// safe: gst_device_provider_start calls klass-&gt;probe with no NULL check when
    /// klass-&gt;start is NULL (gstdeviceprovider.c:476-481), and a managed provider never
    /// installs probe, which is introspectable=0 and carries no managed surface, so a provider
    /// that declares stop alone ends a Start call in a call through a NULL function pointer
    /// rather than in an exception. Answer false to refuse to start; there is no error channel,
    /// the C slot is a plain gboolean.</para>
    /// </remarks>
    /// <returns>%TRUE if the device providering could be started</returns>
    protected bool ChainUpStart()
    {
        bool result = ChainUpStart(Handle);
        GC.KeepAlive(this);
        return result;
    }

    /// <summary>Runs the implementation of <c>stop</c> below the managed override.</summary>
    /// <remarks>
    /// <para>Declaring this slot alone is unsafe: it leaves start NULL, which sends
    /// gst_device_provider_start into its unguarded klass-&gt;probe call
    /// (gstdeviceprovider.c:476-481) and crashes the process. Declare the start slot beside it.
    /// The dispatch of this one is guarded (gstdeviceprovider.c:522-536), so a provider that
    /// declares start alone is fine.</para>
    /// </remarks>
    protected void ChainUpStop()
    {
        ChainUpStop(Handle);
        GC.KeepAlive(this);
    }

    private static bool ChainUpStart(nint provider)
    {
        delegate* unmanaged[Cdecl]<nint, int> slot =
            (delegate* unmanaged[Cdecl]<nint, int>)ParentClassOf(provider)->Start;

        if (slot is null)
        {
            return true;
        }

        return slot(provider) != 0;
    }

    private static void ChainUpStop(nint provider)
    {
        delegate* unmanaged[Cdecl]<nint, void> slot =
            (delegate* unmanaged[Cdecl]<nint, void>)ParentClassOf(provider)->Stop;

        if (slot is null)
        {
            return;
        }

        slot(provider);
    }

    /// <summary>
    /// Returns the class the registration captured, which is the one an override
    /// chains up through.
    /// </summary>
    /// <param name="instance">The native <c>GstDeviceProvider</c>.</param>
    /// <returns>The parent class of the managed subclass.</returns>
    private static Gst.DeviceProviderClassRaw* ParentClassOf(nint instance) =>
        (Gst.DeviceProviderClassRaw*)Gst.GObject.SubclassRegistry.DescriptorFor(instance).ParentClass;

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static int StartTrampoline(nint provider)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(provider) is not DeviceProvider managed)
            {
                return (ChainUpStart(provider)) ? 1 : 0;
            }

            return (managed.OnStart()) ? 1 : 0;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    [UnmanagedCallersOnly(CallConvs = new[] { typeof(CallConvCdecl) })]
    private static void StopTrampoline(nint provider)
    {
        try
        {
            if (Gst.GObject.Object.TryGetOrFabricate(provider) is not DeviceProvider managed)
            {
                ChainUpStop(provider);
                return;
            }

            managed.OnStop();
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }
}
