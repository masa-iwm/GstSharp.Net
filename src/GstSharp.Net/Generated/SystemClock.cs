// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// The GStreamer core provides a GstSystemClock based on the system time.
/// Asynchronous callbacks are scheduled from an internal thread.
/// </summary>
/// <remarks>
/// <para>
/// Clock implementors are encouraged to subclass this systemclock as it
/// implements the async notification.
/// </para>
/// <para>
/// Subclasses can however override all of the important methods for sync and
/// async notifications to implement their own callback methods or blocking
/// wait operations.
/// </para>
/// </remarks>
public unsafe partial class SystemClock : Gst.Clock
{
    /// <summary>Wraps a native <c>GstSystemClock</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstSystemClock</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected SystemClock(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Get a handle to the default system clock. The refcount of the
    /// clock will be increased so you need to unref the clock after
    /// usage.
    /// </summary>
    /// <returns>the default clock.</returns>
    public static Gst.Clock Obtain()
    {
        nint nativeResult = GstSystemClockObtain();
        return Gst.GObject.Object.FromNative<Gst.Clock>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_system_clock_obtain returned no value.");
    }

    /// <summary>
    /// Sets the default system clock that can be obtained with
    /// gst_system_clock_obtain().
    /// </summary>
    /// <remarks>
    /// <para>
    /// This is mostly used for testing and debugging purposes when you
    /// want to have control over the time reported by the default system
    /// clock.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="newClock">The <c>newClock</c> argument.</param>
    public static void SetDefault(Gst.Clock? newClock)
    {
        GstSystemClockSetDefault(newClock is null ? 0 : newClock.Handle);
        System.GC.KeepAlive(newClock);
    }

    /// <summary>The <c>clock-type</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public Gst.ClockType ClockType
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("clock-type");
            return (Gst.ClockType)holder.GetEnum();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("clock-type");
            holder.SetEnum((int)value);
            SetPropertyValue("clock-type", in holder);
        }
    }

    /// <summary>The <c>gst_system_clock_obtain</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_system_clock_obtain")]
    private static partial nint GstSystemClockObtain();

    /// <summary>The <c>gst_system_clock_set_default</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_system_clock_set_default")]
    private static partial void GstSystemClockSetDefault(nint newClock);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstSystemClock</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_system_clock_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new SystemClock(handle, transfer);
}
