// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>A collection of #GstStream that are available.</summary>
/// <remarks>
/// <para>
/// A #GstStreamCollection will be provided by elements that can make those
/// streams available. Applications can use the collection to show the user
/// what streams are available by using %gst_stream_collection_get_stream()
/// </para>
/// <para>
/// Once posted, a #GstStreamCollection is immutable. Updates are made by sending
/// a new #GstStreamCollection message, which may or may not share some of
/// the #GstStream objects from the collection it replaces. The receiver can check
/// the sender of a stream collection message to know which collection is
/// obsoleted.
/// </para>
/// <para>Several elements in a pipeline can provide #GstStreamCollection.</para>
/// <para>
/// Applications can activate streams from a collection by using the
/// #GST_EVENT_SELECT_STREAMS event on a pipeline, bin or element.
/// </para>
/// </remarks>
public unsafe partial class StreamCollection : Gst.Object
{
    /// <summary>Wraps a native <c>GstStreamCollection</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstStreamCollection</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected StreamCollection(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Create a new #GstStreamCollection.</summary>
    /// <param name="upstreamId">The <c>upstreamId</c> argument.</param>
    /// <returns>The new #GstStreamCollection.</returns>
    public static Gst.StreamCollection New(string? upstreamId)
    {
        System.Span<byte> upstreamIdBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope upstreamIdScope = Gst.Interop.GMarshal.StackUtf8(upstreamId, upstreamIdBuffer);
        nint nativeResult = GstStreamCollectionNew(upstreamIdScope.Pointer);
        return Gst.GObject.Object.FromNative<Gst.StreamCollection>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_stream_collection_new returned no value.");
    }

    /// <summary>Get the number of streams this collection contains</summary>
    /// <returns>The number of streams that @collection contains</returns>
    public uint GetSize()
    {
        uint nativeResult = GstStreamCollectionGetSize(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Retrieve the #GstStream with index @index from the collection.</summary>
    /// <remarks>
    /// <para>The caller should not modify the returned #GstStream</para>
    /// </remarks>
    /// <param name="index">The <c>index</c> argument.</param>
    /// <returns>A #GstStream</returns>
    public Gst.Stream? GetStream(uint index)
    {
        nint nativeResult = GstStreamCollectionGetStream(Handle, index);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Stream>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Returns the upstream id of the @collection.</summary>
    /// <returns>The upstream id</returns>
    public string? GetUpstreamId()
    {
        nint nativeResult = GstStreamCollectionGetUpstreamId(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>stream-id</summary>
    public string? UpstreamId => GetUpstreamId();

    /// <summary>The arguments of the <c>stream-notify</c> signal of <c>GstStreamCollection</c>.</summary>
    public sealed class StreamNotifySignalArgs
    {
        /// <summary>Initializes a new instance of the <see cref="StreamNotifySignalArgs"/> class.</summary>
        /// <param name="propStream">the #GstStream that originated the signal</param>
        /// <param name="prop">the property that changed</param>
        internal StreamNotifySignalArgs(Gst.Stream propStream, Gst.GObject.ParamSpec prop)
        {
            PropStream = propStream;
            Prop = prop;
        }

        /// <summary>the #GstStream that originated the signal</summary>
        public Gst.Stream PropStream { get; }

        /// <summary>the property that changed</summary>
        /// <remarks>
        /// The value is only valid while the handler runs: the wrapper is disposed
        /// once it returns. Read out of it what is needed, or copy it where the
        /// type offers a copy.
        /// </remarks>
        public Gst.GObject.ParamSpec Prop { get; }
    }

    /// <summary>
    /// The stream notify signal is used to be notified of property changes to
    /// streams within the collection.
    /// </summary>
    /// <remarks>
    /// The signal is detailed. The handler is connected to <c>stream-notify</c>
    /// without a detail, so it runs for every detail of the signal.
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.StreamCollection.StreamNotifySignalArgs> StreamNotify
    {
        add => Gst.SignalConnections.Add(this, "stream-notify", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&StreamNotifyTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "stream-notify", value);
    }

    /// <summary>The native handler of the <c>stream-notify</c> signal of <c>GstStreamCollection</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void StreamNotifyTrampoline(nint instance, nint propStream, nint prop, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.StreamCollection.StreamNotifySignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Stream propStreamValue = Gst.GObject.Object.FromNative<Gst.Stream>(propStream, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The stream-notify signal of GstStreamCollection passed no prop_stream.");
            using Gst.GObject.ParamSpec propValue = new Gst.GObject.ParamSpec(prop, Gst.Interop.Transfer.None);
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.StreamCollection.StreamNotifySignalArgs(propStreamValue, propValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_stream_collection_new</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_stream_collection_new")]
    private static partial nint GstStreamCollectionNew(byte* upstreamId);

    /// <summary>The <c>gst_stream_collection_get_size</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_stream_collection_get_size")]
    private static partial uint GstStreamCollectionGetSize(nint collection);

    /// <summary>The <c>gst_stream_collection_get_stream</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_stream_collection_get_stream")]
    private static partial nint GstStreamCollectionGetStream(nint collection, uint index);

    /// <summary>The <c>gst_stream_collection_get_upstream_id</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_stream_collection_get_upstream_id")]
    private static partial nint GstStreamCollectionGetUpstreamId(nint collection);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstStreamCollection</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_stream_collection_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new StreamCollection(handle, transfer);
}
