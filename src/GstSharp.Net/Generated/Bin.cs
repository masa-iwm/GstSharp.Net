// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// #GstBin is an element that can contain other #GstElement, allowing them to be
/// managed as a group.
/// Pads from the child elements can be ghosted to the bin, see #GstGhostPad.
/// This makes the bin look like any other elements and enables creation of
/// higher-level abstraction elements.
/// </summary>
/// <remarks>
/// <para>
/// A new #GstBin is created with gst_bin_new(). Use a #GstPipeline instead if you
/// want to create a toplevel bin because a normal bin doesn't have a bus or
/// handle clock distribution of its own.
/// </para>
/// <para>
/// After the bin has been created you will typically add elements to it with
/// gst_bin_add(). You can remove elements with gst_bin_remove().
/// </para>
/// <para>
/// An element can be retrieved from a bin with gst_bin_get_by_name(), using the
/// elements name. gst_bin_get_by_name_recurse_up() is mainly used for internal
/// purposes and will query the parent bins when the element is not found in the
/// current bin.
/// </para>
/// <para>
/// An iterator of elements in a bin can be retrieved with
/// gst_bin_iterate_elements(). Various other iterators exist to retrieve the
/// elements in a bin.
/// </para>
/// <para>gst_object_unref() is used to drop your reference to the bin.</para>
/// <para>
/// The #GstBin::element-added signal is fired whenever a new element is added to
/// the bin. Likewise the #GstBin::element-removed signal is fired whenever an
/// element is removed from the bin.
/// </para>
/// <para>
/// A #GstBin internally intercepts every #GstMessage posted by its children and
/// implements the following default behaviour for each of them:
/// </para>
/// <para>
/// * %GST_MESSAGE_EOS: This message is only posted by sinks in the PLAYING
/// state. If all sinks posted the EOS message, this bin will post and EOS
/// message upwards.
/// </para>
/// <para>
/// * %GST_MESSAGE_SEGMENT_START: Just collected and never forwarded upwards.
///   The messages are used to decide when all elements have completed playback
///   of their segment.
/// </para>
/// <para>
/// * %GST_MESSAGE_SEGMENT_DONE: Is posted by #GstBin when all elements that posted
///   a SEGMENT_START have posted a SEGMENT_DONE.
/// </para>
/// <para>
/// * %GST_MESSAGE_DURATION_CHANGED: Is posted by an element that detected a change
///   in the stream duration. The duration change is posted to the
///   application so that it can refetch the new duration with a duration
///   query.
/// </para>
/// <para>
///   Note that these messages can be posted before the bin is prerolled, in which
///   case the duration query might fail.
/// </para>
/// <para>
///   Note also that there might be a discrepancy (due to internal buffering/queueing)
///   between the stream being currently displayed and the returned duration query.
/// </para>
/// <para>
///   Applications might want to also query for duration (and changes) by
///   listening to the %GST_MESSAGE_STREAM_START message, signaling the active start
///   of a (new) stream.
/// </para>
/// <para>
/// * %GST_MESSAGE_CLOCK_LOST: This message is posted by an element when it
///   can no longer provide a clock.
/// </para>
/// <para>
///   The default bin behaviour is to check if the lost clock was the one provided
///   by the bin. If so and the bin is currently in the PLAYING state, the message
///   is forwarded to the bin parent.
/// </para>
/// <para>
///   This message is also generated when a clock provider is removed from
///   the bin. If this message is received by the application, it should
///   PAUSE the pipeline and set it back to PLAYING to force a new clock
///   distribution.
/// </para>
/// <para>
/// * %GST_MESSAGE_CLOCK_PROVIDE: This message is generated when an element
///   can provide a clock. This mostly happens when a new clock
///   provider is added to the bin.
/// </para>
/// <para>
///   The default behaviour of the bin is to mark the currently selected clock as
///   dirty, which will perform a clock recalculation the next time the bin is
///   asked to provide a clock.
/// </para>
/// <para>
///   This message is never sent to the application but is forwarded to
///   the parent of the bin.
/// </para>
/// <para>* OTHERS: posted upwards.</para>
/// <para>
/// A #GstBin implements the following default behaviour for answering to a
/// #GstQuery:
/// </para>
/// <para>
/// * %GST_QUERY_DURATION: The bin will forward the query to all sink
///   elements contained within and will return the maximum value.
///   If no sinks are available in the bin, the query fails.
/// </para>
/// <para>
/// * %GST_QUERY_POSITION: The query is sent to all sink elements in the bin and the
///   MAXIMUM of all values is returned. If no sinks are available in the bin,
///   the query fails.
/// </para>
/// <para>
/// * OTHERS: the query is forwarded to all sink elements, the result
///   of the first sink that answers the query successfully is returned. If no
///   sink is in the bin, the query fails.
/// </para>
/// <para>
/// A #GstBin will by default forward any event sent to it to all sink
/// ( %GST_EVENT_TYPE_UPSTREAM ) or source ( %GST_EVENT_TYPE_DOWNSTREAM ) elements
/// depending on the event type.
/// </para>
/// <para>
/// If all the elements return %TRUE, the bin will also return %TRUE, else %FALSE
/// is returned. If no elements of the required type are in the bin, the event
/// handler will return %TRUE.
/// </para>
/// </remarks>
public unsafe partial class Bin : Gst.Element, Gst.IChildProxy
{
    /// <summary>Wraps a native <c>GstBin</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstBin</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Bin(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Creates a new bin with the given name.</summary>
    /// <param name="name">the name of the new bin</param>
    /// <returns>a new #GstBin</returns>
    public static Gst.Bin New(string? name)
    {
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstBinNew(nameScope.Pointer);
        return Gst.GObject.Object.FromNative<Gst.Bin>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_bin_new returned no value.");
    }

    /// <summary>
    /// Adds the given element to the bin.  Sets the element's parent, and thus
    /// takes ownership of the element. An element can only be added to one bin.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If the element's pads are linked to other pads, the pads will be unlinked
    /// before the element is added to the bin.
    /// </para>
    /// <para>
    /// &gt; When you add an element to an already-running pipeline, you will have to
    /// &gt; take care to set the state of the newly-added element to the desired
    /// &gt; state (usually PLAYING or PAUSED, same you set the pipeline to originally)
    /// &gt; with gst_element_set_state(), or use gst_element_sync_state_with_parent().
    /// &gt; The bin or pipeline will not take care of this for you.
    /// </para>
    /// </remarks>
    /// <param name="element">the #GstElement to add</param>
    /// <returns>
    /// %TRUE if the element could be added, %FALSE if
    /// the bin does not want to accept the element.
    /// </returns>
    public bool Add(Gst.Element element)
    {
        ArgumentNullException.ThrowIfNull(element);
        int nativeResult = GstBinAdd(Handle, element.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(element);
        return nativeResult != 0;
    }

    /// <summary>
    /// Recursively looks for elements with an unlinked pad of the given
    /// direction within the specified bin and returns an unlinked pad
    /// if one is found, or %NULL otherwise. If a pad is found, the caller
    /// owns a reference to it and should use gst_object_unref() on the
    /// pad when it is not needed any longer.
    /// </summary>
    /// <param name="direction">whether to look for an unlinked source or sink pad</param>
    /// <returns>
    /// unlinked pad of the given
    /// direction.
    /// </returns>
    public Gst.Pad? FindUnlinkedPad(Gst.PadDirection direction)
    {
        nint nativeResult = GstBinFindUnlinkedPad(Handle, (int)direction);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Pad>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Looks for an element inside the bin that implements the given
    /// interface. If such an element is found, it returns the element.
    /// You can cast this element to the given interface afterwards.  If you want
    /// all elements that implement the interface, use
    /// gst_bin_iterate_all_by_interface(). This function recurses into child bins.
    /// </summary>
    /// <param name="iface">the #GType of an interface</param>
    /// <returns>
    /// A #GstElement inside the bin
    /// implementing the interface
    /// </returns>
    public Gst.Element? GetByInterface(Gst.GObject.GType iface)
    {
        nint nativeResult = GstBinGetByInterface(Handle, iface.Value);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Element>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets the element with the given name from a bin. This
    /// function recurses into child bins.
    /// </summary>
    /// <param name="name">the element name to search for</param>
    /// <returns>
    /// the #GstElement with the given
    /// name
    /// </returns>
    public Gst.Element? GetByName(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstBinGetByName(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Element>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Gets the element with the given name from this bin. If the
    /// element is not found, a recursion is performed on the parent bin.
    /// </summary>
    /// <param name="name">the element name to search for</param>
    /// <returns>
    /// the #GstElement with the given
    /// name
    /// </returns>
    public Gst.Element? GetByNameRecurseUp(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstBinGetByNameRecurseUp(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Element>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The <c>gst_bin_get_suppressed_flags</c> function.</summary>
    /// <returns>the bin's suppressed #GstElementFlags.</returns>
    public Gst.ElementFlags GetSuppressedFlags()
    {
        int nativeResult = GstBinGetSuppressedFlags(Handle);
        System.GC.KeepAlive(this);
        return (Gst.ElementFlags)nativeResult;
    }

    /// <summary>
    /// Looks for all elements inside the bin with the given element factory name.
    /// The function recurses inside child bins. The iterator will yield a series of
    /// #GstElement.
    /// </summary>
    /// <param name="factoryName">the name of the #GstElementFactory</param>
    /// <returns>
    /// a #GstIterator of #GstElement
    ///     for all elements in the bin with the given element factory name
    /// </returns>
    public Gst.Iterator IterateAllByElementFactoryName(string factoryName)
    {
        ArgumentNullException.ThrowIfNull(factoryName);
        System.Span<byte> factoryNameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope factoryNameScope = Gst.Interop.GMarshal.StackUtf8(factoryName, factoryNameBuffer);
        nint nativeResult = GstBinIterateAllByElementFactoryName(Handle, factoryNameScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bin_iterate_all_by_element_factory_name returned no value.");
    }

    /// <summary>
    /// Looks for all elements inside the bin that implements the given
    /// interface. You can safely cast all returned elements to the given interface.
    /// The function recurses inside child bins. The iterator will yield a series
    /// of #GstElement.
    /// </summary>
    /// <param name="iface">the #GType of an interface</param>
    /// <returns>
    /// a #GstIterator of #GstElement
    ///     for all elements in the bin implementing the given interface
    /// </returns>
    public Gst.Iterator IterateAllByInterface(Gst.GObject.GType iface)
    {
        nint nativeResult = GstBinIterateAllByInterface(Handle, iface.Value);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bin_iterate_all_by_interface returned no value.");
    }

    /// <summary>Gets an iterator for the elements in this bin.</summary>
    /// <returns>a #GstIterator of #GstElement</returns>
    public Gst.Iterator IterateElements()
    {
        nint nativeResult = GstBinIterateElements(Handle);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bin_iterate_elements returned no value.");
    }

    /// <summary>
    /// Gets an iterator for the elements in this bin.
    /// This iterator recurses into GstBin children.
    /// </summary>
    /// <returns>a #GstIterator of #GstElement</returns>
    public Gst.Iterator IterateRecurse()
    {
        nint nativeResult = GstBinIterateRecurse(Handle);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bin_iterate_recurse returned no value.");
    }

    /// <summary>
    /// Gets an iterator for all elements in the bin that have the
    /// #GST_ELEMENT_FLAG_SINK flag set.
    /// </summary>
    /// <returns>a #GstIterator of #GstElement</returns>
    public Gst.Iterator IterateSinks()
    {
        nint nativeResult = GstBinIterateSinks(Handle);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bin_iterate_sinks returned no value.");
    }

    /// <summary>
    /// Gets an iterator for the elements in this bin in topologically
    /// sorted order. This means that the elements are returned from
    /// the most downstream elements (sinks) to the sources.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function is used internally to perform the state changes
    /// of the bin elements and for clock selection.
    /// </para>
    /// </remarks>
    /// <returns>a #GstIterator of #GstElement</returns>
    public Gst.Iterator IterateSorted()
    {
        nint nativeResult = GstBinIterateSorted(Handle);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bin_iterate_sorted returned no value.");
    }

    /// <summary>
    /// Gets an iterator for all elements in the bin that have the
    /// #GST_ELEMENT_FLAG_SOURCE flag set.
    /// </summary>
    /// <returns>a #GstIterator of #GstElement</returns>
    public Gst.Iterator IterateSources()
    {
        nint nativeResult = GstBinIterateSources(Handle);
        System.GC.KeepAlive(this);
        return Gst.Iterator.FromNative(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_bin_iterate_sources returned no value.");
    }

    /// <summary>
    /// Queries @bin for the current latency and reconfigures this latency on all the
    /// elements using a LATENCY event.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This method is typically called on the pipeline when a #GST_MESSAGE_LATENCY
    /// is posted on the bus.
    /// </para>
    /// <para>
    /// This function simply emits the #GstBin::do-latency signal so any custom latency
    /// calculations will be performed.
    /// </para>
    /// </remarks>
    /// <returns>%TRUE if the latency could be queried and reconfigured.</returns>
    public bool RecalculateLatency()
    {
        int nativeResult = GstBinRecalculateLatency(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Removes the element from the bin, unparenting it as well.
    /// Unparenting the element means that the element will be dereferenced,
    /// so if the bin holds the only reference to the element, the element
    /// will be freed in the process of removing it from the bin.  If you
    /// want the element to still exist after removing, you need to call
    /// gst_object_ref() before removing it from the bin.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If the element's pads are linked to other pads, the pads will be unlinked
    /// before the element is removed from the bin.
    /// </para>
    /// </remarks>
    /// <param name="element">the #GstElement to remove</param>
    /// <returns>
    /// %TRUE if the element could be removed, %FALSE if
    /// the bin does not want to remove the element.
    /// </returns>
    public bool Remove(Gst.Element element)
    {
        ArgumentNullException.ThrowIfNull(element);
        int nativeResult = GstBinRemove(Handle, element.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(element);
        return nativeResult != 0;
    }

    /// <summary>
    /// Suppresses the given flags on the bin. #GstElementFlags of a
    /// child element are propagated when it is added to the bin.
    /// When suppressed flags are set, those specified flags will
    /// not be propagated to the bin.
    /// </summary>
    /// <param name="flags">the #GstElementFlags to suppress</param>
    public void SetSuppressedFlags(Gst.ElementFlags flags)
    {
        GstBinSetSuppressedFlags(Handle, (int)flags);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Synchronizes the state of every child of @bin with the state
    /// of @bin. See also gst_element_sync_state_with_parent().
    /// </summary>
    /// <returns>
    /// %TRUE if syncing the state was successful for all children,
    ///  otherwise %FALSE.
    /// </returns>
    public bool SyncChildrenStates()
    {
        int nativeResult = GstBinSyncChildrenStates(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// If set to %TRUE, the bin will handle asynchronous state changes.
    /// This should be used only if the bin subclass is modifying the state
    /// of its children on its own.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool AsyncHandling
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("async-handling");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("async-handling");
            holder.SetBoolean(value);
            SetPropertyValue("async-handling", in holder);
        }
    }

    /// <summary>
    /// Forward all children messages, even those that would normally be filtered by
    /// the bin. This can be interesting when one wants to be notified of the EOS
    /// state of individual elements, for example.
    /// </summary>
    /// <remarks>
    /// <para>
    /// The messages are converted to an ELEMENT message with the bin as the
    /// source. The structure of the message is named `GstBinForwarded` and contains
    /// a field named `message` that contains the original forwarded #GstMessage.
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool MessageForward
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("message-forward");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("message-forward");
            holder.SetBoolean(value);
            SetPropertyValue("message-forward", in holder);
        }
    }

    /// <summary>The arguments of the <c>deep-element-added</c> signal of <c>GstBin</c>.</summary>
    public sealed class DeepElementAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="DeepElementAddedSignalArgs"/> class.</summary>
        /// <param name="subBin">the #GstBin the element was added to</param>
        /// <param name="element">the #GstElement that was added to @sub_bin</param>
        internal DeepElementAddedSignalArgs(Gst.Bin subBin, Gst.Element element)
        {
            SubBin = subBin;
            Element = element;
        }

        /// <summary>the #GstBin the element was added to</summary>
        public Gst.Bin SubBin { get; }

        /// <summary>the #GstElement that was added to @sub_bin</summary>
        public Gst.Element Element { get; }
    }

    /// <summary>Will be emitted after the element was added to @sub_bin.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Bin.DeepElementAddedSignalArgs> DeepElementAdded
    {
        add => Gst.SignalConnections.Add(this, "deep-element-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&DeepElementAddedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "deep-element-added", value);
    }

    /// <summary>The native handler of the <c>deep-element-added</c> signal of <c>GstBin</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void DeepElementAddedTrampoline(nint instance, nint subBin, nint element, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Bin.DeepElementAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Bin subBinValue = Gst.GObject.Object.FromNative<Gst.Bin>(subBin, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The deep-element-added signal of GstBin passed no sub_bin.");
            Gst.Element elementValue = Gst.GObject.Object.FromNative<Gst.Element>(element, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The deep-element-added signal of GstBin passed no element.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Bin.DeepElementAddedSignalArgs(subBinValue, elementValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>deep-element-removed</c> signal of <c>GstBin</c>.</summary>
    public sealed class DeepElementRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="DeepElementRemovedSignalArgs"/> class.</summary>
        /// <param name="subBin">the #GstBin the element was removed from</param>
        /// <param name="element">the #GstElement that was removed from @sub_bin</param>
        internal DeepElementRemovedSignalArgs(Gst.Bin subBin, Gst.Element element)
        {
            SubBin = subBin;
            Element = element;
        }

        /// <summary>the #GstBin the element was removed from</summary>
        public Gst.Bin SubBin { get; }

        /// <summary>the #GstElement that was removed from @sub_bin</summary>
        public Gst.Element Element { get; }
    }

    /// <summary>Will be emitted after the element was removed from @sub_bin.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Bin.DeepElementRemovedSignalArgs> DeepElementRemoved
    {
        add => Gst.SignalConnections.Add(this, "deep-element-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, nint, void>)&DeepElementRemovedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "deep-element-removed", value);
    }

    /// <summary>The native handler of the <c>deep-element-removed</c> signal of <c>GstBin</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void DeepElementRemovedTrampoline(nint instance, nint subBin, nint element, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Bin.DeepElementRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Bin subBinValue = Gst.GObject.Object.FromNative<Gst.Bin>(subBin, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The deep-element-removed signal of GstBin passed no sub_bin.");
            Gst.Element elementValue = Gst.GObject.Object.FromNative<Gst.Element>(element, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The deep-element-removed signal of GstBin passed no element.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Bin.DeepElementRemovedSignalArgs(subBinValue, elementValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The handler of the <c>do-latency</c> signal of <c>GstBin</c>.</summary>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>The result the emission collects.</returns>
    public delegate bool DoLatencyHandler(object? sender, System.EventArgs args);

    /// <summary>
    /// Will be emitted when the bin needs to perform latency calculations. This
    /// signal is only emitted for toplevel bins or when #GstBin:async-handling is
    /// enabled.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Only one signal handler is invoked. If no signals are connected, the
    /// default handler is invoked, which will query and distribute the lowest
    /// possible latency to all sinks.
    /// </para>
    /// <para>
    /// Connect to this signal if the default latency calculations are not
    /// sufficient, like when you need different latencies for different sinks in
    /// the same pipeline.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.Bin.DoLatencyHandler DoLatency
    {
        add => Gst.SignalConnections.Add(this, "do-latency", (nint)(delegate* unmanaged[Cdecl]<nint, nint, int>)&DoLatencyTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "do-latency", value);
    }

    /// <summary>The native handler of the <c>do-latency</c> signal of <c>GstBin</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static int DoLatencyTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.Bin.DoLatencyHandler>(userData) is not { } handler)
            {
                return default;
            }

            bool result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
            return result ? 1 : 0;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The arguments of the <c>element-added</c> signal of <c>GstBin</c>.</summary>
    public sealed class ElementAddedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ElementAddedSignalArgs"/> class.</summary>
        /// <param name="element">the #GstElement that was added to the bin</param>
        internal ElementAddedSignalArgs(Gst.Element element)
        {
            Element = element;
        }

        /// <summary>the #GstElement that was added to the bin</summary>
        public Gst.Element Element { get; }
    }

    /// <summary>Will be emitted after the element was added to the bin.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Bin.ElementAddedSignalArgs> ElementAdded
    {
        add => Gst.SignalConnections.Add(this, "element-added", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&ElementAddedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "element-added", value);
    }

    /// <summary>The native handler of the <c>element-added</c> signal of <c>GstBin</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ElementAddedTrampoline(nint instance, nint element, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Bin.ElementAddedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Element elementValue = Gst.GObject.Object.FromNative<Gst.Element>(element, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The element-added signal of GstBin passed no element.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Bin.ElementAddedSignalArgs(elementValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>element-removed</c> signal of <c>GstBin</c>.</summary>
    public sealed class ElementRemovedSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ElementRemovedSignalArgs"/> class.</summary>
        /// <param name="element">the #GstElement that was removed from the bin</param>
        internal ElementRemovedSignalArgs(Gst.Element element)
        {
            Element = element;
        }

        /// <summary>the #GstElement that was removed from the bin</summary>
        public Gst.Element Element { get; }
    }

    /// <summary>Will be emitted after the element was removed from the bin.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.Bin.ElementRemovedSignalArgs> ElementRemoved
    {
        add => Gst.SignalConnections.Add(this, "element-removed", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, void>)&ElementRemovedTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "element-removed", value);
    }

    /// <summary>The native handler of the <c>element-removed</c> signal of <c>GstBin</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ElementRemovedTrampoline(nint instance, nint element, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.Bin.ElementRemovedSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            Gst.Element elementValue = Gst.GObject.Object.FromNative<Gst.Element>(element, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The element-removed signal of GstBin passed no element.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.Bin.ElementRemovedSignalArgs(elementValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_bin_new</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_new")]
    private static partial nint GstBinNew(byte* name);

    /// <summary>The <c>gst_bin_add</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_add")]
    private static partial int GstBinAdd(nint bin, nint element);

    /// <summary>The <c>gst_bin_find_unlinked_pad</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_find_unlinked_pad")]
    private static partial nint GstBinFindUnlinkedPad(nint bin, int direction);

    /// <summary>The <c>gst_bin_get_by_interface</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_get_by_interface")]
    private static partial nint GstBinGetByInterface(nint bin, nuint iface);

    /// <summary>The <c>gst_bin_get_by_name</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_get_by_name")]
    private static partial nint GstBinGetByName(nint bin, byte* name);

    /// <summary>The <c>gst_bin_get_by_name_recurse_up</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_get_by_name_recurse_up")]
    private static partial nint GstBinGetByNameRecurseUp(nint bin, byte* name);

    /// <summary>The <c>gst_bin_get_suppressed_flags</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_get_suppressed_flags")]
    private static partial int GstBinGetSuppressedFlags(nint bin);

    /// <summary>The <c>gst_bin_iterate_all_by_element_factory_name</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_iterate_all_by_element_factory_name")]
    private static partial nint GstBinIterateAllByElementFactoryName(nint bin, byte* factoryName);

    /// <summary>The <c>gst_bin_iterate_all_by_interface</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_iterate_all_by_interface")]
    private static partial nint GstBinIterateAllByInterface(nint bin, nuint iface);

    /// <summary>The <c>gst_bin_iterate_elements</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_iterate_elements")]
    private static partial nint GstBinIterateElements(nint bin);

    /// <summary>The <c>gst_bin_iterate_recurse</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_iterate_recurse")]
    private static partial nint GstBinIterateRecurse(nint bin);

    /// <summary>The <c>gst_bin_iterate_sinks</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_iterate_sinks")]
    private static partial nint GstBinIterateSinks(nint bin);

    /// <summary>The <c>gst_bin_iterate_sorted</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_iterate_sorted")]
    private static partial nint GstBinIterateSorted(nint bin);

    /// <summary>The <c>gst_bin_iterate_sources</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_iterate_sources")]
    private static partial nint GstBinIterateSources(nint bin);

    /// <summary>The <c>gst_bin_recalculate_latency</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_recalculate_latency")]
    private static partial int GstBinRecalculateLatency(nint bin);

    /// <summary>The <c>gst_bin_remove</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_remove")]
    private static partial int GstBinRemove(nint bin, nint element);

    /// <summary>The <c>gst_bin_set_suppressed_flags</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_set_suppressed_flags")]
    private static partial void GstBinSetSuppressedFlags(nint bin, int flags);

    /// <summary>The <c>gst_bin_sync_children_states</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_bin_sync_children_states")]
    private static partial int GstBinSyncChildrenStates(nint bin);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstBin</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_bin_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Bin(handle, transfer);
}
