// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// GStreamer is extensible, so #GstElement instances can be loaded at runtime.
/// A plugin system can provide one or more of the basic GStreamer
/// #GstPluginFeature subclasses.
/// </summary>
/// <remarks>
/// <para>
/// A plugin should export a symbol `gst_plugin_desc` that is a
/// struct of type #GstPluginDesc.
/// the plugin loader will check the version of the core library the plugin was
/// linked against and will create a new #GstPlugin. It will then call the
/// #GstPluginInitFunc function that was provided in the
/// `gst_plugin_desc`.
/// </para>
/// <para>
/// Once you have a handle to a #GstPlugin (e.g. from the #GstRegistry), you
/// can add any object that subclasses #GstPluginFeature.
/// </para>
/// <para>
/// Usually plugins are always automatically loaded so you don't need to call
/// gst_plugin_load() explicitly to bring it into memory. There are options to
/// statically link plugins to an app or even use GStreamer without a plugin
/// repository in which case gst_plugin_load() can be needed to bring the plugin
/// into memory.
/// </para>
/// </remarks>
public unsafe partial class Plugin : Gst.Object
{
    /// <summary>Wraps a native <c>GstPlugin</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstPlugin</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected Plugin(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Make GStreamer aware of external dependencies which affect the feature
    /// set of this plugin (ie. the elements or typefinders associated with it).
    /// </summary>
    /// <remarks>
    /// <para>
    /// GStreamer will re-inspect plugins with external dependencies whenever any
    /// of the external dependencies change. This is useful for plugins which wrap
    /// other plugin systems, e.g. a plugin which wraps a plugin-based visualisation
    /// library and makes visualisations available as GStreamer elements, or a
    /// codec loader which exposes elements and/or caps dependent on what external
    /// codec libraries are currently installed.
    /// </para>
    /// </remarks>
    /// <param name="envVars">
    /// %NULL-terminated array of environment variables affecting the
    ///     feature set of the plugin (e.g. an environment variable containing
    ///     paths where to look for additional modules/plugins of a library),
    ///     or %NULL. Environment variable names may be followed by a path component
    ///      which will be added to the content of the environment variable, e.g.
    ///      "HOME/.mystuff/plugins".
    /// </param>
    /// <param name="paths">
    /// %NULL-terminated array of directories/paths where dependent files
    ///     may be, or %NULL.
    /// </param>
    /// <param name="names">
    /// %NULL-terminated array of file names (or file name suffixes,
    ///     depending on @flags) to be used in combination with the paths from
    ///     @paths and/or the paths extracted from the environment variables in
    ///     @env_vars, or %NULL.
    /// </param>
    /// <param name="flags">optional flags, or #GST_PLUGIN_DEPENDENCY_FLAG_NONE</param>
    public void AddDependency(string[]? envVars, string[]? paths, string[]? names, Gst.PluginDependencyFlags flags)
    {
        using Gst.Interop.StrvScope envVarsScope = Gst.Interop.GMarshal.AllocStrv(envVars);
        using Gst.Interop.StrvScope pathsScope = Gst.Interop.GMarshal.AllocStrv(paths);
        using Gst.Interop.StrvScope namesScope = Gst.Interop.GMarshal.AllocStrv(names);
        GstPluginAddDependency(Handle, envVarsScope.Pointer, pathsScope.Pointer, namesScope.Pointer, (int)flags);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Make GStreamer aware of external dependencies which affect the feature
    /// set of this plugin (ie. the elements or typefinders associated with it).
    /// </summary>
    /// <remarks>
    /// <para>
    /// GStreamer will re-inspect plugins with external dependencies whenever any
    /// of the external dependencies change. This is useful for plugins which wrap
    /// other plugin systems, e.g. a plugin which wraps a plugin-based visualisation
    /// library and makes visualisations available as GStreamer elements, or a
    /// codec loader which exposes elements and/or caps dependent on what external
    /// codec libraries are currently installed.
    /// </para>
    /// <para>
    /// Convenience wrapper function for gst_plugin_add_dependency() which
    /// takes simple strings as arguments instead of string arrays, with multiple
    /// arguments separated by predefined delimiters (see above).
    /// </para>
    /// </remarks>
    /// <param name="envVars">
    /// one or more environment variables (separated by ':', ';' or ','),
    ///      or %NULL. Environment variable names may be followed by a path component
    ///      which will be added to the content of the environment variable, e.g.
    ///      "HOME/.mystuff/plugins:MYSTUFF_PLUGINS_PATH"
    /// </param>
    /// <param name="paths">
    /// one ore more directory paths (separated by ':' or ';' or ','),
    ///      or %NULL. Example: "/usr/lib/mystuff/plugins"
    /// </param>
    /// <param name="names">
    /// one or more file names or file name suffixes (separated by commas),
    ///      or %NULL
    /// </param>
    /// <param name="flags">optional flags, or #GST_PLUGIN_DEPENDENCY_FLAG_NONE</param>
    public void AddDependencySimple(string? envVars, string? paths, string? names, Gst.PluginDependencyFlags flags)
    {
        System.Span<byte> envVarsBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope envVarsScope = Gst.Interop.GMarshal.StackUtf8(envVars, envVarsBuffer);
        System.Span<byte> pathsBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope pathsScope = Gst.Interop.GMarshal.StackUtf8(paths, pathsBuffer);
        System.Span<byte> namesBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope namesScope = Gst.Interop.GMarshal.StackUtf8(names, namesBuffer);
        GstPluginAddDependencySimple(Handle, envVarsScope.Pointer, pathsScope.Pointer, namesScope.Pointer, (int)flags);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_plugin_add_status_error</c> function.</summary>
    /// <param name="message">the status error message</param>
    public void AddStatusError(string message)
    {
        ArgumentNullException.ThrowIfNull(message);
        System.Span<byte> messageBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope messageScope = Gst.Interop.GMarshal.StackUtf8(message, messageBuffer);
        GstPluginAddStatusError(Handle, messageScope.Pointer);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_plugin_add_status_info</c> function.</summary>
    /// <param name="message">the status info message</param>
    public void AddStatusInfo(string message)
    {
        ArgumentNullException.ThrowIfNull(message);
        System.Span<byte> messageBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope messageScope = Gst.Interop.GMarshal.StackUtf8(message, messageBuffer);
        GstPluginAddStatusInfo(Handle, messageScope.Pointer);
        System.GC.KeepAlive(this);
    }

    /// <summary>The <c>gst_plugin_add_status_warning</c> function.</summary>
    /// <param name="message">the status warning message</param>
    public void AddStatusWarning(string message)
    {
        ArgumentNullException.ThrowIfNull(message);
        System.Span<byte> messageBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope messageScope = Gst.Interop.GMarshal.StackUtf8(message, messageBuffer);
        GstPluginAddStatusWarning(Handle, messageScope.Pointer);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Gets the plugin specific data cache. If it is %NULL there is no cached data
    /// stored. This is the case when the registry is getting rebuilt.
    /// </summary>
    /// <returns>
    /// The cached data as a
    /// #GstStructure or %NULL.
    /// The wrapper owns a reference of its own, which is a copy for a boxed type:
    /// dispose it when you are done, and note that changes made to a copy of a
    /// boxed value are not written back.
    /// </returns>
    public Gst.Structure? GetCacheData()
    {
        nint nativeResult = GstPluginGetCacheData(Handle);
        System.GC.KeepAlive(this);
        return Gst.Structure.FromNative(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Get the long descriptive name of the plugin</summary>
    /// <returns>the long name of the plugin</returns>
    public string GetDescription()
    {
        nint nativeResult = GstPluginGetDescription(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_plugin_get_description returned no value.");
    }

    /// <summary>get the filename of the plugin</summary>
    /// <returns>the filename of the plugin</returns>
    public string? GetFilename()
    {
        nint nativeResult = GstPluginGetFilename(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>get the license of the plugin</summary>
    /// <returns>the license of the plugin</returns>
    public string GetLicense()
    {
        nint nativeResult = GstPluginGetLicense(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_plugin_get_license returned no value.");
    }

    /// <summary>Get the short name of the plugin</summary>
    /// <returns>the name of the plugin</returns>
    public new string GetName()
    {
        nint nativeResult = GstPluginGetName(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_plugin_get_name returned no value.");
    }

    /// <summary>get the URL where the plugin comes from</summary>
    /// <returns>the origin of the plugin</returns>
    public string GetOrigin()
    {
        nint nativeResult = GstPluginGetOrigin(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_plugin_get_origin returned no value.");
    }

    /// <summary>get the package the plugin belongs to.</summary>
    /// <returns>the package of the plugin</returns>
    public string GetPackage()
    {
        nint nativeResult = GstPluginGetPackage(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_plugin_get_package returned no value.");
    }

    /// <summary>Get the release date (and possibly time) in form of a string, if available.</summary>
    /// <remarks>
    /// <para>
    /// For normal GStreamer plugin releases this will usually just be a date in
    /// the form of "YYYY-MM-DD", while pre-releases and builds from git may contain
    /// a time component after the date as well, in which case the string will be
    /// formatted like "YYYY-MM-DDTHH:MMZ" (e.g. "2012-04-30T09:30Z").
    /// </para>
    /// <para>There may be plugins that do not have a valid release date set on them.</para>
    /// </remarks>
    /// <returns>
    /// the date string of the plugin, or %NULL if not
    /// available.
    /// </returns>
    public string? GetReleaseDateString()
    {
        nint nativeResult = GstPluginGetReleaseDateString(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult);
    }

    /// <summary>get the source module the plugin belongs to.</summary>
    /// <returns>the source of the plugin</returns>
    public string GetSource()
    {
        nint nativeResult = GstPluginGetSource(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_plugin_get_source returned no value.");
    }

    /// <summary>The <c>gst_plugin_get_status_errors</c> function.</summary>
    /// <returns>an array of plugin status error messages, or NULL</returns>
    public string[]? GetStatusErrors()
    {
        nint nativeResult = GstPluginGetStatusErrors(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.StrvToArray(nativeResult, free: true);
    }

    /// <summary>The <c>gst_plugin_get_status_infos</c> function.</summary>
    /// <returns>an array of plugin status info messages, or NULL</returns>
    public string[]? GetStatusInfos()
    {
        nint nativeResult = GstPluginGetStatusInfos(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.StrvToArray(nativeResult, free: true);
    }

    /// <summary>The <c>gst_plugin_get_status_warnings</c> function.</summary>
    /// <returns>an array of plugin status warning messages, or NULL</returns>
    public string[]? GetStatusWarnings()
    {
        nint nativeResult = GstPluginGetStatusWarnings(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.StrvToArray(nativeResult, free: true);
    }

    /// <summary>get the version of the plugin</summary>
    /// <returns>the version of the plugin</returns>
    public string GetVersion()
    {
        nint nativeResult = GstPluginGetVersion(Handle);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_plugin_get_version returned no value.");
    }

    /// <summary>queries if the plugin is loaded into memory</summary>
    /// <returns>%TRUE is loaded, %FALSE otherwise</returns>
    public bool IsLoaded()
    {
        int nativeResult = GstPluginIsLoaded(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Loads @plugin. Note that the *return value* is the loaded plugin; @plugin is
    /// untouched. The normal use pattern of this function goes like this:
    /// </summary>
    /// <remarks>
    /// <para>
    /// |[
    /// GstPlugin *loaded_plugin;
    /// loaded_plugin = gst_plugin_load (plugin);
    /// // presumably, we're no longer interested in the potentially-unloaded plugin
    /// gst_object_unref (plugin);
    /// plugin = loaded_plugin;
    /// ]|
    /// </para>
    /// </remarks>
    /// <returns>
    /// a reference to a loaded plugin, or
    /// %NULL on error.
    /// </returns>
    public Gst.Plugin? Load()
    {
        nint nativeResult = GstPluginLoad(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Plugin>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// Adds plugin specific data to cache. Passes the ownership of the structure to
    /// the @plugin.
    /// </summary>
    /// <remarks>
    /// <para>The cache is flushed every time the registry is rebuilt.</para>
    /// <para>
    /// The <c>cacheData</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a copy of the value and the wrapper is disposed afterwards, which
    /// leaves the caller with exactly what the C call leaves it with. A boxed
    /// value has no reference count to raise, so the copy is what a reference is
    /// there. <see cref="Gst.GObject.Boxed.Dispose()"/> is idempotent, so a
    /// <c>using</c> declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="cacheData">
    /// a structure containing the data to cache
    /// The call consumes it: <paramref name="cacheData"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="cacheData"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="cacheData"/> was disposed.
    /// </exception>
    public void SetCacheData(Gst.Structure cacheData)
    {
        ArgumentNullException.ThrowIfNull(cacheData);
        nint instanceHandle = Handle;
        nint cacheDataNative = cacheData.Handle;
        nuint cacheDataType = cacheData.BoxedType.Value;
        nint cacheDataOwned = Gst.Interop.GObjectNative.BoxedCopy(cacheDataType, cacheDataNative);
        GstPluginSetCacheData(instanceHandle, cacheDataOwned);
        System.GC.KeepAlive(this);
        cacheData.Dispose();
    }

    /// <summary>Load the named plugin. Refs the plugin.</summary>
    /// <param name="name">name of plugin to load</param>
    /// <returns>
    /// a reference to a loaded plugin, or
    /// %NULL on error.
    /// </returns>
    public static Gst.Plugin? LoadByName(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstPluginLoadByName(nameScope.Pointer);
        return Gst.GObject.Object.FromNative<Gst.Plugin>(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Loads the given plugin and refs it.  Caller needs to unref after use.</summary>
    /// <param name="filename">the plugin filename to load</param>
    /// <returns>
    /// a reference to the existing loaded GstPlugin, a
    /// reference to the newly-loaded GstPlugin, or %NULL if an error occurred.
    /// </returns>
    /// <exception cref="Gst.GLib.GException">The native call failed.</exception>
    public static Gst.Plugin LoadFile(string filename)
    {
        ArgumentNullException.ThrowIfNull(filename);
        System.Span<byte> filenameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope filenameScope = Gst.Interop.GMarshal.StackUtf8(filename, filenameBuffer);
        nint errorNative = 0;
        nint nativeResult = GstPluginLoadFile(filenameScope.Pointer, &errorNative);
        if (errorNative != 0 && nativeResult != 0)
        {
            // The call failed and transferred a value all the same. The throw
            // below puts it out of reach, so it is released rather than leaked.
            Gst.Interop.GObjectNative.ObjectUnref(nativeResult);
        }
        Gst.GLib.GException.ThrowIfSet(ref errorNative);
        return Gst.GObject.Object.FromNative<Gst.Plugin>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_plugin_load_file returned no value.");
    }

    /// <summary>
    /// Registers a static plugin, ie. a plugin which is private to an application
    /// or library and contained within the application or library (as opposed to
    /// being shipped as a separate module file) with a #GstPluginInitFullFunc
    /// which allows user data to be passed to the callback function (useful
    /// for bindings).
    /// </summary>
    /// <remarks>
    /// <para>
    /// You must make sure that GStreamer has been initialised (with gst_init() or
    /// via gst_init_get_option_group()) before calling this function.
    /// </para>
    /// </remarks>
    /// <param name="majorVersion">
    /// the major version number of the GStreamer core that the
    ///     plugin was compiled for, you can just use GST_VERSION_MAJOR here
    /// </param>
    /// <param name="minorVersion">
    /// the minor version number of the GStreamer core that the
    ///     plugin was compiled for, you can just use GST_VERSION_MINOR here
    /// </param>
    /// <param name="name">
    /// a unique name of the plugin (ideally prefixed with an application- or
    ///     library-specific namespace prefix in order to avoid name conflicts in
    ///     case a similar plugin with the same name ever gets added to GStreamer)
    /// </param>
    /// <param name="description">description of the plugin</param>
    /// <param name="initFullFunc">
    /// pointer to the init function with user data
    ///     of this plugin.
    /// </param>
    /// <param name="version">version string of the plugin</param>
    /// <param name="license">
    /// effective license of plugin. Must be one of the approved licenses
    ///     (see #GstPluginDesc above) or the plugin will not be registered.
    /// </param>
    /// <param name="source">source module plugin belongs to</param>
    /// <param name="package">shipped package plugin belongs to</param>
    /// <param name="origin">URL to provider of plugin</param>
    /// <returns>%TRUE if the plugin was registered correctly, otherwise %FALSE.</returns>
    public static bool RegisterStaticFull(int majorVersion, int minorVersion, string name, string description, Gst.PluginInitFullFunc initFullFunc, string version, string license, string source, string package, string origin)
    {
        ArgumentNullException.ThrowIfNull(name);
        ArgumentNullException.ThrowIfNull(description);
        ArgumentNullException.ThrowIfNull(initFullFunc);
        ArgumentNullException.ThrowIfNull(version);
        ArgumentNullException.ThrowIfNull(license);
        ArgumentNullException.ThrowIfNull(source);
        ArgumentNullException.ThrowIfNull(package);
        ArgumentNullException.ThrowIfNull(origin);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        System.Span<byte> descriptionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope descriptionScope = Gst.Interop.GMarshal.StackUtf8(description, descriptionBuffer);
        System.Span<byte> versionBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope versionScope = Gst.Interop.GMarshal.StackUtf8(version, versionBuffer);
        System.Span<byte> licenseBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope licenseScope = Gst.Interop.GMarshal.StackUtf8(license, licenseBuffer);
        System.Span<byte> sourceBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope sourceScope = Gst.Interop.GMarshal.StackUtf8(source, sourceBuffer);
        System.Span<byte> packageBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope packageScope = Gst.Interop.GMarshal.StackUtf8(package, packageBuffer);
        System.Span<byte> originBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope originScope = Gst.Interop.GMarshal.StackUtf8(origin, originBuffer);
        Gst.Interop.CallbackHandle initFullFuncState = Gst.Interop.CallbackHandle.Alloc(initFullFunc);
        try
        {
            int nativeResult = GstPluginRegisterStaticFull(majorVersion, minorVersion, nameScope.Pointer, descriptionScope.Pointer, Gst.PluginInitFullFuncTrampoline.Pointer, versionScope.Pointer, licenseScope.Pointer, sourceScope.Pointer, packageScope.Pointer, originScope.Pointer, initFullFuncState.UserData);
            return nativeResult != 0;
        }
        finally
        {
            initFullFuncState.Free();
        }
    }

    /// <summary>The <c>gst_plugin_add_dependency</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_add_dependency")]
    private static partial void GstPluginAddDependency(nint plugin, nint* envVars, nint* paths, nint* names, int flags);

    /// <summary>The <c>gst_plugin_add_dependency_simple</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_add_dependency_simple")]
    private static partial void GstPluginAddDependencySimple(nint plugin, byte* envVars, byte* paths, byte* names, int flags);

    /// <summary>The <c>gst_plugin_add_status_error</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_add_status_error")]
    private static partial void GstPluginAddStatusError(nint plugin, byte* message);

    /// <summary>The <c>gst_plugin_add_status_info</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_add_status_info")]
    private static partial void GstPluginAddStatusInfo(nint plugin, byte* message);

    /// <summary>The <c>gst_plugin_add_status_warning</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_add_status_warning")]
    private static partial void GstPluginAddStatusWarning(nint plugin, byte* message);

    /// <summary>The <c>gst_plugin_get_cache_data</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_cache_data")]
    private static partial nint GstPluginGetCacheData(nint plugin);

    /// <summary>The <c>gst_plugin_get_description</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_description")]
    private static partial nint GstPluginGetDescription(nint plugin);

    /// <summary>The <c>gst_plugin_get_filename</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_filename")]
    private static partial nint GstPluginGetFilename(nint plugin);

    /// <summary>The <c>gst_plugin_get_license</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_license")]
    private static partial nint GstPluginGetLicense(nint plugin);

    /// <summary>The <c>gst_plugin_get_name</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_name")]
    private static partial nint GstPluginGetName(nint plugin);

    /// <summary>The <c>gst_plugin_get_origin</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_origin")]
    private static partial nint GstPluginGetOrigin(nint plugin);

    /// <summary>The <c>gst_plugin_get_package</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_package")]
    private static partial nint GstPluginGetPackage(nint plugin);

    /// <summary>The <c>gst_plugin_get_release_date_string</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_release_date_string")]
    private static partial nint GstPluginGetReleaseDateString(nint plugin);

    /// <summary>The <c>gst_plugin_get_source</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_source")]
    private static partial nint GstPluginGetSource(nint plugin);

    /// <summary>The <c>gst_plugin_get_status_errors</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_status_errors")]
    private static partial nint GstPluginGetStatusErrors(nint plugin);

    /// <summary>The <c>gst_plugin_get_status_infos</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_status_infos")]
    private static partial nint GstPluginGetStatusInfos(nint plugin);

    /// <summary>The <c>gst_plugin_get_status_warnings</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_status_warnings")]
    private static partial nint GstPluginGetStatusWarnings(nint plugin);

    /// <summary>The <c>gst_plugin_get_version</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_version")]
    private static partial nint GstPluginGetVersion(nint plugin);

    /// <summary>The <c>gst_plugin_is_loaded</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_is_loaded")]
    private static partial int GstPluginIsLoaded(nint plugin);

    /// <summary>The <c>gst_plugin_load</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_load")]
    private static partial nint GstPluginLoad(nint plugin);

    /// <summary>The <c>gst_plugin_set_cache_data</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_set_cache_data")]
    private static partial void GstPluginSetCacheData(nint plugin, nint cacheData);

    /// <summary>The <c>gst_plugin_load_by_name</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_load_by_name")]
    private static partial nint GstPluginLoadByName(byte* name);

    /// <summary>The <c>gst_plugin_load_file</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_load_file")]
    private static partial nint GstPluginLoadFile(byte* filename, nint* error);

    /// <summary>The <c>gst_plugin_register_static_full</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_register_static_full")]
    private static partial int GstPluginRegisterStaticFull(int majorVersion, int minorVersion, byte* name, byte* description, nint initFullFunc, byte* version, byte* license, byte* source, byte* package, byte* origin, nint userData);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstPlugin</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_plugin_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Plugin(handle, transfer);
}
