// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// A #GstDeviceProvider subclass is provided by a plugin that handles devices
/// if there is a way to programmatically list connected devices. It can also
/// optionally provide updates to the list of connected devices.
/// </summary>
/// <remarks>
/// <para>
/// Each #GstDeviceProvider subclass is a singleton, a plugin should
/// normally provide a single subclass for all devices.
/// </para>
/// <para>
/// Applications would normally use a #GstDeviceMonitor to monitor devices
/// from all relevant providers.
/// </para>
/// </remarks>
public abstract unsafe partial class DeviceProvider : Gst.Object
{
    /// <summary>Wraps a native <c>GstDeviceProvider</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstDeviceProvider</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected DeviceProvider(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>The <c>gst_device_provider_can_monitor</c> function.</summary>
    /// <returns>The result of <c>gst_device_provider_can_monitor</c>.</returns>
    public bool CanMonitor()
    {
        int nativeResult = GstDeviceProviderCanMonitor(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Posts a message on the provider's #GstBus to inform applications that
    /// a new device has been added.
    /// </summary>
    /// <remarks>
    /// <para>This is for use by subclasses.</para>
    /// <para>
    /// @device's reference count will be incremented, and any floating reference
    /// will be removed (see gst_object_ref_sink()).
    /// </para>
    /// </remarks>
    /// <param name="device">The <c>device</c> argument.</param>
    public void DeviceAdd(Gst.Device device)
    {
        ArgumentNullException.ThrowIfNull(device);
        GstDeviceProviderDeviceAdd(Handle, device.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(device);
    }

    /// <summary>
    /// This function is used when @changed_device was modified into its new form
    /// @device. This will post a `DEVICE_CHANGED` message on the bus to let
    /// the application know that the device was modified. #GstDevice is immutable
    /// for MT. safety purposes so this is an "atomic" way of letting the application
    /// know when a device was modified.
    /// </summary>
    /// <param name="device">The <c>device</c> argument.</param>
    /// <param name="changedDevice">The <c>changedDevice</c> argument.</param>
    public void DeviceChanged(Gst.Device device, Gst.Device changedDevice)
    {
        ArgumentNullException.ThrowIfNull(device);
        ArgumentNullException.ThrowIfNull(changedDevice);
        GstDeviceProviderDeviceChanged(Handle, device.Handle, changedDevice.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(device);
        System.GC.KeepAlive(changedDevice);
    }

    /// <summary>
    /// Posts a message on the provider's #GstBus to inform applications that
    /// a device has been removed.
    /// </summary>
    /// <remarks>
    /// <para>This is for use by subclasses.</para>
    /// </remarks>
    /// <param name="device">The <c>device</c> argument.</param>
    public void DeviceRemove(Gst.Device device)
    {
        ArgumentNullException.ThrowIfNull(device);
        GstDeviceProviderDeviceRemove(Handle, device.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(device);
    }

    /// <summary>Gets the #GstBus of this #GstDeviceProvider</summary>
    /// <returns>a #GstBus</returns>
    public Gst.Bus GetBus()
    {
        nint nativeResult = GstDeviceProviderGetBus(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Bus>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_device_provider_get_bus returned no value.");
    }

    /// <summary>
    /// Gets a list of devices that this provider understands. This may actually
    /// probe the hardware if the provider is not currently started.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If the provider has been started, this will return the same #GstDevice
    /// objects that have been returned by the #GST_MESSAGE_DEVICE_ADDED messages.
    /// </para>
    /// </remarks>
    /// <returns>
    /// a #GList of
    ///   #GstDevice
    /// </returns>
    public System.Collections.Generic.IReadOnlyList<Gst.Device> GetDevices()
    {
        nint nativeResult = GstDeviceProviderGetDevices(Handle);
        System.GC.KeepAlive(this);
        nint[] nativeItems = Gst.Interop.GListMarshal.CollectAndFreeSpine(nativeResult);
        System.Collections.Generic.List<Gst.Device> result = new(nativeItems.Length);
        foreach (nint nativeItem in nativeItems)
        {
            if (nativeItem != 0 && Gst.GObject.Object.FromNative<Gst.Device>(nativeItem, Gst.Interop.Transfer.Full) is { } adopted)
            {
                result.Add(adopted);
            }
        }

        return result;
    }

    /// <summary>Retrieves the factory that was used to create this device provider.</summary>
    /// <returns>
    /// the #GstDeviceProviderFactory used for
    ///     creating this device provider. no refcounting is needed.
    /// </returns>
    public Gst.DeviceProviderFactory? GetFactory()
    {
        nint nativeResult = GstDeviceProviderGetFactory(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.DeviceProviderFactory>(nativeResult, Gst.Interop.Transfer.None);
    }

    /// <summary>Get metadata with @key in @provider.</summary>
    /// <param name="key">The <c>key</c> argument.</param>
    /// <returns>the metadata for @key.</returns>
    public string GetMetadata(string key)
    {
        ArgumentNullException.ThrowIfNull(key);
        System.Span<byte> keyBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope keyScope = Gst.Interop.GMarshal.StackUtf8(key, keyBuffer);
        nint nativeResult = GstDeviceProviderGetMetadata(Handle, keyScope.Pointer);
        System.GC.KeepAlive(this);
        return Gst.Interop.GMarshal.PtrToStringUtf8(nativeResult)
            ?? throw new InvalidOperationException("gst_device_provider_get_metadata returned no value.");
    }

    /// <summary>Make @provider hide the devices from the factory with @name.</summary>
    /// <remarks>
    /// <para>
    /// This function is used when @provider will also provide the devices reported
    /// by provider factory @name. A monitor should stop monitoring the
    /// device provider with @name to avoid duplicate devices.
    /// </para>
    /// </remarks>
    /// <param name="name">The <c>name</c> argument.</param>
    public void HideProvider(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        GstDeviceProviderHideProvider(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
    }

    /// <summary>This function can be used to know if the @provider was successfully started.</summary>
    /// <returns>The result of <c>gst_device_provider_is_started</c>.</returns>
    public bool IsStarted()
    {
        int nativeResult = GstDeviceProviderIsStarted(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Starts providering the devices. This will cause #GST_MESSAGE_DEVICE_ADDED
    /// and #GST_MESSAGE_DEVICE_REMOVED messages to be posted on the provider's bus
    /// when devices are added or removed from the system.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Since the #GstDeviceProvider is a singleton,
    /// gst_device_provider_start() may already have been called by another
    /// user of the object, gst_device_provider_stop() needs to be called the same
    /// number of times.
    /// </para>
    /// <para>
    /// After this function has been called, gst_device_provider_get_devices() will
    /// return the same objects that have been received from the
    /// #GST_MESSAGE_DEVICE_ADDED messages and will no longer probe.
    /// </para>
    /// </remarks>
    /// <returns>%TRUE if the device providering could be started</returns>
    public bool Start()
    {
        int nativeResult = GstDeviceProviderStart(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Decreases the use-count by one. If the use count reaches zero, this
    /// #GstDeviceProvider will stop providering the devices. This needs to be
    /// called the same number of times that gst_device_provider_start() was called.
    /// </summary>
    public void Stop()
    {
        GstDeviceProviderStop(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>Make @provider unhide the devices from factory @name.</summary>
    /// <remarks>
    /// <para>
    /// This function is used when @provider will no longer provide the devices
    /// reported by provider factory @name. A monitor should start
    /// monitoring the devices from provider factory @name in order to see
    /// all devices again.
    /// </para>
    /// </remarks>
    /// <param name="name">The <c>name</c> argument.</param>
    public void UnhideProvider(string name)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        GstDeviceProviderUnhideProvider(Handle, nameScope.Pointer);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Create a new device providerfactory capable of instantiating objects of the
    /// @type and add the factory to @plugin.
    /// </summary>
    /// <param name="plugin">The <c>plugin</c> argument.</param>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <param name="rank">The <c>rank</c> argument.</param>
    /// <param name="type">The <c>type</c> argument.</param>
    /// <returns>%TRUE, if the registering succeeded, %FALSE on error</returns>
    public static bool Register(Gst.Plugin? plugin, string name, uint rank, Gst.GObject.GType type)
    {
        ArgumentNullException.ThrowIfNull(name);
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        int nativeResult = GstDeviceProviderRegister(plugin is null ? 0 : plugin.Handle, nameScope.Pointer, rank, type.Value);
        System.GC.KeepAlive(plugin);
        return nativeResult != 0;
    }

    /// <summary>The arguments of the <c>provider-hidden</c> signal of <c>GstDeviceProvider</c>.</summary>
    public sealed class ProviderHiddenSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ProviderHiddenSignalArgs"/> class.</summary>
        /// <param name="object">The <c>object</c> argument.</param>
        internal ProviderHiddenSignalArgs(string @object)
        {
            Object = @object;
        }

        /// <summary>The <c>object</c> argument.</summary>
        public string Object { get; }
    }

    /// <summary>Raised for the <c>provider-hidden</c> signal of <c>GstDeviceProvider</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.DeviceProvider.ProviderHiddenSignalArgs> ProviderHidden
    {
        add => Gst.SignalConnections.Add(this, "provider-hidden", (nint)(delegate* unmanaged[Cdecl]<nint, byte*, nint, void>)&ProviderHiddenTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "provider-hidden", value);
    }

    /// <summary>The native handler of the <c>provider-hidden</c> signal of <c>GstDeviceProvider</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ProviderHiddenTrampoline(nint instance, byte* @object, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.DeviceProvider.ProviderHiddenSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            string objectValue = Gst.Interop.GMarshal.PtrToStringUtf8((nint)@object)
                ?? throw new InvalidOperationException("The provider-hidden signal of GstDeviceProvider passed no object.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.DeviceProvider.ProviderHiddenSignalArgs(objectValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>provider-unhidden</c> signal of <c>GstDeviceProvider</c>.</summary>
    public sealed class ProviderUnhiddenSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ProviderUnhiddenSignalArgs"/> class.</summary>
        /// <param name="object">The <c>object</c> argument.</param>
        internal ProviderUnhiddenSignalArgs(string @object)
        {
            Object = @object;
        }

        /// <summary>The <c>object</c> argument.</summary>
        public string Object { get; }
    }

    /// <summary>Raised for the <c>provider-unhidden</c> signal of <c>GstDeviceProvider</c>.</summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.DeviceProvider.ProviderUnhiddenSignalArgs> ProviderUnhidden
    {
        add => Gst.SignalConnections.Add(this, "provider-unhidden", (nint)(delegate* unmanaged[Cdecl]<nint, byte*, nint, void>)&ProviderUnhiddenTrampoline, value);
        remove => Gst.SignalConnections.Remove(this, "provider-unhidden", value);
    }

    /// <summary>The native handler of the <c>provider-unhidden</c> signal of <c>GstDeviceProvider</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void ProviderUnhiddenTrampoline(nint instance, byte* @object, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.DeviceProvider.ProviderUnhiddenSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            string objectValue = Gst.Interop.GMarshal.PtrToStringUtf8((nint)@object)
                ?? throw new InvalidOperationException("The provider-unhidden signal of GstDeviceProvider passed no object.");
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.DeviceProvider.ProviderUnhiddenSignalArgs(objectValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The <c>gst_device_provider_can_monitor</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_can_monitor")]
    private static partial int GstDeviceProviderCanMonitor(nint provider);

    /// <summary>The <c>gst_device_provider_device_add</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_device_add")]
    private static partial void GstDeviceProviderDeviceAdd(nint provider, nint device);

    /// <summary>The <c>gst_device_provider_device_changed</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_device_changed")]
    private static partial void GstDeviceProviderDeviceChanged(nint provider, nint device, nint changedDevice);

    /// <summary>The <c>gst_device_provider_device_remove</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_device_remove")]
    private static partial void GstDeviceProviderDeviceRemove(nint provider, nint device);

    /// <summary>The <c>gst_device_provider_get_bus</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_get_bus")]
    private static partial nint GstDeviceProviderGetBus(nint provider);

    /// <summary>The <c>gst_device_provider_get_devices</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_get_devices")]
    private static partial nint GstDeviceProviderGetDevices(nint provider);

    /// <summary>The <c>gst_device_provider_get_factory</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_get_factory")]
    private static partial nint GstDeviceProviderGetFactory(nint provider);

    /// <summary>The <c>gst_device_provider_get_metadata</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_get_metadata")]
    private static partial nint GstDeviceProviderGetMetadata(nint provider, byte* key);

    /// <summary>The <c>gst_device_provider_hide_provider</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_hide_provider")]
    private static partial void GstDeviceProviderHideProvider(nint provider, byte* name);

    /// <summary>The <c>gst_device_provider_is_started</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_is_started")]
    private static partial int GstDeviceProviderIsStarted(nint provider);

    /// <summary>The <c>gst_device_provider_start</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_start")]
    private static partial int GstDeviceProviderStart(nint provider);

    /// <summary>The <c>gst_device_provider_stop</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_stop")]
    private static partial void GstDeviceProviderStop(nint provider);

    /// <summary>The <c>gst_device_provider_unhide_provider</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_unhide_provider")]
    private static partial void GstDeviceProviderUnhideProvider(nint provider, byte* name);

    /// <summary>The <c>gst_device_provider_register</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_register")]
    private static partial int GstDeviceProviderRegister(nint plugin, byte* name, uint rank, nuint type);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstDeviceProvider</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_device_provider_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Concrete(handle, transfer);

    /// <summary>
    /// The wrapper of a native type that derives from <c>GstDeviceProvider</c> and has no
    /// generated binding of its own, for example an element of a plugin.
    /// </summary>
    private sealed class Concrete : DeviceProvider
    {
        /// <summary>Wraps a native instance.</summary>
        /// <param name="handle">The native instance.</param>
        /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
        internal Concrete(nint handle, Gst.Interop.Transfer transfer)
            : base(handle, transfer)
        {
        }
    }
}
