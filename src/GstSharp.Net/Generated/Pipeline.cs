// <auto-generated/>
// Generated by GstSharp.Generator from Gst-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst;

/// <summary>
/// A #GstPipeline is a special #GstBin used as the toplevel container for
/// the filter graph. The #GstPipeline will manage the selection and
/// distribution of a global #GstClock as well as provide a #GstBus to the
/// application.
/// </summary>
/// <remarks>
/// <para>
/// gst_pipeline_new() is used to create a pipeline. when you are done with
/// the pipeline, use gst_object_unref() to free its resources including all
/// added #GstElement objects (if not otherwise referenced).
/// </para>
/// <para>
/// Elements are added and removed from the pipeline using the #GstBin
/// methods like gst_bin_add() and gst_bin_remove() (see #GstBin).
/// </para>
/// <para>
/// Before changing the state of the #GstPipeline (see #GstElement) a #GstBus
/// should be retrieved with gst_pipeline_get_bus(). This #GstBus should then
/// be used to receive #GstMessage from the elements in the pipeline. Listening
/// to the #GstBus is necessary for retrieving error messages from the
/// #GstPipeline and otherwise the #GstPipeline might stop without any
/// indication, why. Furthermore, the #GstPipeline posts messages even if
/// nobody listens on the #GstBus, which will pile up and use up memory.
/// </para>
/// <para>
/// By default, a #GstPipeline will automatically flush the pending #GstBus
/// messages when going to the NULL state to ensure that no circular
/// references exist when no messages are read from the #GstBus. This
/// behaviour can be changed with gst_pipeline_set_auto_flush_bus().
/// </para>
/// <para>
/// When the #GstPipeline performs the PAUSED to PLAYING state change it will
/// select a clock for the elements. The clock selection algorithm will by
/// default select a clock provided by an element that is most upstream
/// (closest to the source). For live pipelines (ones that return
/// #GST_STATE_CHANGE_NO_PREROLL from the gst_element_set_state() call) this
/// will select the clock provided by the live source. For normal pipelines
/// this will select a clock provided by the sinks (most likely the audio
/// sink). If no element provides a clock, a default #GstSystemClock is used.
/// </para>
/// <para>
/// The clock selection can be controlled with the gst_pipeline_use_clock()
/// method, which will enforce a given clock on the pipeline. With
/// gst_pipeline_auto_clock() the default clock selection algorithm can be
/// restored.
/// </para>
/// <para>
/// A #GstPipeline maintains a running time for the elements. The running
/// time is defined as the difference between the current clock time and
/// the base time. When the pipeline goes to READY or a flushing seek is
/// performed on it, the running time is reset to 0. When the pipeline is
/// set from PLAYING to PAUSED, the current clock time is sampled and used to
/// configure the base time for the elements when the pipeline is set
/// to PLAYING again. The effect is that the running time (as the difference
/// between the clock time and the base time) will count how much time was spent
/// in the PLAYING state. This default behaviour can be changed with the
/// gst_element_set_start_time() method.
/// </para>
/// </remarks>
public unsafe partial class Pipeline : Gst.Bin, Gst.IChildProxy
{
    /// <summary>Wraps a native <c>GstPipeline</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal Pipeline(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Create a new pipeline with the given name.</summary>
    /// <param name="name">The <c>name</c> argument.</param>
    /// <returns>newly created GstPipeline</returns>
    public static new Gst.Pipeline New(string? name)
    {
        System.Span<byte> nameBuffer = stackalloc byte[Gst.Interop.GMarshal.StackBufferSize];
        using Gst.Interop.Utf8Scope nameScope = Gst.Interop.GMarshal.StackUtf8(name, nameBuffer);
        nint nativeResult = GstPipelineNew(nameScope.Pointer);
        return Gst.GObject.Object.FromNative<Gst.Pipeline>(nativeResult, Gst.Interop.Transfer.None)
            ?? throw new InvalidOperationException("gst_pipeline_new returned no value.");
    }

    /// <summary>
    /// Let @pipeline select a clock automatically. This is the default
    /// behaviour.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Use this function if you previous forced a fixed clock with
    /// gst_pipeline_use_clock() and want to restore the default
    /// pipeline clock selection algorithm.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    public void AutoClock()
    {
        GstPipelineAutoClock(Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Check if @pipeline will automatically flush messages when going to
    /// the NULL state.
    /// </summary>
    /// <returns>
    /// whether the pipeline will automatically flush its bus when
    /// going from READY to NULL state or not.
    /// </returns>
    public bool GetAutoFlushBus()
    {
        int nativeResult = GstPipelineGetAutoFlushBus(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Gets the #GstBus of @pipeline. The bus allows applications to receive
    /// #GstMessage packets.
    /// </summary>
    /// <returns>a #GstBus, unref after usage.</returns>
    public new Gst.Bus GetBus()
    {
        nint nativeResult = GstPipelineGetBus(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Bus>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_pipeline_get_bus returned no value.");
    }

    /// <summary>Return the configured latency on @pipeline.</summary>
    /// <returns>
    /// @pipeline configured latency, or %GST_CLOCK_TIME_NONE if none has been configured
    /// because @pipeline did not reach the PLAYING state yet.
    /// </returns>
    public Gst.ClockTime GetConfiguredLatency()
    {
        ulong nativeResult = GstPipelineGetConfiguredLatency(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Get the configured delay (see gst_pipeline_set_delay()).</summary>
    /// <returns>The configured delay.</returns>
    public Gst.ClockTime GetDelay()
    {
        ulong nativeResult = GstPipelineGetDelay(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Gets the latency that should be configured on the pipeline. See
    /// gst_pipeline_set_latency().
    /// </summary>
    /// <returns>Latency to configure on the pipeline or GST_CLOCK_TIME_NONE</returns>
    public Gst.ClockTime GetLatency()
    {
        ulong nativeResult = GstPipelineGetLatency(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Gets the current clock used by @pipeline.</summary>
    /// <remarks>
    /// <para>
    /// Unlike gst_element_get_clock(), this function will always return a
    /// clock, even if the pipeline is not in the PLAYING state.
    /// </para>
    /// </remarks>
    /// <returns>a #GstClock, unref after usage.</returns>
    public Gst.Clock GetPipelineClock()
    {
        nint nativeResult = GstPipelineGetPipelineClock(Handle);
        System.GC.KeepAlive(this);
        return Gst.GObject.Object.FromNative<Gst.Clock>(nativeResult, Gst.Interop.Transfer.Full)
            ?? throw new InvalidOperationException("gst_pipeline_get_pipeline_clock returned no value.");
    }

    /// <summary>Check if @pipeline is live.</summary>
    /// <returns>%TRUE if @pipeline is live, %FALSE if not or if it did not reach the PAUSED state yet.</returns>
    public bool IsLive()
    {
        int nativeResult = GstPipelineIsLive(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Usually, when a pipeline goes from READY to NULL state, it automatically
    /// flushes all pending messages on the bus, which is done for refcounting
    /// purposes, to break circular references.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This means that applications that update state using (async) bus messages
    /// (e.g. do certain things when a pipeline goes from PAUSED to READY) might
    /// not get to see messages when the pipeline is shut down, because they might
    /// be flushed before they can be dispatched in the main thread. This behaviour
    /// can be disabled using this function.
    /// </para>
    /// <para>
    /// It is important that all messages on the bus are handled when the
    /// automatic flushing is disabled else memory leaks will be introduced.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="autoFlush">The <c>autoFlush</c> argument.</param>
    public void SetAutoFlushBus(bool autoFlush)
    {
        GstPipelineSetAutoFlushBus(Handle, autoFlush ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the expected delay needed for all elements to perform the
    /// PAUSED to PLAYING state change. @delay will be added to the
    /// base time of the elements so that they wait an additional @delay
    /// amount of time before starting to process buffers and cannot be
    /// #GST_CLOCK_TIME_NONE.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This option is used for tuning purposes and should normally not be
    /// used.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="delay">The <c>delay</c> argument.</param>
    public void SetDelay(Gst.ClockTime delay)
    {
        GstPipelineSetDelay(Handle, delay.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Sets the latency that should be configured on the pipeline. Setting
    /// GST_CLOCK_TIME_NONE will restore the default behaviour of using the minimum
    /// latency from the LATENCY query. Setting this is usually not required and
    /// the pipeline will figure out an appropriate latency automatically.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Setting a too low latency, especially lower than the minimum latency from
    /// the LATENCY query, will most likely cause the pipeline to fail.
    /// </para>
    /// </remarks>
    /// <param name="latency">The <c>latency</c> argument.</param>
    public void SetLatency(Gst.ClockTime latency)
    {
        GstPipelineSetLatency(Handle, latency.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Force @pipeline to use the given @clock. The pipeline will
    /// always use the given clock even if new clock providers are added
    /// to this pipeline.
    /// </summary>
    /// <remarks>
    /// <para>
    /// If @clock is %NULL all clocking will be disabled which will make
    /// the pipeline run as fast as possible.
    /// </para>
    /// <para>MT safe.</para>
    /// </remarks>
    /// <param name="clock">The <c>clock</c> argument.</param>
    public void UseClock(Gst.Clock? clock)
    {
        GstPipelineUseClock(Handle, clock is null ? 0 : clock.Handle);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Whether or not to automatically flush all messages on the
    /// pipeline's bus when going from READY to NULL state. Please see
    /// gst_pipeline_set_auto_flush_bus() for more information on this option.
    /// </summary>
    public bool AutoFlushBus
    {
        get => GetAutoFlushBus();
        set => SetAutoFlushBus(value);
    }

    /// <summary>
    /// The expected delay needed for elements to spin up to the
    /// PLAYING state expressed in nanoseconds.
    /// see gst_pipeline_set_delay() for more information on this option.
    /// </summary>
    public Gst.ClockTime Delay
    {
        get => GetDelay();
        set => SetDelay(value);
    }

    /// <summary>Latency to configure on the pipeline. See gst_pipeline_set_latency().</summary>
    public Gst.ClockTime Latency
    {
        get => GetLatency();
        set => SetLatency(value);
    }

    /// <summary>The <c>gst_pipeline_new</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_new")]
    private static partial nint GstPipelineNew(byte* name);

    /// <summary>The <c>gst_pipeline_auto_clock</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_auto_clock")]
    private static partial void GstPipelineAutoClock(nint pipeline);

    /// <summary>The <c>gst_pipeline_get_auto_flush_bus</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_get_auto_flush_bus")]
    private static partial int GstPipelineGetAutoFlushBus(nint pipeline);

    /// <summary>The <c>gst_pipeline_get_bus</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_get_bus")]
    private static partial nint GstPipelineGetBus(nint pipeline);

    /// <summary>The <c>gst_pipeline_get_configured_latency</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_get_configured_latency")]
    private static partial ulong GstPipelineGetConfiguredLatency(nint pipeline);

    /// <summary>The <c>gst_pipeline_get_delay</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_get_delay")]
    private static partial ulong GstPipelineGetDelay(nint pipeline);

    /// <summary>The <c>gst_pipeline_get_latency</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_get_latency")]
    private static partial ulong GstPipelineGetLatency(nint pipeline);

    /// <summary>The <c>gst_pipeline_get_pipeline_clock</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_get_pipeline_clock")]
    private static partial nint GstPipelineGetPipelineClock(nint pipeline);

    /// <summary>The <c>gst_pipeline_is_live</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_is_live")]
    private static partial int GstPipelineIsLive(nint pipeline);

    /// <summary>The <c>gst_pipeline_set_auto_flush_bus</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_set_auto_flush_bus")]
    private static partial void GstPipelineSetAutoFlushBus(nint pipeline, int autoFlush);

    /// <summary>The <c>gst_pipeline_set_delay</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_set_delay")]
    private static partial void GstPipelineSetDelay(nint pipeline, ulong delay);

    /// <summary>The <c>gst_pipeline_set_latency</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_set_latency")]
    private static partial void GstPipelineSetLatency(nint pipeline, ulong latency);

    /// <summary>The <c>gst_pipeline_use_clock</c> entry point.</summary>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_use_clock")]
    private static partial void GstPipelineUseClock(nint pipeline, nint clock);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstPipeline</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("Gst", EntryPoint = "gst_pipeline_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new Pipeline(handle, transfer);
}
