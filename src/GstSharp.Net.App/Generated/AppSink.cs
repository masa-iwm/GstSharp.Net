// <auto-generated/>
// Generated by GstSharp.Generator from GstApp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.App;

/// <summary>
/// Appsink is a sink plugin that supports many different methods for making
/// the application get a handle on the GStreamer data in a pipeline. Unlike
/// most GStreamer elements, Appsink provides external API functions.
/// </summary>
/// <remarks>
/// <para>
/// appsink can be used by linking to the gstappsink.h header file to access the
/// methods or by using the appsink action signals and properties.
/// </para>
/// <para>
/// The normal way of retrieving samples from appsink is by using the
/// gst_app_sink_pull_sample() and gst_app_sink_pull_preroll() methods.
/// These methods block until a sample becomes available in the sink or when the
/// sink is shut down or reaches EOS. There are also timed variants of these
/// methods, gst_app_sink_try_pull_sample() and gst_app_sink_try_pull_preroll(),
/// which accept a timeout parameter to limit the amount of time to wait.
/// </para>
/// <para>
/// Appsink will internally use a queue to collect buffers from the streaming
/// thread. If the application is not pulling samples fast enough, this queue
/// will consume a lot of memory over time. The "max-buffers", "max-time" and "max-bytes"
/// properties can be used to limit the queue size. The "leaky-type" property controls whether the
/// streaming thread blocks or if older buffers are dropped when the maximum
/// queue size is reached. Note that blocking the streaming thread can negatively
/// affect real-time performance and should be avoided.
/// </para>
/// <para>
/// If a blocking behaviour is not desirable, setting the "emit-signals" property
/// to %TRUE will make appsink emit the "new-sample" and "new-preroll" signals
/// when a sample can be pulled without blocking.
/// </para>
/// <para>
/// The "caps" property on appsink can be used to control the formats that
/// appsink can receive. This property can contain non-fixed caps, the format of
/// the pulled samples can be obtained by getting the sample caps.
/// </para>
/// <para>
/// If one of the pull-preroll or pull-sample methods return %NULL, the appsink
/// is stopped or in the EOS state. You can check for the EOS state with the
/// "eos" property or with the gst_app_sink_is_eos() method.
/// </para>
/// <para>
/// The eos signal can also be used to be informed when the EOS state is reached
/// to avoid polling.
/// </para>
/// </remarks>
public unsafe partial class AppSink : Gst.Base.BaseSink, Gst.IURIHandler
{
    /// <summary>Wraps a native <c>GstAppSink</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    internal AppSink(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>Check if @appsink supports buffer lists.</summary>
    /// <returns>%TRUE if @appsink supports buffer lists.</returns>
    public bool GetBufferListSupport()
    {
        int nativeResult = GstAppSinkGetBufferListSupport(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Get the configured caps on @appsink.</summary>
    /// <returns>the #GstCaps accepted by the sink. gst_caps_unref() after usage.</returns>
    public Gst.Caps? GetCaps()
    {
        nint nativeResult = GstAppSinkGetCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the number of currently queued buffers inside @appsink.</summary>
    /// <returns>The number of currently queued buffers.</returns>
    public ulong GetCurrentLevelBuffers()
    {
        ulong nativeResult = GstAppSinkGetCurrentLevelBuffers(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the number of currently queued bytes inside @appsink.</summary>
    /// <returns>The number of currently queued bytes.</returns>
    public ulong GetCurrentLevelBytes()
    {
        ulong nativeResult = GstAppSinkGetCurrentLevelBytes(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the amount of currently queued time inside @appsink.</summary>
    /// <returns>The amount of currently queued time.</returns>
    public Gst.ClockTime GetCurrentLevelTime()
    {
        ulong nativeResult = GstAppSinkGetCurrentLevelTime(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Check if @appsink will drop old buffers when the maximum amount of queued
    /// data is reached (meaning max buffers, time or bytes limit, whichever is hit first).
    /// </summary>
    /// <returns>
    /// %TRUE if @appsink is dropping old buffers when the queue is
    /// filled.
    /// </returns>
    [Obsolete("Use gst_app_src_get_leaky_type() instead. (deprecated since 1.28)")]
    public bool GetDrop()
    {
        int nativeResult = GstAppSinkGetDrop(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Check if appsink will emit the "new-preroll" and "new-sample" signals.</summary>
    /// <returns>
    /// %TRUE if @appsink is emitting the "new-preroll" and "new-sample"
    /// signals.
    /// </returns>
    public bool GetEmitSignals()
    {
        int nativeResult = GstAppSinkGetEmitSignals(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Returns the currently set #GstAppLeakyType. See gst_app_sink_set_leaky_type()
    /// for more details.
    /// </summary>
    /// <returns>The currently set #GstAppLeakyType.</returns>
    public Gst.App.AppLeakyType GetLeakyType()
    {
        int nativeResult = GstAppSinkGetLeakyType(Handle);
        System.GC.KeepAlive(this);
        return (Gst.App.AppLeakyType)nativeResult;
    }

    /// <summary>Get the maximum amount of buffers that can be queued in @appsink.</summary>
    /// <returns>The maximum amount of buffers that can be queued.</returns>
    public uint GetMaxBuffers()
    {
        uint nativeResult = GstAppSinkGetMaxBuffers(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the maximum total size, in bytes, that can be queued in @appsink.</summary>
    /// <returns>The maximum amount of bytes that can be queued</returns>
    public ulong GetMaxBytes()
    {
        ulong nativeResult = GstAppSinkGetMaxBytes(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the maximum total duration that can be queued in @appsink.</summary>
    /// <returns>The maximum total duration that can be queued.</returns>
    public Gst.ClockTime GetMaxTime()
    {
        ulong nativeResult = GstAppSinkGetMaxTime(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Check if @appsink will wait for all buffers to be consumed when an EOS is
    /// received.
    /// </summary>
    /// <returns>
    /// %TRUE if @appsink will wait for all buffers to be consumed when an
    /// EOS is received.
    /// </returns>
    public bool GetWaitOnEos()
    {
        int nativeResult = GstAppSinkGetWaitOnEos(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Check if @appsink is EOS, which is when no more samples can be pulled because
    /// an EOS event was received.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function also returns %TRUE when the appsink is not in the PAUSED or
    /// PLAYING state.
    /// </para>
    /// </remarks>
    /// <returns>%TRUE if no more samples can be pulled and the appsink is EOS.</returns>
    public bool IsEos()
    {
        int nativeResult = GstAppSinkIsEos(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>
    /// Get the last preroll sample in @appsink. This was the sample that caused the
    /// appsink to preroll in the PAUSED state.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function is typically used when dealing with a pipeline in the PAUSED
    /// state. Calling this function after doing a seek will give the sample right
    /// after the seek position.
    /// </para>
    /// <para>
    /// Calling this function will clear the internal reference to the preroll
    /// buffer.
    /// </para>
    /// <para>
    /// Note that the preroll sample will also be returned as the first sample
    /// when calling gst_app_sink_pull_sample().
    /// </para>
    /// <para>
    /// If an EOS event was received before any buffers, this function returns
    /// %NULL. Use gst_app_sink_is_eos () to check for the EOS condition.
    /// </para>
    /// <para>
    /// This function blocks until a preroll sample or EOS is received or the appsink
    /// element is set to the READY/NULL state.
    /// </para>
    /// </remarks>
    /// <returns>
    /// a #GstSample or NULL when the appsink is stopped or EOS.
    ///          Call gst_sample_unref() after usage.
    /// </returns>
    public Gst.Sample? PullPreroll()
    {
        nint nativeResult = GstAppSinkPullPreroll(Handle);
        System.GC.KeepAlive(this);
        return Gst.Sample.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// This function blocks until a sample or EOS becomes available or the appsink
    /// element is set to the READY/NULL state.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function will only return samples when the appsink is in the PLAYING
    /// state. All rendered buffers will be put in a queue so that the application
    /// can pull samples at its own rate. Note that when the application does not
    /// pull samples fast enough, the queued buffers could consume a lot of memory,
    /// especially when dealing with raw video frames.
    /// </para>
    /// <para>
    /// If an EOS event was received before any buffers, this function returns
    /// %NULL. Use gst_app_sink_is_eos () to check for the EOS condition.
    /// </para>
    /// </remarks>
    /// <returns>
    /// a #GstSample or NULL when the appsink is stopped or EOS.
    ///          Call gst_sample_unref() after usage.
    /// </returns>
    public Gst.Sample? PullSample()
    {
        nint nativeResult = GstAppSinkPullSample(Handle);
        System.GC.KeepAlive(this);
        return Gst.Sample.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Instruct @appsink to enable or disable buffer list support.</summary>
    /// <remarks>
    /// <para>
    /// For backwards-compatibility reasons applications need to opt in
    /// to indicate that they will be able to handle buffer lists.
    /// </para>
    /// </remarks>
    /// <param name="enableLists">The <c>enableLists</c> argument.</param>
    public void SetBufferListSupport(bool enableLists)
    {
        GstAppSinkSetBufferListSupport(Handle, enableLists ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the capabilities on the appsink element.  This function takes
    /// a copy of the caps structure. After calling this method, the sink will only
    /// accept caps that match @caps. If @caps is non-fixed, or incomplete,
    /// you must check the caps on the samples to get the actual used caps.
    /// </summary>
    /// <param name="caps">The <c>caps</c> argument.</param>
    public void SetCaps(Gst.Caps? caps)
    {
        GstAppSinkSetCaps(Handle, caps is null ? 0 : caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
    }

    /// <summary>
    /// Instruct @appsink to drop old buffers when the maximum amount of queued
    /// data is reached, that is, when any configured limit is hit (max-buffers, max-time or max-bytes).
    /// </summary>
    /// <param name="drop">The <c>drop</c> argument.</param>
    [Obsolete("Use gst_app_src_get_leaky_type() instead. (deprecated since 1.28)")]
    public void SetDrop(bool drop)
    {
        GstAppSinkSetDrop(Handle, drop ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Make appsink emit the "new-preroll" and "new-sample" signals. This option is
    /// by default disabled because signal emission is expensive and unneeded when
    /// the application prefers to operate in pull mode.
    /// </summary>
    /// <param name="emit">The <c>emit</c> argument.</param>
    public void SetEmitSignals(bool emit)
    {
        GstAppSinkSetEmitSignals(Handle, emit ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// When set to any other value than GST_APP_LEAKY_TYPE_NONE then the appsink
    /// will drop any buffers that are pushed into it once its internal queue is
    /// full. The selected type defines whether to drop the oldest or new
    /// buffers.
    /// </summary>
    /// <param name="leaky">The <c>leaky</c> argument.</param>
    public void SetLeakyType(Gst.App.AppLeakyType leaky)
    {
        GstAppSinkSetLeakyType(Handle, (int)leaky);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the maximum amount of buffers that can be queued in @appsink. After this
    /// amount of buffers are queued in appsink, any more buffers will block upstream
    /// elements until a sample is pulled from @appsink, unless 'drop' is set, in which
    /// case new buffers will be discarded.
    /// </summary>
    /// <param name="max">The <c>max</c> argument.</param>
    public void SetMaxBuffers(uint max)
    {
        GstAppSinkSetMaxBuffers(Handle, max);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the maximum total size that can be queued in @appsink. After this
    /// amount of buffers are queued in appsink, any more buffers will block upstream
    /// elements until a sample is pulled from @appsink, unless 'drop' is set, in which
    /// case new buffers will be discarded.
    /// </summary>
    /// <param name="max">The <c>max</c> argument.</param>
    public void SetMaxBytes(ulong max)
    {
        GstAppSinkSetMaxBytes(Handle, max);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the maximum total duration that can be queued in @appsink. After this
    /// amount of buffers are queued in appsink, any more buffers will block upstream
    /// elements until a sample is pulled from @appsink, unless 'drop' is set, in which
    /// case new buffers will be discarded.
    /// </summary>
    /// <param name="max">The <c>max</c> argument.</param>
    public void SetMaxTime(Gst.ClockTime max)
    {
        GstAppSinkSetMaxTime(Handle, max.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>Instruct @appsink to wait for all buffers to be consumed when an EOS is received.</summary>
    /// <param name="wait">The <c>wait</c> argument.</param>
    public void SetWaitOnEos(bool wait)
    {
        GstAppSinkSetWaitOnEos(Handle, wait ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Get the last preroll sample in @appsink. This was the sample that caused the
    /// appsink to preroll in the PAUSED state.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function is typically used when dealing with a pipeline in the PAUSED
    /// state. Calling this function after doing a seek will give the sample right
    /// after the seek position.
    /// </para>
    /// <para>
    /// Calling this function will clear the internal reference to the preroll
    /// buffer.
    /// </para>
    /// <para>
    /// Note that the preroll sample will also be returned as the first sample
    /// when calling gst_app_sink_pull_sample().
    /// </para>
    /// <para>
    /// If an EOS event was received before any buffers or the timeout expires,
    /// this function returns %NULL. Use gst_app_sink_is_eos () to check for the EOS
    /// condition.
    /// </para>
    /// <para>
    /// This function blocks until a preroll sample or EOS is received, the appsink
    /// element is set to the READY/NULL state, or the timeout expires.
    /// </para>
    /// </remarks>
    /// <param name="timeout">The <c>timeout</c> argument.</param>
    /// <returns>
    /// a #GstSample or NULL when the appsink is stopped or EOS or the timeout expires.
    ///          Call gst_sample_unref() after usage.
    /// </returns>
    public Gst.Sample? TryPullPreroll(Gst.ClockTime timeout)
    {
        nint nativeResult = GstAppSinkTryPullPreroll(Handle, timeout.Nanoseconds);
        System.GC.KeepAlive(this);
        return Gst.Sample.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>
    /// This function blocks until a sample or EOS becomes available or the appsink
    /// element is set to the READY/NULL state or the timeout expires.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function will only return samples when the appsink is in the PLAYING
    /// state. All rendered buffers will be put in a queue so that the application
    /// can pull samples at its own rate. Note that when the application does not
    /// pull samples fast enough, the queued buffers could consume a lot of memory,
    /// especially when dealing with raw video frames.
    /// </para>
    /// <para>
    /// If an EOS event was received before any buffers or the timeout expires,
    /// this function returns %NULL. Use gst_app_sink_is_eos () to check for the EOS
    /// condition.
    /// </para>
    /// </remarks>
    /// <param name="timeout">The <c>timeout</c> argument.</param>
    /// <returns>
    /// a #GstSample or NULL when the appsink is stopped or EOS or the timeout expires.
    ///          Call gst_sample_unref() after usage.
    /// </returns>
    public Gst.Sample? TryPullSample(Gst.ClockTime timeout)
    {
        nint nativeResult = GstAppSinkTryPullSample(Handle, timeout.Nanoseconds);
        System.GC.KeepAlive(this);
        return Gst.Sample.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>The number of currently queued buffers inside appsink.</summary>
    public ulong CurrentLevelBuffers => GetCurrentLevelBuffers();

    /// <summary>The number of currently queued bytes inside appsink.</summary>
    public ulong CurrentLevelBytes => GetCurrentLevelBytes();

    /// <summary>The amount of currently queued time inside appsink.</summary>
    public Gst.ClockTime CurrentLevelTime => GetCurrentLevelTime();

    /// <summary>Drop old buffers when the buffer queue is filled.</summary>
    [Obsolete("Use \"leaky-type\" property instead. (deprecated since 1.28)")]
    public bool Drop
    {
        get => GetDrop();
        set => SetDrop(value);
    }

    /// <summary>The <c>emit-signals</c> property.</summary>
    public bool EmitSignals
    {
        get => GetEmitSignals();
        set => SetEmitSignals(value);
    }

    /// <summary>
    /// When set to any other value than GST_APP_LEAKY_TYPE_NONE then the appsink
    /// will drop any buffers that are pushed into it once its internal queue is
    /// full. The selected type defines whether to drop the oldest or new
    /// buffers.
    /// </summary>
    public Gst.App.AppLeakyType LeakyType
    {
        get => GetLeakyType();
        set => SetLeakyType(value);
    }

    /// <summary>Maximum amount of buffers in the queue (0 = unlimited).</summary>
    public uint MaxBuffers
    {
        get => GetMaxBuffers();
        set => SetMaxBuffers(value);
    }

    /// <summary>Maximum amount of bytes in the queue (0 = unlimited)</summary>
    public ulong MaxBytes
    {
        get => GetMaxBytes();
        set => SetMaxBytes(value);
    }

    /// <summary>Maximum total duration of data in the queue (0 = unlimited)</summary>
    public Gst.ClockTime MaxTime
    {
        get => GetMaxTime();
        set => SetMaxTime(value);
    }

    /// <summary>Wait for all buffers to be processed after receiving an EOS.</summary>
    /// <remarks>
    /// <para>
    /// In cases where it is uncertain if an @appsink will have a consumer for its buffers
    /// when it receives an EOS, set to %FALSE to ensure that the @appsink will not hang.
    /// </para>
    /// </remarks>
    public bool WaitOnEos
    {
        get => GetWaitOnEos();
        set => SetWaitOnEos(value);
    }

    /// <summary>
    /// Signal that the end-of-stream has been reached. This signal is emitted from
    /// the streaming thread.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler Eos
    {
        add => Gst.App.SignalConnections.Add(this, "eos", (nint)(delegate* unmanaged[Cdecl]<nint, nint, void>)&EosTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "eos", value);
    }

    /// <summary>The native handler of the <c>eos</c> signal of <c>GstAppSink</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void EosTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler>(userData) is not { } handler)
            {
                return;
            }

            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The handler of the <c>new-preroll</c> signal of <c>GstAppSink</c>.</summary>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>The result the emission collects.</returns>
    public delegate Gst.FlowReturn NewPrerollHandler(object? sender, System.EventArgs args);

    /// <summary>Signal that a new preroll sample is available.</summary>
    /// <remarks>
    /// <para>
    /// This signal is emitted from the streaming thread and only when the
    /// "emit-signals" property is %TRUE.
    /// </para>
    /// <para>
    /// The new preroll sample can be retrieved with the "pull-preroll" action
    /// signal or gst_app_sink_pull_preroll() either from this signal callback
    /// or from any other thread.
    /// </para>
    /// <para>
    /// Note that this signal is only emitted when the "emit-signals" property is
    /// set to %TRUE, which it is not by default for performance reasons.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.App.AppSink.NewPrerollHandler NewPreroll
    {
        add => Gst.App.SignalConnections.Add(this, "new-preroll", (nint)(delegate* unmanaged[Cdecl]<nint, nint, int>)&NewPrerollTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "new-preroll", value);
    }

    /// <summary>The native handler of the <c>new-preroll</c> signal of <c>GstAppSink</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static int NewPrerollTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.App.AppSink.NewPrerollHandler>(userData) is not { } handler)
            {
                return (int)Gst.FlowReturn.Error;
            }

            Gst.FlowReturn result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
            return (int)result;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return (int)Gst.FlowReturn.Error;
        }
    }

    /// <summary>The handler of the <c>new-sample</c> signal of <c>GstAppSink</c>.</summary>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>The result the emission collects.</returns>
    public delegate Gst.FlowReturn NewSampleHandler(object? sender, System.EventArgs args);

    /// <summary>Signal that a new sample is available.</summary>
    /// <remarks>
    /// <para>
    /// This signal is emitted from the streaming thread and only when the
    /// "emit-signals" property is %TRUE.
    /// </para>
    /// <para>
    /// The new sample can be retrieved with the "pull-sample" action
    /// signal or gst_app_sink_pull_sample() either from this signal callback
    /// or from any other thread.
    /// </para>
    /// <para>
    /// Note that this signal is only emitted when the "emit-signals" property is
    /// set to %TRUE, which it is not by default for performance reasons.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.App.AppSink.NewSampleHandler NewSample
    {
        add => Gst.App.SignalConnections.Add(this, "new-sample", (nint)(delegate* unmanaged[Cdecl]<nint, nint, int>)&NewSampleTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "new-sample", value);
    }

    /// <summary>The native handler of the <c>new-sample</c> signal of <c>GstAppSink</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static int NewSampleTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.App.AppSink.NewSampleHandler>(userData) is not { } handler)
            {
                return (int)Gst.FlowReturn.Error;
            }

            Gst.FlowReturn result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
            return (int)result;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return (int)Gst.FlowReturn.Error;
        }
    }

    /// <summary>The handler of the <c>new-serialized-event</c> signal of <c>GstAppSink</c>.</summary>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>The result the emission collects.</returns>
    public delegate bool NewSerializedEventHandler(object? sender, System.EventArgs args);

    /// <summary>Signal that a new downstream serialized event is available.</summary>
    /// <remarks>
    /// <para>
    /// This signal is emitted from the streaming thread and only when the
    /// "emit-signals" property is %TRUE.
    /// </para>
    /// <para>
    /// The new event can be retrieved with the "try-pull-object" action
    /// signal or gst_app_sink_pull_object() either from this signal callback
    /// or from any other thread.
    /// </para>
    /// <para>
    /// EOS will not be notified using this signal, use #GstAppSink::eos instead.
    /// EOS cannot be pulled either, use gst_app_sink_is_eos() to check for it.
    /// </para>
    /// <para>
    /// Note that this signal is only emitted when the "emit-signals" property is
    /// set to %TRUE, which it is not by default for performance reasons.
    /// </para>
    /// <para>
    /// The callback should return %TRUE if the event has been handled, which will
    /// skip basesink handling of the event, %FALSE otherwise.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.App.AppSink.NewSerializedEventHandler NewSerializedEvent
    {
        add => Gst.App.SignalConnections.Add(this, "new-serialized-event", (nint)(delegate* unmanaged[Cdecl]<nint, nint, int>)&NewSerializedEventTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "new-serialized-event", value);
    }

    /// <summary>The native handler of the <c>new-serialized-event</c> signal of <c>GstAppSink</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static int NewSerializedEventTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.App.AppSink.NewSerializedEventHandler>(userData) is not { } handler)
            {
                return default;
            }

            bool result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
            return result ? 1 : 0;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The arguments of the <c>propose-allocation</c> signal of <c>GstAppSink</c>.</summary>
    public sealed class ProposeAllocationSignalArgs
    {
        /// <summary>Initializes a new instance of the <see cref="ProposeAllocationSignalArgs"/> class.</summary>
        /// <param name="query">the allocation query</param>
        internal ProposeAllocationSignalArgs(Gst.Query query)
        {
            Query = query;
        }

        /// <summary>the allocation query</summary>
        /// <remarks>
        /// The value is only valid while the handler runs: the wrapper is disposed
        /// once it returns. Read out of it what is needed, or copy it where the
        /// type offers a copy.
        /// </remarks>
        public Gst.Query Query { get; }
    }

    /// <summary>The handler of the <c>propose-allocation</c> signal of <c>GstAppSink</c>.</summary>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>The result the emission collects.</returns>
    public delegate bool ProposeAllocationHandler(object? sender, Gst.App.AppSink.ProposeAllocationSignalArgs args);

    /// <summary>Signal that a new propose_allocation query is available.</summary>
    /// <remarks>
    /// <para>
    /// This signal is emitted from the streaming thread and only when the
    /// "emit-signals" property is %TRUE.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.App.AppSink.ProposeAllocationHandler ProposeAllocation
    {
        add => Gst.App.SignalConnections.Add(this, "propose-allocation", (nint)(delegate* unmanaged[Cdecl]<nint, nint, nint, int>)&ProposeAllocationTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "propose-allocation", value);
    }

    /// <summary>The native handler of the <c>propose-allocation</c> signal of <c>GstAppSink</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static int ProposeAllocationTrampoline(nint instance, nint query, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.App.AppSink.ProposeAllocationHandler>(userData) is not { } handler)
            {
                return default;
            }

            using Gst.Query queryValue = Gst.Query.FromNative(query, Gst.Interop.Transfer.None)
                ?? throw new InvalidOperationException("The propose-allocation signal of GstAppSink passed no query.");
            bool result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.App.AppSink.ProposeAllocationSignalArgs(queryValue));
            return result ? 1 : 0;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The <c>gst_app_sink_get_buffer_list_support</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_buffer_list_support")]
    private static partial int GstAppSinkGetBufferListSupport(nint appsink);

    /// <summary>The <c>gst_app_sink_get_caps</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_caps")]
    private static partial nint GstAppSinkGetCaps(nint appsink);

    /// <summary>The <c>gst_app_sink_get_current_level_buffers</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_current_level_buffers")]
    private static partial ulong GstAppSinkGetCurrentLevelBuffers(nint appsink);

    /// <summary>The <c>gst_app_sink_get_current_level_bytes</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_current_level_bytes")]
    private static partial ulong GstAppSinkGetCurrentLevelBytes(nint appsink);

    /// <summary>The <c>gst_app_sink_get_current_level_time</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_current_level_time")]
    private static partial ulong GstAppSinkGetCurrentLevelTime(nint appsink);

    /// <summary>The <c>gst_app_sink_get_drop</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_drop")]
    private static partial int GstAppSinkGetDrop(nint appsink);

    /// <summary>The <c>gst_app_sink_get_emit_signals</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_emit_signals")]
    private static partial int GstAppSinkGetEmitSignals(nint appsink);

    /// <summary>The <c>gst_app_sink_get_leaky_type</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_leaky_type")]
    private static partial int GstAppSinkGetLeakyType(nint appsink);

    /// <summary>The <c>gst_app_sink_get_max_buffers</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_max_buffers")]
    private static partial uint GstAppSinkGetMaxBuffers(nint appsink);

    /// <summary>The <c>gst_app_sink_get_max_bytes</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_max_bytes")]
    private static partial ulong GstAppSinkGetMaxBytes(nint appsink);

    /// <summary>The <c>gst_app_sink_get_max_time</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_max_time")]
    private static partial ulong GstAppSinkGetMaxTime(nint appsink);

    /// <summary>The <c>gst_app_sink_get_wait_on_eos</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_wait_on_eos")]
    private static partial int GstAppSinkGetWaitOnEos(nint appsink);

    /// <summary>The <c>gst_app_sink_is_eos</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_is_eos")]
    private static partial int GstAppSinkIsEos(nint appsink);

    /// <summary>The <c>gst_app_sink_pull_preroll</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_pull_preroll")]
    private static partial nint GstAppSinkPullPreroll(nint appsink);

    /// <summary>The <c>gst_app_sink_pull_sample</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_pull_sample")]
    private static partial nint GstAppSinkPullSample(nint appsink);

    /// <summary>The <c>gst_app_sink_set_buffer_list_support</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_buffer_list_support")]
    private static partial void GstAppSinkSetBufferListSupport(nint appsink, int enableLists);

    /// <summary>The <c>gst_app_sink_set_caps</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_caps")]
    private static partial void GstAppSinkSetCaps(nint appsink, nint caps);

    /// <summary>The <c>gst_app_sink_set_drop</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_drop")]
    private static partial void GstAppSinkSetDrop(nint appsink, int drop);

    /// <summary>The <c>gst_app_sink_set_emit_signals</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_emit_signals")]
    private static partial void GstAppSinkSetEmitSignals(nint appsink, int emit);

    /// <summary>The <c>gst_app_sink_set_leaky_type</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_leaky_type")]
    private static partial void GstAppSinkSetLeakyType(nint appsink, int leaky);

    /// <summary>The <c>gst_app_sink_set_max_buffers</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_max_buffers")]
    private static partial void GstAppSinkSetMaxBuffers(nint appsink, uint max);

    /// <summary>The <c>gst_app_sink_set_max_bytes</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_max_bytes")]
    private static partial void GstAppSinkSetMaxBytes(nint appsink, ulong max);

    /// <summary>The <c>gst_app_sink_set_max_time</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_max_time")]
    private static partial void GstAppSinkSetMaxTime(nint appsink, ulong max);

    /// <summary>The <c>gst_app_sink_set_wait_on_eos</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_set_wait_on_eos")]
    private static partial void GstAppSinkSetWaitOnEos(nint appsink, int wait);

    /// <summary>The <c>gst_app_sink_try_pull_preroll</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_try_pull_preroll")]
    private static partial nint GstAppSinkTryPullPreroll(nint appsink, ulong timeout);

    /// <summary>The <c>gst_app_sink_try_pull_sample</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_try_pull_sample")]
    private static partial nint GstAppSinkTryPullSample(nint appsink, ulong timeout);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAppSink</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstApp", EntryPoint = "gst_app_sink_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new AppSink(handle, transfer);
}
