// <auto-generated/>
// Generated by GstSharp.Generator from GstApp-1.0.gir. Do not edit.

#nullable enable

using System;
using System.Runtime.InteropServices;

namespace Gst.App;

/// <summary>
/// The appsrc element can be used by applications to insert data into a
/// GStreamer pipeline. Unlike most GStreamer elements, appsrc provides
/// external API functions.
/// </summary>
/// <remarks>
/// <para>
/// appsrc can be used by linking with the libgstapp library to access the
/// methods directly or by using the appsrc action signals.
/// </para>
/// <para>
/// Before operating appsrc, the caps property must be set to fixed caps
/// describing the format of the data that will be pushed with appsrc. An
/// exception to this is when pushing buffers with unknown caps, in which case no
/// caps should be set. This is typically true of file-like sources that push raw
/// byte buffers. If you don't want to explicitly set the caps, you can use
/// gst_app_src_push_sample. This method gets the caps associated with the
/// sample and sets them on the appsrc replacing any previously set caps (if
/// different from sample's caps).
/// </para>
/// <para>
/// The main way of handing data to the appsrc element is by calling the
/// gst_app_src_push_buffer() method or by emitting the push-buffer action signal.
/// This will put the buffer onto a queue from which appsrc will read from in its
/// streaming thread. It is important to note that data transport will not happen
/// from the thread that performed the push-buffer call.
/// </para>
/// <para>
/// The "max-bytes", "max-buffers" and "max-time" properties control how much
/// data can be queued in appsrc before appsrc considers the queue full. A
/// filled internal queue will always signal the "enough-data" signal, which
/// signals the application that it should stop pushing data into appsrc. The
/// "block" property will cause appsrc to block the push-buffer method until
/// free data becomes available again.
/// </para>
/// <para>
/// When the internal queue is running out of data, the "need-data" signal is
/// emitted, which signals the application that it should start pushing more data
/// into appsrc.
/// </para>
/// <para>
/// In addition to the "need-data" and "enough-data" signals, appsrc can emit the
/// "seek-data" signal when the "stream-mode" property is set to "seekable" or
/// "random-access". The signal argument will contain the new desired position in
/// the stream expressed in the unit set with the "format" property. After
/// receiving the seek-data signal, the application should push-buffers from the
/// new position.
/// </para>
/// <para>
/// These signals allow the application to operate the appsrc in two different
/// ways:
/// </para>
/// <para>
/// The push mode, in which the application repeatedly calls the push-buffer/push-sample
/// method with a new buffer/sample. Optionally, the queue size in the appsrc
/// can be controlled with the enough-data and need-data signals by respectively
/// stopping/starting the push-buffer/push-sample calls. This is a typical
/// mode of operation for the stream-type "stream" and "seekable". Use this
/// mode when implementing various network protocols or hardware devices.
/// </para>
/// <para>
/// The pull mode, in which the need-data signal triggers the next push-buffer call.
/// This mode is typically used in the "random-access" stream-type. Use this
/// mode for file access or other randomly accessible sources. In this mode, a
/// buffer of exactly the amount of bytes given by the need-data signal should be
/// pushed into appsrc.
/// </para>
/// <para>
/// In all modes, the size property on appsrc should contain the total stream
/// size in bytes. Setting this property is mandatory in the random-access mode.
/// For the stream and seekable modes, setting this property is optional but
/// recommended.
/// </para>
/// <para>
/// When the application has finished pushing data into appsrc, it should call
/// gst_app_src_end_of_stream() or emit the end-of-stream action signal. After
/// this call, no more buffers can be pushed into appsrc until a flushing seek
/// occurs or the state of the appsrc has gone through READY.
/// </para>
/// </remarks>
public unsafe partial class AppSrc : Gst.Base.BaseSrc, Gst.IURIHandler
{
    /// <summary>Wraps a native <c>GstAppSrc</c>.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <remarks>
    /// <para>
    /// This is where a binding module attaches its own wrappers: derive from
    /// this class to wrap a native type that derives from
    /// <c>GstAppSrc</c> and has no binding of its own here. See
    /// <see href="https://github.com/masa-iwm/GstSharp.Net/blob/main/docs/modules.md">docs/modules.md</see>.
    /// </para>
    /// <para>
    /// <b>Call it from a type-registry factory, never from application code.</b>
    /// GObject wrappers are interned, and wrapping a handle that a live wrapper
    /// already holds throws. Expose a <c>CreateWrapper</c> to
    /// <see cref="Gst.Interop.ModuleTypeEntry"/>, keep the constructor out of
    /// your public surface, and write your own factories through
    /// <see cref="Gst.GObject.Object.FromNative{T}(nint, Gst.Interop.Transfer)"/>.
    /// </para>
    /// <para>
    /// <b>Pass the transfer the C function documented.</b> The wrapper owns one
    /// reference either way, so getting it wrong leaks the object or releases a
    /// reference that was never handed over.
    /// </para>
    /// </remarks>
    protected AppSrc(nint handle, Gst.Interop.Transfer transfer)
        : base(handle, transfer)
    {
    }

    /// <summary>
    /// Indicates to the appsrc element that the last buffer queued in the
    /// element is the last buffer of the stream.
    /// </summary>
    /// <returns>
    /// #GST_FLOW_OK when the EOS was successfully queued.
    /// #GST_FLOW_FLUSHING when @appsrc is not PAUSED or PLAYING.
    /// </returns>
    public Gst.FlowReturn EndOfStream()
    {
        int nativeResult = GstAppSrcEndOfStream(Handle);
        System.GC.KeepAlive(this);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>Get the configured caps on @appsrc.</summary>
    /// <returns>the #GstCaps produced by the source. gst_caps_unref() after usage.</returns>
    public Gst.Caps? GetCaps()
    {
        nint nativeResult = GstAppSrcGetCaps(Handle);
        System.GC.KeepAlive(this);
        return Gst.Caps.FromNative(nativeResult, Gst.Interop.Transfer.Full);
    }

    /// <summary>Get the number of currently queued buffers inside @appsrc.</summary>
    /// <returns>The number of currently queued buffers.</returns>
    public ulong GetCurrentLevelBuffers()
    {
        ulong nativeResult = GstAppSrcGetCurrentLevelBuffers(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the number of currently queued bytes inside @appsrc.</summary>
    /// <returns>The number of currently queued bytes.</returns>
    public ulong GetCurrentLevelBytes()
    {
        ulong nativeResult = GstAppSrcGetCurrentLevelBytes(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the amount of currently queued time inside @appsrc.</summary>
    /// <returns>The amount of currently queued time.</returns>
    public Gst.ClockTime GetCurrentLevelTime()
    {
        ulong nativeResult = GstAppSrcGetCurrentLevelTime(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the duration of the stream in nanoseconds. A value of GST_CLOCK_TIME_NONE means that the duration is
    /// not known.
    /// </summary>
    /// <returns>the duration of the stream previously set with gst_app_src_set_duration();</returns>
    public Gst.ClockTime GetDuration()
    {
        ulong nativeResult = GstAppSrcGetDuration(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>Check if appsrc will emit the "new-preroll" and "new-buffer" signals.</summary>
    /// <returns>
    /// %TRUE if @appsrc is emitting the "new-preroll" and "new-buffer"
    /// signals.
    /// </returns>
    public bool GetEmitSignals()
    {
        int nativeResult = GstAppSrcGetEmitSignals(Handle);
        System.GC.KeepAlive(this);
        return nativeResult != 0;
    }

    /// <summary>Retrieve the min and max latencies in @min and @max respectively.</summary>
    /// <param name="min">the min latency</param>
    /// <param name="max">the max latency</param>
    public void GetLatency(out ulong min, out ulong max)
    {
        ulong minNative = default;
        ulong maxNative = default;
        GstAppSrcGetLatency(Handle, &minNative, &maxNative);
        System.GC.KeepAlive(this);
        min = minNative;
        max = maxNative;
    }

    /// <summary>
    /// Returns the currently set #GstAppLeakyType. See gst_app_src_set_leaky_type()
    /// for more details.
    /// </summary>
    /// <returns>The currently set #GstAppLeakyType.</returns>
    public Gst.App.AppLeakyType GetLeakyType()
    {
        int nativeResult = GstAppSrcGetLeakyType(Handle);
        System.GC.KeepAlive(this);
        return (Gst.App.AppLeakyType)nativeResult;
    }

    /// <summary>Get the maximum amount of buffers that can be queued in @appsrc.</summary>
    /// <returns>The maximum amount of buffers that can be queued.</returns>
    public ulong GetMaxBuffers()
    {
        ulong nativeResult = GstAppSrcGetMaxBuffers(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the maximum amount of bytes that can be queued in @appsrc.</summary>
    /// <returns>The maximum amount of bytes that can be queued.</returns>
    public ulong GetMaxBytes()
    {
        ulong nativeResult = GstAppSrcGetMaxBytes(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>Get the maximum amount of time that can be queued in @appsrc.</summary>
    /// <returns>The maximum amount of time that can be queued.</returns>
    public Gst.ClockTime GetMaxTime()
    {
        ulong nativeResult = GstAppSrcGetMaxTime(Handle);
        System.GC.KeepAlive(this);
        return new Gst.ClockTime(nativeResult);
    }

    /// <summary>
    /// Get the size of the stream in bytes. A value of -1 means that the size is
    /// not known.
    /// </summary>
    /// <returns>the size of the stream previously set with gst_app_src_set_size();</returns>
    public long GetSize()
    {
        long nativeResult = GstAppSrcGetSize(Handle);
        System.GC.KeepAlive(this);
        return nativeResult;
    }

    /// <summary>
    /// Get the stream type. Control the stream type of @appsrc
    /// with gst_app_src_set_stream_type().
    /// </summary>
    /// <returns>the stream type.</returns>
    public Gst.App.AppStreamType GetStreamType()
    {
        int nativeResult = GstAppSrcGetStreamType(Handle);
        System.GC.KeepAlive(this);
        return (Gst.App.AppStreamType)nativeResult;
    }

    /// <summary>
    /// Adds a buffer list to the queue of buffers and buffer lists that the
    /// appsrc element will push to its source pad.  This function takes ownership
    /// of @buffer_list.
    /// </summary>
    /// <remarks>
    /// <para>
    /// When the block property is TRUE, this function can block until free
    /// space becomes available in the queue.
    /// </para>
    /// <para>
    /// The <c>bufferList</c> parameter is <c>transfer-ownership="full"</c>: the call is
    /// handed a reference of its own and the wrapper is disposed afterwards, which
    /// leaves the native reference count exactly where the C call leaves it.
    /// <see cref="Gst.MiniObject.Dispose()"/> is idempotent, so a <c>using</c>
    /// declaration around the argument stays correct.
    /// </para>
    /// </remarks>
    /// <param name="bufferList">
    /// a #GstBufferList to push
    /// The call consumes it: <paramref name="bufferList"/> is disposed when this
    /// method returns, and using it afterwards throws <see cref="ObjectDisposedException"/>.
    /// </param>
    /// <returns>
    /// #GST_FLOW_OK when the buffer list was successfully queued.
    /// #GST_FLOW_FLUSHING when @appsrc is not PAUSED or PLAYING.
    /// #GST_FLOW_EOS when EOS occurred.
    /// </returns>
    /// <exception cref="ArgumentNullException">
    /// <paramref name="bufferList"/> is <see langword="null"/>.
    /// </exception>
    /// <exception cref="ObjectDisposedException">
    /// This wrapper or <paramref name="bufferList"/> was disposed.
    /// </exception>
    public Gst.FlowReturn PushBufferList(Gst.BufferList bufferList)
    {
        ArgumentNullException.ThrowIfNull(bufferList);
        nint instanceHandle = Handle;
        nint bufferListNative = bufferList.Handle;
        nint bufferListOwned = Gst.GstNative.MiniObjectRef(bufferListNative);
        int nativeResult = GstAppSrcPushBufferList(instanceHandle, bufferListOwned);
        System.GC.KeepAlive(this);
        bufferList.Dispose();
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Extract a buffer from the provided sample and adds it to the queue of
    /// buffers that the appsrc element will push to its source pad. Any
    /// previous caps that were set on appsrc will be replaced by the caps
    /// associated with the sample if not equal.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This function does not take ownership of the
    /// sample so the sample needs to be unreffed after calling this function.
    /// </para>
    /// <para>
    /// When the block property is TRUE, this function can block until free
    /// space becomes available in the queue.
    /// </para>
    /// </remarks>
    /// <param name="sample">
    /// a #GstSample from which buffer and caps may be
    /// extracted
    /// </param>
    /// <returns>
    /// #GST_FLOW_OK when the buffer was successfully queued.
    /// #GST_FLOW_FLUSHING when @appsrc is not PAUSED or PLAYING.
    /// #GST_FLOW_EOS when EOS occurred.
    /// </returns>
    public Gst.FlowReturn PushSample(Gst.Sample sample)
    {
        ArgumentNullException.ThrowIfNull(sample);
        int nativeResult = GstAppSrcPushSample(Handle, sample.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(sample);
        return (Gst.FlowReturn)nativeResult;
    }

    /// <summary>
    /// Set the capabilities on the appsrc element.  This function takes
    /// a copy of the caps structure. After calling this method, the source will
    /// only produce caps that match @caps. @caps must be fixed and the caps on the
    /// buffers must match the caps or left NULL.
    /// </summary>
    /// <param name="caps">caps to set</param>
    public new void SetCaps(Gst.Caps? caps)
    {
        GstAppSrcSetCaps(Handle, caps is null ? 0 : caps.Handle);
        System.GC.KeepAlive(this);
        System.GC.KeepAlive(caps);
    }

    /// <summary>
    /// Set the duration of the stream in nanoseconds. A value of GST_CLOCK_TIME_NONE means that the duration is
    /// not known.
    /// </summary>
    /// <param name="duration">the duration to set</param>
    public void SetDuration(Gst.ClockTime duration)
    {
        GstAppSrcSetDuration(Handle, duration.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Make appsrc emit the "new-preroll" and "new-buffer" signals. This option is
    /// by default disabled because signal emission is expensive and unneeded when
    /// the application prefers to operate in pull mode.
    /// </summary>
    /// <param name="emit">the new state</param>
    public void SetEmitSignals(bool emit)
    {
        GstAppSrcSetEmitSignals(Handle, emit ? 1 : 0);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Configure the @min and @max latency in @src. If @min is set to -1, the
    /// default latency calculations for pseudo-live sources will be used.
    /// </summary>
    /// <param name="min">the min latency</param>
    /// <param name="max">the max latency</param>
    public void SetLatency(ulong min, ulong max)
    {
        GstAppSrcSetLatency(Handle, min, max);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// When set to any other value than GST_APP_LEAKY_TYPE_NONE then the appsrc
    /// will drop any buffers that are pushed into it once its internal queue is
    /// full. The selected type defines whether to drop the oldest or new
    /// buffers.
    /// </summary>
    /// <param name="leaky">the #GstAppLeakyType</param>
    public void SetLeakyType(Gst.App.AppLeakyType leaky)
    {
        GstAppSrcSetLeakyType(Handle, (int)leaky);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the maximum amount of buffers that can be queued in @appsrc.
    /// After the maximum amount of buffers are queued, @appsrc will emit the
    /// "enough-data" signal.
    /// </summary>
    /// <param name="max">the maximum number of buffers to queue</param>
    public void SetMaxBuffers(ulong max)
    {
        GstAppSrcSetMaxBuffers(Handle, max);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the maximum amount of bytes that can be queued in @appsrc.
    /// After the maximum amount of bytes are queued, @appsrc will emit the
    /// "enough-data" signal.
    /// </summary>
    /// <param name="max">the maximum number of bytes to queue</param>
    public void SetMaxBytes(ulong max)
    {
        GstAppSrcSetMaxBytes(Handle, max);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the maximum amount of time that can be queued in @appsrc.
    /// After the maximum amount of time are queued, @appsrc will emit the
    /// "enough-data" signal.
    /// </summary>
    /// <param name="max">the maximum amonut of time to queue</param>
    public void SetMaxTime(Gst.ClockTime max)
    {
        GstAppSrcSetMaxTime(Handle, max.Nanoseconds);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the size of the stream in bytes. A value of -1 means that the size is
    /// not known.
    /// </summary>
    /// <param name="size">the size to set</param>
    public void SetSize(long size)
    {
        GstAppSrcSetSize(Handle, size);
        System.GC.KeepAlive(this);
    }

    /// <summary>
    /// Set the stream type on @appsrc. For seekable streams, the "seek" signal must
    /// be connected to.
    /// </summary>
    /// <remarks>
    /// <para>A stream_type stream</para>
    /// </remarks>
    /// <param name="type">the new state</param>
    public void SetStreamType(Gst.App.AppStreamType type)
    {
        GstAppSrcSetStreamType(Handle, (int)type);
        System.GC.KeepAlive(this);
    }

    /// <summary>The number of currently queued buffers inside appsrc.</summary>
    public ulong CurrentLevelBuffers => GetCurrentLevelBuffers();

    /// <summary>The number of currently queued bytes inside appsrc.</summary>
    public ulong CurrentLevelBytes => GetCurrentLevelBytes();

    /// <summary>The amount of currently queued time inside appsrc.</summary>
    public Gst.ClockTime CurrentLevelTime => GetCurrentLevelTime();

    /// <summary>
    /// The total duration in nanoseconds of the data stream. If the total duration is known, it
    /// is recommended to configure it with this property.
    /// </summary>
    public Gst.ClockTime Duration
    {
        get => GetDuration();
        set => SetDuration(value);
    }

    /// <summary>
    /// Make appsrc emit the "need-data", "enough-data" and "seek-data" signals.
    /// This option is by default enabled for backwards compatibility reasons but
    /// can disabled when needed because signal emission is expensive.
    /// </summary>
    public bool EmitSignals
    {
        get => GetEmitSignals();
        set => SetEmitSignals(value);
    }

    /// <summary>
    /// When set to any other value than GST_APP_LEAKY_TYPE_NONE then the appsrc
    /// will drop any buffers that are pushed into it once its internal queue is
    /// full. The selected type defines whether to drop the oldest or new
    /// buffers.
    /// </summary>
    public Gst.App.AppLeakyType LeakyType
    {
        get => GetLeakyType();
        set => SetLeakyType(value);
    }

    /// <summary>
    /// The maximum amount of buffers that can be queued internally.
    /// After the maximum amount of buffers are queued, appsrc will emit the
    /// "enough-data" signal.
    /// </summary>
    public ulong MaxBuffers
    {
        get => GetMaxBuffers();
        set => SetMaxBuffers(value);
    }

    /// <summary>
    /// The maximum amount of bytes that can be queued internally.
    /// After the maximum amount of bytes are queued, appsrc will emit the
    /// "enough-data" signal.
    /// </summary>
    public ulong MaxBytes
    {
        get => GetMaxBytes();
        set => SetMaxBytes(value);
    }

    /// <summary>
    /// The maximum amount of time that can be queued internally.
    /// After the maximum amount of time are queued, appsrc will emit the
    /// "enough-data" signal.
    /// </summary>
    public Gst.ClockTime MaxTime
    {
        get => GetMaxTime();
        set => SetMaxTime(value);
    }

    /// <summary>
    /// The total size in bytes of the data stream. If the total size is known, it
    /// is recommended to configure it with this property.
    /// </summary>
    public long Size
    {
        get => GetSize();
        set => SetSize(value);
    }

    /// <summary>
    /// The type of stream that this source is producing.  For seekable streams the
    /// application should connect to the seek-data signal.
    /// </summary>
    public Gst.App.AppStreamType StreamType
    {
        get => GetStreamType();
        set => SetStreamType(value);
    }

    /// <summary>
    /// When max-bytes are queued and after the enough-data signal has been emitted,
    /// block any further push-buffer calls until the amount of queued bytes drops
    /// below the max-bytes limit.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool Block
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("block");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("block");
            holder.SetBoolean(value);
            SetPropertyValue("block", in holder);
        }
    }

    /// <summary>Number of buffers that were dropped.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>Available since GStreamer 1.28.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public ulong Dropped
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("dropped");
            return holder.GetUInt64();
        }
    }

    /// <summary>
    /// The format to use for segment events. When the source is producing
    /// timestamped buffers this property should be set to GST_FORMAT_TIME.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public Gst.Format Format
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("format");
            return (Gst.Format)holder.GetEnum();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("format");
            holder.SetEnum((int)value);
            SetPropertyValue("format", in holder);
        }
    }

    /// <summary>
    /// When enabled, appsrc will check GstSegment in GstSample which was
    /// pushed via gst_app_src_push_sample() or "push-sample" signal action.
    /// If a GstSegment is changed, corresponding segment event will be followed
    /// by next data flow.
    /// </summary>
    /// <remarks>
    /// <para>
    /// FIXME: currently only GST_FORMAT_TIME format is supported and therefore
    /// GstAppSrc::format should be time. However, possibly #GstAppSrc can support
    /// other formats.
    /// </para>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool HandleSegmentChange
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("handle-segment-change");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("handle-segment-change");
            holder.SetBoolean(value);
            SetPropertyValue("handle-segment-change", in holder);
        }
    }

    /// <summary>Number of input buffers that were queued.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>Available since GStreamer 1.28.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public ulong In
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("in");
            return holder.GetUInt64();
        }
    }

    /// <summary>The <c>max-latency</c> property.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public long MaxLatency
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("max-latency");
            return holder.GetInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("max-latency");
            holder.SetInt64(value);
            SetPropertyValue("max-latency", in holder);
        }
    }

    /// <summary>
    /// The minimum latency of the source. A value of -1 will use the default
    /// latency calculations of #GstBaseSrc.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public long MinLatency
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("min-latency");
            return holder.GetInt64();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("min-latency");
            holder.SetInt64(value);
            SetPropertyValue("min-latency", in holder);
        }
    }

    /// <summary>
    /// Make appsrc emit the "need-data" signal when the amount of bytes in the
    /// queue drops below this percentage of max-bytes.
    /// </summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public uint MinPercent
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("min-percent");
            return holder.GetUInt();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("min-percent");
            holder.SetUInt(value);
            SetPropertyValue("min-percent", in holder);
        }
    }

    /// <summary>Number of output buffers that were dequeued.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>Available since GStreamer 1.28.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class.
    /// </exception>
    public ulong Out
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("out");
            return holder.GetUInt64();
        }
    }

    /// <summary>Don't emit notify for input, output and dropped buffers.</summary>
    /// <remarks>
    /// <para>
    /// This property has no C accessor; it is read and written through the GObject
    /// property system (<c>g_object_get_property</c> / <c>g_object_set_property</c>).
    /// </para>
    /// <para>Available since GStreamer 1.28.</para>
    /// </remarks>
    /// <exception cref="System.ObjectDisposedException">The wrapper was disposed.</exception>
    /// <exception cref="System.ArgumentException">
    /// The installed GStreamer declares no such property on this class, or
    /// declares it read-only.
    /// </exception>
    public bool Silent
    {
        get
        {
            using Gst.GObject.Value holder = GetProperty("silent");
            return holder.GetBoolean();
        }

        set
        {
            using Gst.GObject.Value holder = NewPropertyValue("silent");
            holder.SetBoolean(value);
            SetPropertyValue("silent", in holder);
        }
    }

    /// <summary>
    /// Signal that the source has enough data. It is recommended that the
    /// application stops calling push-buffer until the need-data signal is
    /// emitted again to avoid excessive buffer queueing.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler EnoughData
    {
        add => Gst.App.SignalConnections.Add(this, "enough-data", (nint)(delegate* unmanaged[Cdecl]<nint, nint, void>)&EnoughDataTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "enough-data", value);
    }

    /// <summary>The native handler of the <c>enough-data</c> signal of <c>GstAppSrc</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void EnoughDataTrampoline(nint instance, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler>(userData) is not { } handler)
            {
                return;
            }

            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                System.EventArgs.Empty);
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>need-data</c> signal of <c>GstAppSrc</c>.</summary>
    public sealed class NeedDataSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="NeedDataSignalArgs"/> class.</summary>
        /// <param name="length">the amount of bytes needed.</param>
        internal NeedDataSignalArgs(uint length)
        {
            Length = length;
        }

        /// <summary>the amount of bytes needed.</summary>
        public uint Length { get; }
    }

    /// <summary>
    /// Signal that the source needs more data. In the callback or from another
    /// thread you should call push-buffer or end-of-stream.
    /// </summary>
    /// <remarks>
    /// <para>
    /// @length is just a hint and when it is set to -1, any number of bytes can be
    /// pushed into @appsrc.
    /// </para>
    /// <para>
    /// You can call push-buffer multiple times until the enough-data signal is
    /// fired.
    /// </para>
    /// </remarks>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event System.EventHandler<Gst.App.AppSrc.NeedDataSignalArgs> NeedData
    {
        add => Gst.App.SignalConnections.Add(this, "need-data", (nint)(delegate* unmanaged[Cdecl]<nint, uint, nint, void>)&NeedDataTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "need-data", value);
    }

    /// <summary>The native handler of the <c>need-data</c> signal of <c>GstAppSrc</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static void NeedDataTrampoline(nint instance, uint length, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<System.EventHandler<Gst.App.AppSrc.NeedDataSignalArgs>>(userData) is not { } handler)
            {
                return;
            }

            uint lengthValue = length;
            handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.App.AppSrc.NeedDataSignalArgs(lengthValue));
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
        }
    }

    /// <summary>The arguments of the <c>seek-data</c> signal of <c>GstAppSrc</c>.</summary>
    public sealed class SeekDataSignalArgs : System.EventArgs
    {
        /// <summary>Initializes a new instance of the <see cref="SeekDataSignalArgs"/> class.</summary>
        /// <param name="offset">the offset to seek to</param>
        internal SeekDataSignalArgs(ulong offset)
        {
            Offset = offset;
        }

        /// <summary>the offset to seek to</summary>
        public ulong Offset { get; }
    }

    /// <summary>The handler of the <c>seek-data</c> signal of <c>GstAppSrc</c>.</summary>
    /// <param name="sender">The instance that emitted the signal.</param>
    /// <param name="args">The arguments of the signal.</param>
    /// <returns>%TRUE if the seek succeeded.</returns>
    public delegate bool SeekDataHandler(object? sender, Gst.App.AppSrc.SeekDataSignalArgs args);

    /// <summary>
    /// Seek to the given offset. The next push-buffer should produce buffers from
    /// the new @offset.
    /// This callback is only called for seekable stream types.
    /// </summary>
    /// <remarks>
    /// The handler is remembered on the wrapper it was added to and has to be
    /// removed from that same instance. Looking the object up again normally
    /// hands the same wrapper out, but one that was disposed in between is
    /// replaced by a new one, which knows nothing of the handler.
    /// </remarks>
    public event Gst.App.AppSrc.SeekDataHandler SeekData
    {
        add => Gst.App.SignalConnections.Add(this, "seek-data", (nint)(delegate* unmanaged[Cdecl]<nint, ulong, nint, int>)&SeekDataTrampoline, value);
        remove => Gst.App.SignalConnections.Remove(this, "seek-data", value);
    }

    /// <summary>The native handler of the <c>seek-data</c> signal of <c>GstAppSrc</c>.</summary>
    [UnmanagedCallersOnly(CallConvs = new[] { typeof(System.Runtime.CompilerServices.CallConvCdecl) })]
    private static int SeekDataTrampoline(nint instance, ulong offset, nint userData)
    {
        try
        {
            if (Gst.Interop.CallbackHandle.GetState<Gst.App.AppSrc.SeekDataHandler>(userData) is not { } handler)
            {
                return default;
            }

            ulong offsetValue = offset;
            bool result = handler(
                Gst.GObject.Object.FromNative(instance, Gst.Interop.Transfer.None),
                new Gst.App.AppSrc.SeekDataSignalArgs(offsetValue));
            return result ? 1 : 0;
        }
        catch (Exception exception)
        {
            Gst.Interop.ExceptionTrap.Report(exception);
            return default;
        }
    }

    /// <summary>The <c>gst_app_src_end_of_stream</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_end_of_stream")]
    private static partial int GstAppSrcEndOfStream(nint appsrc);

    /// <summary>The <c>gst_app_src_get_caps</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_caps")]
    private static partial nint GstAppSrcGetCaps(nint appsrc);

    /// <summary>The <c>gst_app_src_get_current_level_buffers</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_current_level_buffers")]
    private static partial ulong GstAppSrcGetCurrentLevelBuffers(nint appsrc);

    /// <summary>The <c>gst_app_src_get_current_level_bytes</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_current_level_bytes")]
    private static partial ulong GstAppSrcGetCurrentLevelBytes(nint appsrc);

    /// <summary>The <c>gst_app_src_get_current_level_time</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_current_level_time")]
    private static partial ulong GstAppSrcGetCurrentLevelTime(nint appsrc);

    /// <summary>The <c>gst_app_src_get_duration</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_duration")]
    private static partial ulong GstAppSrcGetDuration(nint appsrc);

    /// <summary>The <c>gst_app_src_get_emit_signals</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_emit_signals")]
    private static partial int GstAppSrcGetEmitSignals(nint appsrc);

    /// <summary>The <c>gst_app_src_get_latency</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_latency")]
    private static partial void GstAppSrcGetLatency(nint appsrc, ulong* min, ulong* max);

    /// <summary>The <c>gst_app_src_get_leaky_type</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_leaky_type")]
    private static partial int GstAppSrcGetLeakyType(nint appsrc);

    /// <summary>The <c>gst_app_src_get_max_buffers</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_max_buffers")]
    private static partial ulong GstAppSrcGetMaxBuffers(nint appsrc);

    /// <summary>The <c>gst_app_src_get_max_bytes</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_max_bytes")]
    private static partial ulong GstAppSrcGetMaxBytes(nint appsrc);

    /// <summary>The <c>gst_app_src_get_max_time</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_max_time")]
    private static partial ulong GstAppSrcGetMaxTime(nint appsrc);

    /// <summary>The <c>gst_app_src_get_size</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_size")]
    private static partial long GstAppSrcGetSize(nint appsrc);

    /// <summary>The <c>gst_app_src_get_stream_type</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_stream_type")]
    private static partial int GstAppSrcGetStreamType(nint appsrc);

    /// <summary>The <c>gst_app_src_push_buffer_list</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_push_buffer_list")]
    private static partial int GstAppSrcPushBufferList(nint appsrc, nint bufferList);

    /// <summary>The <c>gst_app_src_push_sample</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_push_sample")]
    private static partial int GstAppSrcPushSample(nint appsrc, nint sample);

    /// <summary>The <c>gst_app_src_set_caps</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_caps")]
    private static partial void GstAppSrcSetCaps(nint appsrc, nint caps);

    /// <summary>The <c>gst_app_src_set_duration</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_duration")]
    private static partial void GstAppSrcSetDuration(nint appsrc, ulong duration);

    /// <summary>The <c>gst_app_src_set_emit_signals</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_emit_signals")]
    private static partial void GstAppSrcSetEmitSignals(nint appsrc, int emit);

    /// <summary>The <c>gst_app_src_set_latency</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_latency")]
    private static partial void GstAppSrcSetLatency(nint appsrc, ulong min, ulong max);

    /// <summary>The <c>gst_app_src_set_leaky_type</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_leaky_type")]
    private static partial void GstAppSrcSetLeakyType(nint appsrc, int leaky);

    /// <summary>The <c>gst_app_src_set_max_buffers</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_max_buffers")]
    private static partial void GstAppSrcSetMaxBuffers(nint appsrc, ulong max);

    /// <summary>The <c>gst_app_src_set_max_bytes</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_max_bytes")]
    private static partial void GstAppSrcSetMaxBytes(nint appsrc, ulong max);

    /// <summary>The <c>gst_app_src_set_max_time</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_max_time")]
    private static partial void GstAppSrcSetMaxTime(nint appsrc, ulong max);

    /// <summary>The <c>gst_app_src_set_size</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_size")]
    private static partial void GstAppSrcSetSize(nint appsrc, long size);

    /// <summary>The <c>gst_app_src_set_stream_type</c> entry point.</summary>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_set_stream_type")]
    private static partial void GstAppSrcSetStreamType(nint appsrc, int type);

    /// <summary>Returns the <c>GType</c> that GObject registered <c>GstAppSrc</c> under.</summary>
    /// <returns>The type of the instances of this wrapper.</returns>
    [LibraryImport("GstApp", EntryPoint = "gst_app_src_get_type")]
    internal static new partial nuint GetGType();

    /// <summary>Creates the wrapper of a native instance, for the type registry.</summary>
    /// <param name="handle">The native instance.</param>
    /// <param name="transfer">How ownership of <paramref name="handle"/> is transferred.</param>
    /// <returns>The new wrapper.</returns>
    internal static new object CreateWrapper(nint handle, Gst.Interop.Transfer transfer) => new AppSrc(handle, transfer);
}
